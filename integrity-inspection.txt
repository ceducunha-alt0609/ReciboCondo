
===== async load @ 6447755 =====
:'Autônomo',document:'',category:'Jardineiro',phone:'',whatsapp:'',pix:'',notes:''},providerSearch:'',providerCategoryFilter:'',providerSortField:'name',providerSortDir:'asc',
  serviceForm:{providerId:'',category:'Jardineiro',reference:new Date().toISOString().slice(0,7),serviceDate:new Date().toISOString().slice(0,10),dueDate:new Date().toISOString().slice(0,10),amount:0,location:'',description:'',paymentDate:'',paymentMethod:'',paidValue:0},
  recurrenceForm:{id:null,providerId:'',category:'Jardineiro',amount:0,dueDay:5,paymentDay:5,paymentMethod:'PIX',location:'',description:'',active:true},
  serviceFilter:'todos',serviceCategoryFilter:'todas',serviceProviderFilter:'todos',serviceMonthFilter:'',paymentMonthFilter:new Date().toISOString().slice(0,7),paymentStatusFilter:'todos',paymentProviderFilter:'todos',receiptFilter:{q:'',month:'',category:'todas'},receiptServiceId:'',currentReceipt:null,dashboardYear:new Date().getFullYear(),payModal:false,paymentTarget:null,payment:{date:new Date().toISOString().slice(0,10),value:0,method:'PIX'},actionModal:{open:false,type:'',tone:'default',icon:'',target:null,service:null,title:'',subtitle:'',note:'',confirmLabel:'',details:[]},toasts:[],
  report:{from:new Date(new Date().getFullYear(),new Date().getMonth(),1).toISOString().slice(0,10),to:new Date().toISOString().slice(0,10),category:'todas',status:'todos'},passwords:{admin:'',viewer:''},
  async init(){document.documentElement.classList.toggle('dark',this.dark);window.alert=(msg)=>this.toast(String(msg||''),this.toastType(String(msg||'')));if('serviceWorker'in navigator){navigator.serviceWorker.register('./sw.js').catch(console.warn)}await this.seed();this.refreshIcons()},
  async seed(){let s=await db.settings.get('main');if(s)this.settings=s.value;await this.load();},
  async load(){this.providers=await db.providers.toArray();this.services=await db.services.toArray();this.receipts=await db.receipts.toArray();this.recurrences=await db.recurrences.toArray();this.logs=(await db.logs.orderBy('at').reverse().limit(30).toArray());let meta=await db.meta.get('receiptSeq');this.receiptSeqValue=Number(meta?.value||0);if(!this.receiptSeqInput)this.receiptSeqInput=this.nextReceiptNumber();this.after()},
  get isViewer(){return this.auth.role==='viewer'},get title(){return {'dashboard':'Dashboard inicial','prestadores':'Cadastro de prestadores','servicos':'Serviços','pagamentos':'Central de pagamentos','recibos':'Emissão de recibos','relatorios':'Relatórios','busca':'Busca global','config':'Configurações'}[this.view]||'ReciboCondo'},get selectedProvider(){return this.providers.find(p=>Number(p.id)===Number(this.selectedProviderId))||null},
  get cards(){return this.dashboardCards},
  get yearlyRows(){let y=String(this.dashboardYear||new Date().getFullYear());return this.services.filter(s=>String(s.paymentDate||s.serviceDate||'').startsWith(y))},
  get dashboardCards(){let nowRef=new Date().toISOString().slice(0,7), year=this.yearlyRows, month=year.filter(s=>String(s.paymentDate||s.serviceDate||'').slice(0,7)===nowRef), paidYear=year.reduce((a,s)=>a+Number(s.paidValue||0),0), paidMonth=month.reduce((a,s)=>a+Number(s.paidValue||0),0), countMonth=month.filter(s=>Number(s.paidValue||0)>0).length, open=year.reduce((a,s)=>a+this.balance(s),0), top=this.annualCategorySummary[0], topYear=this.topPaidNames(year), topMonth=this.topPaidNames(month), openTop=this.topOpenNames(year), catTop=this.annualCategorySummary.slice(0,3).map(c=>c.category+' • '+this.money(c.total)), activeCats=[...new Set(this.providers.map(p=>p.category).filter(Boolean))].slice(0,4), receiptsMonth=this.receipts.filter(r=>String(r.paymentDate||r.date||'').slice(0,7)===nowRef).length;return[
    {label:'Pago no ano',value:this.money(paidYear),hint:String(this.dashboardYear),icon:'wallet-cards',color:'text-rc-600',tooltipTitle:'Resumo anual',tooltipMain:this.money(paidYear),tooltipTag:'Ano '+this.dashboardYear,tooltip:['Total efetivamente pago no ano selecionado.', 'Top recebimentos: '+(topYear.length?topYear.join(' | '):'sem pagamentos lançados.'), 'Use para comparar o volume anual com o relatório anual premium.']},
    {label:'Pago no mês',value:this.money(paidMonth),hint:this.month(nowRef),icon:'circle-dollar-sign',color:'text-emerald-600',tooltipTitle:'Resumo do mês',tooltipMain:this.money(paidMonth),tooltipTag:'Mês atual',tooltip:['Mês de referência: '+this.month(nowRef)+'.', 'Quem recebeu: '+(topMonth.length?topMonth.join(' | '):'sem pagamentos neste mês.'), 'Ajuda a conferir rapidamente o fechamento mensal.']},
    {label:'Pagamentos no mês',value:countMonth,hint:'registros pagos',icon:'receipt-text',color:'text-blue-600',tooltipTitle:'Movimento mensal',tooltipMain:countMonth+' pagamento(s)',tooltipTag:'Recibos '+receiptsMonth,tooltip:['Valor médio por pagamento: '+this.money(countMonth?paidMonth/countMonth:0)+'.', 'Recibos emitidos no mês: '+receiptsMonth+'.', 'Bom para conferir se todos os pagamentos pagos já têm recibo.']},
    {label:'Prestadores ativos',value:this.providers.length,hint:'cadastrados',icon:'users',color:'text-rc-600',tooltipTitle:'Base de prestadores',tooltipMain:this.providers.length+' cadastro(s)',tooltipTag:'Cadastros',tooltip:['Categorias em uso: '+(activeCats.length?activeCats.join(', '):'nenhuma categoria cadastrada.'), 'Use para localizar quem presta serviço recorrente ou avulso.', 'Dica: mantenha PIX, WhatsApp e categoria sempre atualizados.']},
    {label:'Em aberto no ano',value:this.money(open),hint:'pendente/parcial',icon:'triangle-alert',color:'text-red-600',tooltipTitle:'Pendências do ano',tooltipMain:this.money(open),tooltipTag:'Atenção',tooltip:['Saldo lançado e ainda não quitado no ano.', 'Maiores pendências: '+(openTop.length?openTop.join(' | '):'nenhuma pendência no ano.'), 'Depois de pagar, registre o pagamento antes de emitir o recibo.']},
    {label:'Maior categoria',value:top?top.category:'-',hint:top?this.money(top.total):'sem dados',icon:'trophy',color:'text-yellow-600',tooltipTitle:'Categor

===== load(){ @ 6447761 =====
nomo',document:'',category:'Jardineiro',phone:'',whatsapp:'',pix:'',notes:''},providerSearch:'',providerCategoryFilter:'',providerSortField:'name',providerSortDir:'asc',
  serviceForm:{providerId:'',category:'Jardineiro',reference:new Date().toISOString().slice(0,7),serviceDate:new Date().toISOString().slice(0,10),dueDate:new Date().toISOString().slice(0,10),amount:0,location:'',description:'',paymentDate:'',paymentMethod:'',paidValue:0},
  recurrenceForm:{id:null,providerId:'',category:'Jardineiro',amount:0,dueDay:5,paymentDay:5,paymentMethod:'PIX',location:'',description:'',active:true},
  serviceFilter:'todos',serviceCategoryFilter:'todas',serviceProviderFilter:'todos',serviceMonthFilter:'',paymentMonthFilter:new Date().toISOString().slice(0,7),paymentStatusFilter:'todos',paymentProviderFilter:'todos',receiptFilter:{q:'',month:'',category:'todas'},receiptServiceId:'',currentReceipt:null,dashboardYear:new Date().getFullYear(),payModal:false,paymentTarget:null,payment:{date:new Date().toISOString().slice(0,10),value:0,method:'PIX'},actionModal:{open:false,type:'',tone:'default',icon:'',target:null,service:null,title:'',subtitle:'',note:'',confirmLabel:'',details:[]},toasts:[],
  report:{from:new Date(new Date().getFullYear(),new Date().getMonth(),1).toISOString().slice(0,10),to:new Date().toISOString().slice(0,10),category:'todas',status:'todos'},passwords:{admin:'',viewer:''},
  async init(){document.documentElement.classList.toggle('dark',this.dark);window.alert=(msg)=>this.toast(String(msg||''),this.toastType(String(msg||'')));if('serviceWorker'in navigator){navigator.serviceWorker.register('./sw.js').catch(console.warn)}await this.seed();this.refreshIcons()},
  async seed(){let s=await db.settings.get('main');if(s)this.settings=s.value;await this.load();},
  async load(){this.providers=await db.providers.toArray();this.services=await db.services.toArray();this.receipts=await db.receipts.toArray();this.recurrences=await db.recurrences.toArray();this.logs=(await db.logs.orderBy('at').reverse().limit(30).toArray());let meta=await db.meta.get('receiptSeq');this.receiptSeqValue=Number(meta?.value||0);if(!this.receiptSeqInput)this.receiptSeqInput=this.nextReceiptNumber();this.after()},
  get isViewer(){return this.auth.role==='viewer'},get title(){return {'dashboard':'Dashboard inicial','prestadores':'Cadastro de prestadores','servicos':'Serviços','pagamentos':'Central de pagamentos','recibos':'Emissão de recibos','relatorios':'Relatórios','busca':'Busca global','config':'Configurações'}[this.view]||'ReciboCondo'},get selectedProvider(){return this.providers.find(p=>Number(p.id)===Number(this.selectedProviderId))||null},
  get cards(){return this.dashboardCards},
  get yearlyRows(){let y=String(this.dashboardYear||new Date().getFullYear());return this.services.filter(s=>String(s.paymentDate||s.serviceDate||'').startsWith(y))},
  get dashboardCards(){let nowRef=new Date().toISOString().slice(0,7), year=this.yearlyRows, month=year.filter(s=>String(s.paymentDate||s.serviceDate||'').slice(0,7)===nowRef), paidYear=year.reduce((a,s)=>a+Number(s.paidValue||0),0), paidMonth=month.reduce((a,s)=>a+Number(s.paidValue||0),0), countMonth=month.filter(s=>Number(s.paidValue||0)>0).length, open=year.reduce((a,s)=>a+this.balance(s),0), top=this.annualCategorySummary[0], topYear=this.topPaidNames(year), topMonth=this.topPaidNames(month), openTop=this.topOpenNames(year), catTop=this.annualCategorySummary.slice(0,3).map(c=>c.category+' • '+this.money(c.total)), activeCats=[...new Set(this.providers.map(p=>p.category).filter(Boolean))].slice(0,4), receiptsMonth=this.receipts.filter(r=>String(r.paymentDate||r.date||'').slice(0,7)===nowRef).length;return[
    {label:'Pago no ano',value:this.money(paidYear),hint:String(this.dashboardYear),icon:'wallet-cards',color:'text-rc-600',tooltipTitle:'Resumo anual',tooltipMain:this.money(paidYear),tooltipTag:'Ano '+this.dashboardYear,tooltip:['Total efetivamente pago no ano selecionado.', 'Top recebimentos: '+(topYear.length?topYear.join(' | '):'sem pagamentos lançados.'), 'Use para comparar o volume anual com o relatório anual premium.']},
    {label:'Pago no mês',value:this.money(paidMonth),hint:this.month(nowRef),icon:'circle-dollar-sign',color:'text-emerald-600',tooltipTitle:'Resumo do mês',tooltipMain:this.money(paidMonth),tooltipTag:'Mês atual',tooltip:['Mês de referência: '+this.month(nowRef)+'.', 'Quem recebeu: '+(topMonth.length?topMonth.join(' | '):'sem pagamentos neste mês.'), 'Ajuda a conferir rapidamente o fechamento mensal.']},
    {label:'Pagamentos no mês',value:countMonth,hint:'registros pagos',icon:'receipt-text',color:'text-blue-600',tooltipTitle:'Movimento mensal',tooltipMain:countMonth+' pagamento(s)',tooltipTag:'Recibos '+receiptsMonth,tooltip:['Valor médio por pagamento: '+this.money(countMonth?paidMonth/countMonth:0)+'.', 'Recibos emitidos no mês: '+receiptsMonth+'.', 'Bom para conferir se todos os pagamentos pagos já têm recibo.']},
    {label:'Prestadores ativos',value:this.providers.length,hint:'cadastrados',icon:'users',color:'text-rc-600',tooltipTitle:'Base de prestadores',tooltipMain:this.providers.length+' cadastro(s)',tooltipTag:'Cadastros',tooltip:['Categorias em uso: '+(activeCats.length?activeCats.join(', '):'nenhuma categoria cadastrada.'), 'Use para localizar quem presta serviço recorrente ou avulso.', 'Dica: mantenha PIX, WhatsApp e categoria sempre atualizados.']},
    {label:'Em aberto no ano',value:this.money(open),hint:'pendente/parcial',icon:'triangle-alert',color:'text-red-600',tooltipTitle:'Pendências do ano',tooltipMain:this.money(open),tooltipTag:'Atenção',tooltip:['Saldo lançado e ainda não quitado no ano.', 'Maiores pendências: '+(openTop.length?openTop.join(' | '):'nenhuma pendência no ano.'), 'Depois de pagar, registre o pagamento antes de emitir o recibo.']},
    {label:'Maior categoria',value:top?top.category:'-',hint:top?this.money(top.total):'sem dados',icon:'trophy',color:'text-yellow-600',tooltipTitle:'Categoria líd

===== backupHealthLabel @ 6460934 =====
||'').localeCompare(String(b.serviceDate||''))||Number(a.id||0)-Number(b.id||0)})},
  get reportDetailRows(){let rows=[];for(const s of this.reportRows){let rs=this.receipts.filter(r=>Number(r.serviceId)===Number(s.id)).sort((a,b)=>(Number(String(a.number||'').replace(/\D/g,''))||0)-(Number(String(b.number||'').replace(/\D/g,''))||0)),sum=0;for(const r of rs){let v=Number(r.total||0);sum+=v;rows.push({...s,reportKey:'r-'+r.id,receiptNumber:r.number||'-',paymentDate:r.paymentDate||r.date||s.paymentDate,description:r.description||s.description,reportValue:v,paymentMethod:r.paymentMethod||s.paymentMethod||''})}let u=Math.max(0,Number(s.paidValue||0)-sum);if(u>0)rows.push({...s,reportKey:'u-'+s.id,receiptNumber:'PENDENTE',paymentDate:s.paymentDate,description:s.description,reportValue:u,paymentMethod:s.paymentMethod||''});if(!rs.length&&u<=0)rows.push({...s,reportKey:'s-'+s.id,receiptNumber:'-',description:s.description,reportValue:Number(s.paidValue||s.amount||0),paymentMethod:s.paymentMethod||''})}return rows},get reportTotals(){let e=this.reportRows.reduce((a,s)=>a+Number(s.amount||0),0),p=this.reportRows.reduce((a,s)=>a+Number(s.paidValue||0),0);return{expected:e,paid:p,open:e-p}},backupStats(){let records=this.providers.length+this.services.length+this.receipts.length+this.recurrences.length;return{records,providers:this.providers.length,services:this.services.length,receipts:this.receipts.length,recurrences:this.recurrences.length}},backupDays(){if(!this.backupLast)return null;let d=new Date(this.backupLast), now=new Date();return Math.max(0,Math.floor((now-d)/86400000))},backupLastLabel(){if(!this.backupLast)return'Nunca feito';let days=this.backupDays();let date=new Date(this.backupLast).toLocaleString('pt-BR');return days===0?'Hoje • '+date:days+' dia(s) • '+date},backupHealthLabel(){let d=this.backupDays();if(d===null)return'Backup pendente';if(d<=7)return'Protegido';if(d<=15)return'Atenção';return'Backup atrasado'},backupHealthClass(){let d=this.backupDays();if(d===null||d>15)return'bg-red-500/10 text-red-700';if(d>7)return'bg-amber-500/10 text-amber-700';return'bg-emerald-500/10 text-emerald-700'},backupHint(){let s=this.backupStats();let d=this.backupDays();if(d===null)return'Você ainda não exportou um backup. Recomendo gerar um JSON antes de continuar cadastrando dados importantes.';if(d>15)return'Seu último backup já passou de 15 dias. Exporte um novo JSON para proteger '+s.records+' registros locais.';if(d>7)return'Backup existente, mas já vale gerar uma cópia atualizada com os últimos lançamentos e recibos.';return'Backup em dia. Mesmo assim, exporte uma nova cópia sempre antes de grandes alterações.'},
  get topProviders(){let y=new Date().getFullYear(), map={};this.services.filter(s=>String(s.serviceDate||'').startsWith(String(y))).forEach(s=>{let id=s.providerId,p=this.providers.find(x=>Number(x.id)===Number(id))||{};if(!map[id])map[id]={id,name:p.name||'-',category:p.category||s.category,count:0,total:0,open:0};map[id].count++;map[id].total+=Number(s.paidValue||s.amount||0);map[id].open+=this.balance(s)});return Object.values(map).sort((a,b)=>b.total-a.total).slice(0,8)},
  get categorySummary(){let total=this.services.reduce((a,s)=>a+Number(s.paidValue||s.amount||0),0)||1;return this.categories.map(category=>{let val=this.services.filter(s=>s.category===category).reduce((a,s)=>a+Number(s.paidValue||s.amount||0),0);return{category,total:val,percent:Math.min(100,Math.round(val/total*100))}}).filter(x=>x.total>0).sort((a,b)=>b.total-a.total)},
  get criticalOpen(){return this.openServices.slice().sort((a,b)=>new Date(a.dueDate||'2999-12-31')-new Date(b.dueDate||'2999-12-31')).slice(0,8)},
  get notifications(){const now=this.today();let pend=this.services.filter(s=>s.status!=='Pago'&&s.dueDate<=now).slice(0,3).map(s=>({id:s.id,title:'Pagamento pendente',body:this.providerName(s.providerId)+' • '+s.category+' • '+this.money(this.balance(s))}));let rec=this.recurrences.filter(r=>r.active&&!this.recurrenceGenerated(r)).slice(0,2).map(r=>({id:'r'+r.id,title:'Recorrência do mês pendente',body:this.providerName(r.providerId)+' • '+r.category+' • '+this.money(r.amount)}));return[...rec,...pend].slice(0,4)},
  get dayOverdue(){let t=this.today();return this.services.filter(s=>s.status!=='Pago'&&s.dueDate&&s.dueDate<t).sort((a,b)=>String(a.dueDate).localeCompare(String(b.dueDate)))},
  get dayDueToday(){let t=this.today();return this.services.filter(s=>s.status!=='Pago'&&s.dueDate===t).sort((a,b)=>this.balance(b)-this.balance(a))},
  get dayNextDue(){let t=new Date(this.today()+'T00:00:00'),limit=new Date(t);limit.setDate(t.getDate()+7);let a=this.today(),b=limit.toISOString().slice(0,10);return this.services.filter(s=>s.status!=='Pago'&&s.dueDate&&s.dueDate>a&&s.dueDate<=b).sort((x,y)=>String(x.dueDate).localeCompare(String(y.dueDate)))},
  get dayPendingRecurrences(){return this.recurrences.filter(r=>r.active&&!this.recurrenceGenerated(r))},
  get dayCenterRows(){let rows=[];this.dayPendingRecurrences.forEach(r=>rows.push({key:'r'+r.id,kind:'recurrence',recurrence:r,title:this.providerName(r.providerId),subtitle:'Recorrência pendente • '+r.category+' • '+this.month(this.today().slice(0,7)),badge:'Gerar mês',value:this.money(r.amount||0),tone:'success'}));this.dayOverdue.forEach(s=>rows.push({key:'v'+s.id,kind:'service',service:s,title:this.providerName(s.providerId),subtitle:s.category+' • venceu '+this.fmt(s.dueDate)+' • '+this.overdueLabel(s),badge:'Vencido',value:this.money(this.balance(s)),tone:'danger'}));this.dayDueToday.forEach(s=>rows.push({key:'h'+s.id,kind:'service',service:s,title:this.providerName(s.providerId),subtitle:s.category+' • vence hoje',badge:'Hoje',value:this.money(this.balance(s)),tone:'warning'}));this.dayNextDue.forEach(s=>rows.push({key:'p'+s.id,kind:'service',service:s,title:this.providerName(s.providerId),subtitle:s.category+' • vence '+this.fmt(s.dueDate)+' • '+this.overdueLabel(s),badge:'Próximo',value:this.money(this.balance(s)),tone:'success'}));r

===== backupHint @ 6461277 =====
for(const r of rs){let v=Number(r.total||0);sum+=v;rows.push({...s,reportKey:'r-'+r.id,receiptNumber:r.number||'-',paymentDate:r.paymentDate||r.date||s.paymentDate,description:r.description||s.description,reportValue:v,paymentMethod:r.paymentMethod||s.paymentMethod||''})}let u=Math.max(0,Number(s.paidValue||0)-sum);if(u>0)rows.push({...s,reportKey:'u-'+s.id,receiptNumber:'PENDENTE',paymentDate:s.paymentDate,description:s.description,reportValue:u,paymentMethod:s.paymentMethod||''});if(!rs.length&&u<=0)rows.push({...s,reportKey:'s-'+s.id,receiptNumber:'-',description:s.description,reportValue:Number(s.paidValue||s.amount||0),paymentMethod:s.paymentMethod||''})}return rows},get reportTotals(){let e=this.reportRows.reduce((a,s)=>a+Number(s.amount||0),0),p=this.reportRows.reduce((a,s)=>a+Number(s.paidValue||0),0);return{expected:e,paid:p,open:e-p}},backupStats(){let records=this.providers.length+this.services.length+this.receipts.length+this.recurrences.length;return{records,providers:this.providers.length,services:this.services.length,receipts:this.receipts.length,recurrences:this.recurrences.length}},backupDays(){if(!this.backupLast)return null;let d=new Date(this.backupLast), now=new Date();return Math.max(0,Math.floor((now-d)/86400000))},backupLastLabel(){if(!this.backupLast)return'Nunca feito';let days=this.backupDays();let date=new Date(this.backupLast).toLocaleString('pt-BR');return days===0?'Hoje • '+date:days+' dia(s) • '+date},backupHealthLabel(){let d=this.backupDays();if(d===null)return'Backup pendente';if(d<=7)return'Protegido';if(d<=15)return'Atenção';return'Backup atrasado'},backupHealthClass(){let d=this.backupDays();if(d===null||d>15)return'bg-red-500/10 text-red-700';if(d>7)return'bg-amber-500/10 text-amber-700';return'bg-emerald-500/10 text-emerald-700'},backupHint(){let s=this.backupStats();let d=this.backupDays();if(d===null)return'Você ainda não exportou um backup. Recomendo gerar um JSON antes de continuar cadastrando dados importantes.';if(d>15)return'Seu último backup já passou de 15 dias. Exporte um novo JSON para proteger '+s.records+' registros locais.';if(d>7)return'Backup existente, mas já vale gerar uma cópia atualizada com os últimos lançamentos e recibos.';return'Backup em dia. Mesmo assim, exporte uma nova cópia sempre antes de grandes alterações.'},
  get topProviders(){let y=new Date().getFullYear(), map={};this.services.filter(s=>String(s.serviceDate||'').startsWith(String(y))).forEach(s=>{let id=s.providerId,p=this.providers.find(x=>Number(x.id)===Number(id))||{};if(!map[id])map[id]={id,name:p.name||'-',category:p.category||s.category,count:0,total:0,open:0};map[id].count++;map[id].total+=Number(s.paidValue||s.amount||0);map[id].open+=this.balance(s)});return Object.values(map).sort((a,b)=>b.total-a.total).slice(0,8)},
  get categorySummary(){let total=this.services.reduce((a,s)=>a+Number(s.paidValue||s.amount||0),0)||1;return this.categories.map(category=>{let val=this.services.filter(s=>s.category===category).reduce((a,s)=>a+Number(s.paidValue||s.amount||0),0);return{category,total:val,percent:Math.min(100,Math.round(val/total*100))}}).filter(x=>x.total>0).sort((a,b)=>b.total-a.total)},
  get criticalOpen(){return this.openServices.slice().sort((a,b)=>new Date(a.dueDate||'2999-12-31')-new Date(b.dueDate||'2999-12-31')).slice(0,8)},
  get notifications(){const now=this.today();let pend=this.services.filter(s=>s.status!=='Pago'&&s.dueDate<=now).slice(0,3).map(s=>({id:s.id,title:'Pagamento pendente',body:this.providerName(s.providerId)+' • '+s.category+' • '+this.money(this.balance(s))}));let rec=this.recurrences.filter(r=>r.active&&!this.recurrenceGenerated(r)).slice(0,2).map(r=>({id:'r'+r.id,title:'Recorrência do mês pendente',body:this.providerName(r.providerId)+' • '+r.category+' • '+this.money(r.amount)}));return[...rec,...pend].slice(0,4)},
  get dayOverdue(){let t=this.today();return this.services.filter(s=>s.status!=='Pago'&&s.dueDate&&s.dueDate<t).sort((a,b)=>String(a.dueDate).localeCompare(String(b.dueDate)))},
  get dayDueToday(){let t=this.today();return this.services.filter(s=>s.status!=='Pago'&&s.dueDate===t).sort((a,b)=>this.balance(b)-this.balance(a))},
  get dayNextDue(){let t=new Date(this.today()+'T00:00:00'),limit=new Date(t);limit.setDate(t.getDate()+7);let a=this.today(),b=limit.toISOString().slice(0,10);return this.services.filter(s=>s.status!=='Pago'&&s.dueDate&&s.dueDate>a&&s.dueDate<=b).sort((x,y)=>String(x.dueDate).localeCompare(String(y.dueDate)))},
  get dayPendingRecurrences(){return this.recurrences.filter(r=>r.active&&!this.recurrenceGenerated(r))},
  get dayCenterRows(){let rows=[];this.dayPendingRecurrences.forEach(r=>rows.push({key:'r'+r.id,kind:'recurrence',recurrence:r,title:this.providerName(r.providerId),subtitle:'Recorrência pendente • '+r.category+' • '+this.month(this.today().slice(0,7)),badge:'Gerar mês',value:this.money(r.amount||0),tone:'success'}));this.dayOverdue.forEach(s=>rows.push({key:'v'+s.id,kind:'service',service:s,title:this.providerName(s.providerId),subtitle:s.category+' • venceu '+this.fmt(s.dueDate)+' • '+this.overdueLabel(s),badge:'Vencido',value:this.money(this.balance(s)),tone:'danger'}));this.dayDueToday.forEach(s=>rows.push({key:'h'+s.id,kind:'service',service:s,title:this.providerName(s.providerId),subtitle:s.category+' • vence hoje',badge:'Hoje',value:this.money(this.balance(s)),tone:'warning'}));this.dayNextDue.forEach(s=>rows.push({key:'p'+s.id,kind:'service',service:s,title:this.providerName(s.providerId),subtitle:s.category+' • vence '+this.fmt(s.dueDate)+' • '+this.overdueLabel(s),badge:'Próximo',value:this.money(this.balance(s)),tone:'success'}));return rows},
  get dayCenterSummary(){let v=this.dayOverdue.length,h=this.dayDueToday.length,p=this.dayNextDue.length,r=this.dayPendingRecurrences.length,total=this.dayOverdue.concat(this.dayDueToday,this.dayNextDue).reduce((a,s)=>a+this.balance(s),0);if(!v&&!h&&!p&&!r)return 'Nenhuma ação urgente no momento. Sem vencimentos críticos ou reco

===== nextReceipt @ 2211219 =====
font-bold">Responsável</span><input x-model="settings.manager" class="input mt-1"></label>
            </div>
            <div class="grid lg:grid-cols-2 gap-3">
              <label><span class="text-sm font-bold">CNPJ</span><input x-model="settings.cnpj" class="input mt-1"></label>
              <label><span class="text-sm font-bold">Logo</span><input @change="readLogo" type="file" accept="image/*" class="input mt-1"></label>
            </div>
            <label><span class="text-sm font-bold">Endereço</span><input x-model="settings.address" class="input mt-1"></label>
            <button class="primary">Salvar configurações</button>
          </form>

          <div class="glass rounded-3xl p-4 space-y-4">
            <div class="flex items-start justify-between gap-3">
              <div class="flex items-start gap-3">
                <div class="h-11 w-11 rounded-2xl bg-gradient-to-br from-rc-800 to-rc-gold text-white grid place-items-center shadow-soft"><i data-lucide="list-ordered" class="w-5 h-5"></i></div>
                <div>
                  <h3 class="font-black text-lg">Numeração dos recibos</h3>
                  <p class="text-xs text-slate-500 dark:text-slate-400">Conferência da sequência oficial. O número só é consumido quando um recibo é emitido.</p>
                </div>
              </div>
              <span class="badge" x-text="receiptSequenceStatus()"></span>
            </div>
            <div class="grid md:grid-cols-4 gap-3">
              <div class="rounded-3xl border border-slate-200/70 dark:border-slate-700/70 bg-white/70 dark:bg-slate-950/35 p-4">
                <p class="text-xs uppercase tracking-[.14em] text-slate-500 font-black">Próximo recibo</p>
                <p class="text-2xl font-black receipt-number mt-1" x-text="String(nextReceiptNumber()).padStart(6,'0')"></p>
              </div>
              <div class="rounded-3xl border border-slate-200/70 dark:border-slate-700/70 bg-white/70 dark:bg-slate-950/35 p-4">
                <p class="text-xs uppercase tracking-[.14em] text-slate-500 font-black">Último emitido</p>
                <p class="text-2xl font-black mt-1" x-text="String(receiptMaxNumber()).padStart(6,'0')"></p>
              </div>
              <div class="rounded-3xl border border-slate-200/70 dark:border-slate-700/70 bg-white/70 dark:bg-slate-950/35 p-4">
                <p class="text-xs uppercase tracking-[.14em] text-slate-500 font-black">Recibos</p>
                <p class="text-2xl font-black mt-1" x-text="receipts.length"></p>
              </div>
              <div class="rounded-3xl border border-slate-200/70 dark:border-slate-700/70 bg-white/70 dark:bg-slate-950/35 p-4">
                <p class="text-xs uppercase tracking-[.14em] text-slate-500 font-black">Sequência interna</p>
                <p class="text-2xl font-black mt-1" x-text="String(Number(receiptSeqValue||0)).padStart(6,'0')"></p>
              </div>
            </div>
            <div class="rounded-2xl border border-amber-500/25 bg-amber-500/10 p-3 space-y-3">
              <div class="flex items-start gap-3">
                <i data-lucide="shield-alert" class="w-5 h-5 text-amber-700 mt-1"></i>
                <div>
                  <p class="text-sm font-black text-amber-800 dark:text-amber-300">Ajuste manual protegido</p>
                  <p class="text-xs text-slate-500 dark:text-slate-400">Use apenas se estiver migrando recibos antigos ou alinhando a sequência com documentos já arquivados. O app não permite definir número menor ou igual ao maior recibo já emitido.</p>
                </div>
              </div>
              <div class="grid sm:grid-cols-[1fr_auto_auto] gap-2 items-end">
                <label><span class="text-sm font-bold">Definir próximo número</span><input x-model.number="receiptSeqInput" type="number" min="1" class="input mt-1" placeholder="Ex.: 25"></label>
                <button @click="receiptSeqInput=nextReceiptNumber()" class="secondary" :disabled="isViewer">Usar sugerido</button>
                <button @click="saveReceiptSequence" class="primary" :disabled="isViewer">Salvar sequência</button>
              </div>
            </div>
          </div>

          <div class="glass rounded-3xl p-4 space-y-4">
            <div class="flex items-start gap-3">
              <div class="h-11 w-11 rounded-2xl bg-gradient-to-br from-slate-800 to-red-700 text-white grid place-items-center shadow-soft"><i data-lucide="key-round" class="w-5 h-5"></i></div>
              <div>
                <h3 class="font-black text-lg">Senhas e acesso</h3>
                <p class="text-xs text-slate-500 dark:text-slate-400">Área sensível para alterar senhas e apagar dados locais.</p>
              </div>
            </div>
            <div class="grid sm:grid-cols-2 gap-3">
              <input x-model="passwords.admin" type="password" class="input" placeholder="Nova senha admin">
              <input x-model="passwords.viewer" type="password" class="input" placeholder="Nova senha viewer">
            </div>
            <button @click="savePasswords" class="primary w-full" :disabled="isViewer">Atualizar senhas</button>
          </div>

          <div class="glass rounded-3xl p-4 space-y-4">
            <div class="flex items-start justify-between gap-3">
              <div class="flex items-start gap-3">
                <div class="h-11 w-11 rounded-2xl bg-gradient-to-br from-rc-800 to-rc-gold text-white grid place-items-center shadow-soft"><i data-lucide="history" class="w-5 h-5"></i></div>
                <div>
                  <h3 class="font-black text-lg">Log de atividades</h3>
                  <p class="text-xs text-slate-500 dark:text-slate-400">Registro local das principais ações: pagamentos, recibos, recorrências, backup e alterações sensíveis.</p>
                </div>
              </div>
              <span class="badge"><b x-text="logs.length"></b> eventos</span>
            </div>
            <div class="space-y-2 max-h-[3

===== nextReceipt @ 2213363 =====
k mt-1" x-text="String(receiptMaxNumber()).padStart(6,'0')"></p>
              </div>
              <div class="rounded-3xl border border-slate-200/70 dark:border-slate-700/70 bg-white/70 dark:bg-slate-950/35 p-4">
                <p class="text-xs uppercase tracking-[.14em] text-slate-500 font-black">Recibos</p>
                <p class="text-2xl font-black mt-1" x-text="receipts.length"></p>
              </div>
              <div class="rounded-3xl border border-slate-200/70 dark:border-slate-700/70 bg-white/70 dark:bg-slate-950/35 p-4">
                <p class="text-xs uppercase tracking-[.14em] text-slate-500 font-black">Sequência interna</p>
                <p class="text-2xl font-black mt-1" x-text="String(Number(receiptSeqValue||0)).padStart(6,'0')"></p>
              </div>
            </div>
            <div class="rounded-2xl border border-amber-500/25 bg-amber-500/10 p-3 space-y-3">
              <div class="flex items-start gap-3">
                <i data-lucide="shield-alert" class="w-5 h-5 text-amber-700 mt-1"></i>
                <div>
                  <p class="text-sm font-black text-amber-800 dark:text-amber-300">Ajuste manual protegido</p>
                  <p class="text-xs text-slate-500 dark:text-slate-400">Use apenas se estiver migrando recibos antigos ou alinhando a sequência com documentos já arquivados. O app não permite definir número menor ou igual ao maior recibo já emitido.</p>
                </div>
              </div>
              <div class="grid sm:grid-cols-[1fr_auto_auto] gap-2 items-end">
                <label><span class="text-sm font-bold">Definir próximo número</span><input x-model.number="receiptSeqInput" type="number" min="1" class="input mt-1" placeholder="Ex.: 25"></label>
                <button @click="receiptSeqInput=nextReceiptNumber()" class="secondary" :disabled="isViewer">Usar sugerido</button>
                <button @click="saveReceiptSequence" class="primary" :disabled="isViewer">Salvar sequência</button>
              </div>
            </div>
          </div>

          <div class="glass rounded-3xl p-4 space-y-4">
            <div class="flex items-start gap-3">
              <div class="h-11 w-11 rounded-2xl bg-gradient-to-br from-slate-800 to-red-700 text-white grid place-items-center shadow-soft"><i data-lucide="key-round" class="w-5 h-5"></i></div>
              <div>
                <h3 class="font-black text-lg">Senhas e acesso</h3>
                <p class="text-xs text-slate-500 dark:text-slate-400">Área sensível para alterar senhas e apagar dados locais.</p>
              </div>
            </div>
            <div class="grid sm:grid-cols-2 gap-3">
              <input x-model="passwords.admin" type="password" class="input" placeholder="Nova senha admin">
              <input x-model="passwords.viewer" type="password" class="input" placeholder="Nova senha viewer">
            </div>
            <button @click="savePasswords" class="primary w-full" :disabled="isViewer">Atualizar senhas</button>
          </div>

          <div class="glass rounded-3xl p-4 space-y-4">
            <div class="flex items-start justify-between gap-3">
              <div class="flex items-start gap-3">
                <div class="h-11 w-11 rounded-2xl bg-gradient-to-br from-rc-800 to-rc-gold text-white grid place-items-center shadow-soft"><i data-lucide="history" class="w-5 h-5"></i></div>
                <div>
                  <h3 class="font-black text-lg">Log de atividades</h3>
                  <p class="text-xs text-slate-500 dark:text-slate-400">Registro local das principais ações: pagamentos, recibos, recorrências, backup e alterações sensíveis.</p>
                </div>
              </div>
              <span class="badge"><b x-text="logs.length"></b> eventos</span>
            </div>
            <div class="space-y-2 max-h-[360px] overflow-auto scrollbar pr-1">
              <template x-for="l in logs" :key="l.id">
                <div class="rounded-2xl border border-slate-200/70 dark:border-slate-700/70 bg-white/70 dark:bg-slate-950/35 p-3 flex gap-3">
                  <div class="h-9 w-9 rounded-xl bg-rc-800/10 text-rc-800 dark:text-rc-gold grid place-items-center shrink-0"><i :data-lucide="logIcon(l.type)" class="w-4 h-4"></i></div>
                  <div class="min-w-0 flex-1">
                    <div class="flex flex-wrap items-center justify-between gap-2"><p class="font-black text-sm" x-text="l.title"></p><p class="text-[11px] text-slate-500" x-text="fmtDateTime(l.at)"></p></div>
                    <p class="text-xs text-slate-500 dark:text-slate-400 mt-1" x-text="l.detail"></p>
                    <p class="text-[11px] text-slate-400 mt-1">Perfil: <span x-text="l.role || auth.role"></span></p>
                  </div>
                </div>
              </template>
              <p x-show="!logs.length" class="text-sm text-slate-500">Nenhuma atividade registrada ainda.</p>
            </div>
            <button @click="clearLogs" class="secondary w-full" :disabled="isViewer || !logs.length">Limpar log</button>
          </div>
        </section>
      </main>

      <div x-show="payModal" class="no-print fixed inset-0 z-50 grid place-items-center bg-slate-950/55 p-4">
        <div class="glass rounded-3xl p-5 w-full max-w-2xl space-y-4">
          <div class="flex items-start justify-between gap-3">
            <div>
              <p class="text-xs uppercase tracking-[.18em] text-rc-gold font-black">Janela flutuante</p>
              <h3 class="font-black text-xl">Registrar pagamento</h3>
              <p class="text-sm text-slate-500" x-text="paymentTarget ? providerName(paymentTarget.providerId)+' • '+paymentTarget.category : ''"></p>
            </div>
            <button @click="payModal=false" class="secondary px-3 py-2">Fechar</button>
          </div>
          <div class="grid sm:grid-cols-3 gap-3" x-show="paymentTarget">
            <div class="rounded-2xl border border-slate-200/70 dark:border-slate-700/7

===== nextReceipt @ 6448150 =====
tion:'',paymentDate:'',paymentMethod:'',paidValue:0},
  recurrenceForm:{id:null,providerId:'',category:'Jardineiro',amount:0,dueDay:5,paymentDay:5,paymentMethod:'PIX',location:'',description:'',active:true},
  serviceFilter:'todos',serviceCategoryFilter:'todas',serviceProviderFilter:'todos',serviceMonthFilter:'',paymentMonthFilter:new Date().toISOString().slice(0,7),paymentStatusFilter:'todos',paymentProviderFilter:'todos',receiptFilter:{q:'',month:'',category:'todas'},receiptServiceId:'',currentReceipt:null,dashboardYear:new Date().getFullYear(),payModal:false,paymentTarget:null,payment:{date:new Date().toISOString().slice(0,10),value:0,method:'PIX'},actionModal:{open:false,type:'',tone:'default',icon:'',target:null,service:null,title:'',subtitle:'',note:'',confirmLabel:'',details:[]},toasts:[],
  report:{from:new Date(new Date().getFullYear(),new Date().getMonth(),1).toISOString().slice(0,10),to:new Date().toISOString().slice(0,10),category:'todas',status:'todos'},passwords:{admin:'',viewer:''},
  async init(){document.documentElement.classList.toggle('dark',this.dark);window.alert=(msg)=>this.toast(String(msg||''),this.toastType(String(msg||'')));if('serviceWorker'in navigator){navigator.serviceWorker.register('./sw.js').catch(console.warn)}await this.seed();this.refreshIcons()},
  async seed(){let s=await db.settings.get('main');if(s)this.settings=s.value;await this.load();},
  async load(){this.providers=await db.providers.toArray();this.services=await db.services.toArray();this.receipts=await db.receipts.toArray();this.recurrences=await db.recurrences.toArray();this.logs=(await db.logs.orderBy('at').reverse().limit(30).toArray());let meta=await db.meta.get('receiptSeq');this.receiptSeqValue=Number(meta?.value||0);if(!this.receiptSeqInput)this.receiptSeqInput=this.nextReceiptNumber();this.after()},
  get isViewer(){return this.auth.role==='viewer'},get title(){return {'dashboard':'Dashboard inicial','prestadores':'Cadastro de prestadores','servicos':'Serviços','pagamentos':'Central de pagamentos','recibos':'Emissão de recibos','relatorios':'Relatórios','busca':'Busca global','config':'Configurações'}[this.view]||'ReciboCondo'},get selectedProvider(){return this.providers.find(p=>Number(p.id)===Number(this.selectedProviderId))||null},
  get cards(){return this.dashboardCards},
  get yearlyRows(){let y=String(this.dashboardYear||new Date().getFullYear());return this.services.filter(s=>String(s.paymentDate||s.serviceDate||'').startsWith(y))},
  get dashboardCards(){let nowRef=new Date().toISOString().slice(0,7), year=this.yearlyRows, month=year.filter(s=>String(s.paymentDate||s.serviceDate||'').slice(0,7)===nowRef), paidYear=year.reduce((a,s)=>a+Number(s.paidValue||0),0), paidMonth=month.reduce((a,s)=>a+Number(s.paidValue||0),0), countMonth=month.filter(s=>Number(s.paidValue||0)>0).length, open=year.reduce((a,s)=>a+this.balance(s),0), top=this.annualCategorySummary[0], topYear=this.topPaidNames(year), topMonth=this.topPaidNames(month), openTop=this.topOpenNames(year), catTop=this.annualCategorySummary.slice(0,3).map(c=>c.category+' • '+this.money(c.total)), activeCats=[...new Set(this.providers.map(p=>p.category).filter(Boolean))].slice(0,4), receiptsMonth=this.receipts.filter(r=>String(r.paymentDate||r.date||'').slice(0,7)===nowRef).length;return[
    {label:'Pago no ano',value:this.money(paidYear),hint:String(this.dashboardYear),icon:'wallet-cards',color:'text-rc-600',tooltipTitle:'Resumo anual',tooltipMain:this.money(paidYear),tooltipTag:'Ano '+this.dashboardYear,tooltip:['Total efetivamente pago no ano selecionado.', 'Top recebimentos: '+(topYear.length?topYear.join(' | '):'sem pagamentos lançados.'), 'Use para comparar o volume anual com o relatório anual premium.']},
    {label:'Pago no mês',value:this.money(paidMonth),hint:this.month(nowRef),icon:'circle-dollar-sign',color:'text-emerald-600',tooltipTitle:'Resumo do mês',tooltipMain:this.money(paidMonth),tooltipTag:'Mês atual',tooltip:['Mês de referência: '+this.month(nowRef)+'.', 'Quem recebeu: '+(topMonth.length?topMonth.join(' | '):'sem pagamentos neste mês.'), 'Ajuda a conferir rapidamente o fechamento mensal.']},
    {label:'Pagamentos no mês',value:countMonth,hint:'registros pagos',icon:'receipt-text',color:'text-blue-600',tooltipTitle:'Movimento mensal',tooltipMain:countMonth+' pagamento(s)',tooltipTag:'Recibos '+receiptsMonth,tooltip:['Valor médio por pagamento: '+this.money(countMonth?paidMonth/countMonth:0)+'.', 'Recibos emitidos no mês: '+receiptsMonth+'.', 'Bom para conferir se todos os pagamentos pagos já têm recibo.']},
    {label:'Prestadores ativos',value:this.providers.length,hint:'cadastrados',icon:'users',color:'text-rc-600',tooltipTitle:'Base de prestadores',tooltipMain:this.providers.length+' cadastro(s)',tooltipTag:'Cadastros',tooltip:['Categorias em uso: '+(activeCats.length?activeCats.join(', '):'nenhuma categoria cadastrada.'), 'Use para localizar quem presta serviço recorrente ou avulso.', 'Dica: mantenha PIX, WhatsApp e categoria sempre atualizados.']},
    {label:'Em aberto no ano',value:this.money(open),hint:'pendente/parcial',icon:'triangle-alert',color:'text-red-600',tooltipTitle:'Pendências do ano',tooltipMain:this.money(open),tooltipTag:'Atenção',tooltip:['Saldo lançado e ainda não quitado no ano.', 'Maiores pendências: '+(openTop.length?openTop.join(' | '):'nenhuma pendência no ano.'), 'Depois de pagar, registre o pagamento antes de emitir o recibo.']},
    {label:'Maior categoria',value:top?top.category:'-',hint:top?this.money(top.total):'sem dados',icon:'trophy',color:'text-yellow-600',tooltipTitle:'Categoria líder',tooltipMain:top?top.category:'Sem dados',tooltipTag:top?this.money(top.total):'Sem dados',tooltip:['Categoria com maior volume pago no ano.', 'Ranking: '+(catTop.length?catTop.join(' | '):'sem categorias pagas no ano.'), 'Ajuda a identificar onde o condomínio mais gastou.']}
  ]},
  get annualTotals(){let paid=this.yearlyRows.reduce((a,s)=>a+Number(s.paidValue||0),0),open=this.yearl

===== nextReceipt @ 6494595 =====
es,db.receipts,db.recurrences,db.logs,db.settings,db.meta,async()=>{await db.providers.clear();await db.services.clear();await db.receipts.clear();await db.recurrences.clear();await db.logs.clear();if(data.settings){let restoredSettings=JSON.parse(JSON.stringify(data.settings));await db.settings.put({key:'main',value:restoredSettings});this.settings=restoredSettings}if(providers.length)await db.providers.bulkAdd(providers);if(services.length)await db.services.bulkAdd(services);if(receipts.length)await db.receipts.bulkAdd(receipts);if(recurrences.length)await db.recurrences.bulkAdd(recurrences);if(logs.length)await db.logs.bulkAdd(logs.map(l=>{let x={...l};delete x.id;return x}).slice(0,200));let restoredMax=receipts.reduce((m,r)=>r.source==='Excel legado'?m:Math.max(m,Number(String(r.number||'').replace(/\D/g,'')||0)),0),safeSeq=Math.max(Number(data.receiptSeq||0),restoredMax);await db.meta.put({key:'receiptSeq',value:safeSeq})});await this.logAction('restore','Backup restaurado','Dados restaurados por arquivo JSON.');await this.load();this.toast('Backup restaurado com sucesso!')}catch(e){console.error(e);this.toast('Não foi possível restaurar o backup: '+e.message,'error')}},async clearAllInternal(){await db.transaction('rw',db.providers,db.services,db.receipts,db.recurrences,db.logs,async()=>{await db.providers.clear();await db.services.clear();await db.receipts.clear();await db.recurrences.clear();await db.logs.clear()});await this.load()},async clearAll(){this.openClearAllModal()},readLogo(e){let f=e.target.files[0];if(!f)return;let r=new FileReader();r.onload=()=>this.settings.logo=r.result;r.readAsDataURL(f)},
  receiptMaxNumber(){return this.receipts.reduce((max,r)=>r.source==='Excel legado'?max:Math.max(max,Number(String(r.number||'').replace(/\D/g,'')||0)),0)},nextReceiptNumber(){return Math.max(Number(this.receiptSeqValue||0),this.receiptMaxNumber())+1},receiptSequenceStatus(){return Number(this.receiptSeqValue||0)<this.receiptMaxNumber()?'Sincronizar':'Sequência OK'},async saveReceiptSequence(){if(this.isViewer)return;let target=Number(this.receiptSeqInput||0), max=this.receiptMaxNumber();if(!target||target<1)return this.toast('Informe um próximo número válido.','warning');if(target<=max)return this.toast('O número precisa ser maior que o último emitido: '+String(max).padStart(6,'0')+'.','warning');await db.meta.put({key:'receiptSeq',value:target-1});this.receiptSeqValue=target-1;await this.logAction('settings','Sequência de recibos ajustada','Próximo recibo definido para nº '+String(target).padStart(6,'0'));this.toast('Sequência atualizada. Próximo recibo: '+String(target).padStart(6,'0'));await this.load()},
  topPaidNames(rows){let map={};rows.forEach(s=>{let v=Number(s.paidValue||0);if(v<=0)return;let name=this.providerName(s.providerId);map[name]=(map[name]||0)+v});return Object.entries(map).sort((a,b)=>b[1]-a[1]).slice(0,3).map(([name,total])=>name+' • '+this.money(total))},topOpenNames(rows){let map={};rows.forEach(s=>{let v=this.balance(s);if(v<=0)return;let name=this.providerName(s.providerId);map[name]=(map[name]||0)+v});return Object.entries(map).sort((a,b)=>b[1]-a[1]).slice(0,3).map(([name,total])=>name+' • '+this.money(total))},
  providerServices(id){return this.services.filter(s=>Number(s.providerId)===Number(id)).sort((a,b)=>String(b.serviceDate||'').localeCompare(String(a.serviceDate||'')))},providerReceipts(id){return this.receipts.filter(r=>Number(r.providerId)===Number(id)).sort((a,b)=>String(b.date||'').localeCompare(String(a.date||'')))},providerLastServiceLabel(id){let s=this.providerServices(id)[0];return s?this.fmt(s.serviceDate)+' • '+this.money(s.paidValue||s.amount):'-'},async logAction(type,title,detail){try{await db.logs.add({at:new Date().toISOString(),type,title,detail,role:this.auth?.role||'sistema'});this.logs=(await db.logs.orderBy('at').reverse().limit(30).toArray())}catch(e){console.warn('logAction',e)}},logIcon(type){return {payment:'circle-dollar-sign',receipt:'receipt-text',service:'clipboard-list',provider:'users',recurrence:'repeat-2',backup:'shield-check',restore:'rotate-ccw',danger:'triangle-alert',settings:'settings',delete:'trash-2'}[type]||'history'},fmtDateTime(d){if(!d)return'-';return new Date(d).toLocaleString('pt-BR')},async clearLogs(){if(this.isViewer)return;await db.logs.clear();this.logs=[];this.after()},providerSpend(id){return this.services.filter(s=>Number(s.providerId)===Number(id)).reduce((a,s)=>a+Number(s.paidValue||s.amount||0),0)},providerOpen(id){return this.services.filter(s=>Number(s.providerId)===Number(id)).reduce((a,s)=>a+this.balance(s),0)},openWhatsapp(p){let raw=String(p.whatsapp||p.phone||'').replace(/\D/g,'');if(!raw)return this.toast('Este prestador não tem WhatsApp/telefone cadastrado.','warning');let phone=raw.startsWith('55')?raw:'55'+raw;let msg=encodeURIComponent('Olá, '+p.name+'. Aqui é do '+this.settings.condoName+'.');window.open('https://wa.me/'+phone+'?text='+msg,'_blank')},overdueLabel(s){if(!s.dueDate||this.balance(s)<=0)return'';let today=new Date(this.today()+'T00:00:00'), due=new Date(s.dueDate+'T00:00:00'), days=Math.ceil((due-today)/86400000);if(days<0)return Math.abs(days)+' dia(s) em atraso';if(days===0)return'vence hoje';return'faltam '+days+' dia(s)'},providerName(id){return (this.providers.find(p=>Number(p.id)===Number(id))||{}).name||'-'},reportReceiptNumber(service){let r=this.receipts.find(x=>Number(x.serviceId)===Number(service.id));return r?.number||'-';},balance(s){return Math.max(0,Number(s.amount||0)-Number(s.paidValue||0))},statusClass(st){return st==='Pago'?'bg-emerald-500/10 text-emerald-700':st==='Parcial'?'bg-amber-500/10 text-amber-700':'bg-red-500/10 text-red-700'},valorPorExtenso(v){v=Number(v||0);let reais=Math.floor(v),cent=Math.round((v-reais)*100);let base={0:'zero',1:'um',2:'dois',3:'três',4:'quatro',5:'cinco',6:'seis',7:'sete',8:'oito',9:'nove',10:'dez',11:'onze',12:'doze',13:'treze',14:'quatorze',15:'quinze',16:'dezesseis',17:'dezessete',18:'dezoito',19:'dez

===== receiptSeq @ 2210797 =====
ndomínio</h3>
                <p class="text-xs text-slate-500 dark:text-slate-400">Identidade usada nos recibos, relatórios e PDFs de prestação de contas.</p>
              </div>
            </div>
            <div class="grid lg:grid-cols-2 gap-3">
              <label><span class="text-sm font-bold">Nome</span><input x-model="settings.condoName" class="input mt-1"></label>
              <label><span class="text-sm font-bold">Responsável</span><input x-model="settings.manager" class="input mt-1"></label>
            </div>
            <div class="grid lg:grid-cols-2 gap-3">
              <label><span class="text-sm font-bold">CNPJ</span><input x-model="settings.cnpj" class="input mt-1"></label>
              <label><span class="text-sm font-bold">Logo</span><input @change="readLogo" type="file" accept="image/*" class="input mt-1"></label>
            </div>
            <label><span class="text-sm font-bold">Endereço</span><input x-model="settings.address" class="input mt-1"></label>
            <button class="primary">Salvar configurações</button>
          </form>

          <div class="glass rounded-3xl p-4 space-y-4">
            <div class="flex items-start justify-between gap-3">
              <div class="flex items-start gap-3">
                <div class="h-11 w-11 rounded-2xl bg-gradient-to-br from-rc-800 to-rc-gold text-white grid place-items-center shadow-soft"><i data-lucide="list-ordered" class="w-5 h-5"></i></div>
                <div>
                  <h3 class="font-black text-lg">Numeração dos recibos</h3>
                  <p class="text-xs text-slate-500 dark:text-slate-400">Conferência da sequência oficial. O número só é consumido quando um recibo é emitido.</p>
                </div>
              </div>
              <span class="badge" x-text="receiptSequenceStatus()"></span>
            </div>
            <div class="grid md:grid-cols-4 gap-3">
              <div class="rounded-3xl border border-slate-200/70 dark:border-slate-700/70 bg-white/70 dark:bg-slate-950/35 p-4">
                <p class="text-xs uppercase tracking-[.14em] text-slate-500 font-black">Próximo recibo</p>
                <p class="text-2xl font-black receipt-number mt-1" x-text="String(nextReceiptNumber()).padStart(6,'0')"></p>
              </div>
              <div class="rounded-3xl border border-slate-200/70 dark:border-slate-700/70 bg-white/70 dark:bg-slate-950/35 p-4">
                <p class="text-xs uppercase tracking-[.14em] text-slate-500 font-black">Último emitido</p>
                <p class="text-2xl font-black mt-1" x-text="String(receiptMaxNumber()).padStart(6,'0')"></p>
              </div>
              <div class="rounded-3xl border border-slate-200/70 dark:border-slate-700/70 bg-white/70 dark:bg-slate-950/35 p-4">
                <p class="text-xs uppercase tracking-[.14em] text-slate-500 font-black">Recibos</p>
                <p class="text-2xl font-black mt-1" x-text="receipts.length"></p>
              </div>
              <div class="rounded-3xl border border-slate-200/70 dark:border-slate-700/70 bg-white/70 dark:bg-slate-950/35 p-4">
                <p class="text-xs uppercase tracking-[.14em] text-slate-500 font-black">Sequência interna</p>
                <p class="text-2xl font-black mt-1" x-text="String(Number(receiptSeqValue||0)).padStart(6,'0')"></p>
              </div>
            </div>
            <div class="rounded-2xl border border-amber-500/25 bg-amber-500/10 p-3 space-y-3">
              <div class="flex items-start gap-3">
                <i data-lucide="shield-alert" class="w-5 h-5 text-amber-700 mt-1"></i>
                <div>
                  <p class="text-sm font-black text-amber-800 dark:text-amber-300">Ajuste manual protegido</p>
                  <p class="text-xs text-slate-500 dark:text-slate-400">Use apenas se estiver migrando recibos antigos ou alinhando a sequência com documentos já arquivados. O app não permite definir número menor ou igual ao maior recibo já emitido.</p>
                </div>
              </div>
              <div class="grid sm:grid-cols-[1fr_auto_auto] gap-2 items-end">
                <label><span class="text-sm font-bold">Definir próximo número</span><input x-model.number="receiptSeqInput" type="number" min="1" class="input mt-1" placeholder="Ex.: 25"></label>
                <button @click="receiptSeqInput=nextReceiptNumber()" class="secondary" :disabled="isViewer">Usar sugerido</button>
                <button @click="saveReceiptSequence" class="primary" :disabled="isViewer">Salvar sequência</button>
              </div>
            </div>
          </div>

          <div class="glass rounded-3xl p-4 space-y-4">
            <div class="flex items-start gap-3">
              <div class="h-11 w-11 rounded-2xl bg-gradient-to-br from-slate-800 to-red-700 text-white grid place-items-center shadow-soft"><i data-lucide="key-round" class="w-5 h-5"></i></div>
              <div>
                <h3 class="font-black text-lg">Senhas e acesso</h3>
                <p class="text-xs text-slate-500 dark:text-slate-400">Área sensível para alterar senhas e apagar dados locais.</p>
              </div>
            </div>
            <div class="grid sm:grid-cols-2 gap-3">
              <input x-model="passwords.admin" type="password" class="input" placeholder="Nova senha admin">
              <input x-model="passwords.viewer" type="password" class="input" placeholder="Nova senha viewer">
            </div>
            <button @click="savePasswords" class="primary w-full" :disabled="isViewer">Atualizar senhas</button>
          </div>

          <div class="glass rounded-3xl p-4 space-y-4">
            <div class="flex items-start justify-between gap-3">
              <div class="flex items-start gap-3">
                <div class="h-11 w-11 rounded-2xl bg-gradient-to-br from-rc-800 to-rc-gold text-white grid place-items-center shadow-soft"><i data-lucide="history" class="w-5 h-5"></i></div>
                <div>
            

===== receiptSeq @ 2212294 =====
 class="font-black text-lg">Numeração dos recibos</h3>
                  <p class="text-xs text-slate-500 dark:text-slate-400">Conferência da sequência oficial. O número só é consumido quando um recibo é emitido.</p>
                </div>
              </div>
              <span class="badge" x-text="receiptSequenceStatus()"></span>
            </div>
            <div class="grid md:grid-cols-4 gap-3">
              <div class="rounded-3xl border border-slate-200/70 dark:border-slate-700/70 bg-white/70 dark:bg-slate-950/35 p-4">
                <p class="text-xs uppercase tracking-[.14em] text-slate-500 font-black">Próximo recibo</p>
                <p class="text-2xl font-black receipt-number mt-1" x-text="String(nextReceiptNumber()).padStart(6,'0')"></p>
              </div>
              <div class="rounded-3xl border border-slate-200/70 dark:border-slate-700/70 bg-white/70 dark:bg-slate-950/35 p-4">
                <p class="text-xs uppercase tracking-[.14em] text-slate-500 font-black">Último emitido</p>
                <p class="text-2xl font-black mt-1" x-text="String(receiptMaxNumber()).padStart(6,'0')"></p>
              </div>
              <div class="rounded-3xl border border-slate-200/70 dark:border-slate-700/70 bg-white/70 dark:bg-slate-950/35 p-4">
                <p class="text-xs uppercase tracking-[.14em] text-slate-500 font-black">Recibos</p>
                <p class="text-2xl font-black mt-1" x-text="receipts.length"></p>
              </div>
              <div class="rounded-3xl border border-slate-200/70 dark:border-slate-700/70 bg-white/70 dark:bg-slate-950/35 p-4">
                <p class="text-xs uppercase tracking-[.14em] text-slate-500 font-black">Sequência interna</p>
                <p class="text-2xl font-black mt-1" x-text="String(Number(receiptSeqValue||0)).padStart(6,'0')"></p>
              </div>
            </div>
            <div class="rounded-2xl border border-amber-500/25 bg-amber-500/10 p-3 space-y-3">
              <div class="flex items-start gap-3">
                <i data-lucide="shield-alert" class="w-5 h-5 text-amber-700 mt-1"></i>
                <div>
                  <p class="text-sm font-black text-amber-800 dark:text-amber-300">Ajuste manual protegido</p>
                  <p class="text-xs text-slate-500 dark:text-slate-400">Use apenas se estiver migrando recibos antigos ou alinhando a sequência com documentos já arquivados. O app não permite definir número menor ou igual ao maior recibo já emitido.</p>
                </div>
              </div>
              <div class="grid sm:grid-cols-[1fr_auto_auto] gap-2 items-end">
                <label><span class="text-sm font-bold">Definir próximo número</span><input x-model.number="receiptSeqInput" type="number" min="1" class="input mt-1" placeholder="Ex.: 25"></label>
                <button @click="receiptSeqInput=nextReceiptNumber()" class="secondary" :disabled="isViewer">Usar sugerido</button>
                <button @click="saveReceiptSequence" class="primary" :disabled="isViewer">Salvar sequência</button>
              </div>
            </div>
          </div>

          <div class="glass rounded-3xl p-4 space-y-4">
            <div class="flex items-start gap-3">
              <div class="h-11 w-11 rounded-2xl bg-gradient-to-br from-slate-800 to-red-700 text-white grid place-items-center shadow-soft"><i data-lucide="key-round" class="w-5 h-5"></i></div>
              <div>
                <h3 class="font-black text-lg">Senhas e acesso</h3>
                <p class="text-xs text-slate-500 dark:text-slate-400">Área sensível para alterar senhas e apagar dados locais.</p>
              </div>
            </div>
            <div class="grid sm:grid-cols-2 gap-3">
              <input x-model="passwords.admin" type="password" class="input" placeholder="Nova senha admin">
              <input x-model="passwords.viewer" type="password" class="input" placeholder="Nova senha viewer">
            </div>
            <button @click="savePasswords" class="primary w-full" :disabled="isViewer">Atualizar senhas</button>
          </div>

          <div class="glass rounded-3xl p-4 space-y-4">
            <div class="flex items-start justify-between gap-3">
              <div class="flex items-start gap-3">
                <div class="h-11 w-11 rounded-2xl bg-gradient-to-br from-rc-800 to-rc-gold text-white grid place-items-center shadow-soft"><i data-lucide="history" class="w-5 h-5"></i></div>
                <div>
                  <h3 class="font-black text-lg">Log de atividades</h3>
                  <p class="text-xs text-slate-500 dark:text-slate-400">Registro local das principais ações: pagamentos, recibos, recorrências, backup e alterações sensíveis.</p>
                </div>
              </div>
              <span class="badge"><b x-text="logs.length"></b> eventos</span>
            </div>
            <div class="space-y-2 max-h-[360px] overflow-auto scrollbar pr-1">
              <template x-for="l in logs" :key="l.id">
                <div class="rounded-2xl border border-slate-200/70 dark:border-slate-700/70 bg-white/70 dark:bg-slate-950/35 p-3 flex gap-3">
                  <div class="h-9 w-9 rounded-xl bg-rc-800/10 text-rc-800 dark:text-rc-gold grid place-items-center shrink-0"><i :data-lucide="logIcon(l.type)" class="w-4 h-4"></i></div>
                  <div class="min-w-0 flex-1">
                    <div class="flex flex-wrap items-center justify-between gap-2"><p class="font-black text-sm" x-text="l.title"></p><p class="text-[11px] text-slate-500" x-text="fmtDateTime(l.at)"></p></div>
                    <p class="text-xs text-slate-500 dark:text-slate-400 mt-1" x-text="l.detail"></p>
                    <p class="text-[11px] text-slate-400 mt-1">Perfil: <span x-text="l.role || auth.role"></span></p>
                  </div>
                </div>
              </template>
              <p x-show="!logs.length" class="text-sm text-slate-500">Nenhuma atividade registrada aind

===== receiptSeq @ 2213226 =====
  <p class="text-xs uppercase tracking-[.14em] text-slate-500 font-black">Último emitido</p>
                <p class="text-2xl font-black mt-1" x-text="String(receiptMaxNumber()).padStart(6,'0')"></p>
              </div>
              <div class="rounded-3xl border border-slate-200/70 dark:border-slate-700/70 bg-white/70 dark:bg-slate-950/35 p-4">
                <p class="text-xs uppercase tracking-[.14em] text-slate-500 font-black">Recibos</p>
                <p class="text-2xl font-black mt-1" x-text="receipts.length"></p>
              </div>
              <div class="rounded-3xl border border-slate-200/70 dark:border-slate-700/70 bg-white/70 dark:bg-slate-950/35 p-4">
                <p class="text-xs uppercase tracking-[.14em] text-slate-500 font-black">Sequência interna</p>
                <p class="text-2xl font-black mt-1" x-text="String(Number(receiptSeqValue||0)).padStart(6,'0')"></p>
              </div>
            </div>
            <div class="rounded-2xl border border-amber-500/25 bg-amber-500/10 p-3 space-y-3">
              <div class="flex items-start gap-3">
                <i data-lucide="shield-alert" class="w-5 h-5 text-amber-700 mt-1"></i>
                <div>
                  <p class="text-sm font-black text-amber-800 dark:text-amber-300">Ajuste manual protegido</p>
                  <p class="text-xs text-slate-500 dark:text-slate-400">Use apenas se estiver migrando recibos antigos ou alinhando a sequência com documentos já arquivados. O app não permite definir número menor ou igual ao maior recibo já emitido.</p>
                </div>
              </div>
              <div class="grid sm:grid-cols-[1fr_auto_auto] gap-2 items-end">
                <label><span class="text-sm font-bold">Definir próximo número</span><input x-model.number="receiptSeqInput" type="number" min="1" class="input mt-1" placeholder="Ex.: 25"></label>
                <button @click="receiptSeqInput=nextReceiptNumber()" class="secondary" :disabled="isViewer">Usar sugerido</button>
                <button @click="saveReceiptSequence" class="primary" :disabled="isViewer">Salvar sequência</button>
              </div>
            </div>
          </div>

          <div class="glass rounded-3xl p-4 space-y-4">
            <div class="flex items-start gap-3">
              <div class="h-11 w-11 rounded-2xl bg-gradient-to-br from-slate-800 to-red-700 text-white grid place-items-center shadow-soft"><i data-lucide="key-round" class="w-5 h-5"></i></div>
              <div>
                <h3 class="font-black text-lg">Senhas e acesso</h3>
                <p class="text-xs text-slate-500 dark:text-slate-400">Área sensível para alterar senhas e apagar dados locais.</p>
              </div>
            </div>
            <div class="grid sm:grid-cols-2 gap-3">
              <input x-model="passwords.admin" type="password" class="input" placeholder="Nova senha admin">
              <input x-model="passwords.viewer" type="password" class="input" placeholder="Nova senha viewer">
            </div>
            <button @click="savePasswords" class="primary w-full" :disabled="isViewer">Atualizar senhas</button>
          </div>

          <div class="glass rounded-3xl p-4 space-y-4">
            <div class="flex items-start justify-between gap-3">
              <div class="flex items-start gap-3">
                <div class="h-11 w-11 rounded-2xl bg-gradient-to-br from-rc-800 to-rc-gold text-white grid place-items-center shadow-soft"><i data-lucide="history" class="w-5 h-5"></i></div>
                <div>
                  <h3 class="font-black text-lg">Log de atividades</h3>
                  <p class="text-xs text-slate-500 dark:text-slate-400">Registro local das principais ações: pagamentos, recibos, recorrências, backup e alterações sensíveis.</p>
                </div>
              </div>
              <span class="badge"><b x-text="logs.length"></b> eventos</span>
            </div>
            <div class="space-y-2 max-h-[360px] overflow-auto scrollbar pr-1">
              <template x-for="l in logs" :key="l.id">
                <div class="rounded-2xl border border-slate-200/70 dark:border-slate-700/70 bg-white/70 dark:bg-slate-950/35 p-3 flex gap-3">
                  <div class="h-9 w-9 rounded-xl bg-rc-800/10 text-rc-800 dark:text-rc-gold grid place-items-center shrink-0"><i :data-lucide="logIcon(l.type)" class="w-4 h-4"></i></div>
                  <div class="min-w-0 flex-1">
                    <div class="flex flex-wrap items-center justify-between gap-2"><p class="font-black text-sm" x-text="l.title"></p><p class="text-[11px] text-slate-500" x-text="fmtDateTime(l.at)"></p></div>
                    <p class="text-xs text-slate-500 dark:text-slate-400 mt-1" x-text="l.detail"></p>
                    <p class="text-[11px] text-slate-400 mt-1">Perfil: <span x-text="l.role || auth.role"></span></p>
                  </div>
                </div>
              </template>
              <p x-show="!logs.length" class="text-sm text-slate-500">Nenhuma atividade registrada ainda.</p>
            </div>
            <button @click="clearLogs" class="secondary w-full" :disabled="isViewer || !logs.length">Limpar log</button>
          </div>
        </section>
      </main>

      <div x-show="payModal" class="no-print fixed inset-0 z-50 grid place-items-center bg-slate-950/55 p-4">
        <div class="glass rounded-3xl p-5 w-full max-w-2xl space-y-4">
          <div class="flex items-start justify-between gap-3">
            <div>
              <p class="text-xs uppercase tracking-[.18em] text-rc-gold font-black">Janela flutuante</p>
              <h3 class="font-black text-xl">Registrar pagamento</h3>
              <p class="text-sm text-slate-500" x-text="paymentTarget ? providerName(paymentTarget.providerId)+' • '+paymentTarget.category : ''"></p>
            </div>
            <button @click="payModal=false" class="secondary px-3 py-2">Fechar</button>
          </div>
          <div class="

===== receiptSeq @ 2213347 =====
xt-2xl font-black mt-1" x-text="String(receiptMaxNumber()).padStart(6,'0')"></p>
              </div>
              <div class="rounded-3xl border border-slate-200/70 dark:border-slate-700/70 bg-white/70 dark:bg-slate-950/35 p-4">
                <p class="text-xs uppercase tracking-[.14em] text-slate-500 font-black">Recibos</p>
                <p class="text-2xl font-black mt-1" x-text="receipts.length"></p>
              </div>
              <div class="rounded-3xl border border-slate-200/70 dark:border-slate-700/70 bg-white/70 dark:bg-slate-950/35 p-4">
                <p class="text-xs uppercase tracking-[.14em] text-slate-500 font-black">Sequência interna</p>
                <p class="text-2xl font-black mt-1" x-text="String(Number(receiptSeqValue||0)).padStart(6,'0')"></p>
              </div>
            </div>
            <div class="rounded-2xl border border-amber-500/25 bg-amber-500/10 p-3 space-y-3">
              <div class="flex items-start gap-3">
                <i data-lucide="shield-alert" class="w-5 h-5 text-amber-700 mt-1"></i>
                <div>
                  <p class="text-sm font-black text-amber-800 dark:text-amber-300">Ajuste manual protegido</p>
                  <p class="text-xs text-slate-500 dark:text-slate-400">Use apenas se estiver migrando recibos antigos ou alinhando a sequência com documentos já arquivados. O app não permite definir número menor ou igual ao maior recibo já emitido.</p>
                </div>
              </div>
              <div class="grid sm:grid-cols-[1fr_auto_auto] gap-2 items-end">
                <label><span class="text-sm font-bold">Definir próximo número</span><input x-model.number="receiptSeqInput" type="number" min="1" class="input mt-1" placeholder="Ex.: 25"></label>
                <button @click="receiptSeqInput=nextReceiptNumber()" class="secondary" :disabled="isViewer">Usar sugerido</button>
                <button @click="saveReceiptSequence" class="primary" :disabled="isViewer">Salvar sequência</button>
              </div>
            </div>
          </div>

          <div class="glass rounded-3xl p-4 space-y-4">
            <div class="flex items-start gap-3">
              <div class="h-11 w-11 rounded-2xl bg-gradient-to-br from-slate-800 to-red-700 text-white grid place-items-center shadow-soft"><i data-lucide="key-round" class="w-5 h-5"></i></div>
              <div>
                <h3 class="font-black text-lg">Senhas e acesso</h3>
                <p class="text-xs text-slate-500 dark:text-slate-400">Área sensível para alterar senhas e apagar dados locais.</p>
              </div>
            </div>
            <div class="grid sm:grid-cols-2 gap-3">
              <input x-model="passwords.admin" type="password" class="input" placeholder="Nova senha admin">
              <input x-model="passwords.viewer" type="password" class="input" placeholder="Nova senha viewer">
            </div>
            <button @click="savePasswords" class="primary w-full" :disabled="isViewer">Atualizar senhas</button>
          </div>

          <div class="glass rounded-3xl p-4 space-y-4">
            <div class="flex items-start justify-between gap-3">
              <div class="flex items-start gap-3">
                <div class="h-11 w-11 rounded-2xl bg-gradient-to-br from-rc-800 to-rc-gold text-white grid place-items-center shadow-soft"><i data-lucide="history" class="w-5 h-5"></i></div>
                <div>
                  <h3 class="font-black text-lg">Log de atividades</h3>
                  <p class="text-xs text-slate-500 dark:text-slate-400">Registro local das principais ações: pagamentos, recibos, recorrências, backup e alterações sensíveis.</p>
                </div>
              </div>
              <span class="badge"><b x-text="logs.length"></b> eventos</span>
            </div>
            <div class="space-y-2 max-h-[360px] overflow-auto scrollbar pr-1">
              <template x-for="l in logs" :key="l.id">
                <div class="rounded-2xl border border-slate-200/70 dark:border-slate-700/70 bg-white/70 dark:bg-slate-950/35 p-3 flex gap-3">
                  <div class="h-9 w-9 rounded-xl bg-rc-800/10 text-rc-800 dark:text-rc-gold grid place-items-center shrink-0"><i :data-lucide="logIcon(l.type)" class="w-4 h-4"></i></div>
                  <div class="min-w-0 flex-1">
                    <div class="flex flex-wrap items-center justify-between gap-2"><p class="font-black text-sm" x-text="l.title"></p><p class="text-[11px] text-slate-500" x-text="fmtDateTime(l.at)"></p></div>
                    <p class="text-xs text-slate-500 dark:text-slate-400 mt-1" x-text="l.detail"></p>
                    <p class="text-[11px] text-slate-400 mt-1">Perfil: <span x-text="l.role || auth.role"></span></p>
                  </div>
                </div>
              </template>
              <p x-show="!logs.length" class="text-sm text-slate-500">Nenhuma atividade registrada ainda.</p>
            </div>
            <button @click="clearLogs" class="secondary w-full" :disabled="isViewer || !logs.length">Limpar log</button>
          </div>
        </section>
      </main>

      <div x-show="payModal" class="no-print fixed inset-0 z-50 grid place-items-center bg-slate-950/55 p-4">
        <div class="glass rounded-3xl p-5 w-full max-w-2xl space-y-4">
          <div class="flex items-start justify-between gap-3">
            <div>
              <p class="text-xs uppercase tracking-[.18em] text-rc-gold font-black">Janela flutuante</p>
              <h3 class="font-black text-xl">Registrar pagamento</h3>
              <p class="text-sm text-slate-500" x-text="paymentTarget ? providerName(paymentTarget.providerId)+' • '+paymentTarget.category : ''"></p>
            </div>
            <button @click="payModal=false" class="secondary px-3 py-2">Fechar</button>
          </div>
          <div class="grid sm:grid-cols-3 gap-3" x-show="paymentTarget">
            <div class="rounded-2xl border border-slate-200/70 dark:bo

===== receiptSeq @ 6445738 =====
        </div>
        </div>
      </section>
    </div>
  </template>
</div>
<script>
const db=new Dexie('ReciboCondoPrestadoresDB');
db.version(1).stores({providers:'++id,name,category,document',services:'++id,providerId,category,reference,serviceDate,dueDate,status',receipts:'++id,number,date,providerName,category',settings:'key',meta:'key'});
db.version(2).stores({providers:'++id,name,category,document',services:'++id,providerId,category,reference,serviceDate,dueDate,status,recurrenceId',receipts:'++id,number,date,providerName,category',settings:'key',meta:'key',recurrences:'++id,providerId,category,active'});
db.version(3).stores({providers:'++id,name,category,document',services:'++id,providerId,category,reference,serviceDate,dueDate,status,recurrenceId',receipts:'++id,number,date,providerName,category',settings:'key',meta:'key',recurrences:'++id,providerId,category,active',logs:'++id,at,type,title,detail,role'});
function app(){return{
  auth:{logged:false,role:'viewer'},login:{role:'admin',password:''},dark:localStorage.getItem('rc_dark')==='1',view:'dashboard',q:'',charts:{},selectedProviderId:null,activeTooltip:null,
  menu:[{id:'dashboard',label:'Início',icon:'layout-dashboard'},{id:'prestadores',label:'Prestadores',icon:'users'},{id:'servicos',label:'Serviços',icon:'hard-hat'},{id:'pagamentos',label:'Pagamentos',icon:'wallet-cards'},{id:'recibos',label:'Recibos',icon:'receipt-text'},{id:'relatorios',label:'Relatórios',icon:'file-bar-chart'},{id:'config',label:'Config.',icon:'settings'}],
  categories:['Jardineiro','Vidraceiro','Serralheiro','Eletricista','Pedreiro','Paisagista','Pintor','Encanador','Limpeza','Dedetização','Manutenção de bomba','Portão/Interfone','Gratificação natalina','Outros'],
  providers:[],services:[],receipts:[],recurrences:[],logs:[],receiptSeqValue:0,receiptSeqInput:"",settings:{condoName:'Condomínio Exemplo',cnpj:'CNPJ 00.000.000/0001-00',address:'Endereço do condomínio',manager:'Síndico/Tesoureiro',logo:''},
  providerForm:{id:null,name:'',type:'Autônomo',document:'',category:'Jardineiro',phone:'',whatsapp:'',pix:'',notes:''},providerSearch:'',providerCategoryFilter:'',providerSortField:'name',providerSortDir:'asc',
  serviceForm:{providerId:'',category:'Jardineiro',reference:new Date().toISOString().slice(0,7),serviceDate:new Date().toISOString().slice(0,10),dueDate:new Date().toISOString().slice(0,10),amount:0,location:'',description:'',paymentDate:'',paymentMethod:'',paidValue:0},
  recurrenceForm:{id:null,providerId:'',category:'Jardineiro',amount:0,dueDay:5,paymentDay:5,paymentMethod:'PIX',location:'',description:'',active:true},
  serviceFilter:'todos',serviceCategoryFilter:'todas',serviceProviderFilter:'todos',serviceMonthFilter:'',paymentMonthFilter:new Date().toISOString().slice(0,7),paymentStatusFilter:'todos',paymentProviderFilter:'todos',receiptFilter:{q:'',month:'',category:'todas'},receiptServiceId:'',currentReceipt:null,dashboardYear:new Date().getFullYear(),payModal:false,paymentTarget:null,payment:{date:new Date().toISOString().slice(0,10),value:0,method:'PIX'},actionModal:{open:false,type:'',tone:'default',icon:'',target:null,service:null,title:'',subtitle:'',note:'',confirmLabel:'',details:[]},toasts:[],
  report:{from:new Date(new Date().getFullYear(),new Date().getMonth(),1).toISOString().slice(0,10),to:new Date().toISOString().slice(0,10),category:'todas',status:'todos'},passwords:{admin:'',viewer:''},
  async init(){document.documentElement.classList.toggle('dark',this.dark);window.alert=(msg)=>this.toast(String(msg||''),this.toastType(String(msg||'')));if('serviceWorker'in navigator){navigator.serviceWorker.register('./sw.js').catch(console.warn)}await this.seed();this.refreshIcons()},
  async seed(){let s=await db.settings.get('main');if(s)this.settings=s.value;await this.load();},
  async load(){this.providers=await db.providers.toArray();this.services=await db.services.toArray();this.receipts=await db.receipts.toArray();this.recurrences=await db.recurrences.toArray();this.logs=(await db.logs.orderBy('at').reverse().limit(30).toArray());let meta=await db.meta.get('receiptSeq');this.receiptSeqValue=Number(meta?.value||0);if(!this.receiptSeqInput)this.receiptSeqInput=this.nextReceiptNumber();this.after()},
  get isViewer(){return this.auth.role==='viewer'},get title(){return {'dashboard':'Dashboard inicial','prestadores':'Cadastro de prestadores','servicos':'Serviços','pagamentos':'Central de pagamentos','recibos':'Emissão de recibos','relatorios':'Relatórios','busca':'Busca global','config':'Configurações'}[this.view]||'ReciboCondo'},get selectedProvider(){return this.providers.find(p=>Number(p.id)===Number(this.selectedProviderId))||null},
  get cards(){return this.dashboardCards},
  get yearlyRows(){let y=String(this.dashboardYear||new Date().getFullYear());return this.services.filter(s=>String(s.paymentDate||s.serviceDate||'').startsWith(y))},
  get dashboardCards(){let nowRef=new Date().toISOString().slice(0,7), year=this.yearlyRows, month=year.filter(s=>String(s.paymentDate||s.serviceDate||'').slice(0,7)===nowRef), paidYear=year.reduce((a,s)=>a+Number(s.paidValue||0),0), paidMonth=month.reduce((a,s)=>a+Number(s.paidValue||0),0), countMonth=month.filter(s=>Number(s.paidValue||0)>0).length, open=year.reduce((a,s)=>a+this.balance(s),0), top=this.annualCategorySummary[0], topYear=this.topPaidNames(year), topMonth=this.topPaidNames(month), openTop=this.topOpenNames(year), catTop=this.annualCategorySummary.slice(0,3).map(c=>c.category+' • '+this.money(c.total)), activeCats=[...new Set(this.providers.map(p=>p.category).filter(Boolean))].slice(0,4), receiptsMonth=this.receipts.filter(r=>String(r.paymentDate||r.date||'').slice(0,7)===nowRef).length;return[
    {label:'Pago no ano',value:this.money(paidYear),hint:String(this.dashboardYear),icon:'wallet-cards',color:'text-rc-600',tooltipTitle:'Resumo anual',tooltipMain:this.money(paidYear),tooltipTag:'Ano '+this.dashboardYear,tooltip:['Total efetivamente pago no ano selecionado.',

===== receiptSeq @ 6445756 =====
     </div>
      </section>
    </div>
  </template>
</div>
<script>
const db=new Dexie('ReciboCondoPrestadoresDB');
db.version(1).stores({providers:'++id,name,category,document',services:'++id,providerId,category,reference,serviceDate,dueDate,status',receipts:'++id,number,date,providerName,category',settings:'key',meta:'key'});
db.version(2).stores({providers:'++id,name,category,document',services:'++id,providerId,category,reference,serviceDate,dueDate,status,recurrenceId',receipts:'++id,number,date,providerName,category',settings:'key',meta:'key',recurrences:'++id,providerId,category,active'});
db.version(3).stores({providers:'++id,name,category,document',services:'++id,providerId,category,reference,serviceDate,dueDate,status,recurrenceId',receipts:'++id,number,date,providerName,category',settings:'key',meta:'key',recurrences:'++id,providerId,category,active',logs:'++id,at,type,title,detail,role'});
function app(){return{
  auth:{logged:false,role:'viewer'},login:{role:'admin',password:''},dark:localStorage.getItem('rc_dark')==='1',view:'dashboard',q:'',charts:{},selectedProviderId:null,activeTooltip:null,
  menu:[{id:'dashboard',label:'Início',icon:'layout-dashboard'},{id:'prestadores',label:'Prestadores',icon:'users'},{id:'servicos',label:'Serviços',icon:'hard-hat'},{id:'pagamentos',label:'Pagamentos',icon:'wallet-cards'},{id:'recibos',label:'Recibos',icon:'receipt-text'},{id:'relatorios',label:'Relatórios',icon:'file-bar-chart'},{id:'config',label:'Config.',icon:'settings'}],
  categories:['Jardineiro','Vidraceiro','Serralheiro','Eletricista','Pedreiro','Paisagista','Pintor','Encanador','Limpeza','Dedetização','Manutenção de bomba','Portão/Interfone','Gratificação natalina','Outros'],
  providers:[],services:[],receipts:[],recurrences:[],logs:[],receiptSeqValue:0,receiptSeqInput:"",settings:{condoName:'Condomínio Exemplo',cnpj:'CNPJ 00.000.000/0001-00',address:'Endereço do condomínio',manager:'Síndico/Tesoureiro',logo:''},
  providerForm:{id:null,name:'',type:'Autônomo',document:'',category:'Jardineiro',phone:'',whatsapp:'',pix:'',notes:''},providerSearch:'',providerCategoryFilter:'',providerSortField:'name',providerSortDir:'asc',
  serviceForm:{providerId:'',category:'Jardineiro',reference:new Date().toISOString().slice(0,7),serviceDate:new Date().toISOString().slice(0,10),dueDate:new Date().toISOString().slice(0,10),amount:0,location:'',description:'',paymentDate:'',paymentMethod:'',paidValue:0},
  recurrenceForm:{id:null,providerId:'',category:'Jardineiro',amount:0,dueDay:5,paymentDay:5,paymentMethod:'PIX',location:'',description:'',active:true},
  serviceFilter:'todos',serviceCategoryFilter:'todas',serviceProviderFilter:'todos',serviceMonthFilter:'',paymentMonthFilter:new Date().toISOString().slice(0,7),paymentStatusFilter:'todos',paymentProviderFilter:'todos',receiptFilter:{q:'',month:'',category:'todas'},receiptServiceId:'',currentReceipt:null,dashboardYear:new Date().getFullYear(),payModal:false,paymentTarget:null,payment:{date:new Date().toISOString().slice(0,10),value:0,method:'PIX'},actionModal:{open:false,type:'',tone:'default',icon:'',target:null,service:null,title:'',subtitle:'',note:'',confirmLabel:'',details:[]},toasts:[],
  report:{from:new Date(new Date().getFullYear(),new Date().getMonth(),1).toISOString().slice(0,10),to:new Date().toISOString().slice(0,10),category:'todas',status:'todos'},passwords:{admin:'',viewer:''},
  async init(){document.documentElement.classList.toggle('dark',this.dark);window.alert=(msg)=>this.toast(String(msg||''),this.toastType(String(msg||'')));if('serviceWorker'in navigator){navigator.serviceWorker.register('./sw.js').catch(console.warn)}await this.seed();this.refreshIcons()},
  async seed(){let s=await db.settings.get('main');if(s)this.settings=s.value;await this.load();},
  async load(){this.providers=await db.providers.toArray();this.services=await db.services.toArray();this.receipts=await db.receipts.toArray();this.recurrences=await db.recurrences.toArray();this.logs=(await db.logs.orderBy('at').reverse().limit(30).toArray());let meta=await db.meta.get('receiptSeq');this.receiptSeqValue=Number(meta?.value||0);if(!this.receiptSeqInput)this.receiptSeqInput=this.nextReceiptNumber();this.after()},
  get isViewer(){return this.auth.role==='viewer'},get title(){return {'dashboard':'Dashboard inicial','prestadores':'Cadastro de prestadores','servicos':'Serviços','pagamentos':'Central de pagamentos','recibos':'Emissão de recibos','relatorios':'Relatórios','busca':'Busca global','config':'Configurações'}[this.view]||'ReciboCondo'},get selectedProvider(){return this.providers.find(p=>Number(p.id)===Number(this.selectedProviderId))||null},
  get cards(){return this.dashboardCards},
  get yearlyRows(){let y=String(this.dashboardYear||new Date().getFullYear());return this.services.filter(s=>String(s.paymentDate||s.serviceDate||'').startsWith(y))},
  get dashboardCards(){let nowRef=new Date().toISOString().slice(0,7), year=this.yearlyRows, month=year.filter(s=>String(s.paymentDate||s.serviceDate||'').slice(0,7)===nowRef), paidYear=year.reduce((a,s)=>a+Number(s.paidValue||0),0), paidMonth=month.reduce((a,s)=>a+Number(s.paidValue||0),0), countMonth=month.filter(s=>Number(s.paidValue||0)>0).length, open=year.reduce((a,s)=>a+this.balance(s),0), top=this.annualCategorySummary[0], topYear=this.topPaidNames(year), topMonth=this.topPaidNames(month), openTop=this.topOpenNames(year), catTop=this.annualCategorySummary.slice(0,3).map(c=>c.category+' • '+this.money(c.total)), activeCats=[...new Set(this.providers.map(p=>p.category).filter(Boolean))].slice(0,4), receiptsMonth=this.receipts.filter(r=>String(r.paymentDate||r.date||'').slice(0,7)===nowRef).length;return[
    {label:'Pago no ano',value:this.money(paidYear),hint:String(this.dashboardYear),icon:'wallet-cards',color:'text-rc-600',tooltipTitle:'Resumo anual',tooltipMain:this.money(paidYear),tooltipTag:'Ano '+this.dashboardYear,tooltip:['Total efetivamente pago no ano selecionado.', 'Top recebimentos

===== receiptSeq @ 6448042 =====
w Date().toISOString().slice(0,10),dueDate:new Date().toISOString().slice(0,10),amount:0,location:'',description:'',paymentDate:'',paymentMethod:'',paidValue:0},
  recurrenceForm:{id:null,providerId:'',category:'Jardineiro',amount:0,dueDay:5,paymentDay:5,paymentMethod:'PIX',location:'',description:'',active:true},
  serviceFilter:'todos',serviceCategoryFilter:'todas',serviceProviderFilter:'todos',serviceMonthFilter:'',paymentMonthFilter:new Date().toISOString().slice(0,7),paymentStatusFilter:'todos',paymentProviderFilter:'todos',receiptFilter:{q:'',month:'',category:'todas'},receiptServiceId:'',currentReceipt:null,dashboardYear:new Date().getFullYear(),payModal:false,paymentTarget:null,payment:{date:new Date().toISOString().slice(0,10),value:0,method:'PIX'},actionModal:{open:false,type:'',tone:'default',icon:'',target:null,service:null,title:'',subtitle:'',note:'',confirmLabel:'',details:[]},toasts:[],
  report:{from:new Date(new Date().getFullYear(),new Date().getMonth(),1).toISOString().slice(0,10),to:new Date().toISOString().slice(0,10),category:'todas',status:'todos'},passwords:{admin:'',viewer:''},
  async init(){document.documentElement.classList.toggle('dark',this.dark);window.alert=(msg)=>this.toast(String(msg||''),this.toastType(String(msg||'')));if('serviceWorker'in navigator){navigator.serviceWorker.register('./sw.js').catch(console.warn)}await this.seed();this.refreshIcons()},
  async seed(){let s=await db.settings.get('main');if(s)this.settings=s.value;await this.load();},
  async load(){this.providers=await db.providers.toArray();this.services=await db.services.toArray();this.receipts=await db.receipts.toArray();this.recurrences=await db.recurrences.toArray();this.logs=(await db.logs.orderBy('at').reverse().limit(30).toArray());let meta=await db.meta.get('receiptSeq');this.receiptSeqValue=Number(meta?.value||0);if(!this.receiptSeqInput)this.receiptSeqInput=this.nextReceiptNumber();this.after()},
  get isViewer(){return this.auth.role==='viewer'},get title(){return {'dashboard':'Dashboard inicial','prestadores':'Cadastro de prestadores','servicos':'Serviços','pagamentos':'Central de pagamentos','recibos':'Emissão de recibos','relatorios':'Relatórios','busca':'Busca global','config':'Configurações'}[this.view]||'ReciboCondo'},get selectedProvider(){return this.providers.find(p=>Number(p.id)===Number(this.selectedProviderId))||null},
  get cards(){return this.dashboardCards},
  get yearlyRows(){let y=String(this.dashboardYear||new Date().getFullYear());return this.services.filter(s=>String(s.paymentDate||s.serviceDate||'').startsWith(y))},
  get dashboardCards(){let nowRef=new Date().toISOString().slice(0,7), year=this.yearlyRows, month=year.filter(s=>String(s.paymentDate||s.serviceDate||'').slice(0,7)===nowRef), paidYear=year.reduce((a,s)=>a+Number(s.paidValue||0),0), paidMonth=month.reduce((a,s)=>a+Number(s.paidValue||0),0), countMonth=month.filter(s=>Number(s.paidValue||0)>0).length, open=year.reduce((a,s)=>a+this.balance(s),0), top=this.annualCategorySummary[0], topYear=this.topPaidNames(year), topMonth=this.topPaidNames(month), openTop=this.topOpenNames(year), catTop=this.annualCategorySummary.slice(0,3).map(c=>c.category+' • '+this.money(c.total)), activeCats=[...new Set(this.providers.map(p=>p.category).filter(Boolean))].slice(0,4), receiptsMonth=this.receipts.filter(r=>String(r.paymentDate||r.date||'').slice(0,7)===nowRef).length;return[
    {label:'Pago no ano',value:this.money(paidYear),hint:String(this.dashboardYear),icon:'wallet-cards',color:'text-rc-600',tooltipTitle:'Resumo anual',tooltipMain:this.money(paidYear),tooltipTag:'Ano '+this.dashboardYear,tooltip:['Total efetivamente pago no ano selecionado.', 'Top recebimentos: '+(topYear.length?topYear.join(' | '):'sem pagamentos lançados.'), 'Use para comparar o volume anual com o relatório anual premium.']},
    {label:'Pago no mês',value:this.money(paidMonth),hint:this.month(nowRef),icon:'circle-dollar-sign',color:'text-emerald-600',tooltipTitle:'Resumo do mês',tooltipMain:this.money(paidMonth),tooltipTag:'Mês atual',tooltip:['Mês de referência: '+this.month(nowRef)+'.', 'Quem recebeu: '+(topMonth.length?topMonth.join(' | '):'sem pagamentos neste mês.'), 'Ajuda a conferir rapidamente o fechamento mensal.']},
    {label:'Pagamentos no mês',value:countMonth,hint:'registros pagos',icon:'receipt-text',color:'text-blue-600',tooltipTitle:'Movimento mensal',tooltipMain:countMonth+' pagamento(s)',tooltipTag:'Recibos '+receiptsMonth,tooltip:['Valor médio por pagamento: '+this.money(countMonth?paidMonth/countMonth:0)+'.', 'Recibos emitidos no mês: '+receiptsMonth+'.', 'Bom para conferir se todos os pagamentos pagos já têm recibo.']},
    {label:'Prestadores ativos',value:this.providers.length,hint:'cadastrados',icon:'users',color:'text-rc-600',tooltipTitle:'Base de prestadores',tooltipMain:this.providers.length+' cadastro(s)',tooltipTag:'Cadastros',tooltip:['Categorias em uso: '+(activeCats.length?activeCats.join(', '):'nenhuma categoria cadastrada.'), 'Use para localizar quem presta serviço recorrente ou avulso.', 'Dica: mantenha PIX, WhatsApp e categoria sempre atualizados.']},
    {label:'Em aberto no ano',value:this.money(open),hint:'pendente/parcial',icon:'triangle-alert',color:'text-red-600',tooltipTitle:'Pendências do ano',tooltipMain:this.money(open),tooltipTag:'Atenção',tooltip:['Saldo lançado e ainda não quitado no ano.', 'Maiores pendências: '+(openTop.length?openTop.join(' | '):'nenhuma pendência no ano.'), 'Depois de pagar, registre o pagamento antes de emitir o recibo.']},
    {label:'Maior categoria',value:top?top.category:'-',hint:top?this.money(top.total):'sem dados',icon:'trophy',color:'text-yellow-600',tooltipTitle:'Categoria líder',tooltipMain:top?top.category:'Sem dados',tooltipTag:top?this.money(top.total):'Sem dados',tooltip:['Categoria com maior volume pago no ano.', 'Ranking: '+(catTop.length?catTop.join(' | '):'sem categorias pagas no ano.'), 'Ajuda a identificar onde o condomínio mais gastou.']}
 

===== receiptSeq @ 6448060 =====
ng().slice(0,10),dueDate:new Date().toISOString().slice(0,10),amount:0,location:'',description:'',paymentDate:'',paymentMethod:'',paidValue:0},
  recurrenceForm:{id:null,providerId:'',category:'Jardineiro',amount:0,dueDay:5,paymentDay:5,paymentMethod:'PIX',location:'',description:'',active:true},
  serviceFilter:'todos',serviceCategoryFilter:'todas',serviceProviderFilter:'todos',serviceMonthFilter:'',paymentMonthFilter:new Date().toISOString().slice(0,7),paymentStatusFilter:'todos',paymentProviderFilter:'todos',receiptFilter:{q:'',month:'',category:'todas'},receiptServiceId:'',currentReceipt:null,dashboardYear:new Date().getFullYear(),payModal:false,paymentTarget:null,payment:{date:new Date().toISOString().slice(0,10),value:0,method:'PIX'},actionModal:{open:false,type:'',tone:'default',icon:'',target:null,service:null,title:'',subtitle:'',note:'',confirmLabel:'',details:[]},toasts:[],
  report:{from:new Date(new Date().getFullYear(),new Date().getMonth(),1).toISOString().slice(0,10),to:new Date().toISOString().slice(0,10),category:'todas',status:'todos'},passwords:{admin:'',viewer:''},
  async init(){document.documentElement.classList.toggle('dark',this.dark);window.alert=(msg)=>this.toast(String(msg||''),this.toastType(String(msg||'')));if('serviceWorker'in navigator){navigator.serviceWorker.register('./sw.js').catch(console.warn)}await this.seed();this.refreshIcons()},
  async seed(){let s=await db.settings.get('main');if(s)this.settings=s.value;await this.load();},
  async load(){this.providers=await db.providers.toArray();this.services=await db.services.toArray();this.receipts=await db.receipts.toArray();this.recurrences=await db.recurrences.toArray();this.logs=(await db.logs.orderBy('at').reverse().limit(30).toArray());let meta=await db.meta.get('receiptSeq');this.receiptSeqValue=Number(meta?.value||0);if(!this.receiptSeqInput)this.receiptSeqInput=this.nextReceiptNumber();this.after()},
  get isViewer(){return this.auth.role==='viewer'},get title(){return {'dashboard':'Dashboard inicial','prestadores':'Cadastro de prestadores','servicos':'Serviços','pagamentos':'Central de pagamentos','recibos':'Emissão de recibos','relatorios':'Relatórios','busca':'Busca global','config':'Configurações'}[this.view]||'ReciboCondo'},get selectedProvider(){return this.providers.find(p=>Number(p.id)===Number(this.selectedProviderId))||null},
  get cards(){return this.dashboardCards},
  get yearlyRows(){let y=String(this.dashboardYear||new Date().getFullYear());return this.services.filter(s=>String(s.paymentDate||s.serviceDate||'').startsWith(y))},
  get dashboardCards(){let nowRef=new Date().toISOString().slice(0,7), year=this.yearlyRows, month=year.filter(s=>String(s.paymentDate||s.serviceDate||'').slice(0,7)===nowRef), paidYear=year.reduce((a,s)=>a+Number(s.paidValue||0),0), paidMonth=month.reduce((a,s)=>a+Number(s.paidValue||0),0), countMonth=month.filter(s=>Number(s.paidValue||0)>0).length, open=year.reduce((a,s)=>a+this.balance(s),0), top=this.annualCategorySummary[0], topYear=this.topPaidNames(year), topMonth=this.topPaidNames(month), openTop=this.topOpenNames(year), catTop=this.annualCategorySummary.slice(0,3).map(c=>c.category+' • '+this.money(c.total)), activeCats=[...new Set(this.providers.map(p=>p.category).filter(Boolean))].slice(0,4), receiptsMonth=this.receipts.filter(r=>String(r.paymentDate||r.date||'').slice(0,7)===nowRef).length;return[
    {label:'Pago no ano',value:this.money(paidYear),hint:String(this.dashboardYear),icon:'wallet-cards',color:'text-rc-600',tooltipTitle:'Resumo anual',tooltipMain:this.money(paidYear),tooltipTag:'Ano '+this.dashboardYear,tooltip:['Total efetivamente pago no ano selecionado.', 'Top recebimentos: '+(topYear.length?topYear.join(' | '):'sem pagamentos lançados.'), 'Use para comparar o volume anual com o relatório anual premium.']},
    {label:'Pago no mês',value:this.money(paidMonth),hint:this.month(nowRef),icon:'circle-dollar-sign',color:'text-emerald-600',tooltipTitle:'Resumo do mês',tooltipMain:this.money(paidMonth),tooltipTag:'Mês atual',tooltip:['Mês de referência: '+this.month(nowRef)+'.', 'Quem recebeu: '+(topMonth.length?topMonth.join(' | '):'sem pagamentos neste mês.'), 'Ajuda a conferir rapidamente o fechamento mensal.']},
    {label:'Pagamentos no mês',value:countMonth,hint:'registros pagos',icon:'receipt-text',color:'text-blue-600',tooltipTitle:'Movimento mensal',tooltipMain:countMonth+' pagamento(s)',tooltipTag:'Recibos '+receiptsMonth,tooltip:['Valor médio por pagamento: '+this.money(countMonth?paidMonth/countMonth:0)+'.', 'Recibos emitidos no mês: '+receiptsMonth+'.', 'Bom para conferir se todos os pagamentos pagos já têm recibo.']},
    {label:'Prestadores ativos',value:this.providers.length,hint:'cadastrados',icon:'users',color:'text-rc-600',tooltipTitle:'Base de prestadores',tooltipMain:this.providers.length+' cadastro(s)',tooltipTag:'Cadastros',tooltip:['Categorias em uso: '+(activeCats.length?activeCats.join(', '):'nenhuma categoria cadastrada.'), 'Use para localizar quem presta serviço recorrente ou avulso.', 'Dica: mantenha PIX, WhatsApp e categoria sempre atualizados.']},
    {label:'Em aberto no ano',value:this.money(open),hint:'pendente/parcial',icon:'triangle-alert',color:'text-red-600',tooltipTitle:'Pendências do ano',tooltipMain:this.money(open),tooltipTag:'Atenção',tooltip:['Saldo lançado e ainda não quitado no ano.', 'Maiores pendências: '+(openTop.length?openTop.join(' | '):'nenhuma pendência no ano.'), 'Depois de pagar, registre o pagamento antes de emitir o recibo.']},
    {label:'Maior categoria',value:top?top.category:'-',hint:top?this.money(top.total):'sem dados',icon:'trophy',color:'text-yellow-600',tooltipTitle:'Categoria líder',tooltipMain:top?top.category:'Sem dados',tooltipTag:top?this.money(top.total):'Sem dados',tooltip:['Categoria com maior volume pago no ano.', 'Ranking: '+(catTop.length?catTop.join(' | '):'sem categorias pagas no ano.'), 'Ajuda a identificar onde o condomínio mais gastou.']}
  ]},
  get annualT

===== receiptSeq @ 6448108 =====
).slice(0,10),amount:0,location:'',description:'',paymentDate:'',paymentMethod:'',paidValue:0},
  recurrenceForm:{id:null,providerId:'',category:'Jardineiro',amount:0,dueDay:5,paymentDay:5,paymentMethod:'PIX',location:'',description:'',active:true},
  serviceFilter:'todos',serviceCategoryFilter:'todas',serviceProviderFilter:'todos',serviceMonthFilter:'',paymentMonthFilter:new Date().toISOString().slice(0,7),paymentStatusFilter:'todos',paymentProviderFilter:'todos',receiptFilter:{q:'',month:'',category:'todas'},receiptServiceId:'',currentReceipt:null,dashboardYear:new Date().getFullYear(),payModal:false,paymentTarget:null,payment:{date:new Date().toISOString().slice(0,10),value:0,method:'PIX'},actionModal:{open:false,type:'',tone:'default',icon:'',target:null,service:null,title:'',subtitle:'',note:'',confirmLabel:'',details:[]},toasts:[],
  report:{from:new Date(new Date().getFullYear(),new Date().getMonth(),1).toISOString().slice(0,10),to:new Date().toISOString().slice(0,10),category:'todas',status:'todos'},passwords:{admin:'',viewer:''},
  async init(){document.documentElement.classList.toggle('dark',this.dark);window.alert=(msg)=>this.toast(String(msg||''),this.toastType(String(msg||'')));if('serviceWorker'in navigator){navigator.serviceWorker.register('./sw.js').catch(console.warn)}await this.seed();this.refreshIcons()},
  async seed(){let s=await db.settings.get('main');if(s)this.settings=s.value;await this.load();},
  async load(){this.providers=await db.providers.toArray();this.services=await db.services.toArray();this.receipts=await db.receipts.toArray();this.recurrences=await db.recurrences.toArray();this.logs=(await db.logs.orderBy('at').reverse().limit(30).toArray());let meta=await db.meta.get('receiptSeq');this.receiptSeqValue=Number(meta?.value||0);if(!this.receiptSeqInput)this.receiptSeqInput=this.nextReceiptNumber();this.after()},
  get isViewer(){return this.auth.role==='viewer'},get title(){return {'dashboard':'Dashboard inicial','prestadores':'Cadastro de prestadores','servicos':'Serviços','pagamentos':'Central de pagamentos','recibos':'Emissão de recibos','relatorios':'Relatórios','busca':'Busca global','config':'Configurações'}[this.view]||'ReciboCondo'},get selectedProvider(){return this.providers.find(p=>Number(p.id)===Number(this.selectedProviderId))||null},
  get cards(){return this.dashboardCards},
  get yearlyRows(){let y=String(this.dashboardYear||new Date().getFullYear());return this.services.filter(s=>String(s.paymentDate||s.serviceDate||'').startsWith(y))},
  get dashboardCards(){let nowRef=new Date().toISOString().slice(0,7), year=this.yearlyRows, month=year.filter(s=>String(s.paymentDate||s.serviceDate||'').slice(0,7)===nowRef), paidYear=year.reduce((a,s)=>a+Number(s.paidValue||0),0), paidMonth=month.reduce((a,s)=>a+Number(s.paidValue||0),0), countMonth=month.filter(s=>Number(s.paidValue||0)>0).length, open=year.reduce((a,s)=>a+this.balance(s),0), top=this.annualCategorySummary[0], topYear=this.topPaidNames(year), topMonth=this.topPaidNames(month), openTop=this.topOpenNames(year), catTop=this.annualCategorySummary.slice(0,3).map(c=>c.category+' • '+this.money(c.total)), activeCats=[...new Set(this.providers.map(p=>p.category).filter(Boolean))].slice(0,4), receiptsMonth=this.receipts.filter(r=>String(r.paymentDate||r.date||'').slice(0,7)===nowRef).length;return[
    {label:'Pago no ano',value:this.money(paidYear),hint:String(this.dashboardYear),icon:'wallet-cards',color:'text-rc-600',tooltipTitle:'Resumo anual',tooltipMain:this.money(paidYear),tooltipTag:'Ano '+this.dashboardYear,tooltip:['Total efetivamente pago no ano selecionado.', 'Top recebimentos: '+(topYear.length?topYear.join(' | '):'sem pagamentos lançados.'), 'Use para comparar o volume anual com o relatório anual premium.']},
    {label:'Pago no mês',value:this.money(paidMonth),hint:this.month(nowRef),icon:'circle-dollar-sign',color:'text-emerald-600',tooltipTitle:'Resumo do mês',tooltipMain:this.money(paidMonth),tooltipTag:'Mês atual',tooltip:['Mês de referência: '+this.month(nowRef)+'.', 'Quem recebeu: '+(topMonth.length?topMonth.join(' | '):'sem pagamentos neste mês.'), 'Ajuda a conferir rapidamente o fechamento mensal.']},
    {label:'Pagamentos no mês',value:countMonth,hint:'registros pagos',icon:'receipt-text',color:'text-blue-600',tooltipTitle:'Movimento mensal',tooltipMain:countMonth+' pagamento(s)',tooltipTag:'Recibos '+receiptsMonth,tooltip:['Valor médio por pagamento: '+this.money(countMonth?paidMonth/countMonth:0)+'.', 'Recibos emitidos no mês: '+receiptsMonth+'.', 'Bom para conferir se todos os pagamentos pagos já têm recibo.']},
    {label:'Prestadores ativos',value:this.providers.length,hint:'cadastrados',icon:'users',color:'text-rc-600',tooltipTitle:'Base de prestadores',tooltipMain:this.providers.length+' cadastro(s)',tooltipTag:'Cadastros',tooltip:['Categorias em uso: '+(activeCats.length?activeCats.join(', '):'nenhuma categoria cadastrada.'), 'Use para localizar quem presta serviço recorrente ou avulso.', 'Dica: mantenha PIX, WhatsApp e categoria sempre atualizados.']},
    {label:'Em aberto no ano',value:this.money(open),hint:'pendente/parcial',icon:'triangle-alert',color:'text-red-600',tooltipTitle:'Pendências do ano',tooltipMain:this.money(open),tooltipTag:'Atenção',tooltip:['Saldo lançado e ainda não quitado no ano.', 'Maiores pendências: '+(openTop.length?openTop.join(' | '):'nenhuma pendência no ano.'), 'Depois de pagar, registre o pagamento antes de emitir o recibo.']},
    {label:'Maior categoria',value:top?top.category:'-',hint:top?this.money(top.total):'sem dados',icon:'trophy',color:'text-yellow-600',tooltipTitle:'Categoria líder',tooltipMain:top?top.category:'Sem dados',tooltipTag:top?this.money(top.total):'Sem dados',tooltip:['Categoria com maior volume pago no ano.', 'Ranking: '+(catTop.length?catTop.join(' | '):'sem categorias pagas no ano.'), 'Ajuda a identificar onde o condomínio mais gastou.']}
  ]},
  get annualTotals(){let paid=this.yearlyRows.reduce((a,s)=>a

===== receiptSeq @ 6448129 =====
0,location:'',description:'',paymentDate:'',paymentMethod:'',paidValue:0},
  recurrenceForm:{id:null,providerId:'',category:'Jardineiro',amount:0,dueDay:5,paymentDay:5,paymentMethod:'PIX',location:'',description:'',active:true},
  serviceFilter:'todos',serviceCategoryFilter:'todas',serviceProviderFilter:'todos',serviceMonthFilter:'',paymentMonthFilter:new Date().toISOString().slice(0,7),paymentStatusFilter:'todos',paymentProviderFilter:'todos',receiptFilter:{q:'',month:'',category:'todas'},receiptServiceId:'',currentReceipt:null,dashboardYear:new Date().getFullYear(),payModal:false,paymentTarget:null,payment:{date:new Date().toISOString().slice(0,10),value:0,method:'PIX'},actionModal:{open:false,type:'',tone:'default',icon:'',target:null,service:null,title:'',subtitle:'',note:'',confirmLabel:'',details:[]},toasts:[],
  report:{from:new Date(new Date().getFullYear(),new Date().getMonth(),1).toISOString().slice(0,10),to:new Date().toISOString().slice(0,10),category:'todas',status:'todos'},passwords:{admin:'',viewer:''},
  async init(){document.documentElement.classList.toggle('dark',this.dark);window.alert=(msg)=>this.toast(String(msg||''),this.toastType(String(msg||'')));if('serviceWorker'in navigator){navigator.serviceWorker.register('./sw.js').catch(console.warn)}await this.seed();this.refreshIcons()},
  async seed(){let s=await db.settings.get('main');if(s)this.settings=s.value;await this.load();},
  async load(){this.providers=await db.providers.toArray();this.services=await db.services.toArray();this.receipts=await db.receipts.toArray();this.recurrences=await db.recurrences.toArray();this.logs=(await db.logs.orderBy('at').reverse().limit(30).toArray());let meta=await db.meta.get('receiptSeq');this.receiptSeqValue=Number(meta?.value||0);if(!this.receiptSeqInput)this.receiptSeqInput=this.nextReceiptNumber();this.after()},
  get isViewer(){return this.auth.role==='viewer'},get title(){return {'dashboard':'Dashboard inicial','prestadores':'Cadastro de prestadores','servicos':'Serviços','pagamentos':'Central de pagamentos','recibos':'Emissão de recibos','relatorios':'Relatórios','busca':'Busca global','config':'Configurações'}[this.view]||'ReciboCondo'},get selectedProvider(){return this.providers.find(p=>Number(p.id)===Number(this.selectedProviderId))||null},
  get cards(){return this.dashboardCards},
  get yearlyRows(){let y=String(this.dashboardYear||new Date().getFullYear());return this.services.filter(s=>String(s.paymentDate||s.serviceDate||'').startsWith(y))},
  get dashboardCards(){let nowRef=new Date().toISOString().slice(0,7), year=this.yearlyRows, month=year.filter(s=>String(s.paymentDate||s.serviceDate||'').slice(0,7)===nowRef), paidYear=year.reduce((a,s)=>a+Number(s.paidValue||0),0), paidMonth=month.reduce((a,s)=>a+Number(s.paidValue||0),0), countMonth=month.filter(s=>Number(s.paidValue||0)>0).length, open=year.reduce((a,s)=>a+this.balance(s),0), top=this.annualCategorySummary[0], topYear=this.topPaidNames(year), topMonth=this.topPaidNames(month), openTop=this.topOpenNames(year), catTop=this.annualCategorySummary.slice(0,3).map(c=>c.category+' • '+this.money(c.total)), activeCats=[...new Set(this.providers.map(p=>p.category).filter(Boolean))].slice(0,4), receiptsMonth=this.receipts.filter(r=>String(r.paymentDate||r.date||'').slice(0,7)===nowRef).length;return[
    {label:'Pago no ano',value:this.money(paidYear),hint:String(this.dashboardYear),icon:'wallet-cards',color:'text-rc-600',tooltipTitle:'Resumo anual',tooltipMain:this.money(paidYear),tooltipTag:'Ano '+this.dashboardYear,tooltip:['Total efetivamente pago no ano selecionado.', 'Top recebimentos: '+(topYear.length?topYear.join(' | '):'sem pagamentos lançados.'), 'Use para comparar o volume anual com o relatório anual premium.']},
    {label:'Pago no mês',value:this.money(paidMonth),hint:this.month(nowRef),icon:'circle-dollar-sign',color:'text-emerald-600',tooltipTitle:'Resumo do mês',tooltipMain:this.money(paidMonth),tooltipTag:'Mês atual',tooltip:['Mês de referência: '+this.month(nowRef)+'.', 'Quem recebeu: '+(topMonth.length?topMonth.join(' | '):'sem pagamentos neste mês.'), 'Ajuda a conferir rapidamente o fechamento mensal.']},
    {label:'Pagamentos no mês',value:countMonth,hint:'registros pagos',icon:'receipt-text',color:'text-blue-600',tooltipTitle:'Movimento mensal',tooltipMain:countMonth+' pagamento(s)',tooltipTag:'Recibos '+receiptsMonth,tooltip:['Valor médio por pagamento: '+this.money(countMonth?paidMonth/countMonth:0)+'.', 'Recibos emitidos no mês: '+receiptsMonth+'.', 'Bom para conferir se todos os pagamentos pagos já têm recibo.']},
    {label:'Prestadores ativos',value:this.providers.length,hint:'cadastrados',icon:'users',color:'text-rc-600',tooltipTitle:'Base de prestadores',tooltipMain:this.providers.length+' cadastro(s)',tooltipTag:'Cadastros',tooltip:['Categorias em uso: '+(activeCats.length?activeCats.join(', '):'nenhuma categoria cadastrada.'), 'Use para localizar quem presta serviço recorrente ou avulso.', 'Dica: mantenha PIX, WhatsApp e categoria sempre atualizados.']},
    {label:'Em aberto no ano',value:this.money(open),hint:'pendente/parcial',icon:'triangle-alert',color:'text-red-600',tooltipTitle:'Pendências do ano',tooltipMain:this.money(open),tooltipTag:'Atenção',tooltip:['Saldo lançado e ainda não quitado no ano.', 'Maiores pendências: '+(openTop.length?openTop.join(' | '):'nenhuma pendência no ano.'), 'Depois de pagar, registre o pagamento antes de emitir o recibo.']},
    {label:'Maior categoria',value:top?top.category:'-',hint:top?this.money(top.total):'sem dados',icon:'trophy',color:'text-yellow-600',tooltipTitle:'Categoria líder',tooltipMain:top?top.category:'Sem dados',tooltipTag:top?this.money(top.total):'Sem dados',tooltip:['Categoria com maior volume pago no ano.', 'Ranking: '+(catTop.length?catTop.join(' | '):'sem categorias pagas no ano.'), 'Ajuda a identificar onde o condomínio mais gastou.']}
  ]},
  get annualTotals(){let paid=this.yearlyRows.reduce((a,s)=>a+Number(s.paidValue||

===== receiptSeq @ 6487509 =====
load();this.toast(status==='Pago'?'Pagamento registrado. Lançamento quitado.':'Pagamento parcial registrado. Saldo restante: '+this.money(Math.max(0,Number(s.amount||0)-paid)))},prepareReceipt(s){this.receiptServiceId=s.id;this.view='recibos';this.after()},async quickReceipt(s){if(this.isViewer)return;if(Number(s.paidValue||0)<=0)return this.toast('Registre o pagamento antes de emitir o recibo.','warning');let linked=this.receipts.filter(r=>Number(r.serviceId)===Number(s.id)),issued=linked.reduce((a,r)=>a+Number(r.total||0),0),pending=Math.max(0,Number(s.paidValue||0)-issued);if(pending<=0){let latest=linked.slice().sort((a,b)=>Number(b.id||0)-Number(a.id||0))[0];if(latest){this.currentReceipt=latest;this.view='recibos';this.after();this.toast('Todos os pagamentos registrados já possuem recibo.','warning');setTimeout(()=>this.printReceipt(),220)}else this.toast('Não há valor pago pendente de recibo.','warning');return}this.receiptServiceId=s.id;await this.issueReceipt();this.view='recibos';this.after();setTimeout(()=>this.printReceipt(),260)},async issueReceipt(){if(this.isViewer)return;let s=this.services.find(x=>Number(x.id)===Number(this.receiptServiceId));if(!s)return this.toast('Selecione um lançamento.','warning');let linked=this.receipts.filter(r=>Number(r.serviceId)===Number(s.id)),issued=linked.reduce((a,r)=>a+Number(r.total||0),0),pending=Math.max(0,Number(s.paidValue||0)-issued);if(pending<=0)return this.toast('Todos os pagamentos registrados deste lançamento já possuem recibo.','warning');let desc=String(s.description||'').replace(/(\bparcela\s+)(\d+)\s*\/\s*(\d+)/i,(m,prefix,current,total)=>prefix+String(linked.length+1).padStart(current.length,'0')+'/'+total);let p=this.providers.find(x=>Number(x.id)===Number(s.providerId))||{};let meta=await db.meta.get('receiptSeq'),next=Math.max(Number(meta?.value||0),this.receiptMaxNumber())+1;await db.meta.put({key:'receiptSeq',value:next});let r={number:String(next).padStart(6,'0'),date:this.today(),paymentDate:s.paymentDate||this.today(),providerId:p.id,providerName:p.name||'Prestador não informado',document:p.document||'',category:s.category,reference:s.reference,description:desc,total:pending,paymentMethod:s.paymentMethod||'Não informado',location:s.location,serviceId:s.id,manager:this.settings.manager,condoName:this.settings.condoName};let id=await db.receipts.add(r);r.id=id;this.currentReceipt=r;await this.logAction('receipt','Recibo emitido','Nº '+r.number+' • '+r.providerName+' • '+this.money(r.total||0));await this.load();this.toast('Recibo nº '+r.number+' emitido no valor de '+this.money(r.total||0)+'.')},printReceipt(){if(!this.currentReceipt&&this.receipts.length)this.currentReceipt=this.receipts[this.receipts.length-1];document.getElementById('receiptPrint').classList.remove('hidden');document.getElementById('reportPrint').classList.add('hidden');document.getElementById('annualReportPrint').classList.add('hidden');setTimeout(()=>window.print(),100)},async removeReceiptInternal(id){let r=this.receipts.find(x=>Number(x.id)===Number(id));if(!r)return;let linked=this.receipts.filter(x=>Number(x.serviceId)===Number(r.serviceId)).sort((a,b)=>Number(b.id||0)-Number(a.id||0)),latest=linked[0];if(latest&&Number(latest.id)!==Number(r.id))return this.toast('Para preservar a data e a forma de pagamento das parcelas, somente o recibo mais recente deste lançamento pode ser excluído e reemitido.','warning');await db.receipts.delete(id);await this.logAction('delete','Recibo excluído',r?('Nº '+r.number+' • '+r.providerName):('ID '+id));await this.load()},
  printReport(){document.body.classList.add('printing-report');document.getElementById('receiptPrint').classList.add('hidden');document.getElementById('annualReportPrint').classList.add('hidden');document.getElementById('reportPrint').classList.remove('hidden');setTimeout(()=>{window.print();document.body.classList.remove('printing-report');document.getElementById('receiptPrint').classList.remove('hidden');document.getElementById('annualReportPrint').classList.add('hidden');document.getElementById('reportPrint').classList.add('hidden')},100)},printAnnualReport(){document.body.classList.add('printing-annual');let rp=document.getElementById('receiptPrint'),mp=document.getElementById('reportPrint'),ap=document.getElementById('annualReportPrint');rp.classList.add('hidden');mp.classList.add('hidden');ap.classList.remove('hidden');ap.style.display='block';ap.style.visibility='visible';setTimeout(()=>{window.print();document.body.classList.remove('printing-annual');rp.classList.remove('hidden');ap.classList.add('hidden');ap.style.display='';ap.style.visibility='';mp.classList.add('hidden')},180)},csv(){let rows=[['Recibo nº','Data do serviço','Data do pagamento','Prestador','Categoria','Descrição','Valor do serviço','Valor deste pagamento','Status','Forma']].concat(this.reportDetailRows.map(s=>[s.receiptNumber,s.serviceDate,s.paymentDate||'',this.providerName(s.providerId),s.category,s.description,s.amount,s.reportValue||0,s.status,s.paymentMethod||'']));let csv=rows.map(r=>r.map(v=>'"'+String(v??'').replaceAll('"','""')+'"').join(';')).join('\n');this.download('relatorio-prestadores-recibocondo.csv',csv,'text/csv;charset=utf-8')},
  async saveSettings(){let plainSettings=JSON.parse(JSON.stringify(this.settings));await db.settings.put({key:'main',value:plainSettings});await this.logAction('settings','Configurações salvas','Dados do condomínio atualizados.');this.toast('Configurações salvas')},async savePasswords(){if(this.isViewer)return;let cur=JSON.parse(localStorage.getItem('rc_passwords')||'{"admin":"123456","viewer":"123456"}');if(this.passwords.admin)cur.admin=this.passwords.admin;if(this.passwords.viewer)cur.viewer=this.passwords.viewer;localStorage.setItem('rc_passwords',JSON.stringify(cur));await this.logAction('settings','Senhas atualizadas','Senhas locais do app foram atualizadas.');this.passwords={admin:'',viewer:''};this.toast('Senhas atualizadas')},async backup(){let now=new Date()

===== receiptSeq @ 6487610 =====
strado. Saldo restante: '+this.money(Math.max(0,Number(s.amount||0)-paid)))},prepareReceipt(s){this.receiptServiceId=s.id;this.view='recibos';this.after()},async quickReceipt(s){if(this.isViewer)return;if(Number(s.paidValue||0)<=0)return this.toast('Registre o pagamento antes de emitir o recibo.','warning');let linked=this.receipts.filter(r=>Number(r.serviceId)===Number(s.id)),issued=linked.reduce((a,r)=>a+Number(r.total||0),0),pending=Math.max(0,Number(s.paidValue||0)-issued);if(pending<=0){let latest=linked.slice().sort((a,b)=>Number(b.id||0)-Number(a.id||0))[0];if(latest){this.currentReceipt=latest;this.view='recibos';this.after();this.toast('Todos os pagamentos registrados já possuem recibo.','warning');setTimeout(()=>this.printReceipt(),220)}else this.toast('Não há valor pago pendente de recibo.','warning');return}this.receiptServiceId=s.id;await this.issueReceipt();this.view='recibos';this.after();setTimeout(()=>this.printReceipt(),260)},async issueReceipt(){if(this.isViewer)return;let s=this.services.find(x=>Number(x.id)===Number(this.receiptServiceId));if(!s)return this.toast('Selecione um lançamento.','warning');let linked=this.receipts.filter(r=>Number(r.serviceId)===Number(s.id)),issued=linked.reduce((a,r)=>a+Number(r.total||0),0),pending=Math.max(0,Number(s.paidValue||0)-issued);if(pending<=0)return this.toast('Todos os pagamentos registrados deste lançamento já possuem recibo.','warning');let desc=String(s.description||'').replace(/(\bparcela\s+)(\d+)\s*\/\s*(\d+)/i,(m,prefix,current,total)=>prefix+String(linked.length+1).padStart(current.length,'0')+'/'+total);let p=this.providers.find(x=>Number(x.id)===Number(s.providerId))||{};let meta=await db.meta.get('receiptSeq'),next=Math.max(Number(meta?.value||0),this.receiptMaxNumber())+1;await db.meta.put({key:'receiptSeq',value:next});let r={number:String(next).padStart(6,'0'),date:this.today(),paymentDate:s.paymentDate||this.today(),providerId:p.id,providerName:p.name||'Prestador não informado',document:p.document||'',category:s.category,reference:s.reference,description:desc,total:pending,paymentMethod:s.paymentMethod||'Não informado',location:s.location,serviceId:s.id,manager:this.settings.manager,condoName:this.settings.condoName};let id=await db.receipts.add(r);r.id=id;this.currentReceipt=r;await this.logAction('receipt','Recibo emitido','Nº '+r.number+' • '+r.providerName+' • '+this.money(r.total||0));await this.load();this.toast('Recibo nº '+r.number+' emitido no valor de '+this.money(r.total||0)+'.')},printReceipt(){if(!this.currentReceipt&&this.receipts.length)this.currentReceipt=this.receipts[this.receipts.length-1];document.getElementById('receiptPrint').classList.remove('hidden');document.getElementById('reportPrint').classList.add('hidden');document.getElementById('annualReportPrint').classList.add('hidden');setTimeout(()=>window.print(),100)},async removeReceiptInternal(id){let r=this.receipts.find(x=>Number(x.id)===Number(id));if(!r)return;let linked=this.receipts.filter(x=>Number(x.serviceId)===Number(r.serviceId)).sort((a,b)=>Number(b.id||0)-Number(a.id||0)),latest=linked[0];if(latest&&Number(latest.id)!==Number(r.id))return this.toast('Para preservar a data e a forma de pagamento das parcelas, somente o recibo mais recente deste lançamento pode ser excluído e reemitido.','warning');await db.receipts.delete(id);await this.logAction('delete','Recibo excluído',r?('Nº '+r.number+' • '+r.providerName):('ID '+id));await this.load()},
  printReport(){document.body.classList.add('printing-report');document.getElementById('receiptPrint').classList.add('hidden');document.getElementById('annualReportPrint').classList.add('hidden');document.getElementById('reportPrint').classList.remove('hidden');setTimeout(()=>{window.print();document.body.classList.remove('printing-report');document.getElementById('receiptPrint').classList.remove('hidden');document.getElementById('annualReportPrint').classList.add('hidden');document.getElementById('reportPrint').classList.add('hidden')},100)},printAnnualReport(){document.body.classList.add('printing-annual');let rp=document.getElementById('receiptPrint'),mp=document.getElementById('reportPrint'),ap=document.getElementById('annualReportPrint');rp.classList.add('hidden');mp.classList.add('hidden');ap.classList.remove('hidden');ap.style.display='block';ap.style.visibility='visible';setTimeout(()=>{window.print();document.body.classList.remove('printing-annual');rp.classList.remove('hidden');ap.classList.add('hidden');ap.style.display='';ap.style.visibility='';mp.classList.add('hidden')},180)},csv(){let rows=[['Recibo nº','Data do serviço','Data do pagamento','Prestador','Categoria','Descrição','Valor do serviço','Valor deste pagamento','Status','Forma']].concat(this.reportDetailRows.map(s=>[s.receiptNumber,s.serviceDate,s.paymentDate||'',this.providerName(s.providerId),s.category,s.description,s.amount,s.reportValue||0,s.status,s.paymentMethod||'']));let csv=rows.map(r=>r.map(v=>'"'+String(v??'').replaceAll('"','""')+'"').join(';')).join('\n');this.download('relatorio-prestadores-recibocondo.csv',csv,'text/csv;charset=utf-8')},
  async saveSettings(){let plainSettings=JSON.parse(JSON.stringify(this.settings));await db.settings.put({key:'main',value:plainSettings});await this.logAction('settings','Configurações salvas','Dados do condomínio atualizados.');this.toast('Configurações salvas')},async savePasswords(){if(this.isViewer)return;let cur=JSON.parse(localStorage.getItem('rc_passwords')||'{"admin":"123456","viewer":"123456"}');if(this.passwords.admin)cur.admin=this.passwords.admin;if(this.passwords.viewer)cur.viewer=this.passwords.viewer;localStorage.setItem('rc_passwords',JSON.stringify(cur));await this.logAction('settings','Senhas atualizadas','Senhas locais do app foram atualizadas.');this.passwords={admin:'',viewer:''};this.toast('Senhas atualizadas')},async backup(){let now=new Date().toISOString();let seqMeta=await db.meta.get('receiptSeq');let data={settings:this.settings,providers

===== receiptSeq @ 6491755 =====
tElementById('reportPrint'),ap=document.getElementById('annualReportPrint');rp.classList.add('hidden');mp.classList.add('hidden');ap.classList.remove('hidden');ap.style.display='block';ap.style.visibility='visible';setTimeout(()=>{window.print();document.body.classList.remove('printing-annual');rp.classList.remove('hidden');ap.classList.add('hidden');ap.style.display='';ap.style.visibility='';mp.classList.add('hidden')},180)},csv(){let rows=[['Recibo nº','Data do serviço','Data do pagamento','Prestador','Categoria','Descrição','Valor do serviço','Valor deste pagamento','Status','Forma']].concat(this.reportDetailRows.map(s=>[s.receiptNumber,s.serviceDate,s.paymentDate||'',this.providerName(s.providerId),s.category,s.description,s.amount,s.reportValue||0,s.status,s.paymentMethod||'']));let csv=rows.map(r=>r.map(v=>'"'+String(v??'').replaceAll('"','""')+'"').join(';')).join('\n');this.download('relatorio-prestadores-recibocondo.csv',csv,'text/csv;charset=utf-8')},
  async saveSettings(){let plainSettings=JSON.parse(JSON.stringify(this.settings));await db.settings.put({key:'main',value:plainSettings});await this.logAction('settings','Configurações salvas','Dados do condomínio atualizados.');this.toast('Configurações salvas')},async savePasswords(){if(this.isViewer)return;let cur=JSON.parse(localStorage.getItem('rc_passwords')||'{"admin":"123456","viewer":"123456"}');if(this.passwords.admin)cur.admin=this.passwords.admin;if(this.passwords.viewer)cur.viewer=this.passwords.viewer;localStorage.setItem('rc_passwords',JSON.stringify(cur));await this.logAction('settings','Senhas atualizadas','Senhas locais do app foram atualizadas.');this.passwords={admin:'',viewer:''};this.toast('Senhas atualizadas')},async backup(){let now=new Date().toISOString();let seqMeta=await db.meta.get('receiptSeq');let data={settings:this.settings,providers:this.providers,services:this.services,receipts:this.receipts,recurrences:this.recurrences,logs:this.logs,receiptSeq:Number(seqMeta?.value||this.receiptMaxNumber()||0),backupStats:this.backupStats(),exportedAt:now};this.download('backup-recibocondo-prestadores.json',JSON.stringify(data,null,2),'application/json');this.backupLast=now;localStorage.setItem('rc_last_backup',now);await this.logAction('backup','Backup exportado','Backup JSON gerado com '+data.backupStats.records+' registros.')},async restore(ev){let f=ev.target.files[0];if(!f)return;try{let data=JSON.parse(await f.text());if(!data||typeof data!=='object')throw new Error('Backup inválido');let providers=Array.isArray(data.providers)?data.providers:[],services=Array.isArray(data.services)?data.services:[],receipts=Array.isArray(data.receipts)?data.receipts:[],recurrences=Array.isArray(data.recurrences)?data.recurrences:[],logs=Array.isArray(data.logs)?data.logs:[];await db.transaction('rw',db.providers,db.services,db.receipts,db.recurrences,db.logs,db.settings,db.meta,async()=>{await db.providers.clear();await db.services.clear();await db.receipts.clear();await db.recurrences.clear();await db.logs.clear();if(data.settings){let restoredSettings=JSON.parse(JSON.stringify(data.settings));await db.settings.put({key:'main',value:restoredSettings});this.settings=restoredSettings}if(providers.length)await db.providers.bulkAdd(providers);if(services.length)await db.services.bulkAdd(services);if(receipts.length)await db.receipts.bulkAdd(receipts);if(recurrences.length)await db.recurrences.bulkAdd(recurrences);if(logs.length)await db.logs.bulkAdd(logs.map(l=>{let x={...l};delete x.id;return x}).slice(0,200));let restoredMax=receipts.reduce((m,r)=>r.source==='Excel legado'?m:Math.max(m,Number(String(r.number||'').replace(/\D/g,'')||0)),0),safeSeq=Math.max(Number(data.receiptSeq||0),restoredMax);await db.meta.put({key:'receiptSeq',value:safeSeq})});await this.logAction('restore','Backup restaurado','Dados restaurados por arquivo JSON.');await this.load();this.toast('Backup restaurado com sucesso!')}catch(e){console.error(e);this.toast('Não foi possível restaurar o backup: '+e.message,'error')}},async clearAllInternal(){await db.transaction('rw',db.providers,db.services,db.receipts,db.recurrences,db.logs,async()=>{await db.providers.clear();await db.services.clear();await db.receipts.clear();await db.recurrences.clear();await db.logs.clear()});await this.load()},async clearAll(){this.openClearAllModal()},readLogo(e){let f=e.target.files[0];if(!f)return;let r=new FileReader();r.onload=()=>this.settings.logo=r.result;r.readAsDataURL(f)},
  receiptMaxNumber(){return this.receipts.reduce((max,r)=>r.source==='Excel legado'?max:Math.max(max,Number(String(r.number||'').replace(/\D/g,'')||0)),0)},nextReceiptNumber(){return Math.max(Number(this.receiptSeqValue||0),this.receiptMaxNumber())+1},receiptSequenceStatus(){return Number(this.receiptSeqValue||0)<this.receiptMaxNumber()?'Sincronizar':'Sequência OK'},async saveReceiptSequence(){if(this.isViewer)return;let target=Number(this.receiptSeqInput||0), max=this.receiptMaxNumber();if(!target||target<1)return this.toast('Informe um próximo número válido.','warning');if(target<=max)return this.toast('O número precisa ser maior que o último emitido: '+String(max).padStart(6,'0')+'.','warning');await db.meta.put({key:'receiptSeq',value:target-1});this.receiptSeqValue=target-1;await this.logAction('settings','Sequência de recibos ajustada','Próximo recibo definido para nº '+String(target).padStart(6,'0'));this.toast('Sequência atualizada. Próximo recibo: '+String(target).padStart(6,'0'));await this.load()},
  topPaidNames(rows){let map={};rows.forEach(s=>{let v=Number(s.paidValue||0);if(v<=0)return;let name=this.providerName(s.providerId);map[name]=(map[name]||0)+v});return Object.entries(map).sort((a,b)=>b[1]-a[1]).slice(0,3).map(([name,total])=>name+' • '+this.money(total))},topOpenNames(rows){let map={};rows.forEach(s=>{let v=this.balance(s);if(v<=0)return;let name=this.providerName(s.providerId);map[name]=(map[name]||0)+v});return Object.entries(map).sort((a,b)=>b[1]-a[1]).slice(0,3).m

===== receiptSeq @ 6491916 =====
p.style.display='block';ap.style.visibility='visible';setTimeout(()=>{window.print();document.body.classList.remove('printing-annual');rp.classList.remove('hidden');ap.classList.add('hidden');ap.style.display='';ap.style.visibility='';mp.classList.add('hidden')},180)},csv(){let rows=[['Recibo nº','Data do serviço','Data do pagamento','Prestador','Categoria','Descrição','Valor do serviço','Valor deste pagamento','Status','Forma']].concat(this.reportDetailRows.map(s=>[s.receiptNumber,s.serviceDate,s.paymentDate||'',this.providerName(s.providerId),s.category,s.description,s.amount,s.reportValue||0,s.status,s.paymentMethod||'']));let csv=rows.map(r=>r.map(v=>'"'+String(v??'').replaceAll('"','""')+'"').join(';')).join('\n');this.download('relatorio-prestadores-recibocondo.csv',csv,'text/csv;charset=utf-8')},
  async saveSettings(){let plainSettings=JSON.parse(JSON.stringify(this.settings));await db.settings.put({key:'main',value:plainSettings});await this.logAction('settings','Configurações salvas','Dados do condomínio atualizados.');this.toast('Configurações salvas')},async savePasswords(){if(this.isViewer)return;let cur=JSON.parse(localStorage.getItem('rc_passwords')||'{"admin":"123456","viewer":"123456"}');if(this.passwords.admin)cur.admin=this.passwords.admin;if(this.passwords.viewer)cur.viewer=this.passwords.viewer;localStorage.setItem('rc_passwords',JSON.stringify(cur));await this.logAction('settings','Senhas atualizadas','Senhas locais do app foram atualizadas.');this.passwords={admin:'',viewer:''};this.toast('Senhas atualizadas')},async backup(){let now=new Date().toISOString();let seqMeta=await db.meta.get('receiptSeq');let data={settings:this.settings,providers:this.providers,services:this.services,receipts:this.receipts,recurrences:this.recurrences,logs:this.logs,receiptSeq:Number(seqMeta?.value||this.receiptMaxNumber()||0),backupStats:this.backupStats(),exportedAt:now};this.download('backup-recibocondo-prestadores.json',JSON.stringify(data,null,2),'application/json');this.backupLast=now;localStorage.setItem('rc_last_backup',now);await this.logAction('backup','Backup exportado','Backup JSON gerado com '+data.backupStats.records+' registros.')},async restore(ev){let f=ev.target.files[0];if(!f)return;try{let data=JSON.parse(await f.text());if(!data||typeof data!=='object')throw new Error('Backup inválido');let providers=Array.isArray(data.providers)?data.providers:[],services=Array.isArray(data.services)?data.services:[],receipts=Array.isArray(data.receipts)?data.receipts:[],recurrences=Array.isArray(data.recurrences)?data.recurrences:[],logs=Array.isArray(data.logs)?data.logs:[];await db.transaction('rw',db.providers,db.services,db.receipts,db.recurrences,db.logs,db.settings,db.meta,async()=>{await db.providers.clear();await db.services.clear();await db.receipts.clear();await db.recurrences.clear();await db.logs.clear();if(data.settings){let restoredSettings=JSON.parse(JSON.stringify(data.settings));await db.settings.put({key:'main',value:restoredSettings});this.settings=restoredSettings}if(providers.length)await db.providers.bulkAdd(providers);if(services.length)await db.services.bulkAdd(services);if(receipts.length)await db.receipts.bulkAdd(receipts);if(recurrences.length)await db.recurrences.bulkAdd(recurrences);if(logs.length)await db.logs.bulkAdd(logs.map(l=>{let x={...l};delete x.id;return x}).slice(0,200));let restoredMax=receipts.reduce((m,r)=>r.source==='Excel legado'?m:Math.max(m,Number(String(r.number||'').replace(/\D/g,'')||0)),0),safeSeq=Math.max(Number(data.receiptSeq||0),restoredMax);await db.meta.put({key:'receiptSeq',value:safeSeq})});await this.logAction('restore','Backup restaurado','Dados restaurados por arquivo JSON.');await this.load();this.toast('Backup restaurado com sucesso!')}catch(e){console.error(e);this.toast('Não foi possível restaurar o backup: '+e.message,'error')}},async clearAllInternal(){await db.transaction('rw',db.providers,db.services,db.receipts,db.recurrences,db.logs,async()=>{await db.providers.clear();await db.services.clear();await db.receipts.clear();await db.recurrences.clear();await db.logs.clear()});await this.load()},async clearAll(){this.openClearAllModal()},readLogo(e){let f=e.target.files[0];if(!f)return;let r=new FileReader();r.onload=()=>this.settings.logo=r.result;r.readAsDataURL(f)},
  receiptMaxNumber(){return this.receipts.reduce((max,r)=>r.source==='Excel legado'?max:Math.max(max,Number(String(r.number||'').replace(/\D/g,'')||0)),0)},nextReceiptNumber(){return Math.max(Number(this.receiptSeqValue||0),this.receiptMaxNumber())+1},receiptSequenceStatus(){return Number(this.receiptSeqValue||0)<this.receiptMaxNumber()?'Sincronizar':'Sequência OK'},async saveReceiptSequence(){if(this.isViewer)return;let target=Number(this.receiptSeqInput||0), max=this.receiptMaxNumber();if(!target||target<1)return this.toast('Informe um próximo número válido.','warning');if(target<=max)return this.toast('O número precisa ser maior que o último emitido: '+String(max).padStart(6,'0')+'.','warning');await db.meta.put({key:'receiptSeq',value:target-1});this.receiptSeqValue=target-1;await this.logAction('settings','Sequência de recibos ajustada','Próximo recibo definido para nº '+String(target).padStart(6,'0'));this.toast('Sequência atualizada. Próximo recibo: '+String(target).padStart(6,'0'));await this.load()},
  topPaidNames(rows){let map={};rows.forEach(s=>{let v=Number(s.paidValue||0);if(v<=0)return;let name=this.providerName(s.providerId);map[name]=(map[name]||0)+v});return Object.entries(map).sort((a,b)=>b[1]-a[1]).slice(0,3).map(([name,total])=>name+' • '+this.money(total))},topOpenNames(rows){let map={};rows.forEach(s=>{let v=this.balance(s);if(v<=0)return;let name=this.providerName(s.providerId);map[name]=(map[name]||0)+v});return Object.entries(map).sort((a,b)=>b[1]-a[1]).slice(0,3).map(([name,total])=>name+' • '+this.money(total))},
  providerServices(id){return this.services.filter(s=>Number(s.providerId)===Number(id)).sort((a,b)=>String(b.

===== receiptSeq @ 6493657 =====
:this.receipts,recurrences:this.recurrences,logs:this.logs,receiptSeq:Number(seqMeta?.value||this.receiptMaxNumber()||0),backupStats:this.backupStats(),exportedAt:now};this.download('backup-recibocondo-prestadores.json',JSON.stringify(data,null,2),'application/json');this.backupLast=now;localStorage.setItem('rc_last_backup',now);await this.logAction('backup','Backup exportado','Backup JSON gerado com '+data.backupStats.records+' registros.')},async restore(ev){let f=ev.target.files[0];if(!f)return;try{let data=JSON.parse(await f.text());if(!data||typeof data!=='object')throw new Error('Backup inválido');let providers=Array.isArray(data.providers)?data.providers:[],services=Array.isArray(data.services)?data.services:[],receipts=Array.isArray(data.receipts)?data.receipts:[],recurrences=Array.isArray(data.recurrences)?data.recurrences:[],logs=Array.isArray(data.logs)?data.logs:[];await db.transaction('rw',db.providers,db.services,db.receipts,db.recurrences,db.logs,db.settings,db.meta,async()=>{await db.providers.clear();await db.services.clear();await db.receipts.clear();await db.recurrences.clear();await db.logs.clear();if(data.settings){let restoredSettings=JSON.parse(JSON.stringify(data.settings));await db.settings.put({key:'main',value:restoredSettings});this.settings=restoredSettings}if(providers.length)await db.providers.bulkAdd(providers);if(services.length)await db.services.bulkAdd(services);if(receipts.length)await db.receipts.bulkAdd(receipts);if(recurrences.length)await db.recurrences.bulkAdd(recurrences);if(logs.length)await db.logs.bulkAdd(logs.map(l=>{let x={...l};delete x.id;return x}).slice(0,200));let restoredMax=receipts.reduce((m,r)=>r.source==='Excel legado'?m:Math.max(m,Number(String(r.number||'').replace(/\D/g,'')||0)),0),safeSeq=Math.max(Number(data.receiptSeq||0),restoredMax);await db.meta.put({key:'receiptSeq',value:safeSeq})});await this.logAction('restore','Backup restaurado','Dados restaurados por arquivo JSON.');await this.load();this.toast('Backup restaurado com sucesso!')}catch(e){console.error(e);this.toast('Não foi possível restaurar o backup: '+e.message,'error')}},async clearAllInternal(){await db.transaction('rw',db.providers,db.services,db.receipts,db.recurrences,db.logs,async()=>{await db.providers.clear();await db.services.clear();await db.receipts.clear();await db.recurrences.clear();await db.logs.clear()});await this.load()},async clearAll(){this.openClearAllModal()},readLogo(e){let f=e.target.files[0];if(!f)return;let r=new FileReader();r.onload=()=>this.settings.logo=r.result;r.readAsDataURL(f)},
  receiptMaxNumber(){return this.receipts.reduce((max,r)=>r.source==='Excel legado'?max:Math.max(max,Number(String(r.number||'').replace(/\D/g,'')||0)),0)},nextReceiptNumber(){return Math.max(Number(this.receiptSeqValue||0),this.receiptMaxNumber())+1},receiptSequenceStatus(){return Number(this.receiptSeqValue||0)<this.receiptMaxNumber()?'Sincronizar':'Sequência OK'},async saveReceiptSequence(){if(this.isViewer)return;let target=Number(this.receiptSeqInput||0), max=this.receiptMaxNumber();if(!target||target<1)return this.toast('Informe um próximo número válido.','warning');if(target<=max)return this.toast('O número precisa ser maior que o último emitido: '+String(max).padStart(6,'0')+'.','warning');await db.meta.put({key:'receiptSeq',value:target-1});this.receiptSeqValue=target-1;await this.logAction('settings','Sequência de recibos ajustada','Próximo recibo definido para nº '+String(target).padStart(6,'0'));this.toast('Sequência atualizada. Próximo recibo: '+String(target).padStart(6,'0'));await this.load()},
  topPaidNames(rows){let map={};rows.forEach(s=>{let v=Number(s.paidValue||0);if(v<=0)return;let name=this.providerName(s.providerId);map[name]=(map[name]||0)+v});return Object.entries(map).sort((a,b)=>b[1]-a[1]).slice(0,3).map(([name,total])=>name+' • '+this.money(total))},topOpenNames(rows){let map={};rows.forEach(s=>{let v=this.balance(s);if(v<=0)return;let name=this.providerName(s.providerId);map[name]=(map[name]||0)+v});return Object.entries(map).sort((a,b)=>b[1]-a[1]).slice(0,3).map(([name,total])=>name+' • '+this.money(total))},
  providerServices(id){return this.services.filter(s=>Number(s.providerId)===Number(id)).sort((a,b)=>String(b.serviceDate||'').localeCompare(String(a.serviceDate||'')))},providerReceipts(id){return this.receipts.filter(r=>Number(r.providerId)===Number(id)).sort((a,b)=>String(b.date||'').localeCompare(String(a.date||'')))},providerLastServiceLabel(id){let s=this.providerServices(id)[0];return s?this.fmt(s.serviceDate)+' • '+this.money(s.paidValue||s.amount):'-'},async logAction(type,title,detail){try{await db.logs.add({at:new Date().toISOString(),type,title,detail,role:this.auth?.role||'sistema'});this.logs=(await db.logs.orderBy('at').reverse().limit(30).toArray())}catch(e){console.warn('logAction',e)}},logIcon(type){return {payment:'circle-dollar-sign',receipt:'receipt-text',service:'clipboard-list',provider:'users',recurrence:'repeat-2',backup:'shield-check',restore:'rotate-ccw',danger:'triangle-alert',settings:'settings',delete:'trash-2'}[type]||'history'},fmtDateTime(d){if(!d)return'-';return new Date(d).toLocaleString('pt-BR')},async clearLogs(){if(this.isViewer)return;await db.logs.clear();this.logs=[];this.after()},providerSpend(id){return this.services.filter(s=>Number(s.providerId)===Number(id)).reduce((a,s)=>a+Number(s.paidValue||s.amount||0),0)},providerOpen(id){return this.services.filter(s=>Number(s.providerId)===Number(id)).reduce((a,s)=>a+this.balance(s),0)},openWhatsapp(p){let raw=String(p.whatsapp||p.phone||'').replace(/\D/g,'');if(!raw)return this.toast('Este prestador não tem WhatsApp/telefone cadastrado.','warning');let phone=raw.startsWith('55')?raw:'55'+raw;let msg=encodeURIComponent('Olá, '+p.name+'. Aqui é do '+this.settings.condoName+'.');window.open('https://wa.me/'+phone+'?text='+msg,'_blank')},overdueLabel(s){if(!s.dueDate||this.balance(s)<=0)return'';let today=new Date(this.today()+'T00:00:0

===== receiptSeq @ 6493709 =====
s.logs,receiptSeq:Number(seqMeta?.value||this.receiptMaxNumber()||0),backupStats:this.backupStats(),exportedAt:now};this.download('backup-recibocondo-prestadores.json',JSON.stringify(data,null,2),'application/json');this.backupLast=now;localStorage.setItem('rc_last_backup',now);await this.logAction('backup','Backup exportado','Backup JSON gerado com '+data.backupStats.records+' registros.')},async restore(ev){let f=ev.target.files[0];if(!f)return;try{let data=JSON.parse(await f.text());if(!data||typeof data!=='object')throw new Error('Backup inválido');let providers=Array.isArray(data.providers)?data.providers:[],services=Array.isArray(data.services)?data.services:[],receipts=Array.isArray(data.receipts)?data.receipts:[],recurrences=Array.isArray(data.recurrences)?data.recurrences:[],logs=Array.isArray(data.logs)?data.logs:[];await db.transaction('rw',db.providers,db.services,db.receipts,db.recurrences,db.logs,db.settings,db.meta,async()=>{await db.providers.clear();await db.services.clear();await db.receipts.clear();await db.recurrences.clear();await db.logs.clear();if(data.settings){let restoredSettings=JSON.parse(JSON.stringify(data.settings));await db.settings.put({key:'main',value:restoredSettings});this.settings=restoredSettings}if(providers.length)await db.providers.bulkAdd(providers);if(services.length)await db.services.bulkAdd(services);if(receipts.length)await db.receipts.bulkAdd(receipts);if(recurrences.length)await db.recurrences.bulkAdd(recurrences);if(logs.length)await db.logs.bulkAdd(logs.map(l=>{let x={...l};delete x.id;return x}).slice(0,200));let restoredMax=receipts.reduce((m,r)=>r.source==='Excel legado'?m:Math.max(m,Number(String(r.number||'').replace(/\D/g,'')||0)),0),safeSeq=Math.max(Number(data.receiptSeq||0),restoredMax);await db.meta.put({key:'receiptSeq',value:safeSeq})});await this.logAction('restore','Backup restaurado','Dados restaurados por arquivo JSON.');await this.load();this.toast('Backup restaurado com sucesso!')}catch(e){console.error(e);this.toast('Não foi possível restaurar o backup: '+e.message,'error')}},async clearAllInternal(){await db.transaction('rw',db.providers,db.services,db.receipts,db.recurrences,db.logs,async()=>{await db.providers.clear();await db.services.clear();await db.receipts.clear();await db.recurrences.clear();await db.logs.clear()});await this.load()},async clearAll(){this.openClearAllModal()},readLogo(e){let f=e.target.files[0];if(!f)return;let r=new FileReader();r.onload=()=>this.settings.logo=r.result;r.readAsDataURL(f)},
  receiptMaxNumber(){return this.receipts.reduce((max,r)=>r.source==='Excel legado'?max:Math.max(max,Number(String(r.number||'').replace(/\D/g,'')||0)),0)},nextReceiptNumber(){return Math.max(Number(this.receiptSeqValue||0),this.receiptMaxNumber())+1},receiptSequenceStatus(){return Number(this.receiptSeqValue||0)<this.receiptMaxNumber()?'Sincronizar':'Sequência OK'},async saveReceiptSequence(){if(this.isViewer)return;let target=Number(this.receiptSeqInput||0), max=this.receiptMaxNumber();if(!target||target<1)return this.toast('Informe um próximo número válido.','warning');if(target<=max)return this.toast('O número precisa ser maior que o último emitido: '+String(max).padStart(6,'0')+'.','warning');await db.meta.put({key:'receiptSeq',value:target-1});this.receiptSeqValue=target-1;await this.logAction('settings','Sequência de recibos ajustada','Próximo recibo definido para nº '+String(target).padStart(6,'0'));this.toast('Sequência atualizada. Próximo recibo: '+String(target).padStart(6,'0'));await this.load()},
  topPaidNames(rows){let map={};rows.forEach(s=>{let v=Number(s.paidValue||0);if(v<=0)return;let name=this.providerName(s.providerId);map[name]=(map[name]||0)+v});return Object.entries(map).sort((a,b)=>b[1]-a[1]).slice(0,3).map(([name,total])=>name+' • '+this.money(total))},topOpenNames(rows){let map={};rows.forEach(s=>{let v=this.balance(s);if(v<=0)return;let name=this.providerName(s.providerId);map[name]=(map[name]||0)+v});return Object.entries(map).sort((a,b)=>b[1]-a[1]).slice(0,3).map(([name,total])=>name+' • '+this.money(total))},
  providerServices(id){return this.services.filter(s=>Number(s.providerId)===Number(id)).sort((a,b)=>String(b.serviceDate||'').localeCompare(String(a.serviceDate||'')))},providerReceipts(id){return this.receipts.filter(r=>Number(r.providerId)===Number(id)).sort((a,b)=>String(b.date||'').localeCompare(String(a.date||'')))},providerLastServiceLabel(id){let s=this.providerServices(id)[0];return s?this.fmt(s.serviceDate)+' • '+this.money(s.paidValue||s.amount):'-'},async logAction(type,title,detail){try{await db.logs.add({at:new Date().toISOString(),type,title,detail,role:this.auth?.role||'sistema'});this.logs=(await db.logs.orderBy('at').reverse().limit(30).toArray())}catch(e){console.warn('logAction',e)}},logIcon(type){return {payment:'circle-dollar-sign',receipt:'receipt-text',service:'clipboard-list',provider:'users',recurrence:'repeat-2',backup:'shield-check',restore:'rotate-ccw',danger:'triangle-alert',settings:'settings',delete:'trash-2'}[type]||'history'},fmtDateTime(d){if(!d)return'-';return new Date(d).toLocaleString('pt-BR')},async clearLogs(){if(this.isViewer)return;await db.logs.clear();this.logs=[];this.after()},providerSpend(id){return this.services.filter(s=>Number(s.providerId)===Number(id)).reduce((a,s)=>a+Number(s.paidValue||s.amount||0),0)},providerOpen(id){return this.services.filter(s=>Number(s.providerId)===Number(id)).reduce((a,s)=>a+this.balance(s),0)},openWhatsapp(p){let raw=String(p.whatsapp||p.phone||'').replace(/\D/g,'');if(!raw)return this.toast('Este prestador não tem WhatsApp/telefone cadastrado.','warning');let phone=raw.startsWith('55')?raw:'55'+raw;let msg=encodeURIComponent('Olá, '+p.name+'. Aqui é do '+this.settings.condoName+'.');window.open('https://wa.me/'+phone+'?text='+msg,'_blank')},overdueLabel(s){if(!s.dueDate||this.balance(s)<=0)return'';let today=new Date(this.today()+'T00:00:00'), due=new Date(s.dueDate+'T00:00:00'), days=Math.

===== receiptSeq @ 6494643 =====
s,db.meta,async()=>{await db.providers.clear();await db.services.clear();await db.receipts.clear();await db.recurrences.clear();await db.logs.clear();if(data.settings){let restoredSettings=JSON.parse(JSON.stringify(data.settings));await db.settings.put({key:'main',value:restoredSettings});this.settings=restoredSettings}if(providers.length)await db.providers.bulkAdd(providers);if(services.length)await db.services.bulkAdd(services);if(receipts.length)await db.receipts.bulkAdd(receipts);if(recurrences.length)await db.recurrences.bulkAdd(recurrences);if(logs.length)await db.logs.bulkAdd(logs.map(l=>{let x={...l};delete x.id;return x}).slice(0,200));let restoredMax=receipts.reduce((m,r)=>r.source==='Excel legado'?m:Math.max(m,Number(String(r.number||'').replace(/\D/g,'')||0)),0),safeSeq=Math.max(Number(data.receiptSeq||0),restoredMax);await db.meta.put({key:'receiptSeq',value:safeSeq})});await this.logAction('restore','Backup restaurado','Dados restaurados por arquivo JSON.');await this.load();this.toast('Backup restaurado com sucesso!')}catch(e){console.error(e);this.toast('Não foi possível restaurar o backup: '+e.message,'error')}},async clearAllInternal(){await db.transaction('rw',db.providers,db.services,db.receipts,db.recurrences,db.logs,async()=>{await db.providers.clear();await db.services.clear();await db.receipts.clear();await db.recurrences.clear();await db.logs.clear()});await this.load()},async clearAll(){this.openClearAllModal()},readLogo(e){let f=e.target.files[0];if(!f)return;let r=new FileReader();r.onload=()=>this.settings.logo=r.result;r.readAsDataURL(f)},
  receiptMaxNumber(){return this.receipts.reduce((max,r)=>r.source==='Excel legado'?max:Math.max(max,Number(String(r.number||'').replace(/\D/g,'')||0)),0)},nextReceiptNumber(){return Math.max(Number(this.receiptSeqValue||0),this.receiptMaxNumber())+1},receiptSequenceStatus(){return Number(this.receiptSeqValue||0)<this.receiptMaxNumber()?'Sincronizar':'Sequência OK'},async saveReceiptSequence(){if(this.isViewer)return;let target=Number(this.receiptSeqInput||0), max=this.receiptMaxNumber();if(!target||target<1)return this.toast('Informe um próximo número válido.','warning');if(target<=max)return this.toast('O número precisa ser maior que o último emitido: '+String(max).padStart(6,'0')+'.','warning');await db.meta.put({key:'receiptSeq',value:target-1});this.receiptSeqValue=target-1;await this.logAction('settings','Sequência de recibos ajustada','Próximo recibo definido para nº '+String(target).padStart(6,'0'));this.toast('Sequência atualizada. Próximo recibo: '+String(target).padStart(6,'0'));await this.load()},
  topPaidNames(rows){let map={};rows.forEach(s=>{let v=Number(s.paidValue||0);if(v<=0)return;let name=this.providerName(s.providerId);map[name]=(map[name]||0)+v});return Object.entries(map).sort((a,b)=>b[1]-a[1]).slice(0,3).map(([name,total])=>name+' • '+this.money(total))},topOpenNames(rows){let map={};rows.forEach(s=>{let v=this.balance(s);if(v<=0)return;let name=this.providerName(s.providerId);map[name]=(map[name]||0)+v});return Object.entries(map).sort((a,b)=>b[1]-a[1]).slice(0,3).map(([name,total])=>name+' • '+this.money(total))},
  providerServices(id){return this.services.filter(s=>Number(s.providerId)===Number(id)).sort((a,b)=>String(b.serviceDate||'').localeCompare(String(a.serviceDate||'')))},providerReceipts(id){return this.receipts.filter(r=>Number(r.providerId)===Number(id)).sort((a,b)=>String(b.date||'').localeCompare(String(a.date||'')))},providerLastServiceLabel(id){let s=this.providerServices(id)[0];return s?this.fmt(s.serviceDate)+' • '+this.money(s.paidValue||s.amount):'-'},async logAction(type,title,detail){try{await db.logs.add({at:new Date().toISOString(),type,title,detail,role:this.auth?.role||'sistema'});this.logs=(await db.logs.orderBy('at').reverse().limit(30).toArray())}catch(e){console.warn('logAction',e)}},logIcon(type){return {payment:'circle-dollar-sign',receipt:'receipt-text',service:'clipboard-list',provider:'users',recurrence:'repeat-2',backup:'shield-check',restore:'rotate-ccw',danger:'triangle-alert',settings:'settings',delete:'trash-2'}[type]||'history'},fmtDateTime(d){if(!d)return'-';return new Date(d).toLocaleString('pt-BR')},async clearLogs(){if(this.isViewer)return;await db.logs.clear();this.logs=[];this.after()},providerSpend(id){return this.services.filter(s=>Number(s.providerId)===Number(id)).reduce((a,s)=>a+Number(s.paidValue||s.amount||0),0)},providerOpen(id){return this.services.filter(s=>Number(s.providerId)===Number(id)).reduce((a,s)=>a+this.balance(s),0)},openWhatsapp(p){let raw=String(p.whatsapp||p.phone||'').replace(/\D/g,'');if(!raw)return this.toast('Este prestador não tem WhatsApp/telefone cadastrado.','warning');let phone=raw.startsWith('55')?raw:'55'+raw;let msg=encodeURIComponent('Olá, '+p.name+'. Aqui é do '+this.settings.condoName+'.');window.open('https://wa.me/'+phone+'?text='+msg,'_blank')},overdueLabel(s){if(!s.dueDate||this.balance(s)<=0)return'';let today=new Date(this.today()+'T00:00:00'), due=new Date(s.dueDate+'T00:00:00'), days=Math.ceil((due-today)/86400000);if(days<0)return Math.abs(days)+' dia(s) em atraso';if(days===0)return'vence hoje';return'faltam '+days+' dia(s)'},providerName(id){return (this.providers.find(p=>Number(p.id)===Number(id))||{}).name||'-'},reportReceiptNumber(service){let r=this.receipts.find(x=>Number(x.serviceId)===Number(service.id));return r?.number||'-';},balance(s){return Math.max(0,Number(s.amount||0)-Number(s.paidValue||0))},statusClass(st){return st==='Pago'?'bg-emerald-500/10 text-emerald-700':st==='Parcial'?'bg-amber-500/10 text-amber-700':'bg-red-500/10 text-red-700'},valorPorExtenso(v){v=Number(v||0);let reais=Math.floor(v),cent=Math.round((v-reais)*100);let base={0:'zero',1:'um',2:'dois',3:'três',4:'quatro',5:'cinco',6:'seis',7:'sete',8:'oito',9:'nove',10:'dez',11:'onze',12:'doze',13:'treze',14:'quatorze',15:'quinze',16:'dezesseis',17:'dezessete',18:'dezoito',19:'dezenove',20:'vinte',30:'trinta',40:'quarenta',50:'

===== receiptSeq @ 6494691 =====
wait db.services.clear();await db.receipts.clear();await db.recurrences.clear();await db.logs.clear();if(data.settings){let restoredSettings=JSON.parse(JSON.stringify(data.settings));await db.settings.put({key:'main',value:restoredSettings});this.settings=restoredSettings}if(providers.length)await db.providers.bulkAdd(providers);if(services.length)await db.services.bulkAdd(services);if(receipts.length)await db.receipts.bulkAdd(receipts);if(recurrences.length)await db.recurrences.bulkAdd(recurrences);if(logs.length)await db.logs.bulkAdd(logs.map(l=>{let x={...l};delete x.id;return x}).slice(0,200));let restoredMax=receipts.reduce((m,r)=>r.source==='Excel legado'?m:Math.max(m,Number(String(r.number||'').replace(/\D/g,'')||0)),0),safeSeq=Math.max(Number(data.receiptSeq||0),restoredMax);await db.meta.put({key:'receiptSeq',value:safeSeq})});await this.logAction('restore','Backup restaurado','Dados restaurados por arquivo JSON.');await this.load();this.toast('Backup restaurado com sucesso!')}catch(e){console.error(e);this.toast('Não foi possível restaurar o backup: '+e.message,'error')}},async clearAllInternal(){await db.transaction('rw',db.providers,db.services,db.receipts,db.recurrences,db.logs,async()=>{await db.providers.clear();await db.services.clear();await db.receipts.clear();await db.recurrences.clear();await db.logs.clear()});await this.load()},async clearAll(){this.openClearAllModal()},readLogo(e){let f=e.target.files[0];if(!f)return;let r=new FileReader();r.onload=()=>this.settings.logo=r.result;r.readAsDataURL(f)},
  receiptMaxNumber(){return this.receipts.reduce((max,r)=>r.source==='Excel legado'?max:Math.max(max,Number(String(r.number||'').replace(/\D/g,'')||0)),0)},nextReceiptNumber(){return Math.max(Number(this.receiptSeqValue||0),this.receiptMaxNumber())+1},receiptSequenceStatus(){return Number(this.receiptSeqValue||0)<this.receiptMaxNumber()?'Sincronizar':'Sequência OK'},async saveReceiptSequence(){if(this.isViewer)return;let target=Number(this.receiptSeqInput||0), max=this.receiptMaxNumber();if(!target||target<1)return this.toast('Informe um próximo número válido.','warning');if(target<=max)return this.toast('O número precisa ser maior que o último emitido: '+String(max).padStart(6,'0')+'.','warning');await db.meta.put({key:'receiptSeq',value:target-1});this.receiptSeqValue=target-1;await this.logAction('settings','Sequência de recibos ajustada','Próximo recibo definido para nº '+String(target).padStart(6,'0'));this.toast('Sequência atualizada. Próximo recibo: '+String(target).padStart(6,'0'));await this.load()},
  topPaidNames(rows){let map={};rows.forEach(s=>{let v=Number(s.paidValue||0);if(v<=0)return;let name=this.providerName(s.providerId);map[name]=(map[name]||0)+v});return Object.entries(map).sort((a,b)=>b[1]-a[1]).slice(0,3).map(([name,total])=>name+' • '+this.money(total))},topOpenNames(rows){let map={};rows.forEach(s=>{let v=this.balance(s);if(v<=0)return;let name=this.providerName(s.providerId);map[name]=(map[name]||0)+v});return Object.entries(map).sort((a,b)=>b[1]-a[1]).slice(0,3).map(([name,total])=>name+' • '+this.money(total))},
  providerServices(id){return this.services.filter(s=>Number(s.providerId)===Number(id)).sort((a,b)=>String(b.serviceDate||'').localeCompare(String(a.serviceDate||'')))},providerReceipts(id){return this.receipts.filter(r=>Number(r.providerId)===Number(id)).sort((a,b)=>String(b.date||'').localeCompare(String(a.date||'')))},providerLastServiceLabel(id){let s=this.providerServices(id)[0];return s?this.fmt(s.serviceDate)+' • '+this.money(s.paidValue||s.amount):'-'},async logAction(type,title,detail){try{await db.logs.add({at:new Date().toISOString(),type,title,detail,role:this.auth?.role||'sistema'});this.logs=(await db.logs.orderBy('at').reverse().limit(30).toArray())}catch(e){console.warn('logAction',e)}},logIcon(type){return {payment:'circle-dollar-sign',receipt:'receipt-text',service:'clipboard-list',provider:'users',recurrence:'repeat-2',backup:'shield-check',restore:'rotate-ccw',danger:'triangle-alert',settings:'settings',delete:'trash-2'}[type]||'history'},fmtDateTime(d){if(!d)return'-';return new Date(d).toLocaleString('pt-BR')},async clearLogs(){if(this.isViewer)return;await db.logs.clear();this.logs=[];this.after()},providerSpend(id){return this.services.filter(s=>Number(s.providerId)===Number(id)).reduce((a,s)=>a+Number(s.paidValue||s.amount||0),0)},providerOpen(id){return this.services.filter(s=>Number(s.providerId)===Number(id)).reduce((a,s)=>a+this.balance(s),0)},openWhatsapp(p){let raw=String(p.whatsapp||p.phone||'').replace(/\D/g,'');if(!raw)return this.toast('Este prestador não tem WhatsApp/telefone cadastrado.','warning');let phone=raw.startsWith('55')?raw:'55'+raw;let msg=encodeURIComponent('Olá, '+p.name+'. Aqui é do '+this.settings.condoName+'.');window.open('https://wa.me/'+phone+'?text='+msg,'_blank')},overdueLabel(s){if(!s.dueDate||this.balance(s)<=0)return'';let today=new Date(this.today()+'T00:00:00'), due=new Date(s.dueDate+'T00:00:00'), days=Math.ceil((due-today)/86400000);if(days<0)return Math.abs(days)+' dia(s) em atraso';if(days===0)return'vence hoje';return'faltam '+days+' dia(s)'},providerName(id){return (this.providers.find(p=>Number(p.id)===Number(id))||{}).name||'-'},reportReceiptNumber(service){let r=this.receipts.find(x=>Number(x.serviceId)===Number(service.id));return r?.number||'-';},balance(s){return Math.max(0,Number(s.amount||0)-Number(s.paidValue||0))},statusClass(st){return st==='Pago'?'bg-emerald-500/10 text-emerald-700':st==='Parcial'?'bg-amber-500/10 text-amber-700':'bg-red-500/10 text-red-700'},valorPorExtenso(v){v=Number(v||0);let reais=Math.floor(v),cent=Math.round((v-reais)*100);let base={0:'zero',1:'um',2:'dois',3:'três',4:'quatro',5:'cinco',6:'seis',7:'sete',8:'oito',9:'nove',10:'dez',11:'onze',12:'doze',13:'treze',14:'quatorze',15:'quinze',16:'dezesseis',17:'dezessete',18:'dezoito',19:'dezenove',20:'vinte',30:'trinta',40:'quarenta',50:'cinquenta',60:'sessenta',70:'setenta',80:'oitent

===== receiptSeq @ 6494734 =====
clear();await db.recurrences.clear();await db.logs.clear();if(data.settings){let restoredSettings=JSON.parse(JSON.stringify(data.settings));await db.settings.put({key:'main',value:restoredSettings});this.settings=restoredSettings}if(providers.length)await db.providers.bulkAdd(providers);if(services.length)await db.services.bulkAdd(services);if(receipts.length)await db.receipts.bulkAdd(receipts);if(recurrences.length)await db.recurrences.bulkAdd(recurrences);if(logs.length)await db.logs.bulkAdd(logs.map(l=>{let x={...l};delete x.id;return x}).slice(0,200));let restoredMax=receipts.reduce((m,r)=>r.source==='Excel legado'?m:Math.max(m,Number(String(r.number||'').replace(/\D/g,'')||0)),0),safeSeq=Math.max(Number(data.receiptSeq||0),restoredMax);await db.meta.put({key:'receiptSeq',value:safeSeq})});await this.logAction('restore','Backup restaurado','Dados restaurados por arquivo JSON.');await this.load();this.toast('Backup restaurado com sucesso!')}catch(e){console.error(e);this.toast('Não foi possível restaurar o backup: '+e.message,'error')}},async clearAllInternal(){await db.transaction('rw',db.providers,db.services,db.receipts,db.recurrences,db.logs,async()=>{await db.providers.clear();await db.services.clear();await db.receipts.clear();await db.recurrences.clear();await db.logs.clear()});await this.load()},async clearAll(){this.openClearAllModal()},readLogo(e){let f=e.target.files[0];if(!f)return;let r=new FileReader();r.onload=()=>this.settings.logo=r.result;r.readAsDataURL(f)},
  receiptMaxNumber(){return this.receipts.reduce((max,r)=>r.source==='Excel legado'?max:Math.max(max,Number(String(r.number||'').replace(/\D/g,'')||0)),0)},nextReceiptNumber(){return Math.max(Number(this.receiptSeqValue||0),this.receiptMaxNumber())+1},receiptSequenceStatus(){return Number(this.receiptSeqValue||0)<this.receiptMaxNumber()?'Sincronizar':'Sequência OK'},async saveReceiptSequence(){if(this.isViewer)return;let target=Number(this.receiptSeqInput||0), max=this.receiptMaxNumber();if(!target||target<1)return this.toast('Informe um próximo número válido.','warning');if(target<=max)return this.toast('O número precisa ser maior que o último emitido: '+String(max).padStart(6,'0')+'.','warning');await db.meta.put({key:'receiptSeq',value:target-1});this.receiptSeqValue=target-1;await this.logAction('settings','Sequência de recibos ajustada','Próximo recibo definido para nº '+String(target).padStart(6,'0'));this.toast('Sequência atualizada. Próximo recibo: '+String(target).padStart(6,'0'));await this.load()},
  topPaidNames(rows){let map={};rows.forEach(s=>{let v=Number(s.paidValue||0);if(v<=0)return;let name=this.providerName(s.providerId);map[name]=(map[name]||0)+v});return Object.entries(map).sort((a,b)=>b[1]-a[1]).slice(0,3).map(([name,total])=>name+' • '+this.money(total))},topOpenNames(rows){let map={};rows.forEach(s=>{let v=this.balance(s);if(v<=0)return;let name=this.providerName(s.providerId);map[name]=(map[name]||0)+v});return Object.entries(map).sort((a,b)=>b[1]-a[1]).slice(0,3).map(([name,total])=>name+' • '+this.money(total))},
  providerServices(id){return this.services.filter(s=>Number(s.providerId)===Number(id)).sort((a,b)=>String(b.serviceDate||'').localeCompare(String(a.serviceDate||'')))},providerReceipts(id){return this.receipts.filter(r=>Number(r.providerId)===Number(id)).sort((a,b)=>String(b.date||'').localeCompare(String(a.date||'')))},providerLastServiceLabel(id){let s=this.providerServices(id)[0];return s?this.fmt(s.serviceDate)+' • '+this.money(s.paidValue||s.amount):'-'},async logAction(type,title,detail){try{await db.logs.add({at:new Date().toISOString(),type,title,detail,role:this.auth?.role||'sistema'});this.logs=(await db.logs.orderBy('at').reverse().limit(30).toArray())}catch(e){console.warn('logAction',e)}},logIcon(type){return {payment:'circle-dollar-sign',receipt:'receipt-text',service:'clipboard-list',provider:'users',recurrence:'repeat-2',backup:'shield-check',restore:'rotate-ccw',danger:'triangle-alert',settings:'settings',delete:'trash-2'}[type]||'history'},fmtDateTime(d){if(!d)return'-';return new Date(d).toLocaleString('pt-BR')},async clearLogs(){if(this.isViewer)return;await db.logs.clear();this.logs=[];this.after()},providerSpend(id){return this.services.filter(s=>Number(s.providerId)===Number(id)).reduce((a,s)=>a+Number(s.paidValue||s.amount||0),0)},providerOpen(id){return this.services.filter(s=>Number(s.providerId)===Number(id)).reduce((a,s)=>a+this.balance(s),0)},openWhatsapp(p){let raw=String(p.whatsapp||p.phone||'').replace(/\D/g,'');if(!raw)return this.toast('Este prestador não tem WhatsApp/telefone cadastrado.','warning');let phone=raw.startsWith('55')?raw:'55'+raw;let msg=encodeURIComponent('Olá, '+p.name+'. Aqui é do '+this.settings.condoName+'.');window.open('https://wa.me/'+phone+'?text='+msg,'_blank')},overdueLabel(s){if(!s.dueDate||this.balance(s)<=0)return'';let today=new Date(this.today()+'T00:00:00'), due=new Date(s.dueDate+'T00:00:00'), days=Math.ceil((due-today)/86400000);if(days<0)return Math.abs(days)+' dia(s) em atraso';if(days===0)return'vence hoje';return'faltam '+days+' dia(s)'},providerName(id){return (this.providers.find(p=>Number(p.id)===Number(id))||{}).name||'-'},reportReceiptNumber(service){let r=this.receipts.find(x=>Number(x.serviceId)===Number(service.id));return r?.number||'-';},balance(s){return Math.max(0,Number(s.amount||0)-Number(s.paidValue||0))},statusClass(st){return st==='Pago'?'bg-emerald-500/10 text-emerald-700':st==='Parcial'?'bg-amber-500/10 text-amber-700':'bg-red-500/10 text-red-700'},valorPorExtenso(v){v=Number(v||0);let reais=Math.floor(v),cent=Math.round((v-reais)*100);let base={0:'zero',1:'um',2:'dois',3:'três',4:'quatro',5:'cinco',6:'seis',7:'sete',8:'oito',9:'nove',10:'dez',11:'onze',12:'doze',13:'treze',14:'quatorze',15:'quinze',16:'dezesseis',17:'dezessete',18:'dezoito',19:'dezenove',20:'vinte',30:'trinta',40:'quarenta',50:'cinquenta',60:'sessenta',70:'setenta',80:'oitenta',90:'noventa',100:'cem'};let ext=(n)=>{if

===== receiptSeq @ 6494883 =====
settings.put({key:'main',value:restoredSettings});this.settings=restoredSettings}if(providers.length)await db.providers.bulkAdd(providers);if(services.length)await db.services.bulkAdd(services);if(receipts.length)await db.receipts.bulkAdd(receipts);if(recurrences.length)await db.recurrences.bulkAdd(recurrences);if(logs.length)await db.logs.bulkAdd(logs.map(l=>{let x={...l};delete x.id;return x}).slice(0,200));let restoredMax=receipts.reduce((m,r)=>r.source==='Excel legado'?m:Math.max(m,Number(String(r.number||'').replace(/\D/g,'')||0)),0),safeSeq=Math.max(Number(data.receiptSeq||0),restoredMax);await db.meta.put({key:'receiptSeq',value:safeSeq})});await this.logAction('restore','Backup restaurado','Dados restaurados por arquivo JSON.');await this.load();this.toast('Backup restaurado com sucesso!')}catch(e){console.error(e);this.toast('Não foi possível restaurar o backup: '+e.message,'error')}},async clearAllInternal(){await db.transaction('rw',db.providers,db.services,db.receipts,db.recurrences,db.logs,async()=>{await db.providers.clear();await db.services.clear();await db.receipts.clear();await db.recurrences.clear();await db.logs.clear()});await this.load()},async clearAll(){this.openClearAllModal()},readLogo(e){let f=e.target.files[0];if(!f)return;let r=new FileReader();r.onload=()=>this.settings.logo=r.result;r.readAsDataURL(f)},
  receiptMaxNumber(){return this.receipts.reduce((max,r)=>r.source==='Excel legado'?max:Math.max(max,Number(String(r.number||'').replace(/\D/g,'')||0)),0)},nextReceiptNumber(){return Math.max(Number(this.receiptSeqValue||0),this.receiptMaxNumber())+1},receiptSequenceStatus(){return Number(this.receiptSeqValue||0)<this.receiptMaxNumber()?'Sincronizar':'Sequência OK'},async saveReceiptSequence(){if(this.isViewer)return;let target=Number(this.receiptSeqInput||0), max=this.receiptMaxNumber();if(!target||target<1)return this.toast('Informe um próximo número válido.','warning');if(target<=max)return this.toast('O número precisa ser maior que o último emitido: '+String(max).padStart(6,'0')+'.','warning');await db.meta.put({key:'receiptSeq',value:target-1});this.receiptSeqValue=target-1;await this.logAction('settings','Sequência de recibos ajustada','Próximo recibo definido para nº '+String(target).padStart(6,'0'));this.toast('Sequência atualizada. Próximo recibo: '+String(target).padStart(6,'0'));await this.load()},
  topPaidNames(rows){let map={};rows.forEach(s=>{let v=Number(s.paidValue||0);if(v<=0)return;let name=this.providerName(s.providerId);map[name]=(map[name]||0)+v});return Object.entries(map).sort((a,b)=>b[1]-a[1]).slice(0,3).map(([name,total])=>name+' • '+this.money(total))},topOpenNames(rows){let map={};rows.forEach(s=>{let v=this.balance(s);if(v<=0)return;let name=this.providerName(s.providerId);map[name]=(map[name]||0)+v});return Object.entries(map).sort((a,b)=>b[1]-a[1]).slice(0,3).map(([name,total])=>name+' • '+this.money(total))},
  providerServices(id){return this.services.filter(s=>Number(s.providerId)===Number(id)).sort((a,b)=>String(b.serviceDate||'').localeCompare(String(a.serviceDate||'')))},providerReceipts(id){return this.receipts.filter(r=>Number(r.providerId)===Number(id)).sort((a,b)=>String(b.date||'').localeCompare(String(a.date||'')))},providerLastServiceLabel(id){let s=this.providerServices(id)[0];return s?this.fmt(s.serviceDate)+' • '+this.money(s.paidValue||s.amount):'-'},async logAction(type,title,detail){try{await db.logs.add({at:new Date().toISOString(),type,title,detail,role:this.auth?.role||'sistema'});this.logs=(await db.logs.orderBy('at').reverse().limit(30).toArray())}catch(e){console.warn('logAction',e)}},logIcon(type){return {payment:'circle-dollar-sign',receipt:'receipt-text',service:'clipboard-list',provider:'users',recurrence:'repeat-2',backup:'shield-check',restore:'rotate-ccw',danger:'triangle-alert',settings:'settings',delete:'trash-2'}[type]||'history'},fmtDateTime(d){if(!d)return'-';return new Date(d).toLocaleString('pt-BR')},async clearLogs(){if(this.isViewer)return;await db.logs.clear();this.logs=[];this.after()},providerSpend(id){return this.services.filter(s=>Number(s.providerId)===Number(id)).reduce((a,s)=>a+Number(s.paidValue||s.amount||0),0)},providerOpen(id){return this.services.filter(s=>Number(s.providerId)===Number(id)).reduce((a,s)=>a+this.balance(s),0)},openWhatsapp(p){let raw=String(p.whatsapp||p.phone||'').replace(/\D/g,'');if(!raw)return this.toast('Este prestador não tem WhatsApp/telefone cadastrado.','warning');let phone=raw.startsWith('55')?raw:'55'+raw;let msg=encodeURIComponent('Olá, '+p.name+'. Aqui é do '+this.settings.condoName+'.');window.open('https://wa.me/'+phone+'?text='+msg,'_blank')},overdueLabel(s){if(!s.dueDate||this.balance(s)<=0)return'';let today=new Date(this.today()+'T00:00:00'), due=new Date(s.dueDate+'T00:00:00'), days=Math.ceil((due-today)/86400000);if(days<0)return Math.abs(days)+' dia(s) em atraso';if(days===0)return'vence hoje';return'faltam '+days+' dia(s)'},providerName(id){return (this.providers.find(p=>Number(p.id)===Number(id))||{}).name||'-'},reportReceiptNumber(service){let r=this.receipts.find(x=>Number(x.serviceId)===Number(service.id));return r?.number||'-';},balance(s){return Math.max(0,Number(s.amount||0)-Number(s.paidValue||0))},statusClass(st){return st==='Pago'?'bg-emerald-500/10 text-emerald-700':st==='Parcial'?'bg-amber-500/10 text-amber-700':'bg-red-500/10 text-red-700'},valorPorExtenso(v){v=Number(v||0);let reais=Math.floor(v),cent=Math.round((v-reais)*100);let base={0:'zero',1:'um',2:'dois',3:'três',4:'quatro',5:'cinco',6:'seis',7:'sete',8:'oito',9:'nove',10:'dez',11:'onze',12:'doze',13:'treze',14:'quatorze',15:'quinze',16:'dezesseis',17:'dezessete',18:'dezoito',19:'dezenove',20:'vinte',30:'trinta',40:'quarenta',50:'cinquenta',60:'sessenta',70:'setenta',80:'oitenta',90:'noventa',100:'cem'};let ext=(n)=>{if(n<=20||base[n])return base[n];if(n<100){let d=Math.floor(n/10)*10,u=n%10;return base[d]+(u?' e '+base[u]:'')}if(n<1000){let c=Math.floor(n/100),r=n%

===== receiptSeq @ 6495170 =====
nces.bulkAdd(recurrences);if(logs.length)await db.logs.bulkAdd(logs.map(l=>{let x={...l};delete x.id;return x}).slice(0,200));let restoredMax=receipts.reduce((m,r)=>r.source==='Excel legado'?m:Math.max(m,Number(String(r.number||'').replace(/\D/g,'')||0)),0),safeSeq=Math.max(Number(data.receiptSeq||0),restoredMax);await db.meta.put({key:'receiptSeq',value:safeSeq})});await this.logAction('restore','Backup restaurado','Dados restaurados por arquivo JSON.');await this.load();this.toast('Backup restaurado com sucesso!')}catch(e){console.error(e);this.toast('Não foi possível restaurar o backup: '+e.message,'error')}},async clearAllInternal(){await db.transaction('rw',db.providers,db.services,db.receipts,db.recurrences,db.logs,async()=>{await db.providers.clear();await db.services.clear();await db.receipts.clear();await db.recurrences.clear();await db.logs.clear()});await this.load()},async clearAll(){this.openClearAllModal()},readLogo(e){let f=e.target.files[0];if(!f)return;let r=new FileReader();r.onload=()=>this.settings.logo=r.result;r.readAsDataURL(f)},
  receiptMaxNumber(){return this.receipts.reduce((max,r)=>r.source==='Excel legado'?max:Math.max(max,Number(String(r.number||'').replace(/\D/g,'')||0)),0)},nextReceiptNumber(){return Math.max(Number(this.receiptSeqValue||0),this.receiptMaxNumber())+1},receiptSequenceStatus(){return Number(this.receiptSeqValue||0)<this.receiptMaxNumber()?'Sincronizar':'Sequência OK'},async saveReceiptSequence(){if(this.isViewer)return;let target=Number(this.receiptSeqInput||0), max=this.receiptMaxNumber();if(!target||target<1)return this.toast('Informe um próximo número válido.','warning');if(target<=max)return this.toast('O número precisa ser maior que o último emitido: '+String(max).padStart(6,'0')+'.','warning');await db.meta.put({key:'receiptSeq',value:target-1});this.receiptSeqValue=target-1;await this.logAction('settings','Sequência de recibos ajustada','Próximo recibo definido para nº '+String(target).padStart(6,'0'));this.toast('Sequência atualizada. Próximo recibo: '+String(target).padStart(6,'0'));await this.load()},
  topPaidNames(rows){let map={};rows.forEach(s=>{let v=Number(s.paidValue||0);if(v<=0)return;let name=this.providerName(s.providerId);map[name]=(map[name]||0)+v});return Object.entries(map).sort((a,b)=>b[1]-a[1]).slice(0,3).map(([name,total])=>name+' • '+this.money(total))},topOpenNames(rows){let map={};rows.forEach(s=>{let v=this.balance(s);if(v<=0)return;let name=this.providerName(s.providerId);map[name]=(map[name]||0)+v});return Object.entries(map).sort((a,b)=>b[1]-a[1]).slice(0,3).map(([name,total])=>name+' • '+this.money(total))},
  providerServices(id){return this.services.filter(s=>Number(s.providerId)===Number(id)).sort((a,b)=>String(b.serviceDate||'').localeCompare(String(a.serviceDate||'')))},providerReceipts(id){return this.receipts.filter(r=>Number(r.providerId)===Number(id)).sort((a,b)=>String(b.date||'').localeCompare(String(a.date||'')))},providerLastServiceLabel(id){let s=this.providerServices(id)[0];return s?this.fmt(s.serviceDate)+' • '+this.money(s.paidValue||s.amount):'-'},async logAction(type,title,detail){try{await db.logs.add({at:new Date().toISOString(),type,title,detail,role:this.auth?.role||'sistema'});this.logs=(await db.logs.orderBy('at').reverse().limit(30).toArray())}catch(e){console.warn('logAction',e)}},logIcon(type){return {payment:'circle-dollar-sign',receipt:'receipt-text',service:'clipboard-list',provider:'users',recurrence:'repeat-2',backup:'shield-check',restore:'rotate-ccw',danger:'triangle-alert',settings:'settings',delete:'trash-2'}[type]||'history'},fmtDateTime(d){if(!d)return'-';return new Date(d).toLocaleString('pt-BR')},async clearLogs(){if(this.isViewer)return;await db.logs.clear();this.logs=[];this.after()},providerSpend(id){return this.services.filter(s=>Number(s.providerId)===Number(id)).reduce((a,s)=>a+Number(s.paidValue||s.amount||0),0)},providerOpen(id){return this.services.filter(s=>Number(s.providerId)===Number(id)).reduce((a,s)=>a+this.balance(s),0)},openWhatsapp(p){let raw=String(p.whatsapp||p.phone||'').replace(/\D/g,'');if(!raw)return this.toast('Este prestador não tem WhatsApp/telefone cadastrado.','warning');let phone=raw.startsWith('55')?raw:'55'+raw;let msg=encodeURIComponent('Olá, '+p.name+'. Aqui é do '+this.settings.condoName+'.');window.open('https://wa.me/'+phone+'?text='+msg,'_blank')},overdueLabel(s){if(!s.dueDate||this.balance(s)<=0)return'';let today=new Date(this.today()+'T00:00:00'), due=new Date(s.dueDate+'T00:00:00'), days=Math.ceil((due-today)/86400000);if(days<0)return Math.abs(days)+' dia(s) em atraso';if(days===0)return'vence hoje';return'faltam '+days+' dia(s)'},providerName(id){return (this.providers.find(p=>Number(p.id)===Number(id))||{}).name||'-'},reportReceiptNumber(service){let r=this.receipts.find(x=>Number(x.serviceId)===Number(service.id));return r?.number||'-';},balance(s){return Math.max(0,Number(s.amount||0)-Number(s.paidValue||0))},statusClass(st){return st==='Pago'?'bg-emerald-500/10 text-emerald-700':st==='Parcial'?'bg-amber-500/10 text-amber-700':'bg-red-500/10 text-red-700'},valorPorExtenso(v){v=Number(v||0);let reais=Math.floor(v),cent=Math.round((v-reais)*100);let base={0:'zero',1:'um',2:'dois',3:'três',4:'quatro',5:'cinco',6:'seis',7:'sete',8:'oito',9:'nove',10:'dez',11:'onze',12:'doze',13:'treze',14:'quatorze',15:'quinze',16:'dezesseis',17:'dezessete',18:'dezoito',19:'dezenove',20:'vinte',30:'trinta',40:'quarenta',50:'cinquenta',60:'sessenta',70:'setenta',80:'oitenta',90:'noventa',100:'cem'};let ext=(n)=>{if(n<=20||base[n])return base[n];if(n<100){let d=Math.floor(n/10)*10,u=n%10;return base[d]+(u?' e '+base[u]:'')}if(n<1000){let c=Math.floor(n/100),r=n%100,cn=c===1?'cento':base[c]+' centos';cn=cn.replace('dois centos','duzentos').replace('três centos','trezentos').replace('quatro centos','quatrocentos').replace('cinco centos','quinhentos').replace('seis centos','seiscentos').replace('sete centos','setecentos').replace('oito centos','o

===== receiptSeq @ 6495204 =====
length)await db.logs.bulkAdd(logs.map(l=>{let x={...l};delete x.id;return x}).slice(0,200));let restoredMax=receipts.reduce((m,r)=>r.source==='Excel legado'?m:Math.max(m,Number(String(r.number||'').replace(/\D/g,'')||0)),0),safeSeq=Math.max(Number(data.receiptSeq||0),restoredMax);await db.meta.put({key:'receiptSeq',value:safeSeq})});await this.logAction('restore','Backup restaurado','Dados restaurados por arquivo JSON.');await this.load();this.toast('Backup restaurado com sucesso!')}catch(e){console.error(e);this.toast('Não foi possível restaurar o backup: '+e.message,'error')}},async clearAllInternal(){await db.transaction('rw',db.providers,db.services,db.receipts,db.recurrences,db.logs,async()=>{await db.providers.clear();await db.services.clear();await db.receipts.clear();await db.recurrences.clear();await db.logs.clear()});await this.load()},async clearAll(){this.openClearAllModal()},readLogo(e){let f=e.target.files[0];if(!f)return;let r=new FileReader();r.onload=()=>this.settings.logo=r.result;r.readAsDataURL(f)},
  receiptMaxNumber(){return this.receipts.reduce((max,r)=>r.source==='Excel legado'?max:Math.max(max,Number(String(r.number||'').replace(/\D/g,'')||0)),0)},nextReceiptNumber(){return Math.max(Number(this.receiptSeqValue||0),this.receiptMaxNumber())+1},receiptSequenceStatus(){return Number(this.receiptSeqValue||0)<this.receiptMaxNumber()?'Sincronizar':'Sequência OK'},async saveReceiptSequence(){if(this.isViewer)return;let target=Number(this.receiptSeqInput||0), max=this.receiptMaxNumber();if(!target||target<1)return this.toast('Informe um próximo número válido.','warning');if(target<=max)return this.toast('O número precisa ser maior que o último emitido: '+String(max).padStart(6,'0')+'.','warning');await db.meta.put({key:'receiptSeq',value:target-1});this.receiptSeqValue=target-1;await this.logAction('settings','Sequência de recibos ajustada','Próximo recibo definido para nº '+String(target).padStart(6,'0'));this.toast('Sequência atualizada. Próximo recibo: '+String(target).padStart(6,'0'));await this.load()},
  topPaidNames(rows){let map={};rows.forEach(s=>{let v=Number(s.paidValue||0);if(v<=0)return;let name=this.providerName(s.providerId);map[name]=(map[name]||0)+v});return Object.entries(map).sort((a,b)=>b[1]-a[1]).slice(0,3).map(([name,total])=>name+' • '+this.money(total))},topOpenNames(rows){let map={};rows.forEach(s=>{let v=this.balance(s);if(v<=0)return;let name=this.providerName(s.providerId);map[name]=(map[name]||0)+v});return Object.entries(map).sort((a,b)=>b[1]-a[1]).slice(0,3).map(([name,total])=>name+' • '+this.money(total))},
  providerServices(id){return this.services.filter(s=>Number(s.providerId)===Number(id)).sort((a,b)=>String(b.serviceDate||'').localeCompare(String(a.serviceDate||'')))},providerReceipts(id){return this.receipts.filter(r=>Number(r.providerId)===Number(id)).sort((a,b)=>String(b.date||'').localeCompare(String(a.date||'')))},providerLastServiceLabel(id){let s=this.providerServices(id)[0];return s?this.fmt(s.serviceDate)+' • '+this.money(s.paidValue||s.amount):'-'},async logAction(type,title,detail){try{await db.logs.add({at:new Date().toISOString(),type,title,detail,role:this.auth?.role||'sistema'});this.logs=(await db.logs.orderBy('at').reverse().limit(30).toArray())}catch(e){console.warn('logAction',e)}},logIcon(type){return {payment:'circle-dollar-sign',receipt:'receipt-text',service:'clipboard-list',provider:'users',recurrence:'repeat-2',backup:'shield-check',restore:'rotate-ccw',danger:'triangle-alert',settings:'settings',delete:'trash-2'}[type]||'history'},fmtDateTime(d){if(!d)return'-';return new Date(d).toLocaleString('pt-BR')},async clearLogs(){if(this.isViewer)return;await db.logs.clear();this.logs=[];this.after()},providerSpend(id){return this.services.filter(s=>Number(s.providerId)===Number(id)).reduce((a,s)=>a+Number(s.paidValue||s.amount||0),0)},providerOpen(id){return this.services.filter(s=>Number(s.providerId)===Number(id)).reduce((a,s)=>a+this.balance(s),0)},openWhatsapp(p){let raw=String(p.whatsapp||p.phone||'').replace(/\D/g,'');if(!raw)return this.toast('Este prestador não tem WhatsApp/telefone cadastrado.','warning');let phone=raw.startsWith('55')?raw:'55'+raw;let msg=encodeURIComponent('Olá, '+p.name+'. Aqui é do '+this.settings.condoName+'.');window.open('https://wa.me/'+phone+'?text='+msg,'_blank')},overdueLabel(s){if(!s.dueDate||this.balance(s)<=0)return'';let today=new Date(this.today()+'T00:00:00'), due=new Date(s.dueDate+'T00:00:00'), days=Math.ceil((due-today)/86400000);if(days<0)return Math.abs(days)+' dia(s) em atraso';if(days===0)return'vence hoje';return'faltam '+days+' dia(s)'},providerName(id){return (this.providers.find(p=>Number(p.id)===Number(id))||{}).name||'-'},reportReceiptNumber(service){let r=this.receipts.find(x=>Number(x.serviceId)===Number(service.id));return r?.number||'-';},balance(s){return Math.max(0,Number(s.amount||0)-Number(s.paidValue||0))},statusClass(st){return st==='Pago'?'bg-emerald-500/10 text-emerald-700':st==='Parcial'?'bg-amber-500/10 text-amber-700':'bg-red-500/10 text-red-700'},valorPorExtenso(v){v=Number(v||0);let reais=Math.floor(v),cent=Math.round((v-reais)*100);let base={0:'zero',1:'um',2:'dois',3:'três',4:'quatro',5:'cinco',6:'seis',7:'sete',8:'oito',9:'nove',10:'dez',11:'onze',12:'doze',13:'treze',14:'quatorze',15:'quinze',16:'dezesseis',17:'dezessete',18:'dezoito',19:'dezenove',20:'vinte',30:'trinta',40:'quarenta',50:'cinquenta',60:'sessenta',70:'setenta',80:'oitenta',90:'noventa',100:'cem'};let ext=(n)=>{if(n<=20||base[n])return base[n];if(n<100){let d=Math.floor(n/10)*10,u=n%10;return base[d]+(u?' e '+base[u]:'')}if(n<1000){let c=Math.floor(n/100),r=n%100,cn=c===1?'cento':base[c]+' centos';cn=cn.replace('dois centos','duzentos').replace('três centos','trezentos').replace('quatro centos','quatrocentos').replace('cinco centos','quinhentos').replace('seis centos','seiscentos').replace('sete centos','setecentos').replace('oito centos','oitocentos').replace('nove centos',

===== receiptNumber @ 2202915 =====
goria</span><select x-model="report.category" class="input mt-1"><option value="todas">Todas</option><template x-for="c in categories"><option x-text="c"></option></template></select></label><label><span class="text-sm font-bold">Status</span><select x-model="report.status" class="input mt-1"><option value="todos">Todos</option><option>Pendente</option><option>Parcial</option><option>Pago</option></select></label><div class="flex gap-2"><button @click="csv" class="primary flex-1">CSV</button><button @click="printReport" class="secondary">PDF</button></div></div>
          <div class="grid xl:grid-cols-3 gap-4"><div class="glass rounded-3xl p-4 xl:col-span-2"><h3 class="font-black mb-3">Relatório de pagamentos</h3><div class="report-chart-box"><canvas id="repChart"></canvas></div></div><div class="glass rounded-3xl p-4"><h3 class="font-black mb-3">Resumo</h3><p class="flex justify-between"><span>Total lançado</span><b x-text="money(reportTotals.expected)"></b></p><p class="flex justify-between"><span>Total pago</span><b class="text-emerald-600" x-text="money(reportTotals.paid)"></b></p><p class="flex justify-between"><span>Em aberto</span><b class="text-red-600" x-text="money(reportTotals.open)"></b></p></div></div>
          <div class="glass rounded-3xl p-4 overflow-auto scrollbar"><table class="w-full text-sm"><thead class="text-left text-slate-500"><tr><th class="p-2">Recibo nº</th><th class="p-2">Serviço</th><th class="p-2">Pagamento</th><th class="p-2">Prestador</th><th class="p-2">Categoria</th><th class="p-2">Valor</th><th class="p-2">Pago</th><th class="p-2">Status</th></tr></thead><tbody><template x-for="s in reportDetailRows" :key="s.reportKey"><tr class="border-t border-slate-200/60 dark:border-slate-700/60"><td class="p-2 font-bold text-amber-700" x-text="s.receiptNumber"></td><td class="p-2" x-text="fmt(s.serviceDate)"></td><td class="p-2" x-text="fmt(s.paymentDate)"></td><td class="p-2" x-text="providerName(s.providerId)"></td><td class="p-2" x-text="s.category"></td><td class="p-2" x-text="money(s.amount)"></td><td class="p-2" x-text="money(s.reportValue||0)"></td><td class="p-2" x-text="s.status"></td></tr></template></tbody></table></div>
        </section>

        <section x-show="view==='busca'" class="space-y-4">
          <div class="glass rounded-3xl p-4 md:p-5">
            <div class="flex flex-col lg:flex-row lg:items-end justify-between gap-4">
              <div>
                <p class="text-xs uppercase tracking-[.18em] text-rc-gold font-black">Central de localização</p>
                <h3 class="font-black text-2xl">Busca global inteligente</h3>
                <p class="text-sm text-slate-500 dark:text-slate-400 mt-1">Localize prestadores, serviços, recibos e recorrências em um só lugar.</p>
              </div>
              <div class="w-full lg:w-[420px]">
                <label class="text-sm font-bold">Pesquisar</label>
                <input x-model="q" class="input mt-1" placeholder="Ex.: jardineiro, recibo 000012, PIX, maio, serralheiro...">
              </div>
            </div>
            <div class="grid sm:grid-cols-4 gap-3 mt-4">
              <div class="rounded-2xl border border-slate-200/60 dark:border-slate-700/60 bg-white/70 dark:bg-slate-950/30 p-3"><p class="text-xs uppercase tracking-[.14em] text-slate-500 font-black">Resultados</p><b class="text-xl" x-text="searchResults.length"></b></div>
              <div class="rounded-2xl border border-slate-200/60 dark:border-slate-700/60 bg-white/70 dark:bg-slate-950/30 p-3"><p class="text-xs uppercase tracking-[.14em] text-slate-500 font-black">Prestadores</p><b class="text-xl" x-text="searchResults.filter(i=>i.kind==='provider').length"></b></div>
              <div class="rounded-2xl border border-slate-200/60 dark:border-slate-700/60 bg-white/70 dark:bg-slate-950/30 p-3"><p class="text-xs uppercase tracking-[.14em] text-slate-500 font-black">Serviços</p><b class="text-xl" x-text="searchResults.filter(i=>i.kind==='service').length"></b></div>
              <div class="rounded-2xl border border-slate-200/60 dark:border-slate-700/60 bg-white/70 dark:bg-slate-950/30 p-3"><p class="text-xs uppercase tracking-[.14em] text-slate-500 font-black">Recibos</p><b class="text-xl text-amber-700" x-text="searchResults.filter(i=>i.kind==='receipt').length"></b></div>
            </div>
          </div>

          <div class="grid xl:grid-cols-2 gap-4">
            <template x-for="item in searchResults" :key="item.kind+'-'+item.id">
              <div class="glass rounded-3xl p-4 border-t-4" :class="item.kind==='receipt'?'border-t-amber-500':item.kind==='provider'?'border-t-emerald-600':item.kind==='recurrence'?'border-t-blue-500':'border-t-rc-gold'">
                <div class="flex items-start justify-between gap-3">
                  <div>
                    <span class="badge" x-text="item.type"></span>
                    <h4 class="font-black text-lg mt-2" x-text="item.title"></h4>
                    <p class="text-sm text-slate-500 dark:text-slate-400" x-text="item.subtitle"></p>
                  </div>
                  <div class="text-right shrink-0">
                    <p class="font-black" x-text="item.amount"></p>
                    <p class="text-xs text-slate-500" x-text="item.date"></p>
                  </div>
                </div>
                <p class="text-sm mt-3 leading-relaxed" x-text="item.description"></p>
                <div class="flex flex-wrap gap-2 mt-4">
                  <button x-show="item.kind==='provider'" @click="openSearchProvider(item.data)" class="mini">Painel</button>
                  <button x-show="item.kind==='provider'" @click="fillServiceFromProvider(item.data)" class="mini" :disabled="isViewer">Lançar</button>
                  <button x-show="item.kind==='provider'" @click="openWhatsapp(item.data)" class="mini">WhatsApp</button>

                  <button x-show="item.kind==='service'" @click="openSearchService(item.data)" class="mini">Ver serv

===== receiptNumber @ 6437610 =====
te-600 mt-1">Resumo para arquivo interno, administradora e livro de prestação de contas.</p>
            </div>

            <div class="grid grid-cols-4 gap-3 mb-5">
              <div class="report-kpi"><p class="label">Total lançado</p><p class="value" x-text="money(reportTotals.expected)"></p></div>
              <div class="report-kpi"><p class="label">Total pago</p><p class="value green" x-text="money(reportTotals.paid)"></p></div>
              <div class="report-kpi"><p class="label">Em aberto</p><p class="value red" x-text="money(reportTotals.open)"></p></div>
              <div class="report-kpi"><p class="label">Mês de referência</p><p class="value text-[13px] leading-tight" x-text="reportMonthLabel()"></p></div>
            </div>

            <div class="grid grid-cols-2 gap-3 mb-5">
              <div class="receipt-box p-4">
                <p class="receipt-label">Filtro de categoria</p>
                <p class="receipt-value" x-text="report.category==='todas'?'Todas as categorias':report.category"></p>
              </div>
              <div class="receipt-box p-4">
                <p class="receipt-label">Filtro de status</p>
                <p class="receipt-value" x-text="report.status==='todos'?'Todos os status':report.status"></p>
              </div>
            </div>

            <table class="report-table mb-5">
              <thead><tr><th style="width:11%">Recibo nº</th><th style="width:11%">Pagamento</th><th style="width:22%">Prestador</th><th style="width:16%">Categoria</th><th style="width:28%">Descrição</th><th style="width:12%">Valor</th></tr></thead>
              <tbody>
                <template x-for="s in reportDetailRows" :key="s.reportKey">
                  <tr>
                    <td class="font-bold text-slate-700" x-text="s.receiptNumber"></td>
                    <td x-text="fmt(s.paymentDate)"></td>
                    <td x-text="providerName(s.providerId)"></td>
                    <td x-text="s.category"></td>
                    <td x-text="s.description || '-'"></td>
                    <td class="font-bold" x-text="money(s.reportValue || 0)"></td>
                  </tr>
                </template>
              </tbody>
            </table>

            <div class="report-footer-credit pt-4 border-t border-slate-200 text-[11px] text-slate-500 leading-relaxed">
              <p><b>Observação:</b> relatório emitido para controle interno do condomínio e composição do livro de prestação de contas. Recomenda-se arquivar junto aos recibos, comprovantes de pagamento, notas fiscais, contratos ou autorizações correspondentes, quando houver.</p>
              <p class="mt-2 text-center">Documento gerado localmente pelo ReciboCondo PWA • <span x-text="settings.condoName"></span> • <span x-text="fmt(today())"></span></p>
              <p class="report-dev-credit">Aplicativo desenvolvido por Carlos Cunha</p>
            </div>
          </div>
        </div>
        <div id="annualReportPrint" class="receipt report-premium hidden">
          <div class="report-watermark">ReciboCondo</div>
          <div class="relative z-10">
            <div class="flex justify-between gap-6 items-start mb-5">
              <div class="flex gap-4 items-center">
                <template x-if="settings.logo"><img :src="settings.logo" class="h-16 w-16 object-contain rounded-xl border border-slate-200 p-1"></template>
                <template x-if="!settings.logo"><div class="h-16 w-16 rounded-xl grid place-items-center text-white font-black text-2xl" style="background:linear-gradient(135deg,#0F2A3A,#15803D)">RC</div></template>
                <div>
                  <h1 class="text-2xl font-bold leading-tight" x-text="settings.condoName"></h1>
                  <p class="text-sm text-slate-600" x-text="settings.cnpj"></p>
                  <p class="text-sm text-slate-600 max-w-[118mm]" x-text="settings.address"></p>
                </div>
              </div>
              <div class="text-right shrink-0">
                <span class="receipt-stamp">Prestação de contas</span>
                <p class="receipt-label mt-3">Ano</p>
                <p class="text-2xl font-black receipt-number" x-text="dashboardYear"></p>
                <p class="receipt-label mt-2">Emitido em</p>
                <p class="receipt-value" x-text="fmt(today())"></p>
              </div>
            </div>

            <div class="mb-4">
              <p class="receipt-label">Documento financeiro anual</p>
              <h2 class="text-3xl font-black tracking-tight text-slate-900">RELATÓRIO ANUAL DE PRESTADORES</h2>
              <p class="text-sm text-slate-600 mt-1">Resumo consolidado de pagamentos, categorias e prestadores para acompanhamento anual.</p>
            </div>

            <div class="grid grid-cols-4 gap-3 mb-4">
              <div class="report-kpi"><p class="label">Total pago</p><p class="value green" x-text="money(annualTotals.paid)"></p></div>
              <div class="report-kpi"><p class="label">Em aberto</p><p class="value red" x-text="money(annualTotals.open)"></p></div>
              <div class="report-kpi"><p class="label">Pagamentos</p><p class="value" x-text="annualTotals.count"></p></div>
              <div class="report-kpi"><p class="label">Maior categoria</p><p class="value text-[13px] leading-tight" x-text="annualCategorySummary[0]?.category || '-'"></p></div>
            </div>

            <div class="grid grid-cols-2 gap-4 mb-4">
              <div class="receipt-box p-4">
                <p class="receipt-label mb-2">Resumo mensal</p>
                <table class="annual-mini-table">
                  <thead><tr><th>Mês</th><th>Pag.</th><th>Total pago</th><th>Aberto</th></tr></thead>
                  <tbody>
                    <template x-for="m in monthlyHistory" :key="m.key">
                      <tr><td class="font-bold" x-text="m.label.slice(0,3)"></td><td x-text="m.count"></td><td class="font-bold" x-text="money(m.paid)"></td><td x-text="mo

===== receiptNumber @ 6459564 =====
>r.active&&!this.recurrenceGenerated(r,this.servicePanelMonth))},
  get servicePanelReceiptCount(){return this.receipts.filter(r=>String(r.paymentDate||r.date||'').slice(0,7)===this.servicePanelMonth).length},
  get filteredReceipts(){let q=String(this.receiptFilter.q||'').toLowerCase().trim();return this.receipts.filter(r=>{let txt=[r.number,r.providerName,r.category,r.description,r.paymentMethod,r.total].join(' ').toLowerCase();return(!q||txt.includes(q))&&(!this.receiptFilter.month||r.reference===this.receiptFilter.month)&&(this.receiptFilter.category==='todas'||r.category===this.receiptFilter.category)}).sort((a,b)=>String(b.date||'').localeCompare(String(a.date||''))||String(b.number||'').localeCompare(String(a.number||'')))},
  get reportRows(){return this.services.filter(s=>(!this.report.from||s.serviceDate>=this.report.from)&&(!this.report.to||s.serviceDate<=this.report.to)&&(this.report.category==='todas'||s.category===this.report.category)&&(this.report.status==='todos'||s.status===this.report.status)).sort((a,b)=>{let nums=x=>this.receipts.filter(r=>Number(r.serviceId)===Number(x.id)).map(r=>Number(String(r.number||'').replace(/\D/g,''))||0).filter(Boolean),aa=nums(a),bb=nums(b),na=aa.length?Math.min(...aa):0,nb=bb.length?Math.min(...bb):0;if(na&&nb&&na!==nb)return na-nb;if(na&&!nb)return-1;if(!na&&nb)return 1;return String(a.serviceDate||'').localeCompare(String(b.serviceDate||''))||Number(a.id||0)-Number(b.id||0)})},
  get reportDetailRows(){let rows=[];for(const s of this.reportRows){let rs=this.receipts.filter(r=>Number(r.serviceId)===Number(s.id)).sort((a,b)=>(Number(String(a.number||'').replace(/\D/g,''))||0)-(Number(String(b.number||'').replace(/\D/g,''))||0)),sum=0;for(const r of rs){let v=Number(r.total||0);sum+=v;rows.push({...s,reportKey:'r-'+r.id,receiptNumber:r.number||'-',paymentDate:r.paymentDate||r.date||s.paymentDate,description:r.description||s.description,reportValue:v,paymentMethod:r.paymentMethod||s.paymentMethod||''})}let u=Math.max(0,Number(s.paidValue||0)-sum);if(u>0)rows.push({...s,reportKey:'u-'+s.id,receiptNumber:'PENDENTE',paymentDate:s.paymentDate,description:s.description,reportValue:u,paymentMethod:s.paymentMethod||''});if(!rs.length&&u<=0)rows.push({...s,reportKey:'s-'+s.id,receiptNumber:'-',description:s.description,reportValue:Number(s.paidValue||s.amount||0),paymentMethod:s.paymentMethod||''})}return rows},get reportTotals(){let e=this.reportRows.reduce((a,s)=>a+Number(s.amount||0),0),p=this.reportRows.reduce((a,s)=>a+Number(s.paidValue||0),0);return{expected:e,paid:p,open:e-p}},backupStats(){let records=this.providers.length+this.services.length+this.receipts.length+this.recurrences.length;return{records,providers:this.providers.length,services:this.services.length,receipts:this.receipts.length,recurrences:this.recurrences.length}},backupDays(){if(!this.backupLast)return null;let d=new Date(this.backupLast), now=new Date();return Math.max(0,Math.floor((now-d)/86400000))},backupLastLabel(){if(!this.backupLast)return'Nunca feito';let days=this.backupDays();let date=new Date(this.backupLast).toLocaleString('pt-BR');return days===0?'Hoje • '+date:days+' dia(s) • '+date},backupHealthLabel(){let d=this.backupDays();if(d===null)return'Backup pendente';if(d<=7)return'Protegido';if(d<=15)return'Atenção';return'Backup atrasado'},backupHealthClass(){let d=this.backupDays();if(d===null||d>15)return'bg-red-500/10 text-red-700';if(d>7)return'bg-amber-500/10 text-amber-700';return'bg-emerald-500/10 text-emerald-700'},backupHint(){let s=this.backupStats();let d=this.backupDays();if(d===null)return'Você ainda não exportou um backup. Recomendo gerar um JSON antes de continuar cadastrando dados importantes.';if(d>15)return'Seu último backup já passou de 15 dias. Exporte um novo JSON para proteger '+s.records+' registros locais.';if(d>7)return'Backup existente, mas já vale gerar uma cópia atualizada com os últimos lançamentos e recibos.';return'Backup em dia. Mesmo assim, exporte uma nova cópia sempre antes de grandes alterações.'},
  get topProviders(){let y=new Date().getFullYear(), map={};this.services.filter(s=>String(s.serviceDate||'').startsWith(String(y))).forEach(s=>{let id=s.providerId,p=this.providers.find(x=>Number(x.id)===Number(id))||{};if(!map[id])map[id]={id,name:p.name||'-',category:p.category||s.category,count:0,total:0,open:0};map[id].count++;map[id].total+=Number(s.paidValue||s.amount||0);map[id].open+=this.balance(s)});return Object.values(map).sort((a,b)=>b.total-a.total).slice(0,8)},
  get categorySummary(){let total=this.services.reduce((a,s)=>a+Number(s.paidValue||s.amount||0),0)||1;return this.categories.map(category=>{let val=this.services.filter(s=>s.category===category).reduce((a,s)=>a+Number(s.paidValue||s.amount||0),0);return{category,total:val,percent:Math.min(100,Math.round(val/total*100))}}).filter(x=>x.total>0).sort((a,b)=>b.total-a.total)},
  get criticalOpen(){return this.openServices.slice().sort((a,b)=>new Date(a.dueDate||'2999-12-31')-new Date(b.dueDate||'2999-12-31')).slice(0,8)},
  get notifications(){const now=this.today();let pend=this.services.filter(s=>s.status!=='Pago'&&s.dueDate<=now).slice(0,3).map(s=>({id:s.id,title:'Pagamento pendente',body:this.providerName(s.providerId)+' • '+s.category+' • '+this.money(this.balance(s))}));let rec=this.recurrences.filter(r=>r.active&&!this.recurrenceGenerated(r)).slice(0,2).map(r=>({id:'r'+r.id,title:'Recorrência do mês pendente',body:this.providerName(r.providerId)+' • '+r.category+' • '+this.money(r.amount)}));return[...rec,...pend].slice(0,4)},
  get dayOverdue(){let t=this.today();return this.services.filter(s=>s.status!=='Pago'&&s.dueDate&&s.dueDate<t).sort((a,b)=>String(a.dueDate).localeCompare(String(b.dueDate)))},
  get dayDueToday(){let t=this.today();return this.services.filter(s=>s.status!=='Pago'&&s.dueDate===t).sort((a,b)=>this.balance(b)-this.balance(a))},
  get dayNextDue(){let t=new Date(this.today()+'T00:00:00'),limit=new Date(t);limit.setDate(t.getDate()+7);l

===== receiptNumber @ 6459837 =====
.toLowerCase().trim();return this.receipts.filter(r=>{let txt=[r.number,r.providerName,r.category,r.description,r.paymentMethod,r.total].join(' ').toLowerCase();return(!q||txt.includes(q))&&(!this.receiptFilter.month||r.reference===this.receiptFilter.month)&&(this.receiptFilter.category==='todas'||r.category===this.receiptFilter.category)}).sort((a,b)=>String(b.date||'').localeCompare(String(a.date||''))||String(b.number||'').localeCompare(String(a.number||'')))},
  get reportRows(){return this.services.filter(s=>(!this.report.from||s.serviceDate>=this.report.from)&&(!this.report.to||s.serviceDate<=this.report.to)&&(this.report.category==='todas'||s.category===this.report.category)&&(this.report.status==='todos'||s.status===this.report.status)).sort((a,b)=>{let nums=x=>this.receipts.filter(r=>Number(r.serviceId)===Number(x.id)).map(r=>Number(String(r.number||'').replace(/\D/g,''))||0).filter(Boolean),aa=nums(a),bb=nums(b),na=aa.length?Math.min(...aa):0,nb=bb.length?Math.min(...bb):0;if(na&&nb&&na!==nb)return na-nb;if(na&&!nb)return-1;if(!na&&nb)return 1;return String(a.serviceDate||'').localeCompare(String(b.serviceDate||''))||Number(a.id||0)-Number(b.id||0)})},
  get reportDetailRows(){let rows=[];for(const s of this.reportRows){let rs=this.receipts.filter(r=>Number(r.serviceId)===Number(s.id)).sort((a,b)=>(Number(String(a.number||'').replace(/\D/g,''))||0)-(Number(String(b.number||'').replace(/\D/g,''))||0)),sum=0;for(const r of rs){let v=Number(r.total||0);sum+=v;rows.push({...s,reportKey:'r-'+r.id,receiptNumber:r.number||'-',paymentDate:r.paymentDate||r.date||s.paymentDate,description:r.description||s.description,reportValue:v,paymentMethod:r.paymentMethod||s.paymentMethod||''})}let u=Math.max(0,Number(s.paidValue||0)-sum);if(u>0)rows.push({...s,reportKey:'u-'+s.id,receiptNumber:'PENDENTE',paymentDate:s.paymentDate,description:s.description,reportValue:u,paymentMethod:s.paymentMethod||''});if(!rs.length&&u<=0)rows.push({...s,reportKey:'s-'+s.id,receiptNumber:'-',description:s.description,reportValue:Number(s.paidValue||s.amount||0),paymentMethod:s.paymentMethod||''})}return rows},get reportTotals(){let e=this.reportRows.reduce((a,s)=>a+Number(s.amount||0),0),p=this.reportRows.reduce((a,s)=>a+Number(s.paidValue||0),0);return{expected:e,paid:p,open:e-p}},backupStats(){let records=this.providers.length+this.services.length+this.receipts.length+this.recurrences.length;return{records,providers:this.providers.length,services:this.services.length,receipts:this.receipts.length,recurrences:this.recurrences.length}},backupDays(){if(!this.backupLast)return null;let d=new Date(this.backupLast), now=new Date();return Math.max(0,Math.floor((now-d)/86400000))},backupLastLabel(){if(!this.backupLast)return'Nunca feito';let days=this.backupDays();let date=new Date(this.backupLast).toLocaleString('pt-BR');return days===0?'Hoje • '+date:days+' dia(s) • '+date},backupHealthLabel(){let d=this.backupDays();if(d===null)return'Backup pendente';if(d<=7)return'Protegido';if(d<=15)return'Atenção';return'Backup atrasado'},backupHealthClass(){let d=this.backupDays();if(d===null||d>15)return'bg-red-500/10 text-red-700';if(d>7)return'bg-amber-500/10 text-amber-700';return'bg-emerald-500/10 text-emerald-700'},backupHint(){let s=this.backupStats();let d=this.backupDays();if(d===null)return'Você ainda não exportou um backup. Recomendo gerar um JSON antes de continuar cadastrando dados importantes.';if(d>15)return'Seu último backup já passou de 15 dias. Exporte um novo JSON para proteger '+s.records+' registros locais.';if(d>7)return'Backup existente, mas já vale gerar uma cópia atualizada com os últimos lançamentos e recibos.';return'Backup em dia. Mesmo assim, exporte uma nova cópia sempre antes de grandes alterações.'},
  get topProviders(){let y=new Date().getFullYear(), map={};this.services.filter(s=>String(s.serviceDate||'').startsWith(String(y))).forEach(s=>{let id=s.providerId,p=this.providers.find(x=>Number(x.id)===Number(id))||{};if(!map[id])map[id]={id,name:p.name||'-',category:p.category||s.category,count:0,total:0,open:0};map[id].count++;map[id].total+=Number(s.paidValue||s.amount||0);map[id].open+=this.balance(s)});return Object.values(map).sort((a,b)=>b.total-a.total).slice(0,8)},
  get categorySummary(){let total=this.services.reduce((a,s)=>a+Number(s.paidValue||s.amount||0),0)||1;return this.categories.map(category=>{let val=this.services.filter(s=>s.category===category).reduce((a,s)=>a+Number(s.paidValue||s.amount||0),0);return{category,total:val,percent:Math.min(100,Math.round(val/total*100))}}).filter(x=>x.total>0).sort((a,b)=>b.total-a.total)},
  get criticalOpen(){return this.openServices.slice().sort((a,b)=>new Date(a.dueDate||'2999-12-31')-new Date(b.dueDate||'2999-12-31')).slice(0,8)},
  get notifications(){const now=this.today();let pend=this.services.filter(s=>s.status!=='Pago'&&s.dueDate<=now).slice(0,3).map(s=>({id:s.id,title:'Pagamento pendente',body:this.providerName(s.providerId)+' • '+s.category+' • '+this.money(this.balance(s))}));let rec=this.recurrences.filter(r=>r.active&&!this.recurrenceGenerated(r)).slice(0,2).map(r=>({id:'r'+r.id,title:'Recorrência do mês pendente',body:this.providerName(r.providerId)+' • '+r.category+' • '+this.money(r.amount)}));return[...rec,...pend].slice(0,4)},
  get dayOverdue(){let t=this.today();return this.services.filter(s=>s.status!=='Pago'&&s.dueDate&&s.dueDate<t).sort((a,b)=>String(a.dueDate).localeCompare(String(b.dueDate)))},
  get dayDueToday(){let t=this.today();return this.services.filter(s=>s.status!=='Pago'&&s.dueDate===t).sort((a,b)=>this.balance(b)-this.balance(a))},
  get dayNextDue(){let t=new Date(this.today()+'T00:00:00'),limit=new Date(t);limit.setDate(t.getDate()+7);let a=this.today(),b=limit.toISOString().slice(0,10);return this.services.filter(s=>s.status!=='Pago'&&s.dueDate&&s.dueDate>a&&s.dueDate<=b).sort((x,y)=>String(x.dueDate).localeCompare(String(y.dueDate)))},
  get dayPendingRecurrences(){return this.recurrences.filter(r=>r.a

===== receiptNumber @ 6460020 =====
s(q))&&(!this.receiptFilter.month||r.reference===this.receiptFilter.month)&&(this.receiptFilter.category==='todas'||r.category===this.receiptFilter.category)}).sort((a,b)=>String(b.date||'').localeCompare(String(a.date||''))||String(b.number||'').localeCompare(String(a.number||'')))},
  get reportRows(){return this.services.filter(s=>(!this.report.from||s.serviceDate>=this.report.from)&&(!this.report.to||s.serviceDate<=this.report.to)&&(this.report.category==='todas'||s.category===this.report.category)&&(this.report.status==='todos'||s.status===this.report.status)).sort((a,b)=>{let nums=x=>this.receipts.filter(r=>Number(r.serviceId)===Number(x.id)).map(r=>Number(String(r.number||'').replace(/\D/g,''))||0).filter(Boolean),aa=nums(a),bb=nums(b),na=aa.length?Math.min(...aa):0,nb=bb.length?Math.min(...bb):0;if(na&&nb&&na!==nb)return na-nb;if(na&&!nb)return-1;if(!na&&nb)return 1;return String(a.serviceDate||'').localeCompare(String(b.serviceDate||''))||Number(a.id||0)-Number(b.id||0)})},
  get reportDetailRows(){let rows=[];for(const s of this.reportRows){let rs=this.receipts.filter(r=>Number(r.serviceId)===Number(s.id)).sort((a,b)=>(Number(String(a.number||'').replace(/\D/g,''))||0)-(Number(String(b.number||'').replace(/\D/g,''))||0)),sum=0;for(const r of rs){let v=Number(r.total||0);sum+=v;rows.push({...s,reportKey:'r-'+r.id,receiptNumber:r.number||'-',paymentDate:r.paymentDate||r.date||s.paymentDate,description:r.description||s.description,reportValue:v,paymentMethod:r.paymentMethod||s.paymentMethod||''})}let u=Math.max(0,Number(s.paidValue||0)-sum);if(u>0)rows.push({...s,reportKey:'u-'+s.id,receiptNumber:'PENDENTE',paymentDate:s.paymentDate,description:s.description,reportValue:u,paymentMethod:s.paymentMethod||''});if(!rs.length&&u<=0)rows.push({...s,reportKey:'s-'+s.id,receiptNumber:'-',description:s.description,reportValue:Number(s.paidValue||s.amount||0),paymentMethod:s.paymentMethod||''})}return rows},get reportTotals(){let e=this.reportRows.reduce((a,s)=>a+Number(s.amount||0),0),p=this.reportRows.reduce((a,s)=>a+Number(s.paidValue||0),0);return{expected:e,paid:p,open:e-p}},backupStats(){let records=this.providers.length+this.services.length+this.receipts.length+this.recurrences.length;return{records,providers:this.providers.length,services:this.services.length,receipts:this.receipts.length,recurrences:this.recurrences.length}},backupDays(){if(!this.backupLast)return null;let d=new Date(this.backupLast), now=new Date();return Math.max(0,Math.floor((now-d)/86400000))},backupLastLabel(){if(!this.backupLast)return'Nunca feito';let days=this.backupDays();let date=new Date(this.backupLast).toLocaleString('pt-BR');return days===0?'Hoje • '+date:days+' dia(s) • '+date},backupHealthLabel(){let d=this.backupDays();if(d===null)return'Backup pendente';if(d<=7)return'Protegido';if(d<=15)return'Atenção';return'Backup atrasado'},backupHealthClass(){let d=this.backupDays();if(d===null||d>15)return'bg-red-500/10 text-red-700';if(d>7)return'bg-amber-500/10 text-amber-700';return'bg-emerald-500/10 text-emerald-700'},backupHint(){let s=this.backupStats();let d=this.backupDays();if(d===null)return'Você ainda não exportou um backup. Recomendo gerar um JSON antes de continuar cadastrando dados importantes.';if(d>15)return'Seu último backup já passou de 15 dias. Exporte um novo JSON para proteger '+s.records+' registros locais.';if(d>7)return'Backup existente, mas já vale gerar uma cópia atualizada com os últimos lançamentos e recibos.';return'Backup em dia. Mesmo assim, exporte uma nova cópia sempre antes de grandes alterações.'},
  get topProviders(){let y=new Date().getFullYear(), map={};this.services.filter(s=>String(s.serviceDate||'').startsWith(String(y))).forEach(s=>{let id=s.providerId,p=this.providers.find(x=>Number(x.id)===Number(id))||{};if(!map[id])map[id]={id,name:p.name||'-',category:p.category||s.category,count:0,total:0,open:0};map[id].count++;map[id].total+=Number(s.paidValue||s.amount||0);map[id].open+=this.balance(s)});return Object.values(map).sort((a,b)=>b.total-a.total).slice(0,8)},
  get categorySummary(){let total=this.services.reduce((a,s)=>a+Number(s.paidValue||s.amount||0),0)||1;return this.categories.map(category=>{let val=this.services.filter(s=>s.category===category).reduce((a,s)=>a+Number(s.paidValue||s.amount||0),0);return{category,total:val,percent:Math.min(100,Math.round(val/total*100))}}).filter(x=>x.total>0).sort((a,b)=>b.total-a.total)},
  get criticalOpen(){return this.openServices.slice().sort((a,b)=>new Date(a.dueDate||'2999-12-31')-new Date(b.dueDate||'2999-12-31')).slice(0,8)},
  get notifications(){const now=this.today();let pend=this.services.filter(s=>s.status!=='Pago'&&s.dueDate<=now).slice(0,3).map(s=>({id:s.id,title:'Pagamento pendente',body:this.providerName(s.providerId)+' • '+s.category+' • '+this.money(this.balance(s))}));let rec=this.recurrences.filter(r=>r.active&&!this.recurrenceGenerated(r)).slice(0,2).map(r=>({id:'r'+r.id,title:'Recorrência do mês pendente',body:this.providerName(r.providerId)+' • '+r.category+' • '+this.money(r.amount)}));return[...rec,...pend].slice(0,4)},
  get dayOverdue(){let t=this.today();return this.services.filter(s=>s.status!=='Pago'&&s.dueDate&&s.dueDate<t).sort((a,b)=>String(a.dueDate).localeCompare(String(b.dueDate)))},
  get dayDueToday(){let t=this.today();return this.services.filter(s=>s.status!=='Pago'&&s.dueDate===t).sort((a,b)=>this.balance(b)-this.balance(a))},
  get dayNextDue(){let t=new Date(this.today()+'T00:00:00'),limit=new Date(t);limit.setDate(t.getDate()+7);let a=this.today(),b=limit.toISOString().slice(0,10);return this.services.filter(s=>s.status!=='Pago'&&s.dueDate&&s.dueDate>a&&s.dueDate<=b).sort((x,y)=>String(x.dueDate).localeCompare(String(y.dueDate)))},
  get dayPendingRecurrences(){return this.recurrences.filter(r=>r.active&&!this.recurrenceGenerated(r))},
  get dayCenterRows(){let rows=[];this.dayPendingRecurrences.forEach(r=>rows.push({key:'r'+r.id,kind:'recurrence',recurrence:r,title:this.provid

===== receiptNumber @ 6490589 =====
his.receipts.filter(x=>Number(x.serviceId)===Number(r.serviceId)).sort((a,b)=>Number(b.id||0)-Number(a.id||0)),latest=linked[0];if(latest&&Number(latest.id)!==Number(r.id))return this.toast('Para preservar a data e a forma de pagamento das parcelas, somente o recibo mais recente deste lançamento pode ser excluído e reemitido.','warning');await db.receipts.delete(id);await this.logAction('delete','Recibo excluído',r?('Nº '+r.number+' • '+r.providerName):('ID '+id));await this.load()},
  printReport(){document.body.classList.add('printing-report');document.getElementById('receiptPrint').classList.add('hidden');document.getElementById('annualReportPrint').classList.add('hidden');document.getElementById('reportPrint').classList.remove('hidden');setTimeout(()=>{window.print();document.body.classList.remove('printing-report');document.getElementById('receiptPrint').classList.remove('hidden');document.getElementById('annualReportPrint').classList.add('hidden');document.getElementById('reportPrint').classList.add('hidden')},100)},printAnnualReport(){document.body.classList.add('printing-annual');let rp=document.getElementById('receiptPrint'),mp=document.getElementById('reportPrint'),ap=document.getElementById('annualReportPrint');rp.classList.add('hidden');mp.classList.add('hidden');ap.classList.remove('hidden');ap.style.display='block';ap.style.visibility='visible';setTimeout(()=>{window.print();document.body.classList.remove('printing-annual');rp.classList.remove('hidden');ap.classList.add('hidden');ap.style.display='';ap.style.visibility='';mp.classList.add('hidden')},180)},csv(){let rows=[['Recibo nº','Data do serviço','Data do pagamento','Prestador','Categoria','Descrição','Valor do serviço','Valor deste pagamento','Status','Forma']].concat(this.reportDetailRows.map(s=>[s.receiptNumber,s.serviceDate,s.paymentDate||'',this.providerName(s.providerId),s.category,s.description,s.amount,s.reportValue||0,s.status,s.paymentMethod||'']));let csv=rows.map(r=>r.map(v=>'"'+String(v??'').replaceAll('"','""')+'"').join(';')).join('\n');this.download('relatorio-prestadores-recibocondo.csv',csv,'text/csv;charset=utf-8')},
  async saveSettings(){let plainSettings=JSON.parse(JSON.stringify(this.settings));await db.settings.put({key:'main',value:plainSettings});await this.logAction('settings','Configurações salvas','Dados do condomínio atualizados.');this.toast('Configurações salvas')},async savePasswords(){if(this.isViewer)return;let cur=JSON.parse(localStorage.getItem('rc_passwords')||'{"admin":"123456","viewer":"123456"}');if(this.passwords.admin)cur.admin=this.passwords.admin;if(this.passwords.viewer)cur.viewer=this.passwords.viewer;localStorage.setItem('rc_passwords',JSON.stringify(cur));await this.logAction('settings','Senhas atualizadas','Senhas locais do app foram atualizadas.');this.passwords={admin:'',viewer:''};this.toast('Senhas atualizadas')},async backup(){let now=new Date().toISOString();let seqMeta=await db.meta.get('receiptSeq');let data={settings:this.settings,providers:this.providers,services:this.services,receipts:this.receipts,recurrences:this.recurrences,logs:this.logs,receiptSeq:Number(seqMeta?.value||this.receiptMaxNumber()||0),backupStats:this.backupStats(),exportedAt:now};this.download('backup-recibocondo-prestadores.json',JSON.stringify(data,null,2),'application/json');this.backupLast=now;localStorage.setItem('rc_last_backup',now);await this.logAction('backup','Backup exportado','Backup JSON gerado com '+data.backupStats.records+' registros.')},async restore(ev){let f=ev.target.files[0];if(!f)return;try{let data=JSON.parse(await f.text());if(!data||typeof data!=='object')throw new Error('Backup inválido');let providers=Array.isArray(data.providers)?data.providers:[],services=Array.isArray(data.services)?data.services:[],receipts=Array.isArray(data.receipts)?data.receipts:[],recurrences=Array.isArray(data.recurrences)?data.recurrences:[],logs=Array.isArray(data.logs)?data.logs:[];await db.transaction('rw',db.providers,db.services,db.receipts,db.recurrences,db.logs,db.settings,db.meta,async()=>{await db.providers.clear();await db.services.clear();await db.receipts.clear();await db.recurrences.clear();await db.logs.clear();if(data.settings){let restoredSettings=JSON.parse(JSON.stringify(data.settings));await db.settings.put({key:'main',value:restoredSettings});this.settings=restoredSettings}if(providers.length)await db.providers.bulkAdd(providers);if(services.length)await db.services.bulkAdd(services);if(receipts.length)await db.receipts.bulkAdd(receipts);if(recurrences.length)await db.recurrences.bulkAdd(recurrences);if(logs.length)await db.logs.bulkAdd(logs.map(l=>{let x={...l};delete x.id;return x}).slice(0,200));let restoredMax=receipts.reduce((m,r)=>r.source==='Excel legado'?m:Math.max(m,Number(String(r.number||'').replace(/\D/g,'')||0)),0),safeSeq=Math.max(Number(data.receiptSeq||0),restoredMax);await db.meta.put({key:'receiptSeq',value:safeSeq})});await this.logAction('restore','Backup restaurado','Dados restaurados por arquivo JSON.');await this.load();this.toast('Backup restaurado com sucesso!')}catch(e){console.error(e);this.toast('Não foi possível restaurar o backup: '+e.message,'error')}},async clearAllInternal(){await db.transaction('rw',db.providers,db.services,db.receipts,db.recurrences,db.logs,async()=>{await db.providers.clear();await db.services.clear();await db.receipts.clear();await db.recurrences.clear();await db.logs.clear()});await this.load()},async clearAll(){this.openClearAllModal()},readLogo(e){let f=e.target.files[0];if(!f)return;let r=new FileReader();r.onload=()=>this.settings.logo=r.result;r.readAsDataURL(f)},
  receiptMaxNumber(){return this.receipts.reduce((max,r)=>r.source==='Excel legado'?max:Math.max(max,Number(String(r.number||'').replace(/\D/g,'')||0)),0)},nextReceiptNumber(){return Math.max(Number(this.receiptSeqValue||0),this.receiptMaxNumber())+1},receiptSequenceStatus(){return Number(this.receiptSeqValue||0)<this.receiptMaxNumber()?'Sincroniza

===== settings @ 2141288 =====
nt-to-br from-rc-800 to-rc-500 text-white grid place-items-center shadow-premium"><i data-lucide="receipt-text" class="w-7 h-7"></i></div>
          <div><h1 class="text-2xl font-black tracking-tight">ReciboCondo</h1><p class="text-sm text-slate-500 dark:text-slate-400">Prestadores de serviços condominiais</p></div>
        </div>
        <div class="space-y-4 relative">
          <label class="block"><span class="text-sm font-semibold">Perfil</span><select x-model="login.role" class="input mt-1"><option value="admin">Administrador</option><option value="viewer">Visualizador</option></select></label>
          <label class="block"><span class="text-sm font-semibold">Senha</span><input x-model="login.password" @keydown.enter="doLogin" type="password" class="input mt-1" placeholder="Senha padrão: 123456" /></label>
          <button @click="doLogin" class="primary w-full">Entrar</button>
          <p class="text-xs text-slate-500 dark:text-slate-400 leading-relaxed">Primeiro acesso: <b>admin / 123456</b> ou <b>viewer / 123456</b>. As senhas ficam somente neste dispositivo.</p>
        </div>
      </section>
    </main>
  </template>

  <template x-if="auth.logged">
    <div class="min-h-screen lg:flex">
      <aside class="no-print app-sidebar fixed bottom-0 lg:top-0 left-0 right-0 lg:right-auto z-40 lg:h-screen lg:w-72 p-2 lg:p-4">
        <div class="glass rounded-3xl h-full lg:p-4 shadow-soft overflow-hidden">
          <div class="hidden lg:flex items-center gap-3 mb-6">
            <div class="h-12 w-12 rounded-2xl bg-gradient-to-br from-rc-800 to-rc-500 text-white grid place-items-center"><i data-lucide="hard-hat" class="w-6 h-6"></i></div>
            <div><h2 class="font-black text-xl">ReciboCondo</h2><p class="text-xs text-slate-500 dark:text-slate-400" x-text="settings.condoName"></p></div>
          </div>
          <nav class="grid grid-cols-6 lg:grid-cols-1 gap-1 lg:gap-2">
            <template x-for="item in menu" :key="item.id"><button @click="go(item.id)" :class="view===item.id?'bg-rc-800 text-white shadow-soft':'text-slate-600 dark:text-slate-300 hover:bg-white/60 dark:hover:bg-slate-800/60'" class="rounded-2xl px-2 py-3 lg:px-4 lg:py-3 flex flex-col lg:flex-row items-center justify-center lg:justify-start gap-1 lg:gap-3 transition"><i :data-lucide="item.icon" class="w-5 h-5"></i><span class="text-[10px] lg:text-sm font-bold" x-text="item.label"></span></button></template>
          </nav>
        </div>
      </aside>

      <main class="app-main flex-1 p-4 pb-28 lg:pb-4 lg:pl-0 max-w-[1500px] mx-auto w-full">
        <header class="no-print app-topbar glass rounded-[2rem] p-4 mb-4 shadow-soft z-30">
          <div class="flex flex-col md:flex-row md:items-center gap-3 justify-between">
            <div><p class="text-xs uppercase tracking-[.22em] text-emerald-600 dark:text-emerald-400 font-black">Prestadores • Serviços • Recibos</p><h1 class="text-xl md:text-3xl font-black" x-text="title"></h1></div>
            <div class="flex items-center gap-2">
              <div class="relative flex-1 min-w-[180px]"><i data-lucide="search" class="absolute left-3 top-3.5 w-4 h-4 text-slate-400"></i><input x-model="q" @input="view='busca'" class="input pl-9 text-sm" placeholder="Buscar prestador, serviço, CPF..." /></div>
              <button @click="toggleDark" class="h-11 w-11 rounded-2xl glass grid place-items-center hover:scale-105 transition"><i :data-lucide="dark?'sun':'moon'" class="w-5 h-5"></i></button>
              <button @click="logout" class="h-11 w-11 rounded-2xl bg-red-500/10 text-red-600 grid place-items-center hover:bg-red-500 hover:text-white transition"><i data-lucide="log-out" class="w-5 h-5"></i></button>
            </div>
          </div>
        </header>

        <template x-if="notifications.length"><section class="no-print mb-4 grid md:grid-cols-2 gap-3"><template x-for="n in notifications" :key="n.id"><div class="glass rounded-2xl p-3 border-l-4 border-amber-400 flex gap-3"><i data-lucide="bell-ring" class="w-5 h-5 text-amber-500"></i><p class="text-sm"><b x-text="n.title"></b><br><span class="text-slate-500 dark:text-slate-400" x-text="n.body"></span></p></div></template></section></template>

        <section x-show="view==='dashboard'" class="space-y-4">
          <div class="glass rounded-3xl p-4 md:p-5 flex flex-col lg:flex-row lg:items-center justify-between gap-4">
            <div>
              <p class="text-xs uppercase tracking-[.22em] text-emerald-600 dark:text-emerald-400 font-black">Histórico anual premium</p>
              <h2 class="text-2xl font-black">Dashboard de pagamentos e prestadores</h2>
              <p class="text-sm text-slate-500 dark:text-slate-400">Acompanhe quanto foi gasto mês a mês, por categoria e por prestador.</p>
            </div>
            <div class="flex flex-col sm:flex-row gap-2 w-full lg:w-auto lg:items-end">
              <label class="w-full lg:w-44"><span class="text-sm font-bold">Ano de análise</span><input x-model.number="dashboardYear" type="number" min="2016" max="2099" class="input mt-1"></label>
              <button @click="printAnnualReport" class="secondary h-11 px-4 whitespace-nowrap">PDF anual</button>
            </div>
          </div>

          <div class="grid grid-cols-2 xl:grid-cols-6 gap-3">
            <template x-for="card in dashboardCards" :key="card.label">
              <div class="glass premium dashboard-card rounded-3xl p-4 relative outline-none cursor-pointer"
                   tabindex="0"
                   @mouseenter="activeTooltip=card.label"
                   @mouseleave="activeTooltip=null"
                   @focus="activeTooltip=card.label"
                   @blur="activeTooltip=null"
                   @click.stop="activeTooltip = activeTooltip===card.label ? null : card.label">
                <div class="flex justify-between gap-2"><p class="text-xs text-slate-500 dark:text-slate-400 font-bold" x-text="card.label"></p><i :data-lucide="card.icon" class="w-5 h-5"

===== settings @ 2209329 =====
n x-show="item.kind==='receipt'" @click="openSearchReceipt(item.data)" class="mini">Abrir recibo</button>
                  <button x-show="item.kind==='receipt'" @click="currentReceipt=item.data; printReceipt()" class="mini">Imprimir</button>

                  <button x-show="item.kind==='recurrence'" @click="openSearchRecurrence(item.data)" class="mini">Ver recorrência</button>
                  <button x-show="item.kind==='recurrence'" @click="generateRecurrence(item.data)" class="mini" :disabled="isViewer">Gerar mês</button>
                </div>
              </div>
            </template>
          </div>
          <div x-show="q && !searchResults.length" class="glass rounded-3xl p-5 text-center text-slate-500">
            Nenhum resultado encontrado para a busca atual.
          </div>
          <div x-show="!q" class="glass rounded-3xl p-5 text-center text-slate-500">
            Digite no campo de busca para localizar informações do app.
          </div>
        </section>

        <section x-show="view==='config'" class="space-y-4">
          <form @submit.prevent="saveSettings" class="glass rounded-3xl p-4 space-y-4">
            <div class="flex items-start gap-3">
              <div class="h-11 w-11 rounded-2xl bg-gradient-to-br from-rc-800 to-rc-500 text-white grid place-items-center shadow-soft"><i data-lucide="building-2" class="w-5 h-5"></i></div>
              <div>
                <h3 class="font-black text-lg">Dados do condomínio</h3>
                <p class="text-xs text-slate-500 dark:text-slate-400">Identidade usada nos recibos, relatórios e PDFs de prestação de contas.</p>
              </div>
            </div>
            <div class="grid lg:grid-cols-2 gap-3">
              <label><span class="text-sm font-bold">Nome</span><input x-model="settings.condoName" class="input mt-1"></label>
              <label><span class="text-sm font-bold">Responsável</span><input x-model="settings.manager" class="input mt-1"></label>
            </div>
            <div class="grid lg:grid-cols-2 gap-3">
              <label><span class="text-sm font-bold">CNPJ</span><input x-model="settings.cnpj" class="input mt-1"></label>
              <label><span class="text-sm font-bold">Logo</span><input @change="readLogo" type="file" accept="image/*" class="input mt-1"></label>
            </div>
            <label><span class="text-sm font-bold">Endereço</span><input x-model="settings.address" class="input mt-1"></label>
            <button class="primary">Salvar configurações</button>
          </form>

          <div class="glass rounded-3xl p-4 space-y-4">
            <div class="flex items-start justify-between gap-3">
              <div class="flex items-start gap-3">
                <div class="h-11 w-11 rounded-2xl bg-gradient-to-br from-rc-800 to-rc-gold text-white grid place-items-center shadow-soft"><i data-lucide="list-ordered" class="w-5 h-5"></i></div>
                <div>
                  <h3 class="font-black text-lg">Numeração dos recibos</h3>
                  <p class="text-xs text-slate-500 dark:text-slate-400">Conferência da sequência oficial. O número só é consumido quando um recibo é emitido.</p>
                </div>
              </div>
              <span class="badge" x-text="receiptSequenceStatus()"></span>
            </div>
            <div class="grid md:grid-cols-4 gap-3">
              <div class="rounded-3xl border border-slate-200/70 dark:border-slate-700/70 bg-white/70 dark:bg-slate-950/35 p-4">
                <p class="text-xs uppercase tracking-[.14em] text-slate-500 font-black">Próximo recibo</p>
                <p class="text-2xl font-black receipt-number mt-1" x-text="String(nextReceiptNumber()).padStart(6,'0')"></p>
              </div>
              <div class="rounded-3xl border border-slate-200/70 dark:border-slate-700/70 bg-white/70 dark:bg-slate-950/35 p-4">
                <p class="text-xs uppercase tracking-[.14em] text-slate-500 font-black">Último emitido</p>
                <p class="text-2xl font-black mt-1" x-text="String(receiptMaxNumber()).padStart(6,'0')"></p>
              </div>
              <div class="rounded-3xl border border-slate-200/70 dark:border-slate-700/70 bg-white/70 dark:bg-slate-950/35 p-4">
                <p class="text-xs uppercase tracking-[.14em] text-slate-500 font-black">Recibos</p>
                <p class="text-2xl font-black mt-1" x-text="receipts.length"></p>
              </div>
              <div class="rounded-3xl border border-slate-200/70 dark:border-slate-700/70 bg-white/70 dark:bg-slate-950/35 p-4">
                <p class="text-xs uppercase tracking-[.14em] text-slate-500 font-black">Sequência interna</p>
                <p class="text-2xl font-black mt-1" x-text="String(Number(receiptSeqValue||0)).padStart(6,'0')"></p>
              </div>
            </div>
            <div class="rounded-2xl border border-amber-500/25 bg-amber-500/10 p-3 space-y-3">
              <div class="flex items-start gap-3">
                <i data-lucide="shield-alert" class="w-5 h-5 text-amber-700 mt-1"></i>
                <div>
                  <p class="text-sm font-black text-amber-800 dark:text-amber-300">Ajuste manual protegido</p>
                  <p class="text-xs text-slate-500 dark:text-slate-400">Use apenas se estiver migrando recibos antigos ou alinhando a sequência com documentos já arquivados. O app não permite definir número menor ou igual ao maior recibo já emitido.</p>
                </div>
              </div>
              <div class="grid sm:grid-cols-[1fr_auto_auto] gap-2 items-end">
                <label><span class="text-sm font-bold">Definir próximo número</span><input x-model.number="receiptSeqInput" type="number" min="1" class="input mt-1" placeholder="Ex.: 25"></label>
                <button @click="receiptSeqInput=nextReceiptNumber()" class="secondary" :disabled="isViewer">Usar sugerido</button>
                <button @click="saveReceiptSequence" class="primary" :disabled="isV

===== settings @ 2209464 =====
how="item.kind==='receipt'" @click="currentReceipt=item.data; printReceipt()" class="mini">Imprimir</button>

                  <button x-show="item.kind==='recurrence'" @click="openSearchRecurrence(item.data)" class="mini">Ver recorrência</button>
                  <button x-show="item.kind==='recurrence'" @click="generateRecurrence(item.data)" class="mini" :disabled="isViewer">Gerar mês</button>
                </div>
              </div>
            </template>
          </div>
          <div x-show="q && !searchResults.length" class="glass rounded-3xl p-5 text-center text-slate-500">
            Nenhum resultado encontrado para a busca atual.
          </div>
          <div x-show="!q" class="glass rounded-3xl p-5 text-center text-slate-500">
            Digite no campo de busca para localizar informações do app.
          </div>
        </section>

        <section x-show="view==='config'" class="space-y-4">
          <form @submit.prevent="saveSettings" class="glass rounded-3xl p-4 space-y-4">
            <div class="flex items-start gap-3">
              <div class="h-11 w-11 rounded-2xl bg-gradient-to-br from-rc-800 to-rc-500 text-white grid place-items-center shadow-soft"><i data-lucide="building-2" class="w-5 h-5"></i></div>
              <div>
                <h3 class="font-black text-lg">Dados do condomínio</h3>
                <p class="text-xs text-slate-500 dark:text-slate-400">Identidade usada nos recibos, relatórios e PDFs de prestação de contas.</p>
              </div>
            </div>
            <div class="grid lg:grid-cols-2 gap-3">
              <label><span class="text-sm font-bold">Nome</span><input x-model="settings.condoName" class="input mt-1"></label>
              <label><span class="text-sm font-bold">Responsável</span><input x-model="settings.manager" class="input mt-1"></label>
            </div>
            <div class="grid lg:grid-cols-2 gap-3">
              <label><span class="text-sm font-bold">CNPJ</span><input x-model="settings.cnpj" class="input mt-1"></label>
              <label><span class="text-sm font-bold">Logo</span><input @change="readLogo" type="file" accept="image/*" class="input mt-1"></label>
            </div>
            <label><span class="text-sm font-bold">Endereço</span><input x-model="settings.address" class="input mt-1"></label>
            <button class="primary">Salvar configurações</button>
          </form>

          <div class="glass rounded-3xl p-4 space-y-4">
            <div class="flex items-start justify-between gap-3">
              <div class="flex items-start gap-3">
                <div class="h-11 w-11 rounded-2xl bg-gradient-to-br from-rc-800 to-rc-gold text-white grid place-items-center shadow-soft"><i data-lucide="list-ordered" class="w-5 h-5"></i></div>
                <div>
                  <h3 class="font-black text-lg">Numeração dos recibos</h3>
                  <p class="text-xs text-slate-500 dark:text-slate-400">Conferência da sequência oficial. O número só é consumido quando um recibo é emitido.</p>
                </div>
              </div>
              <span class="badge" x-text="receiptSequenceStatus()"></span>
            </div>
            <div class="grid md:grid-cols-4 gap-3">
              <div class="rounded-3xl border border-slate-200/70 dark:border-slate-700/70 bg-white/70 dark:bg-slate-950/35 p-4">
                <p class="text-xs uppercase tracking-[.14em] text-slate-500 font-black">Próximo recibo</p>
                <p class="text-2xl font-black receipt-number mt-1" x-text="String(nextReceiptNumber()).padStart(6,'0')"></p>
              </div>
              <div class="rounded-3xl border border-slate-200/70 dark:border-slate-700/70 bg-white/70 dark:bg-slate-950/35 p-4">
                <p class="text-xs uppercase tracking-[.14em] text-slate-500 font-black">Último emitido</p>
                <p class="text-2xl font-black mt-1" x-text="String(receiptMaxNumber()).padStart(6,'0')"></p>
              </div>
              <div class="rounded-3xl border border-slate-200/70 dark:border-slate-700/70 bg-white/70 dark:bg-slate-950/35 p-4">
                <p class="text-xs uppercase tracking-[.14em] text-slate-500 font-black">Recibos</p>
                <p class="text-2xl font-black mt-1" x-text="receipts.length"></p>
              </div>
              <div class="rounded-3xl border border-slate-200/70 dark:border-slate-700/70 bg-white/70 dark:bg-slate-950/35 p-4">
                <p class="text-xs uppercase tracking-[.14em] text-slate-500 font-black">Sequência interna</p>
                <p class="text-2xl font-black mt-1" x-text="String(Number(receiptSeqValue||0)).padStart(6,'0')"></p>
              </div>
            </div>
            <div class="rounded-2xl border border-amber-500/25 bg-amber-500/10 p-3 space-y-3">
              <div class="flex items-start gap-3">
                <i data-lucide="shield-alert" class="w-5 h-5 text-amber-700 mt-1"></i>
                <div>
                  <p class="text-sm font-black text-amber-800 dark:text-amber-300">Ajuste manual protegido</p>
                  <p class="text-xs text-slate-500 dark:text-slate-400">Use apenas se estiver migrando recibos antigos ou alinhando a sequência com documentos já arquivados. O app não permite definir número menor ou igual ao maior recibo já emitido.</p>
                </div>
              </div>
              <div class="grid sm:grid-cols-[1fr_auto_auto] gap-2 items-end">
                <label><span class="text-sm font-bold">Definir próximo número</span><input x-model.number="receiptSeqInput" type="number" min="1" class="input mt-1" placeholder="Ex.: 25"></label>
                <button @click="receiptSeqInput=nextReceiptNumber()" class="secondary" :disabled="isViewer">Usar sugerido</button>
                <button @click="saveReceiptSequence" class="primary" :disabled="isViewer">Salvar sequência</button>
              </div>
            </div>
          </div>

          <div class="glass rounded-3xl p-4 

===== settings @ 2209661 =====
e(item.data)" class="mini">Ver recorrência</button>
                  <button x-show="item.kind==='recurrence'" @click="generateRecurrence(item.data)" class="mini" :disabled="isViewer">Gerar mês</button>
                </div>
              </div>
            </template>
          </div>
          <div x-show="q && !searchResults.length" class="glass rounded-3xl p-5 text-center text-slate-500">
            Nenhum resultado encontrado para a busca atual.
          </div>
          <div x-show="!q" class="glass rounded-3xl p-5 text-center text-slate-500">
            Digite no campo de busca para localizar informações do app.
          </div>
        </section>

        <section x-show="view==='config'" class="space-y-4">
          <form @submit.prevent="saveSettings" class="glass rounded-3xl p-4 space-y-4">
            <div class="flex items-start gap-3">
              <div class="h-11 w-11 rounded-2xl bg-gradient-to-br from-rc-800 to-rc-500 text-white grid place-items-center shadow-soft"><i data-lucide="building-2" class="w-5 h-5"></i></div>
              <div>
                <h3 class="font-black text-lg">Dados do condomínio</h3>
                <p class="text-xs text-slate-500 dark:text-slate-400">Identidade usada nos recibos, relatórios e PDFs de prestação de contas.</p>
              </div>
            </div>
            <div class="grid lg:grid-cols-2 gap-3">
              <label><span class="text-sm font-bold">Nome</span><input x-model="settings.condoName" class="input mt-1"></label>
              <label><span class="text-sm font-bold">Responsável</span><input x-model="settings.manager" class="input mt-1"></label>
            </div>
            <div class="grid lg:grid-cols-2 gap-3">
              <label><span class="text-sm font-bold">CNPJ</span><input x-model="settings.cnpj" class="input mt-1"></label>
              <label><span class="text-sm font-bold">Logo</span><input @change="readLogo" type="file" accept="image/*" class="input mt-1"></label>
            </div>
            <label><span class="text-sm font-bold">Endereço</span><input x-model="settings.address" class="input mt-1"></label>
            <button class="primary">Salvar configurações</button>
          </form>

          <div class="glass rounded-3xl p-4 space-y-4">
            <div class="flex items-start justify-between gap-3">
              <div class="flex items-start gap-3">
                <div class="h-11 w-11 rounded-2xl bg-gradient-to-br from-rc-800 to-rc-gold text-white grid place-items-center shadow-soft"><i data-lucide="list-ordered" class="w-5 h-5"></i></div>
                <div>
                  <h3 class="font-black text-lg">Numeração dos recibos</h3>
                  <p class="text-xs text-slate-500 dark:text-slate-400">Conferência da sequência oficial. O número só é consumido quando um recibo é emitido.</p>
                </div>
              </div>
              <span class="badge" x-text="receiptSequenceStatus()"></span>
            </div>
            <div class="grid md:grid-cols-4 gap-3">
              <div class="rounded-3xl border border-slate-200/70 dark:border-slate-700/70 bg-white/70 dark:bg-slate-950/35 p-4">
                <p class="text-xs uppercase tracking-[.14em] text-slate-500 font-black">Próximo recibo</p>
                <p class="text-2xl font-black receipt-number mt-1" x-text="String(nextReceiptNumber()).padStart(6,'0')"></p>
              </div>
              <div class="rounded-3xl border border-slate-200/70 dark:border-slate-700/70 bg-white/70 dark:bg-slate-950/35 p-4">
                <p class="text-xs uppercase tracking-[.14em] text-slate-500 font-black">Último emitido</p>
                <p class="text-2xl font-black mt-1" x-text="String(receiptMaxNumber()).padStart(6,'0')"></p>
              </div>
              <div class="rounded-3xl border border-slate-200/70 dark:border-slate-700/70 bg-white/70 dark:bg-slate-950/35 p-4">
                <p class="text-xs uppercase tracking-[.14em] text-slate-500 font-black">Recibos</p>
                <p class="text-2xl font-black mt-1" x-text="receipts.length"></p>
              </div>
              <div class="rounded-3xl border border-slate-200/70 dark:border-slate-700/70 bg-white/70 dark:bg-slate-950/35 p-4">
                <p class="text-xs uppercase tracking-[.14em] text-slate-500 font-black">Sequência interna</p>
                <p class="text-2xl font-black mt-1" x-text="String(Number(receiptSeqValue||0)).padStart(6,'0')"></p>
              </div>
            </div>
            <div class="rounded-2xl border border-amber-500/25 bg-amber-500/10 p-3 space-y-3">
              <div class="flex items-start gap-3">
                <i data-lucide="shield-alert" class="w-5 h-5 text-amber-700 mt-1"></i>
                <div>
                  <p class="text-sm font-black text-amber-800 dark:text-amber-300">Ajuste manual protegido</p>
                  <p class="text-xs text-slate-500 dark:text-slate-400">Use apenas se estiver migrando recibos antigos ou alinhando a sequência com documentos já arquivados. O app não permite definir número menor ou igual ao maior recibo já emitido.</p>
                </div>
              </div>
              <div class="grid sm:grid-cols-[1fr_auto_auto] gap-2 items-end">
                <label><span class="text-sm font-bold">Definir próximo número</span><input x-model.number="receiptSeqInput" type="number" min="1" class="input mt-1" placeholder="Ex.: 25"></label>
                <button @click="receiptSeqInput=nextReceiptNumber()" class="secondary" :disabled="isViewer">Usar sugerido</button>
                <button @click="saveReceiptSequence" class="primary" :disabled="isViewer">Salvar sequência</button>
              </div>
            </div>
          </div>

          <div class="glass rounded-3xl p-4 space-y-4">
            <div class="flex items-start gap-3">
              <div class="h-11 w-11 rounded-2xl bg-gradient-to-br from-slate-800 to-red-700 text-white grid place-items-center shadow-so

===== settings @ 2209952 =====
        <div x-show="q && !searchResults.length" class="glass rounded-3xl p-5 text-center text-slate-500">
            Nenhum resultado encontrado para a busca atual.
          </div>
          <div x-show="!q" class="glass rounded-3xl p-5 text-center text-slate-500">
            Digite no campo de busca para localizar informações do app.
          </div>
        </section>

        <section x-show="view==='config'" class="space-y-4">
          <form @submit.prevent="saveSettings" class="glass rounded-3xl p-4 space-y-4">
            <div class="flex items-start gap-3">
              <div class="h-11 w-11 rounded-2xl bg-gradient-to-br from-rc-800 to-rc-500 text-white grid place-items-center shadow-soft"><i data-lucide="building-2" class="w-5 h-5"></i></div>
              <div>
                <h3 class="font-black text-lg">Dados do condomínio</h3>
                <p class="text-xs text-slate-500 dark:text-slate-400">Identidade usada nos recibos, relatórios e PDFs de prestação de contas.</p>
              </div>
            </div>
            <div class="grid lg:grid-cols-2 gap-3">
              <label><span class="text-sm font-bold">Nome</span><input x-model="settings.condoName" class="input mt-1"></label>
              <label><span class="text-sm font-bold">Responsável</span><input x-model="settings.manager" class="input mt-1"></label>
            </div>
            <div class="grid lg:grid-cols-2 gap-3">
              <label><span class="text-sm font-bold">CNPJ</span><input x-model="settings.cnpj" class="input mt-1"></label>
              <label><span class="text-sm font-bold">Logo</span><input @change="readLogo" type="file" accept="image/*" class="input mt-1"></label>
            </div>
            <label><span class="text-sm font-bold">Endereço</span><input x-model="settings.address" class="input mt-1"></label>
            <button class="primary">Salvar configurações</button>
          </form>

          <div class="glass rounded-3xl p-4 space-y-4">
            <div class="flex items-start justify-between gap-3">
              <div class="flex items-start gap-3">
                <div class="h-11 w-11 rounded-2xl bg-gradient-to-br from-rc-800 to-rc-gold text-white grid place-items-center shadow-soft"><i data-lucide="list-ordered" class="w-5 h-5"></i></div>
                <div>
                  <h3 class="font-black text-lg">Numeração dos recibos</h3>
                  <p class="text-xs text-slate-500 dark:text-slate-400">Conferência da sequência oficial. O número só é consumido quando um recibo é emitido.</p>
                </div>
              </div>
              <span class="badge" x-text="receiptSequenceStatus()"></span>
            </div>
            <div class="grid md:grid-cols-4 gap-3">
              <div class="rounded-3xl border border-slate-200/70 dark:border-slate-700/70 bg-white/70 dark:bg-slate-950/35 p-4">
                <p class="text-xs uppercase tracking-[.14em] text-slate-500 font-black">Próximo recibo</p>
                <p class="text-2xl font-black receipt-number mt-1" x-text="String(nextReceiptNumber()).padStart(6,'0')"></p>
              </div>
              <div class="rounded-3xl border border-slate-200/70 dark:border-slate-700/70 bg-white/70 dark:bg-slate-950/35 p-4">
                <p class="text-xs uppercase tracking-[.14em] text-slate-500 font-black">Último emitido</p>
                <p class="text-2xl font-black mt-1" x-text="String(receiptMaxNumber()).padStart(6,'0')"></p>
              </div>
              <div class="rounded-3xl border border-slate-200/70 dark:border-slate-700/70 bg-white/70 dark:bg-slate-950/35 p-4">
                <p class="text-xs uppercase tracking-[.14em] text-slate-500 font-black">Recibos</p>
                <p class="text-2xl font-black mt-1" x-text="receipts.length"></p>
              </div>
              <div class="rounded-3xl border border-slate-200/70 dark:border-slate-700/70 bg-white/70 dark:bg-slate-950/35 p-4">
                <p class="text-xs uppercase tracking-[.14em] text-slate-500 font-black">Sequência interna</p>
                <p class="text-2xl font-black mt-1" x-text="String(Number(receiptSeqValue||0)).padStart(6,'0')"></p>
              </div>
            </div>
            <div class="rounded-2xl border border-amber-500/25 bg-amber-500/10 p-3 space-y-3">
              <div class="flex items-start gap-3">
                <i data-lucide="shield-alert" class="w-5 h-5 text-amber-700 mt-1"></i>
                <div>
                  <p class="text-sm font-black text-amber-800 dark:text-amber-300">Ajuste manual protegido</p>
                  <p class="text-xs text-slate-500 dark:text-slate-400">Use apenas se estiver migrando recibos antigos ou alinhando a sequência com documentos já arquivados. O app não permite definir número menor ou igual ao maior recibo já emitido.</p>
                </div>
              </div>
              <div class="grid sm:grid-cols-[1fr_auto_auto] gap-2 items-end">
                <label><span class="text-sm font-bold">Definir próximo número</span><input x-model.number="receiptSeqInput" type="number" min="1" class="input mt-1" placeholder="Ex.: 25"></label>
                <button @click="receiptSeqInput=nextReceiptNumber()" class="secondary" :disabled="isViewer">Usar sugerido</button>
                <button @click="saveReceiptSequence" class="primary" :disabled="isViewer">Salvar sequência</button>
              </div>
            </div>
          </div>

          <div class="glass rounded-3xl p-4 space-y-4">
            <div class="flex items-start gap-3">
              <div class="h-11 w-11 rounded-2xl bg-gradient-to-br from-slate-800 to-red-700 text-white grid place-items-center shadow-soft"><i data-lucide="key-round" class="w-5 h-5"></i></div>
              <div>
                <h3 class="font-black text-lg">Senhas e acesso</h3>
                <p class="text-xs text-slate-500 dark:text-slate-400">Área sensível para alterar senhas e apagar dados locais.</p>
              

===== settings @ 2223216 =====
   <button @click="closeActionModal" class="secondary">Cancelar</button>
          </div>
        </div>
      </div>

      <div class="no-print fixed top-4 right-4 z-[80] w-[min(92vw,390px)] space-y-3 pointer-events-none">
        <template x-for="t in toasts" :key="t.id">
          <div x-transition.opacity.scale.origin.top.right class="pointer-events-auto rounded-3xl border bg-white/95 dark:bg-slate-900/95 backdrop-blur-xl shadow-premium overflow-hidden" :class="t.border">
            <div class="h-1" :class="t.bar"></div>
            <div class="p-4 flex gap-3 items-start">
              <div class="h-10 w-10 rounded-2xl grid place-items-center text-white shrink-0" :class="t.iconBg">
                <i :data-lucide="t.icon" class="w-5 h-5"></i>
              </div>
              <div class="min-w-0 flex-1">
                <p class="font-black text-sm text-slate-900 dark:text-white" x-text="t.title"></p>
                <p class="text-xs text-slate-600 dark:text-slate-300 leading-relaxed mt-1" x-text="t.message"></p>
              </div>
              <button @click="dismissToast(t.id)" class="h-8 w-8 rounded-xl grid place-items-center text-slate-400 hover:text-slate-700 hover:bg-slate-100 dark:hover:bg-slate-800">
                <i data-lucide="x" class="w-4 h-4"></i>
              </button>
            </div>
          </div>
        </template>
      </div>

      <section class="print-area">
        <div id="receiptPrint" class="receipt receipt-premium" x-show="currentReceipt">
          <div class="receipt-watermark">ReciboCondo</div>
          <div class="pt-7 relative z-10">
            <div class="flex justify-between gap-6 items-start border-b border-slate-300 pb-5 mb-5">
              <div class="flex gap-4 items-center">
                <template x-if="settings.logo"><img :src="settings.logo" class="receipt-app-logo" alt="Logotipo do condomínio"></template>
                <template x-if="!settings.logo"><img src="data:image/png;base64,

===== settings @ 2223242 =====
tionModal" class="secondary">Cancelar</button>
          </div>
        </div>
      </div>

      <div class="no-print fixed top-4 right-4 z-[80] w-[min(92vw,390px)] space-y-3 pointer-events-none">
        <template x-for="t in toasts" :key="t.id">
          <div x-transition.opacity.scale.origin.top.right class="pointer-events-auto rounded-3xl border bg-white/95 dark:bg-slate-900/95 backdrop-blur-xl shadow-premium overflow-hidden" :class="t.border">
            <div class="h-1" :class="t.bar"></div>
            <div class="p-4 flex gap-3 items-start">
              <div class="h-10 w-10 rounded-2xl grid place-items-center text-white shrink-0" :class="t.iconBg">
                <i :data-lucide="t.icon" class="w-5 h-5"></i>
              </div>
              <div class="min-w-0 flex-1">
                <p class="font-black text-sm text-slate-900 dark:text-white" x-text="t.title"></p>
                <p class="text-xs text-slate-600 dark:text-slate-300 leading-relaxed mt-1" x-text="t.message"></p>
              </div>
              <button @click="dismissToast(t.id)" class="h-8 w-8 rounded-xl grid place-items-center text-slate-400 hover:text-slate-700 hover:bg-slate-100 dark:hover:bg-slate-800">
                <i data-lucide="x" class="w-4 h-4"></i>
              </button>
            </div>
          </div>
        </template>
      </div>

      <section class="print-area">
        <div id="receiptPrint" class="receipt receipt-premium" x-show="currentReceipt">
          <div class="receipt-watermark">ReciboCondo</div>
          <div class="pt-7 relative z-10">
            <div class="flex justify-between gap-6 items-start border-b border-slate-300 pb-5 mb-5">
              <div class="flex gap-4 items-center">
                <template x-if="settings.logo"><img :src="settings.logo" class="receipt-app-logo" alt="Logotipo do condomínio"></template>
                <template x-if="!settings.logo"><img src="data:image/png;base64,

===== settings @ 2223356 =====
print fixed top-4 right-4 z-[80] w-[min(92vw,390px)] space-y-3 pointer-events-none">
        <template x-for="t in toasts" :key="t.id">
          <div x-transition.opacity.scale.origin.top.right class="pointer-events-auto rounded-3xl border bg-white/95 dark:bg-slate-900/95 backdrop-blur-xl shadow-premium overflow-hidden" :class="t.border">
            <div class="h-1" :class="t.bar"></div>
            <div class="p-4 flex gap-3 items-start">
              <div class="h-10 w-10 rounded-2xl grid place-items-center text-white shrink-0" :class="t.iconBg">
                <i :data-lucide="t.icon" class="w-5 h-5"></i>
              </div>
              <div class="min-w-0 flex-1">
                <p class="font-black text-sm text-slate-900 dark:text-white" x-text="t.title"></p>
                <p class="text-xs text-slate-600 dark:text-slate-300 leading-relaxed mt-1" x-text="t.message"></p>
              </div>
              <button @click="dismissToast(t.id)" class="h-8 w-8 rounded-xl grid place-items-center text-slate-400 hover:text-slate-700 hover:bg-slate-100 dark:hover:bg-slate-800">
                <i data-lucide="x" class="w-4 h-4"></i>
              </button>
            </div>
          </div>
        </template>
      </div>

      <section class="print-area">
        <div id="receiptPrint" class="receipt receipt-premium" x-show="currentReceipt">
          <div class="receipt-watermark">ReciboCondo</div>
          <div class="pt-7 relative z-10">
            <div class="flex justify-between gap-6 items-start border-b border-slate-300 pb-5 mb-5">
              <div class="flex gap-4 items-center">
                <template x-if="settings.logo"><img :src="settings.logo" class="receipt-app-logo" alt="Logotipo do condomínio"></template>
                <template x-if="!settings.logo"><img src="data:image/png;base64,

===== settings @ 4326318 =====
" class="receipt-app-logo" alt="Logotipo ReciboCondo"></template>
                <div>
                  <h1 class="text-2xl font-bold leading-tight" x-text="settings.condoName"></h1>
                  <p class="text-sm text-slate-600" x-text="settings.cnpj"></p>
                  <p class="text-sm text-slate-600 max-w-[118mm]" x-text="settings.address"></p>
                </div>
              </div>
              <div class="text-right shrink-0">
                <span class="receipt-stamp">Prestação de contas</span>
                <p class="receipt-label mt-3">Recibo Nº</p>
                <p class="text-2xl font-black receipt-number" x-text="currentReceipt?.number"></p>
                <p class="receipt-label mt-2">Emissão</p>
                <p class="receipt-value" x-text="fmt(currentReceipt?.date)"></p>
              </div>
            </div>

            <div class="mb-5">
              <p class="receipt-label">Documento financeiro</p>
              <h2 class="text-3xl font-black tracking-tight text-slate-900">RECIBO DE PRESTAÇÃO DE SERVIÇO</h2>
              <p class="text-sm text-slate-600 mt-1">Comprovante para arquivo interno, administradora e livro de prestação de contas.</p>
            </div>

            <div class="grid grid-cols-2 gap-4 mb-5">
              <div class="receipt-box p-4">
                <p class="receipt-label">Pagador / Condomínio</p>
                <p class="text-lg font-bold" x-text="settings.condoName"></p>
                <p class="text-sm text-slate-600" x-text="settings.cnpj"></p>
              </div>
              <div class="receipt-box p-4">
                <p class="receipt-label">Favorecido / Prestador</p>
                <p class="text-lg font-bold" x-text="currentReceipt?.providerName"></p>
                <p class="text-sm text-slate-600">CPF/CNPJ: <span x-text="currentReceipt?.document || 'não informado'"></span></p>
              </div>
            </div>

            <div class="receipt-box p-5 mb-5">
              <p class="leading-8 text-[17px] text-slate-800">
                Declaro, para os devidos fins, que recebi de <b x-text="settings.condoName"></b> a importância de
                <b x-text="money(currentReceipt?.total)"></b>
                (<span class="font-bold" x-text="valorPorExtenso(currentReceipt?.total)"></span>), referente aos serviços discriminados neste recibo.
              </p>
            </div>

            <table class="receipt-table mb-5">
              <thead><tr><th style="width:28%">Campo</th><th>Informação</th></tr></thead>
              <tbody>
                <tr><td class="font-bold">Categoria</td><td x-text="currentReceipt?.category"></td></tr>
                <tr><td class="font-bold">Descrição detalhada</td><td x-text="currentReceipt?.description"></td></tr>
                <tr><td class="font-bold">Referência</td><td x-text="month(currentReceipt?.reference)"></td></tr>
                <tr><td class="font-bold">Local / área atendida</td><td x-text="currentReceipt?.location || '-'"></td></tr>
                <tr><td class="font-bold">Data do pagamento</td><td x-text="fmt(currentReceipt?.paymentDate || currentReceipt?.date)"></td></tr>
                <tr><td class="font-bold">Forma de pagamento</td><td x-text="currentReceipt?.paymentMethod || 'Não informado'"></td></tr>
              </tbody>
            </table>

            <div class="flex justify-between items-stretch gap-5 mb-7">
              <div class="receipt-box p-4 flex-1">
                <p class="receipt-label">Responsável pelo lançamento</p>
                <p class="receipt-value text-lg" x-text="settings.manager"></p>
                <p class="text-xs text-slate-500 mt-1">Síndico, tesoureiro ou representante autorizado</p>
              </div>
              <div class="receipt-total min-w-[72mm]">
                <p class="text-xs uppercase tracking-[.18em] receipt-total-label">Valor total recebido</p>
                <p class="receipt-total-amount mt-1" x-text="money(currentReceipt?.total)"></p>
              </div>
            </div>

            <div class="grid grid-cols-2 gap-12 text-center mt-14">
              <div><div class="border-t border-slate-900 pt-2 font-bold">Assinatura do prestador</div><p class="text-xs text-slate-500 mt-1" x-text="currentReceipt?.providerName"></p></div>
              <div><div class="bo

===== settings @ 4326404 =====
" class="receipt-app-logo" alt="Logotipo ReciboCondo"></template>
                <div>
                  <h1 class="text-2xl font-bold leading-tight" x-text="settings.condoName"></h1>
                  <p class="text-sm text-slate-600" x-text="settings.cnpj"></p>
                  <p class="text-sm text-slate-600 max-w-[118mm]" x-text="settings.address"></p>
                </div>
              </div>
              <div class="text-right shrink-0">
                <span class="receipt-stamp">Prestação de contas</span>
                <p class="receipt-label mt-3">Recibo Nº</p>
                <p class="text-2xl font-black receipt-number" x-text="currentReceipt?.number"></p>
                <p class="receipt-label mt-2">Emissão</p>
                <p class="receipt-value" x-text="fmt(currentReceipt?.date)"></p>
              </div>
            </div>

            <div class="mb-5">
              <p class="receipt-label">Documento financeiro</p>
              <h2 class="text-3xl font-black tracking-tight text-slate-900">RECIBO DE PRESTAÇÃO DE SERVIÇO</h2>
              <p class="text-sm text-slate-600 mt-1">Comprovante para arquivo interno, administradora e livro de prestação de contas.</p>
            </div>

            <div class="grid grid-cols-2 gap-4 mb-5">
              <div class="receipt-box p-4">
                <p class="receipt-label">Pagador / Condomínio</p>
                <p class="text-lg font-bold" x-text="settings.condoName"></p>
                <p class="text-sm text-slate-600" x-text="settings.cnpj"></p>
              </div>
              <div class="receipt-box p-4">
                <p class="receipt-label">Favorecido / Prestador</p>
                <p class="text-lg font-bold" x-text="currentReceipt?.providerName"></p>
                <p class="text-sm text-slate-600">CPF/CNPJ: <span x-text="currentReceipt?.document || 'não informado'"></span></p>
              </div>
            </div>

            <div class="receipt-box p-5 mb-5">
              <p class="leading-8 text-[17px] text-slate-800">
                Declaro, para os devidos fins, que recebi de <b x-text="settings.condoName"></b> a importância de
                <b x-text="money(currentReceipt?.total)"></b>
                (<span class="font-bold" x-text="valorPorExtenso(currentReceipt?.total)"></span>), referente aos serviços discriminados neste recibo.
              </p>
            </div>

            <table class="receipt-table mb-5">
              <thead><tr><th style="width:28%">Campo</th><th>Informação</th></tr></thead>
              <tbody>
                <tr><td class="font-bold">Categoria</td><td x-text="currentReceipt?.category"></td></tr>
                <tr><td class="font-bold">Descrição detalhada</td><td x-text="currentReceipt?.description"></td></tr>
                <tr><td class="font-bold">Referência</td><td x-text="month(currentReceipt?.reference)"></td></tr>
                <tr><td class="font-bold">Local / área atendida</td><td x-text="currentReceipt?.location || '-'"></td></tr>
                <tr><td class="font-bold">Data do pagamento</td><td x-text="fmt(currentReceipt?.paymentDate || currentReceipt?.date)"></td></tr>
                <tr><td class="font-bold">Forma de pagamento</td><td x-text="currentReceipt?.paymentMethod || 'Não informado'"></td></tr>
              </tbody>
            </table>

            <div class="flex justify-between items-stretch gap-5 mb-7">
              <div class="receipt-box p-4 flex-1">
                <p class="receipt-label">Responsável pelo lançamento</p>
                <p class="receipt-value text-lg" x-text="settings.manager"></p>
                <p class="text-xs text-slate-500 mt-1">Síndico, tesoureiro ou representante autorizado</p>
              </div>
              <div class="receipt-total min-w-[72mm]">
                <p class="text-xs uppercase tracking-[.18em] receipt-total-label">Valor total recebido</p>
                <p class="receipt-total-amount mt-1" x-text="money(currentReceipt?.total)"></p>
              </div>
            </div>

            <div class="grid grid-cols-2 gap-12 text-center mt-14">
              <div><div class="border-t border-slate-900 pt-2 font-bold">Assinatura do prestador</div><p class="text-xs text-slate-500 mt-1" x-text="currentReceipt?.providerName"></p></div>
              <div><div class="border-t border-slate-900 pt-2 font-bold">Conferência do condomínio</div><p class="text-

===== settings @ 4326498 =====
" class="receipt-app-logo" alt="Logotipo ReciboCondo"></template>
                <div>
                  <h1 class="text-2xl font-bold leading-tight" x-text="settings.condoName"></h1>
                  <p class="text-sm text-slate-600" x-text="settings.cnpj"></p>
                  <p class="text-sm text-slate-600 max-w-[118mm]" x-text="settings.address"></p>
                </div>
              </div>
              <div class="text-right shrink-0">
                <span class="receipt-stamp">Prestação de contas</span>
                <p class="receipt-label mt-3">Recibo Nº</p>
                <p class="text-2xl font-black receipt-number" x-text="currentReceipt?.number"></p>
                <p class="receipt-label mt-2">Emissão</p>
                <p class="receipt-value" x-text="fmt(currentReceipt?.date)"></p>
              </div>
            </div>

            <div class="mb-5">
              <p class="receipt-label">Documento financeiro</p>
              <h2 class="text-3xl font-black tracking-tight text-slate-900">RECIBO DE PRESTAÇÃO DE SERVIÇO</h2>
              <p class="text-sm text-slate-600 mt-1">Comprovante para arquivo interno, administradora e livro de prestação de contas.</p>
            </div>

            <div class="grid grid-cols-2 gap-4 mb-5">
              <div class="receipt-box p-4">
                <p class="receipt-label">Pagador / Condomínio</p>
                <p class="text-lg font-bold" x-text="settings.condoName"></p>
                <p class="text-sm text-slate-600" x-text="settings.cnpj"></p>
              </div>
              <div class="receipt-box p-4">
                <p class="receipt-label">Favorecido / Prestador</p>
                <p class="text-lg font-bold" x-text="currentReceipt?.providerName"></p>
                <p class="text-sm text-slate-600">CPF/CNPJ: <span x-text="currentReceipt?.document || 'não informado'"></span></p>
              </div>
            </div>

            <div class="receipt-box p-5 mb-5">
              <p class="leading-8 text-[17px] text-slate-800">
                Declaro, para os devidos fins, que recebi de <b x-text="settings.condoName"></b> a importância de
                <b x-text="money(currentReceipt?.total)"></b>
                (<span class="font-bold" x-text="valorPorExtenso(currentReceipt?.total)"></span>), referente aos serviços discriminados neste recibo.
              </p>
            </div>

            <table class="receipt-table mb-5">
              <thead><tr><th style="width:28%">Campo</th><th>Informação</th></tr></thead>
              <tbody>
                <tr><td class="font-bold">Categoria</td><td x-text="currentReceipt?.category"></td></tr>
                <tr><td class="font-bold">Descrição detalhada</td><td x-text="currentReceipt?.description"></td></tr>
                <tr><td class="font-bold">Referência</td><td x-text="month(currentReceipt?.reference)"></td></tr>
                <tr><td class="font-bold">Local / área atendida</td><td x-text="currentReceipt?.location || '-'"></td></tr>
                <tr><td class="font-bold">Data do pagamento</td><td x-text="fmt(currentReceipt?.paymentDate || currentReceipt?.date)"></td></tr>
                <tr><td class="font-bold">Forma de pagamento</td><td x-text="currentReceipt?.paymentMethod || 'Não informado'"></td></tr>
              </tbody>
            </table>

            <div class="flex justify-between items-stretch gap-5 mb-7">
              <div class="receipt-box p-4 flex-1">
                <p class="receipt-label">Responsável pelo lançamento</p>
                <p class="receipt-value text-lg" x-text="settings.manager"></p>
                <p class="text-xs text-slate-500 mt-1">Síndico, tesoureiro ou representante autorizado</p>
              </div>
              <div class="receipt-total min-w-[72mm]">
                <p class="text-xs uppercase tracking-[.18em] receipt-total-label">Valor total recebido</p>
                <p class="receipt-total-amount mt-1" x-text="money(currentReceipt?.total)"></p>
              </div>
            </div>

            <div class="grid grid-cols-2 gap-12 text-center mt-14">
              <div><div class="border-t border-slate-900 pt-2 font-bold">Assinatura do prestador</div><p class="text-xs text-slate-500 mt-1" x-text="currentReceipt?.providerName"></p></div>
              <div><div class="border-t border-slate-900 pt-2 font-bold">Conferência do condomínio</div><p class="text-xs text-slate-500 mt-1" x-text="settings.manager"></p></div>
            </div>

            <

===== settings @ 4327605 =====
" class="receipt-app-logo" alt="Logotipo ReciboCondo"></template>
                <div>
                  <h1 class="text-2xl font-bold leading-tight" x-text="settings.condoName"></h1>
                  <p class="text-sm text-slate-600" x-text="settings.cnpj"></p>
                  <p class="text-sm text-slate-600 max-w-[118mm]" x-text="settings.address"></p>
                </div>
              </div>
              <div class="text-right shrink-0">
                <span class="receipt-stamp">Prestação de contas</span>
                <p class="receipt-label mt-3">Recibo Nº</p>
                <p class="text-2xl font-black receipt-number" x-text="currentReceipt?.number"></p>
                <p class="receipt-label mt-2">Emissão</p>
                <p class="receipt-value" x-text="fmt(currentReceipt?.date)"></p>
              </div>
            </div>

            <div class="mb-5">
              <p class="receipt-label">Documento financeiro</p>
              <h2 class="text-3xl font-black tracking-tight text-slate-900">RECIBO DE PRESTAÇÃO DE SERVIÇO</h2>
              <p class="text-sm text-slate-600 mt-1">Comprovante para arquivo interno, administradora e livro de prestação de contas.</p>
            </div>

            <div class="grid grid-cols-2 gap-4 mb-5">
              <div class="receipt-box p-4">
                <p class="receipt-label">Pagador / Condomínio</p>
                <p class="text-lg font-bold" x-text="settings.condoName"></p>
                <p class="text-sm text-slate-600" x-text="settings.cnpj"></p>
              </div>
              <div class="receipt-box p-4">
                <p class="receipt-label">Favorecido / Prestador</p>
                <p class="text-lg font-bold" x-text="currentReceipt?.providerName"></p>
                <p class="text-sm text-slate-600">CPF/CNPJ: <span x-text="currentReceipt?.document || 'não informado'"></span></p>
              </div>
            </div>

            <div class="receipt-box p-5 mb-5">
              <p class="leading-8 text-[17px] text-slate-800">
                Declaro, para os devidos fins, que recebi de <b x-text="settings.condoName"></b> a importância de
                <b x-text="money(currentReceipt?.total)"></b>
                (<span class="font-bold" x-text="valorPorExtenso(currentReceipt?.total)"></span>), referente aos serviços discriminados neste recibo.
              </p>
            </div>

            <table class="receipt-table mb-5">
              <thead><tr><th style="width:28%">Campo</th><th>Informação</th></tr></thead>
              <tbody>
                <tr><td class="font-bold">Categoria</td><td x-text="currentReceipt?.category"></td></tr>
                <tr><td class="font-bold">Descrição detalhada</td><td x-text="currentReceipt?.description"></td></tr>
                <tr><td class="font-bold">Referência</td><td x-text="month(currentReceipt?.reference)"></td></tr>
                <tr><td class="font-bold">Local / área atendida</td><td x-text="currentReceipt?.location || '-'"></td></tr>
                <tr><td class="font-bold">Data do pagamento</td><td x-text="fmt(currentReceipt?.paymentDate || currentReceipt?.date)"></td></tr>
                <tr><td class="font-bold">Forma de pagamento</td><td x-text="currentReceipt?.paymentMethod || 'Não informado'"></td></tr>
              </tbody>
            </table>

            <div class="flex justify-between items-stretch gap-5 mb-7">
              <div class="receipt-box p-4 flex-1">
                <p class="receipt-label">Responsável pelo lançamento</p>
                <p class="receipt-value text-lg" x-text="settings.manager"></p>
                <p class="text-xs text-slate-500 mt-1">Síndico, tesoureiro ou representante autorizado</p>
              </div>
              <div class="receipt-total min-w-[72mm]">
                <p class="text-xs uppercase tracking-[.18em] receipt-total-label">Valor total recebido</p>
                <p class="receipt-total-amount mt-1" x-text="money(currentReceipt?.total)"></p>
              </div>
            </div>

            <div class="grid grid-cols-2 gap-12 text-center mt-14">
              <div><div class="border-t border-slate-900 pt-2 font-bold">Assinatura do prestador</div><p class="text-xs text-slate-500 mt-1" x-text="currentReceipt?.providerName"></p></div>
              <div><div class="border-t border-slate-900 pt-2 font-bold">Conferência do condomínio</div><p class="text-xs text-slate-500 mt-1" x-text="settings.manager"></p></div>
            </div>

            <div class="receipt-footer-credit pt-4 border-t border-slate-200 text-[11px] text-slate-500 leading-relaxed">
              <p><b>Observação:</b> recibo emitido para controle interno do condomínio e composição do livro de prestação de contas. Guarde junto aos comprovantes de pagamento, notas fiscais, contratos ou autorizações correspondentes, quando houver.</p>
              <p class="mt-2 text-center">Documento gerado localmente pelo ReciboCondo PWA • <span x-text="settings.condoName"></span> • <span x-text="fmt(currentReceipt?.date)"></span></p>
              <p class="receipt-dev-credit">Aplicativo desenvolvido por Carlos Cunha</p>
            </div>
          </div>
        </div>
        <div id="reportPrint" class="receipt report-premium hidden">

<div class="report-watermark">ReciboCondo</div>
          <div class="relative z-10">
            <div class="flex justify-between gap-6 items-start mb-5">
              <div class="flex gap-4 items-center">
                <template x-if="settings.logo"><img :src="settings.logo" class="h-16 w-16 object-contain rounded-xl border border-slate-

===== settings @ 4327688 =====
" class="receipt-app-logo" alt="Logotipo ReciboCondo"></template>
                <div>
                  <h1 class="text-2xl font-bold leading-tight" x-text="settings.condoName"></h1>
                  <p class="text-sm text-slate-600" x-text="settings.cnpj"></p>
                  <p class="text-sm text-slate-600 max-w-[118mm]" x-text="settings.address"></p>
                </div>
              </div>
              <div class="text-right shrink-0">
                <span class="receipt-stamp">Prestação de contas</span>
                <p class="receipt-label mt-3">Recibo Nº</p>
                <p class="text-2xl font-black receipt-number" x-text="currentReceipt?.number"></p>
                <p class="receipt-label mt-2">Emissão</p>
                <p class="receipt-value" x-text="fmt(currentReceipt?.date)"></p>
              </div>
            </div>

            <div class="mb-5">
              <p class="receipt-label">Documento financeiro</p>
              <h2 class="text-3xl font-black tracking-tight text-slate-900">RECIBO DE PRESTAÇÃO DE SERVIÇO</h2>
              <p class="text-sm text-slate-600 mt-1">Comprovante para arquivo interno, administradora e livro de prestação de contas.</p>
            </div>

            <div class="grid grid-cols-2 gap-4 mb-5">
              <div class="receipt-box p-4">
                <p class="receipt-label">Pagador / Condomínio</p>
                <p class="text-lg font-bold" x-text="settings.condoName"></p>
                <p class="text-sm text-slate-600" x-text="settings.cnpj"></p>
              </div>
              <div class="receipt-box p-4">
                <p class="receipt-label">Favorecido / Prestador</p>
                <p class="text-lg font-bold" x-text="currentReceipt?.providerName"></p>
                <p class="text-sm text-slate-600">CPF/CNPJ: <span x-text="currentReceipt?.document || 'não informado'"></span></p>
              </div>
            </div>

            <div class="receipt-box p-5 mb-5">
              <p class="leading-8 text-[17px] text-slate-800">
                Declaro, para os devidos fins, que recebi de <b x-text="settings.condoName"></b> a importância de
                <b x-text="money(currentReceipt?.total)"></b>
                (<span class="font-bold" x-text="valorPorExtenso(currentReceipt?.total)"></span>), referente aos serviços discriminados neste recibo.
              </p>
            </div>

            <table class="receipt-table mb-5">
              <thead><tr><th style="width:28%">Campo</th><th>Informação</th></tr></thead>
              <tbody>
                <tr><td class="font-bold">Categoria</td><td x-text="currentReceipt?.category"></td></tr>
                <tr><td class="font-bold">Descrição detalhada</td><td x-text="currentReceipt?.description"></td></tr>
                <tr><td class="font-bold">Referência</td><td x-text="month(currentReceipt?.reference)"></td></tr>
                <tr><td class="font-bold">Local / área atendida</td><td x-text="currentReceipt?.location || '-'"></td></tr>
                <tr><td class="font-bold">Data do pagamento</td><td x-text="fmt(currentReceipt?.paymentDate || currentReceipt?.date)"></td></tr>
                <tr><td class="font-bold">Forma de pagamento</td><td x-text="currentReceipt?.paymentMethod || 'Não informado'"></td></tr>
              </tbody>
            </table>

            <div class="flex justify-between items-stretch gap-5 mb-7">
              <div class="receipt-box p-4 flex-1">
                <p class="receipt-label">Responsável pelo lançamento</p>
                <p class="receipt-value text-lg" x-text="settings.manager"></p>
                <p class="text-xs text-slate-500 mt-1">Síndico, tesoureiro ou representante autorizado</p>
              </div>
              <div class="receipt-total min-w-[72mm]">
                <p class="text-xs uppercase tracking-[.18em] receipt-total-label">Valor total recebido</p>
                <p class="receipt-total-amount mt-1" x-text="money(currentReceipt?.total)"></p>
              </div>
            </div>

            <div class="grid grid-cols-2 gap-12 text-center mt-14">
              <div><div class="border-t border-slate-900 pt-2 font-bold">Assinatura do prestador</div><p class="text-xs text-slate-500 mt-1" x-text="currentReceipt?.providerName"></p></div>
              <div><div class="border-t border-slate-900 pt-2 font-bold">Conferência do condomínio</div><p class="text-xs text-slate-500 mt-1" x-text="settings.manager"></p></div>
            </div>

            <div class="receipt-footer-credit pt-4 border-t border-slate-200 text-[11px] text-slate-500 leading-relaxed">
              <p><b>Observação:</b> recibo emitido para controle interno do condomínio e composição do livro de prestação de contas. Guarde junto aos comprovantes de pagamento, notas fiscais, contratos ou autorizações correspondentes, quando houver.</p>
              <p class="mt-2 text-center">Documento gerado localmente pelo ReciboCondo PWA • <span x-text="settings.condoName"></span> • <span x-text="fmt(currentReceipt?.date)"></span></p>
              <p class="receipt-dev-credit">Aplicativo desenvolvido por Carlos Cunha</p>
            </div>
          </div>
        </div>
        <div id="reportPrint" class="receipt report-premium hidden">

<div class="report-watermark">ReciboCondo</div>
          <div class="relative z-10">
            <div class="flex justify-between gap-6 items-start mb-5">
              <div class="flex gap-4 items-center">
                <template x-if="settings.logo"><img :src="settings.logo" class="h-16 w-16 object-contain rounded-xl border border-slate-200 p-1"></template>
                <template x-if="!settings.logo"><img src="data

===== settings @ 4328283 =====
18mm]" x-text="settings.address"></p>
                </div>
              </div>
              <div class="text-right shrink-0">
                <span class="receipt-stamp">Prestação de contas</span>
                <p class="receipt-label mt-3">Recibo Nº</p>
                <p class="text-2xl font-black receipt-number" x-text="currentReceipt?.number"></p>
                <p class="receipt-label mt-2">Emissão</p>
                <p class="receipt-value" x-text="fmt(currentReceipt?.date)"></p>
              </div>
            </div>

            <div class="mb-5">
              <p class="receipt-label">Documento financeiro</p>
              <h2 class="text-3xl font-black tracking-tight text-slate-900">RECIBO DE PRESTAÇÃO DE SERVIÇO</h2>
              <p class="text-sm text-slate-600 mt-1">Comprovante para arquivo interno, administradora e livro de prestação de contas.</p>
            </div>

            <div class="grid grid-cols-2 gap-4 mb-5">
              <div class="receipt-box p-4">
                <p class="receipt-label">Pagador / Condomínio</p>
                <p class="text-lg font-bold" x-text="settings.condoName"></p>
                <p class="text-sm text-slate-600" x-text="settings.cnpj"></p>
              </div>
              <div class="receipt-box p-4">
                <p class="receipt-label">Favorecido / Prestador</p>
                <p class="text-lg font-bold" x-text="currentReceipt?.providerName"></p>
                <p class="text-sm text-slate-600">CPF/CNPJ: <span x-text="currentReceipt?.document || 'não informado'"></span></p>
              </div>
            </div>

            <div class="receipt-box p-5 mb-5">
              <p class="leading-8 text-[17px] text-slate-800">
                Declaro, para os devidos fins, que recebi de <b x-text="settings.condoName"></b> a importância de
                <b x-text="money(currentReceipt?.total)"></b>
                (<span class="font-bold" x-text="valorPorExtenso(currentReceipt?.total)"></span>), referente aos serviços discriminados neste recibo.
              </p>
            </div>

            <table class="receipt-table mb-5">
              <thead><tr><th style="width:28%">Campo</th><th>Informação</th></tr></thead>
              <tbody>
                <tr><td class="font-bold">Categoria</td><td x-text="currentReceipt?.category"></td></tr>
                <tr><td class="font-bold">Descrição detalhada</td><td x-text="currentReceipt?.description"></td></tr>
                <tr><td class="font-bold">Referência</td><td x-text="month(currentReceipt?.reference)"></td></tr>
                <tr><td class="font-bold">Local / área atendida</td><td x-text="currentReceipt?.location || '-'"></td></tr>
                <tr><td class="font-bold">Data do pagamento</td><td x-text="fmt(currentReceipt?.paymentDate || currentReceipt?.date)"></td></tr>
                <tr><td class="font-bold">Forma de pagamento</td><td x-text="currentReceipt?.paymentMethod || 'Não informado'"></td></tr>
              </tbody>
            </table>

            <div class="flex justify-between items-stretch gap-5 mb-7">
              <div class="receipt-box p-4 flex-1">
                <p class="receipt-label">Responsável pelo lançamento</p>
                <p class="receipt-value text-lg" x-text="settings.manager"></p>
                <p class="text-xs text-slate-500 mt-1">Síndico, tesoureiro ou representante autorizado</p>
              </div>
              <div class="receipt-total min-w-[72mm]">
                <p class="text-xs uppercase tracking-[.18em] receipt-total-label">Valor total recebido</p>
                <p class="receipt-total-amount mt-1" x-text="money(currentReceipt?.total)"></p>
              </div>
            </div>

            <div class="grid grid-cols-2 gap-12 text-center mt-14">
              <div><div class="border-t border-slate-900 pt-2 font-bold">Assinatura do prestador</div><p class="text-xs text-slate-500 mt-1" x-text="currentReceipt?.providerName"></p></div>
              <div><div class="border-t border-slate-900 pt-2 font-bold">Conferência do condomínio</div><p class="text-xs text-slate-500 mt-1" x-text="settings.manager"></p></div>
            </div>

            <div class="receipt-footer-credit pt-4 border-t border-slate-200 text-[11px] text-slate-500 leading-relaxed">
              <p><b>Observação:</b> recibo emitido para controle interno do condomínio e composição do livro de prestação de contas. Guarde junto aos comprovantes de pagamento, notas fiscais, contratos ou autorizações correspondentes, quando houver.</p>
              <p class="mt-2 text-center">Documento gerado localmente pelo ReciboCondo PWA • <span x-text="settings.condoName"></span> • <span x-text="fmt(currentReceipt?.date)"></span></p>
              <p class="receipt-dev-credit">Aplicativo desenvolvido por Carlos Cunha</p>
            </div>
          </div>
        </div>
        <div id="reportPrint" class="receipt report-premium hidden">

<div class="report-watermark">ReciboCondo</div>
          <div class="relative z-10">
            <div class="flex justify-between gap-6 items-start mb-5">
              <div class="flex gap-4 items-center">
                <template x-if="settings.logo"><img :src="settings.logo" class="h-16 w-16 object-contain rounded-xl border border-slate-200 p-1"></template>
                <template x-if="!settings.logo"><img src="data:image/png;base64,

===== settings @ 4329777 =====
">CPF/CNPJ: <span x-text="currentReceipt?.document || 'não informado'"></span></p>
              </div>
            </div>

            <div class="receipt-box p-5 mb-5">
              <p class="leading-8 text-[17px] text-slate-800">
                Declaro, para os devidos fins, que recebi de <b x-text="settings.condoName"></b> a importância de
                <b x-text="money(currentReceipt?.total)"></b>
                (<span class="font-bold" x-text="valorPorExtenso(currentReceipt?.total)"></span>), referente aos serviços discriminados neste recibo.
              </p>
            </div>

            <table class="receipt-table mb-5">
              <thead><tr><th style="width:28%">Campo</th><th>Informação</th></tr></thead>
              <tbody>
                <tr><td class="font-bold">Categoria</td><td x-text="currentReceipt?.category"></td></tr>
                <tr><td class="font-bold">Descrição detalhada</td><td x-text="currentReceipt?.description"></td></tr>
                <tr><td class="font-bold">Referência</td><td x-text="month(currentReceipt?.reference)"></td></tr>
                <tr><td class="font-bold">Local / área atendida</td><td x-text="currentReceipt?.location || '-'"></td></tr>
                <tr><td class="font-bold">Data do pagamento</td><td x-text="fmt(currentReceipt?.paymentDate || currentReceipt?.date)"></td></tr>
                <tr><td class="font-bold">Forma de pagamento</td><td x-text="currentReceipt?.paymentMethod || 'Não informado'"></td></tr>
              </tbody>
            </table>

            <div class="flex justify-between items-stretch gap-5 mb-7">
              <div class="receipt-box p-4 flex-1">
                <p class="receipt-label">Responsável pelo lançamento</p>
                <p class="receipt-value text-lg" x-text="settings.manager"></p>
                <p class="text-xs text-slate-500 mt-1">Síndico, tesoureiro ou representante autorizado</p>
              </div>
              <div class="receipt-total min-w-[72mm]">
                <p class="text-xs uppercase tracking-[.18em] receipt-total-label">Valor total recebido</p>
                <p class="receipt-total-amount mt-1" x-text="money(currentReceipt?.total)"></p>
              </div>
            </div>

            <div class="grid grid-cols-2 gap-12 text-center mt-14">
              <div><div class="border-t border-slate-900 pt-2 font-bold">Assinatura do prestador</div><p class="text-xs text-slate-500 mt-1" x-text="currentReceipt?.providerName"></p></div>
              <div><div class="border-t border-slate-900 pt-2 font-bold">Conferência do condomínio</div><p class="text-xs text-slate-500 mt-1" x-text="settings.manager"></p></div>
            </div>

            <div class="receipt-footer-credit pt-4 border-t border-slate-200 text-[11px] text-slate-500 leading-relaxed">
              <p><b>Observação:</b> recibo emitido para controle interno do condomínio e composição do livro de prestação de contas. Guarde junto aos comprovantes de pagamento, notas fiscais, contratos ou autorizações correspondentes, quando houver.</p>
              <p class="mt-2 text-center">Documento gerado localmente pelo ReciboCondo PWA • <span x-text="settings.condoName"></span> • <span x-text="fmt(currentReceipt?.date)"></span></p>
              <p class="receipt-dev-credit">Aplicativo desenvolvido por Carlos Cunha</p>
            </div>
          </div>
        </div>
        <div id="reportPrint" class="receipt report-premium hidden">

<div class="report-watermark">ReciboCondo</div>
          <div class="relative z-10">
            <div class="flex justify-between gap-6 items-start mb-5">
              <div class="flex gap-4 items-center">
                <template x-if="settings.logo"><img :src="settings.logo" class="h-16 w-16 object-contain rounded-xl border border-slate-200 p-1"></template>
                <template x-if="!settings.logo"><img src="data:image/png;base64,

===== settings @ 4330636 =====
tr>
                <tr><td class="font-bold">Descrição detalhada</td><td x-text="currentReceipt?.description"></td></tr>
                <tr><td class="font-bold">Referência</td><td x-text="month(currentReceipt?.reference)"></td></tr>
                <tr><td class="font-bold">Local / área atendida</td><td x-text="currentReceipt?.location || '-'"></td></tr>
                <tr><td class="font-bold">Data do pagamento</td><td x-text="fmt(currentReceipt?.paymentDate || currentReceipt?.date)"></td></tr>
                <tr><td class="font-bold">Forma de pagamento</td><td x-text="currentReceipt?.paymentMethod || 'Não informado'"></td></tr>
              </tbody>
            </table>

            <div class="flex justify-between items-stretch gap-5 mb-7">
              <div class="receipt-box p-4 flex-1">
                <p class="receipt-label">Responsável pelo lançamento</p>
                <p class="receipt-value text-lg" x-text="settings.manager"></p>
                <p class="text-xs text-slate-500 mt-1">Síndico, tesoureiro ou representante autorizado</p>
              </div>
              <div class="receipt-total min-w-[72mm]">
                <p class="text-xs uppercase tracking-[.18em] receipt-total-label">Valor total recebido</p>
                <p class="receipt-total-amount mt-1" x-text="money(currentReceipt?.total)"></p>
              </div>
            </div>

            <div class="grid grid-cols-2 gap-12 text-center mt-14">
              <div><div class="border-t border-slate-900 pt-2 font-bold">Assinatura do prestador</div><p class="text-xs text-slate-500 mt-1" x-text="currentReceipt?.providerName"></p></div>
              <div><div class="border-t border-slate-900 pt-2 font-bold">Conferência do condomínio</div><p class="text-xs text-slate-500 mt-1" x-text="settings.manager"></p></div>
            </div>

            <div class="receipt-footer-credit pt-4 border-t border-slate-200 text-[11px] text-slate-500 leading-relaxed">
              <p><b>Observação:</b> recibo emitido para controle interno do condomínio e composição do livro de prestação de contas. Guarde junto aos comprovantes de pagamento, notas fiscais, contratos ou autorizações correspondentes, quando houver.</p>
              <p class="mt-2 text-center">Documento gerado localmente pelo ReciboCondo PWA • <span x-text="settings.condoName"></span> • <span x-text="fmt(currentReceipt?.date)"></span></p>
              <p class="receipt-dev-credit">Aplicativo desenvolvido por Carlos Cunha</p>
            </div>
          </div>
        </div>
        <div id="reportPrint" class="receipt report-premium hidden">

<div class="report-watermark">ReciboCondo</div>
          <div class="relative z-10">
            <div class="flex justify-between gap-6 items-start mb-5">
              <div class="flex gap-4 items-center">
                <template x-if="settings.logo"><img :src="settings.logo" class="h-16 w-16 object-contain rounded-xl border border-slate-200 p-1"></template>
                <template x-if="!settings.logo"><img src="data:image/png;base64,

===== settings @ 4331168 =====
ss="font-bold">Forma de pagamento</td><td x-text="currentReceipt?.paymentMethod || 'Não informado'"></td></tr>
              </tbody>
            </table>

            <div class="flex justify-between items-stretch gap-5 mb-7">
              <div class="receipt-box p-4 flex-1">
                <p class="receipt-label">Responsável pelo lançamento</p>
                <p class="receipt-value text-lg" x-text="settings.manager"></p>
                <p class="text-xs text-slate-500 mt-1">Síndico, tesoureiro ou representante autorizado</p>
              </div>
              <div class="receipt-total min-w-[72mm]">
                <p class="text-xs uppercase tracking-[.18em] receipt-total-label">Valor total recebido</p>
                <p class="receipt-total-amount mt-1" x-text="money(currentReceipt?.total)"></p>
              </div>
            </div>

            <div class="grid grid-cols-2 gap-12 text-center mt-14">
              <div><div class="border-t border-slate-900 pt-2 font-bold">Assinatura do prestador</div><p class="text-xs text-slate-500 mt-1" x-text="currentReceipt?.providerName"></p></div>
              <div><div class="border-t border-slate-900 pt-2 font-bold">Conferência do condomínio</div><p class="text-xs text-slate-500 mt-1" x-text="settings.manager"></p></div>
            </div>

            <div class="receipt-footer-credit pt-4 border-t border-slate-200 text-[11px] text-slate-500 leading-relaxed">
              <p><b>Observação:</b> recibo emitido para controle interno do condomínio e composição do livro de prestação de contas. Guarde junto aos comprovantes de pagamento, notas fiscais, contratos ou autorizações correspondentes, quando houver.</p>
              <p class="mt-2 text-center">Documento gerado localmente pelo ReciboCondo PWA • <span x-text="settings.condoName"></span> • <span x-text="fmt(currentReceipt?.date)"></span></p>
              <p class="receipt-dev-credit">Aplicativo desenvolvido por Carlos Cunha</p>
            </div>
          </div>
        </div>
        <div id="reportPrint" class="receipt report-premium hidden">

<div class="report-watermark">ReciboCondo</div>
          <div class="relative z-10">
            <div class="flex justify-between gap-6 items-start mb-5">
              <div class="flex gap-4 items-center">
                <template x-if="settings.logo"><img :src="settings.logo" class="h-16 w-16 object-contain rounded-xl border border-slate-200 p-1"></template>
                <template x-if="!settings.logo"><img src="data:image/png;base64,

===== settings @ 4331701 =====
o</p>
              </div>
              <div class="receipt-total min-w-[72mm]">
                <p class="text-xs uppercase tracking-[.18em] receipt-total-label">Valor total recebido</p>
                <p class="receipt-total-amount mt-1" x-text="money(currentReceipt?.total)"></p>
              </div>
            </div>

            <div class="grid grid-cols-2 gap-12 text-center mt-14">
              <div><div class="border-t border-slate-900 pt-2 font-bold">Assinatura do prestador</div><p class="text-xs text-slate-500 mt-1" x-text="currentReceipt?.providerName"></p></div>
              <div><div class="border-t border-slate-900 pt-2 font-bold">Conferência do condomínio</div><p class="text-xs text-slate-500 mt-1" x-text="settings.manager"></p></div>
            </div>

            <div class="receipt-footer-credit pt-4 border-t border-slate-200 text-[11px] text-slate-500 leading-relaxed">
              <p><b>Observação:</b> recibo emitido para controle interno do condomínio e composição do livro de prestação de contas. Guarde junto aos comprovantes de pagamento, notas fiscais, contratos ou autorizações correspondentes, quando houver.</p>
              <p class="mt-2 text-center">Documento gerado localmente pelo ReciboCondo PWA • <span x-text="settings.condoName"></span> • <span x-text="fmt(currentReceipt?.date)"></span></p>
              <p class="receipt-dev-credit">Aplicativo desenvolvido por Carlos Cunha</p>
            </div>
          </div>
        </div>
        <div id="reportPrint" class="receipt report-premium hidden">

<div class="report-watermark">ReciboCondo</div>
          <div class="relative z-10">
            <div class="flex justify-between gap-6 items-start mb-5">
              <div class="flex gap-4 items-center">
                <template x-if="settings.logo"><img :src="settings.logo" class="h-16 w-16 object-contain rounded-xl border border-slate-200 p-1"></template>
                <template x-if="!settings.logo"><img src="data:image/png;base64,

===== settings @ 4331727 =====

              <div class="receipt-total min-w-[72mm]">
                <p class="text-xs uppercase tracking-[.18em] receipt-total-label">Valor total recebido</p>
                <p class="receipt-total-amount mt-1" x-text="money(currentReceipt?.total)"></p>
              </div>
            </div>

            <div class="grid grid-cols-2 gap-12 text-center mt-14">
              <div><div class="border-t border-slate-900 pt-2 font-bold">Assinatura do prestador</div><p class="text-xs text-slate-500 mt-1" x-text="currentReceipt?.providerName"></p></div>
              <div><div class="border-t border-slate-900 pt-2 font-bold">Conferência do condomínio</div><p class="text-xs text-slate-500 mt-1" x-text="settings.manager"></p></div>
            </div>

            <div class="receipt-footer-credit pt-4 border-t border-slate-200 text-[11px] text-slate-500 leading-relaxed">
              <p><b>Observação:</b> recibo emitido para controle interno do condomínio e composição do livro de prestação de contas. Guarde junto aos comprovantes de pagamento, notas fiscais, contratos ou autorizações correspondentes, quando houver.</p>
              <p class="mt-2 text-center">Documento gerado localmente pelo ReciboCondo PWA • <span x-text="settings.condoName"></span> • <span x-text="fmt(currentReceipt?.date)"></span></p>
              <p class="receipt-dev-credit">Aplicativo desenvolvido por Carlos Cunha</p>
            </div>
          </div>
        </div>
        <div id="reportPrint" class="receipt report-premium hidden">

<div class="report-watermark">ReciboCondo</div>
          <div class="relative z-10">
            <div class="flex justify-between gap-6 items-start mb-5">
              <div class="flex gap-4 items-center">
                <template x-if="settings.logo"><img :src="settings.logo" class="h-16 w-16 object-contain rounded-xl border border-slate-200 p-1"></template>
                <template x-if="!settings.logo"><img src="data:image/png;base64,

===== settings @ 4331859 =====
abel">Valor total recebido</p>
                <p class="receipt-total-amount mt-1" x-text="money(currentReceipt?.total)"></p>
              </div>
            </div>

            <div class="grid grid-cols-2 gap-12 text-center mt-14">
              <div><div class="border-t border-slate-900 pt-2 font-bold">Assinatura do prestador</div><p class="text-xs text-slate-500 mt-1" x-text="currentReceipt?.providerName"></p></div>
              <div><div class="border-t border-slate-900 pt-2 font-bold">Conferência do condomínio</div><p class="text-xs text-slate-500 mt-1" x-text="settings.manager"></p></div>
            </div>

            <div class="receipt-footer-credit pt-4 border-t border-slate-200 text-[11px] text-slate-500 leading-relaxed">
              <p><b>Observação:</b> recibo emitido para controle interno do condomínio e composição do livro de prestação de contas. Guarde junto aos comprovantes de pagamento, notas fiscais, contratos ou autorizações correspondentes, quando houver.</p>
              <p class="mt-2 text-center">Documento gerado localmente pelo ReciboCondo PWA • <span x-text="settings.condoName"></span> • <span x-text="fmt(currentReceipt?.date)"></span></p>
              <p class="receipt-dev-credit">Aplicativo desenvolvido por Carlos Cunha</p>
            </div>
          </div>
        </div>
        <div id="reportPrint" class="receipt report-premium hidden">

<div class="report-watermark">ReciboCondo</div>
          <div class="relative z-10">
            <div class="flex justify-between gap-6 items-start mb-5">
              <div class="flex gap-4 items-center">
                <template x-if="settings.logo"><img :src="settings.logo" class="h-16 w-16 object-contain rounded-xl border border-slate-200 p-1"></template>
                <template x-if="!settings.logo"><img src="data:image/png;base64,

===== settings @ 6434860 =====
" class="h-16 w-16 object-contain rounded-xl border border-slate-200 bg-white p-1 shadow-sm"></template>
                <div>
                  <h1 class="text-2xl font-bold leading-tight" x-text="settings.condoName"></h1>
                  <p class="text-sm text-slate-600" x-text="settings.cnpj"></p>
                  <p class="text-sm text-slate-600 max-w-[118mm]" x-text="settings.address"></p>
                </div>
              </div>
              <div class="text-right shrink-0">
                <span class="receipt-stamp">Prestação de contas</span>
                <p class="receipt-label mt-3">Emitido em</p>
                <p class="receipt-value" x-text="fmt(today())"></p>
                <p class="receipt-label mt-2">Registros</p>
                <p class="text-2xl font-black receipt-number" x-text="reportDetailRows.length"></p>
              </div>
            </div>

            <div class="mb-5">
              <p class="receipt-label">Documento financeiro</p>
              <h2 class="text-3xl font-black tracking-tight text-slate-900">RELATÓRIO DE PRESTADORES E PAGAMENTOS</h2>
              <p class="text-sm text-slate-600 mt-1">Resumo para arquivo interno, administradora e livro de prestação de contas.</p>
            </div>

            <div class="grid grid-cols-4 gap-3 mb-5">
              <div class="report-kpi"><p class="label">Total lançado</p><p class="value" x-text="money(reportTotals.expected)"></p></div>
              <div class="report-kpi"><p class="label">Total pago</p><p class="value green" x-text="money(reportTotals.paid)"></p></div>
              <div class="report-kpi"><p class="label">Em aberto</p><p class="value red" x-text="money(reportTotals.open)"></p></div>
              <div class="report-kpi"><p class="label">Mês de referência</p><p class="value text-[13px] leading-tight" x-text="reportMonthLabel()"></p></div>
            </div>

            <div class="grid grid-cols-2 gap-3 mb-5">
              <div class="receipt-box p-4">
                <p class="receipt-label">Filtro de categoria</p>
                <p class="receipt-value" x-text="report.category==='todas'?'Todas as categorias':report.category"></p>
              </div>
              <div class="receipt-box p-4">
                <p class="receipt-label">Filtro de status</p>
                <p class="receipt-value" x-text="report.status==='todos'?'Todos os status':report.status"></p>
              </div>
            </div>

            <table class="report-table mb-5">
              <thead><tr><th style="width:11%">Recibo nº</th><th style="width:11%">Pagamento</th><th style="width:22%">Prestador</th><th style="width:16%">Categoria</th><th style="width:28%">Descrição</th><th style="width:12%">Valor</th></tr></thead>
              <tbody>
                <template x-for="s in reportDetailRows" :key="s.reportKey">
                  <tr>
                    <td class="font-bold text-slate-700" x-text="s.receiptNumber"></td>
                    <td x-text="fmt(s.paymentDate)"></td>
                    <td x-text="providerName(s.providerId)"></td>
                    <td x-text="s.category"></td>
                    <td x-text="s.description || '-'"></td>
                    <td class="font-bold" x-text="money(s.reportValue || 0)"></td>
                  </tr>
                </template>
              </tbody>
            </table>

            <div class="report-footer-credit pt-4 border-t border-slate-200 text-[11px] text-slate-500 leading-relaxed">
              <p><b>Observação:</b> relatório emitido para controle interno do condomínio e composição do livro de prestação de contas. Recomenda-se arquivar junto aos recibos, comprovantes de pagamento, notas fiscais, contratos ou autorizações correspondentes, quando houver.</p>
              <p class="mt-2 text-center">Documento gerado localmente pelo ReciboCondo PWA • <span x-text="settings.condoName"></span> • <span x-text="fmt(today())"></span></p>
              <p class="report-dev-credit">Aplicativo desenvolvido por Carlos Cunha</p>
            </div>
          </div>
        </div>
        <div id="annualReportPrint" class="receipt report-premium hidden">
          <div class="report-watermark">ReciboCondo</div>
          <div class="relative z-10">
            <div class="flex justify-between gap-6 items-start mb-5">
              <div class="flex gap-4 items-center">
    

===== settings @ 6434946 =====
" class="h-16 w-16 object-contain rounded-xl border border-slate-200 bg-white p-1 shadow-sm"></template>
                <div>
                  <h1 class="text-2xl font-bold leading-tight" x-text="settings.condoName"></h1>
                  <p class="text-sm text-slate-600" x-text="settings.cnpj"></p>
                  <p class="text-sm text-slate-600 max-w-[118mm]" x-text="settings.address"></p>
                </div>
              </div>
              <div class="text-right shrink-0">
                <span class="receipt-stamp">Prestação de contas</span>
                <p class="receipt-label mt-3">Emitido em</p>
                <p class="receipt-value" x-text="fmt(today())"></p>
                <p class="receipt-label mt-2">Registros</p>
                <p class="text-2xl font-black receipt-number" x-text="reportDetailRows.length"></p>
              </div>
            </div>

            <div class="mb-5">
              <p class="receipt-label">Documento financeiro</p>
              <h2 class="text-3xl font-black tracking-tight text-slate-900">RELATÓRIO DE PRESTADORES E PAGAMENTOS</h2>
              <p class="text-sm text-slate-600 mt-1">Resumo para arquivo interno, administradora e livro de prestação de contas.</p>
            </div>

            <div class="grid grid-cols-4 gap-3 mb-5">
              <div class="report-kpi"><p class="label">Total lançado</p><p class="value" x-text="money(reportTotals.expected)"></p></div>
              <div class="report-kpi"><p class="label">Total pago</p><p class="value green" x-text="money(reportTotals.paid)"></p></div>
              <div class="report-kpi"><p class="label">Em aberto</p><p class="value red" x-text="money(reportTotals.open)"></p></div>
              <div class="report-kpi"><p class="label">Mês de referência</p><p class="value text-[13px] leading-tight" x-text="reportMonthLabel()"></p></div>
            </div>

            <div class="grid grid-cols-2 gap-3 mb-5">
              <div class="receipt-box p-4">
                <p class="receipt-label">Filtro de categoria</p>
                <p class="receipt-value" x-text="report.category==='todas'?'Todas as categorias':report.category"></p>
              </div>
              <div class="receipt-box p-4">
                <p class="receipt-label">Filtro de status</p>
                <p class="receipt-value" x-text="report.status==='todos'?'Todos os status':report.status"></p>
              </div>
            </div>

            <table class="report-table mb-5">
              <thead><tr><th style="width:11%">Recibo nº</th><th style="width:11%">Pagamento</th><th style="width:22%">Prestador</th><th style="width:16%">Categoria</th><th style="width:28%">Descrição</th><th style="width:12%">Valor</th></tr></thead>
              <tbody>
                <template x-for="s in reportDetailRows" :key="s.reportKey">
                  <tr>
                    <td class="font-bold text-slate-700" x-text="s.receiptNumber"></td>
                    <td x-text="fmt(s.paymentDate)"></td>
                    <td x-text="providerName(s.providerId)"></td>
                    <td x-text="s.category"></td>
                    <td x-text="s.description || '-'"></td>
                    <td class="font-bold" x-text="money(s.reportValue || 0)"></td>
                  </tr>
                </template>
              </tbody>
            </table>

            <div class="report-footer-credit pt-4 border-t border-slate-200 text-[11px] text-slate-500 leading-relaxed">
              <p><b>Observação:</b> relatório emitido para controle interno do condomínio e composição do livro de prestação de contas. Recomenda-se arquivar junto aos recibos, comprovantes de pagamento, notas fiscais, contratos ou autorizações correspondentes, quando houver.</p>
              <p class="mt-2 text-center">Documento gerado localmente pelo ReciboCondo PWA • <span x-text="settings.condoName"></span> • <span x-text="fmt(today())"></span></p>
              <p class="report-dev-credit">Aplicativo desenvolvido por Carlos Cunha</p>
            </div>
          </div>
        </div>
        <div id="annualReportPrint" class="receipt report-premium hidden">
          <div class="report-watermark">ReciboCondo</div>
          <div class="relative z-10">
            <div class="flex justify-between gap-6 items-start mb-5">
              <div class="flex gap-4 items-center">
                <template x-if="settings.logo"><img :src="settings.logo" class="h-16 w-16 

===== settings @ 6435040 =====
" class="h-16 w-16 object-contain rounded-xl border border-slate-200 bg-white p-1 shadow-sm"></template>
                <div>
                  <h1 class="text-2xl font-bold leading-tight" x-text="settings.condoName"></h1>
                  <p class="text-sm text-slate-600" x-text="settings.cnpj"></p>
                  <p class="text-sm text-slate-600 max-w-[118mm]" x-text="settings.address"></p>
                </div>
              </div>
              <div class="text-right shrink-0">
                <span class="receipt-stamp">Prestação de contas</span>
                <p class="receipt-label mt-3">Emitido em</p>
                <p class="receipt-value" x-text="fmt(today())"></p>
                <p class="receipt-label mt-2">Registros</p>
                <p class="text-2xl font-black receipt-number" x-text="reportDetailRows.length"></p>
              </div>
            </div>

            <div class="mb-5">
              <p class="receipt-label">Documento financeiro</p>
              <h2 class="text-3xl font-black tracking-tight text-slate-900">RELATÓRIO DE PRESTADORES E PAGAMENTOS</h2>
              <p class="text-sm text-slate-600 mt-1">Resumo para arquivo interno, administradora e livro de prestação de contas.</p>
            </div>

            <div class="grid grid-cols-4 gap-3 mb-5">
              <div class="report-kpi"><p class="label">Total lançado</p><p class="value" x-text="money(reportTotals.expected)"></p></div>
              <div class="report-kpi"><p class="label">Total pago</p><p class="value green" x-text="money(reportTotals.paid)"></p></div>
              <div class="report-kpi"><p class="label">Em aberto</p><p class="value red" x-text="money(reportTotals.open)"></p></div>
              <div class="report-kpi"><p class="label">Mês de referência</p><p class="value text-[13px] leading-tight" x-text="reportMonthLabel()"></p></div>
            </div>

            <div class="grid grid-cols-2 gap-3 mb-5">
              <div class="receipt-box p-4">
                <p class="receipt-label">Filtro de categoria</p>
                <p class="receipt-value" x-text="report.category==='todas'?'Todas as categorias':report.category"></p>
              </div>
              <div class="receipt-box p-4">
                <p class="receipt-label">Filtro de status</p>
                <p class="receipt-value" x-text="report.status==='todos'?'Todos os status':report.status"></p>
              </div>
            </div>

            <table class="report-table mb-5">
              <thead><tr><th style="width:11%">Recibo nº</th><th style="width:11%">Pagamento</th><th style="width:22%">Prestador</th><th style="width:16%">Categoria</th><th style="width:28%">Descrição</th><th style="width:12%">Valor</th></tr></thead>
              <tbody>
                <template x-for="s in reportDetailRows" :key="s.reportKey">
                  <tr>
                    <td class="font-bold text-slate-700" x-text="s.receiptNumber"></td>
                    <td x-text="fmt(s.paymentDate)"></td>
                    <td x-text="providerName(s.providerId)"></td>
                    <td x-text="s.category"></td>
                    <td x-text="s.description || '-'"></td>
                    <td class="font-bold" x-text="money(s.reportValue || 0)"></td>
                  </tr>
                </template>
              </tbody>
            </table>

            <div class="report-footer-credit pt-4 border-t border-slate-200 text-[11px] text-slate-500 leading-relaxed">
              <p><b>Observação:</b> relatório emitido para controle interno do condomínio e composição do livro de prestação de contas. Recomenda-se arquivar junto aos recibos, comprovantes de pagamento, notas fiscais, contratos ou autorizações correspondentes, quando houver.</p>
              <p class="mt-2 text-center">Documento gerado localmente pelo ReciboCondo PWA • <span x-text="settings.condoName"></span> • <span x-text="fmt(today())"></span></p>
              <p class="report-dev-credit">Aplicativo desenvolvido por Carlos Cunha</p>
            </div>
          </div>
        </div>
        <div id="annualReportPrint" class="receipt report-premium hidden">
          <div class="report-watermark">ReciboCondo</div>
          <div class="relative z-10">
            <div class="flex justify-between gap-6 items-start mb-5">
              <div class="flex gap-4 items-center">
                <template x-if="settings.logo"><img :src="settings.logo" class="h-16 w-16 object-contain rounded-xl border border-slate-200 p-1"></template>
                <template x

===== settings @ 6438554 =====
eceipt-value" x-text="report.category==='todas'?'Todas as categorias':report.category"></p>
              </div>
              <div class="receipt-box p-4">
                <p class="receipt-label">Filtro de status</p>
                <p class="receipt-value" x-text="report.status==='todos'?'Todos os status':report.status"></p>
              </div>
            </div>

            <table class="report-table mb-5">
              <thead><tr><th style="width:11%">Recibo nº</th><th style="width:11%">Pagamento</th><th style="width:22%">Prestador</th><th style="width:16%">Categoria</th><th style="width:28%">Descrição</th><th style="width:12%">Valor</th></tr></thead>
              <tbody>
                <template x-for="s in reportDetailRows" :key="s.reportKey">
                  <tr>
                    <td class="font-bold text-slate-700" x-text="s.receiptNumber"></td>
                    <td x-text="fmt(s.paymentDate)"></td>
                    <td x-text="providerName(s.providerId)"></td>
                    <td x-text="s.category"></td>
                    <td x-text="s.description || '-'"></td>
                    <td class="font-bold" x-text="money(s.reportValue || 0)"></td>
                  </tr>
                </template>
              </tbody>
            </table>

            <div class="report-footer-credit pt-4 border-t border-slate-200 text-[11px] text-slate-500 leading-relaxed">
              <p><b>Observação:</b> relatório emitido para controle interno do condomínio e composição do livro de prestação de contas. Recomenda-se arquivar junto aos recibos, comprovantes de pagamento, notas fiscais, contratos ou autorizações correspondentes, quando houver.</p>
              <p class="mt-2 text-center">Documento gerado localmente pelo ReciboCondo PWA • <span x-text="settings.condoName"></span> • <span x-text="fmt(today())"></span></p>
              <p class="report-dev-credit">Aplicativo desenvolvido por Carlos Cunha</p>
            </div>
          </div>
        </div>
        <div id="annualReportPrint" class="receipt report-premium hidden">
          <div class="report-watermark">ReciboCondo</div>
          <div class="relative z-10">
            <div class="flex justify-between gap-6 items-start mb-5">
              <div class="flex gap-4 items-center">
                <template x-if="settings.logo"><img :src="settings.logo" class="h-16 w-16 object-contain rounded-xl border border-slate-200 p-1"></template>
                <template x-if="!settings.logo"><div class="h-16 w-16 rounded-xl grid place-items-center text-white font-black text-2xl" style="background:linear-gradient(135deg,#0F2A3A,#15803D)">RC</div></template>
                <div>
                  <h1 class="text-2xl font-bold leading-tight" x-text="settings.condoName"></h1>
                  <p class="text-sm text-slate-600" x-text="settings.cnpj"></p>
                  <p class="text-sm text-slate-600 max-w-[118mm]" x-text="settings.address"></p>
                </div>
              </div>
              <div class="text-right shrink-0">
                <span class="receipt-stamp">Prestação de contas</span>
                <p class="receipt-label mt-3">Ano</p>
                <p class="text-2xl font-black receipt-number" x-text="dashboardYear"></p>
                <p class="receipt-label mt-2">Emitido em</p>
                <p class="receipt-value" x-text="fmt(today())"></p>
              </div>
            </div>

            <div class="mb-4">
              <p class="receipt-label">Documento financeiro anual</p>
              <h2 class="text-3xl font-black tracking-tight text-slate-900">RELATÓRIO ANUAL DE PRESTADORES</h2>
              <p class="text-sm text-slate-600 mt-1">Resumo consolidado de pagamentos, categorias e prestadores para acompanhamento anual.</p>
            </div>

            <div class="grid grid-cols-4 gap-3 mb-4">
              <div class="report-kpi"><p class="label">Total pago</p><p class="value green" x-text="money(annualTotals.paid)"></p></div>
              <div class="report-kpi"><p class="label">Em aberto</p><p class="value red" x-text="money(annualTotals.open)"></p></div>
              <div class="report-kpi"><p class="label">Pagamentos</p><p class="value" x-text="annualTotals.count"></p></div>
              <div class="report-kpi"><p class="label">Maior categoria</p><p class="value text-[13px] leading-tight" x-text="annualCategorySummary[0]?.category || '-'"></p></div>
            </div>

            <div class="grid grid-cols-2 gap-4 mb-4">
              <div class="receipt-box p-4">
                <p class="receipt-label mb-2">Resumo mensal</p>
                <table class="annual-mini-table">
                  <thead><tr><th>Mês</th><th>Pag.</th><th>Total pago</th><th>Aberto</th></tr></thead>
                  <tbody>
                    <template x-for="m in monthlyHistory" :key="m.key">
                      <tr><td class="font-bold" x-text="m.label.slice(0,3)"></td><td x-text="m.count"></td><td class="font-bold" x-text="money(m.paid)"></td><td x-text="money(m.open)"></td></tr>
                    </template>
                  </tbody>
                </table>
              </div>
              <div class="receipt-box p-4">
                <p class="receipt-label mb-2">Top prestadores</p>
                <div class="annual-report-list">
                  <template x-for="(p,idx) in annualTopProviders.slice(0,7)" :key="p.id">
                    <div class="row"><span><b x-text="(idx+1)+'. '+p.name"></b><br><small x-text="p.category+' • '+p.count+' pgto(s)'"></small></span><b x-text="money(p.total)"></b></div>
                  </template>
                  <p x-show="!annualTopProviders.length" class="text-slate-500">Sem movimentação no ano.</p>
                </div>
              </div>
            </div>

            <div class="receipt-box p-4 mb-4">
              <p class="receipt-label mb-2">Categorias mais movimentadas</p>
              <div class="space-y-2">
             

===== settings @ 6439088 =====
">Prestador</th><th style="width:16%">Categoria</th><th style="width:28%">Descrição</th><th style="width:12%">Valor</th></tr></thead>
              <tbody>
                <template x-for="s in reportDetailRows" :key="s.reportKey">
                  <tr>
                    <td class="font-bold text-slate-700" x-text="s.receiptNumber"></td>
                    <td x-text="fmt(s.paymentDate)"></td>
                    <td x-text="providerName(s.providerId)"></td>
                    <td x-text="s.category"></td>
                    <td x-text="s.description || '-'"></td>
                    <td class="font-bold" x-text="money(s.reportValue || 0)"></td>
                  </tr>
                </template>
              </tbody>
            </table>

            <div class="report-footer-credit pt-4 border-t border-slate-200 text-[11px] text-slate-500 leading-relaxed">
              <p><b>Observação:</b> relatório emitido para controle interno do condomínio e composição do livro de prestação de contas. Recomenda-se arquivar junto aos recibos, comprovantes de pagamento, notas fiscais, contratos ou autorizações correspondentes, quando houver.</p>
              <p class="mt-2 text-center">Documento gerado localmente pelo ReciboCondo PWA • <span x-text="settings.condoName"></span> • <span x-text="fmt(today())"></span></p>
              <p class="report-dev-credit">Aplicativo desenvolvido por Carlos Cunha</p>
            </div>
          </div>
        </div>
        <div id="annualReportPrint" class="receipt report-premium hidden">
          <div class="report-watermark">ReciboCondo</div>
          <div class="relative z-10">
            <div class="flex justify-between gap-6 items-start mb-5">
              <div class="flex gap-4 items-center">
                <template x-if="settings.logo"><img :src="settings.logo" class="h-16 w-16 object-contain rounded-xl border border-slate-200 p-1"></template>
                <template x-if="!settings.logo"><div class="h-16 w-16 rounded-xl grid place-items-center text-white font-black text-2xl" style="background:linear-gradient(135deg,#0F2A3A,#15803D)">RC</div></template>
                <div>
                  <h1 class="text-2xl font-bold leading-tight" x-text="settings.condoName"></h1>
                  <p class="text-sm text-slate-600" x-text="settings.cnpj"></p>
                  <p class="text-sm text-slate-600 max-w-[118mm]" x-text="settings.address"></p>
                </div>
              </div>
              <div class="text-right shrink-0">
                <span class="receipt-stamp">Prestação de contas</span>
                <p class="receipt-label mt-3">Ano</p>
                <p class="text-2xl font-black receipt-number" x-text="dashboardYear"></p>
                <p class="receipt-label mt-2">Emitido em</p>
                <p class="receipt-value" x-text="fmt(today())"></p>
              </div>
            </div>

            <div class="mb-4">
              <p class="receipt-label">Documento financeiro anual</p>
              <h2 class="text-3xl font-black tracking-tight text-slate-900">RELATÓRIO ANUAL DE PRESTADORES</h2>
              <p class="text-sm text-slate-600 mt-1">Resumo consolidado de pagamentos, categorias e prestadores para acompanhamento anual.</p>
            </div>

            <div class="grid grid-cols-4 gap-3 mb-4">
              <div class="report-kpi"><p class="label">Total pago</p><p class="value green" x-text="money(annualTotals.paid)"></p></div>
              <div class="report-kpi"><p class="label">Em aberto</p><p class="value red" x-text="money(annualTotals.open)"></p></div>
              <div class="report-kpi"><p class="label">Pagamentos</p><p class="value" x-text="annualTotals.count"></p></div>
              <div class="report-kpi"><p class="label">Maior categoria</p><p class="value text-[13px] leading-tight" x-text="annualCategorySummary[0]?.category || '-'"></p></div>
            </div>

            <div class="grid grid-cols-2 gap-4 mb-4">
              <div class="receipt-box p-4">
                <p class="receipt-label mb-2">Resumo mensal</p>
                <table class="annual-mini-table">
                  <thead><tr><th>Mês</th><th>Pag.</th><th>Total pago</th><th>Aberto</th></tr></thead>
                  <tbody>
                    <template x-for="m in monthlyHistory" :key="m.key">
                      <tr><td class="font-bold" x-text="m.label.slice(0,3)"></td><td x-text="m.count"></td><td class="font-bold" x-text="money(m.paid)"></td><td x-text="money(m.open)"></td></tr>
                    </template>
                  </tbody>
                </table>
              </div>
              <div class="receipt-box p-4">
                <p class="receipt-label mb-2">Top prestadores</p>
                <div class="annual-report-list">
                  <template x-for="(p,idx) in annualTopProviders.slice(0,7)" :key="p.id">
                    <div class="row"><span><b x-text="(idx+1)+'. '+p.name"></b><br><small x-text="p.category+' • '+p.count+' pgto(s)'"></small></span><b x-text="money(p.total)"></b></div>
                  </template>
                  <p x-show="!annualTopProviders.length" class="text-slate-500">Sem movimentação no ano.</p>
                </div>
              </div>
            </div>

            <div class="receipt-box p-4 mb-4">
              <p class="receipt-label mb-2">Categorias mais movimentadas</p>
              <div class="space-y-2">
                <template x-for="c in annualCategorySummary.slice(0,8)" :key="c.category">
                  <div>
                    <div class="flex justify-between text-[10px] mb-1"><b x-text="c.category"></b><span x-text="money(c.total)"></span></div>
                    <div class="annual-bar"><span :style="'width:'+c.percent+'%'"></span></div>
                  </div>
                </template>
                <p x-show="!annualCategorySummary.length" class="text-slate-500">Sem categorias movimentadas no ano.</p>
              </div>

===== settings @ 6439114 =====
"width:16%">Categoria</th><th style="width:28%">Descrição</th><th style="width:12%">Valor</th></tr></thead>
              <tbody>
                <template x-for="s in reportDetailRows" :key="s.reportKey">
                  <tr>
                    <td class="font-bold text-slate-700" x-text="s.receiptNumber"></td>
                    <td x-text="fmt(s.paymentDate)"></td>
                    <td x-text="providerName(s.providerId)"></td>
                    <td x-text="s.category"></td>
                    <td x-text="s.description || '-'"></td>
                    <td class="font-bold" x-text="money(s.reportValue || 0)"></td>
                  </tr>
                </template>
              </tbody>
            </table>

            <div class="report-footer-credit pt-4 border-t border-slate-200 text-[11px] text-slate-500 leading-relaxed">
              <p><b>Observação:</b> relatório emitido para controle interno do condomínio e composição do livro de prestação de contas. Recomenda-se arquivar junto aos recibos, comprovantes de pagamento, notas fiscais, contratos ou autorizações correspondentes, quando houver.</p>
              <p class="mt-2 text-center">Documento gerado localmente pelo ReciboCondo PWA • <span x-text="settings.condoName"></span> • <span x-text="fmt(today())"></span></p>
              <p class="report-dev-credit">Aplicativo desenvolvido por Carlos Cunha</p>
            </div>
          </div>
        </div>
        <div id="annualReportPrint" class="receipt report-premium hidden">
          <div class="report-watermark">ReciboCondo</div>
          <div class="relative z-10">
            <div class="flex justify-between gap-6 items-start mb-5">
              <div class="flex gap-4 items-center">
                <template x-if="settings.logo"><img :src="settings.logo" class="h-16 w-16 object-contain rounded-xl border border-slate-200 p-1"></template>
                <template x-if="!settings.logo"><div class="h-16 w-16 rounded-xl grid place-items-center text-white font-black text-2xl" style="background:linear-gradient(135deg,#0F2A3A,#15803D)">RC</div></template>
                <div>
                  <h1 class="text-2xl font-bold leading-tight" x-text="settings.condoName"></h1>
                  <p class="text-sm text-slate-600" x-text="settings.cnpj"></p>
                  <p class="text-sm text-slate-600 max-w-[118mm]" x-text="settings.address"></p>
                </div>
              </div>
              <div class="text-right shrink-0">
                <span class="receipt-stamp">Prestação de contas</span>
                <p class="receipt-label mt-3">Ano</p>
                <p class="text-2xl font-black receipt-number" x-text="dashboardYear"></p>
                <p class="receipt-label mt-2">Emitido em</p>
                <p class="receipt-value" x-text="fmt(today())"></p>
              </div>
            </div>

            <div class="mb-4">
              <p class="receipt-label">Documento financeiro anual</p>
              <h2 class="text-3xl font-black tracking-tight text-slate-900">RELATÓRIO ANUAL DE PRESTADORES</h2>
              <p class="text-sm text-slate-600 mt-1">Resumo consolidado de pagamentos, categorias e prestadores para acompanhamento anual.</p>
            </div>

            <div class="grid grid-cols-4 gap-3 mb-4">
              <div class="report-kpi"><p class="label">Total pago</p><p class="value green" x-text="money(annualTotals.paid)"></p></div>
              <div class="report-kpi"><p class="label">Em aberto</p><p class="value red" x-text="money(annualTotals.open)"></p></div>
              <div class="report-kpi"><p class="label">Pagamentos</p><p class="value" x-text="annualTotals.count"></p></div>
              <div class="report-kpi"><p class="label">Maior categoria</p><p class="value text-[13px] leading-tight" x-text="annualCategorySummary[0]?.category || '-'"></p></div>
            </div>

            <div class="grid grid-cols-2 gap-4 mb-4">
              <div class="receipt-box p-4">
                <p class="receipt-label mb-2">Resumo mensal</p>
                <table class="annual-mini-table">
                  <thead><tr><th>Mês</th><th>Pag.</th><th>Total pago</th><th>Aberto</th></tr></thead>
                  <tbody>
                    <template x-for="m in monthlyHistory" :key="m.key">
                      <tr><td class="font-bold" x-text="m.label.slice(0,3)"></td><td x-text="m.count"></td><td class="font-bold" x-text="money(m.paid)"></td><td x-text="money(m.open)"></td></tr>
                    </template>
                  </tbody>
                </table>
              </div>
              <div class="receipt-box p-4">
                <p class="receipt-label mb-2">Top prestadores</p>
                <div class="annual-report-list">
                  <template x-for="(p,idx) in annualTopProviders.slice(0,7)" :key="p.id">
                    <div class="row"><span><b x-text="(idx+1)+'. '+p.name"></b><br><small x-text="p.category+' • '+p.count+' pgto(s)'"></small></span><b x-text="money(p.total)"></b></div>
                  </template>
                  <p x-show="!annualTopProviders.length" class="text-slate-500">Sem movimentação no ano.</p>
                </div>
              </div>
            </div>

            <div class="receipt-box p-4 mb-4">
              <p class="receipt-label mb-2">Categorias mais movimentadas</p>
              <div class="space-y-2">
                <template x-for="c in annualCategorySummary.slice(0,8)" :key="c.category">
                  <div>
                    <div class="flex justify-between text-[10px] mb-1"><b x-text="c.category"></b><span x-text="money(c.total)"></span></div>
                    <div class="annual-bar"><span :style="'width:'+c.percent+'%'"></span></div>
                  </div>
                </template>
                <p x-show="!annualCategorySummary.length" class="text-slate-500">Sem categorias movimentadas no ano.</p>
              </div>
            </div>

     

===== settings @ 6439246 =====
              <template x-for="s in reportDetailRows" :key="s.reportKey">
                  <tr>
                    <td class="font-bold text-slate-700" x-text="s.receiptNumber"></td>
                    <td x-text="fmt(s.paymentDate)"></td>
                    <td x-text="providerName(s.providerId)"></td>
                    <td x-text="s.category"></td>
                    <td x-text="s.description || '-'"></td>
                    <td class="font-bold" x-text="money(s.reportValue || 0)"></td>
                  </tr>
                </template>
              </tbody>
            </table>

            <div class="report-footer-credit pt-4 border-t border-slate-200 text-[11px] text-slate-500 leading-relaxed">
              <p><b>Observação:</b> relatório emitido para controle interno do condomínio e composição do livro de prestação de contas. Recomenda-se arquivar junto aos recibos, comprovantes de pagamento, notas fiscais, contratos ou autorizações correspondentes, quando houver.</p>
              <p class="mt-2 text-center">Documento gerado localmente pelo ReciboCondo PWA • <span x-text="settings.condoName"></span> • <span x-text="fmt(today())"></span></p>
              <p class="report-dev-credit">Aplicativo desenvolvido por Carlos Cunha</p>
            </div>
          </div>
        </div>
        <div id="annualReportPrint" class="receipt report-premium hidden">
          <div class="report-watermark">ReciboCondo</div>
          <div class="relative z-10">
            <div class="flex justify-between gap-6 items-start mb-5">
              <div class="flex gap-4 items-center">
                <template x-if="settings.logo"><img :src="settings.logo" class="h-16 w-16 object-contain rounded-xl border border-slate-200 p-1"></template>
                <template x-if="!settings.logo"><div class="h-16 w-16 rounded-xl grid place-items-center text-white font-black text-2xl" style="background:linear-gradient(135deg,#0F2A3A,#15803D)">RC</div></template>
                <div>
                  <h1 class="text-2xl font-bold leading-tight" x-text="settings.condoName"></h1>
                  <p class="text-sm text-slate-600" x-text="settings.cnpj"></p>
                  <p class="text-sm text-slate-600 max-w-[118mm]" x-text="settings.address"></p>
                </div>
              </div>
              <div class="text-right shrink-0">
                <span class="receipt-stamp">Prestação de contas</span>
                <p class="receipt-label mt-3">Ano</p>
                <p class="text-2xl font-black receipt-number" x-text="dashboardYear"></p>
                <p class="receipt-label mt-2">Emitido em</p>
                <p class="receipt-value" x-text="fmt(today())"></p>
              </div>
            </div>

            <div class="mb-4">
              <p class="receipt-label">Documento financeiro anual</p>
              <h2 class="text-3xl font-black tracking-tight text-slate-900">RELATÓRIO ANUAL DE PRESTADORES</h2>
              <p class="text-sm text-slate-600 mt-1">Resumo consolidado de pagamentos, categorias e prestadores para acompanhamento anual.</p>
            </div>

            <div class="grid grid-cols-4 gap-3 mb-4">
              <div class="report-kpi"><p class="label">Total pago</p><p class="value green" x-text="money(annualTotals.paid)"></p></div>
              <div class="report-kpi"><p class="label">Em aberto</p><p class="value red" x-text="money(annualTotals.open)"></p></div>
              <div class="report-kpi"><p class="label">Pagamentos</p><p class="value" x-text="annualTotals.count"></p></div>
              <div class="report-kpi"><p class="label">Maior categoria</p><p class="value text-[13px] leading-tight" x-text="annualCategorySummary[0]?.category || '-'"></p></div>
            </div>

            <div class="grid grid-cols-2 gap-4 mb-4">
              <div class="receipt-box p-4">
                <p class="receipt-label mb-2">Resumo mensal</p>
                <table class="annual-mini-table">
                  <thead><tr><th>Mês</th><th>Pag.</th><th>Total pago</th><th>Aberto</th></tr></thead>
                  <tbody>
                    <template x-for="m in monthlyHistory" :key="m.key">
                      <tr><td class="font-bold" x-text="m.label.slice(0,3)"></td><td x-text="m.count"></td><td class="font-bold" x-text="money(m.paid)"></td><td x-text="money(m.open)"></td></tr>
                    </template>
                  </tbody>
                </table>
              </div>
              <div class="receipt-box p-4">
                <p class="receipt-label mb-2">Top prestadores</p>
                <div class="annual-report-list">
                  <template x-for="(p,idx) in annualTopProviders.slice(0,7)" :key="p.id">
                    <div class="row"><span><b x-text="(idx+1)+'. '+p.name"></b><br><small x-text="p.category+' • '+p.count+' pgto(s)'"></small></span><b x-text="money(p.total)"></b></div>
                  </template>
                  <p x-show="!annualTopProviders.length" class="text-slate-500">Sem movimentação no ano.</p>
                </div>
              </div>
            </div>

            <div class="receipt-box p-4 mb-4">
              <p class="receipt-label mb-2">Categorias mais movimentadas</p>
              <div class="space-y-2">
                <template x-for="c in annualCategorySummary.slice(0,8)" :key="c.category">
                  <div>
                    <div class="flex justify-between text-[10px] mb-1"><b x-text="c.category"></b><span x-text="money(c.total)"></span></div>
                    <div class="annual-bar"><span :style="'width:'+c.percent+'%'"></span></div>
                  </div>
                </template>
                <p x-show="!annualCategorySummary.length" class="text-slate-500">Sem categorias movimentadas no ano.</p>
              </div>
            </div>

            <div class="report-footer-credit pt-4 border-t border-slate-200 text-[11px] text-slate-500 leading-relaxed">
              <p

===== settings @ 6439522 =====
roviderName(s.providerId)"></td>
                    <td x-text="s.category"></td>
                    <td x-text="s.description || '-'"></td>
                    <td class="font-bold" x-text="money(s.reportValue || 0)"></td>
                  </tr>
                </template>
              </tbody>
            </table>

            <div class="report-footer-credit pt-4 border-t border-slate-200 text-[11px] text-slate-500 leading-relaxed">
              <p><b>Observação:</b> relatório emitido para controle interno do condomínio e composição do livro de prestação de contas. Recomenda-se arquivar junto aos recibos, comprovantes de pagamento, notas fiscais, contratos ou autorizações correspondentes, quando houver.</p>
              <p class="mt-2 text-center">Documento gerado localmente pelo ReciboCondo PWA • <span x-text="settings.condoName"></span> • <span x-text="fmt(today())"></span></p>
              <p class="report-dev-credit">Aplicativo desenvolvido por Carlos Cunha</p>
            </div>
          </div>
        </div>
        <div id="annualReportPrint" class="receipt report-premium hidden">
          <div class="report-watermark">ReciboCondo</div>
          <div class="relative z-10">
            <div class="flex justify-between gap-6 items-start mb-5">
              <div class="flex gap-4 items-center">
                <template x-if="settings.logo"><img :src="settings.logo" class="h-16 w-16 object-contain rounded-xl border border-slate-200 p-1"></template>
                <template x-if="!settings.logo"><div class="h-16 w-16 rounded-xl grid place-items-center text-white font-black text-2xl" style="background:linear-gradient(135deg,#0F2A3A,#15803D)">RC</div></template>
                <div>
                  <h1 class="text-2xl font-bold leading-tight" x-text="settings.condoName"></h1>
                  <p class="text-sm text-slate-600" x-text="settings.cnpj"></p>
                  <p class="text-sm text-slate-600 max-w-[118mm]" x-text="settings.address"></p>
                </div>
              </div>
              <div class="text-right shrink-0">
                <span class="receipt-stamp">Prestação de contas</span>
                <p class="receipt-label mt-3">Ano</p>
                <p class="text-2xl font-black receipt-number" x-text="dashboardYear"></p>
                <p class="receipt-label mt-2">Emitido em</p>
                <p class="receipt-value" x-text="fmt(today())"></p>
              </div>
            </div>

            <div class="mb-4">
              <p class="receipt-label">Documento financeiro anual</p>
              <h2 class="text-3xl font-black tracking-tight text-slate-900">RELATÓRIO ANUAL DE PRESTADORES</h2>
              <p class="text-sm text-slate-600 mt-1">Resumo consolidado de pagamentos, categorias e prestadores para acompanhamento anual.</p>
            </div>

            <div class="grid grid-cols-4 gap-3 mb-4">
              <div class="report-kpi"><p class="label">Total pago</p><p class="value green" x-text="money(annualTotals.paid)"></p></div>
              <div class="report-kpi"><p class="label">Em aberto</p><p class="value red" x-text="money(annualTotals.open)"></p></div>
              <div class="report-kpi"><p class="label">Pagamentos</p><p class="value" x-text="annualTotals.count"></p></div>
              <div class="report-kpi"><p class="label">Maior categoria</p><p class="value text-[13px] leading-tight" x-text="annualCategorySummary[0]?.category || '-'"></p></div>
            </div>

            <div class="grid grid-cols-2 gap-4 mb-4">
              <div class="receipt-box p-4">
                <p class="receipt-label mb-2">Resumo mensal</p>
                <table class="annual-mini-table">
                  <thead><tr><th>Mês</th><th>Pag.</th><th>Total pago</th><th>Aberto</th></tr></thead>
                  <tbody>
                    <template x-for="m in monthlyHistory" :key="m.key">
                      <tr><td class="font-bold" x-text="m.label.slice(0,3)"></td><td x-text="m.count"></td><td class="font-bold" x-text="money(m.paid)"></td><td x-text="money(m.open)"></td></tr>
                    </template>
                  </tbody>
                </table>
              </div>
              <div class="receipt-box p-4">
                <p class="receipt-label mb-2">Top prestadores</p>
                <div class="annual-report-list">
                  <template x-for="(p,idx) in annualTopProviders.slice(0,7)" :key="p.id">
                    <div class="row"><span><b x-text="(idx+1)+'. '+p.name"></b><br><small x-text="p.category+' • '+p.count+' pgto(s)'"></small></span><b x-text="money(p.total)"></b></div>
                  </template>
                  <p x-show="!annualTopProviders.length" class="text-slate-500">Sem movimentação no ano.</p>
                </div>
              </div>
            </div>

            <div class="receipt-box p-4 mb-4">
              <p class="receipt-label mb-2">Categorias mais movimentadas</p>
              <div class="space-y-2">
                <template x-for="c in annualCategorySummary.slice(0,8)" :key="c.category">
                  <div>
                    <div class="flex justify-between text-[10px] mb-1"><b x-text="c.category"></b><span x-text="money(c.total)"></span></div>
                    <div class="annual-bar"><span :style="'width:'+c.percent+'%'"></span></div>
                  </div>
                </template>
                <p x-show="!annualCategorySummary.length" class="text-slate-500">Sem categorias movimentadas no ano.</p>
              </div>
            </div>

            <div class="report-footer-credit pt-4 border-t border-slate-200 text-[11px] text-slate-500 leading-relaxed">
              <p><b>Observação:</b> relatório anual emitido para leitura gerencial do condomínio. Os recibos individuais e relatórios mensais permanecem como documentos de conferência e arquivo da prestação de contas.</p>
              <p class="mt-2 text-center">Documento gerado localmente 

===== settings @ 6439608 =====
                 <td x-text="s.description || '-'"></td>
                    <td class="font-bold" x-text="money(s.reportValue || 0)"></td>
                  </tr>
                </template>
              </tbody>
            </table>

            <div class="report-footer-credit pt-4 border-t border-slate-200 text-[11px] text-slate-500 leading-relaxed">
              <p><b>Observação:</b> relatório emitido para controle interno do condomínio e composição do livro de prestação de contas. Recomenda-se arquivar junto aos recibos, comprovantes de pagamento, notas fiscais, contratos ou autorizações correspondentes, quando houver.</p>
              <p class="mt-2 text-center">Documento gerado localmente pelo ReciboCondo PWA • <span x-text="settings.condoName"></span> • <span x-text="fmt(today())"></span></p>
              <p class="report-dev-credit">Aplicativo desenvolvido por Carlos Cunha</p>
            </div>
          </div>
        </div>
        <div id="annualReportPrint" class="receipt report-premium hidden">
          <div class="report-watermark">ReciboCondo</div>
          <div class="relative z-10">
            <div class="flex justify-between gap-6 items-start mb-5">
              <div class="flex gap-4 items-center">
                <template x-if="settings.logo"><img :src="settings.logo" class="h-16 w-16 object-contain rounded-xl border border-slate-200 p-1"></template>
                <template x-if="!settings.logo"><div class="h-16 w-16 rounded-xl grid place-items-center text-white font-black text-2xl" style="background:linear-gradient(135deg,#0F2A3A,#15803D)">RC</div></template>
                <div>
                  <h1 class="text-2xl font-bold leading-tight" x-text="settings.condoName"></h1>
                  <p class="text-sm text-slate-600" x-text="settings.cnpj"></p>
                  <p class="text-sm text-slate-600 max-w-[118mm]" x-text="settings.address"></p>
                </div>
              </div>
              <div class="text-right shrink-0">
                <span class="receipt-stamp">Prestação de contas</span>
                <p class="receipt-label mt-3">Ano</p>
                <p class="text-2xl font-black receipt-number" x-text="dashboardYear"></p>
                <p class="receipt-label mt-2">Emitido em</p>
                <p class="receipt-value" x-text="fmt(today())"></p>
              </div>
            </div>

            <div class="mb-4">
              <p class="receipt-label">Documento financeiro anual</p>
              <h2 class="text-3xl font-black tracking-tight text-slate-900">RELATÓRIO ANUAL DE PRESTADORES</h2>
              <p class="text-sm text-slate-600 mt-1">Resumo consolidado de pagamentos, categorias e prestadores para acompanhamento anual.</p>
            </div>

            <div class="grid grid-cols-4 gap-3 mb-4">
              <div class="report-kpi"><p class="label">Total pago</p><p class="value green" x-text="money(annualTotals.paid)"></p></div>
              <div class="report-kpi"><p class="label">Em aberto</p><p class="value red" x-text="money(annualTotals.open)"></p></div>
              <div class="report-kpi"><p class="label">Pagamentos</p><p class="value" x-text="annualTotals.count"></p></div>
              <div class="report-kpi"><p class="label">Maior categoria</p><p class="value text-[13px] leading-tight" x-text="annualCategorySummary[0]?.category || '-'"></p></div>
            </div>

            <div class="grid grid-cols-2 gap-4 mb-4">
              <div class="receipt-box p-4">
                <p class="receipt-label mb-2">Resumo mensal</p>
                <table class="annual-mini-table">
                  <thead><tr><th>Mês</th><th>Pag.</th><th>Total pago</th><th>Aberto</th></tr></thead>
                  <tbody>
                    <template x-for="m in monthlyHistory" :key="m.key">
                      <tr><td class="font-bold" x-text="m.label.slice(0,3)"></td><td x-text="m.count"></td><td class="font-bold" x-text="money(m.paid)"></td><td x-text="money(m.open)"></td></tr>
                    </template>
                  </tbody>
                </table>
              </div>
              <div class="receipt-box p-4">
                <p class="receipt-label mb-2">Top prestadores</p>
                <div class="annual-report-list">
                  <template x-for="(p,idx) in annualTopProviders.slice(0,7)" :key="p.id">
                    <div class="row"><span><b x-text="(idx+1)+'. '+p.name"></b><br><small x-text="p.category+' • '+p.count+' pgto(s)'"></small></span><b x-text="money(p.total)"></b></div>
                  </template>
                  <p x-show="!annualTopProviders.length" class="text-slate-500">Sem movimentação no ano.</p>
                </div>
              </div>
            </div>

            <div class="receipt-box p-4 mb-4">
              <p class="receipt-label mb-2">Categorias mais movimentadas</p>
              <div class="space-y-2">
                <template x-for="c in annualCategorySummary.slice(0,8)" :key="c.category">
                  <div>
                    <div class="flex justify-between text-[10px] mb-1"><b x-text="c.category"></b><span x-text="money(c.total)"></span></div>
                    <div class="annual-bar"><span :style="'width:'+c.percent+'%'"></span></div>
                  </div>
                </template>
                <p x-show="!annualCategorySummary.length" class="text-slate-500">Sem categorias movimentadas no ano.</p>
              </div>
            </div>

            <div class="report-footer-credit pt-4 border-t border-slate-200 text-[11px] text-slate-500 leading-relaxed">
              <p><b>Observação:</b> relatório anual emitido para leitura gerencial do condomínio. Os recibos individuais e relatórios mensais permanecem como documentos de conferência e arquivo da prestação de contas.</p>
              <p class="mt-2 text-center">Documento gerado localmente pelo ReciboCondo PWA • <span x-text="settings.condoName"></span> • <span x-text="fmt(t

===== settings @ 6439702 =====
old" x-text="money(s.reportValue || 0)"></td>
                  </tr>
                </template>
              </tbody>
            </table>

            <div class="report-footer-credit pt-4 border-t border-slate-200 text-[11px] text-slate-500 leading-relaxed">
              <p><b>Observação:</b> relatório emitido para controle interno do condomínio e composição do livro de prestação de contas. Recomenda-se arquivar junto aos recibos, comprovantes de pagamento, notas fiscais, contratos ou autorizações correspondentes, quando houver.</p>
              <p class="mt-2 text-center">Documento gerado localmente pelo ReciboCondo PWA • <span x-text="settings.condoName"></span> • <span x-text="fmt(today())"></span></p>
              <p class="report-dev-credit">Aplicativo desenvolvido por Carlos Cunha</p>
            </div>
          </div>
        </div>
        <div id="annualReportPrint" class="receipt report-premium hidden">
          <div class="report-watermark">ReciboCondo</div>
          <div class="relative z-10">
            <div class="flex justify-between gap-6 items-start mb-5">
              <div class="flex gap-4 items-center">
                <template x-if="settings.logo"><img :src="settings.logo" class="h-16 w-16 object-contain rounded-xl border border-slate-200 p-1"></template>
                <template x-if="!settings.logo"><div class="h-16 w-16 rounded-xl grid place-items-center text-white font-black text-2xl" style="background:linear-gradient(135deg,#0F2A3A,#15803D)">RC</div></template>
                <div>
                  <h1 class="text-2xl font-bold leading-tight" x-text="settings.condoName"></h1>
                  <p class="text-sm text-slate-600" x-text="settings.cnpj"></p>
                  <p class="text-sm text-slate-600 max-w-[118mm]" x-text="settings.address"></p>
                </div>
              </div>
              <div class="text-right shrink-0">
                <span class="receipt-stamp">Prestação de contas</span>
                <p class="receipt-label mt-3">Ano</p>
                <p class="text-2xl font-black receipt-number" x-text="dashboardYear"></p>
                <p class="receipt-label mt-2">Emitido em</p>
                <p class="receipt-value" x-text="fmt(today())"></p>
              </div>
            </div>

            <div class="mb-4">
              <p class="receipt-label">Documento financeiro anual</p>
              <h2 class="text-3xl font-black tracking-tight text-slate-900">RELATÓRIO ANUAL DE PRESTADORES</h2>
              <p class="text-sm text-slate-600 mt-1">Resumo consolidado de pagamentos, categorias e prestadores para acompanhamento anual.</p>
            </div>

            <div class="grid grid-cols-4 gap-3 mb-4">
              <div class="report-kpi"><p class="label">Total pago</p><p class="value green" x-text="money(annualTotals.paid)"></p></div>
              <div class="report-kpi"><p class="label">Em aberto</p><p class="value red" x-text="money(annualTotals.open)"></p></div>
              <div class="report-kpi"><p class="label">Pagamentos</p><p class="value" x-text="annualTotals.count"></p></div>
              <div class="report-kpi"><p class="label">Maior categoria</p><p class="value text-[13px] leading-tight" x-text="annualCategorySummary[0]?.category || '-'"></p></div>
            </div>

            <div class="grid grid-cols-2 gap-4 mb-4">
              <div class="receipt-box p-4">
                <p class="receipt-label mb-2">Resumo mensal</p>
                <table class="annual-mini-table">
                  <thead><tr><th>Mês</th><th>Pag.</th><th>Total pago</th><th>Aberto</th></tr></thead>
                  <tbody>
                    <template x-for="m in monthlyHistory" :key="m.key">
                      <tr><td class="font-bold" x-text="m.label.slice(0,3)"></td><td x-text="m.count"></td><td class="font-bold" x-text="money(m.paid)"></td><td x-text="money(m.open)"></td></tr>
                    </template>
                  </tbody>
                </table>
              </div>
              <div class="receipt-box p-4">
                <p class="receipt-label mb-2">Top prestadores</p>
                <div class="annual-report-list">
                  <template x-for="(p,idx) in annualTopProviders.slice(0,7)" :key="p.id">
                    <div class="row"><span><b x-text="(idx+1)+'. '+p.name"></b><br><small x-text="p.category+' • '+p.count+' pgto(s)'"></small></span><b x-text="money(p.total)"></b></div>
                  </template>
                  <p x-show="!annualTopProviders.length" class="text-slate-500">Sem movimentação no ano.</p>
                </div>
              </div>
            </div>

            <div class="receipt-box p-4 mb-4">
              <p class="receipt-label mb-2">Categorias mais movimentadas</p>
              <div class="space-y-2">
                <template x-for="c in annualCategorySummary.slice(0,8)" :key="c.category">
                  <div>
                    <div class="flex justify-between text-[10px] mb-1"><b x-text="c.category"></b><span x-text="money(c.total)"></span></div>
                    <div class="annual-bar"><span :style="'width:'+c.percent+'%'"></span></div>
                  </div>
                </template>
                <p x-show="!annualCategorySummary.length" class="text-slate-500">Sem categorias movimentadas no ano.</p>
              </div>
            </div>

            <div class="report-footer-credit pt-4 border-t border-slate-200 text-[11px] text-slate-500 leading-relaxed">
              <p><b>Observação:</b> relatório anual emitido para leitura gerencial do condomínio. Os recibos individuais e relatórios mensais permanecem como documentos de conferência e arquivo da prestação de contas.</p>
              <p class="mt-2 text-center">Documento gerado localmente pelo ReciboCondo PWA • <span x-text="settings.condoName"></span> • <span x-text="fmt(today())"></span></p>
              <p class="report-dev-credit">Aplicativo desenvolvido por Ca

===== settings @ 6443759 =====
lass="receipt-box p-4">
                <p class="receipt-label mb-2">Top prestadores</p>
                <div class="annual-report-list">
                  <template x-for="(p,idx) in annualTopProviders.slice(0,7)" :key="p.id">
                    <div class="row"><span><b x-text="(idx+1)+'. '+p.name"></b><br><small x-text="p.category+' • '+p.count+' pgto(s)'"></small></span><b x-text="money(p.total)"></b></div>
                  </template>
                  <p x-show="!annualTopProviders.length" class="text-slate-500">Sem movimentação no ano.</p>
                </div>
              </div>
            </div>

            <div class="receipt-box p-4 mb-4">
              <p class="receipt-label mb-2">Categorias mais movimentadas</p>
              <div class="space-y-2">
                <template x-for="c in annualCategorySummary.slice(0,8)" :key="c.category">
                  <div>
                    <div class="flex justify-between text-[10px] mb-1"><b x-text="c.category"></b><span x-text="money(c.total)"></span></div>
                    <div class="annual-bar"><span :style="'width:'+c.percent+'%'"></span></div>
                  </div>
                </template>
                <p x-show="!annualCategorySummary.length" class="text-slate-500">Sem categorias movimentadas no ano.</p>
              </div>
            </div>

            <div class="report-footer-credit pt-4 border-t border-slate-200 text-[11px] text-slate-500 leading-relaxed">
              <p><b>Observação:</b> relatório anual emitido para leitura gerencial do condomínio. Os recibos individuais e relatórios mensais permanecem como documentos de conferência e arquivo da prestação de contas.</p>
              <p class="mt-2 text-center">Documento gerado localmente pelo ReciboCondo PWA • <span x-text="settings.condoName"></span> • <span x-text="fmt(today())"></span></p>
              <p class="report-dev-credit">Aplicativo desenvolvido por Carlos Cunha</p>
            </div>
          </div>
        </div>
      </section>
    </div>
  </template>
</div>
<script>
const db=new Dexie('ReciboCondoPrestadoresDB');
db.version(1).stores({providers:'++id,name,category,document',services:'++id,providerId,category,reference,serviceDate,dueDate,status',receipts:'++id,number,date,providerName,category',settings:'key',meta:'key'});
db.version(2).stores({providers:'++id,name,category,document',services:'++id,providerId,category,reference,serviceDate,dueDate,status,recurrenceId',receipts:'++id,number,date,providerName,category',settings:'key',meta:'key',recurrences:'++id,providerId,category,active'});
db.version(3).stores({providers:'++id,name,category,document',services:'++id,providerId,category,reference,serviceDate,dueDate,status,recurrenceId',receipts:'++id,number,date,providerName,category',settings:'key',meta:'key',recurrences:'++id,providerId,category,active',logs:'++id,at,type,title,detail,role'});
function app(){return{
  auth:{logged:false,role:'viewer'},login:{role:'admin',password:''},dark:localStorage.getItem('rc_dark')==='1',view:'dashboard',q:'',charts:{},selectedProviderId:null,activeTooltip:null,
  menu:[{id:'dashboard',label:'Início',icon:'layout-dashboard'},{id:'prestadores',label:'Prestadores',icon:'users'},{id:'servicos',label:'Serviços',icon:'hard-hat'},{id:'pagamentos',label:'Pagamentos',icon:'wallet-cards'},{id:'recibos',label:'Recibos',icon:'receipt-text'},{id:'relatorios',label:'Relatórios',icon:'file-bar-chart'},{id:'config',label:'Config.',icon:'settings'}],
  categories:['Jardineiro','Vidraceiro','Serralheiro','Eletricista','Pedreiro','Paisagista','Pintor','Encanador','Limpeza','Dedetização','Manutenção de bomba','Portão/Interfone','Gratificação natalina','Outros'],
  providers:[],services:[],receipts:[],recurrences:[],logs:[],receiptSeqValue:0,receiptSeqInput:"",settings:{condoName:'Condomínio Exemplo',cnpj:'CNPJ 00.000.000/0001-00',address:'Endereço do condomínio',manager:'Síndico/Tesoureiro',logo:''},
  providerForm:{id:null,name:'',type:'Autônomo',document:'',category:'Jardineiro',phone:'',whatsapp:'',pix:'',notes:''},providerSearch:'',providerCategoryFilter:'',providerSortField:'name',providerSortDir:'asc',
  serviceForm:{providerId:'',category:'Jardineiro',reference:new Date().toISOString().slice(0,7),serviceDate:new Date().toISOString().slice(0,10),dueDate:new Date().toISOString().slice(0,10),amount:0,location:'',description:'',paymentDate:'',paymentMethod:'',paidValue:0},
  recurrenceForm:{id:null,providerId:'',category:'Jardineiro',amount:0,dueDay:5,paymentDay:5,paymentMethod:'PIX',location:'',description:'',active:true},
  serviceFilter:'todos',serviceCategoryFilter:'todas',serviceProviderFilter:'todos',serviceMonthFilter:'',paymentMonthFilter:new Date().toISOString().slice(0,7),paymentStatusFilter:'todos',paymentProviderFilter:'todos',receiptFilter:{q:'',month:'',category:'todas'},receiptServiceId:'',currentReceipt:null,dashboardYear:new Date().getFullYear(),payModal:false,paymentTarget:null,payment:{date:new Date().toISOString().slice(0,10),value:0,method:'PIX'},actionModal:{open:false,type:'',tone:'default',icon:'',target:null,service:null,title:'',subtitle:'',note:'',confirmLabel:'',details:[]},toasts:[],
  report:{from:new Date(new Date().getFullYear(),new Date().getMonth(),1).toISOString().slice(0,10),to:new Date().toISOString().slice(0,10),category:'todas',status:'todos'},passwords:{admin:'',viewer:''},
  async init(){document.documentElement.classList.toggle('dark',this.dark);window.alert=(msg)=>this.toast(String(msg||''),this.toastType(String(msg||'')));if('serviceWorker'in navigator){navigator.serviceWorker.register('./sw.js').catch(console.warn)}await this.seed();this.refreshIcons()},
  async seed(){let s=await db.settings.get('main');if(s)this.settings=s.value;await this.load();},
  async load(){this.providers=await db.providers.toArray();this.services=await db.services.toArray();this.receipts=await db.receipts.toArray();this.recurrences=await db.recurrences.toArray();this.logs=(awai

===== settings @ 6444259 =====
th" class="text-slate-500">Sem movimentação no ano.</p>
                </div>
              </div>
            </div>

            <div class="receipt-box p-4 mb-4">
              <p class="receipt-label mb-2">Categorias mais movimentadas</p>
              <div class="space-y-2">
                <template x-for="c in annualCategorySummary.slice(0,8)" :key="c.category">
                  <div>
                    <div class="flex justify-between text-[10px] mb-1"><b x-text="c.category"></b><span x-text="money(c.total)"></span></div>
                    <div class="annual-bar"><span :style="'width:'+c.percent+'%'"></span></div>
                  </div>
                </template>
                <p x-show="!annualCategorySummary.length" class="text-slate-500">Sem categorias movimentadas no ano.</p>
              </div>
            </div>

            <div class="report-footer-credit pt-4 border-t border-slate-200 text-[11px] text-slate-500 leading-relaxed">
              <p><b>Observação:</b> relatório anual emitido para leitura gerencial do condomínio. Os recibos individuais e relatórios mensais permanecem como documentos de conferência e arquivo da prestação de contas.</p>
              <p class="mt-2 text-center">Documento gerado localmente pelo ReciboCondo PWA • <span x-text="settings.condoName"></span> • <span x-text="fmt(today())"></span></p>
              <p class="report-dev-credit">Aplicativo desenvolvido por Carlos Cunha</p>
            </div>
          </div>
        </div>
      </section>
    </div>
  </template>
</div>
<script>
const db=new Dexie('ReciboCondoPrestadoresDB');
db.version(1).stores({providers:'++id,name,category,document',services:'++id,providerId,category,reference,serviceDate,dueDate,status',receipts:'++id,number,date,providerName,category',settings:'key',meta:'key'});
db.version(2).stores({providers:'++id,name,category,document',services:'++id,providerId,category,reference,serviceDate,dueDate,status,recurrenceId',receipts:'++id,number,date,providerName,category',settings:'key',meta:'key',recurrences:'++id,providerId,category,active'});
db.version(3).stores({providers:'++id,name,category,document',services:'++id,providerId,category,reference,serviceDate,dueDate,status,recurrenceId',receipts:'++id,number,date,providerName,category',settings:'key',meta:'key',recurrences:'++id,providerId,category,active',logs:'++id,at,type,title,detail,role'});
function app(){return{
  auth:{logged:false,role:'viewer'},login:{role:'admin',password:''},dark:localStorage.getItem('rc_dark')==='1',view:'dashboard',q:'',charts:{},selectedProviderId:null,activeTooltip:null,
  menu:[{id:'dashboard',label:'Início',icon:'layout-dashboard'},{id:'prestadores',label:'Prestadores',icon:'users'},{id:'servicos',label:'Serviços',icon:'hard-hat'},{id:'pagamentos',label:'Pagamentos',icon:'wallet-cards'},{id:'recibos',label:'Recibos',icon:'receipt-text'},{id:'relatorios',label:'Relatórios',icon:'file-bar-chart'},{id:'config',label:'Config.',icon:'settings'}],
  categories:['Jardineiro','Vidraceiro','Serralheiro','Eletricista','Pedreiro','Paisagista','Pintor','Encanador','Limpeza','Dedetização','Manutenção de bomba','Portão/Interfone','Gratificação natalina','Outros'],
  providers:[],services:[],receipts:[],recurrences:[],logs:[],receiptSeqValue:0,receiptSeqInput:"",settings:{condoName:'Condomínio Exemplo',cnpj:'CNPJ 00.000.000/0001-00',address:'Endereço do condomínio',manager:'Síndico/Tesoureiro',logo:''},
  providerForm:{id:null,name:'',type:'Autônomo',document:'',category:'Jardineiro',phone:'',whatsapp:'',pix:'',notes:''},providerSearch:'',providerCategoryFilter:'',providerSortField:'name',providerSortDir:'asc',
  serviceForm:{providerId:'',category:'Jardineiro',reference:new Date().toISOString().slice(0,7),serviceDate:new Date().toISOString().slice(0,10),dueDate:new Date().toISOString().slice(0,10),amount:0,location:'',description:'',paymentDate:'',paymentMethod:'',paidValue:0},
  recurrenceForm:{id:null,providerId:'',category:'Jardineiro',amount:0,dueDay:5,paymentDay:5,paymentMethod:'PIX',location:'',description:'',active:true},
  serviceFilter:'todos',serviceCategoryFilter:'todas',serviceProviderFilter:'todos',serviceMonthFilter:'',paymentMonthFilter:new Date().toISOString().slice(0,7),paymentStatusFilter:'todos',paymentProviderFilter:'todos',receiptFilter:{q:'',month:'',category:'todas'},receiptServiceId:'',currentReceipt:null,dashboardYear:new Date().getFullYear(),payModal:false,paymentTarget:null,payment:{date:new Date().toISOString().slice(0,10),value:0,method:'PIX'},actionModal:{open:false,type:'',tone:'default',icon:'',target:null,service:null,title:'',subtitle:'',note:'',confirmLabel:'',details:[]},toasts:[],
  report:{from:new Date(new Date().getFullYear(),new Date().getMonth(),1).toISOString().slice(0,10),to:new Date().toISOString().slice(0,10),category:'todas',status:'todos'},passwords:{admin:'',viewer:''},
  async init(){document.documentElement.classList.toggle('dark',this.dark);window.alert=(msg)=>this.toast(String(msg||''),this.toastType(String(msg||'')));if('serviceWorker'in navigator){navigator.serviceWorker.register('./sw.js').catch(console.warn)}await this.seed();this.refreshIcons()},
  async seed(){let s=await db.settings.get('main');if(s)this.settings=s.value;await this.load();},
  async load(){this.providers=await db.providers.toArray();this.services=await db.services.toArray();this.receipts=await db.receipts.toArray();this.recurrences=await db.recurrences.toArray();this.logs=(await db.logs.orderBy('at').reverse().limit(30).toArray());let meta=await db.meta.get('receiptSeq');this.receiptSeqValue=Number(meta?.value||0);if(!this.receiptSeqInput)this.receiptSeqInput=this.nextReceiptNumber();this.after()},
  get isViewer(){return this.auth.role==='viewer'},get title(){return {'dashboard':'Dashboard inicial','prestadores':'Cadastro de prestadores','servicos':'Serviços','pagamentos':'Central de pagamentos','recibos':'Emissão de recibos','relatorios':'Relatórios','busca':'Busca 

===== settings @ 6444486 =====
movimentadas</p>
              <div class="space-y-2">
                <template x-for="c in annualCategorySummary.slice(0,8)" :key="c.category">
                  <div>
                    <div class="flex justify-between text-[10px] mb-1"><b x-text="c.category"></b><span x-text="money(c.total)"></span></div>
                    <div class="annual-bar"><span :style="'width:'+c.percent+'%'"></span></div>
                  </div>
                </template>
                <p x-show="!annualCategorySummary.length" class="text-slate-500">Sem categorias movimentadas no ano.</p>
              </div>
            </div>

            <div class="report-footer-credit pt-4 border-t border-slate-200 text-[11px] text-slate-500 leading-relaxed">
              <p><b>Observação:</b> relatório anual emitido para leitura gerencial do condomínio. Os recibos individuais e relatórios mensais permanecem como documentos de conferência e arquivo da prestação de contas.</p>
              <p class="mt-2 text-center">Documento gerado localmente pelo ReciboCondo PWA • <span x-text="settings.condoName"></span> • <span x-text="fmt(today())"></span></p>
              <p class="report-dev-credit">Aplicativo desenvolvido por Carlos Cunha</p>
            </div>
          </div>
        </div>
      </section>
    </div>
  </template>
</div>
<script>
const db=new Dexie('ReciboCondoPrestadoresDB');
db.version(1).stores({providers:'++id,name,category,document',services:'++id,providerId,category,reference,serviceDate,dueDate,status',receipts:'++id,number,date,providerName,category',settings:'key',meta:'key'});
db.version(2).stores({providers:'++id,name,category,document',services:'++id,providerId,category,reference,serviceDate,dueDate,status,recurrenceId',receipts:'++id,number,date,providerName,category',settings:'key',meta:'key',recurrences:'++id,providerId,category,active'});
db.version(3).stores({providers:'++id,name,category,document',services:'++id,providerId,category,reference,serviceDate,dueDate,status,recurrenceId',receipts:'++id,number,date,providerName,category',settings:'key',meta:'key',recurrences:'++id,providerId,category,active',logs:'++id,at,type,title,detail,role'});
function app(){return{
  auth:{logged:false,role:'viewer'},login:{role:'admin',password:''},dark:localStorage.getItem('rc_dark')==='1',view:'dashboard',q:'',charts:{},selectedProviderId:null,activeTooltip:null,
  menu:[{id:'dashboard',label:'Início',icon:'layout-dashboard'},{id:'prestadores',label:'Prestadores',icon:'users'},{id:'servicos',label:'Serviços',icon:'hard-hat'},{id:'pagamentos',label:'Pagamentos',icon:'wallet-cards'},{id:'recibos',label:'Recibos',icon:'receipt-text'},{id:'relatorios',label:'Relatórios',icon:'file-bar-chart'},{id:'config',label:'Config.',icon:'settings'}],
  categories:['Jardineiro','Vidraceiro','Serralheiro','Eletricista','Pedreiro','Paisagista','Pintor','Encanador','Limpeza','Dedetização','Manutenção de bomba','Portão/Interfone','Gratificação natalina','Outros'],
  providers:[],services:[],receipts:[],recurrences:[],logs:[],receiptSeqValue:0,receiptSeqInput:"",settings:{condoName:'Condomínio Exemplo',cnpj:'CNPJ 00.000.000/0001-00',address:'Endereço do condomínio',manager:'Síndico/Tesoureiro',logo:''},
  providerForm:{id:null,name:'',type:'Autônomo',document:'',category:'Jardineiro',phone:'',whatsapp:'',pix:'',notes:''},providerSearch:'',providerCategoryFilter:'',providerSortField:'name',providerSortDir:'asc',
  serviceForm:{providerId:'',category:'Jardineiro',reference:new Date().toISOString().slice(0,7),serviceDate:new Date().toISOString().slice(0,10),dueDate:new Date().toISOString().slice(0,10),amount:0,location:'',description:'',paymentDate:'',paymentMethod:'',paidValue:0},
  recurrenceForm:{id:null,providerId:'',category:'Jardineiro',amount:0,dueDay:5,paymentDay:5,paymentMethod:'PIX',location:'',description:'',active:true},
  serviceFilter:'todos',serviceCategoryFilter:'todas',serviceProviderFilter:'todos',serviceMonthFilter:'',paymentMonthFilter:new Date().toISOString().slice(0,7),paymentStatusFilter:'todos',paymentProviderFilter:'todos',receiptFilter:{q:'',month:'',category:'todas'},receiptServiceId:'',currentReceipt:null,dashboardYear:new Date().getFullYear(),payModal:false,paymentTarget:null,payment:{date:new Date().toISOString().slice(0,10),value:0,method:'PIX'},actionModal:{open:false,type:'',tone:'default',icon:'',target:null,service:null,title:'',subtitle:'',note:'',confirmLabel:'',details:[]},toasts:[],
  report:{from:new Date(new Date().getFullYear(),new Date().getMonth(),1).toISOString().slice(0,10),to:new Date().toISOString().slice(0,10),category:'todas',status:'todos'},passwords:{admin:'',viewer:''},
  async init(){document.documentElement.classList.toggle('dark',this.dark);window.alert=(msg)=>this.toast(String(msg||''),this.toastType(String(msg||'')));if('serviceWorker'in navigator){navigator.serviceWorker.register('./sw.js').catch(console.warn)}await this.seed();this.refreshIcons()},
  async seed(){let s=await db.settings.get('main');if(s)this.settings=s.value;await this.load();},
  async load(){this.providers=await db.providers.toArray();this.services=await db.services.toArray();this.receipts=await db.receipts.toArray();this.recurrences=await db.recurrences.toArray();this.logs=(await db.logs.orderBy('at').reverse().limit(30).toArray());let meta=await db.meta.get('receiptSeq');this.receiptSeqValue=Number(meta?.value||0);if(!this.receiptSeqInput)this.receiptSeqInput=this.nextReceiptNumber();this.after()},
  get isViewer(){return this.auth.role==='viewer'},get title(){return {'dashboard':'Dashboard inicial','prestadores':'Cadastro de prestadores','servicos':'Serviços','pagamentos':'Central de pagamentos','recibos':'Emissão de recibos','relatorios':'Relatórios','busca':'Busca global','config':'Configurações'}[this.view]||'ReciboCondo'},get selectedProvider(){return this.providers.find(p=>Number(p.id)===Number(this.selectedProviderId))||null},
  get cards(){return this.dashboardCards},
  get yearlyRo

===== settings @ 6444759 =====
 x-text="money(c.total)"></span></div>
                    <div class="annual-bar"><span :style="'width:'+c.percent+'%'"></span></div>
                  </div>
                </template>
                <p x-show="!annualCategorySummary.length" class="text-slate-500">Sem categorias movimentadas no ano.</p>
              </div>
            </div>

            <div class="report-footer-credit pt-4 border-t border-slate-200 text-[11px] text-slate-500 leading-relaxed">
              <p><b>Observação:</b> relatório anual emitido para leitura gerencial do condomínio. Os recibos individuais e relatórios mensais permanecem como documentos de conferência e arquivo da prestação de contas.</p>
              <p class="mt-2 text-center">Documento gerado localmente pelo ReciboCondo PWA • <span x-text="settings.condoName"></span> • <span x-text="fmt(today())"></span></p>
              <p class="report-dev-credit">Aplicativo desenvolvido por Carlos Cunha</p>
            </div>
          </div>
        </div>
      </section>
    </div>
  </template>
</div>
<script>
const db=new Dexie('ReciboCondoPrestadoresDB');
db.version(1).stores({providers:'++id,name,category,document',services:'++id,providerId,category,reference,serviceDate,dueDate,status',receipts:'++id,number,date,providerName,category',settings:'key',meta:'key'});
db.version(2).stores({providers:'++id,name,category,document',services:'++id,providerId,category,reference,serviceDate,dueDate,status,recurrenceId',receipts:'++id,number,date,providerName,category',settings:'key',meta:'key',recurrences:'++id,providerId,category,active'});
db.version(3).stores({providers:'++id,name,category,document',services:'++id,providerId,category,reference,serviceDate,dueDate,status,recurrenceId',receipts:'++id,number,date,providerName,category',settings:'key',meta:'key',recurrences:'++id,providerId,category,active',logs:'++id,at,type,title,detail,role'});
function app(){return{
  auth:{logged:false,role:'viewer'},login:{role:'admin',password:''},dark:localStorage.getItem('rc_dark')==='1',view:'dashboard',q:'',charts:{},selectedProviderId:null,activeTooltip:null,
  menu:[{id:'dashboard',label:'Início',icon:'layout-dashboard'},{id:'prestadores',label:'Prestadores',icon:'users'},{id:'servicos',label:'Serviços',icon:'hard-hat'},{id:'pagamentos',label:'Pagamentos',icon:'wallet-cards'},{id:'recibos',label:'Recibos',icon:'receipt-text'},{id:'relatorios',label:'Relatórios',icon:'file-bar-chart'},{id:'config',label:'Config.',icon:'settings'}],
  categories:['Jardineiro','Vidraceiro','Serralheiro','Eletricista','Pedreiro','Paisagista','Pintor','Encanador','Limpeza','Dedetização','Manutenção de bomba','Portão/Interfone','Gratificação natalina','Outros'],
  providers:[],services:[],receipts:[],recurrences:[],logs:[],receiptSeqValue:0,receiptSeqInput:"",settings:{condoName:'Condomínio Exemplo',cnpj:'CNPJ 00.000.000/0001-00',address:'Endereço do condomínio',manager:'Síndico/Tesoureiro',logo:''},
  providerForm:{id:null,name:'',type:'Autônomo',document:'',category:'Jardineiro',phone:'',whatsapp:'',pix:'',notes:''},providerSearch:'',providerCategoryFilter:'',providerSortField:'name',providerSortDir:'asc',
  serviceForm:{providerId:'',category:'Jardineiro',reference:new Date().toISOString().slice(0,7),serviceDate:new Date().toISOString().slice(0,10),dueDate:new Date().toISOString().slice(0,10),amount:0,location:'',description:'',paymentDate:'',paymentMethod:'',paidValue:0},
  recurrenceForm:{id:null,providerId:'',category:'Jardineiro',amount:0,dueDay:5,paymentDay:5,paymentMethod:'PIX',location:'',description:'',active:true},
  serviceFilter:'todos',serviceCategoryFilter:'todas',serviceProviderFilter:'todos',serviceMonthFilter:'',paymentMonthFilter:new Date().toISOString().slice(0,7),paymentStatusFilter:'todos',paymentProviderFilter:'todos',receiptFilter:{q:'',month:'',category:'todas'},receiptServiceId:'',currentReceipt:null,dashboardYear:new Date().getFullYear(),payModal:false,paymentTarget:null,payment:{date:new Date().toISOString().slice(0,10),value:0,method:'PIX'},actionModal:{open:false,type:'',tone:'default',icon:'',target:null,service:null,title:'',subtitle:'',note:'',confirmLabel:'',details:[]},toasts:[],
  report:{from:new Date(new Date().getFullYear(),new Date().getMonth(),1).toISOString().slice(0,10),to:new Date().toISOString().slice(0,10),category:'todas',status:'todos'},passwords:{admin:'',viewer:''},
  async init(){document.documentElement.classList.toggle('dark',this.dark);window.alert=(msg)=>this.toast(String(msg||''),this.toastType(String(msg||'')));if('serviceWorker'in navigator){navigator.serviceWorker.register('./sw.js').catch(console.warn)}await this.seed();this.refreshIcons()},
  async seed(){let s=await db.settings.get('main');if(s)this.settings=s.value;await this.load();},
  async load(){this.providers=await db.providers.toArray();this.services=await db.services.toArray();this.receipts=await db.receipts.toArray();this.recurrences=await db.recurrences.toArray();this.logs=(await db.logs.orderBy('at').reverse().limit(30).toArray());let meta=await db.meta.get('receiptSeq');this.receiptSeqValue=Number(meta?.value||0);if(!this.receiptSeqInput)this.receiptSeqInput=this.nextReceiptNumber();this.after()},
  get isViewer(){return this.auth.role==='viewer'},get title(){return {'dashboard':'Dashboard inicial','prestadores':'Cadastro de prestadores','servicos':'Serviços','pagamentos':'Central de pagamentos','recibos':'Emissão de recibos','relatorios':'Relatórios','busca':'Busca global','config':'Configurações'}[this.view]||'ReciboCondo'},get selectedProvider(){return this.providers.find(p=>Number(p.id)===Number(this.selectedProviderId))||null},
  get cards(){return this.dashboardCards},
  get yearlyRows(){let y=String(this.dashboardYear||new Date().getFullYear());return this.services.filter(s=>String(s.paymentDate||s.serviceDate||'').startsWith(y))},
  get dashboardCards(){let nowRef=new Date().toISOString().slice(0,7), year=this.yearlyRows, month=year.filter(s=>String

===== settings @ 6445450 =====
>
              <p class="mt-2 text-center">Documento gerado localmente pelo ReciboCondo PWA • <span x-text="settings.condoName"></span> • <span x-text="fmt(today())"></span></p>
              <p class="report-dev-credit">Aplicativo desenvolvido por Carlos Cunha</p>
            </div>
          </div>
        </div>
      </section>
    </div>
  </template>
</div>
<script>
const db=new Dexie('ReciboCondoPrestadoresDB');
db.version(1).stores({providers:'++id,name,category,document',services:'++id,providerId,category,reference,serviceDate,dueDate,status',receipts:'++id,number,date,providerName,category',settings:'key',meta:'key'});
db.version(2).stores({providers:'++id,name,category,document',services:'++id,providerId,category,reference,serviceDate,dueDate,status,recurrenceId',receipts:'++id,number,date,providerName,category',settings:'key',meta:'key',recurrences:'++id,providerId,category,active'});
db.version(3).stores({providers:'++id,name,category,document',services:'++id,providerId,category,reference,serviceDate,dueDate,status,recurrenceId',receipts:'++id,number,date,providerName,category',settings:'key',meta:'key',recurrences:'++id,providerId,category,active',logs:'++id,at,type,title,detail,role'});
function app(){return{
  auth:{logged:false,role:'viewer'},login:{role:'admin',password:''},dark:localStorage.getItem('rc_dark')==='1',view:'dashboard',q:'',charts:{},selectedProviderId:null,activeTooltip:null,
  menu:[{id:'dashboard',label:'Início',icon:'layout-dashboard'},{id:'prestadores',label:'Prestadores',icon:'users'},{id:'servicos',label:'Serviços',icon:'hard-hat'},{id:'pagamentos',label:'Pagamentos',icon:'wallet-cards'},{id:'recibos',label:'Recibos',icon:'receipt-text'},{id:'relatorios',label:'Relatórios',icon:'file-bar-chart'},{id:'config',label:'Config.',icon:'settings'}],
  categories:['Jardineiro','Vidraceiro','Serralheiro','Eletricista','Pedreiro','Paisagista','Pintor','Encanador','Limpeza','Dedetização','Manutenção de bomba','Portão/Interfone','Gratificação natalina','Outros'],
  providers:[],services:[],receipts:[],recurrences:[],logs:[],receiptSeqValue:0,receiptSeqInput:"",settings:{condoName:'Condomínio Exemplo',cnpj:'CNPJ 00.000.000/0001-00',address:'Endereço do condomínio',manager:'Síndico/Tesoureiro',logo:''},
  providerForm:{id:null,name:'',type:'Autônomo',document:'',category:'Jardineiro',phone:'',whatsapp:'',pix:'',notes:''},providerSearch:'',providerCategoryFilter:'',providerSortField:'name',providerSortDir:'asc',
  serviceForm:{providerId:'',category:'Jardineiro',reference:new Date().toISOString().slice(0,7),serviceDate:new Date().toISOString().slice(0,10),dueDate:new Date().toISOString().slice(0,10),amount:0,location:'',description:'',paymentDate:'',paymentMethod:'',paidValue:0},
  recurrenceForm:{id:null,providerId:'',category:'Jardineiro',amount:0,dueDay:5,paymentDay:5,paymentMethod:'PIX',location:'',description:'',active:true},
  serviceFilter:'todos',serviceCategoryFilter:'todas',serviceProviderFilter:'todos',serviceMonthFilter:'',paymentMonthFilter:new Date().toISOString().slice(0,7),paymentStatusFilter:'todos',paymentProviderFilter:'todos',receiptFilter:{q:'',month:'',category:'todas'},receiptServiceId:'',currentReceipt:null,dashboardYear:new Date().getFullYear(),payModal:false,paymentTarget:null,payment:{date:new Date().toISOString().slice(0,10),value:0,method:'PIX'},actionModal:{open:false,type:'',tone:'default',icon:'',target:null,service:null,title:'',subtitle:'',note:'',confirmLabel:'',details:[]},toasts:[],
  report:{from:new Date(new Date().getFullYear(),new Date().getMonth(),1).toISOString().slice(0,10),to:new Date().toISOString().slice(0,10),category:'todas',status:'todos'},passwords:{admin:'',viewer:''},
  async init(){document.documentElement.classList.toggle('dark',this.dark);window.alert=(msg)=>this.toast(String(msg||''),this.toastType(String(msg||'')));if('serviceWorker'in navigator){navigator.serviceWorker.register('./sw.js').catch(console.warn)}await this.seed();this.refreshIcons()},
  async seed(){let s=await db.settings.get('main');if(s)this.settings=s.value;await this.load();},
  async load(){this.providers=await db.providers.toArray();this.services=await db.services.toArray();this.receipts=await db.receipts.toArray();this.recurrences=await db.recurrences.toArray();this.logs=(await db.logs.orderBy('at').reverse().limit(30).toArray());let meta=await db.meta.get('receiptSeq');this.receiptSeqValue=Number(meta?.value||0);if(!this.receiptSeqInput)this.receiptSeqInput=this.nextReceiptNumber();this.after()},
  get isViewer(){return this.auth.role==='viewer'},get title(){return {'dashboard':'Dashboard inicial','prestadores':'Cadastro de prestadores','servicos':'Serviços','pagamentos':'Central de pagamentos','recibos':'Emissão de recibos','relatorios':'Relatórios','busca':'Busca global','config':'Configurações'}[this.view]||'ReciboCondo'},get selectedProvider(){return this.providers.find(p=>Number(p.id)===Number(this.selectedProviderId))||null},
  get cards(){return this.dashboardCards},
  get yearlyRows(){let y=String(this.dashboardYear||new Date().getFullYear());return this.services.filter(s=>String(s.paymentDate||s.serviceDate||'').startsWith(y))},
  get dashboardCards(){let nowRef=new Date().toISOString().slice(0,7), year=this.yearlyRows, month=year.filter(s=>String(s.paymentDate||s.serviceDate||'').slice(0,7)===nowRef), paidYear=year.reduce((a,s)=>a+Number(s.paidValue||0),0), paidMonth=month.reduce((a,s)=>a+Number(s.paidValue||0),0), countMonth=month.filter(s=>Number(s.paidValue||0)>0).length, open=year.reduce((a,s)=>a+this.balance(s),0), top=this.annualCategorySummary[0], topYear=this.topPaidNames(year), topMonth=this.topPaidNames(month), openTop=this.topOpenNames(year), catTop=this.annualCategorySummary.slice(0,3).map(c=>c.category+' • '+this.money(c.total)), activeCats=[...new Set(this.providers.map(p=>p.category).filter(Boolean))].slice(0,4), receiptsMonth=this.receipts.filter(r=>String(r.paymentDate||r.date||'').slice(0,7)===nowRef).leng

===== settings @ 6445775 =====
/section>
    </div>
  </template>
</div>
<script>
const db=new Dexie('ReciboCondoPrestadoresDB');
db.version(1).stores({providers:'++id,name,category,document',services:'++id,providerId,category,reference,serviceDate,dueDate,status',receipts:'++id,number,date,providerName,category',settings:'key',meta:'key'});
db.version(2).stores({providers:'++id,name,category,document',services:'++id,providerId,category,reference,serviceDate,dueDate,status,recurrenceId',receipts:'++id,number,date,providerName,category',settings:'key',meta:'key',recurrences:'++id,providerId,category,active'});
db.version(3).stores({providers:'++id,name,category,document',services:'++id,providerId,category,reference,serviceDate,dueDate,status,recurrenceId',receipts:'++id,number,date,providerName,category',settings:'key',meta:'key',recurrences:'++id,providerId,category,active',logs:'++id,at,type,title,detail,role'});
function app(){return{
  auth:{logged:false,role:'viewer'},login:{role:'admin',password:''},dark:localStorage.getItem('rc_dark')==='1',view:'dashboard',q:'',charts:{},selectedProviderId:null,activeTooltip:null,
  menu:[{id:'dashboard',label:'Início',icon:'layout-dashboard'},{id:'prestadores',label:'Prestadores',icon:'users'},{id:'servicos',label:'Serviços',icon:'hard-hat'},{id:'pagamentos',label:'Pagamentos',icon:'wallet-cards'},{id:'recibos',label:'Recibos',icon:'receipt-text'},{id:'relatorios',label:'Relatórios',icon:'file-bar-chart'},{id:'config',label:'Config.',icon:'settings'}],
  categories:['Jardineiro','Vidraceiro','Serralheiro','Eletricista','Pedreiro','Paisagista','Pintor','Encanador','Limpeza','Dedetização','Manutenção de bomba','Portão/Interfone','Gratificação natalina','Outros'],
  providers:[],services:[],receipts:[],recurrences:[],logs:[],receiptSeqValue:0,receiptSeqInput:"",settings:{condoName:'Condomínio Exemplo',cnpj:'CNPJ 00.000.000/0001-00',address:'Endereço do condomínio',manager:'Síndico/Tesoureiro',logo:''},
  providerForm:{id:null,name:'',type:'Autônomo',document:'',category:'Jardineiro',phone:'',whatsapp:'',pix:'',notes:''},providerSearch:'',providerCategoryFilter:'',providerSortField:'name',providerSortDir:'asc',
  serviceForm:{providerId:'',category:'Jardineiro',reference:new Date().toISOString().slice(0,7),serviceDate:new Date().toISOString().slice(0,10),dueDate:new Date().toISOString().slice(0,10),amount:0,location:'',description:'',paymentDate:'',paymentMethod:'',paidValue:0},
  recurrenceForm:{id:null,providerId:'',category:'Jardineiro',amount:0,dueDay:5,paymentDay:5,paymentMethod:'PIX',location:'',description:'',active:true},
  serviceFilter:'todos',serviceCategoryFilter:'todas',serviceProviderFilter:'todos',serviceMonthFilter:'',paymentMonthFilter:new Date().toISOString().slice(0,7),paymentStatusFilter:'todos',paymentProviderFilter:'todos',receiptFilter:{q:'',month:'',category:'todas'},receiptServiceId:'',currentReceipt:null,dashboardYear:new Date().getFullYear(),payModal:false,paymentTarget:null,payment:{date:new Date().toISOString().slice(0,10),value:0,method:'PIX'},actionModal:{open:false,type:'',tone:'default',icon:'',target:null,service:null,title:'',subtitle:'',note:'',confirmLabel:'',details:[]},toasts:[],
  report:{from:new Date(new Date().getFullYear(),new Date().getMonth(),1).toISOString().slice(0,10),to:new Date().toISOString().slice(0,10),category:'todas',status:'todos'},passwords:{admin:'',viewer:''},
  async init(){document.documentElement.classList.toggle('dark',this.dark);window.alert=(msg)=>this.toast(String(msg||''),this.toastType(String(msg||'')));if('serviceWorker'in navigator){navigator.serviceWorker.register('./sw.js').catch(console.warn)}await this.seed();this.refreshIcons()},
  async seed(){let s=await db.settings.get('main');if(s)this.settings=s.value;await this.load();},
  async load(){this.providers=await db.providers.toArray();this.services=await db.services.toArray();this.receipts=await db.receipts.toArray();this.recurrences=await db.recurrences.toArray();this.logs=(await db.logs.orderBy('at').reverse().limit(30).toArray());let meta=await db.meta.get('receiptSeq');this.receiptSeqValue=Number(meta?.value||0);if(!this.receiptSeqInput)this.receiptSeqInput=this.nextReceiptNumber();this.after()},
  get isViewer(){return this.auth.role==='viewer'},get title(){return {'dashboard':'Dashboard inicial','prestadores':'Cadastro de prestadores','servicos':'Serviços','pagamentos':'Central de pagamentos','recibos':'Emissão de recibos','relatorios':'Relatórios','busca':'Busca global','config':'Configurações'}[this.view]||'ReciboCondo'},get selectedProvider(){return this.providers.find(p=>Number(p.id)===Number(this.selectedProviderId))||null},
  get cards(){return this.dashboardCards},
  get yearlyRows(){let y=String(this.dashboardYear||new Date().getFullYear());return this.services.filter(s=>String(s.paymentDate||s.serviceDate||'').startsWith(y))},
  get dashboardCards(){let nowRef=new Date().toISOString().slice(0,7), year=this.yearlyRows, month=year.filter(s=>String(s.paymentDate||s.serviceDate||'').slice(0,7)===nowRef), paidYear=year.reduce((a,s)=>a+Number(s.paidValue||0),0), paidMonth=month.reduce((a,s)=>a+Number(s.paidValue||0),0), countMonth=month.filter(s=>Number(s.paidValue||0)>0).length, open=year.reduce((a,s)=>a+this.balance(s),0), top=this.annualCategorySummary[0], topYear=this.topPaidNames(year), topMonth=this.topPaidNames(month), openTop=this.topOpenNames(year), catTop=this.annualCategorySummary.slice(0,3).map(c=>c.category+' • '+this.money(c.total)), activeCats=[...new Set(this.providers.map(p=>p.category).filter(Boolean))].slice(0,4), receiptsMonth=this.receipts.filter(r=>String(r.paymentDate||r.date||'').slice(0,7)===nowRef).length;return[
    {label:'Pago no ano',value:this.money(paidYear),hint:String(this.dashboardYear),icon:'wallet-cards',color:'text-rc-600',tooltipTitle:'Resumo anual',tooltipMain:this.money(paidYear),tooltipTag:'Ano '+this.dashboardYear,tooltip:['Total efetivamente pago no ano selecionado.', 'Top recebimentos: '+(topYear.length

===== settings @ 6447684 =====
ger:'Síndico/Tesoureiro',logo:''},
  providerForm:{id:null,name:'',type:'Autônomo',document:'',category:'Jardineiro',phone:'',whatsapp:'',pix:'',notes:''},providerSearch:'',providerCategoryFilter:'',providerSortField:'name',providerSortDir:'asc',
  serviceForm:{providerId:'',category:'Jardineiro',reference:new Date().toISOString().slice(0,7),serviceDate:new Date().toISOString().slice(0,10),dueDate:new Date().toISOString().slice(0,10),amount:0,location:'',description:'',paymentDate:'',paymentMethod:'',paidValue:0},
  recurrenceForm:{id:null,providerId:'',category:'Jardineiro',amount:0,dueDay:5,paymentDay:5,paymentMethod:'PIX',location:'',description:'',active:true},
  serviceFilter:'todos',serviceCategoryFilter:'todas',serviceProviderFilter:'todos',serviceMonthFilter:'',paymentMonthFilter:new Date().toISOString().slice(0,7),paymentStatusFilter:'todos',paymentProviderFilter:'todos',receiptFilter:{q:'',month:'',category:'todas'},receiptServiceId:'',currentReceipt:null,dashboardYear:new Date().getFullYear(),payModal:false,paymentTarget:null,payment:{date:new Date().toISOString().slice(0,10),value:0,method:'PIX'},actionModal:{open:false,type:'',tone:'default',icon:'',target:null,service:null,title:'',subtitle:'',note:'',confirmLabel:'',details:[]},toasts:[],
  report:{from:new Date(new Date().getFullYear(),new Date().getMonth(),1).toISOString().slice(0,10),to:new Date().toISOString().slice(0,10),category:'todas',status:'todos'},passwords:{admin:'',viewer:''},
  async init(){document.documentElement.classList.toggle('dark',this.dark);window.alert=(msg)=>this.toast(String(msg||''),this.toastType(String(msg||'')));if('serviceWorker'in navigator){navigator.serviceWorker.register('./sw.js').catch(console.warn)}await this.seed();this.refreshIcons()},
  async seed(){let s=await db.settings.get('main');if(s)this.settings=s.value;await this.load();},
  async load(){this.providers=await db.providers.toArray();this.services=await db.services.toArray();this.receipts=await db.receipts.toArray();this.recurrences=await db.recurrences.toArray();this.logs=(await db.logs.orderBy('at').reverse().limit(30).toArray());let meta=await db.meta.get('receiptSeq');this.receiptSeqValue=Number(meta?.value||0);if(!this.receiptSeqInput)this.receiptSeqInput=this.nextReceiptNumber();this.after()},
  get isViewer(){return this.auth.role==='viewer'},get title(){return {'dashboard':'Dashboard inicial','prestadores':'Cadastro de prestadores','servicos':'Serviços','pagamentos':'Central de pagamentos','recibos':'Emissão de recibos','relatorios':'Relatórios','busca':'Busca global','config':'Configurações'}[this.view]||'ReciboCondo'},get selectedProvider(){return this.providers.find(p=>Number(p.id)===Number(this.selectedProviderId))||null},
  get cards(){return this.dashboardCards},
  get yearlyRows(){let y=String(this.dashboardYear||new Date().getFullYear());return this.services.filter(s=>String(s.paymentDate||s.serviceDate||'').startsWith(y))},
  get dashboardCards(){let nowRef=new Date().toISOString().slice(0,7), year=this.yearlyRows, month=year.filter(s=>String(s.paymentDate||s.serviceDate||'').slice(0,7)===nowRef), paidYear=year.reduce((a,s)=>a+Number(s.paidValue||0),0), paidMonth=month.reduce((a,s)=>a+Number(s.paidValue||0),0), countMonth=month.filter(s=>Number(s.paidValue||0)>0).length, open=year.reduce((a,s)=>a+this.balance(s),0), top=this.annualCategorySummary[0], topYear=this.topPaidNames(year), topMonth=this.topPaidNames(month), openTop=this.topOpenNames(year), catTop=this.annualCategorySummary.slice(0,3).map(c=>c.category+' • '+this.money(c.total)), activeCats=[...new Set(this.providers.map(p=>p.category).filter(Boolean))].slice(0,4), receiptsMonth=this.receipts.filter(r=>String(r.paymentDate||r.date||'').slice(0,7)===nowRef).length;return[
    {label:'Pago no ano',value:this.money(paidYear),hint:String(this.dashboardYear),icon:'wallet-cards',color:'text-rc-600',tooltipTitle:'Resumo anual',tooltipMain:this.money(paidYear),tooltipTag:'Ano '+this.dashboardYear,tooltip:['Total efetivamente pago no ano selecionado.', 'Top recebimentos: '+(topYear.length?topYear.join(' | '):'sem pagamentos lançados.'), 'Use para comparar o volume anual com o relatório anual premium.']},
    {label:'Pago no mês',value:this.money(paidMonth),hint:this.month(nowRef),icon:'circle-dollar-sign',color:'text-emerald-600',tooltipTitle:'Resumo do mês',tooltipMain:this.money(paidMonth),tooltipTag:'Mês atual',tooltip:['Mês de referência: '+this.month(nowRef)+'.', 'Quem recebeu: '+(topMonth.length?topMonth.join(' | '):'sem pagamentos neste mês.'), 'Ajuda a conferir rapidamente o fechamento mensal.']},
    {label:'Pagamentos no mês',value:countMonth,hint:'registros pagos',icon:'receipt-text',color:'text-blue-600',tooltipTitle:'Movimento mensal',tooltipMain:countMonth+' pagamento(s)',tooltipTag:'Recibos '+receiptsMonth,tooltip:['Valor médio por pagamento: '+this.money(countMonth?paidMonth/countMonth:0)+'.', 'Recibos emitidos no mês: '+receiptsMonth+'.', 'Bom para conferir se todos os pagamentos pagos já têm recibo.']},
    {label:'Prestadores ativos',value:this.providers.length,hint:'cadastrados',icon:'users',color:'text-rc-600',tooltipTitle:'Base de prestadores',tooltipMain:this.providers.length+' cadastro(s)',tooltipTag:'Cadastros',tooltip:['Categorias em uso: '+(activeCats.length?activeCats.join(', '):'nenhuma categoria cadastrada.'), 'Use para localizar quem presta serviço recorrente ou avulso.', 'Dica: mantenha PIX, WhatsApp e categoria sempre atualizados.']},
    {label:'Em aberto no ano',value:this.money(open),hint:'pendente/parcial',icon:'triangle-alert',color:'text-red-600',tooltipTitle:'Pendências do ano',tooltipMain:this.money(open),tooltipTag:'Atenção',tooltip:['Saldo lançado e ainda não quitado no ano.', 'Maiores pendências: '+(openTop.length?openTop.join(' | '):'nenhuma pendência no ano.'), 'Depois de pagar, registre o pagamento antes de emitir o recibo.']},
    {label:'Maior categoria',value:top?top.category:'-',hint:top?this.money(top.total):

===== settings @ 6447715 =====
'},
  providerForm:{id:null,name:'',type:'Autônomo',document:'',category:'Jardineiro',phone:'',whatsapp:'',pix:'',notes:''},providerSearch:'',providerCategoryFilter:'',providerSortField:'name',providerSortDir:'asc',
  serviceForm:{providerId:'',category:'Jardineiro',reference:new Date().toISOString().slice(0,7),serviceDate:new Date().toISOString().slice(0,10),dueDate:new Date().toISOString().slice(0,10),amount:0,location:'',description:'',paymentDate:'',paymentMethod:'',paidValue:0},
  recurrenceForm:{id:null,providerId:'',category:'Jardineiro',amount:0,dueDay:5,paymentDay:5,paymentMethod:'PIX',location:'',description:'',active:true},
  serviceFilter:'todos',serviceCategoryFilter:'todas',serviceProviderFilter:'todos',serviceMonthFilter:'',paymentMonthFilter:new Date().toISOString().slice(0,7),paymentStatusFilter:'todos',paymentProviderFilter:'todos',receiptFilter:{q:'',month:'',category:'todas'},receiptServiceId:'',currentReceipt:null,dashboardYear:new Date().getFullYear(),payModal:false,paymentTarget:null,payment:{date:new Date().toISOString().slice(0,10),value:0,method:'PIX'},actionModal:{open:false,type:'',tone:'default',icon:'',target:null,service:null,title:'',subtitle:'',note:'',confirmLabel:'',details:[]},toasts:[],
  report:{from:new Date(new Date().getFullYear(),new Date().getMonth(),1).toISOString().slice(0,10),to:new Date().toISOString().slice(0,10),category:'todas',status:'todos'},passwords:{admin:'',viewer:''},
  async init(){document.documentElement.classList.toggle('dark',this.dark);window.alert=(msg)=>this.toast(String(msg||''),this.toastType(String(msg||'')));if('serviceWorker'in navigator){navigator.serviceWorker.register('./sw.js').catch(console.warn)}await this.seed();this.refreshIcons()},
  async seed(){let s=await db.settings.get('main');if(s)this.settings=s.value;await this.load();},
  async load(){this.providers=await db.providers.toArray();this.services=await db.services.toArray();this.receipts=await db.receipts.toArray();this.recurrences=await db.recurrences.toArray();this.logs=(await db.logs.orderBy('at').reverse().limit(30).toArray());let meta=await db.meta.get('receiptSeq');this.receiptSeqValue=Number(meta?.value||0);if(!this.receiptSeqInput)this.receiptSeqInput=this.nextReceiptNumber();this.after()},
  get isViewer(){return this.auth.role==='viewer'},get title(){return {'dashboard':'Dashboard inicial','prestadores':'Cadastro de prestadores','servicos':'Serviços','pagamentos':'Central de pagamentos','recibos':'Emissão de recibos','relatorios':'Relatórios','busca':'Busca global','config':'Configurações'}[this.view]||'ReciboCondo'},get selectedProvider(){return this.providers.find(p=>Number(p.id)===Number(this.selectedProviderId))||null},
  get cards(){return this.dashboardCards},
  get yearlyRows(){let y=String(this.dashboardYear||new Date().getFullYear());return this.services.filter(s=>String(s.paymentDate||s.serviceDate||'').startsWith(y))},
  get dashboardCards(){let nowRef=new Date().toISOString().slice(0,7), year=this.yearlyRows, month=year.filter(s=>String(s.paymentDate||s.serviceDate||'').slice(0,7)===nowRef), paidYear=year.reduce((a,s)=>a+Number(s.paidValue||0),0), paidMonth=month.reduce((a,s)=>a+Number(s.paidValue||0),0), countMonth=month.filter(s=>Number(s.paidValue||0)>0).length, open=year.reduce((a,s)=>a+this.balance(s),0), top=this.annualCategorySummary[0], topYear=this.topPaidNames(year), topMonth=this.topPaidNames(month), openTop=this.topOpenNames(year), catTop=this.annualCategorySummary.slice(0,3).map(c=>c.category+' • '+this.money(c.total)), activeCats=[...new Set(this.providers.map(p=>p.category).filter(Boolean))].slice(0,4), receiptsMonth=this.receipts.filter(r=>String(r.paymentDate||r.date||'').slice(0,7)===nowRef).length;return[
    {label:'Pago no ano',value:this.money(paidYear),hint:String(this.dashboardYear),icon:'wallet-cards',color:'text-rc-600',tooltipTitle:'Resumo anual',tooltipMain:this.money(paidYear),tooltipTag:'Ano '+this.dashboardYear,tooltip:['Total efetivamente pago no ano selecionado.', 'Top recebimentos: '+(topYear.length?topYear.join(' | '):'sem pagamentos lançados.'), 'Use para comparar o volume anual com o relatório anual premium.']},
    {label:'Pago no mês',value:this.money(paidMonth),hint:this.month(nowRef),icon:'circle-dollar-sign',color:'text-emerald-600',tooltipTitle:'Resumo do mês',tooltipMain:this.money(paidMonth),tooltipTag:'Mês atual',tooltip:['Mês de referência: '+this.month(nowRef)+'.', 'Quem recebeu: '+(topMonth.length?topMonth.join(' | '):'sem pagamentos neste mês.'), 'Ajuda a conferir rapidamente o fechamento mensal.']},
    {label:'Pagamentos no mês',value:countMonth,hint:'registros pagos',icon:'receipt-text',color:'text-blue-600',tooltipTitle:'Movimento mensal',tooltipMain:countMonth+' pagamento(s)',tooltipTag:'Recibos '+receiptsMonth,tooltip:['Valor médio por pagamento: '+this.money(countMonth?paidMonth/countMonth:0)+'.', 'Recibos emitidos no mês: '+receiptsMonth+'.', 'Bom para conferir se todos os pagamentos pagos já têm recibo.']},
    {label:'Prestadores ativos',value:this.providers.length,hint:'cadastrados',icon:'users',color:'text-rc-600',tooltipTitle:'Base de prestadores',tooltipMain:this.providers.length+' cadastro(s)',tooltipTag:'Cadastros',tooltip:['Categorias em uso: '+(activeCats.length?activeCats.join(', '):'nenhuma categoria cadastrada.'), 'Use para localizar quem presta serviço recorrente ou avulso.', 'Dica: mantenha PIX, WhatsApp e categoria sempre atualizados.']},
    {label:'Em aberto no ano',value:this.money(open),hint:'pendente/parcial',icon:'triangle-alert',color:'text-red-600',tooltipTitle:'Pendências do ano',tooltipMain:this.money(open),tooltipTag:'Atenção',tooltip:['Saldo lançado e ainda não quitado no ano.', 'Maiores pendências: '+(openTop.length?openTop.join(' | '):'nenhuma pendência no ano.'), 'Depois de pagar, registre o pagamento antes de emitir o recibo.']},
    {label:'Maior categoria',value:top?top.category:'-',hint:top?this.money(top.total):'sem dados',icon:'trophy',color

===== settings @ 6487991 =====
ssued=linked.reduce((a,r)=>a+Number(r.total||0),0),pending=Math.max(0,Number(s.paidValue||0)-issued);if(pending<=0){let latest=linked.slice().sort((a,b)=>Number(b.id||0)-Number(a.id||0))[0];if(latest){this.currentReceipt=latest;this.view='recibos';this.after();this.toast('Todos os pagamentos registrados já possuem recibo.','warning');setTimeout(()=>this.printReceipt(),220)}else this.toast('Não há valor pago pendente de recibo.','warning');return}this.receiptServiceId=s.id;await this.issueReceipt();this.view='recibos';this.after();setTimeout(()=>this.printReceipt(),260)},async issueReceipt(){if(this.isViewer)return;let s=this.services.find(x=>Number(x.id)===Number(this.receiptServiceId));if(!s)return this.toast('Selecione um lançamento.','warning');let linked=this.receipts.filter(r=>Number(r.serviceId)===Number(s.id)),issued=linked.reduce((a,r)=>a+Number(r.total||0),0),pending=Math.max(0,Number(s.paidValue||0)-issued);if(pending<=0)return this.toast('Todos os pagamentos registrados deste lançamento já possuem recibo.','warning');let desc=String(s.description||'').replace(/(\bparcela\s+)(\d+)\s*\/\s*(\d+)/i,(m,prefix,current,total)=>prefix+String(linked.length+1).padStart(current.length,'0')+'/'+total);let p=this.providers.find(x=>Number(x.id)===Number(s.providerId))||{};let meta=await db.meta.get('receiptSeq'),next=Math.max(Number(meta?.value||0),this.receiptMaxNumber())+1;await db.meta.put({key:'receiptSeq',value:next});let r={number:String(next).padStart(6,'0'),date:this.today(),paymentDate:s.paymentDate||this.today(),providerId:p.id,providerName:p.name||'Prestador não informado',document:p.document||'',category:s.category,reference:s.reference,description:desc,total:pending,paymentMethod:s.paymentMethod||'Não informado',location:s.location,serviceId:s.id,manager:this.settings.manager,condoName:this.settings.condoName};let id=await db.receipts.add(r);r.id=id;this.currentReceipt=r;await this.logAction('receipt','Recibo emitido','Nº '+r.number+' • '+r.providerName+' • '+this.money(r.total||0));await this.load();this.toast('Recibo nº '+r.number+' emitido no valor de '+this.money(r.total||0)+'.')},printReceipt(){if(!this.currentReceipt&&this.receipts.length)this.currentReceipt=this.receipts[this.receipts.length-1];document.getElementById('receiptPrint').classList.remove('hidden');document.getElementById('reportPrint').classList.add('hidden');document.getElementById('annualReportPrint').classList.add('hidden');setTimeout(()=>window.print(),100)},async removeReceiptInternal(id){let r=this.receipts.find(x=>Number(x.id)===Number(id));if(!r)return;let linked=this.receipts.filter(x=>Number(x.serviceId)===Number(r.serviceId)).sort((a,b)=>Number(b.id||0)-Number(a.id||0)),latest=linked[0];if(latest&&Number(latest.id)!==Number(r.id))return this.toast('Para preservar a data e a forma de pagamento das parcelas, somente o recibo mais recente deste lançamento pode ser excluído e reemitido.','warning');await db.receipts.delete(id);await this.logAction('delete','Recibo excluído',r?('Nº '+r.number+' • '+r.providerName):('ID '+id));await this.load()},
  printReport(){document.body.classList.add('printing-report');document.getElementById('receiptPrint').classList.add('hidden');document.getElementById('annualReportPrint').classList.add('hidden');document.getElementById('reportPrint').classList.remove('hidden');setTimeout(()=>{window.print();document.body.classList.remove('printing-report');document.getElementById('receiptPrint').classList.remove('hidden');document.getElementById('annualReportPrint').classList.add('hidden');document.getElementById('reportPrint').classList.add('hidden')},100)},printAnnualReport(){document.body.classList.add('printing-annual');let rp=document.getElementById('receiptPrint'),mp=document.getElementById('reportPrint'),ap=document.getElementById('annualReportPrint');rp.classList.add('hidden');mp.classList.add('hidden');ap.classList.remove('hidden');ap.style.display='block';ap.style.visibility='visible';setTimeout(()=>{window.print();document.body.classList.remove('printing-annual');rp.classList.remove('hidden');ap.classList.add('hidden');ap.style.display='';ap.style.visibility='';mp.classList.add('hidden')},180)},csv(){let rows=[['Recibo nº','Data do serviço','Data do pagamento','Prestador','Categoria','Descrição','Valor do serviço','Valor deste pagamento','Status','Forma']].concat(this.reportDetailRows.map(s=>[s.receiptNumber,s.serviceDate,s.paymentDate||'',this.providerName(s.providerId),s.category,s.description,s.amount,s.reportValue||0,s.status,s.paymentMethod||'']));let csv=rows.map(r=>r.map(v=>'"'+String(v??'').replaceAll('"','""')+'"').join(';')).join('\n');this.download('relatorio-prestadores-recibocondo.csv',csv,'text/csv;charset=utf-8')},
  async saveSettings(){let plainSettings=JSON.parse(JSON.stringify(this.settings));await db.settings.put({key:'main',value:plainSettings});await this.logAction('settings','Configurações salvas','Dados do condomínio atualizados.');this.toast('Configurações salvas')},async savePasswords(){if(this.isViewer)return;let cur=JSON.parse(localStorage.getItem('rc_passwords')||'{"admin":"123456","viewer":"123456"}');if(this.passwords.admin)cur.admin=this.passwords.admin;if(this.passwords.viewer)cur.viewer=this.passwords.viewer;localStorage.setItem('rc_passwords',JSON.stringify(cur));await this.logAction('settings','Senhas atualizadas','Senhas locais do app foram atualizadas.');this.passwords={admin:'',viewer:''};this.toast('Senhas atualizadas')},async backup(){let now=new Date().toISOString();let seqMeta=await db.meta.get('receiptSeq');let data={settings:this.settings,providers:this.providers,services:this.services,receipts:this.receipts,recurrences:this.recurrences,logs:this.logs,receiptSeq:Number(seqMeta?.value||this.receiptMaxNumber()||0),backupStats:this.backupStats(),exportedAt:now};this.download('backup-recibocondo-prestadores.json',JSON.stringify(data,null,2),'application/json');this.backupLast=now;localStorage.setItem('rc_last_backup',now);awa

===== settings @ 6488023 =====
ber(r.total||0),0),pending=Math.max(0,Number(s.paidValue||0)-issued);if(pending<=0){let latest=linked.slice().sort((a,b)=>Number(b.id||0)-Number(a.id||0))[0];if(latest){this.currentReceipt=latest;this.view='recibos';this.after();this.toast('Todos os pagamentos registrados já possuem recibo.','warning');setTimeout(()=>this.printReceipt(),220)}else this.toast('Não há valor pago pendente de recibo.','warning');return}this.receiptServiceId=s.id;await this.issueReceipt();this.view='recibos';this.after();setTimeout(()=>this.printReceipt(),260)},async issueReceipt(){if(this.isViewer)return;let s=this.services.find(x=>Number(x.id)===Number(this.receiptServiceId));if(!s)return this.toast('Selecione um lançamento.','warning');let linked=this.receipts.filter(r=>Number(r.serviceId)===Number(s.id)),issued=linked.reduce((a,r)=>a+Number(r.total||0),0),pending=Math.max(0,Number(s.paidValue||0)-issued);if(pending<=0)return this.toast('Todos os pagamentos registrados deste lançamento já possuem recibo.','warning');let desc=String(s.description||'').replace(/(\bparcela\s+)(\d+)\s*\/\s*(\d+)/i,(m,prefix,current,total)=>prefix+String(linked.length+1).padStart(current.length,'0')+'/'+total);let p=this.providers.find(x=>Number(x.id)===Number(s.providerId))||{};let meta=await db.meta.get('receiptSeq'),next=Math.max(Number(meta?.value||0),this.receiptMaxNumber())+1;await db.meta.put({key:'receiptSeq',value:next});let r={number:String(next).padStart(6,'0'),date:this.today(),paymentDate:s.paymentDate||this.today(),providerId:p.id,providerName:p.name||'Prestador não informado',document:p.document||'',category:s.category,reference:s.reference,description:desc,total:pending,paymentMethod:s.paymentMethod||'Não informado',location:s.location,serviceId:s.id,manager:this.settings.manager,condoName:this.settings.condoName};let id=await db.receipts.add(r);r.id=id;this.currentReceipt=r;await this.logAction('receipt','Recibo emitido','Nº '+r.number+' • '+r.providerName+' • '+this.money(r.total||0));await this.load();this.toast('Recibo nº '+r.number+' emitido no valor de '+this.money(r.total||0)+'.')},printReceipt(){if(!this.currentReceipt&&this.receipts.length)this.currentReceipt=this.receipts[this.receipts.length-1];document.getElementById('receiptPrint').classList.remove('hidden');document.getElementById('reportPrint').classList.add('hidden');document.getElementById('annualReportPrint').classList.add('hidden');setTimeout(()=>window.print(),100)},async removeReceiptInternal(id){let r=this.receipts.find(x=>Number(x.id)===Number(id));if(!r)return;let linked=this.receipts.filter(x=>Number(x.serviceId)===Number(r.serviceId)).sort((a,b)=>Number(b.id||0)-Number(a.id||0)),latest=linked[0];if(latest&&Number(latest.id)!==Number(r.id))return this.toast('Para preservar a data e a forma de pagamento das parcelas, somente o recibo mais recente deste lançamento pode ser excluído e reemitido.','warning');await db.receipts.delete(id);await this.logAction('delete','Recibo excluído',r?('Nº '+r.number+' • '+r.providerName):('ID '+id));await this.load()},
  printReport(){document.body.classList.add('printing-report');document.getElementById('receiptPrint').classList.add('hidden');document.getElementById('annualReportPrint').classList.add('hidden');document.getElementById('reportPrint').classList.remove('hidden');setTimeout(()=>{window.print();document.body.classList.remove('printing-report');document.getElementById('receiptPrint').classList.remove('hidden');document.getElementById('annualReportPrint').classList.add('hidden');document.getElementById('reportPrint').classList.add('hidden')},100)},printAnnualReport(){document.body.classList.add('printing-annual');let rp=document.getElementById('receiptPrint'),mp=document.getElementById('reportPrint'),ap=document.getElementById('annualReportPrint');rp.classList.add('hidden');mp.classList.add('hidden');ap.classList.remove('hidden');ap.style.display='block';ap.style.visibility='visible';setTimeout(()=>{window.print();document.body.classList.remove('printing-annual');rp.classList.remove('hidden');ap.classList.add('hidden');ap.style.display='';ap.style.visibility='';mp.classList.add('hidden')},180)},csv(){let rows=[['Recibo nº','Data do serviço','Data do pagamento','Prestador','Categoria','Descrição','Valor do serviço','Valor deste pagamento','Status','Forma']].concat(this.reportDetailRows.map(s=>[s.receiptNumber,s.serviceDate,s.paymentDate||'',this.providerName(s.providerId),s.category,s.description,s.amount,s.reportValue||0,s.status,s.paymentMethod||'']));let csv=rows.map(r=>r.map(v=>'"'+String(v??'').replaceAll('"','""')+'"').join(';')).join('\n');this.download('relatorio-prestadores-recibocondo.csv',csv,'text/csv;charset=utf-8')},
  async saveSettings(){let plainSettings=JSON.parse(JSON.stringify(this.settings));await db.settings.put({key:'main',value:plainSettings});await this.logAction('settings','Configurações salvas','Dados do condomínio atualizados.');this.toast('Configurações salvas')},async savePasswords(){if(this.isViewer)return;let cur=JSON.parse(localStorage.getItem('rc_passwords')||'{"admin":"123456","viewer":"123456"}');if(this.passwords.admin)cur.admin=this.passwords.admin;if(this.passwords.viewer)cur.viewer=this.passwords.viewer;localStorage.setItem('rc_passwords',JSON.stringify(cur));await this.logAction('settings','Senhas atualizadas','Senhas locais do app foram atualizadas.');this.passwords={admin:'',viewer:''};this.toast('Senhas atualizadas')},async backup(){let now=new Date().toISOString();let seqMeta=await db.meta.get('receiptSeq');let data={settings:this.settings,providers:this.providers,services:this.services,receipts:this.receipts,recurrences:this.recurrences,logs:this.logs,receiptSeq:Number(seqMeta?.value||this.receiptMaxNumber()||0),backupStats:this.backupStats(),exportedAt:now};this.download('backup-recibocondo-prestadores.json',JSON.stringify(data,null,2),'application/json');this.backupLast=now;localStorage.setItem('rc_last_backup',now);await this.logAction('backup','Back

===== settings @ 6491003 =====
o',r?('Nº '+r.number+' • '+r.providerName):('ID '+id));await this.load()},
  printReport(){document.body.classList.add('printing-report');document.getElementById('receiptPrint').classList.add('hidden');document.getElementById('annualReportPrint').classList.add('hidden');document.getElementById('reportPrint').classList.remove('hidden');setTimeout(()=>{window.print();document.body.classList.remove('printing-report');document.getElementById('receiptPrint').classList.remove('hidden');document.getElementById('annualReportPrint').classList.add('hidden');document.getElementById('reportPrint').classList.add('hidden')},100)},printAnnualReport(){document.body.classList.add('printing-annual');let rp=document.getElementById('receiptPrint'),mp=document.getElementById('reportPrint'),ap=document.getElementById('annualReportPrint');rp.classList.add('hidden');mp.classList.add('hidden');ap.classList.remove('hidden');ap.style.display='block';ap.style.visibility='visible';setTimeout(()=>{window.print();document.body.classList.remove('printing-annual');rp.classList.remove('hidden');ap.classList.add('hidden');ap.style.display='';ap.style.visibility='';mp.classList.add('hidden')},180)},csv(){let rows=[['Recibo nº','Data do serviço','Data do pagamento','Prestador','Categoria','Descrição','Valor do serviço','Valor deste pagamento','Status','Forma']].concat(this.reportDetailRows.map(s=>[s.receiptNumber,s.serviceDate,s.paymentDate||'',this.providerName(s.providerId),s.category,s.description,s.amount,s.reportValue||0,s.status,s.paymentMethod||'']));let csv=rows.map(r=>r.map(v=>'"'+String(v??'').replaceAll('"','""')+'"').join(';')).join('\n');this.download('relatorio-prestadores-recibocondo.csv',csv,'text/csv;charset=utf-8')},
  async saveSettings(){let plainSettings=JSON.parse(JSON.stringify(this.settings));await db.settings.put({key:'main',value:plainSettings});await this.logAction('settings','Configurações salvas','Dados do condomínio atualizados.');this.toast('Configurações salvas')},async savePasswords(){if(this.isViewer)return;let cur=JSON.parse(localStorage.getItem('rc_passwords')||'{"admin":"123456","viewer":"123456"}');if(this.passwords.admin)cur.admin=this.passwords.admin;if(this.passwords.viewer)cur.viewer=this.passwords.viewer;localStorage.setItem('rc_passwords',JSON.stringify(cur));await this.logAction('settings','Senhas atualizadas','Senhas locais do app foram atualizadas.');this.passwords={admin:'',viewer:''};this.toast('Senhas atualizadas')},async backup(){let now=new Date().toISOString();let seqMeta=await db.meta.get('receiptSeq');let data={settings:this.settings,providers:this.providers,services:this.services,receipts:this.receipts,recurrences:this.recurrences,logs:this.logs,receiptSeq:Number(seqMeta?.value||this.receiptMaxNumber()||0),backupStats:this.backupStats(),exportedAt:now};this.download('backup-recibocondo-prestadores.json',JSON.stringify(data,null,2),'application/json');this.backupLast=now;localStorage.setItem('rc_last_backup',now);await this.logAction('backup','Backup exportado','Backup JSON gerado com '+data.backupStats.records+' registros.')},async restore(ev){let f=ev.target.files[0];if(!f)return;try{let data=JSON.parse(await f.text());if(!data||typeof data!=='object')throw new Error('Backup inválido');let providers=Array.isArray(data.providers)?data.providers:[],services=Array.isArray(data.services)?data.services:[],receipts=Array.isArray(data.receipts)?data.receipts:[],recurrences=Array.isArray(data.recurrences)?data.recurrences:[],logs=Array.isArray(data.logs)?data.logs:[];await db.transaction('rw',db.providers,db.services,db.receipts,db.recurrences,db.logs,db.settings,db.meta,async()=>{await db.providers.clear();await db.services.clear();await db.receipts.clear();await db.recurrences.clear();await db.logs.clear();if(data.settings){let restoredSettings=JSON.parse(JSON.stringify(data.settings));await db.settings.put({key:'main',value:restoredSettings});this.settings=restoredSettings}if(providers.length)await db.providers.bulkAdd(providers);if(services.length)await db.services.bulkAdd(services);if(receipts.length)await db.receipts.bulkAdd(receipts);if(recurrences.length)await db.recurrences.bulkAdd(recurrences);if(logs.length)await db.logs.bulkAdd(logs.map(l=>{let x={...l};delete x.id;return x}).slice(0,200));let restoredMax=receipts.reduce((m,r)=>r.source==='Excel legado'?m:Math.max(m,Number(String(r.number||'').replace(/\D/g,'')||0)),0),safeSeq=Math.max(Number(data.receiptSeq||0),restoredMax);await db.meta.put({key:'receiptSeq',value:safeSeq})});await this.logAction('restore','Backup restaurado','Dados restaurados por arquivo JSON.');await this.load();this.toast('Backup restaurado com sucesso!')}catch(e){console.error(e);this.toast('Não foi possível restaurar o backup: '+e.message,'error')}},async clearAllInternal(){await db.transaction('rw',db.providers,db.services,db.receipts,db.recurrences,db.logs,async()=>{await db.providers.clear();await db.services.clear();await db.receipts.clear();await db.recurrences.clear();await db.logs.clear()});await this.load()},async clearAll(){this.openClearAllModal()},readLogo(e){let f=e.target.files[0];if(!f)return;let r=new FileReader();r.onload=()=>this.settings.logo=r.result;r.readAsDataURL(f)},
  receiptMaxNumber(){return this.receipts.reduce((max,r)=>r.source==='Excel legado'?max:Math.max(max,Number(String(r.number||'').replace(/\D/g,'')||0)),0)},nextReceiptNumber(){return Math.max(Number(this.receiptSeqValue||0),this.receiptMaxNumber())+1},receiptSequenceStatus(){return Number(this.receiptSeqValue||0)<this.receiptMaxNumber()?'Sincronizar':'Sequência OK'},async saveReceiptSequence(){if(this.isViewer)return;let target=Number(this.receiptSeqInput||0), max=this.receiptMaxNumber();if(!target||target<1)return this.toast('Informe um próximo número válido.','warning');if(target<=max)return this.toast('O número precisa ser maior que o último emitido: '+String(max).padStart(6,'0')+'.','warning');await db.meta.put({key:'receiptSeq',value:target-1});this

===== settings @ 6491023 =====
+' • '+r.providerName):('ID '+id));await this.load()},
  printReport(){document.body.classList.add('printing-report');document.getElementById('receiptPrint').classList.add('hidden');document.getElementById('annualReportPrint').classList.add('hidden');document.getElementById('reportPrint').classList.remove('hidden');setTimeout(()=>{window.print();document.body.classList.remove('printing-report');document.getElementById('receiptPrint').classList.remove('hidden');document.getElementById('annualReportPrint').classList.add('hidden');document.getElementById('reportPrint').classList.add('hidden')},100)},printAnnualReport(){document.body.classList.add('printing-annual');let rp=document.getElementById('receiptPrint'),mp=document.getElementById('reportPrint'),ap=document.getElementById('annualReportPrint');rp.classList.add('hidden');mp.classList.add('hidden');ap.classList.remove('hidden');ap.style.display='block';ap.style.visibility='visible';setTimeout(()=>{window.print();document.body.classList.remove('printing-annual');rp.classList.remove('hidden');ap.classList.add('hidden');ap.style.display='';ap.style.visibility='';mp.classList.add('hidden')},180)},csv(){let rows=[['Recibo nº','Data do serviço','Data do pagamento','Prestador','Categoria','Descrição','Valor do serviço','Valor deste pagamento','Status','Forma']].concat(this.reportDetailRows.map(s=>[s.receiptNumber,s.serviceDate,s.paymentDate||'',this.providerName(s.providerId),s.category,s.description,s.amount,s.reportValue||0,s.status,s.paymentMethod||'']));let csv=rows.map(r=>r.map(v=>'"'+String(v??'').replaceAll('"','""')+'"').join(';')).join('\n');this.download('relatorio-prestadores-recibocondo.csv',csv,'text/csv;charset=utf-8')},
  async saveSettings(){let plainSettings=JSON.parse(JSON.stringify(this.settings));await db.settings.put({key:'main',value:plainSettings});await this.logAction('settings','Configurações salvas','Dados do condomínio atualizados.');this.toast('Configurações salvas')},async savePasswords(){if(this.isViewer)return;let cur=JSON.parse(localStorage.getItem('rc_passwords')||'{"admin":"123456","viewer":"123456"}');if(this.passwords.admin)cur.admin=this.passwords.admin;if(this.passwords.viewer)cur.viewer=this.passwords.viewer;localStorage.setItem('rc_passwords',JSON.stringify(cur));await this.logAction('settings','Senhas atualizadas','Senhas locais do app foram atualizadas.');this.passwords={admin:'',viewer:''};this.toast('Senhas atualizadas')},async backup(){let now=new Date().toISOString();let seqMeta=await db.meta.get('receiptSeq');let data={settings:this.settings,providers:this.providers,services:this.services,receipts:this.receipts,recurrences:this.recurrences,logs:this.logs,receiptSeq:Number(seqMeta?.value||this.receiptMaxNumber()||0),backupStats:this.backupStats(),exportedAt:now};this.download('backup-recibocondo-prestadores.json',JSON.stringify(data,null,2),'application/json');this.backupLast=now;localStorage.setItem('rc_last_backup',now);await this.logAction('backup','Backup exportado','Backup JSON gerado com '+data.backupStats.records+' registros.')},async restore(ev){let f=ev.target.files[0];if(!f)return;try{let data=JSON.parse(await f.text());if(!data||typeof data!=='object')throw new Error('Backup inválido');let providers=Array.isArray(data.providers)?data.providers:[],services=Array.isArray(data.services)?data.services:[],receipts=Array.isArray(data.receipts)?data.receipts:[],recurrences=Array.isArray(data.recurrences)?data.recurrences:[],logs=Array.isArray(data.logs)?data.logs:[];await db.transaction('rw',db.providers,db.services,db.receipts,db.recurrences,db.logs,db.settings,db.meta,async()=>{await db.providers.clear();await db.services.clear();await db.receipts.clear();await db.recurrences.clear();await db.logs.clear();if(data.settings){let restoredSettings=JSON.parse(JSON.stringify(data.settings));await db.settings.put({key:'main',value:restoredSettings});this.settings=restoredSettings}if(providers.length)await db.providers.bulkAdd(providers);if(services.length)await db.services.bulkAdd(services);if(receipts.length)await db.receipts.bulkAdd(receipts);if(recurrences.length)await db.recurrences.bulkAdd(recurrences);if(logs.length)await db.logs.bulkAdd(logs.map(l=>{let x={...l};delete x.id;return x}).slice(0,200));let restoredMax=receipts.reduce((m,r)=>r.source==='Excel legado'?m:Math.max(m,Number(String(r.number||'').replace(/\D/g,'')||0)),0),safeSeq=Math.max(Number(data.receiptSeq||0),restoredMax);await db.meta.put({key:'receiptSeq',value:safeSeq})});await this.logAction('restore','Backup restaurado','Dados restaurados por arquivo JSON.');await this.load();this.toast('Backup restaurado com sucesso!')}catch(e){console.error(e);this.toast('Não foi possível restaurar o backup: '+e.message,'error')}},async clearAllInternal(){await db.transaction('rw',db.providers,db.services,db.receipts,db.recurrences,db.logs,async()=>{await db.providers.clear();await db.services.clear();await db.receipts.clear();await db.recurrences.clear();await db.logs.clear()});await this.load()},async clearAll(){this.openClearAllModal()},readLogo(e){let f=e.target.files[0];if(!f)return;let r=new FileReader();r.onload=()=>this.settings.logo=r.result;r.readAsDataURL(f)},
  receiptMaxNumber(){return this.receipts.reduce((max,r)=>r.source==='Excel legado'?max:Math.max(max,Number(String(r.number||'').replace(/\D/g,'')||0)),0)},nextReceiptNumber(){return Math.max(Number(this.receiptSeqValue||0),this.receiptMaxNumber())+1},receiptSequenceStatus(){return Number(this.receiptSeqValue||0)<this.receiptMaxNumber()?'Sincronizar':'Sequência OK'},async saveReceiptSequence(){if(this.isViewer)return;let target=Number(this.receiptSeqInput||0), max=this.receiptMaxNumber();if(!target||target<1)return this.toast('Informe um próximo número válido.','warning');if(target<=max)return this.toast('O número precisa ser maior que o último emitido: '+String(max).padStart(6,'0')+'.','warning');await db.meta.put({key:'receiptSeq',value:target-1});this.receiptSeqValue=tar

===== settings @ 6491092 =====
){document.body.classList.add('printing-report');document.getElementById('receiptPrint').classList.add('hidden');document.getElementById('annualReportPrint').classList.add('hidden');document.getElementById('reportPrint').classList.remove('hidden');setTimeout(()=>{window.print();document.body.classList.remove('printing-report');document.getElementById('receiptPrint').classList.remove('hidden');document.getElementById('annualReportPrint').classList.add('hidden');document.getElementById('reportPrint').classList.add('hidden')},100)},printAnnualReport(){document.body.classList.add('printing-annual');let rp=document.getElementById('receiptPrint'),mp=document.getElementById('reportPrint'),ap=document.getElementById('annualReportPrint');rp.classList.add('hidden');mp.classList.add('hidden');ap.classList.remove('hidden');ap.style.display='block';ap.style.visibility='visible';setTimeout(()=>{window.print();document.body.classList.remove('printing-annual');rp.classList.remove('hidden');ap.classList.add('hidden');ap.style.display='';ap.style.visibility='';mp.classList.add('hidden')},180)},csv(){let rows=[['Recibo nº','Data do serviço','Data do pagamento','Prestador','Categoria','Descrição','Valor do serviço','Valor deste pagamento','Status','Forma']].concat(this.reportDetailRows.map(s=>[s.receiptNumber,s.serviceDate,s.paymentDate||'',this.providerName(s.providerId),s.category,s.description,s.amount,s.reportValue||0,s.status,s.paymentMethod||'']));let csv=rows.map(r=>r.map(v=>'"'+String(v??'').replaceAll('"','""')+'"').join(';')).join('\n');this.download('relatorio-prestadores-recibocondo.csv',csv,'text/csv;charset=utf-8')},
  async saveSettings(){let plainSettings=JSON.parse(JSON.stringify(this.settings));await db.settings.put({key:'main',value:plainSettings});await this.logAction('settings','Configurações salvas','Dados do condomínio atualizados.');this.toast('Configurações salvas')},async savePasswords(){if(this.isViewer)return;let cur=JSON.parse(localStorage.getItem('rc_passwords')||'{"admin":"123456","viewer":"123456"}');if(this.passwords.admin)cur.admin=this.passwords.admin;if(this.passwords.viewer)cur.viewer=this.passwords.viewer;localStorage.setItem('rc_passwords',JSON.stringify(cur));await this.logAction('settings','Senhas atualizadas','Senhas locais do app foram atualizadas.');this.passwords={admin:'',viewer:''};this.toast('Senhas atualizadas')},async backup(){let now=new Date().toISOString();let seqMeta=await db.meta.get('receiptSeq');let data={settings:this.settings,providers:this.providers,services:this.services,receipts:this.receipts,recurrences:this.recurrences,logs:this.logs,receiptSeq:Number(seqMeta?.value||this.receiptMaxNumber()||0),backupStats:this.backupStats(),exportedAt:now};this.download('backup-recibocondo-prestadores.json',JSON.stringify(data,null,2),'application/json');this.backupLast=now;localStorage.setItem('rc_last_backup',now);await this.logAction('backup','Backup exportado','Backup JSON gerado com '+data.backupStats.records+' registros.')},async restore(ev){let f=ev.target.files[0];if(!f)return;try{let data=JSON.parse(await f.text());if(!data||typeof data!=='object')throw new Error('Backup inválido');let providers=Array.isArray(data.providers)?data.providers:[],services=Array.isArray(data.services)?data.services:[],receipts=Array.isArray(data.receipts)?data.receipts:[],recurrences=Array.isArray(data.recurrences)?data.recurrences:[],logs=Array.isArray(data.logs)?data.logs:[];await db.transaction('rw',db.providers,db.services,db.receipts,db.recurrences,db.logs,db.settings,db.meta,async()=>{await db.providers.clear();await db.services.clear();await db.receipts.clear();await db.recurrences.clear();await db.logs.clear();if(data.settings){let restoredSettings=JSON.parse(JSON.stringify(data.settings));await db.settings.put({key:'main',value:restoredSettings});this.settings=restoredSettings}if(providers.length)await db.providers.bulkAdd(providers);if(services.length)await db.services.bulkAdd(services);if(receipts.length)await db.receipts.bulkAdd(receipts);if(recurrences.length)await db.recurrences.bulkAdd(recurrences);if(logs.length)await db.logs.bulkAdd(logs.map(l=>{let x={...l};delete x.id;return x}).slice(0,200));let restoredMax=receipts.reduce((m,r)=>r.source==='Excel legado'?m:Math.max(m,Number(String(r.number||'').replace(/\D/g,'')||0)),0),safeSeq=Math.max(Number(data.receiptSeq||0),restoredMax);await db.meta.put({key:'receiptSeq',value:safeSeq})});await this.logAction('restore','Backup restaurado','Dados restaurados por arquivo JSON.');await this.load();this.toast('Backup restaurado com sucesso!')}catch(e){console.error(e);this.toast('Não foi possível restaurar o backup: '+e.message,'error')}},async clearAllInternal(){await db.transaction('rw',db.providers,db.services,db.receipts,db.recurrences,db.logs,async()=>{await db.providers.clear();await db.services.clear();await db.receipts.clear();await db.recurrences.clear();await db.logs.clear()});await this.load()},async clearAll(){this.openClearAllModal()},readLogo(e){let f=e.target.files[0];if(!f)return;let r=new FileReader();r.onload=()=>this.settings.logo=r.result;r.readAsDataURL(f)},
  receiptMaxNumber(){return this.receipts.reduce((max,r)=>r.source==='Excel legado'?max:Math.max(max,Number(String(r.number||'').replace(/\D/g,'')||0)),0)},nextReceiptNumber(){return Math.max(Number(this.receiptSeqValue||0),this.receiptMaxNumber())+1},receiptSequenceStatus(){return Number(this.receiptSeqValue||0)<this.receiptMaxNumber()?'Sincronizar':'Sequência OK'},async saveReceiptSequence(){if(this.isViewer)return;let target=Number(this.receiptSeqInput||0), max=this.receiptMaxNumber();if(!target||target<1)return this.toast('Informe um próximo número válido.','warning');if(target<=max)return this.toast('O número precisa ser maior que o último emitido: '+String(max).padStart(6,'0')+'.','warning');await db.meta.put({key:'receiptSeq',value:target-1});this.receiptSeqValue=target-1;await this.logAction('settings','Sequência de recibos ajustada'

===== settings @ 6491532 =====
.classList.add('hidden');document.getElementById('reportPrint').classList.add('hidden')},100)},printAnnualReport(){document.body.classList.add('printing-annual');let rp=document.getElementById('receiptPrint'),mp=document.getElementById('reportPrint'),ap=document.getElementById('annualReportPrint');rp.classList.add('hidden');mp.classList.add('hidden');ap.classList.remove('hidden');ap.style.display='block';ap.style.visibility='visible';setTimeout(()=>{window.print();document.body.classList.remove('printing-annual');rp.classList.remove('hidden');ap.classList.add('hidden');ap.style.display='';ap.style.visibility='';mp.classList.add('hidden')},180)},csv(){let rows=[['Recibo nº','Data do serviço','Data do pagamento','Prestador','Categoria','Descrição','Valor do serviço','Valor deste pagamento','Status','Forma']].concat(this.reportDetailRows.map(s=>[s.receiptNumber,s.serviceDate,s.paymentDate||'',this.providerName(s.providerId),s.category,s.description,s.amount,s.reportValue||0,s.status,s.paymentMethod||'']));let csv=rows.map(r=>r.map(v=>'"'+String(v??'').replaceAll('"','""')+'"').join(';')).join('\n');this.download('relatorio-prestadores-recibocondo.csv',csv,'text/csv;charset=utf-8')},
  async saveSettings(){let plainSettings=JSON.parse(JSON.stringify(this.settings));await db.settings.put({key:'main',value:plainSettings});await this.logAction('settings','Configurações salvas','Dados do condomínio atualizados.');this.toast('Configurações salvas')},async savePasswords(){if(this.isViewer)return;let cur=JSON.parse(localStorage.getItem('rc_passwords')||'{"admin":"123456","viewer":"123456"}');if(this.passwords.admin)cur.admin=this.passwords.admin;if(this.passwords.viewer)cur.viewer=this.passwords.viewer;localStorage.setItem('rc_passwords',JSON.stringify(cur));await this.logAction('settings','Senhas atualizadas','Senhas locais do app foram atualizadas.');this.passwords={admin:'',viewer:''};this.toast('Senhas atualizadas')},async backup(){let now=new Date().toISOString();let seqMeta=await db.meta.get('receiptSeq');let data={settings:this.settings,providers:this.providers,services:this.services,receipts:this.receipts,recurrences:this.recurrences,logs:this.logs,receiptSeq:Number(seqMeta?.value||this.receiptMaxNumber()||0),backupStats:this.backupStats(),exportedAt:now};this.download('backup-recibocondo-prestadores.json',JSON.stringify(data,null,2),'application/json');this.backupLast=now;localStorage.setItem('rc_last_backup',now);await this.logAction('backup','Backup exportado','Backup JSON gerado com '+data.backupStats.records+' registros.')},async restore(ev){let f=ev.target.files[0];if(!f)return;try{let data=JSON.parse(await f.text());if(!data||typeof data!=='object')throw new Error('Backup inválido');let providers=Array.isArray(data.providers)?data.providers:[],services=Array.isArray(data.services)?data.services:[],receipts=Array.isArray(data.receipts)?data.receipts:[],recurrences=Array.isArray(data.recurrences)?data.recurrences:[],logs=Array.isArray(data.logs)?data.logs:[];await db.transaction('rw',db.providers,db.services,db.receipts,db.recurrences,db.logs,db.settings,db.meta,async()=>{await db.providers.clear();await db.services.clear();await db.receipts.clear();await db.recurrences.clear();await db.logs.clear();if(data.settings){let restoredSettings=JSON.parse(JSON.stringify(data.settings));await db.settings.put({key:'main',value:restoredSettings});this.settings=restoredSettings}if(providers.length)await db.providers.bulkAdd(providers);if(services.length)await db.services.bulkAdd(services);if(receipts.length)await db.receipts.bulkAdd(receipts);if(recurrences.length)await db.recurrences.bulkAdd(recurrences);if(logs.length)await db.logs.bulkAdd(logs.map(l=>{let x={...l};delete x.id;return x}).slice(0,200));let restoredMax=receipts.reduce((m,r)=>r.source==='Excel legado'?m:Math.max(m,Number(String(r.number||'').replace(/\D/g,'')||0)),0),safeSeq=Math.max(Number(data.receiptSeq||0),restoredMax);await db.meta.put({key:'receiptSeq',value:safeSeq})});await this.logAction('restore','Backup restaurado','Dados restaurados por arquivo JSON.');await this.load();this.toast('Backup restaurado com sucesso!')}catch(e){console.error(e);this.toast('Não foi possível restaurar o backup: '+e.message,'error')}},async clearAllInternal(){await db.transaction('rw',db.providers,db.services,db.receipts,db.recurrences,db.logs,async()=>{await db.providers.clear();await db.services.clear();await db.receipts.clear();await db.recurrences.clear();await db.logs.clear()});await this.load()},async clearAll(){this.openClearAllModal()},readLogo(e){let f=e.target.files[0];if(!f)return;let r=new FileReader();r.onload=()=>this.settings.logo=r.result;r.readAsDataURL(f)},
  receiptMaxNumber(){return this.receipts.reduce((max,r)=>r.source==='Excel legado'?max:Math.max(max,Number(String(r.number||'').replace(/\D/g,'')||0)),0)},nextReceiptNumber(){return Math.max(Number(this.receiptSeqValue||0),this.receiptMaxNumber())+1},receiptSequenceStatus(){return Number(this.receiptSeqValue||0)<this.receiptMaxNumber()?'Sincronizar':'Sequência OK'},async saveReceiptSequence(){if(this.isViewer)return;let target=Number(this.receiptSeqInput||0), max=this.receiptMaxNumber();if(!target||target<1)return this.toast('Informe um próximo número válido.','warning');if(target<=max)return this.toast('O número precisa ser maior que o último emitido: '+String(max).padStart(6,'0')+'.','warning');await db.meta.put({key:'receiptSeq',value:target-1});this.receiptSeqValue=target-1;await this.logAction('settings','Sequência de recibos ajustada','Próximo recibo definido para nº '+String(target).padStart(6,'0'));this.toast('Sequência atualizada. Próximo recibo: '+String(target).padStart(6,'0'));await this.load()},
  topPaidNames(rows){let map={};rows.forEach(s=>{let v=Number(s.paidValue||0);if(v<=0)return;let name=this.providerName(s.providerId);map[name]=(map[name]||0)+v});return Object.entries(map).sort((a,b)=>b[1]-a[1]).slice(0,3).map(([name,total])=>name+' • '+this.money(to

===== settings @ 6491778 =====
nt'),ap=document.getElementById('annualReportPrint');rp.classList.add('hidden');mp.classList.add('hidden');ap.classList.remove('hidden');ap.style.display='block';ap.style.visibility='visible';setTimeout(()=>{window.print();document.body.classList.remove('printing-annual');rp.classList.remove('hidden');ap.classList.add('hidden');ap.style.display='';ap.style.visibility='';mp.classList.add('hidden')},180)},csv(){let rows=[['Recibo nº','Data do serviço','Data do pagamento','Prestador','Categoria','Descrição','Valor do serviço','Valor deste pagamento','Status','Forma']].concat(this.reportDetailRows.map(s=>[s.receiptNumber,s.serviceDate,s.paymentDate||'',this.providerName(s.providerId),s.category,s.description,s.amount,s.reportValue||0,s.status,s.paymentMethod||'']));let csv=rows.map(r=>r.map(v=>'"'+String(v??'').replaceAll('"','""')+'"').join(';')).join('\n');this.download('relatorio-prestadores-recibocondo.csv',csv,'text/csv;charset=utf-8')},
  async saveSettings(){let plainSettings=JSON.parse(JSON.stringify(this.settings));await db.settings.put({key:'main',value:plainSettings});await this.logAction('settings','Configurações salvas','Dados do condomínio atualizados.');this.toast('Configurações salvas')},async savePasswords(){if(this.isViewer)return;let cur=JSON.parse(localStorage.getItem('rc_passwords')||'{"admin":"123456","viewer":"123456"}');if(this.passwords.admin)cur.admin=this.passwords.admin;if(this.passwords.viewer)cur.viewer=this.passwords.viewer;localStorage.setItem('rc_passwords',JSON.stringify(cur));await this.logAction('settings','Senhas atualizadas','Senhas locais do app foram atualizadas.');this.passwords={admin:'',viewer:''};this.toast('Senhas atualizadas')},async backup(){let now=new Date().toISOString();let seqMeta=await db.meta.get('receiptSeq');let data={settings:this.settings,providers:this.providers,services:this.services,receipts:this.receipts,recurrences:this.recurrences,logs:this.logs,receiptSeq:Number(seqMeta?.value||this.receiptMaxNumber()||0),backupStats:this.backupStats(),exportedAt:now};this.download('backup-recibocondo-prestadores.json',JSON.stringify(data,null,2),'application/json');this.backupLast=now;localStorage.setItem('rc_last_backup',now);await this.logAction('backup','Backup exportado','Backup JSON gerado com '+data.backupStats.records+' registros.')},async restore(ev){let f=ev.target.files[0];if(!f)return;try{let data=JSON.parse(await f.text());if(!data||typeof data!=='object')throw new Error('Backup inválido');let providers=Array.isArray(data.providers)?data.providers:[],services=Array.isArray(data.services)?data.services:[],receipts=Array.isArray(data.receipts)?data.receipts:[],recurrences=Array.isArray(data.recurrences)?data.recurrences:[],logs=Array.isArray(data.logs)?data.logs:[];await db.transaction('rw',db.providers,db.services,db.receipts,db.recurrences,db.logs,db.settings,db.meta,async()=>{await db.providers.clear();await db.services.clear();await db.receipts.clear();await db.recurrences.clear();await db.logs.clear();if(data.settings){let restoredSettings=JSON.parse(JSON.stringify(data.settings));await db.settings.put({key:'main',value:restoredSettings});this.settings=restoredSettings}if(providers.length)await db.providers.bulkAdd(providers);if(services.length)await db.services.bulkAdd(services);if(receipts.length)await db.receipts.bulkAdd(receipts);if(recurrences.length)await db.recurrences.bulkAdd(recurrences);if(logs.length)await db.logs.bulkAdd(logs.map(l=>{let x={...l};delete x.id;return x}).slice(0,200));let restoredMax=receipts.reduce((m,r)=>r.source==='Excel legado'?m:Math.max(m,Number(String(r.number||'').replace(/\D/g,'')||0)),0),safeSeq=Math.max(Number(data.receiptSeq||0),restoredMax);await db.meta.put({key:'receiptSeq',value:safeSeq})});await this.logAction('restore','Backup restaurado','Dados restaurados por arquivo JSON.');await this.load();this.toast('Backup restaurado com sucesso!')}catch(e){console.error(e);this.toast('Não foi possível restaurar o backup: '+e.message,'error')}},async clearAllInternal(){await db.transaction('rw',db.providers,db.services,db.receipts,db.recurrences,db.logs,async()=>{await db.providers.clear();await db.services.clear();await db.receipts.clear();await db.recurrences.clear();await db.logs.clear()});await this.load()},async clearAll(){this.openClearAllModal()},readLogo(e){let f=e.target.files[0];if(!f)return;let r=new FileReader();r.onload=()=>this.settings.logo=r.result;r.readAsDataURL(f)},
  receiptMaxNumber(){return this.receipts.reduce((max,r)=>r.source==='Excel legado'?max:Math.max(max,Number(String(r.number||'').replace(/\D/g,'')||0)),0)},nextReceiptNumber(){return Math.max(Number(this.receiptSeqValue||0),this.receiptMaxNumber())+1},receiptSequenceStatus(){return Number(this.receiptSeqValue||0)<this.receiptMaxNumber()?'Sincronizar':'Sequência OK'},async saveReceiptSequence(){if(this.isViewer)return;let target=Number(this.receiptSeqInput||0), max=this.receiptMaxNumber();if(!target||target<1)return this.toast('Informe um próximo número válido.','warning');if(target<=max)return this.toast('O número precisa ser maior que o último emitido: '+String(max).padStart(6,'0')+'.','warning');await db.meta.put({key:'receiptSeq',value:target-1});this.receiptSeqValue=target-1;await this.logAction('settings','Sequência de recibos ajustada','Próximo recibo definido para nº '+String(target).padStart(6,'0'));this.toast('Sequência atualizada. Próximo recibo: '+String(target).padStart(6,'0'));await this.load()},
  topPaidNames(rows){let map={};rows.forEach(s=>{let v=Number(s.paidValue||0);if(v<=0)return;let name=this.providerName(s.providerId);map[name]=(map[name]||0)+v});return Object.entries(map).sort((a,b)=>b[1]-a[1]).slice(0,3).map(([name,total])=>name+' • '+this.money(total))},topOpenNames(rows){let map={};rows.forEach(s=>{let v=this.balance(s);if(v<=0)return;let name=this.providerName(s.providerId);map[name]=(map[name]||0)+v});return Object.entries(map).sort((a,b)=>b[1]-a[1]).slice(0,3).map(([name,total])=>name

===== settings @ 6491792 =====
nt.getElementById('annualReportPrint');rp.classList.add('hidden');mp.classList.add('hidden');ap.classList.remove('hidden');ap.style.display='block';ap.style.visibility='visible';setTimeout(()=>{window.print();document.body.classList.remove('printing-annual');rp.classList.remove('hidden');ap.classList.add('hidden');ap.style.display='';ap.style.visibility='';mp.classList.add('hidden')},180)},csv(){let rows=[['Recibo nº','Data do serviço','Data do pagamento','Prestador','Categoria','Descrição','Valor do serviço','Valor deste pagamento','Status','Forma']].concat(this.reportDetailRows.map(s=>[s.receiptNumber,s.serviceDate,s.paymentDate||'',this.providerName(s.providerId),s.category,s.description,s.amount,s.reportValue||0,s.status,s.paymentMethod||'']));let csv=rows.map(r=>r.map(v=>'"'+String(v??'').replaceAll('"','""')+'"').join(';')).join('\n');this.download('relatorio-prestadores-recibocondo.csv',csv,'text/csv;charset=utf-8')},
  async saveSettings(){let plainSettings=JSON.parse(JSON.stringify(this.settings));await db.settings.put({key:'main',value:plainSettings});await this.logAction('settings','Configurações salvas','Dados do condomínio atualizados.');this.toast('Configurações salvas')},async savePasswords(){if(this.isViewer)return;let cur=JSON.parse(localStorage.getItem('rc_passwords')||'{"admin":"123456","viewer":"123456"}');if(this.passwords.admin)cur.admin=this.passwords.admin;if(this.passwords.viewer)cur.viewer=this.passwords.viewer;localStorage.setItem('rc_passwords',JSON.stringify(cur));await this.logAction('settings','Senhas atualizadas','Senhas locais do app foram atualizadas.');this.passwords={admin:'',viewer:''};this.toast('Senhas atualizadas')},async backup(){let now=new Date().toISOString();let seqMeta=await db.meta.get('receiptSeq');let data={settings:this.settings,providers:this.providers,services:this.services,receipts:this.receipts,recurrences:this.recurrences,logs:this.logs,receiptSeq:Number(seqMeta?.value||this.receiptMaxNumber()||0),backupStats:this.backupStats(),exportedAt:now};this.download('backup-recibocondo-prestadores.json',JSON.stringify(data,null,2),'application/json');this.backupLast=now;localStorage.setItem('rc_last_backup',now);await this.logAction('backup','Backup exportado','Backup JSON gerado com '+data.backupStats.records+' registros.')},async restore(ev){let f=ev.target.files[0];if(!f)return;try{let data=JSON.parse(await f.text());if(!data||typeof data!=='object')throw new Error('Backup inválido');let providers=Array.isArray(data.providers)?data.providers:[],services=Array.isArray(data.services)?data.services:[],receipts=Array.isArray(data.receipts)?data.receipts:[],recurrences=Array.isArray(data.recurrences)?data.recurrences:[],logs=Array.isArray(data.logs)?data.logs:[];await db.transaction('rw',db.providers,db.services,db.receipts,db.recurrences,db.logs,db.settings,db.meta,async()=>{await db.providers.clear();await db.services.clear();await db.receipts.clear();await db.recurrences.clear();await db.logs.clear();if(data.settings){let restoredSettings=JSON.parse(JSON.stringify(data.settings));await db.settings.put({key:'main',value:restoredSettings});this.settings=restoredSettings}if(providers.length)await db.providers.bulkAdd(providers);if(services.length)await db.services.bulkAdd(services);if(receipts.length)await db.receipts.bulkAdd(receipts);if(recurrences.length)await db.recurrences.bulkAdd(recurrences);if(logs.length)await db.logs.bulkAdd(logs.map(l=>{let x={...l};delete x.id;return x}).slice(0,200));let restoredMax=receipts.reduce((m,r)=>r.source==='Excel legado'?m:Math.max(m,Number(String(r.number||'').replace(/\D/g,'')||0)),0),safeSeq=Math.max(Number(data.receiptSeq||0),restoredMax);await db.meta.put({key:'receiptSeq',value:safeSeq})});await this.logAction('restore','Backup restaurado','Dados restaurados por arquivo JSON.');await this.load();this.toast('Backup restaurado com sucesso!')}catch(e){console.error(e);this.toast('Não foi possível restaurar o backup: '+e.message,'error')}},async clearAllInternal(){await db.transaction('rw',db.providers,db.services,db.receipts,db.recurrences,db.logs,async()=>{await db.providers.clear();await db.services.clear();await db.receipts.clear();await db.recurrences.clear();await db.logs.clear()});await this.load()},async clearAll(){this.openClearAllModal()},readLogo(e){let f=e.target.files[0];if(!f)return;let r=new FileReader();r.onload=()=>this.settings.logo=r.result;r.readAsDataURL(f)},
  receiptMaxNumber(){return this.receipts.reduce((max,r)=>r.source==='Excel legado'?max:Math.max(max,Number(String(r.number||'').replace(/\D/g,'')||0)),0)},nextReceiptNumber(){return Math.max(Number(this.receiptSeqValue||0),this.receiptMaxNumber())+1},receiptSequenceStatus(){return Number(this.receiptSeqValue||0)<this.receiptMaxNumber()?'Sincronizar':'Sequência OK'},async saveReceiptSequence(){if(this.isViewer)return;let target=Number(this.receiptSeqInput||0), max=this.receiptMaxNumber();if(!target||target<1)return this.toast('Informe um próximo número válido.','warning');if(target<=max)return this.toast('O número precisa ser maior que o último emitido: '+String(max).padStart(6,'0')+'.','warning');await db.meta.put({key:'receiptSeq',value:target-1});this.receiptSeqValue=target-1;await this.logAction('settings','Sequência de recibos ajustada','Próximo recibo definido para nº '+String(target).padStart(6,'0'));this.toast('Sequência atualizada. Próximo recibo: '+String(target).padStart(6,'0'));await this.load()},
  topPaidNames(rows){let map={};rows.forEach(s=>{let v=Number(s.paidValue||0);if(v<=0)return;let name=this.providerName(s.providerId);map[name]=(map[name]||0)+v});return Object.entries(map).sort((a,b)=>b[1]-a[1]).slice(0,3).map(([name,total])=>name+' • '+this.money(total))},topOpenNames(rows){let map={};rows.forEach(s=>{let v=this.balance(s);if(v<=0)return;let name=this.providerName(s.providerId);map[name]=(map[name]||0)+v});return Object.entries(map).sort((a,b)=>b[1]-a[1]).slice(0,3).map(([name,total])=>name+' • '+this.mo

===== settings @ 6492836 =====
{key:'main',value:plainSettings});await this.logAction('settings','Configurações salvas','Dados do condomínio atualizados.');this.toast('Configurações salvas')},async savePasswords(){if(this.isViewer)return;let cur=JSON.parse(localStorage.getItem('rc_passwords')||'{"admin":"123456","viewer":"123456"}');if(this.passwords.admin)cur.admin=this.passwords.admin;if(this.passwords.viewer)cur.viewer=this.passwords.viewer;localStorage.setItem('rc_passwords',JSON.stringify(cur));await this.logAction('settings','Senhas atualizadas','Senhas locais do app foram atualizadas.');this.passwords={admin:'',viewer:''};this.toast('Senhas atualizadas')},async backup(){let now=new Date().toISOString();let seqMeta=await db.meta.get('receiptSeq');let data={settings:this.settings,providers:this.providers,services:this.services,receipts:this.receipts,recurrences:this.recurrences,logs:this.logs,receiptSeq:Number(seqMeta?.value||this.receiptMaxNumber()||0),backupStats:this.backupStats(),exportedAt:now};this.download('backup-recibocondo-prestadores.json',JSON.stringify(data,null,2),'application/json');this.backupLast=now;localStorage.setItem('rc_last_backup',now);await this.logAction('backup','Backup exportado','Backup JSON gerado com '+data.backupStats.records+' registros.')},async restore(ev){let f=ev.target.files[0];if(!f)return;try{let data=JSON.parse(await f.text());if(!data||typeof data!=='object')throw new Error('Backup inválido');let providers=Array.isArray(data.providers)?data.providers:[],services=Array.isArray(data.services)?data.services:[],receipts=Array.isArray(data.receipts)?data.receipts:[],recurrences=Array.isArray(data.recurrences)?data.recurrences:[],logs=Array.isArray(data.logs)?data.logs:[];await db.transaction('rw',db.providers,db.services,db.receipts,db.recurrences,db.logs,db.settings,db.meta,async()=>{await db.providers.clear();await db.services.clear();await db.receipts.clear();await db.recurrences.clear();await db.logs.clear();if(data.settings){let restoredSettings=JSON.parse(JSON.stringify(data.settings));await db.settings.put({key:'main',value:restoredSettings});this.settings=restoredSettings}if(providers.length)await db.providers.bulkAdd(providers);if(services.length)await db.services.bulkAdd(services);if(receipts.length)await db.receipts.bulkAdd(receipts);if(recurrences.length)await db.recurrences.bulkAdd(recurrences);if(logs.length)await db.logs.bulkAdd(logs.map(l=>{let x={...l};delete x.id;return x}).slice(0,200));let restoredMax=receipts.reduce((m,r)=>r.source==='Excel legado'?m:Math.max(m,Number(String(r.number||'').replace(/\D/g,'')||0)),0),safeSeq=Math.max(Number(data.receiptSeq||0),restoredMax);await db.meta.put({key:'receiptSeq',value:safeSeq})});await this.logAction('restore','Backup restaurado','Dados restaurados por arquivo JSON.');await this.load();this.toast('Backup restaurado com sucesso!')}catch(e){console.error(e);this.toast('Não foi possível restaurar o backup: '+e.message,'error')}},async clearAllInternal(){await db.transaction('rw',db.providers,db.services,db.receipts,db.recurrences,db.logs,async()=>{await db.providers.clear();await db.services.clear();await db.receipts.clear();await db.recurrences.clear();await db.logs.clear()});await this.load()},async clearAll(){this.openClearAllModal()},readLogo(e){let f=e.target.files[0];if(!f)return;let r=new FileReader();r.onload=()=>this.settings.logo=r.result;r.readAsDataURL(f)},
  receiptMaxNumber(){return this.receipts.reduce((max,r)=>r.source==='Excel legado'?max:Math.max(max,Number(String(r.number||'').replace(/\D/g,'')||0)),0)},nextReceiptNumber(){return Math.max(Number(this.receiptSeqValue||0),this.receiptMaxNumber())+1},receiptSequenceStatus(){return Number(this.receiptSeqValue||0)<this.receiptMaxNumber()?'Sincronizar':'Sequência OK'},async saveReceiptSequence(){if(this.isViewer)return;let target=Number(this.receiptSeqInput||0), max=this.receiptMaxNumber();if(!target||target<1)return this.toast('Informe um próximo número válido.','warning');if(target<=max)return this.toast('O número precisa ser maior que o último emitido: '+String(max).padStart(6,'0')+'.','warning');await db.meta.put({key:'receiptSeq',value:target-1});this.receiptSeqValue=target-1;await this.logAction('settings','Sequência de recibos ajustada','Próximo recibo definido para nº '+String(target).padStart(6,'0'));this.toast('Sequência atualizada. Próximo recibo: '+String(target).padStart(6,'0'));await this.load()},
  topPaidNames(rows){let map={};rows.forEach(s=>{let v=Number(s.paidValue||0);if(v<=0)return;let name=this.providerName(s.providerId);map[name]=(map[name]||0)+v});return Object.entries(map).sort((a,b)=>b[1]-a[1]).slice(0,3).map(([name,total])=>name+' • '+this.money(total))},topOpenNames(rows){let map={};rows.forEach(s=>{let v=this.balance(s);if(v<=0)return;let name=this.providerName(s.providerId);map[name]=(map[name]||0)+v});return Object.entries(map).sort((a,b)=>b[1]-a[1]).slice(0,3).map(([name,total])=>name+' • '+this.money(total))},
  providerServices(id){return this.services.filter(s=>Number(s.providerId)===Number(id)).sort((a,b)=>String(b.serviceDate||'').localeCompare(String(a.serviceDate||'')))},providerReceipts(id){return this.receipts.filter(r=>Number(r.providerId)===Number(id)).sort((a,b)=>String(b.date||'').localeCompare(String(a.date||'')))},providerLastServiceLabel(id){let s=this.providerServices(id)[0];return s?this.fmt(s.serviceDate)+' • '+this.money(s.paidValue||s.amount):'-'},async logAction(type,title,detail){try{await db.logs.add({at:new Date().toISOString(),type,title,detail,role:this.auth?.role||'sistema'});this.logs=(await db.logs.orderBy('at').reverse().limit(30).toArray())}catch(e){console.warn('logAction',e)}},logIcon(type){return {payment:'circle-dollar-sign',receipt:'receipt-text',service:'clipboard-list',provider:'users',recurrence:'repeat-2',backup:'shield-check',restore:'rotate-ccw',danger:'triangle-alert',settings:'settings',delete:'trash-2'}[type]||'history'},fmtDateTime(d){if(!d)return'-';return new Date(d).toLoca

===== settings @ 6493001 =====
c savePasswords(){if(this.isViewer)return;let cur=JSON.parse(localStorage.getItem('rc_passwords')||'{"admin":"123456","viewer":"123456"}');if(this.passwords.admin)cur.admin=this.passwords.admin;if(this.passwords.viewer)cur.viewer=this.passwords.viewer;localStorage.setItem('rc_passwords',JSON.stringify(cur));await this.logAction('settings','Senhas atualizadas','Senhas locais do app foram atualizadas.');this.passwords={admin:'',viewer:''};this.toast('Senhas atualizadas')},async backup(){let now=new Date().toISOString();let seqMeta=await db.meta.get('receiptSeq');let data={settings:this.settings,providers:this.providers,services:this.services,receipts:this.receipts,recurrences:this.recurrences,logs:this.logs,receiptSeq:Number(seqMeta?.value||this.receiptMaxNumber()||0),backupStats:this.backupStats(),exportedAt:now};this.download('backup-recibocondo-prestadores.json',JSON.stringify(data,null,2),'application/json');this.backupLast=now;localStorage.setItem('rc_last_backup',now);await this.logAction('backup','Backup exportado','Backup JSON gerado com '+data.backupStats.records+' registros.')},async restore(ev){let f=ev.target.files[0];if(!f)return;try{let data=JSON.parse(await f.text());if(!data||typeof data!=='object')throw new Error('Backup inválido');let providers=Array.isArray(data.providers)?data.providers:[],services=Array.isArray(data.services)?data.services:[],receipts=Array.isArray(data.receipts)?data.receipts:[],recurrences=Array.isArray(data.recurrences)?data.recurrences:[],logs=Array.isArray(data.logs)?data.logs:[];await db.transaction('rw',db.providers,db.services,db.receipts,db.recurrences,db.logs,db.settings,db.meta,async()=>{await db.providers.clear();await db.services.clear();await db.receipts.clear();await db.recurrences.clear();await db.logs.clear();if(data.settings){let restoredSettings=JSON.parse(JSON.stringify(data.settings));await db.settings.put({key:'main',value:restoredSettings});this.settings=restoredSettings}if(providers.length)await db.providers.bulkAdd(providers);if(services.length)await db.services.bulkAdd(services);if(receipts.length)await db.receipts.bulkAdd(receipts);if(recurrences.length)await db.recurrences.bulkAdd(recurrences);if(logs.length)await db.logs.bulkAdd(logs.map(l=>{let x={...l};delete x.id;return x}).slice(0,200));let restoredMax=receipts.reduce((m,r)=>r.source==='Excel legado'?m:Math.max(m,Number(String(r.number||'').replace(/\D/g,'')||0)),0),safeSeq=Math.max(Number(data.receiptSeq||0),restoredMax);await db.meta.put({key:'receiptSeq',value:safeSeq})});await this.logAction('restore','Backup restaurado','Dados restaurados por arquivo JSON.');await this.load();this.toast('Backup restaurado com sucesso!')}catch(e){console.error(e);this.toast('Não foi possível restaurar o backup: '+e.message,'error')}},async clearAllInternal(){await db.transaction('rw',db.providers,db.services,db.receipts,db.recurrences,db.logs,async()=>{await db.providers.clear();await db.services.clear();await db.receipts.clear();await db.recurrences.clear();await db.logs.clear()});await this.load()},async clearAll(){this.openClearAllModal()},readLogo(e){let f=e.target.files[0];if(!f)return;let r=new FileReader();r.onload=()=>this.settings.logo=r.result;r.readAsDataURL(f)},
  receiptMaxNumber(){return this.receipts.reduce((max,r)=>r.source==='Excel legado'?max:Math.max(max,Number(String(r.number||'').replace(/\D/g,'')||0)),0)},nextReceiptNumber(){return Math.max(Number(this.receiptSeqValue||0),this.receiptMaxNumber())+1},receiptSequenceStatus(){return Number(this.receiptSeqValue||0)<this.receiptMaxNumber()?'Sincronizar':'Sequência OK'},async saveReceiptSequence(){if(this.isViewer)return;let target=Number(this.receiptSeqInput||0), max=this.receiptMaxNumber();if(!target||target<1)return this.toast('Informe um próximo número válido.','warning');if(target<=max)return this.toast('O número precisa ser maior que o último emitido: '+String(max).padStart(6,'0')+'.','warning');await db.meta.put({key:'receiptSeq',value:target-1});this.receiptSeqValue=target-1;await this.logAction('settings','Sequência de recibos ajustada','Próximo recibo definido para nº '+String(target).padStart(6,'0'));this.toast('Sequência atualizada. Próximo recibo: '+String(target).padStart(6,'0'));await this.load()},
  topPaidNames(rows){let map={};rows.forEach(s=>{let v=Number(s.paidValue||0);if(v<=0)return;let name=this.providerName(s.providerId);map[name]=(map[name]||0)+v});return Object.entries(map).sort((a,b)=>b[1]-a[1]).slice(0,3).map(([name,total])=>name+' • '+this.money(total))},topOpenNames(rows){let map={};rows.forEach(s=>{let v=this.balance(s);if(v<=0)return;let name=this.providerName(s.providerId);map[name]=(map[name]||0)+v});return Object.entries(map).sort((a,b)=>b[1]-a[1]).slice(0,3).map(([name,total])=>name+' • '+this.money(total))},
  providerServices(id){return this.services.filter(s=>Number(s.providerId)===Number(id)).sort((a,b)=>String(b.serviceDate||'').localeCompare(String(a.serviceDate||'')))},providerReceipts(id){return this.receipts.filter(r=>Number(r.providerId)===Number(id)).sort((a,b)=>String(b.date||'').localeCompare(String(a.date||'')))},providerLastServiceLabel(id){let s=this.providerServices(id)[0];return s?this.fmt(s.serviceDate)+' • '+this.money(s.paidValue||s.amount):'-'},async logAction(type,title,detail){try{await db.logs.add({at:new Date().toISOString(),type,title,detail,role:this.auth?.role||'sistema'});this.logs=(await db.logs.orderBy('at').reverse().limit(30).toArray())}catch(e){console.warn('logAction',e)}},logIcon(type){return {payment:'circle-dollar-sign',receipt:'receipt-text',service:'clipboard-list',provider:'users',recurrence:'repeat-2',backup:'shield-check',restore:'rotate-ccw',danger:'triangle-alert',settings:'settings',delete:'trash-2'}[type]||'history'},fmtDateTime(d){if(!d)return'-';return new Date(d).toLocaleString('pt-BR')},async clearLogs(){if(this.isViewer)return;await db.logs.clear();this.logs=[];this.after()},providerSpend(id){return this.services.filter(s=>Number

===== settings @ 6493063 =====
ocalStorage.getItem('rc_passwords')||'{"admin":"123456","viewer":"123456"}');if(this.passwords.admin)cur.admin=this.passwords.admin;if(this.passwords.viewer)cur.viewer=this.passwords.viewer;localStorage.setItem('rc_passwords',JSON.stringify(cur));await this.logAction('settings','Senhas atualizadas','Senhas locais do app foram atualizadas.');this.passwords={admin:'',viewer:''};this.toast('Senhas atualizadas')},async backup(){let now=new Date().toISOString();let seqMeta=await db.meta.get('receiptSeq');let data={settings:this.settings,providers:this.providers,services:this.services,receipts:this.receipts,recurrences:this.recurrences,logs:this.logs,receiptSeq:Number(seqMeta?.value||this.receiptMaxNumber()||0),backupStats:this.backupStats(),exportedAt:now};this.download('backup-recibocondo-prestadores.json',JSON.stringify(data,null,2),'application/json');this.backupLast=now;localStorage.setItem('rc_last_backup',now);await this.logAction('backup','Backup exportado','Backup JSON gerado com '+data.backupStats.records+' registros.')},async restore(ev){let f=ev.target.files[0];if(!f)return;try{let data=JSON.parse(await f.text());if(!data||typeof data!=='object')throw new Error('Backup inválido');let providers=Array.isArray(data.providers)?data.providers:[],services=Array.isArray(data.services)?data.services:[],receipts=Array.isArray(data.receipts)?data.receipts:[],recurrences=Array.isArray(data.recurrences)?data.recurrences:[],logs=Array.isArray(data.logs)?data.logs:[];await db.transaction('rw',db.providers,db.services,db.receipts,db.recurrences,db.logs,db.settings,db.meta,async()=>{await db.providers.clear();await db.services.clear();await db.receipts.clear();await db.recurrences.clear();await db.logs.clear();if(data.settings){let restoredSettings=JSON.parse(JSON.stringify(data.settings));await db.settings.put({key:'main',value:restoredSettings});this.settings=restoredSettings}if(providers.length)await db.providers.bulkAdd(providers);if(services.length)await db.services.bulkAdd(services);if(receipts.length)await db.receipts.bulkAdd(receipts);if(recurrences.length)await db.recurrences.bulkAdd(recurrences);if(logs.length)await db.logs.bulkAdd(logs.map(l=>{let x={...l};delete x.id;return x}).slice(0,200));let restoredMax=receipts.reduce((m,r)=>r.source==='Excel legado'?m:Math.max(m,Number(String(r.number||'').replace(/\D/g,'')||0)),0),safeSeq=Math.max(Number(data.receiptSeq||0),restoredMax);await db.meta.put({key:'receiptSeq',value:safeSeq})});await this.logAction('restore','Backup restaurado','Dados restaurados por arquivo JSON.');await this.load();this.toast('Backup restaurado com sucesso!')}catch(e){console.error(e);this.toast('Não foi possível restaurar o backup: '+e.message,'error')}},async clearAllInternal(){await db.transaction('rw',db.providers,db.services,db.receipts,db.recurrences,db.logs,async()=>{await db.providers.clear();await db.services.clear();await db.receipts.clear();await db.recurrences.clear();await db.logs.clear()});await this.load()},async clearAll(){this.openClearAllModal()},readLogo(e){let f=e.target.files[0];if(!f)return;let r=new FileReader();r.onload=()=>this.settings.logo=r.result;r.readAsDataURL(f)},
  receiptMaxNumber(){return this.receipts.reduce((max,r)=>r.source==='Excel legado'?max:Math.max(max,Number(String(r.number||'').replace(/\D/g,'')||0)),0)},nextReceiptNumber(){return Math.max(Number(this.receiptSeqValue||0),this.receiptMaxNumber())+1},receiptSequenceStatus(){return Number(this.receiptSeqValue||0)<this.receiptMaxNumber()?'Sincronizar':'Sequência OK'},async saveReceiptSequence(){if(this.isViewer)return;let target=Number(this.receiptSeqInput||0), max=this.receiptMaxNumber();if(!target||target<1)return this.toast('Informe um próximo número válido.','warning');if(target<=max)return this.toast('O número precisa ser maior que o último emitido: '+String(max).padStart(6,'0')+'.','warning');await db.meta.put({key:'receiptSeq',value:target-1});this.receiptSeqValue=target-1;await this.logAction('settings','Sequência de recibos ajustada','Próximo recibo definido para nº '+String(target).padStart(6,'0'));this.toast('Sequência atualizada. Próximo recibo: '+String(target).padStart(6,'0'));await this.load()},
  topPaidNames(rows){let map={};rows.forEach(s=>{let v=Number(s.paidValue||0);if(v<=0)return;let name=this.providerName(s.providerId);map[name]=(map[name]||0)+v});return Object.entries(map).sort((a,b)=>b[1]-a[1]).slice(0,3).map(([name,total])=>name+' • '+this.money(total))},topOpenNames(rows){let map={};rows.forEach(s=>{let v=this.balance(s);if(v<=0)return;let name=this.providerName(s.providerId);map[name]=(map[name]||0)+v});return Object.entries(map).sort((a,b)=>b[1]-a[1]).slice(0,3).map(([name,total])=>name+' • '+this.money(total))},
  providerServices(id){return this.services.filter(s=>Number(s.providerId)===Number(id)).sort((a,b)=>String(b.serviceDate||'').localeCompare(String(a.serviceDate||'')))},providerReceipts(id){return this.receipts.filter(r=>Number(r.providerId)===Number(id)).sort((a,b)=>String(b.date||'').localeCompare(String(a.date||'')))},providerLastServiceLabel(id){let s=this.providerServices(id)[0];return s?this.fmt(s.serviceDate)+' • '+this.money(s.paidValue||s.amount):'-'},async logAction(type,title,detail){try{await db.logs.add({at:new Date().toISOString(),type,title,detail,role:this.auth?.role||'sistema'});this.logs=(await db.logs.orderBy('at').reverse().limit(30).toArray())}catch(e){console.warn('logAction',e)}},logIcon(type){return {payment:'circle-dollar-sign',receipt:'receipt-text',service:'clipboard-list',provider:'users',recurrence:'repeat-2',backup:'shield-check',restore:'rotate-ccw',danger:'triangle-alert',settings:'settings',delete:'trash-2'}[type]||'history'},fmtDateTime(d){if(!d)return'-';return new Date(d).toLocaleString('pt-BR')},async clearLogs(){if(this.isViewer)return;await db.logs.clear();this.logs=[];this.after()},providerSpend(id){return this.services.filter(s=>Number(s.providerId)===Number(id)).reduce((a,s)=>a+Number(s.paidValu

===== settings @ 6493083 =====
'rc_passwords')||'{"admin":"123456","viewer":"123456"}');if(this.passwords.admin)cur.admin=this.passwords.admin;if(this.passwords.viewer)cur.viewer=this.passwords.viewer;localStorage.setItem('rc_passwords',JSON.stringify(cur));await this.logAction('settings','Senhas atualizadas','Senhas locais do app foram atualizadas.');this.passwords={admin:'',viewer:''};this.toast('Senhas atualizadas')},async backup(){let now=new Date().toISOString();let seqMeta=await db.meta.get('receiptSeq');let data={settings:this.settings,providers:this.providers,services:this.services,receipts:this.receipts,recurrences:this.recurrences,logs:this.logs,receiptSeq:Number(seqMeta?.value||this.receiptMaxNumber()||0),backupStats:this.backupStats(),exportedAt:now};this.download('backup-recibocondo-prestadores.json',JSON.stringify(data,null,2),'application/json');this.backupLast=now;localStorage.setItem('rc_last_backup',now);await this.logAction('backup','Backup exportado','Backup JSON gerado com '+data.backupStats.records+' registros.')},async restore(ev){let f=ev.target.files[0];if(!f)return;try{let data=JSON.parse(await f.text());if(!data||typeof data!=='object')throw new Error('Backup inválido');let providers=Array.isArray(data.providers)?data.providers:[],services=Array.isArray(data.services)?data.services:[],receipts=Array.isArray(data.receipts)?data.receipts:[],recurrences=Array.isArray(data.recurrences)?data.recurrences:[],logs=Array.isArray(data.logs)?data.logs:[];await db.transaction('rw',db.providers,db.services,db.receipts,db.recurrences,db.logs,db.settings,db.meta,async()=>{await db.providers.clear();await db.services.clear();await db.receipts.clear();await db.recurrences.clear();await db.logs.clear();if(data.settings){let restoredSettings=JSON.parse(JSON.stringify(data.settings));await db.settings.put({key:'main',value:restoredSettings});this.settings=restoredSettings}if(providers.length)await db.providers.bulkAdd(providers);if(services.length)await db.services.bulkAdd(services);if(receipts.length)await db.receipts.bulkAdd(receipts);if(recurrences.length)await db.recurrences.bulkAdd(recurrences);if(logs.length)await db.logs.bulkAdd(logs.map(l=>{let x={...l};delete x.id;return x}).slice(0,200));let restoredMax=receipts.reduce((m,r)=>r.source==='Excel legado'?m:Math.max(m,Number(String(r.number||'').replace(/\D/g,'')||0)),0),safeSeq=Math.max(Number(data.receiptSeq||0),restoredMax);await db.meta.put({key:'receiptSeq',value:safeSeq})});await this.logAction('restore','Backup restaurado','Dados restaurados por arquivo JSON.');await this.load();this.toast('Backup restaurado com sucesso!')}catch(e){console.error(e);this.toast('Não foi possível restaurar o backup: '+e.message,'error')}},async clearAllInternal(){await db.transaction('rw',db.providers,db.services,db.receipts,db.recurrences,db.logs,async()=>{await db.providers.clear();await db.services.clear();await db.receipts.clear();await db.recurrences.clear();await db.logs.clear()});await this.load()},async clearAll(){this.openClearAllModal()},readLogo(e){let f=e.target.files[0];if(!f)return;let r=new FileReader();r.onload=()=>this.settings.logo=r.result;r.readAsDataURL(f)},
  receiptMaxNumber(){return this.receipts.reduce((max,r)=>r.source==='Excel legado'?max:Math.max(max,Number(String(r.number||'').replace(/\D/g,'')||0)),0)},nextReceiptNumber(){return Math.max(Number(this.receiptSeqValue||0),this.receiptMaxNumber())+1},receiptSequenceStatus(){return Number(this.receiptSeqValue||0)<this.receiptMaxNumber()?'Sincronizar':'Sequência OK'},async saveReceiptSequence(){if(this.isViewer)return;let target=Number(this.receiptSeqInput||0), max=this.receiptMaxNumber();if(!target||target<1)return this.toast('Informe um próximo número válido.','warning');if(target<=max)return this.toast('O número precisa ser maior que o último emitido: '+String(max).padStart(6,'0')+'.','warning');await db.meta.put({key:'receiptSeq',value:target-1});this.receiptSeqValue=target-1;await this.logAction('settings','Sequência de recibos ajustada','Próximo recibo definido para nº '+String(target).padStart(6,'0'));this.toast('Sequência atualizada. Próximo recibo: '+String(target).padStart(6,'0'));await this.load()},
  topPaidNames(rows){let map={};rows.forEach(s=>{let v=Number(s.paidValue||0);if(v<=0)return;let name=this.providerName(s.providerId);map[name]=(map[name]||0)+v});return Object.entries(map).sort((a,b)=>b[1]-a[1]).slice(0,3).map(([name,total])=>name+' • '+this.money(total))},topOpenNames(rows){let map={};rows.forEach(s=>{let v=this.balance(s);if(v<=0)return;let name=this.providerName(s.providerId);map[name]=(map[name]||0)+v});return Object.entries(map).sort((a,b)=>b[1]-a[1]).slice(0,3).map(([name,total])=>name+' • '+this.money(total))},
  providerServices(id){return this.services.filter(s=>Number(s.providerId)===Number(id)).sort((a,b)=>String(b.serviceDate||'').localeCompare(String(a.serviceDate||'')))},providerReceipts(id){return this.receipts.filter(r=>Number(r.providerId)===Number(id)).sort((a,b)=>String(b.date||'').localeCompare(String(a.date||'')))},providerLastServiceLabel(id){let s=this.providerServices(id)[0];return s?this.fmt(s.serviceDate)+' • '+this.money(s.paidValue||s.amount):'-'},async logAction(type,title,detail){try{await db.logs.add({at:new Date().toISOString(),type,title,detail,role:this.auth?.role||'sistema'});this.logs=(await db.logs.orderBy('at').reverse().limit(30).toArray())}catch(e){console.warn('logAction',e)}},logIcon(type){return {payment:'circle-dollar-sign',receipt:'receipt-text',service:'clipboard-list',provider:'users',recurrence:'repeat-2',backup:'shield-check',restore:'rotate-ccw',danger:'triangle-alert',settings:'settings',delete:'trash-2'}[type]||'history'},fmtDateTime(d){if(!d)return'-';return new Date(d).toLocaleString('pt-BR')},async clearLogs(){if(this.isViewer)return;await db.logs.clear();this.logs=[];this.after()},providerSpend(id){return this.services.filter(s=>Number(s.providerId)===Number(id)).reduce((a,s)=>a+Number(s.paidValue||s.amount||0),0)},

===== settings @ 6493138 =====
);if(this.passwords.admin)cur.admin=this.passwords.admin;if(this.passwords.viewer)cur.viewer=this.passwords.viewer;localStorage.setItem('rc_passwords',JSON.stringify(cur));await this.logAction('settings','Senhas atualizadas','Senhas locais do app foram atualizadas.');this.passwords={admin:'',viewer:''};this.toast('Senhas atualizadas')},async backup(){let now=new Date().toISOString();let seqMeta=await db.meta.get('receiptSeq');let data={settings:this.settings,providers:this.providers,services:this.services,receipts:this.receipts,recurrences:this.recurrences,logs:this.logs,receiptSeq:Number(seqMeta?.value||this.receiptMaxNumber()||0),backupStats:this.backupStats(),exportedAt:now};this.download('backup-recibocondo-prestadores.json',JSON.stringify(data,null,2),'application/json');this.backupLast=now;localStorage.setItem('rc_last_backup',now);await this.logAction('backup','Backup exportado','Backup JSON gerado com '+data.backupStats.records+' registros.')},async restore(ev){let f=ev.target.files[0];if(!f)return;try{let data=JSON.parse(await f.text());if(!data||typeof data!=='object')throw new Error('Backup inválido');let providers=Array.isArray(data.providers)?data.providers:[],services=Array.isArray(data.services)?data.services:[],receipts=Array.isArray(data.receipts)?data.receipts:[],recurrences=Array.isArray(data.recurrences)?data.recurrences:[],logs=Array.isArray(data.logs)?data.logs:[];await db.transaction('rw',db.providers,db.services,db.receipts,db.recurrences,db.logs,db.settings,db.meta,async()=>{await db.providers.clear();await db.services.clear();await db.receipts.clear();await db.recurrences.clear();await db.logs.clear();if(data.settings){let restoredSettings=JSON.parse(JSON.stringify(data.settings));await db.settings.put({key:'main',value:restoredSettings});this.settings=restoredSettings}if(providers.length)await db.providers.bulkAdd(providers);if(services.length)await db.services.bulkAdd(services);if(receipts.length)await db.receipts.bulkAdd(receipts);if(recurrences.length)await db.recurrences.bulkAdd(recurrences);if(logs.length)await db.logs.bulkAdd(logs.map(l=>{let x={...l};delete x.id;return x}).slice(0,200));let restoredMax=receipts.reduce((m,r)=>r.source==='Excel legado'?m:Math.max(m,Number(String(r.number||'').replace(/\D/g,'')||0)),0),safeSeq=Math.max(Number(data.receiptSeq||0),restoredMax);await db.meta.put({key:'receiptSeq',value:safeSeq})});await this.logAction('restore','Backup restaurado','Dados restaurados por arquivo JSON.');await this.load();this.toast('Backup restaurado com sucesso!')}catch(e){console.error(e);this.toast('Não foi possível restaurar o backup: '+e.message,'error')}},async clearAllInternal(){await db.transaction('rw',db.providers,db.services,db.receipts,db.recurrences,db.logs,async()=>{await db.providers.clear();await db.services.clear();await db.receipts.clear();await db.recurrences.clear();await db.logs.clear()});await this.load()},async clearAll(){this.openClearAllModal()},readLogo(e){let f=e.target.files[0];if(!f)return;let r=new FileReader();r.onload=()=>this.settings.logo=r.result;r.readAsDataURL(f)},
  receiptMaxNumber(){return this.receipts.reduce((max,r)=>r.source==='Excel legado'?max:Math.max(max,Number(String(r.number||'').replace(/\D/g,'')||0)),0)},nextReceiptNumber(){return Math.max(Number(this.receiptSeqValue||0),this.receiptMaxNumber())+1},receiptSequenceStatus(){return Number(this.receiptSeqValue||0)<this.receiptMaxNumber()?'Sincronizar':'Sequência OK'},async saveReceiptSequence(){if(this.isViewer)return;let target=Number(this.receiptSeqInput||0), max=this.receiptMaxNumber();if(!target||target<1)return this.toast('Informe um próximo número válido.','warning');if(target<=max)return this.toast('O número precisa ser maior que o último emitido: '+String(max).padStart(6,'0')+'.','warning');await db.meta.put({key:'receiptSeq',value:target-1});this.receiptSeqValue=target-1;await this.logAction('settings','Sequência de recibos ajustada','Próximo recibo definido para nº '+String(target).padStart(6,'0'));this.toast('Sequência atualizada. Próximo recibo: '+String(target).padStart(6,'0'));await this.load()},
  topPaidNames(rows){let map={};rows.forEach(s=>{let v=Number(s.paidValue||0);if(v<=0)return;let name=this.providerName(s.providerId);map[name]=(map[name]||0)+v});return Object.entries(map).sort((a,b)=>b[1]-a[1]).slice(0,3).map(([name,total])=>name+' • '+this.money(total))},topOpenNames(rows){let map={};rows.forEach(s=>{let v=this.balance(s);if(v<=0)return;let name=this.providerName(s.providerId);map[name]=(map[name]||0)+v});return Object.entries(map).sort((a,b)=>b[1]-a[1]).slice(0,3).map(([name,total])=>name+' • '+this.money(total))},
  providerServices(id){return this.services.filter(s=>Number(s.providerId)===Number(id)).sort((a,b)=>String(b.serviceDate||'').localeCompare(String(a.serviceDate||'')))},providerReceipts(id){return this.receipts.filter(r=>Number(r.providerId)===Number(id)).sort((a,b)=>String(b.date||'').localeCompare(String(a.date||'')))},providerLastServiceLabel(id){let s=this.providerServices(id)[0];return s?this.fmt(s.serviceDate)+' • '+this.money(s.paidValue||s.amount):'-'},async logAction(type,title,detail){try{await db.logs.add({at:new Date().toISOString(),type,title,detail,role:this.auth?.role||'sistema'});this.logs=(await db.logs.orderBy('at').reverse().limit(30).toArray())}catch(e){console.warn('logAction',e)}},logIcon(type){return {payment:'circle-dollar-sign',receipt:'receipt-text',service:'clipboard-list',provider:'users',recurrence:'repeat-2',backup:'shield-check',restore:'rotate-ccw',danger:'triangle-alert',settings:'settings',delete:'trash-2'}[type]||'history'},fmtDateTime(d){if(!d)return'-';return new Date(d).toLocaleString('pt-BR')},async clearLogs(){if(this.isViewer)return;await db.logs.clear();this.logs=[];this.after()},providerSpend(id){return this.services.filter(s=>Number(s.providerId)===Number(id)).reduce((a,s)=>a+Number(s.paidValue||s.amount||0),0)},providerOpen(id){return this.services.filter(s=>Number(

===== settings @ 6494395 =====
rray.isArray(data.receipts)?data.receipts:[],recurrences=Array.isArray(data.recurrences)?data.recurrences:[],logs=Array.isArray(data.logs)?data.logs:[];await db.transaction('rw',db.providers,db.services,db.receipts,db.recurrences,db.logs,db.settings,db.meta,async()=>{await db.providers.clear();await db.services.clear();await db.receipts.clear();await db.recurrences.clear();await db.logs.clear();if(data.settings){let restoredSettings=JSON.parse(JSON.stringify(data.settings));await db.settings.put({key:'main',value:restoredSettings});this.settings=restoredSettings}if(providers.length)await db.providers.bulkAdd(providers);if(services.length)await db.services.bulkAdd(services);if(receipts.length)await db.receipts.bulkAdd(receipts);if(recurrences.length)await db.recurrences.bulkAdd(recurrences);if(logs.length)await db.logs.bulkAdd(logs.map(l=>{let x={...l};delete x.id;return x}).slice(0,200));let restoredMax=receipts.reduce((m,r)=>r.source==='Excel legado'?m:Math.max(m,Number(String(r.number||'').replace(/\D/g,'')||0)),0),safeSeq=Math.max(Number(data.receiptSeq||0),restoredMax);await db.meta.put({key:'receiptSeq',value:safeSeq})});await this.logAction('restore','Backup restaurado','Dados restaurados por arquivo JSON.');await this.load();this.toast('Backup restaurado com sucesso!')}catch(e){console.error(e);this.toast('Não foi possível restaurar o backup: '+e.message,'error')}},async clearAllInternal(){await db.transaction('rw',db.providers,db.services,db.receipts,db.recurrences,db.logs,async()=>{await db.providers.clear();await db.services.clear();await db.receipts.clear();await db.recurrences.clear();await db.logs.clear()});await this.load()},async clearAll(){this.openClearAllModal()},readLogo(e){let f=e.target.files[0];if(!f)return;let r=new FileReader();r.onload=()=>this.settings.logo=r.result;r.readAsDataURL(f)},
  receiptMaxNumber(){return this.receipts.reduce((max,r)=>r.source==='Excel legado'?max:Math.max(max,Number(String(r.number||'').replace(/\D/g,'')||0)),0)},nextReceiptNumber(){return Math.max(Number(this.receiptSeqValue||0),this.receiptMaxNumber())+1},receiptSequenceStatus(){return Number(this.receiptSeqValue||0)<this.receiptMaxNumber()?'Sincronizar':'Sequência OK'},async saveReceiptSequence(){if(this.isViewer)return;let target=Number(this.receiptSeqInput||0), max=this.receiptMaxNumber();if(!target||target<1)return this.toast('Informe um próximo número válido.','warning');if(target<=max)return this.toast('O número precisa ser maior que o último emitido: '+String(max).padStart(6,'0')+'.','warning');await db.meta.put({key:'receiptSeq',value:target-1});this.receiptSeqValue=target-1;await this.logAction('settings','Sequência de recibos ajustada','Próximo recibo definido para nº '+String(target).padStart(6,'0'));this.toast('Sequência atualizada. Próximo recibo: '+String(target).padStart(6,'0'));await this.load()},
  topPaidNames(rows){let map={};rows.forEach(s=>{let v=Number(s.paidValue||0);if(v<=0)return;let name=this.providerName(s.providerId);map[name]=(map[name]||0)+v});return Object.entries(map).sort((a,b)=>b[1]-a[1]).slice(0,3).map(([name,total])=>name+' • '+this.money(total))},topOpenNames(rows){let map={};rows.forEach(s=>{let v=this.balance(s);if(v<=0)return;let name=this.providerName(s.providerId);map[name]=(map[name]||0)+v});return Object.entries(map).sort((a,b)=>b[1]-a[1]).slice(0,3).map(([name,total])=>name+' • '+this.money(total))},
  providerServices(id){return this.services.filter(s=>Number(s.providerId)===Number(id)).sort((a,b)=>String(b.serviceDate||'').localeCompare(String(a.serviceDate||'')))},providerReceipts(id){return this.receipts.filter(r=>Number(r.providerId)===Number(id)).sort((a,b)=>String(b.date||'').localeCompare(String(a.date||'')))},providerLastServiceLabel(id){let s=this.providerServices(id)[0];return s?this.fmt(s.serviceDate)+' • '+this.money(s.paidValue||s.amount):'-'},async logAction(type,title,detail){try{await db.logs.add({at:new Date().toISOString(),type,title,detail,role:this.auth?.role||'sistema'});this.logs=(await db.logs.orderBy('at').reverse().limit(30).toArray())}catch(e){console.warn('logAction',e)}},logIcon(type){return {payment:'circle-dollar-sign',receipt:'receipt-text',service:'clipboard-list',provider:'users',recurrence:'repeat-2',backup:'shield-check',restore:'rotate-ccw',danger:'triangle-alert',settings:'settings',delete:'trash-2'}[type]||'history'},fmtDateTime(d){if(!d)return'-';return new Date(d).toLocaleString('pt-BR')},async clearLogs(){if(this.isViewer)return;await db.logs.clear();this.logs=[];this.after()},providerSpend(id){return this.services.filter(s=>Number(s.providerId)===Number(id)).reduce((a,s)=>a+Number(s.paidValue||s.amount||0),0)},providerOpen(id){return this.services.filter(s=>Number(s.providerId)===Number(id)).reduce((a,s)=>a+this.balance(s),0)},openWhatsapp(p){let raw=String(p.whatsapp||p.phone||'').replace(/\D/g,'');if(!raw)return this.toast('Este prestador não tem WhatsApp/telefone cadastrado.','warning');let phone=raw.startsWith('55')?raw:'55'+raw;let msg=encodeURIComponent('Olá, '+p.name+'. Aqui é do '+this.settings.condoName+'.');window.open('https://wa.me/'+phone+'?text='+msg,'_blank')},overdueLabel(s){if(!s.dueDate||this.balance(s)<=0)return'';let today=new Date(this.today()+'T00:00:00'), due=new Date(s.dueDate+'T00:00:00'), days=Math.ceil((due-today)/86400000);if(days<0)return Math.abs(days)+' dia(s) em atraso';if(days===0)return'vence hoje';return'faltam '+days+' dia(s)'},providerName(id){return (this.providers.find(p=>Number(p.id)===Number(id))||{}).name||'-'},reportReceiptNumber(service){let r=this.receipts.find(x=>Number(x.serviceId)===Number(service.id));return r?.number||'-';},balance(s){return Math.max(0,Number(s.amount||0)-Number(s.paidValue||0))},statusClass(st){return st==='Pago'?'bg-emerald-500/10 text-emerald-700':st==='Parcial'?'bg-amber-500/10 text-amber-700':'bg-red-500/10 text-red-700'},valorPorExtenso(v){v=Number(v||0);let reais=Math.floor(v),cent=Math.round((v-reais)*100);let base={0:'zero

===== settings @ 6495251 =====
={...l};delete x.id;return x}).slice(0,200));let restoredMax=receipts.reduce((m,r)=>r.source==='Excel legado'?m:Math.max(m,Number(String(r.number||'').replace(/\D/g,'')||0)),0),safeSeq=Math.max(Number(data.receiptSeq||0),restoredMax);await db.meta.put({key:'receiptSeq',value:safeSeq})});await this.logAction('restore','Backup restaurado','Dados restaurados por arquivo JSON.');await this.load();this.toast('Backup restaurado com sucesso!')}catch(e){console.error(e);this.toast('Não foi possível restaurar o backup: '+e.message,'error')}},async clearAllInternal(){await db.transaction('rw',db.providers,db.services,db.receipts,db.recurrences,db.logs,async()=>{await db.providers.clear();await db.services.clear();await db.receipts.clear();await db.recurrences.clear();await db.logs.clear()});await this.load()},async clearAll(){this.openClearAllModal()},readLogo(e){let f=e.target.files[0];if(!f)return;let r=new FileReader();r.onload=()=>this.settings.logo=r.result;r.readAsDataURL(f)},
  receiptMaxNumber(){return this.receipts.reduce((max,r)=>r.source==='Excel legado'?max:Math.max(max,Number(String(r.number||'').replace(/\D/g,'')||0)),0)},nextReceiptNumber(){return Math.max(Number(this.receiptSeqValue||0),this.receiptMaxNumber())+1},receiptSequenceStatus(){return Number(this.receiptSeqValue||0)<this.receiptMaxNumber()?'Sincronizar':'Sequência OK'},async saveReceiptSequence(){if(this.isViewer)return;let target=Number(this.receiptSeqInput||0), max=this.receiptMaxNumber();if(!target||target<1)return this.toast('Informe um próximo número válido.','warning');if(target<=max)return this.toast('O número precisa ser maior que o último emitido: '+String(max).padStart(6,'0')+'.','warning');await db.meta.put({key:'receiptSeq',value:target-1});this.receiptSeqValue=target-1;await this.logAction('settings','Sequência de recibos ajustada','Próximo recibo definido para nº '+String(target).padStart(6,'0'));this.toast('Sequência atualizada. Próximo recibo: '+String(target).padStart(6,'0'));await this.load()},
  topPaidNames(rows){let map={};rows.forEach(s=>{let v=Number(s.paidValue||0);if(v<=0)return;let name=this.providerName(s.providerId);map[name]=(map[name]||0)+v});return Object.entries(map).sort((a,b)=>b[1]-a[1]).slice(0,3).map(([name,total])=>name+' • '+this.money(total))},topOpenNames(rows){let map={};rows.forEach(s=>{let v=this.balance(s);if(v<=0)return;let name=this.providerName(s.providerId);map[name]=(map[name]||0)+v});return Object.entries(map).sort((a,b)=>b[1]-a[1]).slice(0,3).map(([name,total])=>name+' • '+this.money(total))},
  providerServices(id){return this.services.filter(s=>Number(s.providerId)===Number(id)).sort((a,b)=>String(b.serviceDate||'').localeCompare(String(a.serviceDate||'')))},providerReceipts(id){return this.receipts.filter(r=>Number(r.providerId)===Number(id)).sort((a,b)=>String(b.date||'').localeCompare(String(a.date||'')))},providerLastServiceLabel(id){let s=this.providerServices(id)[0];return s?this.fmt(s.serviceDate)+' • '+this.money(s.paidValue||s.amount):'-'},async logAction(type,title,detail){try{await db.logs.add({at:new Date().toISOString(),type,title,detail,role:this.auth?.role||'sistema'});this.logs=(await db.logs.orderBy('at').reverse().limit(30).toArray())}catch(e){console.warn('logAction',e)}},logIcon(type){return {payment:'circle-dollar-sign',receipt:'receipt-text',service:'clipboard-list',provider:'users',recurrence:'repeat-2',backup:'shield-check',restore:'rotate-ccw',danger:'triangle-alert',settings:'settings',delete:'trash-2'}[type]||'history'},fmtDateTime(d){if(!d)return'-';return new Date(d).toLocaleString('pt-BR')},async clearLogs(){if(this.isViewer)return;await db.logs.clear();this.logs=[];this.after()},providerSpend(id){return this.services.filter(s=>Number(s.providerId)===Number(id)).reduce((a,s)=>a+Number(s.paidValue||s.amount||0),0)},providerOpen(id){return this.services.filter(s=>Number(s.providerId)===Number(id)).reduce((a,s)=>a+this.balance(s),0)},openWhatsapp(p){let raw=String(p.whatsapp||p.phone||'').replace(/\D/g,'');if(!raw)return this.toast('Este prestador não tem WhatsApp/telefone cadastrado.','warning');let phone=raw.startsWith('55')?raw:'55'+raw;let msg=encodeURIComponent('Olá, '+p.name+'. Aqui é do '+this.settings.condoName+'.');window.open('https://wa.me/'+phone+'?text='+msg,'_blank')},overdueLabel(s){if(!s.dueDate||this.balance(s)<=0)return'';let today=new Date(this.today()+'T00:00:00'), due=new Date(s.dueDate+'T00:00:00'), days=Math.ceil((due-today)/86400000);if(days<0)return Math.abs(days)+' dia(s) em atraso';if(days===0)return'vence hoje';return'faltam '+days+' dia(s)'},providerName(id){return (this.providers.find(p=>Number(p.id)===Number(id))||{}).name||'-'},reportReceiptNumber(service){let r=this.receipts.find(x=>Number(x.serviceId)===Number(service.id));return r?.number||'-';},balance(s){return Math.max(0,Number(s.amount||0)-Number(s.paidValue||0))},statusClass(st){return st==='Pago'?'bg-emerald-500/10 text-emerald-700':st==='Parcial'?'bg-amber-500/10 text-amber-700':'bg-red-500/10 text-red-700'},valorPorExtenso(v){v=Number(v||0);let reais=Math.floor(v),cent=Math.round((v-reais)*100);let base={0:'zero',1:'um',2:'dois',3:'três',4:'quatro',5:'cinco',6:'seis',7:'sete',8:'oito',9:'nove',10:'dez',11:'onze',12:'doze',13:'treze',14:'quatorze',15:'quinze',16:'dezesseis',17:'dezessete',18:'dezoito',19:'dezenove',20:'vinte',30:'trinta',40:'quarenta',50:'cinquenta',60:'sessenta',70:'setenta',80:'oitenta',90:'noventa',100:'cem'};let ext=(n)=>{if(n<=20||base[n])return base[n];if(n<100){let d=Math.floor(n/10)*10,u=n%10;return base[d]+(u?' e '+base[u]:'')}if(n<1000){let c=Math.floor(n/100),r=n%100,cn=c===1?'cento':base[c]+' centos';cn=cn.replace('dois centos','duzentos').replace('três centos','trezentos').replace('quatro centos','quatrocentos').replace('cinco centos','quinhentos').replace('seis centos','seiscentos').replace('sete centos','setecentos').replace('oito centos','oitocentos').replace('nove centos','novecentos');return r?cn+' e '+ext(r):cn}if(n<

===== settings @ 6496924 =====
6,'0')+'.','warning');await db.meta.put({key:'receiptSeq',value:target-1});this.receiptSeqValue=target-1;await this.logAction('settings','Sequência de recibos ajustada','Próximo recibo definido para nº '+String(target).padStart(6,'0'));this.toast('Sequência atualizada. Próximo recibo: '+String(target).padStart(6,'0'));await this.load()},
  topPaidNames(rows){let map={};rows.forEach(s=>{let v=Number(s.paidValue||0);if(v<=0)return;let name=this.providerName(s.providerId);map[name]=(map[name]||0)+v});return Object.entries(map).sort((a,b)=>b[1]-a[1]).slice(0,3).map(([name,total])=>name+' • '+this.money(total))},topOpenNames(rows){let map={};rows.forEach(s=>{let v=this.balance(s);if(v<=0)return;let name=this.providerName(s.providerId);map[name]=(map[name]||0)+v});return Object.entries(map).sort((a,b)=>b[1]-a[1]).slice(0,3).map(([name,total])=>name+' • '+this.money(total))},
  providerServices(id){return this.services.filter(s=>Number(s.providerId)===Number(id)).sort((a,b)=>String(b.serviceDate||'').localeCompare(String(a.serviceDate||'')))},providerReceipts(id){return this.receipts.filter(r=>Number(r.providerId)===Number(id)).sort((a,b)=>String(b.date||'').localeCompare(String(a.date||'')))},providerLastServiceLabel(id){let s=this.providerServices(id)[0];return s?this.fmt(s.serviceDate)+' • '+this.money(s.paidValue||s.amount):'-'},async logAction(type,title,detail){try{await db.logs.add({at:new Date().toISOString(),type,title,detail,role:this.auth?.role||'sistema'});this.logs=(await db.logs.orderBy('at').reverse().limit(30).toArray())}catch(e){console.warn('logAction',e)}},logIcon(type){return {payment:'circle-dollar-sign',receipt:'receipt-text',service:'clipboard-list',provider:'users',recurrence:'repeat-2',backup:'shield-check',restore:'rotate-ccw',danger:'triangle-alert',settings:'settings',delete:'trash-2'}[type]||'history'},fmtDateTime(d){if(!d)return'-';return new Date(d).toLocaleString('pt-BR')},async clearLogs(){if(this.isViewer)return;await db.logs.clear();this.logs=[];this.after()},providerSpend(id){return this.services.filter(s=>Number(s.providerId)===Number(id)).reduce((a,s)=>a+Number(s.paidValue||s.amount||0),0)},providerOpen(id){return this.services.filter(s=>Number(s.providerId)===Number(id)).reduce((a,s)=>a+this.balance(s),0)},openWhatsapp(p){let raw=String(p.whatsapp||p.phone||'').replace(/\D/g,'');if(!raw)return this.toast('Este prestador não tem WhatsApp/telefone cadastrado.','warning');let phone=raw.startsWith('55')?raw:'55'+raw;let msg=encodeURIComponent('Olá, '+p.name+'. Aqui é do '+this.settings.condoName+'.');window.open('https://wa.me/'+phone+'?text='+msg,'_blank')},overdueLabel(s){if(!s.dueDate||this.balance(s)<=0)return'';let today=new Date(this.today()+'T00:00:00'), due=new Date(s.dueDate+'T00:00:00'), days=Math.ceil((due-today)/86400000);if(days<0)return Math.abs(days)+' dia(s) em atraso';if(days===0)return'vence hoje';return'faltam '+days+' dia(s)'},providerName(id){return (this.providers.find(p=>Number(p.id)===Number(id))||{}).name||'-'},reportReceiptNumber(service){let r=this.receipts.find(x=>Number(x.serviceId)===Number(service.id));return r?.number||'-';},balance(s){return Math.max(0,Number(s.amount||0)-Number(s.paidValue||0))},statusClass(st){return st==='Pago'?'bg-emerald-500/10 text-emerald-700':st==='Parcial'?'bg-amber-500/10 text-amber-700':'bg-red-500/10 text-red-700'},valorPorExtenso(v){v=Number(v||0);let reais=Math.floor(v),cent=Math.round((v-reais)*100);let base={0:'zero',1:'um',2:'dois',3:'três',4:'quatro',5:'cinco',6:'seis',7:'sete',8:'oito',9:'nove',10:'dez',11:'onze',12:'doze',13:'treze',14:'quatorze',15:'quinze',16:'dezesseis',17:'dezessete',18:'dezoito',19:'dezenove',20:'vinte',30:'trinta',40:'quarenta',50:'cinquenta',60:'sessenta',70:'setenta',80:'oitenta',90:'noventa',100:'cem'};let ext=(n)=>{if(n<=20||base[n])return base[n];if(n<100){let d=Math.floor(n/10)*10,u=n%10;return base[d]+(u?' e '+base[u]:'')}if(n<1000){let c=Math.floor(n/100),r=n%100,cn=c===1?'cento':base[c]+' centos';cn=cn.replace('dois centos','duzentos').replace('três centos','trezentos').replace('quatro centos','quatrocentos').replace('cinco centos','quinhentos').replace('seis centos','seiscentos').replace('sete centos','setecentos').replace('oito centos','oitocentos').replace('nove centos','novecentos');return r?cn+' e '+ext(r):cn}if(n<1000000){let m=Math.floor(n/1000),r=n%1000;return (m===1?'mil':ext(m)+' mil')+(r?' e '+ext(r):'')}return n.toLocaleString('pt-BR')};let txt=ext(reais)+' '+(reais===1?'real':'reais');if(cent)txt+=' e '+ext(cent)+' '+(cent===1?'centavo':'centavos');return txt;},money(v){return Number(v||0).toLocaleString('pt-BR',{style:'currency',currency:'BRL'})},today(){return new Date().toISOString().slice(0,10)},fmt(d){if(!d)return'-';return new Date(d+'T00:00:00').toLocaleDateString('pt-BR')},month(m){if(!m)return'-';let [y,mo]=String(m).split('-');return new Date(Number(y),Number(mo)-1,1).toLocaleDateString('pt-BR',{month:'long',year:'numeric'})},reportMonthLabel(){let src=(this.reportRows[0]?.reference)||String(this.report.from||this.today()).slice(0,7);let label=this.month(src);return String(label).replace(/ de \d{4}/,'').toUpperCase();},download(n,c,t){let a=document.createElement('a');a.href=URL.createObjectURL(new Blob([c],{type:t}));a.download=n;a.click();URL.revokeObjectURL(a.href)},refreshIcons(){setTimeout(()=>window.lucide&&lucide.createIcons(),60)},after(){this.$nextTick(()=>{this.enhanceAccordions();this.refreshIcons();this.renderCharts();setTimeout(()=>this.resizeCharts(),120)})},enhanceAccordions(){try{const scopes=['prestadores','servicos','pagamentos','recibos','busca','config'];scopes.forEach(scope=>{const section=[...document.querySelectorAll('main section')].find(s=>(s.getAttribute('x-show')||'').includes(scope));if(!section)return;const panels=[...section.children].filter(el=>el.classList&&el.classList.contains('glass'));panels.forEach((panel,idx)=>{if(panel.dataset.rcAccordion==='1')return;panel.dataset.rcAccordion='1';panel.classList.ad

===== settings @ 6496934 =====
,'warning');await db.meta.put({key:'receiptSeq',value:target-1});this.receiptSeqValue=target-1;await this.logAction('settings','Sequência de recibos ajustada','Próximo recibo definido para nº '+String(target).padStart(6,'0'));this.toast('Sequência atualizada. Próximo recibo: '+String(target).padStart(6,'0'));await this.load()},
  topPaidNames(rows){let map={};rows.forEach(s=>{let v=Number(s.paidValue||0);if(v<=0)return;let name=this.providerName(s.providerId);map[name]=(map[name]||0)+v});return Object.entries(map).sort((a,b)=>b[1]-a[1]).slice(0,3).map(([name,total])=>name+' • '+this.money(total))},topOpenNames(rows){let map={};rows.forEach(s=>{let v=this.balance(s);if(v<=0)return;let name=this.providerName(s.providerId);map[name]=(map[name]||0)+v});return Object.entries(map).sort((a,b)=>b[1]-a[1]).slice(0,3).map(([name,total])=>name+' • '+this.money(total))},
  providerServices(id){return this.services.filter(s=>Number(s.providerId)===Number(id)).sort((a,b)=>String(b.serviceDate||'').localeCompare(String(a.serviceDate||'')))},providerReceipts(id){return this.receipts.filter(r=>Number(r.providerId)===Number(id)).sort((a,b)=>String(b.date||'').localeCompare(String(a.date||'')))},providerLastServiceLabel(id){let s=this.providerServices(id)[0];return s?this.fmt(s.serviceDate)+' • '+this.money(s.paidValue||s.amount):'-'},async logAction(type,title,detail){try{await db.logs.add({at:new Date().toISOString(),type,title,detail,role:this.auth?.role||'sistema'});this.logs=(await db.logs.orderBy('at').reverse().limit(30).toArray())}catch(e){console.warn('logAction',e)}},logIcon(type){return {payment:'circle-dollar-sign',receipt:'receipt-text',service:'clipboard-list',provider:'users',recurrence:'repeat-2',backup:'shield-check',restore:'rotate-ccw',danger:'triangle-alert',settings:'settings',delete:'trash-2'}[type]||'history'},fmtDateTime(d){if(!d)return'-';return new Date(d).toLocaleString('pt-BR')},async clearLogs(){if(this.isViewer)return;await db.logs.clear();this.logs=[];this.after()},providerSpend(id){return this.services.filter(s=>Number(s.providerId)===Number(id)).reduce((a,s)=>a+Number(s.paidValue||s.amount||0),0)},providerOpen(id){return this.services.filter(s=>Number(s.providerId)===Number(id)).reduce((a,s)=>a+this.balance(s),0)},openWhatsapp(p){let raw=String(p.whatsapp||p.phone||'').replace(/\D/g,'');if(!raw)return this.toast('Este prestador não tem WhatsApp/telefone cadastrado.','warning');let phone=raw.startsWith('55')?raw:'55'+raw;let msg=encodeURIComponent('Olá, '+p.name+'. Aqui é do '+this.settings.condoName+'.');window.open('https://wa.me/'+phone+'?text='+msg,'_blank')},overdueLabel(s){if(!s.dueDate||this.balance(s)<=0)return'';let today=new Date(this.today()+'T00:00:00'), due=new Date(s.dueDate+'T00:00:00'), days=Math.ceil((due-today)/86400000);if(days<0)return Math.abs(days)+' dia(s) em atraso';if(days===0)return'vence hoje';return'faltam '+days+' dia(s)'},providerName(id){return (this.providers.find(p=>Number(p.id)===Number(id))||{}).name||'-'},reportReceiptNumber(service){let r=this.receipts.find(x=>Number(x.serviceId)===Number(service.id));return r?.number||'-';},balance(s){return Math.max(0,Number(s.amount||0)-Number(s.paidValue||0))},statusClass(st){return st==='Pago'?'bg-emerald-500/10 text-emerald-700':st==='Parcial'?'bg-amber-500/10 text-amber-700':'bg-red-500/10 text-red-700'},valorPorExtenso(v){v=Number(v||0);let reais=Math.floor(v),cent=Math.round((v-reais)*100);let base={0:'zero',1:'um',2:'dois',3:'três',4:'quatro',5:'cinco',6:'seis',7:'sete',8:'oito',9:'nove',10:'dez',11:'onze',12:'doze',13:'treze',14:'quatorze',15:'quinze',16:'dezesseis',17:'dezessete',18:'dezoito',19:'dezenove',20:'vinte',30:'trinta',40:'quarenta',50:'cinquenta',60:'sessenta',70:'setenta',80:'oitenta',90:'noventa',100:'cem'};let ext=(n)=>{if(n<=20||base[n])return base[n];if(n<100){let d=Math.floor(n/10)*10,u=n%10;return base[d]+(u?' e '+base[u]:'')}if(n<1000){let c=Math.floor(n/100),r=n%100,cn=c===1?'cento':base[c]+' centos';cn=cn.replace('dois centos','duzentos').replace('três centos','trezentos').replace('quatro centos','quatrocentos').replace('cinco centos','quinhentos').replace('seis centos','seiscentos').replace('sete centos','setecentos').replace('oito centos','oitocentos').replace('nove centos','novecentos');return r?cn+' e '+ext(r):cn}if(n<1000000){let m=Math.floor(n/1000),r=n%1000;return (m===1?'mil':ext(m)+' mil')+(r?' e '+ext(r):'')}return n.toLocaleString('pt-BR')};let txt=ext(reais)+' '+(reais===1?'real':'reais');if(cent)txt+=' e '+ext(cent)+' '+(cent===1?'centavo':'centavos');return txt;},money(v){return Number(v||0).toLocaleString('pt-BR',{style:'currency',currency:'BRL'})},today(){return new Date().toISOString().slice(0,10)},fmt(d){if(!d)return'-';return new Date(d+'T00:00:00').toLocaleDateString('pt-BR')},month(m){if(!m)return'-';let [y,mo]=String(m).split('-');return new Date(Number(y),Number(mo)-1,1).toLocaleDateString('pt-BR',{month:'long',year:'numeric'})},reportMonthLabel(){let src=(this.reportRows[0]?.reference)||String(this.report.from||this.today()).slice(0,7);let label=this.month(src);return String(label).replace(/ de \d{4}/,'').toUpperCase();},download(n,c,t){let a=document.createElement('a');a.href=URL.createObjectURL(new Blob([c],{type:t}));a.download=n;a.click();URL.revokeObjectURL(a.href)},refreshIcons(){setTimeout(()=>window.lucide&&lucide.createIcons(),60)},after(){this.$nextTick(()=>{this.enhanceAccordions();this.refreshIcons();this.renderCharts();setTimeout(()=>this.resizeCharts(),120)})},enhanceAccordions(){try{const scopes=['prestadores','servicos','pagamentos','recibos','busca','config'];scopes.forEach(scope=>{const section=[...document.querySelectorAll('main section')].find(s=>(s.getAttribute('x-show')||'').includes(scope));if(!section)return;const panels=[...section.children].filter(el=>el.classList&&el.classList.contains('glass'));panels.forEach((panel,idx)=>{if(panel.dataset.rcAccordion==='1')return;panel.dataset.rcAccordion='1';panel.classList.add('rc-acco

===== settings @ 6497674 =====
(map[name]||0)+v});return Object.entries(map).sort((a,b)=>b[1]-a[1]).slice(0,3).map(([name,total])=>name+' • '+this.money(total))},
  providerServices(id){return this.services.filter(s=>Number(s.providerId)===Number(id)).sort((a,b)=>String(b.serviceDate||'').localeCompare(String(a.serviceDate||'')))},providerReceipts(id){return this.receipts.filter(r=>Number(r.providerId)===Number(id)).sort((a,b)=>String(b.date||'').localeCompare(String(a.date||'')))},providerLastServiceLabel(id){let s=this.providerServices(id)[0];return s?this.fmt(s.serviceDate)+' • '+this.money(s.paidValue||s.amount):'-'},async logAction(type,title,detail){try{await db.logs.add({at:new Date().toISOString(),type,title,detail,role:this.auth?.role||'sistema'});this.logs=(await db.logs.orderBy('at').reverse().limit(30).toArray())}catch(e){console.warn('logAction',e)}},logIcon(type){return {payment:'circle-dollar-sign',receipt:'receipt-text',service:'clipboard-list',provider:'users',recurrence:'repeat-2',backup:'shield-check',restore:'rotate-ccw',danger:'triangle-alert',settings:'settings',delete:'trash-2'}[type]||'history'},fmtDateTime(d){if(!d)return'-';return new Date(d).toLocaleString('pt-BR')},async clearLogs(){if(this.isViewer)return;await db.logs.clear();this.logs=[];this.after()},providerSpend(id){return this.services.filter(s=>Number(s.providerId)===Number(id)).reduce((a,s)=>a+Number(s.paidValue||s.amount||0),0)},providerOpen(id){return this.services.filter(s=>Number(s.providerId)===Number(id)).reduce((a,s)=>a+this.balance(s),0)},openWhatsapp(p){let raw=String(p.whatsapp||p.phone||'').replace(/\D/g,'');if(!raw)return this.toast('Este prestador não tem WhatsApp/telefone cadastrado.','warning');let phone=raw.startsWith('55')?raw:'55'+raw;let msg=encodeURIComponent('Olá, '+p.name+'. Aqui é do '+this.settings.condoName+'.');window.open('https://wa.me/'+phone+'?text='+msg,'_blank')},overdueLabel(s){if(!s.dueDate||this.balance(s)<=0)return'';let today=new Date(this.today()+'T00:00:00'), due=new Date(s.dueDate+'T00:00:00'), days=Math.ceil((due-today)/86400000);if(days<0)return Math.abs(days)+' dia(s) em atraso';if(days===0)return'vence hoje';return'faltam '+days+' dia(s)'},providerName(id){return (this.providers.find(p=>Number(p.id)===Number(id))||{}).name||'-'},reportReceiptNumber(service){let r=this.receipts.find(x=>Number(x.serviceId)===Number(service.id));return r?.number||'-';},balance(s){return Math.max(0,Number(s.amount||0)-Number(s.paidValue||0))},statusClass(st){return st==='Pago'?'bg-emerald-500/10 text-emerald-700':st==='Parcial'?'bg-amber-500/10 text-amber-700':'bg-red-500/10 text-red-700'},valorPorExtenso(v){v=Number(v||0);let reais=Math.floor(v),cent=Math.round((v-reais)*100);let base={0:'zero',1:'um',2:'dois',3:'três',4:'quatro',5:'cinco',6:'seis',7:'sete',8:'oito',9:'nove',10:'dez',11:'onze',12:'doze',13:'treze',14:'quatorze',15:'quinze',16:'dezesseis',17:'dezessete',18:'dezoito',19:'dezenove',20:'vinte',30:'trinta',40:'quarenta',50:'cinquenta',60:'sessenta',70:'setenta',80:'oitenta',90:'noventa',100:'cem'};let ext=(n)=>{if(n<=20||base[n])return base[n];if(n<100){let d=Math.floor(n/10)*10,u=n%10;return base[d]+(u?' e '+base[u]:'')}if(n<1000){let c=Math.floor(n/100),r=n%100,cn=c===1?'cento':base[c]+' centos';cn=cn.replace('dois centos','duzentos').replace('três centos','trezentos').replace('quatro centos','quatrocentos').replace('cinco centos','quinhentos').replace('seis centos','seiscentos').replace('sete centos','setecentos').replace('oito centos','oitocentos').replace('nove centos','novecentos');return r?cn+' e '+ext(r):cn}if(n<1000000){let m=Math.floor(n/1000),r=n%1000;return (m===1?'mil':ext(m)+' mil')+(r?' e '+ext(r):'')}return n.toLocaleString('pt-BR')};let txt=ext(reais)+' '+(reais===1?'real':'reais');if(cent)txt+=' e '+ext(cent)+' '+(cent===1?'centavo':'centavos');return txt;},money(v){return Number(v||0).toLocaleString('pt-BR',{style:'currency',currency:'BRL'})},today(){return new Date().toISOString().slice(0,10)},fmt(d){if(!d)return'-';return new Date(d+'T00:00:00').toLocaleDateString('pt-BR')},month(m){if(!m)return'-';let [y,mo]=String(m).split('-');return new Date(Number(y),Number(mo)-1,1).toLocaleDateString('pt-BR',{month:'long',year:'numeric'})},reportMonthLabel(){let src=(this.reportRows[0]?.reference)||String(this.report.from||this.today()).slice(0,7);let label=this.month(src);return String(label).replace(/ de \d{4}/,'').toUpperCase();},download(n,c,t){let a=document.createElement('a');a.href=URL.createObjectURL(new Blob([c],{type:t}));a.download=n;a.click();URL.revokeObjectURL(a.href)},refreshIcons(){setTimeout(()=>window.lucide&&lucide.createIcons(),60)},after(){this.$nextTick(()=>{this.enhanceAccordions();this.refreshIcons();this.renderCharts();setTimeout(()=>this.resizeCharts(),120)})},enhanceAccordions(){try{const scopes=['prestadores','servicos','pagamentos','recibos','busca','config'];scopes.forEach(scope=>{const section=[...document.querySelectorAll('main section')].find(s=>(s.getAttribute('x-show')||'').includes(scope));if(!section)return;const panels=[...section.children].filter(el=>el.classList&&el.classList.contains('glass'));panels.forEach((panel,idx)=>{if(panel.dataset.rcAccordion==='1')return;panel.dataset.rcAccordion='1';panel.classList.add('rc-accordion-panel');const titleEl=panel.querySelector('h3,h2,h4');const title=(titleEl?.textContent||({prestadores:'Prestadores',servicos:'Serviços',pagamentos:'Pagamentos',recibos:'Recibos',busca:'Busca global',config:'Configurações'}[scope]||'Painel')).trim();const subEl=panel.querySelector('p.text-sm,p.text-xs');const sub=(subEl?.textContent||'Clique para abrir ou recolher este painel.').trim();const body=document.createElement('div');body.className='rc-accordion-body';while(panel.firstChild)body.appendChild(panel.firstChild);const head=document.createElement('button');head.type='button';head.className='rc-accordion-head no-print';head.innerHTML=`<span><span class="rc-accordion-kicker">${scope==='config'?'organização':'painel premium

===== db.settings @ 6447681 =====
anager:'Síndico/Tesoureiro',logo:''},
  providerForm:{id:null,name:'',type:'Autônomo',document:'',category:'Jardineiro',phone:'',whatsapp:'',pix:'',notes:''},providerSearch:'',providerCategoryFilter:'',providerSortField:'name',providerSortDir:'asc',
  serviceForm:{providerId:'',category:'Jardineiro',reference:new Date().toISOString().slice(0,7),serviceDate:new Date().toISOString().slice(0,10),dueDate:new Date().toISOString().slice(0,10),amount:0,location:'',description:'',paymentDate:'',paymentMethod:'',paidValue:0},
  recurrenceForm:{id:null,providerId:'',category:'Jardineiro',amount:0,dueDay:5,paymentDay:5,paymentMethod:'PIX',location:'',description:'',active:true},
  serviceFilter:'todos',serviceCategoryFilter:'todas',serviceProviderFilter:'todos',serviceMonthFilter:'',paymentMonthFilter:new Date().toISOString().slice(0,7),paymentStatusFilter:'todos',paymentProviderFilter:'todos',receiptFilter:{q:'',month:'',category:'todas'},receiptServiceId:'',currentReceipt:null,dashboardYear:new Date().getFullYear(),payModal:false,paymentTarget:null,payment:{date:new Date().toISOString().slice(0,10),value:0,method:'PIX'},actionModal:{open:false,type:'',tone:'default',icon:'',target:null,service:null,title:'',subtitle:'',note:'',confirmLabel:'',details:[]},toasts:[],
  report:{from:new Date(new Date().getFullYear(),new Date().getMonth(),1).toISOString().slice(0,10),to:new Date().toISOString().slice(0,10),category:'todas',status:'todos'},passwords:{admin:'',viewer:''},
  async init(){document.documentElement.classList.toggle('dark',this.dark);window.alert=(msg)=>this.toast(String(msg||''),this.toastType(String(msg||'')));if('serviceWorker'in navigator){navigator.serviceWorker.register('./sw.js').catch(console.warn)}await this.seed();this.refreshIcons()},
  async seed(){let s=await db.settings.get('main');if(s)this.settings=s.value;await this.load();},
  async load(){this.providers=await db.providers.toArray();this.services=await db.services.toArray();this.receipts=await db.receipts.toArray();this.recurrences=await db.recurrences.toArray();this.logs=(await db.logs.orderBy('at').reverse().limit(30).toArray());let meta=await db.meta.get('receiptSeq');this.receiptSeqValue=Number(meta?.value||0);if(!this.receiptSeqInput)this.receiptSeqInput=this.nextReceiptNumber();this.after()},
  get isViewer(){return this.auth.role==='viewer'},get title(){return {'dashboard':'Dashboard inicial','prestadores':'Cadastro de prestadores','servicos':'Serviços','pagamentos':'Central de pagamentos','recibos':'Emissão de recibos','relatorios':'Relatórios','busca':'Busca global','config':'Configurações'}[this.view]||'ReciboCondo'},get selectedProvider(){return this.providers.find(p=>Number(p.id)===Number(this.selectedProviderId))||null},
  get cards(){return this.dashboardCards},
  get yearlyRows(){let y=String(this.dashboardYear||new Date().getFullYear());return this.services.filter(s=>String(s.paymentDate||s.serviceDate||'').startsWith(y))},
  get dashboardCards(){let nowRef=new Date().toISOString().slice(0,7), year=this.yearlyRows, month=year.filter(s=>String(s.paymentDate||s.serviceDate||'').slice(0,7)===nowRef), paidYear=year.reduce((a,s)=>a+Number(s.paidValue||0),0), paidMonth=month.reduce((a,s)=>a+Number(s.paidValue||0),0), countMonth=month.filter(s=>Number(s.paidValue||0)>0).length, open=year.reduce((a,s)=>a+this.balance(s),0), top=this.annualCategorySummary[0], topYear=this.topPaidNames(year), topMonth=this.topPaidNames(month), openTop=this.topOpenNames(year), catTop=this.annualCategorySummary.slice(0,3).map(c=>c.category+' • '+this.money(c.total)), activeCats=[...new Set(this.providers.map(p=>p.category).filter(Boolean))].slice(0,4), receiptsMonth=this.receipts.filter(r=>String(r.paymentDate||r.date||'').slice(0,7)===nowRef).length;return[
    {label:'Pago no ano',value:this.money(paidYear),hint:String(this.dashboardYear),icon:'wallet-cards',color:'text-rc-600',tooltipTitle:'Resumo anual',tooltipMain:this.money(paidYear),tooltipTag:'Ano '+this.dashboardYear,tooltip:['Total efetivamente pago no ano selecionado.', 'Top recebimentos: '+(topYear.length?topYear.join(' | '):'sem pagamentos lançados.'), 'Use para comparar o volume anual com o relatório anual premium.']},
    {label:'Pago no mês',value:this.money(paidMonth),hint:this.month(nowRef),icon:'circle-dollar-sign',color:'text-emerald-600',tooltipTitle:'Resumo do mês',tooltipMain:this.money(paidMonth),tooltipTag:'Mês atual',tooltip:['Mês de referência: '+this.month(nowRef)+'.', 'Quem recebeu: '+(topMonth.length?topMonth.join(' | '):'sem pagamentos neste mês.'), 'Ajuda a conferir rapidamente o fechamento mensal.']},
    {label:'Pagamentos no mês',value:countMonth,hint:'registros pagos',icon:'receipt-text',color:'text-blue-600',tooltipTitle:'Movimento mensal',tooltipMain:countMonth+' pagamento(s)',tooltipTag:'Recibos '+receiptsMonth,tooltip:['Valor médio por pagamento: '+this.money(countMonth?paidMonth/countMonth:0)+'.', 'Recibos emitidos no mês: '+receiptsMonth+'.', 'Bom para conferir se todos os pagamentos pagos já têm recibo.']},
    {label:'Prestadores ativos',value:this.providers.length,hint:'cadastrados',icon:'users',color:'text-rc-600',tooltipTitle:'Base de prestadores',tooltipMain:this.providers.length+' cadastro(s)',tooltipTag:'Cadastros',tooltip:['Categorias em uso: '+(activeCats.length?activeCats.join(', '):'nenhuma categoria cadastrada.'), 'Use para localizar quem presta serviço recorrente ou avulso.', 'Dica: mantenha PIX, WhatsApp e categoria sempre atualizados.']},
    {label:'Em aberto no ano',value:this.money(open),hint:'pendente/parcial',icon:'triangle-alert',color:'text-red-600',tooltipTitle:'Pendências do ano',tooltipMain:this.money(open),tooltipTag:'Atenção',tooltip:['Saldo lançado e ainda não quitado no ano.', 'Maiores pendências: '+(openTop.length?openTop.join(' | '):'nenhuma pendência no ano.'), 'Depois de pagar, registre o pagamento antes de emitir o recibo.']},
    {label:'Maior categoria',value:top?top.category:'-',hint:top?this.money(top.tota

===== db.settings @ 6491020 =====
ber+' • '+r.providerName):('ID '+id));await this.load()},
  printReport(){document.body.classList.add('printing-report');document.getElementById('receiptPrint').classList.add('hidden');document.getElementById('annualReportPrint').classList.add('hidden');document.getElementById('reportPrint').classList.remove('hidden');setTimeout(()=>{window.print();document.body.classList.remove('printing-report');document.getElementById('receiptPrint').classList.remove('hidden');document.getElementById('annualReportPrint').classList.add('hidden');document.getElementById('reportPrint').classList.add('hidden')},100)},printAnnualReport(){document.body.classList.add('printing-annual');let rp=document.getElementById('receiptPrint'),mp=document.getElementById('reportPrint'),ap=document.getElementById('annualReportPrint');rp.classList.add('hidden');mp.classList.add('hidden');ap.classList.remove('hidden');ap.style.display='block';ap.style.visibility='visible';setTimeout(()=>{window.print();document.body.classList.remove('printing-annual');rp.classList.remove('hidden');ap.classList.add('hidden');ap.style.display='';ap.style.visibility='';mp.classList.add('hidden')},180)},csv(){let rows=[['Recibo nº','Data do serviço','Data do pagamento','Prestador','Categoria','Descrição','Valor do serviço','Valor deste pagamento','Status','Forma']].concat(this.reportDetailRows.map(s=>[s.receiptNumber,s.serviceDate,s.paymentDate||'',this.providerName(s.providerId),s.category,s.description,s.amount,s.reportValue||0,s.status,s.paymentMethod||'']));let csv=rows.map(r=>r.map(v=>'"'+String(v??'').replaceAll('"','""')+'"').join(';')).join('\n');this.download('relatorio-prestadores-recibocondo.csv',csv,'text/csv;charset=utf-8')},
  async saveSettings(){let plainSettings=JSON.parse(JSON.stringify(this.settings));await db.settings.put({key:'main',value:plainSettings});await this.logAction('settings','Configurações salvas','Dados do condomínio atualizados.');this.toast('Configurações salvas')},async savePasswords(){if(this.isViewer)return;let cur=JSON.parse(localStorage.getItem('rc_passwords')||'{"admin":"123456","viewer":"123456"}');if(this.passwords.admin)cur.admin=this.passwords.admin;if(this.passwords.viewer)cur.viewer=this.passwords.viewer;localStorage.setItem('rc_passwords',JSON.stringify(cur));await this.logAction('settings','Senhas atualizadas','Senhas locais do app foram atualizadas.');this.passwords={admin:'',viewer:''};this.toast('Senhas atualizadas')},async backup(){let now=new Date().toISOString();let seqMeta=await db.meta.get('receiptSeq');let data={settings:this.settings,providers:this.providers,services:this.services,receipts:this.receipts,recurrences:this.recurrences,logs:this.logs,receiptSeq:Number(seqMeta?.value||this.receiptMaxNumber()||0),backupStats:this.backupStats(),exportedAt:now};this.download('backup-recibocondo-prestadores.json',JSON.stringify(data,null,2),'application/json');this.backupLast=now;localStorage.setItem('rc_last_backup',now);await this.logAction('backup','Backup exportado','Backup JSON gerado com '+data.backupStats.records+' registros.')},async restore(ev){let f=ev.target.files[0];if(!f)return;try{let data=JSON.parse(await f.text());if(!data||typeof data!=='object')throw new Error('Backup inválido');let providers=Array.isArray(data.providers)?data.providers:[],services=Array.isArray(data.services)?data.services:[],receipts=Array.isArray(data.receipts)?data.receipts:[],recurrences=Array.isArray(data.recurrences)?data.recurrences:[],logs=Array.isArray(data.logs)?data.logs:[];await db.transaction('rw',db.providers,db.services,db.receipts,db.recurrences,db.logs,db.settings,db.meta,async()=>{await db.providers.clear();await db.services.clear();await db.receipts.clear();await db.recurrences.clear();await db.logs.clear();if(data.settings){let restoredSettings=JSON.parse(JSON.stringify(data.settings));await db.settings.put({key:'main',value:restoredSettings});this.settings=restoredSettings}if(providers.length)await db.providers.bulkAdd(providers);if(services.length)await db.services.bulkAdd(services);if(receipts.length)await db.receipts.bulkAdd(receipts);if(recurrences.length)await db.recurrences.bulkAdd(recurrences);if(logs.length)await db.logs.bulkAdd(logs.map(l=>{let x={...l};delete x.id;return x}).slice(0,200));let restoredMax=receipts.reduce((m,r)=>r.source==='Excel legado'?m:Math.max(m,Number(String(r.number||'').replace(/\D/g,'')||0)),0),safeSeq=Math.max(Number(data.receiptSeq||0),restoredMax);await db.meta.put({key:'receiptSeq',value:safeSeq})});await this.logAction('restore','Backup restaurado','Dados restaurados por arquivo JSON.');await this.load();this.toast('Backup restaurado com sucesso!')}catch(e){console.error(e);this.toast('Não foi possível restaurar o backup: '+e.message,'error')}},async clearAllInternal(){await db.transaction('rw',db.providers,db.services,db.receipts,db.recurrences,db.logs,async()=>{await db.providers.clear();await db.services.clear();await db.receipts.clear();await db.recurrences.clear();await db.logs.clear()});await this.load()},async clearAll(){this.openClearAllModal()},readLogo(e){let f=e.target.files[0];if(!f)return;let r=new FileReader();r.onload=()=>this.settings.logo=r.result;r.readAsDataURL(f)},
  receiptMaxNumber(){return this.receipts.reduce((max,r)=>r.source==='Excel legado'?max:Math.max(max,Number(String(r.number||'').replace(/\D/g,'')||0)),0)},nextReceiptNumber(){return Math.max(Number(this.receiptSeqValue||0),this.receiptMaxNumber())+1},receiptSequenceStatus(){return Number(this.receiptSeqValue||0)<this.receiptMaxNumber()?'Sincronizar':'Sequência OK'},async saveReceiptSequence(){if(this.isViewer)return;let target=Number(this.receiptSeqInput||0), max=this.receiptMaxNumber();if(!target||target<1)return this.toast('Informe um próximo número válido.','warning');if(target<=max)return this.toast('O número precisa ser maior que o último emitido: '+String(max).padStart(6,'0')+'.','warning');await db.meta.put({key:'receiptSeq',value:target-1});this.receiptSeqValue=

===== db.settings @ 6492833 =====
ut({key:'main',value:plainSettings});await this.logAction('settings','Configurações salvas','Dados do condomínio atualizados.');this.toast('Configurações salvas')},async savePasswords(){if(this.isViewer)return;let cur=JSON.parse(localStorage.getItem('rc_passwords')||'{"admin":"123456","viewer":"123456"}');if(this.passwords.admin)cur.admin=this.passwords.admin;if(this.passwords.viewer)cur.viewer=this.passwords.viewer;localStorage.setItem('rc_passwords',JSON.stringify(cur));await this.logAction('settings','Senhas atualizadas','Senhas locais do app foram atualizadas.');this.passwords={admin:'',viewer:''};this.toast('Senhas atualizadas')},async backup(){let now=new Date().toISOString();let seqMeta=await db.meta.get('receiptSeq');let data={settings:this.settings,providers:this.providers,services:this.services,receipts:this.receipts,recurrences:this.recurrences,logs:this.logs,receiptSeq:Number(seqMeta?.value||this.receiptMaxNumber()||0),backupStats:this.backupStats(),exportedAt:now};this.download('backup-recibocondo-prestadores.json',JSON.stringify(data,null,2),'application/json');this.backupLast=now;localStorage.setItem('rc_last_backup',now);await this.logAction('backup','Backup exportado','Backup JSON gerado com '+data.backupStats.records+' registros.')},async restore(ev){let f=ev.target.files[0];if(!f)return;try{let data=JSON.parse(await f.text());if(!data||typeof data!=='object')throw new Error('Backup inválido');let providers=Array.isArray(data.providers)?data.providers:[],services=Array.isArray(data.services)?data.services:[],receipts=Array.isArray(data.receipts)?data.receipts:[],recurrences=Array.isArray(data.recurrences)?data.recurrences:[],logs=Array.isArray(data.logs)?data.logs:[];await db.transaction('rw',db.providers,db.services,db.receipts,db.recurrences,db.logs,db.settings,db.meta,async()=>{await db.providers.clear();await db.services.clear();await db.receipts.clear();await db.recurrences.clear();await db.logs.clear();if(data.settings){let restoredSettings=JSON.parse(JSON.stringify(data.settings));await db.settings.put({key:'main',value:restoredSettings});this.settings=restoredSettings}if(providers.length)await db.providers.bulkAdd(providers);if(services.length)await db.services.bulkAdd(services);if(receipts.length)await db.receipts.bulkAdd(receipts);if(recurrences.length)await db.recurrences.bulkAdd(recurrences);if(logs.length)await db.logs.bulkAdd(logs.map(l=>{let x={...l};delete x.id;return x}).slice(0,200));let restoredMax=receipts.reduce((m,r)=>r.source==='Excel legado'?m:Math.max(m,Number(String(r.number||'').replace(/\D/g,'')||0)),0),safeSeq=Math.max(Number(data.receiptSeq||0),restoredMax);await db.meta.put({key:'receiptSeq',value:safeSeq})});await this.logAction('restore','Backup restaurado','Dados restaurados por arquivo JSON.');await this.load();this.toast('Backup restaurado com sucesso!')}catch(e){console.error(e);this.toast('Não foi possível restaurar o backup: '+e.message,'error')}},async clearAllInternal(){await db.transaction('rw',db.providers,db.services,db.receipts,db.recurrences,db.logs,async()=>{await db.providers.clear();await db.services.clear();await db.receipts.clear();await db.recurrences.clear();await db.logs.clear()});await this.load()},async clearAll(){this.openClearAllModal()},readLogo(e){let f=e.target.files[0];if(!f)return;let r=new FileReader();r.onload=()=>this.settings.logo=r.result;r.readAsDataURL(f)},
  receiptMaxNumber(){return this.receipts.reduce((max,r)=>r.source==='Excel legado'?max:Math.max(max,Number(String(r.number||'').replace(/\D/g,'')||0)),0)},nextReceiptNumber(){return Math.max(Number(this.receiptSeqValue||0),this.receiptMaxNumber())+1},receiptSequenceStatus(){return Number(this.receiptSeqValue||0)<this.receiptMaxNumber()?'Sincronizar':'Sequência OK'},async saveReceiptSequence(){if(this.isViewer)return;let target=Number(this.receiptSeqInput||0), max=this.receiptMaxNumber();if(!target||target<1)return this.toast('Informe um próximo número válido.','warning');if(target<=max)return this.toast('O número precisa ser maior que o último emitido: '+String(max).padStart(6,'0')+'.','warning');await db.meta.put({key:'receiptSeq',value:target-1});this.receiptSeqValue=target-1;await this.logAction('settings','Sequência de recibos ajustada','Próximo recibo definido para nº '+String(target).padStart(6,'0'));this.toast('Sequência atualizada. Próximo recibo: '+String(target).padStart(6,'0'));await this.load()},
  topPaidNames(rows){let map={};rows.forEach(s=>{let v=Number(s.paidValue||0);if(v<=0)return;let name=this.providerName(s.providerId);map[name]=(map[name]||0)+v});return Object.entries(map).sort((a,b)=>b[1]-a[1]).slice(0,3).map(([name,total])=>name+' • '+this.money(total))},topOpenNames(rows){let map={};rows.forEach(s=>{let v=this.balance(s);if(v<=0)return;let name=this.providerName(s.providerId);map[name]=(map[name]||0)+v});return Object.entries(map).sort((a,b)=>b[1]-a[1]).slice(0,3).map(([name,total])=>name+' • '+this.money(total))},
  providerServices(id){return this.services.filter(s=>Number(s.providerId)===Number(id)).sort((a,b)=>String(b.serviceDate||'').localeCompare(String(a.serviceDate||'')))},providerReceipts(id){return this.receipts.filter(r=>Number(r.providerId)===Number(id)).sort((a,b)=>String(b.date||'').localeCompare(String(a.date||'')))},providerLastServiceLabel(id){let s=this.providerServices(id)[0];return s?this.fmt(s.serviceDate)+' • '+this.money(s.paidValue||s.amount):'-'},async logAction(type,title,detail){try{await db.logs.add({at:new Date().toISOString(),type,title,detail,role:this.auth?.role||'sistema'});this.logs=(await db.logs.orderBy('at').reverse().limit(30).toArray())}catch(e){console.warn('logAction',e)}},logIcon(type){return {payment:'circle-dollar-sign',receipt:'receipt-text',service:'clipboard-list',provider:'users',recurrence:'repeat-2',backup:'shield-check',restore:'rotate-ccw',danger:'triangle-alert',settings:'settings',delete:'trash-2'}[type]||'history'},fmtDateTime(d){if(!d)return'-';return new Date(d).toL

===== db.settings @ 6493080 =====
em('rc_passwords')||'{"admin":"123456","viewer":"123456"}');if(this.passwords.admin)cur.admin=this.passwords.admin;if(this.passwords.viewer)cur.viewer=this.passwords.viewer;localStorage.setItem('rc_passwords',JSON.stringify(cur));await this.logAction('settings','Senhas atualizadas','Senhas locais do app foram atualizadas.');this.passwords={admin:'',viewer:''};this.toast('Senhas atualizadas')},async backup(){let now=new Date().toISOString();let seqMeta=await db.meta.get('receiptSeq');let data={settings:this.settings,providers:this.providers,services:this.services,receipts:this.receipts,recurrences:this.recurrences,logs:this.logs,receiptSeq:Number(seqMeta?.value||this.receiptMaxNumber()||0),backupStats:this.backupStats(),exportedAt:now};this.download('backup-recibocondo-prestadores.json',JSON.stringify(data,null,2),'application/json');this.backupLast=now;localStorage.setItem('rc_last_backup',now);await this.logAction('backup','Backup exportado','Backup JSON gerado com '+data.backupStats.records+' registros.')},async restore(ev){let f=ev.target.files[0];if(!f)return;try{let data=JSON.parse(await f.text());if(!data||typeof data!=='object')throw new Error('Backup inválido');let providers=Array.isArray(data.providers)?data.providers:[],services=Array.isArray(data.services)?data.services:[],receipts=Array.isArray(data.receipts)?data.receipts:[],recurrences=Array.isArray(data.recurrences)?data.recurrences:[],logs=Array.isArray(data.logs)?data.logs:[];await db.transaction('rw',db.providers,db.services,db.receipts,db.recurrences,db.logs,db.settings,db.meta,async()=>{await db.providers.clear();await db.services.clear();await db.receipts.clear();await db.recurrences.clear();await db.logs.clear();if(data.settings){let restoredSettings=JSON.parse(JSON.stringify(data.settings));await db.settings.put({key:'main',value:restoredSettings});this.settings=restoredSettings}if(providers.length)await db.providers.bulkAdd(providers);if(services.length)await db.services.bulkAdd(services);if(receipts.length)await db.receipts.bulkAdd(receipts);if(recurrences.length)await db.recurrences.bulkAdd(recurrences);if(logs.length)await db.logs.bulkAdd(logs.map(l=>{let x={...l};delete x.id;return x}).slice(0,200));let restoredMax=receipts.reduce((m,r)=>r.source==='Excel legado'?m:Math.max(m,Number(String(r.number||'').replace(/\D/g,'')||0)),0),safeSeq=Math.max(Number(data.receiptSeq||0),restoredMax);await db.meta.put({key:'receiptSeq',value:safeSeq})});await this.logAction('restore','Backup restaurado','Dados restaurados por arquivo JSON.');await this.load();this.toast('Backup restaurado com sucesso!')}catch(e){console.error(e);this.toast('Não foi possível restaurar o backup: '+e.message,'error')}},async clearAllInternal(){await db.transaction('rw',db.providers,db.services,db.receipts,db.recurrences,db.logs,async()=>{await db.providers.clear();await db.services.clear();await db.receipts.clear();await db.recurrences.clear();await db.logs.clear()});await this.load()},async clearAll(){this.openClearAllModal()},readLogo(e){let f=e.target.files[0];if(!f)return;let r=new FileReader();r.onload=()=>this.settings.logo=r.result;r.readAsDataURL(f)},
  receiptMaxNumber(){return this.receipts.reduce((max,r)=>r.source==='Excel legado'?max:Math.max(max,Number(String(r.number||'').replace(/\D/g,'')||0)),0)},nextReceiptNumber(){return Math.max(Number(this.receiptSeqValue||0),this.receiptMaxNumber())+1},receiptSequenceStatus(){return Number(this.receiptSeqValue||0)<this.receiptMaxNumber()?'Sincronizar':'Sequência OK'},async saveReceiptSequence(){if(this.isViewer)return;let target=Number(this.receiptSeqInput||0), max=this.receiptMaxNumber();if(!target||target<1)return this.toast('Informe um próximo número válido.','warning');if(target<=max)return this.toast('O número precisa ser maior que o último emitido: '+String(max).padStart(6,'0')+'.','warning');await db.meta.put({key:'receiptSeq',value:target-1});this.receiptSeqValue=target-1;await this.logAction('settings','Sequência de recibos ajustada','Próximo recibo definido para nº '+String(target).padStart(6,'0'));this.toast('Sequência atualizada. Próximo recibo: '+String(target).padStart(6,'0'));await this.load()},
  topPaidNames(rows){let map={};rows.forEach(s=>{let v=Number(s.paidValue||0);if(v<=0)return;let name=this.providerName(s.providerId);map[name]=(map[name]||0)+v});return Object.entries(map).sort((a,b)=>b[1]-a[1]).slice(0,3).map(([name,total])=>name+' • '+this.money(total))},topOpenNames(rows){let map={};rows.forEach(s=>{let v=this.balance(s);if(v<=0)return;let name=this.providerName(s.providerId);map[name]=(map[name]||0)+v});return Object.entries(map).sort((a,b)=>b[1]-a[1]).slice(0,3).map(([name,total])=>name+' • '+this.money(total))},
  providerServices(id){return this.services.filter(s=>Number(s.providerId)===Number(id)).sort((a,b)=>String(b.serviceDate||'').localeCompare(String(a.serviceDate||'')))},providerReceipts(id){return this.receipts.filter(r=>Number(r.providerId)===Number(id)).sort((a,b)=>String(b.date||'').localeCompare(String(a.date||'')))},providerLastServiceLabel(id){let s=this.providerServices(id)[0];return s?this.fmt(s.serviceDate)+' • '+this.money(s.paidValue||s.amount):'-'},async logAction(type,title,detail){try{await db.logs.add({at:new Date().toISOString(),type,title,detail,role:this.auth?.role||'sistema'});this.logs=(await db.logs.orderBy('at').reverse().limit(30).toArray())}catch(e){console.warn('logAction',e)}},logIcon(type){return {payment:'circle-dollar-sign',receipt:'receipt-text',service:'clipboard-list',provider:'users',recurrence:'repeat-2',backup:'shield-check',restore:'rotate-ccw',danger:'triangle-alert',settings:'settings',delete:'trash-2'}[type]||'history'},fmtDateTime(d){if(!d)return'-';return new Date(d).toLocaleString('pt-BR')},async clearLogs(){if(this.isViewer)return;await db.logs.clear();this.logs=[];this.after()},providerSpend(id){return this.services.filter(s=>Number(s.providerId)===Number(id)).reduce((a,s)=>a+Number(s.paidValue||s.amount||0),0

===== serviceId @ 6458850 =====
(a,b)=>String(b.paymentDate||b.serviceDate).localeCompare(String(a.paymentDate||a.serviceDate)))},
  get servicePanelOpenRows(){return this.servicePanelRows.filter(s=>this.balance(s)>0).sort((a,b)=>String(a.dueDate||'2999-12-31').localeCompare(String(b.dueDate||'2999-12-31')))},
  get servicePanelTotals(){let expected=this.servicePanelRows.reduce((a,s)=>a+Number(s.amount||0),0),paid=this.servicePanelRows.reduce((a,s)=>a+Number(s.paidValue||0),0);return{expected,paid,open:Math.max(0,expected-paid)}},
  get servicePanelRecurringGenerated(){return this.recurrences.filter(r=>r.active&&this.recurrenceGenerated(r,this.servicePanelMonth))},
  get servicePanelPendingRecurrences(){return this.recurrences.filter(r=>r.active&&!this.recurrenceGenerated(r,this.servicePanelMonth))},
  get servicePanelReceiptCount(){return this.receipts.filter(r=>String(r.paymentDate||r.date||'').slice(0,7)===this.servicePanelMonth).length},
  get filteredReceipts(){let q=String(this.receiptFilter.q||'').toLowerCase().trim();return this.receipts.filter(r=>{let txt=[r.number,r.providerName,r.category,r.description,r.paymentMethod,r.total].join(' ').toLowerCase();return(!q||txt.includes(q))&&(!this.receiptFilter.month||r.reference===this.receiptFilter.month)&&(this.receiptFilter.category==='todas'||r.category===this.receiptFilter.category)}).sort((a,b)=>String(b.date||'').localeCompare(String(a.date||''))||String(b.number||'').localeCompare(String(a.number||'')))},
  get reportRows(){return this.services.filter(s=>(!this.report.from||s.serviceDate>=this.report.from)&&(!this.report.to||s.serviceDate<=this.report.to)&&(this.report.category==='todas'||s.category===this.report.category)&&(this.report.status==='todos'||s.status===this.report.status)).sort((a,b)=>{let nums=x=>this.receipts.filter(r=>Number(r.serviceId)===Number(x.id)).map(r=>Number(String(r.number||'').replace(/\D/g,''))||0).filter(Boolean),aa=nums(a),bb=nums(b),na=aa.length?Math.min(...aa):0,nb=bb.length?Math.min(...bb):0;if(na&&nb&&na!==nb)return na-nb;if(na&&!nb)return-1;if(!na&&nb)return 1;return String(a.serviceDate||'').localeCompare(String(b.serviceDate||''))||Number(a.id||0)-Number(b.id||0)})},
  get reportDetailRows(){let rows=[];for(const s of this.reportRows){let rs=this.receipts.filter(r=>Number(r.serviceId)===Number(s.id)).sort((a,b)=>(Number(String(a.number||'').replace(/\D/g,''))||0)-(Number(String(b.number||'').replace(/\D/g,''))||0)),sum=0;for(const r of rs){let v=Number(r.total||0);sum+=v;rows.push({...s,reportKey:'r-'+r.id,receiptNumber:r.number||'-',paymentDate:r.paymentDate||r.date||s.paymentDate,description:r.description||s.description,reportValue:v,paymentMethod:r.paymentMethod||s.paymentMethod||''})}let u=Math.max(0,Number(s.paidValue||0)-sum);if(u>0)rows.push({...s,reportKey:'u-'+s.id,receiptNumber:'PENDENTE',paymentDate:s.paymentDate,description:s.description,reportValue:u,paymentMethod:s.paymentMethod||''});if(!rs.length&&u<=0)rows.push({...s,reportKey:'s-'+s.id,receiptNumber:'-',description:s.description,reportValue:Number(s.paidValue||s.amount||0),paymentMethod:s.paymentMethod||''})}return rows},get reportTotals(){let e=this.reportRows.reduce((a,s)=>a+Number(s.amount||0),0),p=this.reportRows.reduce((a,s)=>a+Number(s.paidValue||0),0);return{expected:e,paid:p,open:e-p}},backupStats(){let records=this.providers.length+this.services.length+this.receipts.length+this.recurrences.length;return{records,providers:this.providers.length,services:this.services.length,receipts:this.receipts.length,recurrences:this.recurrences.length}},backupDays(){if(!this.backupLast)return null;let d=new Date(this.backupLast), now=new Date();return Math.max(0,Math.floor((now-d)/86400000))},backupLastLabel(){if(!this.backupLast)return'Nunca feito';let days=this.backupDays();let date=new Date(this.backupLast).toLocaleString('pt-BR');return days===0?'Hoje • '+date:days+' dia(s) • '+date},backupHealthLabel(){let d=this.backupDays();if(d===null)return'Backup pendente';if(d<=7)return'Protegido';if(d<=15)return'Atenção';return'Backup atrasado'},backupHealthClass(){let d=this.backupDays();if(d===null||d>15)return'bg-red-500/10 text-red-700';if(d>7)return'bg-amber-500/10 text-amber-700';return'bg-emerald-500/10 text-emerald-700'},backupHint(){let s=this.backupStats();let d=this.backupDays();if(d===null)return'Você ainda não exportou um backup. Recomendo gerar um JSON antes de continuar cadastrando dados importantes.';if(d>15)return'Seu último backup já passou de 15 dias. Exporte um novo JSON para proteger '+s.records+' registros locais.';if(d>7)return'Backup existente, mas já vale gerar uma cópia atualizada com os últimos lançamentos e recibos.';return'Backup em dia. Mesmo assim, exporte uma nova cópia sempre antes de grandes alterações.'},
  get topProviders(){let y=new Date().getFullYear(), map={};this.services.filter(s=>String(s.serviceDate||'').startsWith(String(y))).forEach(s=>{let id=s.providerId,p=this.providers.find(x=>Number(x.id)===Number(id))||{};if(!map[id])map[id]={id,name:p.name||'-',category:p.category||s.category,count:0,total:0,open:0};map[id].count++;map[id].total+=Number(s.paidValue||s.amount||0);map[id].open+=this.balance(s)});return Object.values(map).sort((a,b)=>b.total-a.total).slice(0,8)},
  get categorySummary(){let total=this.services.reduce((a,s)=>a+Number(s.paidValue||s.amount||0),0)||1;return this.categories.map(category=>{let val=this.services.filter(s=>s.category===category).reduce((a,s)=>a+Number(s.paidValue||s.amount||0),0);return{category,total:val,percent:Math.min(100,Math.round(val/total*100))}}).filter(x=>x.total>0).sort((a,b)=>b.total-a.total)},
  get criticalOpen(){return this.openServices.slice().sort((a,b)=>new Date(a.dueDate||'2999-12-31')-new Date(b.dueDate||'2999-12-31')).slice(0,8)},
  get notifications(){const now=this.today();let pend=this.services.filter(s=>s.status!=='Pago'&&s.dueDate<=now).slice(0,3).map(s=>({id:s.id,title:'Pagamento pendente',body:this.providerName(s.providerId)+' • '+s.category+' • '+this.money(this.b

===== serviceId @ 6459327 =====
ath.max(0,expected-paid)}},
  get servicePanelRecurringGenerated(){return this.recurrences.filter(r=>r.active&&this.recurrenceGenerated(r,this.servicePanelMonth))},
  get servicePanelPendingRecurrences(){return this.recurrences.filter(r=>r.active&&!this.recurrenceGenerated(r,this.servicePanelMonth))},
  get servicePanelReceiptCount(){return this.receipts.filter(r=>String(r.paymentDate||r.date||'').slice(0,7)===this.servicePanelMonth).length},
  get filteredReceipts(){let q=String(this.receiptFilter.q||'').toLowerCase().trim();return this.receipts.filter(r=>{let txt=[r.number,r.providerName,r.category,r.description,r.paymentMethod,r.total].join(' ').toLowerCase();return(!q||txt.includes(q))&&(!this.receiptFilter.month||r.reference===this.receiptFilter.month)&&(this.receiptFilter.category==='todas'||r.category===this.receiptFilter.category)}).sort((a,b)=>String(b.date||'').localeCompare(String(a.date||''))||String(b.number||'').localeCompare(String(a.number||'')))},
  get reportRows(){return this.services.filter(s=>(!this.report.from||s.serviceDate>=this.report.from)&&(!this.report.to||s.serviceDate<=this.report.to)&&(this.report.category==='todas'||s.category===this.report.category)&&(this.report.status==='todos'||s.status===this.report.status)).sort((a,b)=>{let nums=x=>this.receipts.filter(r=>Number(r.serviceId)===Number(x.id)).map(r=>Number(String(r.number||'').replace(/\D/g,''))||0).filter(Boolean),aa=nums(a),bb=nums(b),na=aa.length?Math.min(...aa):0,nb=bb.length?Math.min(...bb):0;if(na&&nb&&na!==nb)return na-nb;if(na&&!nb)return-1;if(!na&&nb)return 1;return String(a.serviceDate||'').localeCompare(String(b.serviceDate||''))||Number(a.id||0)-Number(b.id||0)})},
  get reportDetailRows(){let rows=[];for(const s of this.reportRows){let rs=this.receipts.filter(r=>Number(r.serviceId)===Number(s.id)).sort((a,b)=>(Number(String(a.number||'').replace(/\D/g,''))||0)-(Number(String(b.number||'').replace(/\D/g,''))||0)),sum=0;for(const r of rs){let v=Number(r.total||0);sum+=v;rows.push({...s,reportKey:'r-'+r.id,receiptNumber:r.number||'-',paymentDate:r.paymentDate||r.date||s.paymentDate,description:r.description||s.description,reportValue:v,paymentMethod:r.paymentMethod||s.paymentMethod||''})}let u=Math.max(0,Number(s.paidValue||0)-sum);if(u>0)rows.push({...s,reportKey:'u-'+s.id,receiptNumber:'PENDENTE',paymentDate:s.paymentDate,description:s.description,reportValue:u,paymentMethod:s.paymentMethod||''});if(!rs.length&&u<=0)rows.push({...s,reportKey:'s-'+s.id,receiptNumber:'-',description:s.description,reportValue:Number(s.paidValue||s.amount||0),paymentMethod:s.paymentMethod||''})}return rows},get reportTotals(){let e=this.reportRows.reduce((a,s)=>a+Number(s.amount||0),0),p=this.reportRows.reduce((a,s)=>a+Number(s.paidValue||0),0);return{expected:e,paid:p,open:e-p}},backupStats(){let records=this.providers.length+this.services.length+this.receipts.length+this.recurrences.length;return{records,providers:this.providers.length,services:this.services.length,receipts:this.receipts.length,recurrences:this.recurrences.length}},backupDays(){if(!this.backupLast)return null;let d=new Date(this.backupLast), now=new Date();return Math.max(0,Math.floor((now-d)/86400000))},backupLastLabel(){if(!this.backupLast)return'Nunca feito';let days=this.backupDays();let date=new Date(this.backupLast).toLocaleString('pt-BR');return days===0?'Hoje • '+date:days+' dia(s) • '+date},backupHealthLabel(){let d=this.backupDays();if(d===null)return'Backup pendente';if(d<=7)return'Protegido';if(d<=15)return'Atenção';return'Backup atrasado'},backupHealthClass(){let d=this.backupDays();if(d===null||d>15)return'bg-red-500/10 text-red-700';if(d>7)return'bg-amber-500/10 text-amber-700';return'bg-emerald-500/10 text-emerald-700'},backupHint(){let s=this.backupStats();let d=this.backupDays();if(d===null)return'Você ainda não exportou um backup. Recomendo gerar um JSON antes de continuar cadastrando dados importantes.';if(d>15)return'Seu último backup já passou de 15 dias. Exporte um novo JSON para proteger '+s.records+' registros locais.';if(d>7)return'Backup existente, mas já vale gerar uma cópia atualizada com os últimos lançamentos e recibos.';return'Backup em dia. Mesmo assim, exporte uma nova cópia sempre antes de grandes alterações.'},
  get topProviders(){let y=new Date().getFullYear(), map={};this.services.filter(s=>String(s.serviceDate||'').startsWith(String(y))).forEach(s=>{let id=s.providerId,p=this.providers.find(x=>Number(x.id)===Number(id))||{};if(!map[id])map[id]={id,name:p.name||'-',category:p.category||s.category,count:0,total:0,open:0};map[id].count++;map[id].total+=Number(s.paidValue||s.amount||0);map[id].open+=this.balance(s)});return Object.values(map).sort((a,b)=>b.total-a.total).slice(0,8)},
  get categorySummary(){let total=this.services.reduce((a,s)=>a+Number(s.paidValue||s.amount||0),0)||1;return this.categories.map(category=>{let val=this.services.filter(s=>s.category===category).reduce((a,s)=>a+Number(s.paidValue||s.amount||0),0);return{category,total:val,percent:Math.min(100,Math.round(val/total*100))}}).filter(x=>x.total>0).sort((a,b)=>b.total-a.total)},
  get criticalOpen(){return this.openServices.slice().sort((a,b)=>new Date(a.dueDate||'2999-12-31')-new Date(b.dueDate||'2999-12-31')).slice(0,8)},
  get notifications(){const now=this.today();let pend=this.services.filter(s=>s.status!=='Pago'&&s.dueDate<=now).slice(0,3).map(s=>({id:s.id,title:'Pagamento pendente',body:this.providerName(s.providerId)+' • '+s.category+' • '+this.money(this.balance(s))}));let rec=this.recurrences.filter(r=>r.active&&!this.recurrenceGenerated(r)).slice(0,2).map(r=>({id:'r'+r.id,title:'Recorrência do mês pendente',body:this.providerName(r.providerId)+' • '+r.category+' • '+this.money(r.amount)}));return[...rec,...pend].slice(0,4)},
  get dayOverdue(){let t=this.today();return this.services.filter(s=>s.status!=='Pago'&&s.dueDate&&s.dueDate<t).sort((a,b)=>String(a.dueDate).localeCompare(String(b.dueDate)))},
  get dayDueToday(){le

===== serviceId @ 6480113 =====
+' • '+this.money(d.amount||0));this.serviceForm={...this.serviceForm,amount:0,location:'',description:'',paymentDate:'',paymentMethod:'',paidValue:0};await this.load();this.toast('Lançamento salvo')},clearServiceFilters(){this.serviceFilter='todos';this.serviceCategoryFilter='todas';this.serviceProviderFilter='todos';this.serviceMonthFilter=''},clearReceiptFilters(){this.receiptFilter={q:'',month:'',category:'todas'}},fillServiceFromProvider(p){this.serviceForm.providerId=p.id;this.serviceForm.category=p.category||this.serviceForm.category;this.view='servicos';this.after()},openDuplicateModal(s){if(this.isViewer)return;this.actionModal={open:true,type:'duplicate',tone:'default',icon:'copy-plus',target:s,service:s,title:'Duplicar lançamento',subtitle:'Criar uma nova cópia para a referência atual.',note:'O novo lançamento será criado como pendente, sem pagamento e sem recibo, preservando a numeração oficial.',confirmLabel:'Duplicar',details:[{label:'Prestador',value:this.providerName(s.providerId)},{label:'Categoria',value:s.category||'-'},{label:'Valor',value:this.money(s.amount||0)}]};this.after()},openDeleteServiceModal(s){if(this.isViewer)return;this.actionModal={open:true,type:'deleteService',tone:'danger',icon:'trash-2',target:s,service:s,title:'Excluir lançamento',subtitle:'Essa ação remove apenas este lançamento da lista de serviços.',note:'Lançamentos com pagamento ou recibo vinculado não podem ser excluídos por aqui. Use o fluxo de pagamentos/recibos para preservar o histórico.',confirmLabel:'Excluir',details:[{label:'Prestador',value:this.providerName(s.providerId)},{label:'Categoria',value:s.category||'-'},{label:'Valor',value:this.money(s.amount||0)}]};this.after()},openDeletePaymentModal(s){if(this.isViewer)return;let receipt=this.receipts.find(r=>Number(r.serviceId)===Number(s.id));this.actionModal={open:true,type:'deletePayment',tone:'danger',icon:'circle-x',target:s,service:s,title:'Excluir todos os pagamentos',subtitle:'Essa ação zera todo o valor pago deste lançamento.',note:receipt?'O recibo vinculado nº '+receipt.number+' também será removido. O lançamento será mantido e voltará como Pendente; a numeração do recibo não será reaproveitada.':'O pagamento será removido, mas o lançamento será mantido e voltará como Pendente.',confirmLabel:'Excluir pagamentos',details:[{label:'Prestador',value:this.providerName(s.providerId)},{label:'Valor pago',value:this.money(s.paidValue||0)},{label:'Data/forma',value:(s.paymentDate?this.fmt(s.paymentDate):'-')+' • '+(s.paymentMethod||'não informada')},{label:'Recibo vinculado',value:receipt?receipt.number:'não emitido'}]};this.after()},openDeleteReceiptModal(r){if(this.isViewer)return;this.actionModal={open:true,type:'deleteReceipt',tone:'danger',icon:'receipt-text',target:r,service:null,title:'Excluir recibo',subtitle:'Essa ação remove o recibo do histórico.',note:'A numeração já utilizada não será reaproveitada, para manter a rastreabilidade da prestação de contas.',confirmLabel:'Excluir recibo',details:[{label:'Recibo nº',value:r.number||'-'},{label:'Prestador',value:r.providerName||'-'},{label:'Valor',value:this.money(r.total||0)}]};this.after()},openClearAllModal(){if(this.isViewer)return;this.actionModal={open:true,type:'clearAll',tone:'danger',icon:'database-zap',target:null,service:null,title:'Apagar dados operacionais',subtitle:'Essa ação remove os registros operacionais salvos neste navegador.',note:'A numeração oficial dos recibos e os dados do condomínio serão preservados. Faça backup JSON antes de apagar, caso precise recuperar os registros depois.',confirmLabel:'Apagar registros',details:[{label:'Remove',value:'Prestadores, serviços, recibos, recorrências e logs'},{label:'Preserva',value:'Dados do condomínio e sequência dos recibos'}]};this.after()},closeActionModal(){this.actionModal={open:false,type:'',tone:'default',icon:'',target:null,service:null,title:'',subtitle:'',note:'',confirmLabel:'',details:[]}},async confirmActionModal(){if(this.isViewer)return;let m=this.actionModal,t=m.target||m.service;if(m.type==='duplicate'){await this.duplicateServiceInternal(m.service)}else if(m.type==='deleteService'){await this.removeServiceInternal(t.id)}else if(m.type==='deletePayment'){await this.removePaymentInternal(t.id)}else if(m.type==='deleteProvider'){await this.removeProviderInternal(t.id)}else if(m.type==='deleteReceipt'){await this.removeReceiptInternal(t.id)}else if(m.type==='deleteRecurrence'){await this.removeRecurrenceInternal(t.id)}else if(m.type==='clearAll'){await this.clearAllInternal()}this.closeActionModal()},async duplicateServiceInternal(s){let d={...s};delete d.id;d.status='Pendente';d.paidValue=0;d.paymentDate='';d.paymentMethod='';d.serviceDate=this.today();d.dueDate=this.today();d.reference=this.today().slice(0,7);await db.services.add(d);await this.logAction('service','Lançamento duplicado',this.providerName(d.providerId)+' • '+this.money(d.amount||0));await this.load()},async removeServiceInternal(id){let s=this.services.find(x=>Number(x.id)===Number(id));if(!s)return;let linked=this.receipts.find(r=>Number(r.serviceId)===Number(id));if(linked||Number(s.paidValue||0)>0){let linkedCount=this.receipts.filter(r=>Number(r.serviceId)===Number(id)).length;return this.toast(linkedCount>1?'Este lançamento possui '+linkedCount+' recibos vinculados. Exclua pelo fluxo de pagamentos/recibos para preservar o histórico.':linked?'Este lançamento possui o recibo nº '+linked.number+'. Exclua pelo fluxo de pagamento/recibo para preservar o histórico.':'Este lançamento possui pagamento registrado. Exclua pelo fluxo de pagamentos para preservar o histórico.','warning')}await db.services.delete(id);await this.logAction('delete','Lançamento excluído',this.providerName(s.providerId)+' • '+this.money(s.amount||0));await this.load()},async removePaymentInternal(id){let s=this.services.find(x=>Number(x.id)===Number(id));if(!s)return;let linked=this.receipts.filter(r=>Number(r.serviceId)===Number(id));await db.transaction('rw',d

===== serviceId @ 6483403 =====
ull,service:null,title:'Apagar dados operacionais',subtitle:'Essa ação remove os registros operacionais salvos neste navegador.',note:'A numeração oficial dos recibos e os dados do condomínio serão preservados. Faça backup JSON antes de apagar, caso precise recuperar os registros depois.',confirmLabel:'Apagar registros',details:[{label:'Remove',value:'Prestadores, serviços, recibos, recorrências e logs'},{label:'Preserva',value:'Dados do condomínio e sequência dos recibos'}]};this.after()},closeActionModal(){this.actionModal={open:false,type:'',tone:'default',icon:'',target:null,service:null,title:'',subtitle:'',note:'',confirmLabel:'',details:[]}},async confirmActionModal(){if(this.isViewer)return;let m=this.actionModal,t=m.target||m.service;if(m.type==='duplicate'){await this.duplicateServiceInternal(m.service)}else if(m.type==='deleteService'){await this.removeServiceInternal(t.id)}else if(m.type==='deletePayment'){await this.removePaymentInternal(t.id)}else if(m.type==='deleteProvider'){await this.removeProviderInternal(t.id)}else if(m.type==='deleteReceipt'){await this.removeReceiptInternal(t.id)}else if(m.type==='deleteRecurrence'){await this.removeRecurrenceInternal(t.id)}else if(m.type==='clearAll'){await this.clearAllInternal()}this.closeActionModal()},async duplicateServiceInternal(s){let d={...s};delete d.id;d.status='Pendente';d.paidValue=0;d.paymentDate='';d.paymentMethod='';d.serviceDate=this.today();d.dueDate=this.today();d.reference=this.today().slice(0,7);await db.services.add(d);await this.logAction('service','Lançamento duplicado',this.providerName(d.providerId)+' • '+this.money(d.amount||0));await this.load()},async removeServiceInternal(id){let s=this.services.find(x=>Number(x.id)===Number(id));if(!s)return;let linked=this.receipts.find(r=>Number(r.serviceId)===Number(id));if(linked||Number(s.paidValue||0)>0){let linkedCount=this.receipts.filter(r=>Number(r.serviceId)===Number(id)).length;return this.toast(linkedCount>1?'Este lançamento possui '+linkedCount+' recibos vinculados. Exclua pelo fluxo de pagamentos/recibos para preservar o histórico.':linked?'Este lançamento possui o recibo nº '+linked.number+'. Exclua pelo fluxo de pagamento/recibo para preservar o histórico.':'Este lançamento possui pagamento registrado. Exclua pelo fluxo de pagamentos para preservar o histórico.','warning')}await db.services.delete(id);await this.logAction('delete','Lançamento excluído',this.providerName(s.providerId)+' • '+this.money(s.amount||0));await this.load()},async removePaymentInternal(id){let s=this.services.find(x=>Number(x.id)===Number(id));if(!s)return;let linked=this.receipts.filter(r=>Number(r.serviceId)===Number(id));await db.transaction('rw',db.receipts,db.services,async()=>{for(const r of linked)await db.receipts.delete(r.id);await db.services.update(id,{paidValue:0,paymentDate:'',paymentMethod:'',status:'Pendente'})});for(const r of linked)await this.logAction('delete','Recibo removido por exclusão de pagamento','Nº '+r.number+' • '+(r.providerName||this.providerName(s.providerId)));await this.logAction('delete','Pagamento excluído; lançamento mantido',this.providerName(s.providerId)+' • '+this.money(s.paidValue||0));await this.load()},
  pay(s){this.paymentTarget=s;this.payment={date:this.today(),value:this.balance(s),method:'PIX'};this.payModal=true},async savePayment(){if(this.isViewer)return;let s=this.paymentTarget,value=Number(this.payment.value||0),remaining=Math.max(0,Number(s.amount||0)-Number(s.paidValue||0));if(value<=0)return this.toast('Informe um valor de pagamento maior que zero.','warning');if(value>remaining+0.000001)return this.toast('O pagamento não pode ser maior que o saldo pendente de '+this.money(remaining)+'.','warning');let paid=Number(s.paidValue||0)+value,status=paid>=Number(s.amount)?'Pago':'Parcial';await db.services.update(s.id,{paidValue:paid,paymentDate:this.payment.date,paymentMethod:this.payment.method,status});await this.logAction('payment','Pagamento registrado',this.providerName(s.providerId)+' • '+this.money(value)+' • '+this.payment.method);this.payModal=false;await this.load();this.toast(status==='Pago'?'Pagamento registrado. Lançamento quitado.':'Pagamento parcial registrado. Saldo restante: '+this.money(Math.max(0,Number(s.amount||0)-paid)))},prepareReceipt(s){this.receiptServiceId=s.id;this.view='recibos';this.after()},async quickReceipt(s){if(this.isViewer)return;if(Number(s.paidValue||0)<=0)return this.toast('Registre o pagamento antes de emitir o recibo.','warning');let linked=this.receipts.filter(r=>Number(r.serviceId)===Number(s.id)),issued=linked.reduce((a,r)=>a+Number(r.total||0),0),pending=Math.max(0,Number(s.paidValue||0)-issued);if(pending<=0){let latest=linked.slice().sort((a,b)=>Number(b.id||0)-Number(a.id||0))[0];if(latest){this.currentReceipt=latest;this.view='recibos';this.after();this.toast('Todos os pagamentos registrados já possuem recibo.','warning');setTimeout(()=>this.printReceipt(),220)}else this.toast('Não há valor pago pendente de recibo.','warning');return}this.receiptServiceId=s.id;await this.issueReceipt();this.view='recibos';this.after();setTimeout(()=>this.printReceipt(),260)},async issueReceipt(){if(this.isViewer)return;let s=this.services.find(x=>Number(x.id)===Number(this.receiptServiceId));if(!s)return this.toast('Selecione um lançamento.','warning');let linked=this.receipts.filter(r=>Number(r.serviceId)===Number(s.id)),issued=linked.reduce((a,r)=>a+Number(r.total||0),0),pending=Math.max(0,Number(s.paidValue||0)-issued);if(pending<=0)return this.toast('Todos os pagamentos registrados deste lançamento já possuem recibo.','warning');let desc=String(s.description||'').replace(/(\bparcela\s+)(\d+)\s*\/\s*(\d+)/i,(m,prefix,current,total)=>prefix+String(linked.length+1).padStart(current.length,'0')+'/'+total);let p=this.providers.find(x=>Number(x.id)===Number(s.providerId))||{};let meta=await db.meta.get('receiptSeq'),next=Math.max(Number(meta?.value||0),this.receiptMaxNumber())+1;await db.meta.put

===== serviceId @ 6483514 =====
neste navegador.',note:'A numeração oficial dos recibos e os dados do condomínio serão preservados. Faça backup JSON antes de apagar, caso precise recuperar os registros depois.',confirmLabel:'Apagar registros',details:[{label:'Remove',value:'Prestadores, serviços, recibos, recorrências e logs'},{label:'Preserva',value:'Dados do condomínio e sequência dos recibos'}]};this.after()},closeActionModal(){this.actionModal={open:false,type:'',tone:'default',icon:'',target:null,service:null,title:'',subtitle:'',note:'',confirmLabel:'',details:[]}},async confirmActionModal(){if(this.isViewer)return;let m=this.actionModal,t=m.target||m.service;if(m.type==='duplicate'){await this.duplicateServiceInternal(m.service)}else if(m.type==='deleteService'){await this.removeServiceInternal(t.id)}else if(m.type==='deletePayment'){await this.removePaymentInternal(t.id)}else if(m.type==='deleteProvider'){await this.removeProviderInternal(t.id)}else if(m.type==='deleteReceipt'){await this.removeReceiptInternal(t.id)}else if(m.type==='deleteRecurrence'){await this.removeRecurrenceInternal(t.id)}else if(m.type==='clearAll'){await this.clearAllInternal()}this.closeActionModal()},async duplicateServiceInternal(s){let d={...s};delete d.id;d.status='Pendente';d.paidValue=0;d.paymentDate='';d.paymentMethod='';d.serviceDate=this.today();d.dueDate=this.today();d.reference=this.today().slice(0,7);await db.services.add(d);await this.logAction('service','Lançamento duplicado',this.providerName(d.providerId)+' • '+this.money(d.amount||0));await this.load()},async removeServiceInternal(id){let s=this.services.find(x=>Number(x.id)===Number(id));if(!s)return;let linked=this.receipts.find(r=>Number(r.serviceId)===Number(id));if(linked||Number(s.paidValue||0)>0){let linkedCount=this.receipts.filter(r=>Number(r.serviceId)===Number(id)).length;return this.toast(linkedCount>1?'Este lançamento possui '+linkedCount+' recibos vinculados. Exclua pelo fluxo de pagamentos/recibos para preservar o histórico.':linked?'Este lançamento possui o recibo nº '+linked.number+'. Exclua pelo fluxo de pagamento/recibo para preservar o histórico.':'Este lançamento possui pagamento registrado. Exclua pelo fluxo de pagamentos para preservar o histórico.','warning')}await db.services.delete(id);await this.logAction('delete','Lançamento excluído',this.providerName(s.providerId)+' • '+this.money(s.amount||0));await this.load()},async removePaymentInternal(id){let s=this.services.find(x=>Number(x.id)===Number(id));if(!s)return;let linked=this.receipts.filter(r=>Number(r.serviceId)===Number(id));await db.transaction('rw',db.receipts,db.services,async()=>{for(const r of linked)await db.receipts.delete(r.id);await db.services.update(id,{paidValue:0,paymentDate:'',paymentMethod:'',status:'Pendente'})});for(const r of linked)await this.logAction('delete','Recibo removido por exclusão de pagamento','Nº '+r.number+' • '+(r.providerName||this.providerName(s.providerId)));await this.logAction('delete','Pagamento excluído; lançamento mantido',this.providerName(s.providerId)+' • '+this.money(s.paidValue||0));await this.load()},
  pay(s){this.paymentTarget=s;this.payment={date:this.today(),value:this.balance(s),method:'PIX'};this.payModal=true},async savePayment(){if(this.isViewer)return;let s=this.paymentTarget,value=Number(this.payment.value||0),remaining=Math.max(0,Number(s.amount||0)-Number(s.paidValue||0));if(value<=0)return this.toast('Informe um valor de pagamento maior que zero.','warning');if(value>remaining+0.000001)return this.toast('O pagamento não pode ser maior que o saldo pendente de '+this.money(remaining)+'.','warning');let paid=Number(s.paidValue||0)+value,status=paid>=Number(s.amount)?'Pago':'Parcial';await db.services.update(s.id,{paidValue:paid,paymentDate:this.payment.date,paymentMethod:this.payment.method,status});await this.logAction('payment','Pagamento registrado',this.providerName(s.providerId)+' • '+this.money(value)+' • '+this.payment.method);this.payModal=false;await this.load();this.toast(status==='Pago'?'Pagamento registrado. Lançamento quitado.':'Pagamento parcial registrado. Saldo restante: '+this.money(Math.max(0,Number(s.amount||0)-paid)))},prepareReceipt(s){this.receiptServiceId=s.id;this.view='recibos';this.after()},async quickReceipt(s){if(this.isViewer)return;if(Number(s.paidValue||0)<=0)return this.toast('Registre o pagamento antes de emitir o recibo.','warning');let linked=this.receipts.filter(r=>Number(r.serviceId)===Number(s.id)),issued=linked.reduce((a,r)=>a+Number(r.total||0),0),pending=Math.max(0,Number(s.paidValue||0)-issued);if(pending<=0){let latest=linked.slice().sort((a,b)=>Number(b.id||0)-Number(a.id||0))[0];if(latest){this.currentReceipt=latest;this.view='recibos';this.after();this.toast('Todos os pagamentos registrados já possuem recibo.','warning');setTimeout(()=>this.printReceipt(),220)}else this.toast('Não há valor pago pendente de recibo.','warning');return}this.receiptServiceId=s.id;await this.issueReceipt();this.view='recibos';this.after();setTimeout(()=>this.printReceipt(),260)},async issueReceipt(){if(this.isViewer)return;let s=this.services.find(x=>Number(x.id)===Number(this.receiptServiceId));if(!s)return this.toast('Selecione um lançamento.','warning');let linked=this.receipts.filter(r=>Number(r.serviceId)===Number(s.id)),issued=linked.reduce((a,r)=>a+Number(r.total||0),0),pending=Math.max(0,Number(s.paidValue||0)-issued);if(pending<=0)return this.toast('Todos os pagamentos registrados deste lançamento já possuem recibo.','warning');let desc=String(s.description||'').replace(/(\bparcela\s+)(\d+)\s*\/\s*(\d+)/i,(m,prefix,current,total)=>prefix+String(linked.length+1).padStart(current.length,'0')+'/'+total);let p=this.providers.find(x=>Number(x.id)===Number(s.providerId))||{};let meta=await db.meta.get('receiptSeq'),next=Math.max(Number(meta?.value||0),this.receiptMaxNumber())+1;await db.meta.put({key:'receiptSeq',value:next});let r={number:String(next).padStart(6,'0'),date:this.today(),paymentDate:s.paym

===== serviceId @ 6484261 =====
{await this.removeServiceInternal(t.id)}else if(m.type==='deletePayment'){await this.removePaymentInternal(t.id)}else if(m.type==='deleteProvider'){await this.removeProviderInternal(t.id)}else if(m.type==='deleteReceipt'){await this.removeReceiptInternal(t.id)}else if(m.type==='deleteRecurrence'){await this.removeRecurrenceInternal(t.id)}else if(m.type==='clearAll'){await this.clearAllInternal()}this.closeActionModal()},async duplicateServiceInternal(s){let d={...s};delete d.id;d.status='Pendente';d.paidValue=0;d.paymentDate='';d.paymentMethod='';d.serviceDate=this.today();d.dueDate=this.today();d.reference=this.today().slice(0,7);await db.services.add(d);await this.logAction('service','Lançamento duplicado',this.providerName(d.providerId)+' • '+this.money(d.amount||0));await this.load()},async removeServiceInternal(id){let s=this.services.find(x=>Number(x.id)===Number(id));if(!s)return;let linked=this.receipts.find(r=>Number(r.serviceId)===Number(id));if(linked||Number(s.paidValue||0)>0){let linkedCount=this.receipts.filter(r=>Number(r.serviceId)===Number(id)).length;return this.toast(linkedCount>1?'Este lançamento possui '+linkedCount+' recibos vinculados. Exclua pelo fluxo de pagamentos/recibos para preservar o histórico.':linked?'Este lançamento possui o recibo nº '+linked.number+'. Exclua pelo fluxo de pagamento/recibo para preservar o histórico.':'Este lançamento possui pagamento registrado. Exclua pelo fluxo de pagamentos para preservar o histórico.','warning')}await db.services.delete(id);await this.logAction('delete','Lançamento excluído',this.providerName(s.providerId)+' • '+this.money(s.amount||0));await this.load()},async removePaymentInternal(id){let s=this.services.find(x=>Number(x.id)===Number(id));if(!s)return;let linked=this.receipts.filter(r=>Number(r.serviceId)===Number(id));await db.transaction('rw',db.receipts,db.services,async()=>{for(const r of linked)await db.receipts.delete(r.id);await db.services.update(id,{paidValue:0,paymentDate:'',paymentMethod:'',status:'Pendente'})});for(const r of linked)await this.logAction('delete','Recibo removido por exclusão de pagamento','Nº '+r.number+' • '+(r.providerName||this.providerName(s.providerId)));await this.logAction('delete','Pagamento excluído; lançamento mantido',this.providerName(s.providerId)+' • '+this.money(s.paidValue||0));await this.load()},
  pay(s){this.paymentTarget=s;this.payment={date:this.today(),value:this.balance(s),method:'PIX'};this.payModal=true},async savePayment(){if(this.isViewer)return;let s=this.paymentTarget,value=Number(this.payment.value||0),remaining=Math.max(0,Number(s.amount||0)-Number(s.paidValue||0));if(value<=0)return this.toast('Informe um valor de pagamento maior que zero.','warning');if(value>remaining+0.000001)return this.toast('O pagamento não pode ser maior que o saldo pendente de '+this.money(remaining)+'.','warning');let paid=Number(s.paidValue||0)+value,status=paid>=Number(s.amount)?'Pago':'Parcial';await db.services.update(s.id,{paidValue:paid,paymentDate:this.payment.date,paymentMethod:this.payment.method,status});await this.logAction('payment','Pagamento registrado',this.providerName(s.providerId)+' • '+this.money(value)+' • '+this.payment.method);this.payModal=false;await this.load();this.toast(status==='Pago'?'Pagamento registrado. Lançamento quitado.':'Pagamento parcial registrado. Saldo restante: '+this.money(Math.max(0,Number(s.amount||0)-paid)))},prepareReceipt(s){this.receiptServiceId=s.id;this.view='recibos';this.after()},async quickReceipt(s){if(this.isViewer)return;if(Number(s.paidValue||0)<=0)return this.toast('Registre o pagamento antes de emitir o recibo.','warning');let linked=this.receipts.filter(r=>Number(r.serviceId)===Number(s.id)),issued=linked.reduce((a,r)=>a+Number(r.total||0),0),pending=Math.max(0,Number(s.paidValue||0)-issued);if(pending<=0){let latest=linked.slice().sort((a,b)=>Number(b.id||0)-Number(a.id||0))[0];if(latest){this.currentReceipt=latest;this.view='recibos';this.after();this.toast('Todos os pagamentos registrados já possuem recibo.','warning');setTimeout(()=>this.printReceipt(),220)}else this.toast('Não há valor pago pendente de recibo.','warning');return}this.receiptServiceId=s.id;await this.issueReceipt();this.view='recibos';this.after();setTimeout(()=>this.printReceipt(),260)},async issueReceipt(){if(this.isViewer)return;let s=this.services.find(x=>Number(x.id)===Number(this.receiptServiceId));if(!s)return this.toast('Selecione um lançamento.','warning');let linked=this.receipts.filter(r=>Number(r.serviceId)===Number(s.id)),issued=linked.reduce((a,r)=>a+Number(r.total||0),0),pending=Math.max(0,Number(s.paidValue||0)-issued);if(pending<=0)return this.toast('Todos os pagamentos registrados deste lançamento já possuem recibo.','warning');let desc=String(s.description||'').replace(/(\bparcela\s+)(\d+)\s*\/\s*(\d+)/i,(m,prefix,current,total)=>prefix+String(linked.length+1).padStart(current.length,'0')+'/'+total);let p=this.providers.find(x=>Number(x.id)===Number(s.providerId))||{};let meta=await db.meta.get('receiptSeq'),next=Math.max(Number(meta?.value||0),this.receiptMaxNumber())+1;await db.meta.put({key:'receiptSeq',value:next});let r={number:String(next).padStart(6,'0'),date:this.today(),paymentDate:s.paymentDate||this.today(),providerId:p.id,providerName:p.name||'Prestador não informado',document:p.document||'',category:s.category,reference:s.reference,description:desc,total:pending,paymentMethod:s.paymentMethod||'Não informado',location:s.location,serviceId:s.id,manager:this.settings.manager,condoName:this.settings.condoName};let id=await db.receipts.add(r);r.id=id;this.currentReceipt=r;await this.logAction('receipt','Recibo emitido','Nº '+r.number+' • '+r.providerName+' • '+this.money(r.total||0));await this.load();this.toast('Recibo nº '+r.number+' emitido no valor de '+this.money(r.total||0)+'.')},printReceipt(){if(!this.currentReceipt&&this.receipts.length)this.currentReceipt=this.receipts[this.receipts.length-1];document.getElement

===== serviceId @ 6486163 =====
nked)await db.receipts.delete(r.id);await db.services.update(id,{paidValue:0,paymentDate:'',paymentMethod:'',status:'Pendente'})});for(const r of linked)await this.logAction('delete','Recibo removido por exclusão de pagamento','Nº '+r.number+' • '+(r.providerName||this.providerName(s.providerId)));await this.logAction('delete','Pagamento excluído; lançamento mantido',this.providerName(s.providerId)+' • '+this.money(s.paidValue||0));await this.load()},
  pay(s){this.paymentTarget=s;this.payment={date:this.today(),value:this.balance(s),method:'PIX'};this.payModal=true},async savePayment(){if(this.isViewer)return;let s=this.paymentTarget,value=Number(this.payment.value||0),remaining=Math.max(0,Number(s.amount||0)-Number(s.paidValue||0));if(value<=0)return this.toast('Informe um valor de pagamento maior que zero.','warning');if(value>remaining+0.000001)return this.toast('O pagamento não pode ser maior que o saldo pendente de '+this.money(remaining)+'.','warning');let paid=Number(s.paidValue||0)+value,status=paid>=Number(s.amount)?'Pago':'Parcial';await db.services.update(s.id,{paidValue:paid,paymentDate:this.payment.date,paymentMethod:this.payment.method,status});await this.logAction('payment','Pagamento registrado',this.providerName(s.providerId)+' • '+this.money(value)+' • '+this.payment.method);this.payModal=false;await this.load();this.toast(status==='Pago'?'Pagamento registrado. Lançamento quitado.':'Pagamento parcial registrado. Saldo restante: '+this.money(Math.max(0,Number(s.amount||0)-paid)))},prepareReceipt(s){this.receiptServiceId=s.id;this.view='recibos';this.after()},async quickReceipt(s){if(this.isViewer)return;if(Number(s.paidValue||0)<=0)return this.toast('Registre o pagamento antes de emitir o recibo.','warning');let linked=this.receipts.filter(r=>Number(r.serviceId)===Number(s.id)),issued=linked.reduce((a,r)=>a+Number(r.total||0),0),pending=Math.max(0,Number(s.paidValue||0)-issued);if(pending<=0){let latest=linked.slice().sort((a,b)=>Number(b.id||0)-Number(a.id||0))[0];if(latest){this.currentReceipt=latest;this.view='recibos';this.after();this.toast('Todos os pagamentos registrados já possuem recibo.','warning');setTimeout(()=>this.printReceipt(),220)}else this.toast('Não há valor pago pendente de recibo.','warning');return}this.receiptServiceId=s.id;await this.issueReceipt();this.view='recibos';this.after();setTimeout(()=>this.printReceipt(),260)},async issueReceipt(){if(this.isViewer)return;let s=this.services.find(x=>Number(x.id)===Number(this.receiptServiceId));if(!s)return this.toast('Selecione um lançamento.','warning');let linked=this.receipts.filter(r=>Number(r.serviceId)===Number(s.id)),issued=linked.reduce((a,r)=>a+Number(r.total||0),0),pending=Math.max(0,Number(s.paidValue||0)-issued);if(pending<=0)return this.toast('Todos os pagamentos registrados deste lançamento já possuem recibo.','warning');let desc=String(s.description||'').replace(/(\bparcela\s+)(\d+)\s*\/\s*(\d+)/i,(m,prefix,current,total)=>prefix+String(linked.length+1).padStart(current.length,'0')+'/'+total);let p=this.providers.find(x=>Number(x.id)===Number(s.providerId))||{};let meta=await db.meta.get('receiptSeq'),next=Math.max(Number(meta?.value||0),this.receiptMaxNumber())+1;await db.meta.put({key:'receiptSeq',value:next});let r={number:String(next).padStart(6,'0'),date:this.today(),paymentDate:s.paymentDate||this.today(),providerId:p.id,providerName:p.name||'Prestador não informado',document:p.document||'',category:s.category,reference:s.reference,description:desc,total:pending,paymentMethod:s.paymentMethod||'Não informado',location:s.location,serviceId:s.id,manager:this.settings.manager,condoName:this.settings.condoName};let id=await db.receipts.add(r);r.id=id;this.currentReceipt=r;await this.logAction('receipt','Recibo emitido','Nº '+r.number+' • '+r.providerName+' • '+this.money(r.total||0));await this.load();this.toast('Recibo nº '+r.number+' emitido no valor de '+this.money(r.total||0)+'.')},printReceipt(){if(!this.currentReceipt&&this.receipts.length)this.currentReceipt=this.receipts[this.receipts.length-1];document.getElementById('receiptPrint').classList.remove('hidden');document.getElementById('reportPrint').classList.add('hidden');document.getElementById('annualReportPrint').classList.add('hidden');setTimeout(()=>window.print(),100)},async removeReceiptInternal(id){let r=this.receipts.find(x=>Number(x.id)===Number(id));if(!r)return;let linked=this.receipts.filter(x=>Number(x.serviceId)===Number(r.serviceId)).sort((a,b)=>Number(b.id||0)-Number(a.id||0)),latest=linked[0];if(latest&&Number(latest.id)!==Number(r.id))return this.toast('Para preservar a data e a forma de pagamento das parcelas, somente o recibo mais recente deste lançamento pode ser excluído e reemitido.','warning');await db.receipts.delete(id);await this.logAction('delete','Recibo excluído',r?('Nº '+r.number+' • '+r.providerName):('ID '+id));await this.load()},
  printReport(){document.body.classList.add('printing-report');document.getElementById('receiptPrint').classList.add('hidden');document.getElementById('annualReportPrint').classList.add('hidden');document.getElementById('reportPrint').classList.remove('hidden');setTimeout(()=>{window.print();document.body.classList.remove('printing-report');document.getElementById('receiptPrint').classList.remove('hidden');document.getElementById('annualReportPrint').classList.add('hidden');document.getElementById('reportPrint').classList.add('hidden')},100)},printAnnualReport(){document.body.classList.add('printing-annual');let rp=document.getElementById('receiptPrint'),mp=document.getElementById('reportPrint'),ap=document.getElementById('annualReportPrint');rp.classList.add('hidden');mp.classList.add('hidden');ap.classList.remove('hidden');ap.style.display='block';ap.style.visibility='visible';setTimeout(()=>{window.print();document.body.classList.remove('printing-annual');rp.classList.remove('hidden');ap.classList.add('hidden');ap.style.display='';ap.style.visibility='';mp.classList

===== serviceId @ 6486993 =====
');if(value>remaining+0.000001)return this.toast('O pagamento não pode ser maior que o saldo pendente de '+this.money(remaining)+'.','warning');let paid=Number(s.paidValue||0)+value,status=paid>=Number(s.amount)?'Pago':'Parcial';await db.services.update(s.id,{paidValue:paid,paymentDate:this.payment.date,paymentMethod:this.payment.method,status});await this.logAction('payment','Pagamento registrado',this.providerName(s.providerId)+' • '+this.money(value)+' • '+this.payment.method);this.payModal=false;await this.load();this.toast(status==='Pago'?'Pagamento registrado. Lançamento quitado.':'Pagamento parcial registrado. Saldo restante: '+this.money(Math.max(0,Number(s.amount||0)-paid)))},prepareReceipt(s){this.receiptServiceId=s.id;this.view='recibos';this.after()},async quickReceipt(s){if(this.isViewer)return;if(Number(s.paidValue||0)<=0)return this.toast('Registre o pagamento antes de emitir o recibo.','warning');let linked=this.receipts.filter(r=>Number(r.serviceId)===Number(s.id)),issued=linked.reduce((a,r)=>a+Number(r.total||0),0),pending=Math.max(0,Number(s.paidValue||0)-issued);if(pending<=0){let latest=linked.slice().sort((a,b)=>Number(b.id||0)-Number(a.id||0))[0];if(latest){this.currentReceipt=latest;this.view='recibos';this.after();this.toast('Todos os pagamentos registrados já possuem recibo.','warning');setTimeout(()=>this.printReceipt(),220)}else this.toast('Não há valor pago pendente de recibo.','warning');return}this.receiptServiceId=s.id;await this.issueReceipt();this.view='recibos';this.after();setTimeout(()=>this.printReceipt(),260)},async issueReceipt(){if(this.isViewer)return;let s=this.services.find(x=>Number(x.id)===Number(this.receiptServiceId));if(!s)return this.toast('Selecione um lançamento.','warning');let linked=this.receipts.filter(r=>Number(r.serviceId)===Number(s.id)),issued=linked.reduce((a,r)=>a+Number(r.total||0),0),pending=Math.max(0,Number(s.paidValue||0)-issued);if(pending<=0)return this.toast('Todos os pagamentos registrados deste lançamento já possuem recibo.','warning');let desc=String(s.description||'').replace(/(\bparcela\s+)(\d+)\s*\/\s*(\d+)/i,(m,prefix,current,total)=>prefix+String(linked.length+1).padStart(current.length,'0')+'/'+total);let p=this.providers.find(x=>Number(x.id)===Number(s.providerId))||{};let meta=await db.meta.get('receiptSeq'),next=Math.max(Number(meta?.value||0),this.receiptMaxNumber())+1;await db.meta.put({key:'receiptSeq',value:next});let r={number:String(next).padStart(6,'0'),date:this.today(),paymentDate:s.paymentDate||this.today(),providerId:p.id,providerName:p.name||'Prestador não informado',document:p.document||'',category:s.category,reference:s.reference,description:desc,total:pending,paymentMethod:s.paymentMethod||'Não informado',location:s.location,serviceId:s.id,manager:this.settings.manager,condoName:this.settings.condoName};let id=await db.receipts.add(r);r.id=id;this.currentReceipt=r;await this.logAction('receipt','Recibo emitido','Nº '+r.number+' • '+r.providerName+' • '+this.money(r.total||0));await this.load();this.toast('Recibo nº '+r.number+' emitido no valor de '+this.money(r.total||0)+'.')},printReceipt(){if(!this.currentReceipt&&this.receipts.length)this.currentReceipt=this.receipts[this.receipts.length-1];document.getElementById('receiptPrint').classList.remove('hidden');document.getElementById('reportPrint').classList.add('hidden');document.getElementById('annualReportPrint').classList.add('hidden');setTimeout(()=>window.print(),100)},async removeReceiptInternal(id){let r=this.receipts.find(x=>Number(x.id)===Number(id));if(!r)return;let linked=this.receipts.filter(x=>Number(x.serviceId)===Number(r.serviceId)).sort((a,b)=>Number(b.id||0)-Number(a.id||0)),latest=linked[0];if(latest&&Number(latest.id)!==Number(r.id))return this.toast('Para preservar a data e a forma de pagamento das parcelas, somente o recibo mais recente deste lançamento pode ser excluído e reemitido.','warning');await db.receipts.delete(id);await this.logAction('delete','Recibo excluído',r?('Nº '+r.number+' • '+r.providerName):('ID '+id));await this.load()},
  printReport(){document.body.classList.add('printing-report');document.getElementById('receiptPrint').classList.add('hidden');document.getElementById('annualReportPrint').classList.add('hidden');document.getElementById('reportPrint').classList.remove('hidden');setTimeout(()=>{window.print();document.body.classList.remove('printing-report');document.getElementById('receiptPrint').classList.remove('hidden');document.getElementById('annualReportPrint').classList.add('hidden');document.getElementById('reportPrint').classList.add('hidden')},100)},printAnnualReport(){document.body.classList.add('printing-annual');let rp=document.getElementById('receiptPrint'),mp=document.getElementById('reportPrint'),ap=document.getElementById('annualReportPrint');rp.classList.add('hidden');mp.classList.add('hidden');ap.classList.remove('hidden');ap.style.display='block';ap.style.visibility='visible';setTimeout(()=>{window.print();document.body.classList.remove('printing-annual');rp.classList.remove('hidden');ap.classList.add('hidden');ap.style.display='';ap.style.visibility='';mp.classList.add('hidden')},180)},csv(){let rows=[['Recibo nº','Data do serviço','Data do pagamento','Prestador','Categoria','Descrição','Valor do serviço','Valor deste pagamento','Status','Forma']].concat(this.reportDetailRows.map(s=>[s.receiptNumber,s.serviceDate,s.paymentDate||'',this.providerName(s.providerId),s.category,s.description,s.amount,s.reportValue||0,s.status,s.paymentMethod||'']));let csv=rows.map(r=>r.map(v=>'"'+String(v??'').replaceAll('"','""')+'"').join(';')).join('\n');this.download('relatorio-prestadores-recibocondo.csv',csv,'text/csv;charset=utf-8')},
  async saveSettings(){let plainSettings=JSON.parse(JSON.stringify(this.settings));await db.settings.put({key:'main',value:plainSettings});await this.logAction('settings','Configurações salvas','Dados do condomínio atualizados.');this.toast('Configurações salvas

===== serviceId @ 6487963 =====
serviceId)===Number(s.id)),issued=linked.reduce((a,r)=>a+Number(r.total||0),0),pending=Math.max(0,Number(s.paidValue||0)-issued);if(pending<=0){let latest=linked.slice().sort((a,b)=>Number(b.id||0)-Number(a.id||0))[0];if(latest){this.currentReceipt=latest;this.view='recibos';this.after();this.toast('Todos os pagamentos registrados já possuem recibo.','warning');setTimeout(()=>this.printReceipt(),220)}else this.toast('Não há valor pago pendente de recibo.','warning');return}this.receiptServiceId=s.id;await this.issueReceipt();this.view='recibos';this.after();setTimeout(()=>this.printReceipt(),260)},async issueReceipt(){if(this.isViewer)return;let s=this.services.find(x=>Number(x.id)===Number(this.receiptServiceId));if(!s)return this.toast('Selecione um lançamento.','warning');let linked=this.receipts.filter(r=>Number(r.serviceId)===Number(s.id)),issued=linked.reduce((a,r)=>a+Number(r.total||0),0),pending=Math.max(0,Number(s.paidValue||0)-issued);if(pending<=0)return this.toast('Todos os pagamentos registrados deste lançamento já possuem recibo.','warning');let desc=String(s.description||'').replace(/(\bparcela\s+)(\d+)\s*\/\s*(\d+)/i,(m,prefix,current,total)=>prefix+String(linked.length+1).padStart(current.length,'0')+'/'+total);let p=this.providers.find(x=>Number(x.id)===Number(s.providerId))||{};let meta=await db.meta.get('receiptSeq'),next=Math.max(Number(meta?.value||0),this.receiptMaxNumber())+1;await db.meta.put({key:'receiptSeq',value:next});let r={number:String(next).padStart(6,'0'),date:this.today(),paymentDate:s.paymentDate||this.today(),providerId:p.id,providerName:p.name||'Prestador não informado',document:p.document||'',category:s.category,reference:s.reference,description:desc,total:pending,paymentMethod:s.paymentMethod||'Não informado',location:s.location,serviceId:s.id,manager:this.settings.manager,condoName:this.settings.condoName};let id=await db.receipts.add(r);r.id=id;this.currentReceipt=r;await this.logAction('receipt','Recibo emitido','Nº '+r.number+' • '+r.providerName+' • '+this.money(r.total||0));await this.load();this.toast('Recibo nº '+r.number+' emitido no valor de '+this.money(r.total||0)+'.')},printReceipt(){if(!this.currentReceipt&&this.receipts.length)this.currentReceipt=this.receipts[this.receipts.length-1];document.getElementById('receiptPrint').classList.remove('hidden');document.getElementById('reportPrint').classList.add('hidden');document.getElementById('annualReportPrint').classList.add('hidden');setTimeout(()=>window.print(),100)},async removeReceiptInternal(id){let r=this.receipts.find(x=>Number(x.id)===Number(id));if(!r)return;let linked=this.receipts.filter(x=>Number(x.serviceId)===Number(r.serviceId)).sort((a,b)=>Number(b.id||0)-Number(a.id||0)),latest=linked[0];if(latest&&Number(latest.id)!==Number(r.id))return this.toast('Para preservar a data e a forma de pagamento das parcelas, somente o recibo mais recente deste lançamento pode ser excluído e reemitido.','warning');await db.receipts.delete(id);await this.logAction('delete','Recibo excluído',r?('Nº '+r.number+' • '+r.providerName):('ID '+id));await this.load()},
  printReport(){document.body.classList.add('printing-report');document.getElementById('receiptPrint').classList.add('hidden');document.getElementById('annualReportPrint').classList.add('hidden');document.getElementById('reportPrint').classList.remove('hidden');setTimeout(()=>{window.print();document.body.classList.remove('printing-report');document.getElementById('receiptPrint').classList.remove('hidden');document.getElementById('annualReportPrint').classList.add('hidden');document.getElementById('reportPrint').classList.add('hidden')},100)},printAnnualReport(){document.body.classList.add('printing-annual');let rp=document.getElementById('receiptPrint'),mp=document.getElementById('reportPrint'),ap=document.getElementById('annualReportPrint');rp.classList.add('hidden');mp.classList.add('hidden');ap.classList.remove('hidden');ap.style.display='block';ap.style.visibility='visible';setTimeout(()=>{window.print();document.body.classList.remove('printing-annual');rp.classList.remove('hidden');ap.classList.add('hidden');ap.style.display='';ap.style.visibility='';mp.classList.add('hidden')},180)},csv(){let rows=[['Recibo nº','Data do serviço','Data do pagamento','Prestador','Categoria','Descrição','Valor do serviço','Valor deste pagamento','Status','Forma']].concat(this.reportDetailRows.map(s=>[s.receiptNumber,s.serviceDate,s.paymentDate||'',this.providerName(s.providerId),s.category,s.description,s.amount,s.reportValue||0,s.status,s.paymentMethod||'']));let csv=rows.map(r=>r.map(v=>'"'+String(v??'').replaceAll('"','""')+'"').join(';')).join('\n');this.download('relatorio-prestadores-recibocondo.csv',csv,'text/csv;charset=utf-8')},
  async saveSettings(){let plainSettings=JSON.parse(JSON.stringify(this.settings));await db.settings.put({key:'main',value:plainSettings});await this.logAction('settings','Configurações salvas','Dados do condomínio atualizados.');this.toast('Configurações salvas')},async savePasswords(){if(this.isViewer)return;let cur=JSON.parse(localStorage.getItem('rc_passwords')||'{"admin":"123456","viewer":"123456"}');if(this.passwords.admin)cur.admin=this.passwords.admin;if(this.passwords.viewer)cur.viewer=this.passwords.viewer;localStorage.setItem('rc_passwords',JSON.stringify(cur));await this.logAction('settings','Senhas atualizadas','Senhas locais do app foram atualizadas.');this.passwords={admin:'',viewer:''};this.toast('Senhas atualizadas')},async backup(){let now=new Date().toISOString();let seqMeta=await db.meta.get('receiptSeq');let data={settings:this.settings,providers:this.providers,services:this.services,receipts:this.receipts,recurrences:this.recurrences,logs:this.logs,receiptSeq:Number(seqMeta?.value||this.receiptMaxNumber()||0),backupStats:this.backupStats(),exportedAt:now};this.download('backup-recibocondo-prestadores.json',JSON.stringify(data,null,2),'application/json');this.backupLast=now;localStorage.setIt

===== serviceId @ 6488821 =====
ssued=linked.reduce((a,r)=>a+Number(r.total||0),0),pending=Math.max(0,Number(s.paidValue||0)-issued);if(pending<=0)return this.toast('Todos os pagamentos registrados deste lançamento já possuem recibo.','warning');let desc=String(s.description||'').replace(/(\bparcela\s+)(\d+)\s*\/\s*(\d+)/i,(m,prefix,current,total)=>prefix+String(linked.length+1).padStart(current.length,'0')+'/'+total);let p=this.providers.find(x=>Number(x.id)===Number(s.providerId))||{};let meta=await db.meta.get('receiptSeq'),next=Math.max(Number(meta?.value||0),this.receiptMaxNumber())+1;await db.meta.put({key:'receiptSeq',value:next});let r={number:String(next).padStart(6,'0'),date:this.today(),paymentDate:s.paymentDate||this.today(),providerId:p.id,providerName:p.name||'Prestador não informado',document:p.document||'',category:s.category,reference:s.reference,description:desc,total:pending,paymentMethod:s.paymentMethod||'Não informado',location:s.location,serviceId:s.id,manager:this.settings.manager,condoName:this.settings.condoName};let id=await db.receipts.add(r);r.id=id;this.currentReceipt=r;await this.logAction('receipt','Recibo emitido','Nº '+r.number+' • '+r.providerName+' • '+this.money(r.total||0));await this.load();this.toast('Recibo nº '+r.number+' emitido no valor de '+this.money(r.total||0)+'.')},printReceipt(){if(!this.currentReceipt&&this.receipts.length)this.currentReceipt=this.receipts[this.receipts.length-1];document.getElementById('receiptPrint').classList.remove('hidden');document.getElementById('reportPrint').classList.add('hidden');document.getElementById('annualReportPrint').classList.add('hidden');setTimeout(()=>window.print(),100)},async removeReceiptInternal(id){let r=this.receipts.find(x=>Number(x.id)===Number(id));if(!r)return;let linked=this.receipts.filter(x=>Number(x.serviceId)===Number(r.serviceId)).sort((a,b)=>Number(b.id||0)-Number(a.id||0)),latest=linked[0];if(latest&&Number(latest.id)!==Number(r.id))return this.toast('Para preservar a data e a forma de pagamento das parcelas, somente o recibo mais recente deste lançamento pode ser excluído e reemitido.','warning');await db.receipts.delete(id);await this.logAction('delete','Recibo excluído',r?('Nº '+r.number+' • '+r.providerName):('ID '+id));await this.load()},
  printReport(){document.body.classList.add('printing-report');document.getElementById('receiptPrint').classList.add('hidden');document.getElementById('annualReportPrint').classList.add('hidden');document.getElementById('reportPrint').classList.remove('hidden');setTimeout(()=>{window.print();document.body.classList.remove('printing-report');document.getElementById('receiptPrint').classList.remove('hidden');document.getElementById('annualReportPrint').classList.add('hidden');document.getElementById('reportPrint').classList.add('hidden')},100)},printAnnualReport(){document.body.classList.add('printing-annual');let rp=document.getElementById('receiptPrint'),mp=document.getElementById('reportPrint'),ap=document.getElementById('annualReportPrint');rp.classList.add('hidden');mp.classList.add('hidden');ap.classList.remove('hidden');ap.style.display='block';ap.style.visibility='visible';setTimeout(()=>{window.print();document.body.classList.remove('printing-annual');rp.classList.remove('hidden');ap.classList.add('hidden');ap.style.display='';ap.style.visibility='';mp.classList.add('hidden')},180)},csv(){let rows=[['Recibo nº','Data do serviço','Data do pagamento','Prestador','Categoria','Descrição','Valor do serviço','Valor deste pagamento','Status','Forma']].concat(this.reportDetailRows.map(s=>[s.receiptNumber,s.serviceDate,s.paymentDate||'',this.providerName(s.providerId),s.category,s.description,s.amount,s.reportValue||0,s.status,s.paymentMethod||'']));let csv=rows.map(r=>r.map(v=>'"'+String(v??'').replaceAll('"','""')+'"').join(';')).join('\n');this.download('relatorio-prestadores-recibocondo.csv',csv,'text/csv;charset=utf-8')},
  async saveSettings(){let plainSettings=JSON.parse(JSON.stringify(this.settings));await db.settings.put({key:'main',value:plainSettings});await this.logAction('settings','Configurações salvas','Dados do condomínio atualizados.');this.toast('Configurações salvas')},async savePasswords(){if(this.isViewer)return;let cur=JSON.parse(localStorage.getItem('rc_passwords')||'{"admin":"123456","viewer":"123456"}');if(this.passwords.admin)cur.admin=this.passwords.admin;if(this.passwords.viewer)cur.viewer=this.passwords.viewer;localStorage.setItem('rc_passwords',JSON.stringify(cur));await this.logAction('settings','Senhas atualizadas','Senhas locais do app foram atualizadas.');this.passwords={admin:'',viewer:''};this.toast('Senhas atualizadas')},async backup(){let now=new Date().toISOString();let seqMeta=await db.meta.get('receiptSeq');let data={settings:this.settings,providers:this.providers,services:this.services,receipts:this.receipts,recurrences:this.recurrences,logs:this.logs,receiptSeq:Number(seqMeta?.value||this.receiptMaxNumber()||0),backupStats:this.backupStats(),exportedAt:now};this.download('backup-recibocondo-prestadores.json',JSON.stringify(data,null,2),'application/json');this.backupLast=now;localStorage.setItem('rc_last_backup',now);await this.logAction('backup','Backup exportado','Backup JSON gerado com '+data.backupStats.records+' registros.')},async restore(ev){let f=ev.target.files[0];if(!f)return;try{let data=JSON.parse(await f.text());if(!data||typeof data!=='object')throw new Error('Backup inválido');let providers=Array.isArray(data.providers)?data.providers:[],services=Array.isArray(data.services)?data.services:[],receipts=Array.isArray(data.receipts)?data.receipts:[],recurrences=Array.isArray(data.recurrences)?data.recurrences:[],logs=Array.isArray(data.logs)?data.logs:[];await db.transaction('rw',db.providers,db.services,db.receipts,db.recurrences,db.logs,db.settings,db.meta,async()=>{await db.providers.clear();await db.services.clear();await db.receipts.clear();await db.recurrences.clear();await db.logs.clear();if(data.settings){let restor

===== serviceId @ 6488843 =====
,r)=>a+Number(r.total||0),0),pending=Math.max(0,Number(s.paidValue||0)-issued);if(pending<=0)return this.toast('Todos os pagamentos registrados deste lançamento já possuem recibo.','warning');let desc=String(s.description||'').replace(/(\bparcela\s+)(\d+)\s*\/\s*(\d+)/i,(m,prefix,current,total)=>prefix+String(linked.length+1).padStart(current.length,'0')+'/'+total);let p=this.providers.find(x=>Number(x.id)===Number(s.providerId))||{};let meta=await db.meta.get('receiptSeq'),next=Math.max(Number(meta?.value||0),this.receiptMaxNumber())+1;await db.meta.put({key:'receiptSeq',value:next});let r={number:String(next).padStart(6,'0'),date:this.today(),paymentDate:s.paymentDate||this.today(),providerId:p.id,providerName:p.name||'Prestador não informado',document:p.document||'',category:s.category,reference:s.reference,description:desc,total:pending,paymentMethod:s.paymentMethod||'Não informado',location:s.location,serviceId:s.id,manager:this.settings.manager,condoName:this.settings.condoName};let id=await db.receipts.add(r);r.id=id;this.currentReceipt=r;await this.logAction('receipt','Recibo emitido','Nº '+r.number+' • '+r.providerName+' • '+this.money(r.total||0));await this.load();this.toast('Recibo nº '+r.number+' emitido no valor de '+this.money(r.total||0)+'.')},printReceipt(){if(!this.currentReceipt&&this.receipts.length)this.currentReceipt=this.receipts[this.receipts.length-1];document.getElementById('receiptPrint').classList.remove('hidden');document.getElementById('reportPrint').classList.add('hidden');document.getElementById('annualReportPrint').classList.add('hidden');setTimeout(()=>window.print(),100)},async removeReceiptInternal(id){let r=this.receipts.find(x=>Number(x.id)===Number(id));if(!r)return;let linked=this.receipts.filter(x=>Number(x.serviceId)===Number(r.serviceId)).sort((a,b)=>Number(b.id||0)-Number(a.id||0)),latest=linked[0];if(latest&&Number(latest.id)!==Number(r.id))return this.toast('Para preservar a data e a forma de pagamento das parcelas, somente o recibo mais recente deste lançamento pode ser excluído e reemitido.','warning');await db.receipts.delete(id);await this.logAction('delete','Recibo excluído',r?('Nº '+r.number+' • '+r.providerName):('ID '+id));await this.load()},
  printReport(){document.body.classList.add('printing-report');document.getElementById('receiptPrint').classList.add('hidden');document.getElementById('annualReportPrint').classList.add('hidden');document.getElementById('reportPrint').classList.remove('hidden');setTimeout(()=>{window.print();document.body.classList.remove('printing-report');document.getElementById('receiptPrint').classList.remove('hidden');document.getElementById('annualReportPrint').classList.add('hidden');document.getElementById('reportPrint').classList.add('hidden')},100)},printAnnualReport(){document.body.classList.add('printing-annual');let rp=document.getElementById('receiptPrint'),mp=document.getElementById('reportPrint'),ap=document.getElementById('annualReportPrint');rp.classList.add('hidden');mp.classList.add('hidden');ap.classList.remove('hidden');ap.style.display='block';ap.style.visibility='visible';setTimeout(()=>{window.print();document.body.classList.remove('printing-annual');rp.classList.remove('hidden');ap.classList.add('hidden');ap.style.display='';ap.style.visibility='';mp.classList.add('hidden')},180)},csv(){let rows=[['Recibo nº','Data do serviço','Data do pagamento','Prestador','Categoria','Descrição','Valor do serviço','Valor deste pagamento','Status','Forma']].concat(this.reportDetailRows.map(s=>[s.receiptNumber,s.serviceDate,s.paymentDate||'',this.providerName(s.providerId),s.category,s.description,s.amount,s.reportValue||0,s.status,s.paymentMethod||'']));let csv=rows.map(r=>r.map(v=>'"'+String(v??'').replaceAll('"','""')+'"').join(';')).join('\n');this.download('relatorio-prestadores-recibocondo.csv',csv,'text/csv;charset=utf-8')},
  async saveSettings(){let plainSettings=JSON.parse(JSON.stringify(this.settings));await db.settings.put({key:'main',value:plainSettings});await this.logAction('settings','Configurações salvas','Dados do condomínio atualizados.');this.toast('Configurações salvas')},async savePasswords(){if(this.isViewer)return;let cur=JSON.parse(localStorage.getItem('rc_passwords')||'{"admin":"123456","viewer":"123456"}');if(this.passwords.admin)cur.admin=this.passwords.admin;if(this.passwords.viewer)cur.viewer=this.passwords.viewer;localStorage.setItem('rc_passwords',JSON.stringify(cur));await this.logAction('settings','Senhas atualizadas','Senhas locais do app foram atualizadas.');this.passwords={admin:'',viewer:''};this.toast('Senhas atualizadas')},async backup(){let now=new Date().toISOString();let seqMeta=await db.meta.get('receiptSeq');let data={settings:this.settings,providers:this.providers,services:this.services,receipts:this.receipts,recurrences:this.recurrences,logs:this.logs,receiptSeq:Number(seqMeta?.value||this.receiptMaxNumber()||0),backupStats:this.backupStats(),exportedAt:now};this.download('backup-recibocondo-prestadores.json',JSON.stringify(data,null,2),'application/json');this.backupLast=now;localStorage.setItem('rc_last_backup',now);await this.logAction('backup','Backup exportado','Backup JSON gerado com '+data.backupStats.records+' registros.')},async restore(ev){let f=ev.target.files[0];if(!f)return;try{let data=JSON.parse(await f.text());if(!data||typeof data!=='object')throw new Error('Backup inválido');let providers=Array.isArray(data.providers)?data.providers:[],services=Array.isArray(data.services)?data.services:[],receipts=Array.isArray(data.receipts)?data.receipts:[],recurrences=Array.isArray(data.recurrences)?data.recurrences:[],logs=Array.isArray(data.logs)?data.logs:[];await db.transaction('rw',db.providers,db.services,db.receipts,db.recurrences,db.logs,db.settings,db.meta,async()=>{await db.providers.clear();await db.services.clear();await db.receipts.clear();await db.recurrences.clear();await db.logs.clear();if(data.settings){let restoredSettings=JSON.parse(

===== serviceId @ 6498208 =====
fmt(s.serviceDate)+' • '+this.money(s.paidValue||s.amount):'-'},async logAction(type,title,detail){try{await db.logs.add({at:new Date().toISOString(),type,title,detail,role:this.auth?.role||'sistema'});this.logs=(await db.logs.orderBy('at').reverse().limit(30).toArray())}catch(e){console.warn('logAction',e)}},logIcon(type){return {payment:'circle-dollar-sign',receipt:'receipt-text',service:'clipboard-list',provider:'users',recurrence:'repeat-2',backup:'shield-check',restore:'rotate-ccw',danger:'triangle-alert',settings:'settings',delete:'trash-2'}[type]||'history'},fmtDateTime(d){if(!d)return'-';return new Date(d).toLocaleString('pt-BR')},async clearLogs(){if(this.isViewer)return;await db.logs.clear();this.logs=[];this.after()},providerSpend(id){return this.services.filter(s=>Number(s.providerId)===Number(id)).reduce((a,s)=>a+Number(s.paidValue||s.amount||0),0)},providerOpen(id){return this.services.filter(s=>Number(s.providerId)===Number(id)).reduce((a,s)=>a+this.balance(s),0)},openWhatsapp(p){let raw=String(p.whatsapp||p.phone||'').replace(/\D/g,'');if(!raw)return this.toast('Este prestador não tem WhatsApp/telefone cadastrado.','warning');let phone=raw.startsWith('55')?raw:'55'+raw;let msg=encodeURIComponent('Olá, '+p.name+'. Aqui é do '+this.settings.condoName+'.');window.open('https://wa.me/'+phone+'?text='+msg,'_blank')},overdueLabel(s){if(!s.dueDate||this.balance(s)<=0)return'';let today=new Date(this.today()+'T00:00:00'), due=new Date(s.dueDate+'T00:00:00'), days=Math.ceil((due-today)/86400000);if(days<0)return Math.abs(days)+' dia(s) em atraso';if(days===0)return'vence hoje';return'faltam '+days+' dia(s)'},providerName(id){return (this.providers.find(p=>Number(p.id)===Number(id))||{}).name||'-'},reportReceiptNumber(service){let r=this.receipts.find(x=>Number(x.serviceId)===Number(service.id));return r?.number||'-';},balance(s){return Math.max(0,Number(s.amount||0)-Number(s.paidValue||0))},statusClass(st){return st==='Pago'?'bg-emerald-500/10 text-emerald-700':st==='Parcial'?'bg-amber-500/10 text-amber-700':'bg-red-500/10 text-red-700'},valorPorExtenso(v){v=Number(v||0);let reais=Math.floor(v),cent=Math.round((v-reais)*100);let base={0:'zero',1:'um',2:'dois',3:'três',4:'quatro',5:'cinco',6:'seis',7:'sete',8:'oito',9:'nove',10:'dez',11:'onze',12:'doze',13:'treze',14:'quatorze',15:'quinze',16:'dezesseis',17:'dezessete',18:'dezoito',19:'dezenove',20:'vinte',30:'trinta',40:'quarenta',50:'cinquenta',60:'sessenta',70:'setenta',80:'oitenta',90:'noventa',100:'cem'};let ext=(n)=>{if(n<=20||base[n])return base[n];if(n<100){let d=Math.floor(n/10)*10,u=n%10;return base[d]+(u?' e '+base[u]:'')}if(n<1000){let c=Math.floor(n/100),r=n%100,cn=c===1?'cento':base[c]+' centos';cn=cn.replace('dois centos','duzentos').replace('três centos','trezentos').replace('quatro centos','quatrocentos').replace('cinco centos','quinhentos').replace('seis centos','seiscentos').replace('sete centos','setecentos').replace('oito centos','oitocentos').replace('nove centos','novecentos');return r?cn+' e '+ext(r):cn}if(n<1000000){let m=Math.floor(n/1000),r=n%1000;return (m===1?'mil':ext(m)+' mil')+(r?' e '+ext(r):'')}return n.toLocaleString('pt-BR')};let txt=ext(reais)+' '+(reais===1?'real':'reais');if(cent)txt+=' e '+ext(cent)+' '+(cent===1?'centavo':'centavos');return txt;},money(v){return Number(v||0).toLocaleString('pt-BR',{style:'currency',currency:'BRL'})},today(){return new Date().toISOString().slice(0,10)},fmt(d){if(!d)return'-';return new Date(d+'T00:00:00').toLocaleDateString('pt-BR')},month(m){if(!m)return'-';let [y,mo]=String(m).split('-');return new Date(Number(y),Number(mo)-1,1).toLocaleDateString('pt-BR',{month:'long',year:'numeric'})},reportMonthLabel(){let src=(this.reportRows[0]?.reference)||String(this.report.from||this.today()).slice(0,7);let label=this.month(src);return String(label).replace(/ de \d{4}/,'').toUpperCase();},download(n,c,t){let a=document.createElement('a');a.href=URL.createObjectURL(new Blob([c],{type:t}));a.download=n;a.click();URL.revokeObjectURL(a.href)},refreshIcons(){setTimeout(()=>window.lucide&&lucide.createIcons(),60)},after(){this.$nextTick(()=>{this.enhanceAccordions();this.refreshIcons();this.renderCharts();setTimeout(()=>this.resizeCharts(),120)})},enhanceAccordions(){try{const scopes=['prestadores','servicos','pagamentos','recibos','busca','config'];scopes.forEach(scope=>{const section=[...document.querySelectorAll('main section')].find(s=>(s.getAttribute('x-show')||'').includes(scope));if(!section)return;const panels=[...section.children].filter(el=>el.classList&&el.classList.contains('glass'));panels.forEach((panel,idx)=>{if(panel.dataset.rcAccordion==='1')return;panel.dataset.rcAccordion='1';panel.classList.add('rc-accordion-panel');const titleEl=panel.querySelector('h3,h2,h4');const title=(titleEl?.textContent||({prestadores:'Prestadores',servicos:'Serviços',pagamentos:'Pagamentos',recibos:'Recibos',busca:'Busca global',config:'Configurações'}[scope]||'Painel')).trim();const subEl=panel.querySelector('p.text-sm,p.text-xs');const sub=(subEl?.textContent||'Clique para abrir ou recolher este painel.').trim();const body=document.createElement('div');body.className='rc-accordion-body';while(panel.firstChild)body.appendChild(panel.firstChild);const head=document.createElement('button');head.type='button';head.className='rc-accordion-head no-print';head.innerHTML=`<span><span class="rc-accordion-kicker">${scope==='config'?'organização':'painel premium'}</span><span class="rc-accordion-title">${title}</span><span class="rc-accordion-sub">${sub}</span></span><span class="rc-accordion-icon"><i data-lucide="chevron-down" class="w-5 h-5"></i></span>`;head.addEventListener('click',()=>{panel.classList.toggle('rc-open');localStorage.setItem('rc_acc_'+scope+'_'+idx,panel.classList.contains('rc-open')?'1':'0');setTimeout(()=>this.resizeCharts(),90);this.refreshIcons()});panel.appendChild(head);panel.appendChild(body);let saved=localStorage.getItem('rc_acc_'+scope+'_'+idx);if(saved!==

===== paidValue @ 2150536 =====
-[.16em] text-emerald-700">Recorrências</p><p class="text-2xl font-black mt-1" x-text="dayPendingRecurrences.length"></p><p class="text-xs text-slate-500">pendente(s) no mês</p></div>
            </div>
            <div class="grid xl:grid-cols-2 gap-3">
              <div class="space-y-2">
                <div class="flex items-center justify-between"><h4 class="font-black">Ações recomendadas</h4><span class="badge" x-text="dayCenterRows.length+' item(ns)'"></span></div>
                <div class="space-y-2 max-h-80 overflow-auto scrollbar">
                  <template x-for="item in dayCenterRows.slice(0,8)" :key="item.key">
                    <div class="day-center-row">
                      <div class="flex items-start justify-between gap-3">
                        <div class="min-w-0">
                          <p class="font-black truncate" x-text="item.title"></p>
                          <p class="text-xs text-slate-500 dark:text-slate-400" x-text="item.subtitle"></p>
                        </div>
                        <span class="text-xs font-black px-2 py-1 rounded-full shrink-0" :class="item.tone==='danger'?'bg-red-500/10 text-red-700':item.tone==='warning'?'bg-amber-500/10 text-amber-700':'bg-emerald-500/10 text-emerald-700'" x-text="item.badge"></span>
                      </div>
                      <div class="mt-2 flex items-center justify-between gap-2">
                        <b :class="item.tone==='danger'?'text-red-600':'text-emerald-700'" x-text="item.value"></b>
                        <div class="flex gap-2">
                          <button x-show="item.kind==='service'" @click="pay(item.service)" class="mini" :disabled="isViewer">Pagar</button>
                          <button x-show="item.kind==='service' && Number(item.service?.paidValue||0)>0" @click="quickReceipt(item.service)" class="mini">Recibo</button>
                          <button x-show="item.kind==='recurrence'" @click="generateRecurrence(item.recurrence)" class="mini" :disabled="isViewer">Gerar</button>
                        </div>
                      </div>
                    </div>
                  </template>
                  <p x-show="!dayCenterRows.length" class="text-sm text-slate-500 text-center py-4">Tudo em ordem para hoje.</p>
                </div>
              </div>
              <div class="rounded-3xl border border-emerald-500/20 bg-emerald-500/10 p-4">
                <div class="flex items-start gap-3">
                  <div class="h-10 w-10 rounded-2xl bg-emerald-600 text-white grid place-items-center shrink-0"><i data-lucide="shield-check" class="w-5 h-5"></i></div>
                  <div>
                    <h4 class="font-black">Leitura rápida</h4>
                    <p class="text-sm text-slate-600 dark:text-slate-300 mt-1" x-text="dayCenterSummary"></p>
                    <p class="text-xs text-slate-500 dark:text-slate-400 mt-3">Recorrência gera lançamento. Recibo continua sendo numerado somente quando emitido após pagamento.</p>
                  </div>
                </div>
              </div>
            </div>
          </div>

          <div class="grid xl:grid-cols-3 gap-4">
            <div class="glass rounded-3xl p-4 xl:col-span-2">
              <div class="flex flex-col sm:flex-row sm:items-center sm:justify-between gap-2 mb-3">
                <div><h3 class="font-black">Gasto mensal no ano</h3><p class="text-xs text-slate-500 dark:text-slate-400">Total pago por mês em <span x-text="dashboardYear"></span></p></div>
                <span class="badge" x-text="yearlyRows.length+' lançamento(s) no ano'"></span>
              </div>
              <div class="chart-box"><canvas id="monthlyChart"></canvas></div>
            </div>
            <div class="glass rounded-3xl p-4">
              <div class="flex items-center justify-between gap-2 mb-3"><h3 class="font-black">Pagamentos por categoria</h3><span class="badge" x-text="annualCategorySummary.length+' categorias'"></span></div>
              <div class="chart-box chart-box-sm"><canvas id="annualCatChart"></canvas></div>
            </div>
          </div>

          <div class="grid xl:grid-cols-3 gap-4">
            <div class="glass rounded-3xl p-4 xl:col-span-2 overflow-auto scrollbar">
              <div class="flex items-center justify-between mb-3"><h3 class="font-black">Histórico mensal consolidado</h3><span class="badge">Janeiro a dezembro</span></div>
              <table class="w-full text-sm min-w-[760px]">
                <thead class="text-left text-slate-500 dark:text-slate-400"><tr><th class="p-2">Mês</th><th class="p-2 text-right">Pagamentos</th><th class="p-2 text-right">Total pago</th><th class="p-2 text-right">Em aberto</th><th class="p-2">Principal categoria</th></tr></thead>
                <tbody>
                  <template x-for="m in monthlyHistory" :key="m.key">
                    <tr class="border-t border-slate-200/60 dark:border-slate-700/60">
                      <td class="p-2 font-black" x-text="m.label"></td>
                      <td class="p-2 text-right" x-text="m.count"></td>
                      <td class="p-2 text-right font-black text-emerald-700" x-text="money(m.paid)"></td>
                      <td class="p-2 text-right font-bold" :class="m.open>0?'text-red-600':'text-slate-500'" x-text="money(m.open)"></td>
                      <td class="p-2" x-text="m.topCategory"></td>
                    </tr>
                  </template>
                </tbody>
              </table>
            </div>
            <div class="glass rounded-3xl p-4">
              <h3 class="font-black mb-3">Top prestadores do ano</h3>
              <div class="space-y-2 max-h-[410px] overflow-auto scrollbar">
                <template x-for="(p,idx) in annualTopProviders" :key="p.id">
                  <div class="rounded-2xl border border-slate-200/60 dark:border-slate-700/60 p-3 flex items-center justify-between gap-3">
                    <div class="flex ite

===== paidValue @ 2163800 =====
selectedProvider?.id))"></b></div>
              <div class="rounded-2xl border border-slate-200/60 dark:border-slate-700/60 bg-white/70 dark:bg-slate-950/30 p-3"><p class="text-xs uppercase tracking-[.16em] text-slate-500 font-black">Serviços</p><b class="text-lg" x-text="providerServices(selectedProvider?.id).length"></b></div>
              <div class="rounded-2xl border border-slate-200/60 dark:border-slate-700/60 bg-white/70 dark:bg-slate-950/30 p-3"><p class="text-xs uppercase tracking-[.16em] text-slate-500 font-black">Recibos</p><b class="text-lg text-amber-700" x-text="providerReceipts(selectedProvider?.id).length"></b></div>
            </div>

            <div class="grid xl:grid-cols-3 gap-4">
              <div class="xl:col-span-2 rounded-2xl border border-slate-200/60 dark:border-slate-700/60 bg-white/70 dark:bg-slate-950/30 p-4">
                <div class="flex items-center justify-between gap-3 mb-3">
                  <h4 class="font-black">Últimos serviços</h4>
                  <span class="badge" x-text="providerServices(selectedProvider?.id).slice(0,5).length+' recentes'"></span>
                </div>
                <div class="space-y-2">
                  <template x-for="s in providerServices(selectedProvider?.id).slice(0,5)" :key="s.id">
                    <div class="rounded-2xl border border-slate-200/60 dark:border-slate-700/60 p-3 flex flex-col md:flex-row md:items-center md:justify-between gap-2">
                      <div>
                        <b x-text="s.category"></b>
                        <p class="text-xs text-slate-500" x-text="month(s.reference)+' • '+(s.description || 'sem descrição')"></p>
                      </div>
                      <div class="text-left md:text-right">
                        <b x-text="money(s.paidValue || s.amount)"></b>
                        <p class="text-xs" :class="s.status==='Pago'?'text-emerald-600':s.status==='Parcial'?'text-amber-600':'text-red-600'" x-text="s.status"></p>
                      </div>
                    </div>
                  </template>
                  <p x-show="!providerServices(selectedProvider?.id).length" class="text-sm text-slate-500">Nenhum serviço lançado para este prestador.</p>
                </div>
              </div>

              <div class="rounded-2xl border border-slate-200/60 dark:border-slate-700/60 bg-white/70 dark:bg-slate-950/30 p-4">
                <h4 class="font-black mb-3">Dados rápidos</h4>
                <div class="space-y-2 text-sm">
                  <p><span class="text-slate-500">Telefone:</span> <b x-text="selectedProvider?.phone || '-'"></b></p>
                  <p><span class="text-slate-500">WhatsApp:</span> <b x-text="selectedProvider?.whatsapp || '-'"></b></p>
                  <p><span class="text-slate-500">PIX/Banco:</span> <b x-text="selectedProvider?.pix || '-'"></b></p>
                  <p><span class="text-slate-500">Último serviço:</span> <b x-text="providerLastServiceLabel(selectedProvider?.id)"></b></p>
                </div>
                <div class="mt-4 pt-4 border-t border-slate-200/60 dark:border-slate-700/60">
                  <p class="text-xs uppercase tracking-[.16em] text-slate-500 font-black mb-2">Recibos recentes</p>
                  <template x-for="r in providerReceipts(selectedProvider?.id).slice(0,3)" :key="r.id">
                    <p class="text-sm flex justify-between gap-2"><span> Nº <b class="text-amber-700" x-text="r.number"></b></span><b x-text="money(r.total)"></b></p>
                  </template>
                  <p x-show="!providerReceipts(selectedProvider?.id).length" class="text-sm text-slate-500">Nenhum recibo emitido.</p>
                </div>
              </div>
            </div>
          </div>

          <div class="glass rounded-3xl p-4 md:p-5 overflow-hidden">
            <div class="flex flex-col lg:flex-row lg:items-center lg:justify-between gap-3 mb-4">
              <div>
                <p class="text-xs uppercase tracking-[.22em] text-slate-500 font-black">Base cadastrada</p>
                <h3 class="font-black text-xl">Prestadores cadastrados</h3>
                <p class="text-sm text-slate-500 dark:text-slate-400">Lista abaixo do cadastro para leitura mais limpa e menos apertada.</p>
              </div>
              <div class="flex flex-wrap gap-2">
                <span class="badge" x-text="providers.length+' registros'"></span>
                <span class="badge" x-text="filteredProviders.length+' visíveis'"></span>
              </div>
            </div>

            <div class="flex flex-wrap gap-2 mb-3">
              <input x-model="providerSearch" type="text" class="input flex-1 min-w-[180px]" placeholder="Buscar por nome, categoria, documento...">
              <select x-model="providerCategoryFilter" class="input w-auto">
                <option value="">Todas as categorias</option>
                <template x-for="c in categories"><option x-text="c"></option></template>
              </select>
              <button @click="providerSearch='';providerCategoryFilter=''" class="secondary px-3" x-show="providerSearch||providerCategoryFilter">Limpar</button>
            </div>
            <div class="overflow-auto scrollbar rounded-2xl border border-slate-200/60 dark:border-slate-700/60">
              <table class="w-full text-sm">
                <thead class="text-left text-slate-500 bg-slate-50/80 dark:bg-slate-900/50 sticky top-0 z-10">
                  <tr>
                    <th class="p-3 cursor-pointer select-none hover:text-rc-800 transition" @click="providerSortBy('name')">
                      Prestador <span x-text="providerSortField==='name'?(providerSortDir==='asc'?'↑':'↓'):''"></span>
                    </th>
                    <th class="p-3 cursor-pointer select-none hover:text-rc-800 transition" @click="providerSortBy('category')">
                      Categoria <span x-text="providerSortField==='category'?(providerSortDir==='asc'?'↑':

===== paidValue @ 2174406 =====
' pendência(s)'"></p></div>
              <div class="rounded-3xl border border-amber-500/20 bg-amber-500/10 p-3"><p class="text-xs uppercase tracking-[.16em] text-amber-700 font-black">Recorrências</p><p class="text-xl font-black text-amber-700" x-text="servicePanelRecurringGenerated.length+'/'+recurrences.filter(r=>r.active).length"></p><p class="text-xs text-amber-700/80">geradas no mês</p></div>
              <div class="rounded-3xl border border-slate-200/70 dark:border-slate-700/70 bg-white/60 dark:bg-slate-950/30 p-3"><p class="text-xs uppercase tracking-[.16em] text-slate-500 font-black">Recibos</p><p class="text-xl font-black text-rc-gold" x-text="servicePanelReceiptCount"></p><p class="text-xs text-slate-500">emitidos no mês</p></div>
            </div>
            <div class="grid xl:grid-cols-3 gap-3">
              <div class="rounded-3xl border border-slate-200/70 dark:border-slate-700/70 bg-white/50 dark:bg-slate-950/30 p-3 xl:col-span-2">
                <div class="flex items-center justify-between gap-2 mb-2"><h4 class="font-black">Últimos pagamentos do mês</h4><span class="badge" x-text="month(servicePanelMonth)"></span></div>
                <div class="space-y-2">
                  <template x-for="s in servicePanelPaidRows.slice(0,5)" :key="s.id">
                    <div class="flex items-center justify-between gap-3 rounded-2xl bg-white/70 dark:bg-slate-900/40 border border-slate-200/60 dark:border-slate-700/60 p-2">
                      <div><p class="font-black text-sm" x-text="providerName(s.providerId)"></p><p class="text-xs text-slate-500" x-text="fmt(s.paymentDate)+' • '+s.category+' • '+(s.paymentMethod||'forma não informada')"></p></div>
                      <div class="text-right"><p class="font-black text-emerald-700" x-text="money(s.paidValue||0)"></p><button @click="quickReceipt(s)" class="mini" :disabled="isViewer" x-text="reportReceiptNumber(s)==='-'?'Emitir recibo':'Ver recibo'"></button></div>
                    </div>
                  </template>
                  <p x-show="!servicePanelPaidRows.length" class="text-sm text-slate-500 text-center py-3">Nenhum pagamento registrado neste mês.</p>
                </div>
              </div>
              <div class="rounded-3xl border border-slate-200/70 dark:border-slate-700/70 bg-white/50 dark:bg-slate-950/30 p-3">
                <h4 class="font-black mb-2">Pendências e recorrências</h4>
                <div class="space-y-2">
                  <template x-for="s in servicePanelOpenRows.slice(0,4)" :key="'open-'+s.id">
                    <div class="rounded-2xl border border-red-500/15 bg-red-500/5 p-2"><p class="font-black text-sm" x-text="providerName(s.providerId)"></p><p class="text-xs text-red-700" x-text="money(balance(s))+' • '+overdueLabel(s)"></p></div>
                  </template>
                  <template x-for="r in servicePanelPendingRecurrences.slice(0,3)" :key="'rec-'+r.id">
                    <div class="rounded-2xl border border-amber-500/20 bg-amber-500/8 p-2"><p class="font-black text-sm" x-text="providerName(r.providerId)"></p><p class="text-xs text-amber-700" x-text="'Recorrência pendente • '+money(r.amount)"></p></div>
                  </template>
                  <p x-show="!servicePanelOpenRows.length && !servicePanelPendingRecurrences.length" class="text-sm text-slate-500 text-center py-3">Mês sem pendências operacionais.</p>
                </div>
              </div>
            </div>
          </div>
          <form @submit.prevent="saveService" class="glass rounded-3xl p-4 space-y-3">
            <div class="flex flex-col sm:flex-row sm:items-center sm:justify-between gap-2"><div><h3 class="font-black text-lg">Lançar serviço/pagamento</h3><p class="text-xs text-slate-500 dark:text-slate-400">Cadastre primeiro o serviço, gratificação ou pagamento avulso. A lista de lançamentos fica organizada logo abaixo.</p></div><span class="badge">novo lançamento</span></div>
            <div class="grid md:grid-cols-3 gap-3"><label class="md:col-span-1"><span class="text-sm font-bold">Prestador</span><select x-model="serviceForm.providerId" required class="input mt-1"><option value="">Selecione</option><template x-for="p in providers"><option :value="p.id" x-text="p.name"></option></template></select></label><label><span class="text-sm font-bold">Categoria</span><select x-model="serviceForm.category" class="input mt-1"><template x-for="c in categories"><option x-text="c"></option></template></select></label><label><span class="text-sm font-bold">Referência</span><input x-model="serviceForm.reference" type="month" required class="input mt-1"></label></div>
            <div class="grid sm:grid-cols-2 gap-3"><label><span class="text-sm font-bold">Data do serviço</span><input x-model="serviceForm.serviceDate" type="date" required class="input mt-1"></label><label><span class="text-sm font-bold">Vencimento</span><input x-model="serviceForm.dueDate" type="date" required class="input mt-1"></label></div>
            <div class="grid sm:grid-cols-2 gap-3"><label><span class="text-sm font-bold">Valor do serviço</span><input x-model.number="serviceForm.amount" type="number" step="0.01" required class="input mt-1"></label><label><span class="text-sm font-bold">Local/Área</span><input x-model="serviceForm.location" class="input mt-1" placeholder="Jardim, portão, bloco..."></label></div>
            <div class="rounded-3xl border border-emerald-500/20 bg-emerald-500/5 p-3 space-y-3">
              <div class="flex items-start gap-2"><i data-lucide="circle-dollar-sign" class="w-4 h-4 mt-0.5 text-emerald-700"></i><div><p class="text-sm font-black">Dados do pagamento</p><p class="text-xs text-slate-500 dark:text-slate-400">Preencha se o serviço já foi pago. Esses campos sairão no recibo oficial.</p></div></div>
              <div class="grid sm:grid-cols-3 gap-3">
                <label><span class="text-sm font-bold">Data do pagamento</span><input x-model="serviceForm.paymentDate" type=

===== paidValue @ 2179092 =====
 sm:grid-cols-2 gap-3"><label><span class="text-sm font-bold">Data do serviço</span><input x-model="serviceForm.serviceDate" type="date" required class="input mt-1"></label><label><span class="text-sm font-bold">Vencimento</span><input x-model="serviceForm.dueDate" type="date" required class="input mt-1"></label></div>
            <div class="grid sm:grid-cols-2 gap-3"><label><span class="text-sm font-bold">Valor do serviço</span><input x-model.number="serviceForm.amount" type="number" step="0.01" required class="input mt-1"></label><label><span class="text-sm font-bold">Local/Área</span><input x-model="serviceForm.location" class="input mt-1" placeholder="Jardim, portão, bloco..."></label></div>
            <div class="rounded-3xl border border-emerald-500/20 bg-emerald-500/5 p-3 space-y-3">
              <div class="flex items-start gap-2"><i data-lucide="circle-dollar-sign" class="w-4 h-4 mt-0.5 text-emerald-700"></i><div><p class="text-sm font-black">Dados do pagamento</p><p class="text-xs text-slate-500 dark:text-slate-400">Preencha se o serviço já foi pago. Esses campos sairão no recibo oficial.</p></div></div>
              <div class="grid sm:grid-cols-3 gap-3">
                <label><span class="text-sm font-bold">Data do pagamento</span><input x-model="serviceForm.paymentDate" type="date" class="input mt-1"></label>
                <label><span class="text-sm font-bold">Forma de pagamento</span><select x-model="serviceForm.paymentMethod" class="input mt-1"><option value="">Não informado</option><option>PIX</option><option>Dinheiro</option><option>Transferência</option><option>Boleto</option><option>Cheque</option><option>Cartão</option></select></label>
                <label><span class="text-sm font-bold">Valor pago</span><input x-model.number="serviceForm.paidValue" type="number" step="0.01" class="input mt-1" placeholder="0,00"></label>
              </div>
            </div>
            <label><span class="text-sm font-bold">Descrição detalhada</span><textarea x-model="serviceForm.description" required class="input mt-1 min-h-[90px]"></textarea></label>
            <button class="primary w-full" :disabled="isViewer">Salvar lançamento</button>
          </form>
          <div class="glass rounded-3xl p-4 space-y-4">
            <div class="flex flex-col md:flex-row md:items-center md:justify-between gap-3">
              <div>
                <p class="text-xs uppercase tracking-[.18em] text-rc-600 dark:text-rc-300 font-black">Automação segura</p>
                <h3 class="font-black text-lg">Despesas recorrentes</h3>
                <p class="text-xs text-slate-500 dark:text-slate-400">Cadastre serviços fixos mensais. A recorrência gera o lançamento do mês, mas o recibo só é numerado quando você emitir.</p>
              </div>
              <div class="flex gap-2">
                <button @click="generateAllRecurrencesForMonth" class="primary" :disabled="isViewer">Gerar mês atual</button>
              </div>
            </div>
            <form @submit.prevent="saveRecurrence" class="rounded-3xl border border-slate-200/70 dark:border-slate-700/70 bg-white/50 dark:bg-slate-950/30 p-3 space-y-3">
              <div class="grid md:grid-cols-4 gap-3">
                <label class="md:col-span-1"><span class="text-sm font-bold">Prestador</span><select x-model="recurrenceForm.providerId" required class="input mt-1"><option value="">Selecione</option><template x-for="p in providers"><option :value="p.id" x-text="p.name"></option></template></select></label>
                <label><span class="text-sm font-bold">Categoria</span><select x-model="recurrenceForm.category" class="input mt-1"><template x-for="c in categories"><option x-text="c"></option></template></select></label>
                <label><span class="text-sm font-bold">Valor padrão</span><input x-model.number="recurrenceForm.amount" type="number" step="0.01" required class="input mt-1"></label>
                <label><span class="text-sm font-bold">Dia vencimento</span><input x-model.number="recurrenceForm.dueDay" type="number" min="1" max="31" required class="input mt-1"></label>
              </div>
              <div class="grid md:grid-cols-4 gap-3">
                <label><span class="text-sm font-bold">Dia pagamento previsto</span><input x-model.number="recurrenceForm.paymentDay" type="number" min="1" max="31" class="input mt-1"></label>
                <label><span class="text-sm font-bold">Forma padrão</span><select x-model="recurrenceForm.paymentMethod" class="input mt-1"><option>PIX</option><option>Dinheiro</option><option>Transferência</option><option>Boleto</option><option>Cheque</option><option>Cartão</option></select></label>
                <label><span class="text-sm font-bold">Local/Área</span><input x-model="recurrenceForm.location" class="input mt-1" placeholder="Jardim, portão, bloco..."></label>
                <label class="flex items-end gap-2 pb-3"><input x-model="recurrenceForm.active" type="checkbox" class="w-4 h-4"><span class="text-sm font-bold">Ativa</span></label>
              </div>
              <label><span class="text-sm font-bold">Descrição padrão</span><textarea x-model="recurrenceForm.description" required class="input mt-1 min-h-[70px]"></textarea></label>
              <div class="grid sm:grid-cols-2 gap-2">
                <button class="primary w-full" :disabled="isViewer" x-text="recurrenceForm.id?'Atualizar recorrência':'Salvar recorrência'"></button>
                <button type="button" @click="resetRecurrence" class="secondary w-full">Limpar</button>
              </div>
            </form>
            <div class="grid xl:grid-cols-2 gap-3">
              <template x-for="r in recurrences" :key="r.id">
                <div class="rounded-3xl border border-slate-200/70 dark:border-slate-700/70 bg-white/55 dark:bg-slate-950/30 p-3">
                  <div class="flex items-start justify-between gap-3">
                    <div>
                      <span class="badge" x

===== paidValue @ 2186716 =====
s="flex items-center justify-between gap-2"><h3 class="font-black text-lg">Serviços e gratificações</h3><span class="badge" x-text="filteredServices.length+' visíveis'"></span></div><div class="grid sm:grid-cols-2 xl:grid-cols-4 gap-2"><select x-model="serviceFilter" class="input"><option value="todos">Todos os status</option><option>Pendente</option><option>Parcial</option><option>Pago</option></select><select x-model="serviceCategoryFilter" class="input"><option value="todas">Todas as categorias</option><template x-for="c in categories"><option x-text="c"></option></template></select><select x-model="serviceProviderFilter" class="input"><option value="todos">Todos os prestadores</option><template x-for="p in providers"><option :value="p.id" x-text="p.name"></option></template></select><input x-model="serviceMonthFilter" type="month" class="input" title="Filtrar por referência"></div><button @click="clearServiceFilters" class="secondary w-full sm:w-auto sm:self-end">Limpar filtros</button></div><div class="overflow-auto scrollbar"><table class="w-full text-sm"><thead class="text-left text-slate-500"><tr><th class="p-2">Prestador</th><th class="p-2">Serviço</th><th class="p-2">Valor</th><th class="p-2">Status</th><th class="p-2">Ações</th></tr></thead><tbody><template x-for="s in filteredServices" :key="s.id"><tr class="border-t border-slate-200/60 dark:border-slate-700/60"><td class="p-2"><b x-text="providerName(s.providerId)"></b><br><span class="text-xs text-slate-500" x-text="month(s.reference)+' • '+fmt(s.serviceDate)"></span></td><td class="p-2"><span class="badge" x-text="s.category"></span><br><span class="text-xs" x-text="s.description"></span></td><td class="p-2"><b x-text="money(s.amount)"></b><br><span class="text-xs text-slate-500" x-text="'Pago: '+money(s.paidValue||0)"></span><br><span class="text-xs text-slate-500" x-show="s.paymentDate" x-text="fmt(s.paymentDate)+' • '+(s.paymentMethod||'forma não informada')"></span></td><td class="p-2"><span class="badge" :class="statusClass(s.status)" x-text="s.status"></span><br><span class="text-xs text-red-600" x-show="balance(s)>0" x-text="overdueLabel(s)"></span></td><td class="p-2 min-w-[150px]"><select class="input py-2 text-xs font-black" @change="serviceAction($event.target.value,s); $event.target.value=''"><option value="">Ações</option><option value="pagar" :disabled="isViewer">Pagar</option><option value="recibo" :disabled="isViewer || Number(s.paidValue||0)<=0" x-text="reportReceiptNumber(s)==='-'?'Emitir recibo':'Ver recibo'"></option><option value="duplicar" :disabled="isViewer">Duplicar</option><option value="excluir" :disabled="isViewer">Excluir</option></select></td></tr></template></tbody></table></div></div>
        </section>


        <section x-show="view==='pagamentos'" class="space-y-4">
          <div class="glass rounded-3xl p-4 space-y-4">
            <div class="flex flex-col lg:flex-row lg:items-center lg:justify-between gap-3">
              <div>
                <p class="text-xs uppercase tracking-[.22em] text-rc-gold font-black">Central financeira</p>
                <h3 class="font-black text-xl">Pagamentos</h3>
                <p class="text-xs text-slate-500 dark:text-slate-400">Esta guia puxa os lançamentos de Serviços. O pagamento continua sendo registrado pela janela flutuante, sem duplicar dados.</p>
              </div>
              <div class="grid sm:grid-cols-3 gap-2 lg:min-w-[620px]">
                <input x-model="paymentMonthFilter" type="month" class="input">
                <select x-model="paymentStatusFilter" class="input"><option value="todos">Todos os status</option><option>Pendente</option><option>Parcial</option><option>Pago</option></select>
                <select x-model="paymentProviderFilter" class="input"><option value="todos">Todos os prestadores</option><template x-for="p in providers"><option :value="p.id" x-text="p.name"></option></template></select>
              </div>
            </div>
            <div class="grid sm:grid-cols-2 xl:grid-cols-6 gap-3">
              <div class="rounded-3xl border border-slate-200/70 dark:border-slate-700/70 bg-white/60 dark:bg-slate-950/30 p-3"><p class="text-xs uppercase tracking-[.16em] text-slate-500 font-black">Previsto</p><p class="text-xl font-black text-rc-800 dark:text-white" x-text="money(paymentSummary.expected)"></p><p class="text-xs text-slate-500" x-text="filteredPayments.length+' lançamento(s)'"></p></div>
              <div class="rounded-3xl border border-emerald-500/20 bg-emerald-500/10 p-3"><p class="text-xs uppercase tracking-[.16em] text-emerald-700 font-black">Pago</p><p class="text-xl font-black text-emerald-700" x-text="money(paymentSummary.paid)"></p><p class="text-xs text-emerald-700/80" x-text="paymentSummary.paidCount+' pagamento(s)'"></p></div>
              <div class="rounded-3xl border border-red-500/20 bg-red-500/10 p-3"><p class="text-xs uppercase tracking-[.16em] text-red-700 font-black">Em aberto</p><p class="text-xl font-black text-red-700" x-text="money(paymentSummary.open)"></p><p class="text-xs text-red-700/80">pendente/parcial</p></div>
              <div class="rounded-3xl border border-amber-500/20 bg-amber-500/10 p-3"><p class="text-xs uppercase tracking-[.16em] text-amber-700 font-black">Vencidos</p><p class="text-xl font-black text-amber-700" x-text="paymentSummary.overdue"></p><p class="text-xs text-amber-700/80">fora do prazo</p></div>
              <div class="rounded-3xl border border-blue-500/20 bg-blue-500/10 p-3"><p class="text-xs uppercase tracking-[.16em] text-blue-700 font-black">Vence hoje</p><p class="text-xl font-black text-blue-700" x-text="paymentSummary.today"></p><p class="text-xs text-blue-700/80">ação imediata</p></div>
              <div class="rounded-3xl border border-rc-gold/30 bg-amber-50 p-3"><p class="text-xs uppercase tracking-[.16em] text-amber-800 font-black">Sem recibo</p><p class="text-xl font-black text-rc-gold" x-text="paymentSummary.missingReceipt"></p><

===== paidValue @ 2187369 =====
n value="todos">Todos os prestadores</option><template x-for="p in providers"><option :value="p.id" x-text="p.name"></option></template></select><input x-model="serviceMonthFilter" type="month" class="input" title="Filtrar por referência"></div><button @click="clearServiceFilters" class="secondary w-full sm:w-auto sm:self-end">Limpar filtros</button></div><div class="overflow-auto scrollbar"><table class="w-full text-sm"><thead class="text-left text-slate-500"><tr><th class="p-2">Prestador</th><th class="p-2">Serviço</th><th class="p-2">Valor</th><th class="p-2">Status</th><th class="p-2">Ações</th></tr></thead><tbody><template x-for="s in filteredServices" :key="s.id"><tr class="border-t border-slate-200/60 dark:border-slate-700/60"><td class="p-2"><b x-text="providerName(s.providerId)"></b><br><span class="text-xs text-slate-500" x-text="month(s.reference)+' • '+fmt(s.serviceDate)"></span></td><td class="p-2"><span class="badge" x-text="s.category"></span><br><span class="text-xs" x-text="s.description"></span></td><td class="p-2"><b x-text="money(s.amount)"></b><br><span class="text-xs text-slate-500" x-text="'Pago: '+money(s.paidValue||0)"></span><br><span class="text-xs text-slate-500" x-show="s.paymentDate" x-text="fmt(s.paymentDate)+' • '+(s.paymentMethod||'forma não informada')"></span></td><td class="p-2"><span class="badge" :class="statusClass(s.status)" x-text="s.status"></span><br><span class="text-xs text-red-600" x-show="balance(s)>0" x-text="overdueLabel(s)"></span></td><td class="p-2 min-w-[150px]"><select class="input py-2 text-xs font-black" @change="serviceAction($event.target.value,s); $event.target.value=''"><option value="">Ações</option><option value="pagar" :disabled="isViewer">Pagar</option><option value="recibo" :disabled="isViewer || Number(s.paidValue||0)<=0" x-text="reportReceiptNumber(s)==='-'?'Emitir recibo':'Ver recibo'"></option><option value="duplicar" :disabled="isViewer">Duplicar</option><option value="excluir" :disabled="isViewer">Excluir</option></select></td></tr></template></tbody></table></div></div>
        </section>


        <section x-show="view==='pagamentos'" class="space-y-4">
          <div class="glass rounded-3xl p-4 space-y-4">
            <div class="flex flex-col lg:flex-row lg:items-center lg:justify-between gap-3">
              <div>
                <p class="text-xs uppercase tracking-[.22em] text-rc-gold font-black">Central financeira</p>
                <h3 class="font-black text-xl">Pagamentos</h3>
                <p class="text-xs text-slate-500 dark:text-slate-400">Esta guia puxa os lançamentos de Serviços. O pagamento continua sendo registrado pela janela flutuante, sem duplicar dados.</p>
              </div>
              <div class="grid sm:grid-cols-3 gap-2 lg:min-w-[620px]">
                <input x-model="paymentMonthFilter" type="month" class="input">
                <select x-model="paymentStatusFilter" class="input"><option value="todos">Todos os status</option><option>Pendente</option><option>Parcial</option><option>Pago</option></select>
                <select x-model="paymentProviderFilter" class="input"><option value="todos">Todos os prestadores</option><template x-for="p in providers"><option :value="p.id" x-text="p.name"></option></template></select>
              </div>
            </div>
            <div class="grid sm:grid-cols-2 xl:grid-cols-6 gap-3">
              <div class="rounded-3xl border border-slate-200/70 dark:border-slate-700/70 bg-white/60 dark:bg-slate-950/30 p-3"><p class="text-xs uppercase tracking-[.16em] text-slate-500 font-black">Previsto</p><p class="text-xl font-black text-rc-800 dark:text-white" x-text="money(paymentSummary.expected)"></p><p class="text-xs text-slate-500" x-text="filteredPayments.length+' lançamento(s)'"></p></div>
              <div class="rounded-3xl border border-emerald-500/20 bg-emerald-500/10 p-3"><p class="text-xs uppercase tracking-[.16em] text-emerald-700 font-black">Pago</p><p class="text-xl font-black text-emerald-700" x-text="money(paymentSummary.paid)"></p><p class="text-xs text-emerald-700/80" x-text="paymentSummary.paidCount+' pagamento(s)'"></p></div>
              <div class="rounded-3xl border border-red-500/20 bg-red-500/10 p-3"><p class="text-xs uppercase tracking-[.16em] text-red-700 font-black">Em aberto</p><p class="text-xl font-black text-red-700" x-text="money(paymentSummary.open)"></p><p class="text-xs text-red-700/80">pendente/parcial</p></div>
              <div class="rounded-3xl border border-amber-500/20 bg-amber-500/10 p-3"><p class="text-xs uppercase tracking-[.16em] text-amber-700 font-black">Vencidos</p><p class="text-xl font-black text-amber-700" x-text="paymentSummary.overdue"></p><p class="text-xs text-amber-700/80">fora do prazo</p></div>
              <div class="rounded-3xl border border-blue-500/20 bg-blue-500/10 p-3"><p class="text-xs uppercase tracking-[.16em] text-blue-700 font-black">Vence hoje</p><p class="text-xl font-black text-blue-700" x-text="paymentSummary.today"></p><p class="text-xs text-blue-700/80">ação imediata</p></div>
              <div class="rounded-3xl border border-rc-gold/30 bg-amber-50 p-3"><p class="text-xs uppercase tracking-[.16em] text-amber-800 font-black">Sem recibo</p><p class="text-xl font-black text-rc-gold" x-text="paymentSummary.missingReceipt"></p><p class="text-xs text-amber-800/80">pagos sem recibo</p></div>
            </div>
          </div>

          <div class="grid xl:grid-cols-3 gap-4">
            <div class="glass rounded-3xl p-4 xl:col-span-2 overflow-hidden">
              <div class="flex items-center justify-between gap-2 mb-3"><div><h3 class="font-black text-lg">Fila de pagamentos</h3><p class="text-xs text-slate-500">Pague, confira recibos e acompanhe vencimentos sem voltar para Serviços.</p></div><span class="badge" x-text="filteredPayments.length+' itens'"></span></div>
              <div class="overflow-auto scrollbar rounded-2xl border border-slate-200/60 dark:border-s

===== paidValue @ 2192662 =====
-rc-gold" x-text="paymentSummary.missingReceipt"></p><p class="text-xs text-amber-800/80">pagos sem recibo</p></div>
            </div>
          </div>

          <div class="grid xl:grid-cols-3 gap-4">
            <div class="glass rounded-3xl p-4 xl:col-span-2 overflow-hidden">
              <div class="flex items-center justify-between gap-2 mb-3"><div><h3 class="font-black text-lg">Fila de pagamentos</h3><p class="text-xs text-slate-500">Pague, confira recibos e acompanhe vencimentos sem voltar para Serviços.</p></div><span class="badge" x-text="filteredPayments.length+' itens'"></span></div>
              <div class="overflow-auto scrollbar rounded-2xl border border-slate-200/60 dark:border-slate-700/60">
                <table class="w-full text-sm">
                  <thead class="text-left text-slate-500 bg-slate-50/80 dark:bg-slate-900/50"><tr><th class="p-3">Vencimento</th><th class="p-3">Prestador / Serviço</th><th class="p-3">Valores</th><th class="p-3">Status</th><th class="p-3">Ações</th></tr></thead>
                  <tbody>
                    <template x-for="s in filteredPayments" :key="s.id">
                      <tr class="border-t border-slate-200/60 dark:border-slate-700/60 hover:bg-emerald-500/5 transition">
                        <td class="p-3 min-w-[120px]"><b x-text="fmt(s.dueDate)"></b><br><span class="text-xs text-slate-500" x-text="month(s.reference)"></span></td>
                        <td class="p-3 min-w-[260px]"><b x-text="providerName(s.providerId)"></b><br><span class="badge" x-text="s.category"></span><p class="text-xs text-slate-500 mt-1" x-text="s.description||'-'"></p></td>
                        <td class="p-3 min-w-[160px]"><b x-text="money(s.amount)"></b><br><span class="text-xs text-emerald-700" x-text="'Pago: '+money(s.paidValue||0)"></span><br><span class="text-xs text-red-600" x-show="balance(s)>0" x-text="'Aberto: '+money(balance(s))"></span></td>
                        <td class="p-3 min-w-[150px]"><span class="badge" :class="statusClass(s.status)" x-text="s.status"></span><br><span class="text-xs text-slate-500" x-show="s.paymentDate" x-text="fmt(s.paymentDate)+' • '+(s.paymentMethod||'forma não informada')"></span><span class="text-xs text-red-600" x-show="balance(s)>0" x-text="overdueLabel(s)"></span></td>
                        <td class="p-3 min-w-[150px]"><select class="input py-2 text-xs font-black" @change="paymentAction($event.target.value,s); $event.target.value=''"><option value="">Ações</option><option value="pagar" :disabled="isViewer || s.status==='Pago'">Pagar</option><option value="recibo" :disabled="isViewer || Number(s.paidValue||0)<=0" x-text="reportReceiptNumber(s)==='-'?'Emitir recibo':'Ver recibo'"></option><option value="servico">Abrir serviço</option><option value="excluirPagamento" :disabled="isViewer || Number(s.paidValue||0)<=0">Excluir pagamento</option></select></td>
                      </tr>
                    </template>
                    <tr x-show="!filteredPayments.length"><td colspan="5" class="p-6 text-center text-slate-500">Nenhum pagamento encontrado para os filtros selecionados.</td></tr>
                  </tbody>
                </table>
              </div>
            </div>
            <div class="glass rounded-3xl p-4 space-y-3">
              <h3 class="font-black text-lg">Últimos pagamentos</h3>
              <template x-for="s in paymentRecent" :key="'recent-pay-'+s.id">
                <div class="rounded-2xl border border-slate-200/70 dark:border-slate-700/70 bg-white/55 dark:bg-slate-950/30 p-3">
                  <div class="flex items-start justify-between gap-3"><div><p class="font-black text-sm" x-text="providerName(s.providerId)"></p><p class="text-xs text-slate-500" x-text="fmt(s.paymentDate)+' • '+(s.paymentMethod||'forma não informada')"></p></div><p class="font-black text-emerald-700" x-text="money(s.paidValue||0)"></p></div>
                  <div class="flex flex-wrap gap-2 mt-2"><button @click="quickReceipt(s)" class="mini" :disabled="isViewer" x-text="reportReceiptNumber(s)==='-'?'Emitir recibo':'Ver recibo'"></button><button @click="openSearchService(s)" class="mini">Serviço</button><button @click="openDeletePaymentModal(s)" class="danger" :disabled="isViewer || Number(s.paidValue||0)<=0">Excluir pagamento</button></div>
                </div>
              </template>
              <p x-show="!paymentRecent.length" class="text-sm text-slate-500 text-center py-3">Nenhum pagamento registrado ainda.</p>
            </div>
          </div>
        </section>

        <section x-show="view==='recibos'" class="space-y-4">
          <div class="glass rounded-3xl p-4 space-y-4">
            <div class="flex flex-col md:flex-row md:items-center md:justify-between gap-3">
              <div>
                <p class="text-xs uppercase tracking-[.18em] text-emerald-600 dark:text-emerald-400 font-black">Emissão</p>
                <h3 class="font-black text-lg">Emitir recibo profissional</h3>
                <p class="text-xs text-slate-500 dark:text-slate-400">Selecione um serviço/pagamento já lançado para gerar o recibo formal.</p>
              </div>
              <div class="rounded-2xl px-4 py-3 bg-emerald-500/10 border border-emerald-500/20 text-sm">
                <b x-text="receipts.length"></b> recibos no histórico
              </div>
            </div>
            <div class="grid lg:grid-cols-[1fr_auto] gap-3 items-end">
              <label>
                <span class="text-sm font-bold">Lançamento para emissão</span>
                <select x-model="receiptServiceId" class="input mt-1">
                  <option value="">Selecione um serviço/pagamento</option>
                  <template x-for="s in services">
                    <option :value="s.id" x-text="'#'+s.id+' • '+providerName(s.providerId)+' • '+s.category+' • '+money(s.amount)"></option>
                  </template>
                </select>
              </label>
              <div class="grid

===== paidValue @ 2193502 =====
dark:bg-slate-900/50"><tr><th class="p-3">Vencimento</th><th class="p-3">Prestador / Serviço</th><th class="p-3">Valores</th><th class="p-3">Status</th><th class="p-3">Ações</th></tr></thead>
                  <tbody>
                    <template x-for="s in filteredPayments" :key="s.id">
                      <tr class="border-t border-slate-200/60 dark:border-slate-700/60 hover:bg-emerald-500/5 transition">
                        <td class="p-3 min-w-[120px]"><b x-text="fmt(s.dueDate)"></b><br><span class="text-xs text-slate-500" x-text="month(s.reference)"></span></td>
                        <td class="p-3 min-w-[260px]"><b x-text="providerName(s.providerId)"></b><br><span class="badge" x-text="s.category"></span><p class="text-xs text-slate-500 mt-1" x-text="s.description||'-'"></p></td>
                        <td class="p-3 min-w-[160px]"><b x-text="money(s.amount)"></b><br><span class="text-xs text-emerald-700" x-text="'Pago: '+money(s.paidValue||0)"></span><br><span class="text-xs text-red-600" x-show="balance(s)>0" x-text="'Aberto: '+money(balance(s))"></span></td>
                        <td class="p-3 min-w-[150px]"><span class="badge" :class="statusClass(s.status)" x-text="s.status"></span><br><span class="text-xs text-slate-500" x-show="s.paymentDate" x-text="fmt(s.paymentDate)+' • '+(s.paymentMethod||'forma não informada')"></span><span class="text-xs text-red-600" x-show="balance(s)>0" x-text="overdueLabel(s)"></span></td>
                        <td class="p-3 min-w-[150px]"><select class="input py-2 text-xs font-black" @change="paymentAction($event.target.value,s); $event.target.value=''"><option value="">Ações</option><option value="pagar" :disabled="isViewer || s.status==='Pago'">Pagar</option><option value="recibo" :disabled="isViewer || Number(s.paidValue||0)<=0" x-text="reportReceiptNumber(s)==='-'?'Emitir recibo':'Ver recibo'"></option><option value="servico">Abrir serviço</option><option value="excluirPagamento" :disabled="isViewer || Number(s.paidValue||0)<=0">Excluir pagamento</option></select></td>
                      </tr>
                    </template>
                    <tr x-show="!filteredPayments.length"><td colspan="5" class="p-6 text-center text-slate-500">Nenhum pagamento encontrado para os filtros selecionados.</td></tr>
                  </tbody>
                </table>
              </div>
            </div>
            <div class="glass rounded-3xl p-4 space-y-3">
              <h3 class="font-black text-lg">Últimos pagamentos</h3>
              <template x-for="s in paymentRecent" :key="'recent-pay-'+s.id">
                <div class="rounded-2xl border border-slate-200/70 dark:border-slate-700/70 bg-white/55 dark:bg-slate-950/30 p-3">
                  <div class="flex items-start justify-between gap-3"><div><p class="font-black text-sm" x-text="providerName(s.providerId)"></p><p class="text-xs text-slate-500" x-text="fmt(s.paymentDate)+' • '+(s.paymentMethod||'forma não informada')"></p></div><p class="font-black text-emerald-700" x-text="money(s.paidValue||0)"></p></div>
                  <div class="flex flex-wrap gap-2 mt-2"><button @click="quickReceipt(s)" class="mini" :disabled="isViewer" x-text="reportReceiptNumber(s)==='-'?'Emitir recibo':'Ver recibo'"></button><button @click="openSearchService(s)" class="mini">Serviço</button><button @click="openDeletePaymentModal(s)" class="danger" :disabled="isViewer || Number(s.paidValue||0)<=0">Excluir pagamento</button></div>
                </div>
              </template>
              <p x-show="!paymentRecent.length" class="text-sm text-slate-500 text-center py-3">Nenhum pagamento registrado ainda.</p>
            </div>
          </div>
        </section>

        <section x-show="view==='recibos'" class="space-y-4">
          <div class="glass rounded-3xl p-4 space-y-4">
            <div class="flex flex-col md:flex-row md:items-center md:justify-between gap-3">
              <div>
                <p class="text-xs uppercase tracking-[.18em] text-emerald-600 dark:text-emerald-400 font-black">Emissão</p>
                <h3 class="font-black text-lg">Emitir recibo profissional</h3>
                <p class="text-xs text-slate-500 dark:text-slate-400">Selecione um serviço/pagamento já lançado para gerar o recibo formal.</p>
              </div>
              <div class="rounded-2xl px-4 py-3 bg-emerald-500/10 border border-emerald-500/20 text-sm">
                <b x-text="receipts.length"></b> recibos no histórico
              </div>
            </div>
            <div class="grid lg:grid-cols-[1fr_auto] gap-3 items-end">
              <label>
                <span class="text-sm font-bold">Lançamento para emissão</span>
                <select x-model="receiptServiceId" class="input mt-1">
                  <option value="">Selecione um serviço/pagamento</option>
                  <template x-for="s in services">
                    <option :value="s.id" x-text="'#'+s.id+' • '+providerName(s.providerId)+' • '+s.category+' • '+money(s.amount)"></option>
                  </template>
                </select>
              </label>
              <div class="grid sm:grid-cols-2 gap-2 lg:min-w-[360px]">
                <button @click="issueReceipt" class="primary w-full" :disabled="isViewer">Emitir recibo</button>
                <button @click="printReceipt" class="secondary w-full">Imprimir / Salvar PDF</button>
              </div>
            </div>
            <div class="rounded-2xl border border-slate-200/60 dark:border-slate-700/60 bg-white/50 dark:bg-slate-950/30 p-3 text-xs text-slate-500 dark:text-slate-400">
              Use <b>Imprimir / Salvar PDF</b> e escolha “Salvar como PDF”. O recibo agora sai em layout A4 premium/oficial compacto, ajustado para caber em uma única página, com quadro de conferência, valor por extenso, assinatura e rodapé para prestação de contas.
            </div>
          </div>

          <div class="glass rounded-3xl p-4">
            <div class="

===== paidValue @ 2193707 =====
     <tbody>
                    <template x-for="s in filteredPayments" :key="s.id">
                      <tr class="border-t border-slate-200/60 dark:border-slate-700/60 hover:bg-emerald-500/5 transition">
                        <td class="p-3 min-w-[120px]"><b x-text="fmt(s.dueDate)"></b><br><span class="text-xs text-slate-500" x-text="month(s.reference)"></span></td>
                        <td class="p-3 min-w-[260px]"><b x-text="providerName(s.providerId)"></b><br><span class="badge" x-text="s.category"></span><p class="text-xs text-slate-500 mt-1" x-text="s.description||'-'"></p></td>
                        <td class="p-3 min-w-[160px]"><b x-text="money(s.amount)"></b><br><span class="text-xs text-emerald-700" x-text="'Pago: '+money(s.paidValue||0)"></span><br><span class="text-xs text-red-600" x-show="balance(s)>0" x-text="'Aberto: '+money(balance(s))"></span></td>
                        <td class="p-3 min-w-[150px]"><span class="badge" :class="statusClass(s.status)" x-text="s.status"></span><br><span class="text-xs text-slate-500" x-show="s.paymentDate" x-text="fmt(s.paymentDate)+' • '+(s.paymentMethod||'forma não informada')"></span><span class="text-xs text-red-600" x-show="balance(s)>0" x-text="overdueLabel(s)"></span></td>
                        <td class="p-3 min-w-[150px]"><select class="input py-2 text-xs font-black" @change="paymentAction($event.target.value,s); $event.target.value=''"><option value="">Ações</option><option value="pagar" :disabled="isViewer || s.status==='Pago'">Pagar</option><option value="recibo" :disabled="isViewer || Number(s.paidValue||0)<=0" x-text="reportReceiptNumber(s)==='-'?'Emitir recibo':'Ver recibo'"></option><option value="servico">Abrir serviço</option><option value="excluirPagamento" :disabled="isViewer || Number(s.paidValue||0)<=0">Excluir pagamento</option></select></td>
                      </tr>
                    </template>
                    <tr x-show="!filteredPayments.length"><td colspan="5" class="p-6 text-center text-slate-500">Nenhum pagamento encontrado para os filtros selecionados.</td></tr>
                  </tbody>
                </table>
              </div>
            </div>
            <div class="glass rounded-3xl p-4 space-y-3">
              <h3 class="font-black text-lg">Últimos pagamentos</h3>
              <template x-for="s in paymentRecent" :key="'recent-pay-'+s.id">
                <div class="rounded-2xl border border-slate-200/70 dark:border-slate-700/70 bg-white/55 dark:bg-slate-950/30 p-3">
                  <div class="flex items-start justify-between gap-3"><div><p class="font-black text-sm" x-text="providerName(s.providerId)"></p><p class="text-xs text-slate-500" x-text="fmt(s.paymentDate)+' • '+(s.paymentMethod||'forma não informada')"></p></div><p class="font-black text-emerald-700" x-text="money(s.paidValue||0)"></p></div>
                  <div class="flex flex-wrap gap-2 mt-2"><button @click="quickReceipt(s)" class="mini" :disabled="isViewer" x-text="reportReceiptNumber(s)==='-'?'Emitir recibo':'Ver recibo'"></button><button @click="openSearchService(s)" class="mini">Serviço</button><button @click="openDeletePaymentModal(s)" class="danger" :disabled="isViewer || Number(s.paidValue||0)<=0">Excluir pagamento</button></div>
                </div>
              </template>
              <p x-show="!paymentRecent.length" class="text-sm text-slate-500 text-center py-3">Nenhum pagamento registrado ainda.</p>
            </div>
          </div>
        </section>

        <section x-show="view==='recibos'" class="space-y-4">
          <div class="glass rounded-3xl p-4 space-y-4">
            <div class="flex flex-col md:flex-row md:items-center md:justify-between gap-3">
              <div>
                <p class="text-xs uppercase tracking-[.18em] text-emerald-600 dark:text-emerald-400 font-black">Emissão</p>
                <h3 class="font-black text-lg">Emitir recibo profissional</h3>
                <p class="text-xs text-slate-500 dark:text-slate-400">Selecione um serviço/pagamento já lançado para gerar o recibo formal.</p>
              </div>
              <div class="rounded-2xl px-4 py-3 bg-emerald-500/10 border border-emerald-500/20 text-sm">
                <b x-text="receipts.length"></b> recibos no histórico
              </div>
            </div>
            <div class="grid lg:grid-cols-[1fr_auto] gap-3 items-end">
              <label>
                <span class="text-sm font-bold">Lançamento para emissão</span>
                <select x-model="receiptServiceId" class="input mt-1">
                  <option value="">Selecione um serviço/pagamento</option>
                  <template x-for="s in services">
                    <option :value="s.id" x-text="'#'+s.id+' • '+providerName(s.providerId)+' • '+s.category+' • '+money(s.amount)"></option>
                  </template>
                </select>
              </label>
              <div class="grid sm:grid-cols-2 gap-2 lg:min-w-[360px]">
                <button @click="issueReceipt" class="primary w-full" :disabled="isViewer">Emitir recibo</button>
                <button @click="printReceipt" class="secondary w-full">Imprimir / Salvar PDF</button>
              </div>
            </div>
            <div class="rounded-2xl border border-slate-200/60 dark:border-slate-700/60 bg-white/50 dark:bg-slate-950/30 p-3 text-xs text-slate-500 dark:text-slate-400">
              Use <b>Imprimir / Salvar PDF</b> e escolha “Salvar como PDF”. O recibo agora sai em layout A4 premium/oficial compacto, ajustado para caber em uma única página, com quadro de conferência, valor por extenso, assinatura e rodapé para prestação de contas.
            </div>
          </div>

          <div class="glass rounded-3xl p-4">
            <div class="flex flex-col md:flex-row md:items-center md:justify-between gap-2 mb-3">
              <div>
                <p class="text-xs uppercase tracking-[.18em] text-rc-600 dark:text-rc-300 font-black">Histórico

===== paidValue @ 2194754 =====
t-slate-500" x-show="s.paymentDate" x-text="fmt(s.paymentDate)+' • '+(s.paymentMethod||'forma não informada')"></span><span class="text-xs text-red-600" x-show="balance(s)>0" x-text="overdueLabel(s)"></span></td>
                        <td class="p-3 min-w-[150px]"><select class="input py-2 text-xs font-black" @change="paymentAction($event.target.value,s); $event.target.value=''"><option value="">Ações</option><option value="pagar" :disabled="isViewer || s.status==='Pago'">Pagar</option><option value="recibo" :disabled="isViewer || Number(s.paidValue||0)<=0" x-text="reportReceiptNumber(s)==='-'?'Emitir recibo':'Ver recibo'"></option><option value="servico">Abrir serviço</option><option value="excluirPagamento" :disabled="isViewer || Number(s.paidValue||0)<=0">Excluir pagamento</option></select></td>
                      </tr>
                    </template>
                    <tr x-show="!filteredPayments.length"><td colspan="5" class="p-6 text-center text-slate-500">Nenhum pagamento encontrado para os filtros selecionados.</td></tr>
                  </tbody>
                </table>
              </div>
            </div>
            <div class="glass rounded-3xl p-4 space-y-3">
              <h3 class="font-black text-lg">Últimos pagamentos</h3>
              <template x-for="s in paymentRecent" :key="'recent-pay-'+s.id">
                <div class="rounded-2xl border border-slate-200/70 dark:border-slate-700/70 bg-white/55 dark:bg-slate-950/30 p-3">
                  <div class="flex items-start justify-between gap-3"><div><p class="font-black text-sm" x-text="providerName(s.providerId)"></p><p class="text-xs text-slate-500" x-text="fmt(s.paymentDate)+' • '+(s.paymentMethod||'forma não informada')"></p></div><p class="font-black text-emerald-700" x-text="money(s.paidValue||0)"></p></div>
                  <div class="flex flex-wrap gap-2 mt-2"><button @click="quickReceipt(s)" class="mini" :disabled="isViewer" x-text="reportReceiptNumber(s)==='-'?'Emitir recibo':'Ver recibo'"></button><button @click="openSearchService(s)" class="mini">Serviço</button><button @click="openDeletePaymentModal(s)" class="danger" :disabled="isViewer || Number(s.paidValue||0)<=0">Excluir pagamento</button></div>
                </div>
              </template>
              <p x-show="!paymentRecent.length" class="text-sm text-slate-500 text-center py-3">Nenhum pagamento registrado ainda.</p>
            </div>
          </div>
        </section>

        <section x-show="view==='recibos'" class="space-y-4">
          <div class="glass rounded-3xl p-4 space-y-4">
            <div class="flex flex-col md:flex-row md:items-center md:justify-between gap-3">
              <div>
                <p class="text-xs uppercase tracking-[.18em] text-emerald-600 dark:text-emerald-400 font-black">Emissão</p>
                <h3 class="font-black text-lg">Emitir recibo profissional</h3>
                <p class="text-xs text-slate-500 dark:text-slate-400">Selecione um serviço/pagamento já lançado para gerar o recibo formal.</p>
              </div>
              <div class="rounded-2xl px-4 py-3 bg-emerald-500/10 border border-emerald-500/20 text-sm">
                <b x-text="receipts.length"></b> recibos no histórico
              </div>
            </div>
            <div class="grid lg:grid-cols-[1fr_auto] gap-3 items-end">
              <label>
                <span class="text-sm font-bold">Lançamento para emissão</span>
                <select x-model="receiptServiceId" class="input mt-1">
                  <option value="">Selecione um serviço/pagamento</option>
                  <template x-for="s in services">
                    <option :value="s.id" x-text="'#'+s.id+' • '+providerName(s.providerId)+' • '+s.category+' • '+money(s.amount)"></option>
                  </template>
                </select>
              </label>
              <div class="grid sm:grid-cols-2 gap-2 lg:min-w-[360px]">
                <button @click="issueReceipt" class="primary w-full" :disabled="isViewer">Emitir recibo</button>
                <button @click="printReceipt" class="secondary w-full">Imprimir / Salvar PDF</button>
              </div>
            </div>
            <div class="rounded-2xl border border-slate-200/60 dark:border-slate-700/60 bg-white/50 dark:bg-slate-950/30 p-3 text-xs text-slate-500 dark:text-slate-400">
              Use <b>Imprimir / Salvar PDF</b> e escolha “Salvar como PDF”. O recibo agora sai em layout A4 premium/oficial compacto, ajustado para caber em uma única página, com quadro de conferência, valor por extenso, assinatura e rodapé para prestação de contas.
            </div>
          </div>

          <div class="glass rounded-3xl p-4">
            <div class="flex flex-col md:flex-row md:items-center md:justify-between gap-2 mb-3">
              <div>
                <p class="text-xs uppercase tracking-[.18em] text-rc-600 dark:text-rc-300 font-black">Histórico</p>
                <h3 class="font-black text-lg">Recibos emitidos</h3>
              </div>
              <span class="text-xs text-slate-500 dark:text-slate-400">Reimpressão rápida e controle dos comprovantes já emitidos.</span>
            </div>
            <div class="grid md:grid-cols-4 gap-3 mb-4">
              <label><span class="text-xs font-black uppercase tracking-[.12em] text-slate-500">Buscar</span><input x-model="receiptFilter.q" class="input mt-1" placeholder="Nº, prestador, categoria..."></label>
              <label><span class="text-xs font-black uppercase tracking-[.12em] text-slate-500">Mês</span><input x-model="receiptFilter.month" type="month" class="input mt-1"></label>
              <label><span class="text-xs font-black uppercase tracking-[.12em] text-slate-500">Categoria</span><select x-model="receiptFilter.category" class="input mt-1"><option value="todas">Todas</option><template x-for="c in categories"><option x-text="c"></option></template></select></label>
              <div class="flex items-end"><

===== paidValue @ 2195137 =====
><option value="">Ações</option><option value="pagar" :disabled="isViewer || s.status==='Pago'">Pagar</option><option value="recibo" :disabled="isViewer || Number(s.paidValue||0)<=0" x-text="reportReceiptNumber(s)==='-'?'Emitir recibo':'Ver recibo'"></option><option value="servico">Abrir serviço</option><option value="excluirPagamento" :disabled="isViewer || Number(s.paidValue||0)<=0">Excluir pagamento</option></select></td>
                      </tr>
                    </template>
                    <tr x-show="!filteredPayments.length"><td colspan="5" class="p-6 text-center text-slate-500">Nenhum pagamento encontrado para os filtros selecionados.</td></tr>
                  </tbody>
                </table>
              </div>
            </div>
            <div class="glass rounded-3xl p-4 space-y-3">
              <h3 class="font-black text-lg">Últimos pagamentos</h3>
              <template x-for="s in paymentRecent" :key="'recent-pay-'+s.id">
                <div class="rounded-2xl border border-slate-200/70 dark:border-slate-700/70 bg-white/55 dark:bg-slate-950/30 p-3">
                  <div class="flex items-start justify-between gap-3"><div><p class="font-black text-sm" x-text="providerName(s.providerId)"></p><p class="text-xs text-slate-500" x-text="fmt(s.paymentDate)+' • '+(s.paymentMethod||'forma não informada')"></p></div><p class="font-black text-emerald-700" x-text="money(s.paidValue||0)"></p></div>
                  <div class="flex flex-wrap gap-2 mt-2"><button @click="quickReceipt(s)" class="mini" :disabled="isViewer" x-text="reportReceiptNumber(s)==='-'?'Emitir recibo':'Ver recibo'"></button><button @click="openSearchService(s)" class="mini">Serviço</button><button @click="openDeletePaymentModal(s)" class="danger" :disabled="isViewer || Number(s.paidValue||0)<=0">Excluir pagamento</button></div>
                </div>
              </template>
              <p x-show="!paymentRecent.length" class="text-sm text-slate-500 text-center py-3">Nenhum pagamento registrado ainda.</p>
            </div>
          </div>
        </section>

        <section x-show="view==='recibos'" class="space-y-4">
          <div class="glass rounded-3xl p-4 space-y-4">
            <div class="flex flex-col md:flex-row md:items-center md:justify-between gap-3">
              <div>
                <p class="text-xs uppercase tracking-[.18em] text-emerald-600 dark:text-emerald-400 font-black">Emissão</p>
                <h3 class="font-black text-lg">Emitir recibo profissional</h3>
                <p class="text-xs text-slate-500 dark:text-slate-400">Selecione um serviço/pagamento já lançado para gerar o recibo formal.</p>
              </div>
              <div class="rounded-2xl px-4 py-3 bg-emerald-500/10 border border-emerald-500/20 text-sm">
                <b x-text="receipts.length"></b> recibos no histórico
              </div>
            </div>
            <div class="grid lg:grid-cols-[1fr_auto] gap-3 items-end">
              <label>
                <span class="text-sm font-bold">Lançamento para emissão</span>
                <select x-model="receiptServiceId" class="input mt-1">
                  <option value="">Selecione um serviço/pagamento</option>
                  <template x-for="s in services">
                    <option :value="s.id" x-text="'#'+s.id+' • '+providerName(s.providerId)+' • '+s.category+' • '+money(s.amount)"></option>
                  </template>
                </select>
              </label>
              <div class="grid sm:grid-cols-2 gap-2 lg:min-w-[360px]">
                <button @click="issueReceipt" class="primary w-full" :disabled="isViewer">Emitir recibo</button>
                <button @click="printReceipt" class="secondary w-full">Imprimir / Salvar PDF</button>
              </div>
            </div>
            <div class="rounded-2xl border border-slate-200/60 dark:border-slate-700/60 bg-white/50 dark:bg-slate-950/30 p-3 text-xs text-slate-500 dark:text-slate-400">
              Use <b>Imprimir / Salvar PDF</b> e escolha “Salvar como PDF”. O recibo agora sai em layout A4 premium/oficial compacto, ajustado para caber em uma única página, com quadro de conferência, valor por extenso, assinatura e rodapé para prestação de contas.
            </div>
          </div>

          <div class="glass rounded-3xl p-4">
            <div class="flex flex-col md:flex-row md:items-center md:justify-between gap-2 mb-3">
              <div>
                <p class="text-xs uppercase tracking-[.18em] text-rc-600 dark:text-rc-300 font-black">Histórico</p>
                <h3 class="font-black text-lg">Recibos emitidos</h3>
              </div>
              <span class="text-xs text-slate-500 dark:text-slate-400">Reimpressão rápida e controle dos comprovantes já emitidos.</span>
            </div>
            <div class="grid md:grid-cols-4 gap-3 mb-4">
              <label><span class="text-xs font-black uppercase tracking-[.12em] text-slate-500">Buscar</span><input x-model="receiptFilter.q" class="input mt-1" placeholder="Nº, prestador, categoria..."></label>
              <label><span class="text-xs font-black uppercase tracking-[.12em] text-slate-500">Mês</span><input x-model="receiptFilter.month" type="month" class="input mt-1"></label>
              <label><span class="text-xs font-black uppercase tracking-[.12em] text-slate-500">Categoria</span><select x-model="receiptFilter.category" class="input mt-1"><option value="todas">Todas</option><template x-for="c in categories"><option x-text="c"></option></template></select></label>
              <div class="flex items-end"><button @click="clearReceiptFilters" class="secondary w-full">Limpar filtros</button></div>
            </div>
            <div class="rounded-2xl border border-slate-200/60 dark:border-slate-700/60 bg-white/45 dark:bg-slate-950/30 px-4 py-3 mb-4 text-sm flex flex-wrap gap-2 justify-between">
              <span><b x-text="filteredReceipts.length"></b> recibo(s) filtrado(s)</span>


===== paidValue @ 2207335 =====
            <template x-for="item in searchResults" :key="item.kind+'-'+item.id">
              <div class="glass rounded-3xl p-4 border-t-4" :class="item.kind==='receipt'?'border-t-amber-500':item.kind==='provider'?'border-t-emerald-600':item.kind==='recurrence'?'border-t-blue-500':'border-t-rc-gold'">
                <div class="flex items-start justify-between gap-3">
                  <div>
                    <span class="badge" x-text="item.type"></span>
                    <h4 class="font-black text-lg mt-2" x-text="item.title"></h4>
                    <p class="text-sm text-slate-500 dark:text-slate-400" x-text="item.subtitle"></p>
                  </div>
                  <div class="text-right shrink-0">
                    <p class="font-black" x-text="item.amount"></p>
                    <p class="text-xs text-slate-500" x-text="item.date"></p>
                  </div>
                </div>
                <p class="text-sm mt-3 leading-relaxed" x-text="item.description"></p>
                <div class="flex flex-wrap gap-2 mt-4">
                  <button x-show="item.kind==='provider'" @click="openSearchProvider(item.data)" class="mini">Painel</button>
                  <button x-show="item.kind==='provider'" @click="fillServiceFromProvider(item.data)" class="mini" :disabled="isViewer">Lançar</button>
                  <button x-show="item.kind==='provider'" @click="openWhatsapp(item.data)" class="mini">WhatsApp</button>

                  <button x-show="item.kind==='service'" @click="openSearchService(item.data)" class="mini">Ver serviço</button>
                  <button x-show="item.kind==='service'" @click="pay(item.data)" class="mini" :disabled="isViewer">Pagar</button>
                  <button x-show="item.kind==='service' && Number(item.data?.paidValue||0)>0" @click="quickReceipt(item.data)" class="mini" :disabled="isViewer" x-text="reportReceiptNumber(item.data)==='-'?'Emitir recibo':'Ver recibo'"></button>

                  <button x-show="item.kind==='receipt'" @click="openSearchReceipt(item.data)" class="mini">Abrir recibo</button>
                  <button x-show="item.kind==='receipt'" @click="currentReceipt=item.data; printReceipt()" class="mini">Imprimir</button>

                  <button x-show="item.kind==='recurrence'" @click="openSearchRecurrence(item.data)" class="mini">Ver recorrência</button>
                  <button x-show="item.kind==='recurrence'" @click="generateRecurrence(item.data)" class="mini" :disabled="isViewer">Gerar mês</button>
                </div>
              </div>
            </template>
          </div>
          <div x-show="q && !searchResults.length" class="glass rounded-3xl p-5 text-center text-slate-500">
            Nenhum resultado encontrado para a busca atual.
          </div>
          <div x-show="!q" class="glass rounded-3xl p-5 text-center text-slate-500">
            Digite no campo de busca para localizar informações do app.
          </div>
        </section>

        <section x-show="view==='config'" class="space-y-4">
          <form @submit.prevent="saveSettings" class="glass rounded-3xl p-4 space-y-4">
            <div class="flex items-start gap-3">
              <div class="h-11 w-11 rounded-2xl bg-gradient-to-br from-rc-800 to-rc-500 text-white grid place-items-center shadow-soft"><i data-lucide="building-2" class="w-5 h-5"></i></div>
              <div>
                <h3 class="font-black text-lg">Dados do condomínio</h3>
                <p class="text-xs text-slate-500 dark:text-slate-400">Identidade usada nos recibos, relatórios e PDFs de prestação de contas.</p>
              </div>
            </div>
            <div class="grid lg:grid-cols-2 gap-3">
              <label><span class="text-sm font-bold">Nome</span><input x-model="settings.condoName" class="input mt-1"></label>
              <label><span class="text-sm font-bold">Responsável</span><input x-model="settings.manager" class="input mt-1"></label>
            </div>
            <div class="grid lg:grid-cols-2 gap-3">
              <label><span class="text-sm font-bold">CNPJ</span><input x-model="settings.cnpj" class="input mt-1"></label>
              <label><span class="text-sm font-bold">Logo</span><input @change="readLogo" type="file" accept="image/*" class="input mt-1"></label>
            </div>
            <label><span class="text-sm font-bold">Endereço</span><input x-model="settings.address" class="input mt-1"></label>
            <button class="primary">Salvar configurações</button>
          </form>

          <div class="glass rounded-3xl p-4 space-y-4">
            <div class="flex items-start justify-between gap-3">
              <div class="flex items-start gap-3">
                <div class="h-11 w-11 rounded-2xl bg-gradient-to-br from-rc-800 to-rc-gold text-white grid place-items-center shadow-soft"><i data-lucide="list-ordered" class="w-5 h-5"></i></div>
                <div>
                  <h3 class="font-black text-lg">Numeração dos recibos</h3>
                  <p class="text-xs text-slate-500 dark:text-slate-400">Conferência da sequência oficial. O número só é consumido quando um recibo é emitido.</p>
                </div>
              </div>
              <span class="badge" x-text="receiptSequenceStatus()"></span>
            </div>
            <div class="grid md:grid-cols-4 gap-3">
              <div class="rounded-3xl border border-slate-200/70 dark:border-slate-700/70 bg-white/70 dark:bg-slate-950/35 p-4">
                <p class="text-xs uppercase tracking-[.14em] text-slate-500 font-black">Próximo recibo</p>
                <p class="text-2xl font-black receipt-number mt-1" x-text="String(nextReceiptNumber()).padStart(6,'0')"></p>
              </div>
              <div class="rounded-3xl border border-slate-200/70 dark:border-slate-700/70 bg-white/70 dark:bg-slate-950/35 p-4">
                <p class="text-xs uppercase tracking-[.14em] text-slate-500 font-black">Último emitido</p>
                

===== paidValue @ 2218026 =====
s="text-[11px] text-slate-400 mt-1">Perfil: <span x-text="l.role || auth.role"></span></p>
                  </div>
                </div>
              </template>
              <p x-show="!logs.length" class="text-sm text-slate-500">Nenhuma atividade registrada ainda.</p>
            </div>
            <button @click="clearLogs" class="secondary w-full" :disabled="isViewer || !logs.length">Limpar log</button>
          </div>
        </section>
      </main>

      <div x-show="payModal" class="no-print fixed inset-0 z-50 grid place-items-center bg-slate-950/55 p-4">
        <div class="glass rounded-3xl p-5 w-full max-w-2xl space-y-4">
          <div class="flex items-start justify-between gap-3">
            <div>
              <p class="text-xs uppercase tracking-[.18em] text-rc-gold font-black">Janela flutuante</p>
              <h3 class="font-black text-xl">Registrar pagamento</h3>
              <p class="text-sm text-slate-500" x-text="paymentTarget ? providerName(paymentTarget.providerId)+' • '+paymentTarget.category : ''"></p>
            </div>
            <button @click="payModal=false" class="secondary px-3 py-2">Fechar</button>
          </div>
          <div class="grid sm:grid-cols-3 gap-3" x-show="paymentTarget">
            <div class="rounded-2xl border border-slate-200/70 dark:border-slate-700/70 bg-white/60 dark:bg-slate-950/30 p-3"><p class="text-xs uppercase tracking-[.14em] text-slate-500 font-black">Valor do serviço</p><p class="text-lg font-black" x-text="money(paymentTarget?.amount||0)"></p></div>
            <div class="rounded-2xl border border-emerald-500/20 bg-emerald-500/10 p-3"><p class="text-xs uppercase tracking-[.14em] text-emerald-700 font-black">Já pago</p><p class="text-lg font-black text-emerald-700" x-text="money(paymentTarget?.paidValue||0)"></p></div>
            <div class="rounded-2xl border border-red-500/20 bg-red-500/10 p-3"><p class="text-xs uppercase tracking-[.14em] text-red-700 font-black">Em aberto</p><p class="text-lg font-black text-red-700" x-text="money(paymentTarget?balance(paymentTarget):0)"></p></div>
          </div>
          <div class="rounded-2xl border border-slate-200/70 dark:border-slate-700/70 bg-white/50 dark:bg-slate-950/30 p-3" x-show="paymentTarget">
            <p class="text-sm"><b>Descrição:</b> <span x-text="paymentTarget?.description||'-'"></span></p>
            <p class="text-xs text-slate-500 mt-1" x-text="'Referência: '+month(paymentTarget?.reference)+' • Vencimento: '+fmt(paymentTarget?.dueDate)+' • Local: '+(paymentTarget?.location||'-')"></p>
          </div>
          <div class="grid sm:grid-cols-3 gap-3">
            <label><span class="text-sm font-bold">Data do pagamento</span><input x-model="payment.date" type="date" class="input mt-1"></label>
            <label><span class="text-sm font-bold">Valor pago agora</span><input x-model.number="payment.value" type="number" step="0.01" class="input mt-1"></label>
            <label><span class="text-sm font-bold">Forma</span><select x-model="payment.method" class="input mt-1"><option>PIX</option><option>Dinheiro</option><option>Transferência</option><option>Boleto</option><option>Cheque</option><option>Cartão</option></select></label>
          </div>
          <div class="grid sm:grid-cols-3 gap-2">
            <button @click="payment.value=paymentTarget?balance(paymentTarget):0" class="secondary" type="button">Valor em aberto</button>
            <button @click="payment.value=paymentTarget?Number(paymentTarget.amount||0):0" class="secondary" type="button">Valor integral</button>
            <button @click="savePayment" class="primary" :disabled="isViewer">Registrar pagamento</button>
          </div>
        </div>
      </div>

      <div x-show="actionModal.open" class="no-print fixed inset-0 z-50 grid place-items-center bg-slate-950/55 p-4">
        <div class="glass rounded-3xl p-5 w-full max-w-md space-y-4">
          <div class="flex items-start gap-3">
            <div class="h-11 w-11 rounded-2xl grid place-items-center text-white shadow-soft" :class="actionModal.tone==='danger' ? 'bg-red-600' : 'bg-gradient-to-br from-rc-800 to-rc-500'">
              <i :data-lucide="actionModal.icon || (actionModal.tone==='danger' ? 'trash-2' : 'copy-plus')" class="w-5 h-5"></i>
            </div>
            <div>
              <h3 class="font-black text-xl" x-text="actionModal.title"></h3>
              <p class="text-sm text-slate-500 dark:text-slate-400" x-text="actionModal.subtitle"></p>
            </div>
          </div>

          <div class="rounded-2xl border border-slate-200/70 dark:border-slate-700/70 bg-white/55 dark:bg-slate-950/25 p-3 text-sm space-y-1">
            <template x-for="d in actionModal.details" :key="d.label">
              <p><b x-text="d.label + ':'"></b> <span x-text="d.value"></span></p>
            </template>
          </div>

          <p class="text-xs text-slate-500 dark:text-slate-400" x-text="actionModal.note"></p>

          <div class="flex gap-2">
            <button @click="confirmActionModal" class="flex-1" :class="actionModal.tone==='danger' ? 'danger' : 'primary'" x-text="actionModal.confirmLabel"></button>
            <button @click="closeActionModal" class="secondary">Cancelar</button>
          </div>
        </div>
      </div>

      <div class="no-print fixed top-4 right-4 z-[80] w-[min(92vw,390px)] space-y-3 pointer-events-none">
        <template x-for="t in toasts" :key="t.id">
          <div x-transition.opacity.scale.origin.top.right class="pointer-events-auto rounded-3xl border bg-white/95 dark:bg-slate-900/95 backdrop-blur-xl shadow-premium overflow-hidden" :class="t.border">
            <div class="h-1" :class="t.bar"></div>
            <div class="p-4 flex gap-3 items-start">
              <div class="h-10 w-10 rounded-2xl grid place-items-center text-white shrink-0" :class="t.iconBg">
                <i :data-lucide="t.icon" class="w-5 h-5"></i>
              </div>
              <div class="min

===== paidValue @ 6446390 =====
rs:'++id,name,category,document',services:'++id,providerId,category,reference,serviceDate,dueDate,status,recurrenceId',receipts:'++id,number,date,providerName,category',settings:'key',meta:'key',recurrences:'++id,providerId,category,active',logs:'++id,at,type,title,detail,role'});
function app(){return{
  auth:{logged:false,role:'viewer'},login:{role:'admin',password:''},dark:localStorage.getItem('rc_dark')==='1',view:'dashboard',q:'',charts:{},selectedProviderId:null,activeTooltip:null,
  menu:[{id:'dashboard',label:'Início',icon:'layout-dashboard'},{id:'prestadores',label:'Prestadores',icon:'users'},{id:'servicos',label:'Serviços',icon:'hard-hat'},{id:'pagamentos',label:'Pagamentos',icon:'wallet-cards'},{id:'recibos',label:'Recibos',icon:'receipt-text'},{id:'relatorios',label:'Relatórios',icon:'file-bar-chart'},{id:'config',label:'Config.',icon:'settings'}],
  categories:['Jardineiro','Vidraceiro','Serralheiro','Eletricista','Pedreiro','Paisagista','Pintor','Encanador','Limpeza','Dedetização','Manutenção de bomba','Portão/Interfone','Gratificação natalina','Outros'],
  providers:[],services:[],receipts:[],recurrences:[],logs:[],receiptSeqValue:0,receiptSeqInput:"",settings:{condoName:'Condomínio Exemplo',cnpj:'CNPJ 00.000.000/0001-00',address:'Endereço do condomínio',manager:'Síndico/Tesoureiro',logo:''},
  providerForm:{id:null,name:'',type:'Autônomo',document:'',category:'Jardineiro',phone:'',whatsapp:'',pix:'',notes:''},providerSearch:'',providerCategoryFilter:'',providerSortField:'name',providerSortDir:'asc',
  serviceForm:{providerId:'',category:'Jardineiro',reference:new Date().toISOString().slice(0,7),serviceDate:new Date().toISOString().slice(0,10),dueDate:new Date().toISOString().slice(0,10),amount:0,location:'',description:'',paymentDate:'',paymentMethod:'',paidValue:0},
  recurrenceForm:{id:null,providerId:'',category:'Jardineiro',amount:0,dueDay:5,paymentDay:5,paymentMethod:'PIX',location:'',description:'',active:true},
  serviceFilter:'todos',serviceCategoryFilter:'todas',serviceProviderFilter:'todos',serviceMonthFilter:'',paymentMonthFilter:new Date().toISOString().slice(0,7),paymentStatusFilter:'todos',paymentProviderFilter:'todos',receiptFilter:{q:'',month:'',category:'todas'},receiptServiceId:'',currentReceipt:null,dashboardYear:new Date().getFullYear(),payModal:false,paymentTarget:null,payment:{date:new Date().toISOString().slice(0,10),value:0,method:'PIX'},actionModal:{open:false,type:'',tone:'default',icon:'',target:null,service:null,title:'',subtitle:'',note:'',confirmLabel:'',details:[]},toasts:[],
  report:{from:new Date(new Date().getFullYear(),new Date().getMonth(),1).toISOString().slice(0,10),to:new Date().toISOString().slice(0,10),category:'todas',status:'todos'},passwords:{admin:'',viewer:''},
  async init(){document.documentElement.classList.toggle('dark',this.dark);window.alert=(msg)=>this.toast(String(msg||''),this.toastType(String(msg||'')));if('serviceWorker'in navigator){navigator.serviceWorker.register('./sw.js').catch(console.warn)}await this.seed();this.refreshIcons()},
  async seed(){let s=await db.settings.get('main');if(s)this.settings=s.value;await this.load();},
  async load(){this.providers=await db.providers.toArray();this.services=await db.services.toArray();this.receipts=await db.receipts.toArray();this.recurrences=await db.recurrences.toArray();this.logs=(await db.logs.orderBy('at').reverse().limit(30).toArray());let meta=await db.meta.get('receiptSeq');this.receiptSeqValue=Number(meta?.value||0);if(!this.receiptSeqInput)this.receiptSeqInput=this.nextReceiptNumber();this.after()},
  get isViewer(){return this.auth.role==='viewer'},get title(){return {'dashboard':'Dashboard inicial','prestadores':'Cadastro de prestadores','servicos':'Serviços','pagamentos':'Central de pagamentos','recibos':'Emissão de recibos','relatorios':'Relatórios','busca':'Busca global','config':'Configurações'}[this.view]||'ReciboCondo'},get selectedProvider(){return this.providers.find(p=>Number(p.id)===Number(this.selectedProviderId))||null},
  get cards(){return this.dashboardCards},
  get yearlyRows(){let y=String(this.dashboardYear||new Date().getFullYear());return this.services.filter(s=>String(s.paymentDate||s.serviceDate||'').startsWith(y))},
  get dashboardCards(){let nowRef=new Date().toISOString().slice(0,7), year=this.yearlyRows, month=year.filter(s=>String(s.paymentDate||s.serviceDate||'').slice(0,7)===nowRef), paidYear=year.reduce((a,s)=>a+Number(s.paidValue||0),0), paidMonth=month.reduce((a,s)=>a+Number(s.paidValue||0),0), countMonth=month.filter(s=>Number(s.paidValue||0)>0).length, open=year.reduce((a,s)=>a+this.balance(s),0), top=this.annualCategorySummary[0], topYear=this.topPaidNames(year), topMonth=this.topPaidNames(month), openTop=this.topOpenNames(year), catTop=this.annualCategorySummary.slice(0,3).map(c=>c.category+' • '+this.money(c.total)), activeCats=[...new Set(this.providers.map(p=>p.category).filter(Boolean))].slice(0,4), receiptsMonth=this.receipts.filter(r=>String(r.paymentDate||r.date||'').slice(0,7)===nowRef).length;return[
    {label:'Pago no ano',value:this.money(paidYear),hint:String(this.dashboardYear),icon:'wallet-cards',color:'text-rc-600',tooltipTitle:'Resumo anual',tooltipMain:this.money(paidYear),tooltipTag:'Ano '+this.dashboardYear,tooltip:['Total efetivamente pago no ano selecionado.', 'Top recebimentos: '+(topYear.length?topYear.join(' | '):'sem pagamentos lançados.'), 'Use para comparar o volume anual com o relatório anual premium.']},
    {label:'Pago no mês',value:this.money(paidMonth),hint:this.month(nowRef),icon:'circle-dollar-sign',color:'text-emerald-600',tooltipTitle:'Resumo do mês',tooltipMain:this.money(paidMonth),tooltipTag:'Mês atual',tooltip:['Mês de referência: '+this.month(nowRef)+'.', 'Quem recebeu: '+(topMonth.length?topMonth.join(' | '):'sem pagamentos neste mês.'), 'Ajuda a conferir rapidamente o fechamento mensal.']},
    {label:'Pagamentos no mês',value:countMonth,hint:'registros pagos',icon:'receipt-te

===== paidValue @ 6449055 =====
0),to:new Date().toISOString().slice(0,10),category:'todas',status:'todos'},passwords:{admin:'',viewer:''},
  async init(){document.documentElement.classList.toggle('dark',this.dark);window.alert=(msg)=>this.toast(String(msg||''),this.toastType(String(msg||'')));if('serviceWorker'in navigator){navigator.serviceWorker.register('./sw.js').catch(console.warn)}await this.seed();this.refreshIcons()},
  async seed(){let s=await db.settings.get('main');if(s)this.settings=s.value;await this.load();},
  async load(){this.providers=await db.providers.toArray();this.services=await db.services.toArray();this.receipts=await db.receipts.toArray();this.recurrences=await db.recurrences.toArray();this.logs=(await db.logs.orderBy('at').reverse().limit(30).toArray());let meta=await db.meta.get('receiptSeq');this.receiptSeqValue=Number(meta?.value||0);if(!this.receiptSeqInput)this.receiptSeqInput=this.nextReceiptNumber();this.after()},
  get isViewer(){return this.auth.role==='viewer'},get title(){return {'dashboard':'Dashboard inicial','prestadores':'Cadastro de prestadores','servicos':'Serviços','pagamentos':'Central de pagamentos','recibos':'Emissão de recibos','relatorios':'Relatórios','busca':'Busca global','config':'Configurações'}[this.view]||'ReciboCondo'},get selectedProvider(){return this.providers.find(p=>Number(p.id)===Number(this.selectedProviderId))||null},
  get cards(){return this.dashboardCards},
  get yearlyRows(){let y=String(this.dashboardYear||new Date().getFullYear());return this.services.filter(s=>String(s.paymentDate||s.serviceDate||'').startsWith(y))},
  get dashboardCards(){let nowRef=new Date().toISOString().slice(0,7), year=this.yearlyRows, month=year.filter(s=>String(s.paymentDate||s.serviceDate||'').slice(0,7)===nowRef), paidYear=year.reduce((a,s)=>a+Number(s.paidValue||0),0), paidMonth=month.reduce((a,s)=>a+Number(s.paidValue||0),0), countMonth=month.filter(s=>Number(s.paidValue||0)>0).length, open=year.reduce((a,s)=>a+this.balance(s),0), top=this.annualCategorySummary[0], topYear=this.topPaidNames(year), topMonth=this.topPaidNames(month), openTop=this.topOpenNames(year), catTop=this.annualCategorySummary.slice(0,3).map(c=>c.category+' • '+this.money(c.total)), activeCats=[...new Set(this.providers.map(p=>p.category).filter(Boolean))].slice(0,4), receiptsMonth=this.receipts.filter(r=>String(r.paymentDate||r.date||'').slice(0,7)===nowRef).length;return[
    {label:'Pago no ano',value:this.money(paidYear),hint:String(this.dashboardYear),icon:'wallet-cards',color:'text-rc-600',tooltipTitle:'Resumo anual',tooltipMain:this.money(paidYear),tooltipTag:'Ano '+this.dashboardYear,tooltip:['Total efetivamente pago no ano selecionado.', 'Top recebimentos: '+(topYear.length?topYear.join(' | '):'sem pagamentos lançados.'), 'Use para comparar o volume anual com o relatório anual premium.']},
    {label:'Pago no mês',value:this.money(paidMonth),hint:this.month(nowRef),icon:'circle-dollar-sign',color:'text-emerald-600',tooltipTitle:'Resumo do mês',tooltipMain:this.money(paidMonth),tooltipTag:'Mês atual',tooltip:['Mês de referência: '+this.month(nowRef)+'.', 'Quem recebeu: '+(topMonth.length?topMonth.join(' | '):'sem pagamentos neste mês.'), 'Ajuda a conferir rapidamente o fechamento mensal.']},
    {label:'Pagamentos no mês',value:countMonth,hint:'registros pagos',icon:'receipt-text',color:'text-blue-600',tooltipTitle:'Movimento mensal',tooltipMain:countMonth+' pagamento(s)',tooltipTag:'Recibos '+receiptsMonth,tooltip:['Valor médio por pagamento: '+this.money(countMonth?paidMonth/countMonth:0)+'.', 'Recibos emitidos no mês: '+receiptsMonth+'.', 'Bom para conferir se todos os pagamentos pagos já têm recibo.']},
    {label:'Prestadores ativos',value:this.providers.length,hint:'cadastrados',icon:'users',color:'text-rc-600',tooltipTitle:'Base de prestadores',tooltipMain:this.providers.length+' cadastro(s)',tooltipTag:'Cadastros',tooltip:['Categorias em uso: '+(activeCats.length?activeCats.join(', '):'nenhuma categoria cadastrada.'), 'Use para localizar quem presta serviço recorrente ou avulso.', 'Dica: mantenha PIX, WhatsApp e categoria sempre atualizados.']},
    {label:'Em aberto no ano',value:this.money(open),hint:'pendente/parcial',icon:'triangle-alert',color:'text-red-600',tooltipTitle:'Pendências do ano',tooltipMain:this.money(open),tooltipTag:'Atenção',tooltip:['Saldo lançado e ainda não quitado no ano.', 'Maiores pendências: '+(openTop.length?openTop.join(' | '):'nenhuma pendência no ano.'), 'Depois de pagar, registre o pagamento antes de emitir o recibo.']},
    {label:'Maior categoria',value:top?top.category:'-',hint:top?this.money(top.total):'sem dados',icon:'trophy',color:'text-yellow-600',tooltipTitle:'Categoria líder',tooltipMain:top?top.category:'Sem dados',tooltipTag:top?this.money(top.total):'Sem dados',tooltip:['Categoria com maior volume pago no ano.', 'Ranking: '+(catTop.length?catTop.join(' | '):'sem categorias pagas no ano.'), 'Ajuda a identificar onde o condomínio mais gastou.']}
  ]},
  get annualTotals(){let paid=this.yearlyRows.reduce((a,s)=>a+Number(s.paidValue||0),0),open=this.yearlyRows.reduce((a,s)=>a+this.balance(s),0),count=this.yearlyRows.filter(s=>Number(s.paidValue||0)>0).length;return{paid,open,count}},
  get monthlyHistory(){let labels=['Janeiro','Fevereiro','Março','Abril','Maio','Junho','Julho','Agosto','Setembro','Outubro','Novembro','Dezembro'];return labels.map((label,i)=>{let key=String(this.dashboardYear)+'-'+String(i+1).padStart(2,'0'), rows=this.yearlyRows.filter(s=>String(s.paymentDate||s.serviceDate||'').slice(0,7)===key), paid=rows.reduce((a,s)=>a+Number(s.paidValue||0),0), open=rows.reduce((a,s)=>a+this.balance(s),0), map={};rows.forEach(s=>{map[s.category]=(map[s.category]||0)+Number(s.paidValue||s.amount||0)});let top=Object.entries(map).sort((a,b)=>b[1]-a[1])[0];return{key,label,count:rows.filter(s=>Number(s.paidValue||0)>0).length,paid,open,topCategory:top?top[0]:'-'}})},
  get annualTopProviders(){let map={};this.yearlyRows.forEach(s=>{let id=

===== paidValue @ 6449114 =====
,status:'todos'},passwords:{admin:'',viewer:''},
  async init(){document.documentElement.classList.toggle('dark',this.dark);window.alert=(msg)=>this.toast(String(msg||''),this.toastType(String(msg||'')));if('serviceWorker'in navigator){navigator.serviceWorker.register('./sw.js').catch(console.warn)}await this.seed();this.refreshIcons()},
  async seed(){let s=await db.settings.get('main');if(s)this.settings=s.value;await this.load();},
  async load(){this.providers=await db.providers.toArray();this.services=await db.services.toArray();this.receipts=await db.receipts.toArray();this.recurrences=await db.recurrences.toArray();this.logs=(await db.logs.orderBy('at').reverse().limit(30).toArray());let meta=await db.meta.get('receiptSeq');this.receiptSeqValue=Number(meta?.value||0);if(!this.receiptSeqInput)this.receiptSeqInput=this.nextReceiptNumber();this.after()},
  get isViewer(){return this.auth.role==='viewer'},get title(){return {'dashboard':'Dashboard inicial','prestadores':'Cadastro de prestadores','servicos':'Serviços','pagamentos':'Central de pagamentos','recibos':'Emissão de recibos','relatorios':'Relatórios','busca':'Busca global','config':'Configurações'}[this.view]||'ReciboCondo'},get selectedProvider(){return this.providers.find(p=>Number(p.id)===Number(this.selectedProviderId))||null},
  get cards(){return this.dashboardCards},
  get yearlyRows(){let y=String(this.dashboardYear||new Date().getFullYear());return this.services.filter(s=>String(s.paymentDate||s.serviceDate||'').startsWith(y))},
  get dashboardCards(){let nowRef=new Date().toISOString().slice(0,7), year=this.yearlyRows, month=year.filter(s=>String(s.paymentDate||s.serviceDate||'').slice(0,7)===nowRef), paidYear=year.reduce((a,s)=>a+Number(s.paidValue||0),0), paidMonth=month.reduce((a,s)=>a+Number(s.paidValue||0),0), countMonth=month.filter(s=>Number(s.paidValue||0)>0).length, open=year.reduce((a,s)=>a+this.balance(s),0), top=this.annualCategorySummary[0], topYear=this.topPaidNames(year), topMonth=this.topPaidNames(month), openTop=this.topOpenNames(year), catTop=this.annualCategorySummary.slice(0,3).map(c=>c.category+' • '+this.money(c.total)), activeCats=[...new Set(this.providers.map(p=>p.category).filter(Boolean))].slice(0,4), receiptsMonth=this.receipts.filter(r=>String(r.paymentDate||r.date||'').slice(0,7)===nowRef).length;return[
    {label:'Pago no ano',value:this.money(paidYear),hint:String(this.dashboardYear),icon:'wallet-cards',color:'text-rc-600',tooltipTitle:'Resumo anual',tooltipMain:this.money(paidYear),tooltipTag:'Ano '+this.dashboardYear,tooltip:['Total efetivamente pago no ano selecionado.', 'Top recebimentos: '+(topYear.length?topYear.join(' | '):'sem pagamentos lançados.'), 'Use para comparar o volume anual com o relatório anual premium.']},
    {label:'Pago no mês',value:this.money(paidMonth),hint:this.month(nowRef),icon:'circle-dollar-sign',color:'text-emerald-600',tooltipTitle:'Resumo do mês',tooltipMain:this.money(paidMonth),tooltipTag:'Mês atual',tooltip:['Mês de referência: '+this.month(nowRef)+'.', 'Quem recebeu: '+(topMonth.length?topMonth.join(' | '):'sem pagamentos neste mês.'), 'Ajuda a conferir rapidamente o fechamento mensal.']},
    {label:'Pagamentos no mês',value:countMonth,hint:'registros pagos',icon:'receipt-text',color:'text-blue-600',tooltipTitle:'Movimento mensal',tooltipMain:countMonth+' pagamento(s)',tooltipTag:'Recibos '+receiptsMonth,tooltip:['Valor médio por pagamento: '+this.money(countMonth?paidMonth/countMonth:0)+'.', 'Recibos emitidos no mês: '+receiptsMonth+'.', 'Bom para conferir se todos os pagamentos pagos já têm recibo.']},
    {label:'Prestadores ativos',value:this.providers.length,hint:'cadastrados',icon:'users',color:'text-rc-600',tooltipTitle:'Base de prestadores',tooltipMain:this.providers.length+' cadastro(s)',tooltipTag:'Cadastros',tooltip:['Categorias em uso: '+(activeCats.length?activeCats.join(', '):'nenhuma categoria cadastrada.'), 'Use para localizar quem presta serviço recorrente ou avulso.', 'Dica: mantenha PIX, WhatsApp e categoria sempre atualizados.']},
    {label:'Em aberto no ano',value:this.money(open),hint:'pendente/parcial',icon:'triangle-alert',color:'text-red-600',tooltipTitle:'Pendências do ano',tooltipMain:this.money(open),tooltipTag:'Atenção',tooltip:['Saldo lançado e ainda não quitado no ano.', 'Maiores pendências: '+(openTop.length?openTop.join(' | '):'nenhuma pendência no ano.'), 'Depois de pagar, registre o pagamento antes de emitir o recibo.']},
    {label:'Maior categoria',value:top?top.category:'-',hint:top?this.money(top.total):'sem dados',icon:'trophy',color:'text-yellow-600',tooltipTitle:'Categoria líder',tooltipMain:top?top.category:'Sem dados',tooltipTag:top?this.money(top.total):'Sem dados',tooltip:['Categoria com maior volume pago no ano.', 'Ranking: '+(catTop.length?catTop.join(' | '):'sem categorias pagas no ano.'), 'Ajuda a identificar onde o condomínio mais gastou.']}
  ]},
  get annualTotals(){let paid=this.yearlyRows.reduce((a,s)=>a+Number(s.paidValue||0),0),open=this.yearlyRows.reduce((a,s)=>a+this.balance(s),0),count=this.yearlyRows.filter(s=>Number(s.paidValue||0)>0).length;return{paid,open,count}},
  get monthlyHistory(){let labels=['Janeiro','Fevereiro','Março','Abril','Maio','Junho','Julho','Agosto','Setembro','Outubro','Novembro','Dezembro'];return labels.map((label,i)=>{let key=String(this.dashboardYear)+'-'+String(i+1).padStart(2,'0'), rows=this.yearlyRows.filter(s=>String(s.paymentDate||s.serviceDate||'').slice(0,7)===key), paid=rows.reduce((a,s)=>a+Number(s.paidValue||0),0), open=rows.reduce((a,s)=>a+this.balance(s),0), map={};rows.forEach(s=>{map[s.category]=(map[s.category]||0)+Number(s.paidValue||s.amount||0)});let top=Object.entries(map).sort((a,b)=>b[1]-a[1])[0];return{key,label,count:rows.filter(s=>Number(s.paidValue||0)>0).length,paid,open,topCategory:top?top[0]:'-'}})},
  get annualTopProviders(){let map={};this.yearlyRows.forEach(s=>{let id=s.providerId,p=this.providers.find(x=>Number(x.id)===Number

===== paidValue @ 6449168 =====
nc init(){document.documentElement.classList.toggle('dark',this.dark);window.alert=(msg)=>this.toast(String(msg||''),this.toastType(String(msg||'')));if('serviceWorker'in navigator){navigator.serviceWorker.register('./sw.js').catch(console.warn)}await this.seed();this.refreshIcons()},
  async seed(){let s=await db.settings.get('main');if(s)this.settings=s.value;await this.load();},
  async load(){this.providers=await db.providers.toArray();this.services=await db.services.toArray();this.receipts=await db.receipts.toArray();this.recurrences=await db.recurrences.toArray();this.logs=(await db.logs.orderBy('at').reverse().limit(30).toArray());let meta=await db.meta.get('receiptSeq');this.receiptSeqValue=Number(meta?.value||0);if(!this.receiptSeqInput)this.receiptSeqInput=this.nextReceiptNumber();this.after()},
  get isViewer(){return this.auth.role==='viewer'},get title(){return {'dashboard':'Dashboard inicial','prestadores':'Cadastro de prestadores','servicos':'Serviços','pagamentos':'Central de pagamentos','recibos':'Emissão de recibos','relatorios':'Relatórios','busca':'Busca global','config':'Configurações'}[this.view]||'ReciboCondo'},get selectedProvider(){return this.providers.find(p=>Number(p.id)===Number(this.selectedProviderId))||null},
  get cards(){return this.dashboardCards},
  get yearlyRows(){let y=String(this.dashboardYear||new Date().getFullYear());return this.services.filter(s=>String(s.paymentDate||s.serviceDate||'').startsWith(y))},
  get dashboardCards(){let nowRef=new Date().toISOString().slice(0,7), year=this.yearlyRows, month=year.filter(s=>String(s.paymentDate||s.serviceDate||'').slice(0,7)===nowRef), paidYear=year.reduce((a,s)=>a+Number(s.paidValue||0),0), paidMonth=month.reduce((a,s)=>a+Number(s.paidValue||0),0), countMonth=month.filter(s=>Number(s.paidValue||0)>0).length, open=year.reduce((a,s)=>a+this.balance(s),0), top=this.annualCategorySummary[0], topYear=this.topPaidNames(year), topMonth=this.topPaidNames(month), openTop=this.topOpenNames(year), catTop=this.annualCategorySummary.slice(0,3).map(c=>c.category+' • '+this.money(c.total)), activeCats=[...new Set(this.providers.map(p=>p.category).filter(Boolean))].slice(0,4), receiptsMonth=this.receipts.filter(r=>String(r.paymentDate||r.date||'').slice(0,7)===nowRef).length;return[
    {label:'Pago no ano',value:this.money(paidYear),hint:String(this.dashboardYear),icon:'wallet-cards',color:'text-rc-600',tooltipTitle:'Resumo anual',tooltipMain:this.money(paidYear),tooltipTag:'Ano '+this.dashboardYear,tooltip:['Total efetivamente pago no ano selecionado.', 'Top recebimentos: '+(topYear.length?topYear.join(' | '):'sem pagamentos lançados.'), 'Use para comparar o volume anual com o relatório anual premium.']},
    {label:'Pago no mês',value:this.money(paidMonth),hint:this.month(nowRef),icon:'circle-dollar-sign',color:'text-emerald-600',tooltipTitle:'Resumo do mês',tooltipMain:this.money(paidMonth),tooltipTag:'Mês atual',tooltip:['Mês de referência: '+this.month(nowRef)+'.', 'Quem recebeu: '+(topMonth.length?topMonth.join(' | '):'sem pagamentos neste mês.'), 'Ajuda a conferir rapidamente o fechamento mensal.']},
    {label:'Pagamentos no mês',value:countMonth,hint:'registros pagos',icon:'receipt-text',color:'text-blue-600',tooltipTitle:'Movimento mensal',tooltipMain:countMonth+' pagamento(s)',tooltipTag:'Recibos '+receiptsMonth,tooltip:['Valor médio por pagamento: '+this.money(countMonth?paidMonth/countMonth:0)+'.', 'Recibos emitidos no mês: '+receiptsMonth+'.', 'Bom para conferir se todos os pagamentos pagos já têm recibo.']},
    {label:'Prestadores ativos',value:this.providers.length,hint:'cadastrados',icon:'users',color:'text-rc-600',tooltipTitle:'Base de prestadores',tooltipMain:this.providers.length+' cadastro(s)',tooltipTag:'Cadastros',tooltip:['Categorias em uso: '+(activeCats.length?activeCats.join(', '):'nenhuma categoria cadastrada.'), 'Use para localizar quem presta serviço recorrente ou avulso.', 'Dica: mantenha PIX, WhatsApp e categoria sempre atualizados.']},
    {label:'Em aberto no ano',value:this.money(open),hint:'pendente/parcial',icon:'triangle-alert',color:'text-red-600',tooltipTitle:'Pendências do ano',tooltipMain:this.money(open),tooltipTag:'Atenção',tooltip:['Saldo lançado e ainda não quitado no ano.', 'Maiores pendências: '+(openTop.length?openTop.join(' | '):'nenhuma pendência no ano.'), 'Depois de pagar, registre o pagamento antes de emitir o recibo.']},
    {label:'Maior categoria',value:top?top.category:'-',hint:top?this.money(top.total):'sem dados',icon:'trophy',color:'text-yellow-600',tooltipTitle:'Categoria líder',tooltipMain:top?top.category:'Sem dados',tooltipTag:top?this.money(top.total):'Sem dados',tooltip:['Categoria com maior volume pago no ano.', 'Ranking: '+(catTop.length?catTop.join(' | '):'sem categorias pagas no ano.'), 'Ajuda a identificar onde o condomínio mais gastou.']}
  ]},
  get annualTotals(){let paid=this.yearlyRows.reduce((a,s)=>a+Number(s.paidValue||0),0),open=this.yearlyRows.reduce((a,s)=>a+this.balance(s),0),count=this.yearlyRows.filter(s=>Number(s.paidValue||0)>0).length;return{paid,open,count}},
  get monthlyHistory(){let labels=['Janeiro','Fevereiro','Março','Abril','Maio','Junho','Julho','Agosto','Setembro','Outubro','Novembro','Dezembro'];return labels.map((label,i)=>{let key=String(this.dashboardYear)+'-'+String(i+1).padStart(2,'0'), rows=this.yearlyRows.filter(s=>String(s.paymentDate||s.serviceDate||'').slice(0,7)===key), paid=rows.reduce((a,s)=>a+Number(s.paidValue||0),0), open=rows.reduce((a,s)=>a+this.balance(s),0), map={};rows.forEach(s=>{map[s.category]=(map[s.category]||0)+Number(s.paidValue||s.amount||0)});let top=Object.entries(map).sort((a,b)=>b[1]-a[1])[0];return{key,label,count:rows.filter(s=>Number(s.paidValue||0)>0).length,paid,open,topCategory:top?top[0]:'-'}})},
  get annualTopProviders(){let map={};this.yearlyRows.forEach(s=>{let id=s.providerId,p=this.providers.find(x=>Number(x.id)===Number(id))||{};if(!map[id])map[id]={id,name:p.name||'-',cat

===== paidValue @ 6452318 =====
amentos no mês',value:countMonth,hint:'registros pagos',icon:'receipt-text',color:'text-blue-600',tooltipTitle:'Movimento mensal',tooltipMain:countMonth+' pagamento(s)',tooltipTag:'Recibos '+receiptsMonth,tooltip:['Valor médio por pagamento: '+this.money(countMonth?paidMonth/countMonth:0)+'.', 'Recibos emitidos no mês: '+receiptsMonth+'.', 'Bom para conferir se todos os pagamentos pagos já têm recibo.']},
    {label:'Prestadores ativos',value:this.providers.length,hint:'cadastrados',icon:'users',color:'text-rc-600',tooltipTitle:'Base de prestadores',tooltipMain:this.providers.length+' cadastro(s)',tooltipTag:'Cadastros',tooltip:['Categorias em uso: '+(activeCats.length?activeCats.join(', '):'nenhuma categoria cadastrada.'), 'Use para localizar quem presta serviço recorrente ou avulso.', 'Dica: mantenha PIX, WhatsApp e categoria sempre atualizados.']},
    {label:'Em aberto no ano',value:this.money(open),hint:'pendente/parcial',icon:'triangle-alert',color:'text-red-600',tooltipTitle:'Pendências do ano',tooltipMain:this.money(open),tooltipTag:'Atenção',tooltip:['Saldo lançado e ainda não quitado no ano.', 'Maiores pendências: '+(openTop.length?openTop.join(' | '):'nenhuma pendência no ano.'), 'Depois de pagar, registre o pagamento antes de emitir o recibo.']},
    {label:'Maior categoria',value:top?top.category:'-',hint:top?this.money(top.total):'sem dados',icon:'trophy',color:'text-yellow-600',tooltipTitle:'Categoria líder',tooltipMain:top?top.category:'Sem dados',tooltipTag:top?this.money(top.total):'Sem dados',tooltip:['Categoria com maior volume pago no ano.', 'Ranking: '+(catTop.length?catTop.join(' | '):'sem categorias pagas no ano.'), 'Ajuda a identificar onde o condomínio mais gastou.']}
  ]},
  get annualTotals(){let paid=this.yearlyRows.reduce((a,s)=>a+Number(s.paidValue||0),0),open=this.yearlyRows.reduce((a,s)=>a+this.balance(s),0),count=this.yearlyRows.filter(s=>Number(s.paidValue||0)>0).length;return{paid,open,count}},
  get monthlyHistory(){let labels=['Janeiro','Fevereiro','Março','Abril','Maio','Junho','Julho','Agosto','Setembro','Outubro','Novembro','Dezembro'];return labels.map((label,i)=>{let key=String(this.dashboardYear)+'-'+String(i+1).padStart(2,'0'), rows=this.yearlyRows.filter(s=>String(s.paymentDate||s.serviceDate||'').slice(0,7)===key), paid=rows.reduce((a,s)=>a+Number(s.paidValue||0),0), open=rows.reduce((a,s)=>a+this.balance(s),0), map={};rows.forEach(s=>{map[s.category]=(map[s.category]||0)+Number(s.paidValue||s.amount||0)});let top=Object.entries(map).sort((a,b)=>b[1]-a[1])[0];return{key,label,count:rows.filter(s=>Number(s.paidValue||0)>0).length,paid,open,topCategory:top?top[0]:'-'}})},
  get annualTopProviders(){let map={};this.yearlyRows.forEach(s=>{let id=s.providerId,p=this.providers.find(x=>Number(x.id)===Number(id))||{};if(!map[id])map[id]={id,name:p.name||'-',category:p.category||s.category,count:0,total:0,open:0};if(Number(s.paidValue||0)>0)map[id].count++;map[id].total+=Number(s.paidValue||0);map[id].open+=this.balance(s)});return Object.values(map).filter(x=>x.total>0||x.open>0).sort((a,b)=>b.total-a.total).slice(0,10)},
  get annualCategorySummary(){let rows=this.yearlyRows,total=rows.reduce((a,s)=>a+Number(s.paidValue||0),0)||1;return this.categories.map(category=>{let val=rows.filter(s=>s.category===category).reduce((a,s)=>a+Number(s.paidValue||0),0);return{category,total:val,percent:Math.min(100,Math.round(val/total*100))}}).filter(x=>x.total>0).sort((a,b)=>b.total-a.total)},
  get topProviders(){return this.annualTopProviders},
  get categorySummary(){return this.annualCategorySummary},
  get openServices(){return this.services.filter(s=>s.status!=='Pago').sort((a,b)=>String(a.dueDate).localeCompare(String(b.dueDate)))},
  get filteredProviders(){
    let q=(this.q||'').toLowerCase();
    let ps=(this.providerSearch||'').toLowerCase();
    let cat=this.providerCategoryFilter||'';
    let list=this.providers.filter(p=>{
      if(q&&![p.name,p.document,p.category,p.phone,p.whatsapp].join(' ').toLowerCase().includes(q))return false;
      if(ps&&![p.name,p.document,p.category,p.phone,p.whatsapp].join(' ').toLowerCase().includes(ps))return false;
      if(cat&&p.category!==cat)return false;
      return true;
    });
    let field=this.providerSortField||'name';
    let dir=this.providerSortDir||'asc';
    list=[...list].sort((a,b)=>{
      let va=field==='spend'?this.providerSpend(a.id):String(a[field]||'').toLowerCase();
      let vb=field==='spend'?this.providerSpend(b.id):String(b[field]||'').toLowerCase();
      if(va<vb)return dir==='asc'?-1:1;
      if(va>vb)return dir==='asc'?1:-1;
      return 0;
    });
    return list;
  },
  get filteredServices(){return this.services.filter(s=>(this.serviceFilter==='todos'||s.status===this.serviceFilter)&&(this.serviceCategoryFilter==='todas'||s.category===this.serviceCategoryFilter)&&(this.serviceProviderFilter==='todos'||Number(s.providerId)===Number(this.serviceProviderFilter))&&(!this.serviceMonthFilter||s.reference===this.serviceMonthFilter)).sort((a,b)=>String(b.serviceDate).localeCompare(String(a.serviceDate)))},
  get filteredPayments(){return this.services.filter(s=>(!this.paymentMonthFilter||s.reference===this.paymentMonthFilter)&&(this.paymentStatusFilter==='todos'||s.status===this.paymentStatusFilter)&&(this.paymentProviderFilter==='todos'||Number(s.providerId)===Number(this.paymentProviderFilter))).sort((a,b)=>String(a.dueDate||'9999-12-31').localeCompare(String(b.dueDate||'9999-12-31')))},
  get paymentSummary(){let rows=this.filteredPayments, paid=rows.reduce((a,s)=>a+Number(s.paidValue||0),0), expected=rows.reduce((a,s)=>a+Number(s.amount||0),0), open=rows.reduce((a,s)=>a+this.balance(s),0), paidCount=rows.filter(s=>Number(s.paidValue||0)>0).length, missingReceipt=rows.filter(s=>Number(s.paidValue||0)>0&&this.reportReceiptNumber(s)==='-').length;return{rows,expected,paid,open,paidCount,missingReceipt,overdue:rows.filter(s=>s.status!=='Pago'&&s.dueDate<this.today()).length,today:rows.fi

===== paidValue @ 6452432 =====
vimento mensal',tooltipMain:countMonth+' pagamento(s)',tooltipTag:'Recibos '+receiptsMonth,tooltip:['Valor médio por pagamento: '+this.money(countMonth?paidMonth/countMonth:0)+'.', 'Recibos emitidos no mês: '+receiptsMonth+'.', 'Bom para conferir se todos os pagamentos pagos já têm recibo.']},
    {label:'Prestadores ativos',value:this.providers.length,hint:'cadastrados',icon:'users',color:'text-rc-600',tooltipTitle:'Base de prestadores',tooltipMain:this.providers.length+' cadastro(s)',tooltipTag:'Cadastros',tooltip:['Categorias em uso: '+(activeCats.length?activeCats.join(', '):'nenhuma categoria cadastrada.'), 'Use para localizar quem presta serviço recorrente ou avulso.', 'Dica: mantenha PIX, WhatsApp e categoria sempre atualizados.']},
    {label:'Em aberto no ano',value:this.money(open),hint:'pendente/parcial',icon:'triangle-alert',color:'text-red-600',tooltipTitle:'Pendências do ano',tooltipMain:this.money(open),tooltipTag:'Atenção',tooltip:['Saldo lançado e ainda não quitado no ano.', 'Maiores pendências: '+(openTop.length?openTop.join(' | '):'nenhuma pendência no ano.'), 'Depois de pagar, registre o pagamento antes de emitir o recibo.']},
    {label:'Maior categoria',value:top?top.category:'-',hint:top?this.money(top.total):'sem dados',icon:'trophy',color:'text-yellow-600',tooltipTitle:'Categoria líder',tooltipMain:top?top.category:'Sem dados',tooltipTag:top?this.money(top.total):'Sem dados',tooltip:['Categoria com maior volume pago no ano.', 'Ranking: '+(catTop.length?catTop.join(' | '):'sem categorias pagas no ano.'), 'Ajuda a identificar onde o condomínio mais gastou.']}
  ]},
  get annualTotals(){let paid=this.yearlyRows.reduce((a,s)=>a+Number(s.paidValue||0),0),open=this.yearlyRows.reduce((a,s)=>a+this.balance(s),0),count=this.yearlyRows.filter(s=>Number(s.paidValue||0)>0).length;return{paid,open,count}},
  get monthlyHistory(){let labels=['Janeiro','Fevereiro','Março','Abril','Maio','Junho','Julho','Agosto','Setembro','Outubro','Novembro','Dezembro'];return labels.map((label,i)=>{let key=String(this.dashboardYear)+'-'+String(i+1).padStart(2,'0'), rows=this.yearlyRows.filter(s=>String(s.paymentDate||s.serviceDate||'').slice(0,7)===key), paid=rows.reduce((a,s)=>a+Number(s.paidValue||0),0), open=rows.reduce((a,s)=>a+this.balance(s),0), map={};rows.forEach(s=>{map[s.category]=(map[s.category]||0)+Number(s.paidValue||s.amount||0)});let top=Object.entries(map).sort((a,b)=>b[1]-a[1])[0];return{key,label,count:rows.filter(s=>Number(s.paidValue||0)>0).length,paid,open,topCategory:top?top[0]:'-'}})},
  get annualTopProviders(){let map={};this.yearlyRows.forEach(s=>{let id=s.providerId,p=this.providers.find(x=>Number(x.id)===Number(id))||{};if(!map[id])map[id]={id,name:p.name||'-',category:p.category||s.category,count:0,total:0,open:0};if(Number(s.paidValue||0)>0)map[id].count++;map[id].total+=Number(s.paidValue||0);map[id].open+=this.balance(s)});return Object.values(map).filter(x=>x.total>0||x.open>0).sort((a,b)=>b.total-a.total).slice(0,10)},
  get annualCategorySummary(){let rows=this.yearlyRows,total=rows.reduce((a,s)=>a+Number(s.paidValue||0),0)||1;return this.categories.map(category=>{let val=rows.filter(s=>s.category===category).reduce((a,s)=>a+Number(s.paidValue||0),0);return{category,total:val,percent:Math.min(100,Math.round(val/total*100))}}).filter(x=>x.total>0).sort((a,b)=>b.total-a.total)},
  get topProviders(){return this.annualTopProviders},
  get categorySummary(){return this.annualCategorySummary},
  get openServices(){return this.services.filter(s=>s.status!=='Pago').sort((a,b)=>String(a.dueDate).localeCompare(String(b.dueDate)))},
  get filteredProviders(){
    let q=(this.q||'').toLowerCase();
    let ps=(this.providerSearch||'').toLowerCase();
    let cat=this.providerCategoryFilter||'';
    let list=this.providers.filter(p=>{
      if(q&&![p.name,p.document,p.category,p.phone,p.whatsapp].join(' ').toLowerCase().includes(q))return false;
      if(ps&&![p.name,p.document,p.category,p.phone,p.whatsapp].join(' ').toLowerCase().includes(ps))return false;
      if(cat&&p.category!==cat)return false;
      return true;
    });
    let field=this.providerSortField||'name';
    let dir=this.providerSortDir||'asc';
    list=[...list].sort((a,b)=>{
      let va=field==='spend'?this.providerSpend(a.id):String(a[field]||'').toLowerCase();
      let vb=field==='spend'?this.providerSpend(b.id):String(b[field]||'').toLowerCase();
      if(va<vb)return dir==='asc'?-1:1;
      if(va>vb)return dir==='asc'?1:-1;
      return 0;
    });
    return list;
  },
  get filteredServices(){return this.services.filter(s=>(this.serviceFilter==='todos'||s.status===this.serviceFilter)&&(this.serviceCategoryFilter==='todas'||s.category===this.serviceCategoryFilter)&&(this.serviceProviderFilter==='todos'||Number(s.providerId)===Number(this.serviceProviderFilter))&&(!this.serviceMonthFilter||s.reference===this.serviceMonthFilter)).sort((a,b)=>String(b.serviceDate).localeCompare(String(a.serviceDate)))},
  get filteredPayments(){return this.services.filter(s=>(!this.paymentMonthFilter||s.reference===this.paymentMonthFilter)&&(this.paymentStatusFilter==='todos'||s.status===this.paymentStatusFilter)&&(this.paymentProviderFilter==='todos'||Number(s.providerId)===Number(this.paymentProviderFilter))).sort((a,b)=>String(a.dueDate||'9999-12-31').localeCompare(String(b.dueDate||'9999-12-31')))},
  get paymentSummary(){let rows=this.filteredPayments, paid=rows.reduce((a,s)=>a+Number(s.paidValue||0),0), expected=rows.reduce((a,s)=>a+Number(s.amount||0),0), open=rows.reduce((a,s)=>a+this.balance(s),0), paidCount=rows.filter(s=>Number(s.paidValue||0)>0).length, missingReceipt=rows.filter(s=>Number(s.paidValue||0)>0&&this.reportReceiptNumber(s)==='-').length;return{rows,expected,paid,open,paidCount,missingReceipt,overdue:rows.filter(s=>s.status!=='Pago'&&s.dueDate<this.today()).length,today:rows.filter(s=>s.status!=='Pago'&&s.dueDate===this.today()).length}},
  get paymentRecent(){return this.services.filter(s

===== paidValue @ 6452855 =====
se de prestadores',tooltipMain:this.providers.length+' cadastro(s)',tooltipTag:'Cadastros',tooltip:['Categorias em uso: '+(activeCats.length?activeCats.join(', '):'nenhuma categoria cadastrada.'), 'Use para localizar quem presta serviço recorrente ou avulso.', 'Dica: mantenha PIX, WhatsApp e categoria sempre atualizados.']},
    {label:'Em aberto no ano',value:this.money(open),hint:'pendente/parcial',icon:'triangle-alert',color:'text-red-600',tooltipTitle:'Pendências do ano',tooltipMain:this.money(open),tooltipTag:'Atenção',tooltip:['Saldo lançado e ainda não quitado no ano.', 'Maiores pendências: '+(openTop.length?openTop.join(' | '):'nenhuma pendência no ano.'), 'Depois de pagar, registre o pagamento antes de emitir o recibo.']},
    {label:'Maior categoria',value:top?top.category:'-',hint:top?this.money(top.total):'sem dados',icon:'trophy',color:'text-yellow-600',tooltipTitle:'Categoria líder',tooltipMain:top?top.category:'Sem dados',tooltipTag:top?this.money(top.total):'Sem dados',tooltip:['Categoria com maior volume pago no ano.', 'Ranking: '+(catTop.length?catTop.join(' | '):'sem categorias pagas no ano.'), 'Ajuda a identificar onde o condomínio mais gastou.']}
  ]},
  get annualTotals(){let paid=this.yearlyRows.reduce((a,s)=>a+Number(s.paidValue||0),0),open=this.yearlyRows.reduce((a,s)=>a+this.balance(s),0),count=this.yearlyRows.filter(s=>Number(s.paidValue||0)>0).length;return{paid,open,count}},
  get monthlyHistory(){let labels=['Janeiro','Fevereiro','Março','Abril','Maio','Junho','Julho','Agosto','Setembro','Outubro','Novembro','Dezembro'];return labels.map((label,i)=>{let key=String(this.dashboardYear)+'-'+String(i+1).padStart(2,'0'), rows=this.yearlyRows.filter(s=>String(s.paymentDate||s.serviceDate||'').slice(0,7)===key), paid=rows.reduce((a,s)=>a+Number(s.paidValue||0),0), open=rows.reduce((a,s)=>a+this.balance(s),0), map={};rows.forEach(s=>{map[s.category]=(map[s.category]||0)+Number(s.paidValue||s.amount||0)});let top=Object.entries(map).sort((a,b)=>b[1]-a[1])[0];return{key,label,count:rows.filter(s=>Number(s.paidValue||0)>0).length,paid,open,topCategory:top?top[0]:'-'}})},
  get annualTopProviders(){let map={};this.yearlyRows.forEach(s=>{let id=s.providerId,p=this.providers.find(x=>Number(x.id)===Number(id))||{};if(!map[id])map[id]={id,name:p.name||'-',category:p.category||s.category,count:0,total:0,open:0};if(Number(s.paidValue||0)>0)map[id].count++;map[id].total+=Number(s.paidValue||0);map[id].open+=this.balance(s)});return Object.values(map).filter(x=>x.total>0||x.open>0).sort((a,b)=>b.total-a.total).slice(0,10)},
  get annualCategorySummary(){let rows=this.yearlyRows,total=rows.reduce((a,s)=>a+Number(s.paidValue||0),0)||1;return this.categories.map(category=>{let val=rows.filter(s=>s.category===category).reduce((a,s)=>a+Number(s.paidValue||0),0);return{category,total:val,percent:Math.min(100,Math.round(val/total*100))}}).filter(x=>x.total>0).sort((a,b)=>b.total-a.total)},
  get topProviders(){return this.annualTopProviders},
  get categorySummary(){return this.annualCategorySummary},
  get openServices(){return this.services.filter(s=>s.status!=='Pago').sort((a,b)=>String(a.dueDate).localeCompare(String(b.dueDate)))},
  get filteredProviders(){
    let q=(this.q||'').toLowerCase();
    let ps=(this.providerSearch||'').toLowerCase();
    let cat=this.providerCategoryFilter||'';
    let list=this.providers.filter(p=>{
      if(q&&![p.name,p.document,p.category,p.phone,p.whatsapp].join(' ').toLowerCase().includes(q))return false;
      if(ps&&![p.name,p.document,p.category,p.phone,p.whatsapp].join(' ').toLowerCase().includes(ps))return false;
      if(cat&&p.category!==cat)return false;
      return true;
    });
    let field=this.providerSortField||'name';
    let dir=this.providerSortDir||'asc';
    list=[...list].sort((a,b)=>{
      let va=field==='spend'?this.providerSpend(a.id):String(a[field]||'').toLowerCase();
      let vb=field==='spend'?this.providerSpend(b.id):String(b[field]||'').toLowerCase();
      if(va<vb)return dir==='asc'?-1:1;
      if(va>vb)return dir==='asc'?1:-1;
      return 0;
    });
    return list;
  },
  get filteredServices(){return this.services.filter(s=>(this.serviceFilter==='todos'||s.status===this.serviceFilter)&&(this.serviceCategoryFilter==='todas'||s.category===this.serviceCategoryFilter)&&(this.serviceProviderFilter==='todos'||Number(s.providerId)===Number(this.serviceProviderFilter))&&(!this.serviceMonthFilter||s.reference===this.serviceMonthFilter)).sort((a,b)=>String(b.serviceDate).localeCompare(String(a.serviceDate)))},
  get filteredPayments(){return this.services.filter(s=>(!this.paymentMonthFilter||s.reference===this.paymentMonthFilter)&&(this.paymentStatusFilter==='todos'||s.status===this.paymentStatusFilter)&&(this.paymentProviderFilter==='todos'||Number(s.providerId)===Number(this.paymentProviderFilter))).sort((a,b)=>String(a.dueDate||'9999-12-31').localeCompare(String(b.dueDate||'9999-12-31')))},
  get paymentSummary(){let rows=this.filteredPayments, paid=rows.reduce((a,s)=>a+Number(s.paidValue||0),0), expected=rows.reduce((a,s)=>a+Number(s.amount||0),0), open=rows.reduce((a,s)=>a+this.balance(s),0), paidCount=rows.filter(s=>Number(s.paidValue||0)>0).length, missingReceipt=rows.filter(s=>Number(s.paidValue||0)>0&&this.reportReceiptNumber(s)==='-').length;return{rows,expected,paid,open,paidCount,missingReceipt,overdue:rows.filter(s=>s.status!=='Pago'&&s.dueDate<this.today()).length,today:rows.filter(s=>s.status!=='Pago'&&s.dueDate===this.today()).length}},
  get paymentRecent(){return this.services.filter(s=>Number(s.paidValue||0)>0).sort((a,b)=>String(b.paymentDate||'').localeCompare(String(a.paymentDate||''))).slice(0,8)},
  get servicePanelRows(){return this.services.filter(s=>s.reference===this.servicePanelMonth).sort((a,b)=>String(b.paymentDate||b.serviceDate).localeCompare(String(a.paymentDate||a.serviceDate)))},
  get servicePanelPaidRows(){return this.servicePanelRows.filter(s=>Number(s.paidValue||0)>0).sort((a,b)

===== paidValue @ 6452989 =====
length?activeCats.join(', '):'nenhuma categoria cadastrada.'), 'Use para localizar quem presta serviço recorrente ou avulso.', 'Dica: mantenha PIX, WhatsApp e categoria sempre atualizados.']},
    {label:'Em aberto no ano',value:this.money(open),hint:'pendente/parcial',icon:'triangle-alert',color:'text-red-600',tooltipTitle:'Pendências do ano',tooltipMain:this.money(open),tooltipTag:'Atenção',tooltip:['Saldo lançado e ainda não quitado no ano.', 'Maiores pendências: '+(openTop.length?openTop.join(' | '):'nenhuma pendência no ano.'), 'Depois de pagar, registre o pagamento antes de emitir o recibo.']},
    {label:'Maior categoria',value:top?top.category:'-',hint:top?this.money(top.total):'sem dados',icon:'trophy',color:'text-yellow-600',tooltipTitle:'Categoria líder',tooltipMain:top?top.category:'Sem dados',tooltipTag:top?this.money(top.total):'Sem dados',tooltip:['Categoria com maior volume pago no ano.', 'Ranking: '+(catTop.length?catTop.join(' | '):'sem categorias pagas no ano.'), 'Ajuda a identificar onde o condomínio mais gastou.']}
  ]},
  get annualTotals(){let paid=this.yearlyRows.reduce((a,s)=>a+Number(s.paidValue||0),0),open=this.yearlyRows.reduce((a,s)=>a+this.balance(s),0),count=this.yearlyRows.filter(s=>Number(s.paidValue||0)>0).length;return{paid,open,count}},
  get monthlyHistory(){let labels=['Janeiro','Fevereiro','Março','Abril','Maio','Junho','Julho','Agosto','Setembro','Outubro','Novembro','Dezembro'];return labels.map((label,i)=>{let key=String(this.dashboardYear)+'-'+String(i+1).padStart(2,'0'), rows=this.yearlyRows.filter(s=>String(s.paymentDate||s.serviceDate||'').slice(0,7)===key), paid=rows.reduce((a,s)=>a+Number(s.paidValue||0),0), open=rows.reduce((a,s)=>a+this.balance(s),0), map={};rows.forEach(s=>{map[s.category]=(map[s.category]||0)+Number(s.paidValue||s.amount||0)});let top=Object.entries(map).sort((a,b)=>b[1]-a[1])[0];return{key,label,count:rows.filter(s=>Number(s.paidValue||0)>0).length,paid,open,topCategory:top?top[0]:'-'}})},
  get annualTopProviders(){let map={};this.yearlyRows.forEach(s=>{let id=s.providerId,p=this.providers.find(x=>Number(x.id)===Number(id))||{};if(!map[id])map[id]={id,name:p.name||'-',category:p.category||s.category,count:0,total:0,open:0};if(Number(s.paidValue||0)>0)map[id].count++;map[id].total+=Number(s.paidValue||0);map[id].open+=this.balance(s)});return Object.values(map).filter(x=>x.total>0||x.open>0).sort((a,b)=>b.total-a.total).slice(0,10)},
  get annualCategorySummary(){let rows=this.yearlyRows,total=rows.reduce((a,s)=>a+Number(s.paidValue||0),0)||1;return this.categories.map(category=>{let val=rows.filter(s=>s.category===category).reduce((a,s)=>a+Number(s.paidValue||0),0);return{category,total:val,percent:Math.min(100,Math.round(val/total*100))}}).filter(x=>x.total>0).sort((a,b)=>b.total-a.total)},
  get topProviders(){return this.annualTopProviders},
  get categorySummary(){return this.annualCategorySummary},
  get openServices(){return this.services.filter(s=>s.status!=='Pago').sort((a,b)=>String(a.dueDate).localeCompare(String(b.dueDate)))},
  get filteredProviders(){
    let q=(this.q||'').toLowerCase();
    let ps=(this.providerSearch||'').toLowerCase();
    let cat=this.providerCategoryFilter||'';
    let list=this.providers.filter(p=>{
      if(q&&![p.name,p.document,p.category,p.phone,p.whatsapp].join(' ').toLowerCase().includes(q))return false;
      if(ps&&![p.name,p.document,p.category,p.phone,p.whatsapp].join(' ').toLowerCase().includes(ps))return false;
      if(cat&&p.category!==cat)return false;
      return true;
    });
    let field=this.providerSortField||'name';
    let dir=this.providerSortDir||'asc';
    list=[...list].sort((a,b)=>{
      let va=field==='spend'?this.providerSpend(a.id):String(a[field]||'').toLowerCase();
      let vb=field==='spend'?this.providerSpend(b.id):String(b[field]||'').toLowerCase();
      if(va<vb)return dir==='asc'?-1:1;
      if(va>vb)return dir==='asc'?1:-1;
      return 0;
    });
    return list;
  },
  get filteredServices(){return this.services.filter(s=>(this.serviceFilter==='todos'||s.status===this.serviceFilter)&&(this.serviceCategoryFilter==='todas'||s.category===this.serviceCategoryFilter)&&(this.serviceProviderFilter==='todos'||Number(s.providerId)===Number(this.serviceProviderFilter))&&(!this.serviceMonthFilter||s.reference===this.serviceMonthFilter)).sort((a,b)=>String(b.serviceDate).localeCompare(String(a.serviceDate)))},
  get filteredPayments(){return this.services.filter(s=>(!this.paymentMonthFilter||s.reference===this.paymentMonthFilter)&&(this.paymentStatusFilter==='todos'||s.status===this.paymentStatusFilter)&&(this.paymentProviderFilter==='todos'||Number(s.providerId)===Number(this.paymentProviderFilter))).sort((a,b)=>String(a.dueDate||'9999-12-31').localeCompare(String(b.dueDate||'9999-12-31')))},
  get paymentSummary(){let rows=this.filteredPayments, paid=rows.reduce((a,s)=>a+Number(s.paidValue||0),0), expected=rows.reduce((a,s)=>a+Number(s.amount||0),0), open=rows.reduce((a,s)=>a+this.balance(s),0), paidCount=rows.filter(s=>Number(s.paidValue||0)>0).length, missingReceipt=rows.filter(s=>Number(s.paidValue||0)>0&&this.reportReceiptNumber(s)==='-').length;return{rows,expected,paid,open,paidCount,missingReceipt,overdue:rows.filter(s=>s.status!=='Pago'&&s.dueDate<this.today()).length,today:rows.filter(s=>s.status!=='Pago'&&s.dueDate===this.today()).length}},
  get paymentRecent(){return this.services.filter(s=>Number(s.paidValue||0)>0).sort((a,b)=>String(b.paymentDate||'').localeCompare(String(a.paymentDate||''))).slice(0,8)},
  get servicePanelRows(){return this.services.filter(s=>s.reference===this.servicePanelMonth).sort((a,b)=>String(b.paymentDate||b.serviceDate).localeCompare(String(a.paymentDate||a.serviceDate)))},
  get servicePanelPaidRows(){return this.servicePanelRows.filter(s=>Number(s.paidValue||0)>0).sort((a,b)=>String(b.paymentDate||b.serviceDate).localeCompare(String(a.paymentDate||a.serviceDate)))},
  get servicePanelOpenRows(){return this

===== paidValue @ 6453116 =====
'Dica: mantenha PIX, WhatsApp e categoria sempre atualizados.']},
    {label:'Em aberto no ano',value:this.money(open),hint:'pendente/parcial',icon:'triangle-alert',color:'text-red-600',tooltipTitle:'Pendências do ano',tooltipMain:this.money(open),tooltipTag:'Atenção',tooltip:['Saldo lançado e ainda não quitado no ano.', 'Maiores pendências: '+(openTop.length?openTop.join(' | '):'nenhuma pendência no ano.'), 'Depois de pagar, registre o pagamento antes de emitir o recibo.']},
    {label:'Maior categoria',value:top?top.category:'-',hint:top?this.money(top.total):'sem dados',icon:'trophy',color:'text-yellow-600',tooltipTitle:'Categoria líder',tooltipMain:top?top.category:'Sem dados',tooltipTag:top?this.money(top.total):'Sem dados',tooltip:['Categoria com maior volume pago no ano.', 'Ranking: '+(catTop.length?catTop.join(' | '):'sem categorias pagas no ano.'), 'Ajuda a identificar onde o condomínio mais gastou.']}
  ]},
  get annualTotals(){let paid=this.yearlyRows.reduce((a,s)=>a+Number(s.paidValue||0),0),open=this.yearlyRows.reduce((a,s)=>a+this.balance(s),0),count=this.yearlyRows.filter(s=>Number(s.paidValue||0)>0).length;return{paid,open,count}},
  get monthlyHistory(){let labels=['Janeiro','Fevereiro','Março','Abril','Maio','Junho','Julho','Agosto','Setembro','Outubro','Novembro','Dezembro'];return labels.map((label,i)=>{let key=String(this.dashboardYear)+'-'+String(i+1).padStart(2,'0'), rows=this.yearlyRows.filter(s=>String(s.paymentDate||s.serviceDate||'').slice(0,7)===key), paid=rows.reduce((a,s)=>a+Number(s.paidValue||0),0), open=rows.reduce((a,s)=>a+this.balance(s),0), map={};rows.forEach(s=>{map[s.category]=(map[s.category]||0)+Number(s.paidValue||s.amount||0)});let top=Object.entries(map).sort((a,b)=>b[1]-a[1])[0];return{key,label,count:rows.filter(s=>Number(s.paidValue||0)>0).length,paid,open,topCategory:top?top[0]:'-'}})},
  get annualTopProviders(){let map={};this.yearlyRows.forEach(s=>{let id=s.providerId,p=this.providers.find(x=>Number(x.id)===Number(id))||{};if(!map[id])map[id]={id,name:p.name||'-',category:p.category||s.category,count:0,total:0,open:0};if(Number(s.paidValue||0)>0)map[id].count++;map[id].total+=Number(s.paidValue||0);map[id].open+=this.balance(s)});return Object.values(map).filter(x=>x.total>0||x.open>0).sort((a,b)=>b.total-a.total).slice(0,10)},
  get annualCategorySummary(){let rows=this.yearlyRows,total=rows.reduce((a,s)=>a+Number(s.paidValue||0),0)||1;return this.categories.map(category=>{let val=rows.filter(s=>s.category===category).reduce((a,s)=>a+Number(s.paidValue||0),0);return{category,total:val,percent:Math.min(100,Math.round(val/total*100))}}).filter(x=>x.total>0).sort((a,b)=>b.total-a.total)},
  get topProviders(){return this.annualTopProviders},
  get categorySummary(){return this.annualCategorySummary},
  get openServices(){return this.services.filter(s=>s.status!=='Pago').sort((a,b)=>String(a.dueDate).localeCompare(String(b.dueDate)))},
  get filteredProviders(){
    let q=(this.q||'').toLowerCase();
    let ps=(this.providerSearch||'').toLowerCase();
    let cat=this.providerCategoryFilter||'';
    let list=this.providers.filter(p=>{
      if(q&&![p.name,p.document,p.category,p.phone,p.whatsapp].join(' ').toLowerCase().includes(q))return false;
      if(ps&&![p.name,p.document,p.category,p.phone,p.whatsapp].join(' ').toLowerCase().includes(ps))return false;
      if(cat&&p.category!==cat)return false;
      return true;
    });
    let field=this.providerSortField||'name';
    let dir=this.providerSortDir||'asc';
    list=[...list].sort((a,b)=>{
      let va=field==='spend'?this.providerSpend(a.id):String(a[field]||'').toLowerCase();
      let vb=field==='spend'?this.providerSpend(b.id):String(b[field]||'').toLowerCase();
      if(va<vb)return dir==='asc'?-1:1;
      if(va>vb)return dir==='asc'?1:-1;
      return 0;
    });
    return list;
  },
  get filteredServices(){return this.services.filter(s=>(this.serviceFilter==='todos'||s.status===this.serviceFilter)&&(this.serviceCategoryFilter==='todas'||s.category===this.serviceCategoryFilter)&&(this.serviceProviderFilter==='todos'||Number(s.providerId)===Number(this.serviceProviderFilter))&&(!this.serviceMonthFilter||s.reference===this.serviceMonthFilter)).sort((a,b)=>String(b.serviceDate).localeCompare(String(a.serviceDate)))},
  get filteredPayments(){return this.services.filter(s=>(!this.paymentMonthFilter||s.reference===this.paymentMonthFilter)&&(this.paymentStatusFilter==='todos'||s.status===this.paymentStatusFilter)&&(this.paymentProviderFilter==='todos'||Number(s.providerId)===Number(this.paymentProviderFilter))).sort((a,b)=>String(a.dueDate||'9999-12-31').localeCompare(String(b.dueDate||'9999-12-31')))},
  get paymentSummary(){let rows=this.filteredPayments, paid=rows.reduce((a,s)=>a+Number(s.paidValue||0),0), expected=rows.reduce((a,s)=>a+Number(s.amount||0),0), open=rows.reduce((a,s)=>a+this.balance(s),0), paidCount=rows.filter(s=>Number(s.paidValue||0)>0).length, missingReceipt=rows.filter(s=>Number(s.paidValue||0)>0&&this.reportReceiptNumber(s)==='-').length;return{rows,expected,paid,open,paidCount,missingReceipt,overdue:rows.filter(s=>s.status!=='Pago'&&s.dueDate<this.today()).length,today:rows.filter(s=>s.status!=='Pago'&&s.dueDate===this.today()).length}},
  get paymentRecent(){return this.services.filter(s=>Number(s.paidValue||0)>0).sort((a,b)=>String(b.paymentDate||'').localeCompare(String(a.paymentDate||''))).slice(0,8)},
  get servicePanelRows(){return this.services.filter(s=>s.reference===this.servicePanelMonth).sort((a,b)=>String(b.paymentDate||b.serviceDate).localeCompare(String(a.paymentDate||a.serviceDate)))},
  get servicePanelPaidRows(){return this.servicePanelRows.filter(s=>Number(s.paidValue||0)>0).sort((a,b)=>String(b.paymentDate||b.serviceDate).localeCompare(String(a.paymentDate||a.serviceDate)))},
  get servicePanelOpenRows(){return this.servicePanelRows.filter(s=>this.balance(s)>0).sort((a,b)=>String(a.dueDate||'2999-12-31').localeCompare(String(b.dueDate||'299

===== paidValue @ 6453433 =====
no.', 'Maiores pendências: '+(openTop.length?openTop.join(' | '):'nenhuma pendência no ano.'), 'Depois de pagar, registre o pagamento antes de emitir o recibo.']},
    {label:'Maior categoria',value:top?top.category:'-',hint:top?this.money(top.total):'sem dados',icon:'trophy',color:'text-yellow-600',tooltipTitle:'Categoria líder',tooltipMain:top?top.category:'Sem dados',tooltipTag:top?this.money(top.total):'Sem dados',tooltip:['Categoria com maior volume pago no ano.', 'Ranking: '+(catTop.length?catTop.join(' | '):'sem categorias pagas no ano.'), 'Ajuda a identificar onde o condomínio mais gastou.']}
  ]},
  get annualTotals(){let paid=this.yearlyRows.reduce((a,s)=>a+Number(s.paidValue||0),0),open=this.yearlyRows.reduce((a,s)=>a+this.balance(s),0),count=this.yearlyRows.filter(s=>Number(s.paidValue||0)>0).length;return{paid,open,count}},
  get monthlyHistory(){let labels=['Janeiro','Fevereiro','Março','Abril','Maio','Junho','Julho','Agosto','Setembro','Outubro','Novembro','Dezembro'];return labels.map((label,i)=>{let key=String(this.dashboardYear)+'-'+String(i+1).padStart(2,'0'), rows=this.yearlyRows.filter(s=>String(s.paymentDate||s.serviceDate||'').slice(0,7)===key), paid=rows.reduce((a,s)=>a+Number(s.paidValue||0),0), open=rows.reduce((a,s)=>a+this.balance(s),0), map={};rows.forEach(s=>{map[s.category]=(map[s.category]||0)+Number(s.paidValue||s.amount||0)});let top=Object.entries(map).sort((a,b)=>b[1]-a[1])[0];return{key,label,count:rows.filter(s=>Number(s.paidValue||0)>0).length,paid,open,topCategory:top?top[0]:'-'}})},
  get annualTopProviders(){let map={};this.yearlyRows.forEach(s=>{let id=s.providerId,p=this.providers.find(x=>Number(x.id)===Number(id))||{};if(!map[id])map[id]={id,name:p.name||'-',category:p.category||s.category,count:0,total:0,open:0};if(Number(s.paidValue||0)>0)map[id].count++;map[id].total+=Number(s.paidValue||0);map[id].open+=this.balance(s)});return Object.values(map).filter(x=>x.total>0||x.open>0).sort((a,b)=>b.total-a.total).slice(0,10)},
  get annualCategorySummary(){let rows=this.yearlyRows,total=rows.reduce((a,s)=>a+Number(s.paidValue||0),0)||1;return this.categories.map(category=>{let val=rows.filter(s=>s.category===category).reduce((a,s)=>a+Number(s.paidValue||0),0);return{category,total:val,percent:Math.min(100,Math.round(val/total*100))}}).filter(x=>x.total>0).sort((a,b)=>b.total-a.total)},
  get topProviders(){return this.annualTopProviders},
  get categorySummary(){return this.annualCategorySummary},
  get openServices(){return this.services.filter(s=>s.status!=='Pago').sort((a,b)=>String(a.dueDate).localeCompare(String(b.dueDate)))},
  get filteredProviders(){
    let q=(this.q||'').toLowerCase();
    let ps=(this.providerSearch||'').toLowerCase();
    let cat=this.providerCategoryFilter||'';
    let list=this.providers.filter(p=>{
      if(q&&![p.name,p.document,p.category,p.phone,p.whatsapp].join(' ').toLowerCase().includes(q))return false;
      if(ps&&![p.name,p.document,p.category,p.phone,p.whatsapp].join(' ').toLowerCase().includes(ps))return false;
      if(cat&&p.category!==cat)return false;
      return true;
    });
    let field=this.providerSortField||'name';
    let dir=this.providerSortDir||'asc';
    list=[...list].sort((a,b)=>{
      let va=field==='spend'?this.providerSpend(a.id):String(a[field]||'').toLowerCase();
      let vb=field==='spend'?this.providerSpend(b.id):String(b[field]||'').toLowerCase();
      if(va<vb)return dir==='asc'?-1:1;
      if(va>vb)return dir==='asc'?1:-1;
      return 0;
    });
    return list;
  },
  get filteredServices(){return this.services.filter(s=>(this.serviceFilter==='todos'||s.status===this.serviceFilter)&&(this.serviceCategoryFilter==='todas'||s.category===this.serviceCategoryFilter)&&(this.serviceProviderFilter==='todos'||Number(s.providerId)===Number(this.serviceProviderFilter))&&(!this.serviceMonthFilter||s.reference===this.serviceMonthFilter)).sort((a,b)=>String(b.serviceDate).localeCompare(String(a.serviceDate)))},
  get filteredPayments(){return this.services.filter(s=>(!this.paymentMonthFilter||s.reference===this.paymentMonthFilter)&&(this.paymentStatusFilter==='todos'||s.status===this.paymentStatusFilter)&&(this.paymentProviderFilter==='todos'||Number(s.providerId)===Number(this.paymentProviderFilter))).sort((a,b)=>String(a.dueDate||'9999-12-31').localeCompare(String(b.dueDate||'9999-12-31')))},
  get paymentSummary(){let rows=this.filteredPayments, paid=rows.reduce((a,s)=>a+Number(s.paidValue||0),0), expected=rows.reduce((a,s)=>a+Number(s.amount||0),0), open=rows.reduce((a,s)=>a+this.balance(s),0), paidCount=rows.filter(s=>Number(s.paidValue||0)>0).length, missingReceipt=rows.filter(s=>Number(s.paidValue||0)>0&&this.reportReceiptNumber(s)==='-').length;return{rows,expected,paid,open,paidCount,missingReceipt,overdue:rows.filter(s=>s.status!=='Pago'&&s.dueDate<this.today()).length,today:rows.filter(s=>s.status!=='Pago'&&s.dueDate===this.today()).length}},
  get paymentRecent(){return this.services.filter(s=>Number(s.paidValue||0)>0).sort((a,b)=>String(b.paymentDate||'').localeCompare(String(a.paymentDate||''))).slice(0,8)},
  get servicePanelRows(){return this.services.filter(s=>s.reference===this.servicePanelMonth).sort((a,b)=>String(b.paymentDate||b.serviceDate).localeCompare(String(a.paymentDate||a.serviceDate)))},
  get servicePanelPaidRows(){return this.servicePanelRows.filter(s=>Number(s.paidValue||0)>0).sort((a,b)=>String(b.paymentDate||b.serviceDate).localeCompare(String(a.paymentDate||a.serviceDate)))},
  get servicePanelOpenRows(){return this.servicePanelRows.filter(s=>this.balance(s)>0).sort((a,b)=>String(a.dueDate||'2999-12-31').localeCompare(String(b.dueDate||'2999-12-31')))},
  get servicePanelTotals(){let expected=this.servicePanelRows.reduce((a,s)=>a+Number(s.amount||0),0),paid=this.servicePanelRows.reduce((a,s)=>a+Number(s.paidValue||0),0);return{expected,paid,open:Math.max(0,expected-paid)}},
  get servicePanelRecurringGenerated(){return this.recurrences.filter(r=>r.act

===== paidValue @ 6453489 =====
n(' | '):'nenhuma pendência no ano.'), 'Depois de pagar, registre o pagamento antes de emitir o recibo.']},
    {label:'Maior categoria',value:top?top.category:'-',hint:top?this.money(top.total):'sem dados',icon:'trophy',color:'text-yellow-600',tooltipTitle:'Categoria líder',tooltipMain:top?top.category:'Sem dados',tooltipTag:top?this.money(top.total):'Sem dados',tooltip:['Categoria com maior volume pago no ano.', 'Ranking: '+(catTop.length?catTop.join(' | '):'sem categorias pagas no ano.'), 'Ajuda a identificar onde o condomínio mais gastou.']}
  ]},
  get annualTotals(){let paid=this.yearlyRows.reduce((a,s)=>a+Number(s.paidValue||0),0),open=this.yearlyRows.reduce((a,s)=>a+this.balance(s),0),count=this.yearlyRows.filter(s=>Number(s.paidValue||0)>0).length;return{paid,open,count}},
  get monthlyHistory(){let labels=['Janeiro','Fevereiro','Março','Abril','Maio','Junho','Julho','Agosto','Setembro','Outubro','Novembro','Dezembro'];return labels.map((label,i)=>{let key=String(this.dashboardYear)+'-'+String(i+1).padStart(2,'0'), rows=this.yearlyRows.filter(s=>String(s.paymentDate||s.serviceDate||'').slice(0,7)===key), paid=rows.reduce((a,s)=>a+Number(s.paidValue||0),0), open=rows.reduce((a,s)=>a+this.balance(s),0), map={};rows.forEach(s=>{map[s.category]=(map[s.category]||0)+Number(s.paidValue||s.amount||0)});let top=Object.entries(map).sort((a,b)=>b[1]-a[1])[0];return{key,label,count:rows.filter(s=>Number(s.paidValue||0)>0).length,paid,open,topCategory:top?top[0]:'-'}})},
  get annualTopProviders(){let map={};this.yearlyRows.forEach(s=>{let id=s.providerId,p=this.providers.find(x=>Number(x.id)===Number(id))||{};if(!map[id])map[id]={id,name:p.name||'-',category:p.category||s.category,count:0,total:0,open:0};if(Number(s.paidValue||0)>0)map[id].count++;map[id].total+=Number(s.paidValue||0);map[id].open+=this.balance(s)});return Object.values(map).filter(x=>x.total>0||x.open>0).sort((a,b)=>b.total-a.total).slice(0,10)},
  get annualCategorySummary(){let rows=this.yearlyRows,total=rows.reduce((a,s)=>a+Number(s.paidValue||0),0)||1;return this.categories.map(category=>{let val=rows.filter(s=>s.category===category).reduce((a,s)=>a+Number(s.paidValue||0),0);return{category,total:val,percent:Math.min(100,Math.round(val/total*100))}}).filter(x=>x.total>0).sort((a,b)=>b.total-a.total)},
  get topProviders(){return this.annualTopProviders},
  get categorySummary(){return this.annualCategorySummary},
  get openServices(){return this.services.filter(s=>s.status!=='Pago').sort((a,b)=>String(a.dueDate).localeCompare(String(b.dueDate)))},
  get filteredProviders(){
    let q=(this.q||'').toLowerCase();
    let ps=(this.providerSearch||'').toLowerCase();
    let cat=this.providerCategoryFilter||'';
    let list=this.providers.filter(p=>{
      if(q&&![p.name,p.document,p.category,p.phone,p.whatsapp].join(' ').toLowerCase().includes(q))return false;
      if(ps&&![p.name,p.document,p.category,p.phone,p.whatsapp].join(' ').toLowerCase().includes(ps))return false;
      if(cat&&p.category!==cat)return false;
      return true;
    });
    let field=this.providerSortField||'name';
    let dir=this.providerSortDir||'asc';
    list=[...list].sort((a,b)=>{
      let va=field==='spend'?this.providerSpend(a.id):String(a[field]||'').toLowerCase();
      let vb=field==='spend'?this.providerSpend(b.id):String(b[field]||'').toLowerCase();
      if(va<vb)return dir==='asc'?-1:1;
      if(va>vb)return dir==='asc'?1:-1;
      return 0;
    });
    return list;
  },
  get filteredServices(){return this.services.filter(s=>(this.serviceFilter==='todos'||s.status===this.serviceFilter)&&(this.serviceCategoryFilter==='todas'||s.category===this.serviceCategoryFilter)&&(this.serviceProviderFilter==='todos'||Number(s.providerId)===Number(this.serviceProviderFilter))&&(!this.serviceMonthFilter||s.reference===this.serviceMonthFilter)).sort((a,b)=>String(b.serviceDate).localeCompare(String(a.serviceDate)))},
  get filteredPayments(){return this.services.filter(s=>(!this.paymentMonthFilter||s.reference===this.paymentMonthFilter)&&(this.paymentStatusFilter==='todos'||s.status===this.paymentStatusFilter)&&(this.paymentProviderFilter==='todos'||Number(s.providerId)===Number(this.paymentProviderFilter))).sort((a,b)=>String(a.dueDate||'9999-12-31').localeCompare(String(b.dueDate||'9999-12-31')))},
  get paymentSummary(){let rows=this.filteredPayments, paid=rows.reduce((a,s)=>a+Number(s.paidValue||0),0), expected=rows.reduce((a,s)=>a+Number(s.amount||0),0), open=rows.reduce((a,s)=>a+this.balance(s),0), paidCount=rows.filter(s=>Number(s.paidValue||0)>0).length, missingReceipt=rows.filter(s=>Number(s.paidValue||0)>0&&this.reportReceiptNumber(s)==='-').length;return{rows,expected,paid,open,paidCount,missingReceipt,overdue:rows.filter(s=>s.status!=='Pago'&&s.dueDate<this.today()).length,today:rows.filter(s=>s.status!=='Pago'&&s.dueDate===this.today()).length}},
  get paymentRecent(){return this.services.filter(s=>Number(s.paidValue||0)>0).sort((a,b)=>String(b.paymentDate||'').localeCompare(String(a.paymentDate||''))).slice(0,8)},
  get servicePanelRows(){return this.services.filter(s=>s.reference===this.servicePanelMonth).sort((a,b)=>String(b.paymentDate||b.serviceDate).localeCompare(String(a.paymentDate||a.serviceDate)))},
  get servicePanelPaidRows(){return this.servicePanelRows.filter(s=>Number(s.paidValue||0)>0).sort((a,b)=>String(b.paymentDate||b.serviceDate).localeCompare(String(a.paymentDate||a.serviceDate)))},
  get servicePanelOpenRows(){return this.servicePanelRows.filter(s=>this.balance(s)>0).sort((a,b)=>String(a.dueDate||'2999-12-31').localeCompare(String(b.dueDate||'2999-12-31')))},
  get servicePanelTotals(){let expected=this.servicePanelRows.reduce((a,s)=>a+Number(s.amount||0),0),paid=this.servicePanelRows.reduce((a,s)=>a+Number(s.paidValue||0),0);return{expected,paid,open:Math.max(0,expected-paid)}},
  get servicePanelRecurringGenerated(){return this.recurrences.filter(r=>r.active&&this.recurrenceGenerated(r,this.servicePanelMonth))

===== paidValue @ 6453726 =====
ow-600',tooltipTitle:'Categoria líder',tooltipMain:top?top.category:'Sem dados',tooltipTag:top?this.money(top.total):'Sem dados',tooltip:['Categoria com maior volume pago no ano.', 'Ranking: '+(catTop.length?catTop.join(' | '):'sem categorias pagas no ano.'), 'Ajuda a identificar onde o condomínio mais gastou.']}
  ]},
  get annualTotals(){let paid=this.yearlyRows.reduce((a,s)=>a+Number(s.paidValue||0),0),open=this.yearlyRows.reduce((a,s)=>a+this.balance(s),0),count=this.yearlyRows.filter(s=>Number(s.paidValue||0)>0).length;return{paid,open,count}},
  get monthlyHistory(){let labels=['Janeiro','Fevereiro','Março','Abril','Maio','Junho','Julho','Agosto','Setembro','Outubro','Novembro','Dezembro'];return labels.map((label,i)=>{let key=String(this.dashboardYear)+'-'+String(i+1).padStart(2,'0'), rows=this.yearlyRows.filter(s=>String(s.paymentDate||s.serviceDate||'').slice(0,7)===key), paid=rows.reduce((a,s)=>a+Number(s.paidValue||0),0), open=rows.reduce((a,s)=>a+this.balance(s),0), map={};rows.forEach(s=>{map[s.category]=(map[s.category]||0)+Number(s.paidValue||s.amount||0)});let top=Object.entries(map).sort((a,b)=>b[1]-a[1])[0];return{key,label,count:rows.filter(s=>Number(s.paidValue||0)>0).length,paid,open,topCategory:top?top[0]:'-'}})},
  get annualTopProviders(){let map={};this.yearlyRows.forEach(s=>{let id=s.providerId,p=this.providers.find(x=>Number(x.id)===Number(id))||{};if(!map[id])map[id]={id,name:p.name||'-',category:p.category||s.category,count:0,total:0,open:0};if(Number(s.paidValue||0)>0)map[id].count++;map[id].total+=Number(s.paidValue||0);map[id].open+=this.balance(s)});return Object.values(map).filter(x=>x.total>0||x.open>0).sort((a,b)=>b.total-a.total).slice(0,10)},
  get annualCategorySummary(){let rows=this.yearlyRows,total=rows.reduce((a,s)=>a+Number(s.paidValue||0),0)||1;return this.categories.map(category=>{let val=rows.filter(s=>s.category===category).reduce((a,s)=>a+Number(s.paidValue||0),0);return{category,total:val,percent:Math.min(100,Math.round(val/total*100))}}).filter(x=>x.total>0).sort((a,b)=>b.total-a.total)},
  get topProviders(){return this.annualTopProviders},
  get categorySummary(){return this.annualCategorySummary},
  get openServices(){return this.services.filter(s=>s.status!=='Pago').sort((a,b)=>String(a.dueDate).localeCompare(String(b.dueDate)))},
  get filteredProviders(){
    let q=(this.q||'').toLowerCase();
    let ps=(this.providerSearch||'').toLowerCase();
    let cat=this.providerCategoryFilter||'';
    let list=this.providers.filter(p=>{
      if(q&&![p.name,p.document,p.category,p.phone,p.whatsapp].join(' ').toLowerCase().includes(q))return false;
      if(ps&&![p.name,p.document,p.category,p.phone,p.whatsapp].join(' ').toLowerCase().includes(ps))return false;
      if(cat&&p.category!==cat)return false;
      return true;
    });
    let field=this.providerSortField||'name';
    let dir=this.providerSortDir||'asc';
    list=[...list].sort((a,b)=>{
      let va=field==='spend'?this.providerSpend(a.id):String(a[field]||'').toLowerCase();
      let vb=field==='spend'?this.providerSpend(b.id):String(b[field]||'').toLowerCase();
      if(va<vb)return dir==='asc'?-1:1;
      if(va>vb)return dir==='asc'?1:-1;
      return 0;
    });
    return list;
  },
  get filteredServices(){return this.services.filter(s=>(this.serviceFilter==='todos'||s.status===this.serviceFilter)&&(this.serviceCategoryFilter==='todas'||s.category===this.serviceCategoryFilter)&&(this.serviceProviderFilter==='todos'||Number(s.providerId)===Number(this.serviceProviderFilter))&&(!this.serviceMonthFilter||s.reference===this.serviceMonthFilter)).sort((a,b)=>String(b.serviceDate).localeCompare(String(a.serviceDate)))},
  get filteredPayments(){return this.services.filter(s=>(!this.paymentMonthFilter||s.reference===this.paymentMonthFilter)&&(this.paymentStatusFilter==='todos'||s.status===this.paymentStatusFilter)&&(this.paymentProviderFilter==='todos'||Number(s.providerId)===Number(this.paymentProviderFilter))).sort((a,b)=>String(a.dueDate||'9999-12-31').localeCompare(String(b.dueDate||'9999-12-31')))},
  get paymentSummary(){let rows=this.filteredPayments, paid=rows.reduce((a,s)=>a+Number(s.paidValue||0),0), expected=rows.reduce((a,s)=>a+Number(s.amount||0),0), open=rows.reduce((a,s)=>a+this.balance(s),0), paidCount=rows.filter(s=>Number(s.paidValue||0)>0).length, missingReceipt=rows.filter(s=>Number(s.paidValue||0)>0&&this.reportReceiptNumber(s)==='-').length;return{rows,expected,paid,open,paidCount,missingReceipt,overdue:rows.filter(s=>s.status!=='Pago'&&s.dueDate<this.today()).length,today:rows.filter(s=>s.status!=='Pago'&&s.dueDate===this.today()).length}},
  get paymentRecent(){return this.services.filter(s=>Number(s.paidValue||0)>0).sort((a,b)=>String(b.paymentDate||'').localeCompare(String(a.paymentDate||''))).slice(0,8)},
  get servicePanelRows(){return this.services.filter(s=>s.reference===this.servicePanelMonth).sort((a,b)=>String(b.paymentDate||b.serviceDate).localeCompare(String(a.paymentDate||a.serviceDate)))},
  get servicePanelPaidRows(){return this.servicePanelRows.filter(s=>Number(s.paidValue||0)>0).sort((a,b)=>String(b.paymentDate||b.serviceDate).localeCompare(String(a.paymentDate||a.serviceDate)))},
  get servicePanelOpenRows(){return this.servicePanelRows.filter(s=>this.balance(s)>0).sort((a,b)=>String(a.dueDate||'2999-12-31').localeCompare(String(b.dueDate||'2999-12-31')))},
  get servicePanelTotals(){let expected=this.servicePanelRows.reduce((a,s)=>a+Number(s.amount||0),0),paid=this.servicePanelRows.reduce((a,s)=>a+Number(s.paidValue||0),0);return{expected,paid,open:Math.max(0,expected-paid)}},
  get servicePanelRecurringGenerated(){return this.recurrences.filter(r=>r.active&&this.recurrenceGenerated(r,this.servicePanelMonth))},
  get servicePanelPendingRecurrences(){return this.recurrences.filter(r=>r.active&&!this.recurrenceGenerated(r,this.servicePanelMonth))},
  get servicePanelReceiptCount(){return this.receipts.filter(r=>String(r.paymentDate||r.date||''

===== paidValue @ 6453855 =====
tooltip:['Categoria com maior volume pago no ano.', 'Ranking: '+(catTop.length?catTop.join(' | '):'sem categorias pagas no ano.'), 'Ajuda a identificar onde o condomínio mais gastou.']}
  ]},
  get annualTotals(){let paid=this.yearlyRows.reduce((a,s)=>a+Number(s.paidValue||0),0),open=this.yearlyRows.reduce((a,s)=>a+this.balance(s),0),count=this.yearlyRows.filter(s=>Number(s.paidValue||0)>0).length;return{paid,open,count}},
  get monthlyHistory(){let labels=['Janeiro','Fevereiro','Março','Abril','Maio','Junho','Julho','Agosto','Setembro','Outubro','Novembro','Dezembro'];return labels.map((label,i)=>{let key=String(this.dashboardYear)+'-'+String(i+1).padStart(2,'0'), rows=this.yearlyRows.filter(s=>String(s.paymentDate||s.serviceDate||'').slice(0,7)===key), paid=rows.reduce((a,s)=>a+Number(s.paidValue||0),0), open=rows.reduce((a,s)=>a+this.balance(s),0), map={};rows.forEach(s=>{map[s.category]=(map[s.category]||0)+Number(s.paidValue||s.amount||0)});let top=Object.entries(map).sort((a,b)=>b[1]-a[1])[0];return{key,label,count:rows.filter(s=>Number(s.paidValue||0)>0).length,paid,open,topCategory:top?top[0]:'-'}})},
  get annualTopProviders(){let map={};this.yearlyRows.forEach(s=>{let id=s.providerId,p=this.providers.find(x=>Number(x.id)===Number(id))||{};if(!map[id])map[id]={id,name:p.name||'-',category:p.category||s.category,count:0,total:0,open:0};if(Number(s.paidValue||0)>0)map[id].count++;map[id].total+=Number(s.paidValue||0);map[id].open+=this.balance(s)});return Object.values(map).filter(x=>x.total>0||x.open>0).sort((a,b)=>b.total-a.total).slice(0,10)},
  get annualCategorySummary(){let rows=this.yearlyRows,total=rows.reduce((a,s)=>a+Number(s.paidValue||0),0)||1;return this.categories.map(category=>{let val=rows.filter(s=>s.category===category).reduce((a,s)=>a+Number(s.paidValue||0),0);return{category,total:val,percent:Math.min(100,Math.round(val/total*100))}}).filter(x=>x.total>0).sort((a,b)=>b.total-a.total)},
  get topProviders(){return this.annualTopProviders},
  get categorySummary(){return this.annualCategorySummary},
  get openServices(){return this.services.filter(s=>s.status!=='Pago').sort((a,b)=>String(a.dueDate).localeCompare(String(b.dueDate)))},
  get filteredProviders(){
    let q=(this.q||'').toLowerCase();
    let ps=(this.providerSearch||'').toLowerCase();
    let cat=this.providerCategoryFilter||'';
    let list=this.providers.filter(p=>{
      if(q&&![p.name,p.document,p.category,p.phone,p.whatsapp].join(' ').toLowerCase().includes(q))return false;
      if(ps&&![p.name,p.document,p.category,p.phone,p.whatsapp].join(' ').toLowerCase().includes(ps))return false;
      if(cat&&p.category!==cat)return false;
      return true;
    });
    let field=this.providerSortField||'name';
    let dir=this.providerSortDir||'asc';
    list=[...list].sort((a,b)=>{
      let va=field==='spend'?this.providerSpend(a.id):String(a[field]||'').toLowerCase();
      let vb=field==='spend'?this.providerSpend(b.id):String(b[field]||'').toLowerCase();
      if(va<vb)return dir==='asc'?-1:1;
      if(va>vb)return dir==='asc'?1:-1;
      return 0;
    });
    return list;
  },
  get filteredServices(){return this.services.filter(s=>(this.serviceFilter==='todos'||s.status===this.serviceFilter)&&(this.serviceCategoryFilter==='todas'||s.category===this.serviceCategoryFilter)&&(this.serviceProviderFilter==='todos'||Number(s.providerId)===Number(this.serviceProviderFilter))&&(!this.serviceMonthFilter||s.reference===this.serviceMonthFilter)).sort((a,b)=>String(b.serviceDate).localeCompare(String(a.serviceDate)))},
  get filteredPayments(){return this.services.filter(s=>(!this.paymentMonthFilter||s.reference===this.paymentMonthFilter)&&(this.paymentStatusFilter==='todos'||s.status===this.paymentStatusFilter)&&(this.paymentProviderFilter==='todos'||Number(s.providerId)===Number(this.paymentProviderFilter))).sort((a,b)=>String(a.dueDate||'9999-12-31').localeCompare(String(b.dueDate||'9999-12-31')))},
  get paymentSummary(){let rows=this.filteredPayments, paid=rows.reduce((a,s)=>a+Number(s.paidValue||0),0), expected=rows.reduce((a,s)=>a+Number(s.amount||0),0), open=rows.reduce((a,s)=>a+this.balance(s),0), paidCount=rows.filter(s=>Number(s.paidValue||0)>0).length, missingReceipt=rows.filter(s=>Number(s.paidValue||0)>0&&this.reportReceiptNumber(s)==='-').length;return{rows,expected,paid,open,paidCount,missingReceipt,overdue:rows.filter(s=>s.status!=='Pago'&&s.dueDate<this.today()).length,today:rows.filter(s=>s.status!=='Pago'&&s.dueDate===this.today()).length}},
  get paymentRecent(){return this.services.filter(s=>Number(s.paidValue||0)>0).sort((a,b)=>String(b.paymentDate||'').localeCompare(String(a.paymentDate||''))).slice(0,8)},
  get servicePanelRows(){return this.services.filter(s=>s.reference===this.servicePanelMonth).sort((a,b)=>String(b.paymentDate||b.serviceDate).localeCompare(String(a.paymentDate||a.serviceDate)))},
  get servicePanelPaidRows(){return this.servicePanelRows.filter(s=>Number(s.paidValue||0)>0).sort((a,b)=>String(b.paymentDate||b.serviceDate).localeCompare(String(a.paymentDate||a.serviceDate)))},
  get servicePanelOpenRows(){return this.servicePanelRows.filter(s=>this.balance(s)>0).sort((a,b)=>String(a.dueDate||'2999-12-31').localeCompare(String(b.dueDate||'2999-12-31')))},
  get servicePanelTotals(){let expected=this.servicePanelRows.reduce((a,s)=>a+Number(s.amount||0),0),paid=this.servicePanelRows.reduce((a,s)=>a+Number(s.paidValue||0),0);return{expected,paid,open:Math.max(0,expected-paid)}},
  get servicePanelRecurringGenerated(){return this.recurrences.filter(r=>r.active&&this.recurrenceGenerated(r,this.servicePanelMonth))},
  get servicePanelPendingRecurrences(){return this.recurrences.filter(r=>r.active&&!this.recurrenceGenerated(r,this.servicePanelMonth))},
  get servicePanelReceiptCount(){return this.receipts.filter(r=>String(r.paymentDate||r.date||'').slice(0,7)===this.servicePanelMonth).length},
  get filteredReceipts(){let q=String(this.receiptFilter.q||'').toLowerCase().tri

===== paidValue @ 6456101 =====
.toLowerCase();
    let ps=(this.providerSearch||'').toLowerCase();
    let cat=this.providerCategoryFilter||'';
    let list=this.providers.filter(p=>{
      if(q&&![p.name,p.document,p.category,p.phone,p.whatsapp].join(' ').toLowerCase().includes(q))return false;
      if(ps&&![p.name,p.document,p.category,p.phone,p.whatsapp].join(' ').toLowerCase().includes(ps))return false;
      if(cat&&p.category!==cat)return false;
      return true;
    });
    let field=this.providerSortField||'name';
    let dir=this.providerSortDir||'asc';
    list=[...list].sort((a,b)=>{
      let va=field==='spend'?this.providerSpend(a.id):String(a[field]||'').toLowerCase();
      let vb=field==='spend'?this.providerSpend(b.id):String(b[field]||'').toLowerCase();
      if(va<vb)return dir==='asc'?-1:1;
      if(va>vb)return dir==='asc'?1:-1;
      return 0;
    });
    return list;
  },
  get filteredServices(){return this.services.filter(s=>(this.serviceFilter==='todos'||s.status===this.serviceFilter)&&(this.serviceCategoryFilter==='todas'||s.category===this.serviceCategoryFilter)&&(this.serviceProviderFilter==='todos'||Number(s.providerId)===Number(this.serviceProviderFilter))&&(!this.serviceMonthFilter||s.reference===this.serviceMonthFilter)).sort((a,b)=>String(b.serviceDate).localeCompare(String(a.serviceDate)))},
  get filteredPayments(){return this.services.filter(s=>(!this.paymentMonthFilter||s.reference===this.paymentMonthFilter)&&(this.paymentStatusFilter==='todos'||s.status===this.paymentStatusFilter)&&(this.paymentProviderFilter==='todos'||Number(s.providerId)===Number(this.paymentProviderFilter))).sort((a,b)=>String(a.dueDate||'9999-12-31').localeCompare(String(b.dueDate||'9999-12-31')))},
  get paymentSummary(){let rows=this.filteredPayments, paid=rows.reduce((a,s)=>a+Number(s.paidValue||0),0), expected=rows.reduce((a,s)=>a+Number(s.amount||0),0), open=rows.reduce((a,s)=>a+this.balance(s),0), paidCount=rows.filter(s=>Number(s.paidValue||0)>0).length, missingReceipt=rows.filter(s=>Number(s.paidValue||0)>0&&this.reportReceiptNumber(s)==='-').length;return{rows,expected,paid,open,paidCount,missingReceipt,overdue:rows.filter(s=>s.status!=='Pago'&&s.dueDate<this.today()).length,today:rows.filter(s=>s.status!=='Pago'&&s.dueDate===this.today()).length}},
  get paymentRecent(){return this.services.filter(s=>Number(s.paidValue||0)>0).sort((a,b)=>String(b.paymentDate||'').localeCompare(String(a.paymentDate||''))).slice(0,8)},
  get servicePanelRows(){return this.services.filter(s=>s.reference===this.servicePanelMonth).sort((a,b)=>String(b.paymentDate||b.serviceDate).localeCompare(String(a.paymentDate||a.serviceDate)))},
  get servicePanelPaidRows(){return this.servicePanelRows.filter(s=>Number(s.paidValue||0)>0).sort((a,b)=>String(b.paymentDate||b.serviceDate).localeCompare(String(a.paymentDate||a.serviceDate)))},
  get servicePanelOpenRows(){return this.servicePanelRows.filter(s=>this.balance(s)>0).sort((a,b)=>String(a.dueDate||'2999-12-31').localeCompare(String(b.dueDate||'2999-12-31')))},
  get servicePanelTotals(){let expected=this.servicePanelRows.reduce((a,s)=>a+Number(s.amount||0),0),paid=this.servicePanelRows.reduce((a,s)=>a+Number(s.paidValue||0),0);return{expected,paid,open:Math.max(0,expected-paid)}},
  get servicePanelRecurringGenerated(){return this.recurrences.filter(r=>r.active&&this.recurrenceGenerated(r,this.servicePanelMonth))},
  get servicePanelPendingRecurrences(){return this.recurrences.filter(r=>r.active&&!this.recurrenceGenerated(r,this.servicePanelMonth))},
  get servicePanelReceiptCount(){return this.receipts.filter(r=>String(r.paymentDate||r.date||'').slice(0,7)===this.servicePanelMonth).length},
  get filteredReceipts(){let q=String(this.receiptFilter.q||'').toLowerCase().trim();return this.receipts.filter(r=>{let txt=[r.number,r.providerName,r.category,r.description,r.paymentMethod,r.total].join(' ').toLowerCase();return(!q||txt.includes(q))&&(!this.receiptFilter.month||r.reference===this.receiptFilter.month)&&(this.receiptFilter.category==='todas'||r.category===this.receiptFilter.category)}).sort((a,b)=>String(b.date||'').localeCompare(String(a.date||''))||String(b.number||'').localeCompare(String(a.number||'')))},
  get reportRows(){return this.services.filter(s=>(!this.report.from||s.serviceDate>=this.report.from)&&(!this.report.to||s.serviceDate<=this.report.to)&&(this.report.category==='todas'||s.category===this.report.category)&&(this.report.status==='todos'||s.status===this.report.status)).sort((a,b)=>{let nums=x=>this.receipts.filter(r=>Number(r.serviceId)===Number(x.id)).map(r=>Number(String(r.number||'').replace(/\D/g,''))||0).filter(Boolean),aa=nums(a),bb=nums(b),na=aa.length?Math.min(...aa):0,nb=bb.length?Math.min(...bb):0;if(na&&nb&&na!==nb)return na-nb;if(na&&!nb)return-1;if(!na&&nb)return 1;return String(a.serviceDate||'').localeCompare(String(b.serviceDate||''))||Number(a.id||0)-Number(b.id||0)})},
  get reportDetailRows(){let rows=[];for(const s of this.reportRows){let rs=this.receipts.filter(r=>Number(r.serviceId)===Number(s.id)).sort((a,b)=>(Number(String(a.number||'').replace(/\D/g,''))||0)-(Number(String(b.number||'').replace(/\D/g,''))||0)),sum=0;for(const r of rs){let v=Number(r.total||0);sum+=v;rows.push({...s,reportKey:'r-'+r.id,receiptNumber:r.number||'-',paymentDate:r.paymentDate||r.date||s.paymentDate,description:r.description||s.description,reportValue:v,paymentMethod:r.paymentMethod||s.paymentMethod||''})}let u=Math.max(0,Number(s.paidValue||0)-sum);if(u>0)rows.push({...s,reportKey:'u-'+s.id,receiptNumber:'PENDENTE',paymentDate:s.paymentDate,description:s.description,reportValue:u,paymentMethod:s.paymentMethod||''});if(!rs.length&&u<=0)rows.push({...s,reportKey:'s-'+s.id,receiptNumber:'-',description:s.description,reportValue:Number(s.paidValue||s.amount||0),paymentMethod:s.paymentMethod||''})}return rows},get reportTotals(){let e=this.reportRows.reduce((a,s)=>a+Number(s.amount||0),0),p=this.reportRows.reduce((a,s)=>a+Number(s.paidValue||0),0);ret

===== paidValue @ 6456253 =====

      if(q&&![p.name,p.document,p.category,p.phone,p.whatsapp].join(' ').toLowerCase().includes(q))return false;
      if(ps&&![p.name,p.document,p.category,p.phone,p.whatsapp].join(' ').toLowerCase().includes(ps))return false;
      if(cat&&p.category!==cat)return false;
      return true;
    });
    let field=this.providerSortField||'name';
    let dir=this.providerSortDir||'asc';
    list=[...list].sort((a,b)=>{
      let va=field==='spend'?this.providerSpend(a.id):String(a[field]||'').toLowerCase();
      let vb=field==='spend'?this.providerSpend(b.id):String(b[field]||'').toLowerCase();
      if(va<vb)return dir==='asc'?-1:1;
      if(va>vb)return dir==='asc'?1:-1;
      return 0;
    });
    return list;
  },
  get filteredServices(){return this.services.filter(s=>(this.serviceFilter==='todos'||s.status===this.serviceFilter)&&(this.serviceCategoryFilter==='todas'||s.category===this.serviceCategoryFilter)&&(this.serviceProviderFilter==='todos'||Number(s.providerId)===Number(this.serviceProviderFilter))&&(!this.serviceMonthFilter||s.reference===this.serviceMonthFilter)).sort((a,b)=>String(b.serviceDate).localeCompare(String(a.serviceDate)))},
  get filteredPayments(){return this.services.filter(s=>(!this.paymentMonthFilter||s.reference===this.paymentMonthFilter)&&(this.paymentStatusFilter==='todos'||s.status===this.paymentStatusFilter)&&(this.paymentProviderFilter==='todos'||Number(s.providerId)===Number(this.paymentProviderFilter))).sort((a,b)=>String(a.dueDate||'9999-12-31').localeCompare(String(b.dueDate||'9999-12-31')))},
  get paymentSummary(){let rows=this.filteredPayments, paid=rows.reduce((a,s)=>a+Number(s.paidValue||0),0), expected=rows.reduce((a,s)=>a+Number(s.amount||0),0), open=rows.reduce((a,s)=>a+this.balance(s),0), paidCount=rows.filter(s=>Number(s.paidValue||0)>0).length, missingReceipt=rows.filter(s=>Number(s.paidValue||0)>0&&this.reportReceiptNumber(s)==='-').length;return{rows,expected,paid,open,paidCount,missingReceipt,overdue:rows.filter(s=>s.status!=='Pago'&&s.dueDate<this.today()).length,today:rows.filter(s=>s.status!=='Pago'&&s.dueDate===this.today()).length}},
  get paymentRecent(){return this.services.filter(s=>Number(s.paidValue||0)>0).sort((a,b)=>String(b.paymentDate||'').localeCompare(String(a.paymentDate||''))).slice(0,8)},
  get servicePanelRows(){return this.services.filter(s=>s.reference===this.servicePanelMonth).sort((a,b)=>String(b.paymentDate||b.serviceDate).localeCompare(String(a.paymentDate||a.serviceDate)))},
  get servicePanelPaidRows(){return this.servicePanelRows.filter(s=>Number(s.paidValue||0)>0).sort((a,b)=>String(b.paymentDate||b.serviceDate).localeCompare(String(a.paymentDate||a.serviceDate)))},
  get servicePanelOpenRows(){return this.servicePanelRows.filter(s=>this.balance(s)>0).sort((a,b)=>String(a.dueDate||'2999-12-31').localeCompare(String(b.dueDate||'2999-12-31')))},
  get servicePanelTotals(){let expected=this.servicePanelRows.reduce((a,s)=>a+Number(s.amount||0),0),paid=this.servicePanelRows.reduce((a,s)=>a+Number(s.paidValue||0),0);return{expected,paid,open:Math.max(0,expected-paid)}},
  get servicePanelRecurringGenerated(){return this.recurrences.filter(r=>r.active&&this.recurrenceGenerated(r,this.servicePanelMonth))},
  get servicePanelPendingRecurrences(){return this.recurrences.filter(r=>r.active&&!this.recurrenceGenerated(r,this.servicePanelMonth))},
  get servicePanelReceiptCount(){return this.receipts.filter(r=>String(r.paymentDate||r.date||'').slice(0,7)===this.servicePanelMonth).length},
  get filteredReceipts(){let q=String(this.receiptFilter.q||'').toLowerCase().trim();return this.receipts.filter(r=>{let txt=[r.number,r.providerName,r.category,r.description,r.paymentMethod,r.total].join(' ').toLowerCase();return(!q||txt.includes(q))&&(!this.receiptFilter.month||r.reference===this.receiptFilter.month)&&(this.receiptFilter.category==='todas'||r.category===this.receiptFilter.category)}).sort((a,b)=>String(b.date||'').localeCompare(String(a.date||''))||String(b.number||'').localeCompare(String(a.number||'')))},
  get reportRows(){return this.services.filter(s=>(!this.report.from||s.serviceDate>=this.report.from)&&(!this.report.to||s.serviceDate<=this.report.to)&&(this.report.category==='todas'||s.category===this.report.category)&&(this.report.status==='todos'||s.status===this.report.status)).sort((a,b)=>{let nums=x=>this.receipts.filter(r=>Number(r.serviceId)===Number(x.id)).map(r=>Number(String(r.number||'').replace(/\D/g,''))||0).filter(Boolean),aa=nums(a),bb=nums(b),na=aa.length?Math.min(...aa):0,nb=bb.length?Math.min(...bb):0;if(na&&nb&&na!==nb)return na-nb;if(na&&!nb)return-1;if(!na&&nb)return 1;return String(a.serviceDate||'').localeCompare(String(b.serviceDate||''))||Number(a.id||0)-Number(b.id||0)})},
  get reportDetailRows(){let rows=[];for(const s of this.reportRows){let rs=this.receipts.filter(r=>Number(r.serviceId)===Number(s.id)).sort((a,b)=>(Number(String(a.number||'').replace(/\D/g,''))||0)-(Number(String(b.number||'').replace(/\D/g,''))||0)),sum=0;for(const r of rs){let v=Number(r.total||0);sum+=v;rows.push({...s,reportKey:'r-'+r.id,receiptNumber:r.number||'-',paymentDate:r.paymentDate||r.date||s.paymentDate,description:r.description||s.description,reportValue:v,paymentMethod:r.paymentMethod||s.paymentMethod||''})}let u=Math.max(0,Number(s.paidValue||0)-sum);if(u>0)rows.push({...s,reportKey:'u-'+s.id,receiptNumber:'PENDENTE',paymentDate:s.paymentDate,description:s.description,reportValue:u,paymentMethod:s.paymentMethod||''});if(!rs.length&&u<=0)rows.push({...s,reportKey:'s-'+s.id,receiptNumber:'-',description:s.description,reportValue:Number(s.paidValue||s.amount||0),paymentMethod:s.paymentMethod||''})}return rows},get reportTotals(){let e=this.reportRows.reduce((a,s)=>a+Number(s.amount||0),0),p=this.reportRows.reduce((a,s)=>a+Number(s.paidValue||0),0);return{expected:e,paid:p,open:e-p}},backupStats(){let records=this.providers.length+this.services.length+this.receipts.length+this.recurrences.length;retur

===== paidValue @ 6456317 =====
join(' ').toLowerCase().includes(q))return false;
      if(ps&&![p.name,p.document,p.category,p.phone,p.whatsapp].join(' ').toLowerCase().includes(ps))return false;
      if(cat&&p.category!==cat)return false;
      return true;
    });
    let field=this.providerSortField||'name';
    let dir=this.providerSortDir||'asc';
    list=[...list].sort((a,b)=>{
      let va=field==='spend'?this.providerSpend(a.id):String(a[field]||'').toLowerCase();
      let vb=field==='spend'?this.providerSpend(b.id):String(b[field]||'').toLowerCase();
      if(va<vb)return dir==='asc'?-1:1;
      if(va>vb)return dir==='asc'?1:-1;
      return 0;
    });
    return list;
  },
  get filteredServices(){return this.services.filter(s=>(this.serviceFilter==='todos'||s.status===this.serviceFilter)&&(this.serviceCategoryFilter==='todas'||s.category===this.serviceCategoryFilter)&&(this.serviceProviderFilter==='todos'||Number(s.providerId)===Number(this.serviceProviderFilter))&&(!this.serviceMonthFilter||s.reference===this.serviceMonthFilter)).sort((a,b)=>String(b.serviceDate).localeCompare(String(a.serviceDate)))},
  get filteredPayments(){return this.services.filter(s=>(!this.paymentMonthFilter||s.reference===this.paymentMonthFilter)&&(this.paymentStatusFilter==='todos'||s.status===this.paymentStatusFilter)&&(this.paymentProviderFilter==='todos'||Number(s.providerId)===Number(this.paymentProviderFilter))).sort((a,b)=>String(a.dueDate||'9999-12-31').localeCompare(String(b.dueDate||'9999-12-31')))},
  get paymentSummary(){let rows=this.filteredPayments, paid=rows.reduce((a,s)=>a+Number(s.paidValue||0),0), expected=rows.reduce((a,s)=>a+Number(s.amount||0),0), open=rows.reduce((a,s)=>a+this.balance(s),0), paidCount=rows.filter(s=>Number(s.paidValue||0)>0).length, missingReceipt=rows.filter(s=>Number(s.paidValue||0)>0&&this.reportReceiptNumber(s)==='-').length;return{rows,expected,paid,open,paidCount,missingReceipt,overdue:rows.filter(s=>s.status!=='Pago'&&s.dueDate<this.today()).length,today:rows.filter(s=>s.status!=='Pago'&&s.dueDate===this.today()).length}},
  get paymentRecent(){return this.services.filter(s=>Number(s.paidValue||0)>0).sort((a,b)=>String(b.paymentDate||'').localeCompare(String(a.paymentDate||''))).slice(0,8)},
  get servicePanelRows(){return this.services.filter(s=>s.reference===this.servicePanelMonth).sort((a,b)=>String(b.paymentDate||b.serviceDate).localeCompare(String(a.paymentDate||a.serviceDate)))},
  get servicePanelPaidRows(){return this.servicePanelRows.filter(s=>Number(s.paidValue||0)>0).sort((a,b)=>String(b.paymentDate||b.serviceDate).localeCompare(String(a.paymentDate||a.serviceDate)))},
  get servicePanelOpenRows(){return this.servicePanelRows.filter(s=>this.balance(s)>0).sort((a,b)=>String(a.dueDate||'2999-12-31').localeCompare(String(b.dueDate||'2999-12-31')))},
  get servicePanelTotals(){let expected=this.servicePanelRows.reduce((a,s)=>a+Number(s.amount||0),0),paid=this.servicePanelRows.reduce((a,s)=>a+Number(s.paidValue||0),0);return{expected,paid,open:Math.max(0,expected-paid)}},
  get servicePanelRecurringGenerated(){return this.recurrences.filter(r=>r.active&&this.recurrenceGenerated(r,this.servicePanelMonth))},
  get servicePanelPendingRecurrences(){return this.recurrences.filter(r=>r.active&&!this.recurrenceGenerated(r,this.servicePanelMonth))},
  get servicePanelReceiptCount(){return this.receipts.filter(r=>String(r.paymentDate||r.date||'').slice(0,7)===this.servicePanelMonth).length},
  get filteredReceipts(){let q=String(this.receiptFilter.q||'').toLowerCase().trim();return this.receipts.filter(r=>{let txt=[r.number,r.providerName,r.category,r.description,r.paymentMethod,r.total].join(' ').toLowerCase();return(!q||txt.includes(q))&&(!this.receiptFilter.month||r.reference===this.receiptFilter.month)&&(this.receiptFilter.category==='todas'||r.category===this.receiptFilter.category)}).sort((a,b)=>String(b.date||'').localeCompare(String(a.date||''))||String(b.number||'').localeCompare(String(a.number||'')))},
  get reportRows(){return this.services.filter(s=>(!this.report.from||s.serviceDate>=this.report.from)&&(!this.report.to||s.serviceDate<=this.report.to)&&(this.report.category==='todas'||s.category===this.report.category)&&(this.report.status==='todos'||s.status===this.report.status)).sort((a,b)=>{let nums=x=>this.receipts.filter(r=>Number(r.serviceId)===Number(x.id)).map(r=>Number(String(r.number||'').replace(/\D/g,''))||0).filter(Boolean),aa=nums(a),bb=nums(b),na=aa.length?Math.min(...aa):0,nb=bb.length?Math.min(...bb):0;if(na&&nb&&na!==nb)return na-nb;if(na&&!nb)return-1;if(!na&&nb)return 1;return String(a.serviceDate||'').localeCompare(String(b.serviceDate||''))||Number(a.id||0)-Number(b.id||0)})},
  get reportDetailRows(){let rows=[];for(const s of this.reportRows){let rs=this.receipts.filter(r=>Number(r.serviceId)===Number(s.id)).sort((a,b)=>(Number(String(a.number||'').replace(/\D/g,''))||0)-(Number(String(b.number||'').replace(/\D/g,''))||0)),sum=0;for(const r of rs){let v=Number(r.total||0);sum+=v;rows.push({...s,reportKey:'r-'+r.id,receiptNumber:r.number||'-',paymentDate:r.paymentDate||r.date||s.paymentDate,description:r.description||s.description,reportValue:v,paymentMethod:r.paymentMethod||s.paymentMethod||''})}let u=Math.max(0,Number(s.paidValue||0)-sum);if(u>0)rows.push({...s,reportKey:'u-'+s.id,receiptNumber:'PENDENTE',paymentDate:s.paymentDate,description:s.description,reportValue:u,paymentMethod:s.paymentMethod||''});if(!rs.length&&u<=0)rows.push({...s,reportKey:'s-'+s.id,receiptNumber:'-',description:s.description,reportValue:Number(s.paidValue||s.amount||0),paymentMethod:s.paymentMethod||''})}return rows},get reportTotals(){let e=this.reportRows.reduce((a,s)=>a+Number(s.amount||0),0),p=this.reportRows.reduce((a,s)=>a+Number(s.paidValue||0),0);return{expected:e,paid:p,open:e-p}},backupStats(){let records=this.providers.length+this.services.length+this.receipts.length+this.recurrences.length;return{records,providers:this.providers.length,services:this.services

===== paidValue @ 6456643 =====
  list=[...list].sort((a,b)=>{
      let va=field==='spend'?this.providerSpend(a.id):String(a[field]||'').toLowerCase();
      let vb=field==='spend'?this.providerSpend(b.id):String(b[field]||'').toLowerCase();
      if(va<vb)return dir==='asc'?-1:1;
      if(va>vb)return dir==='asc'?1:-1;
      return 0;
    });
    return list;
  },
  get filteredServices(){return this.services.filter(s=>(this.serviceFilter==='todos'||s.status===this.serviceFilter)&&(this.serviceCategoryFilter==='todas'||s.category===this.serviceCategoryFilter)&&(this.serviceProviderFilter==='todos'||Number(s.providerId)===Number(this.serviceProviderFilter))&&(!this.serviceMonthFilter||s.reference===this.serviceMonthFilter)).sort((a,b)=>String(b.serviceDate).localeCompare(String(a.serviceDate)))},
  get filteredPayments(){return this.services.filter(s=>(!this.paymentMonthFilter||s.reference===this.paymentMonthFilter)&&(this.paymentStatusFilter==='todos'||s.status===this.paymentStatusFilter)&&(this.paymentProviderFilter==='todos'||Number(s.providerId)===Number(this.paymentProviderFilter))).sort((a,b)=>String(a.dueDate||'9999-12-31').localeCompare(String(b.dueDate||'9999-12-31')))},
  get paymentSummary(){let rows=this.filteredPayments, paid=rows.reduce((a,s)=>a+Number(s.paidValue||0),0), expected=rows.reduce((a,s)=>a+Number(s.amount||0),0), open=rows.reduce((a,s)=>a+this.balance(s),0), paidCount=rows.filter(s=>Number(s.paidValue||0)>0).length, missingReceipt=rows.filter(s=>Number(s.paidValue||0)>0&&this.reportReceiptNumber(s)==='-').length;return{rows,expected,paid,open,paidCount,missingReceipt,overdue:rows.filter(s=>s.status!=='Pago'&&s.dueDate<this.today()).length,today:rows.filter(s=>s.status!=='Pago'&&s.dueDate===this.today()).length}},
  get paymentRecent(){return this.services.filter(s=>Number(s.paidValue||0)>0).sort((a,b)=>String(b.paymentDate||'').localeCompare(String(a.paymentDate||''))).slice(0,8)},
  get servicePanelRows(){return this.services.filter(s=>s.reference===this.servicePanelMonth).sort((a,b)=>String(b.paymentDate||b.serviceDate).localeCompare(String(a.paymentDate||a.serviceDate)))},
  get servicePanelPaidRows(){return this.servicePanelRows.filter(s=>Number(s.paidValue||0)>0).sort((a,b)=>String(b.paymentDate||b.serviceDate).localeCompare(String(a.paymentDate||a.serviceDate)))},
  get servicePanelOpenRows(){return this.servicePanelRows.filter(s=>this.balance(s)>0).sort((a,b)=>String(a.dueDate||'2999-12-31').localeCompare(String(b.dueDate||'2999-12-31')))},
  get servicePanelTotals(){let expected=this.servicePanelRows.reduce((a,s)=>a+Number(s.amount||0),0),paid=this.servicePanelRows.reduce((a,s)=>a+Number(s.paidValue||0),0);return{expected,paid,open:Math.max(0,expected-paid)}},
  get servicePanelRecurringGenerated(){return this.recurrences.filter(r=>r.active&&this.recurrenceGenerated(r,this.servicePanelMonth))},
  get servicePanelPendingRecurrences(){return this.recurrences.filter(r=>r.active&&!this.recurrenceGenerated(r,this.servicePanelMonth))},
  get servicePanelReceiptCount(){return this.receipts.filter(r=>String(r.paymentDate||r.date||'').slice(0,7)===this.servicePanelMonth).length},
  get filteredReceipts(){let q=String(this.receiptFilter.q||'').toLowerCase().trim();return this.receipts.filter(r=>{let txt=[r.number,r.providerName,r.category,r.description,r.paymentMethod,r.total].join(' ').toLowerCase();return(!q||txt.includes(q))&&(!this.receiptFilter.month||r.reference===this.receiptFilter.month)&&(this.receiptFilter.category==='todas'||r.category===this.receiptFilter.category)}).sort((a,b)=>String(b.date||'').localeCompare(String(a.date||''))||String(b.number||'').localeCompare(String(a.number||'')))},
  get reportRows(){return this.services.filter(s=>(!this.report.from||s.serviceDate>=this.report.from)&&(!this.report.to||s.serviceDate<=this.report.to)&&(this.report.category==='todas'||s.category===this.report.category)&&(this.report.status==='todos'||s.status===this.report.status)).sort((a,b)=>{let nums=x=>this.receipts.filter(r=>Number(r.serviceId)===Number(x.id)).map(r=>Number(String(r.number||'').replace(/\D/g,''))||0).filter(Boolean),aa=nums(a),bb=nums(b),na=aa.length?Math.min(...aa):0,nb=bb.length?Math.min(...bb):0;if(na&&nb&&na!==nb)return na-nb;if(na&&!nb)return-1;if(!na&&nb)return 1;return String(a.serviceDate||'').localeCompare(String(b.serviceDate||''))||Number(a.id||0)-Number(b.id||0)})},
  get reportDetailRows(){let rows=[];for(const s of this.reportRows){let rs=this.receipts.filter(r=>Number(r.serviceId)===Number(s.id)).sort((a,b)=>(Number(String(a.number||'').replace(/\D/g,''))||0)-(Number(String(b.number||'').replace(/\D/g,''))||0)),sum=0;for(const r of rs){let v=Number(r.total||0);sum+=v;rows.push({...s,reportKey:'r-'+r.id,receiptNumber:r.number||'-',paymentDate:r.paymentDate||r.date||s.paymentDate,description:r.description||s.description,reportValue:v,paymentMethod:r.paymentMethod||s.paymentMethod||''})}let u=Math.max(0,Number(s.paidValue||0)-sum);if(u>0)rows.push({...s,reportKey:'u-'+s.id,receiptNumber:'PENDENTE',paymentDate:s.paymentDate,description:s.description,reportValue:u,paymentMethod:s.paymentMethod||''});if(!rs.length&&u<=0)rows.push({...s,reportKey:'s-'+s.id,receiptNumber:'-',description:s.description,reportValue:Number(s.paidValue||s.amount||0),paymentMethod:s.paymentMethod||''})}return rows},get reportTotals(){let e=this.reportRows.reduce((a,s)=>a+Number(s.amount||0),0),p=this.reportRows.reduce((a,s)=>a+Number(s.paidValue||0),0);return{expected:e,paid:p,open:e-p}},backupStats(){let records=this.providers.length+this.services.length+this.receipts.length+this.recurrences.length;return{records,providers:this.providers.length,services:this.services.length,receipts:this.receipts.length,recurrences:this.recurrences.length}},backupDays(){if(!this.backupLast)return null;let d=new Date(this.backupLast), now=new Date();return Math.max(0,Math.floor((now-d)/86400000))},backupLastLabel(){if(!this.backupLast)return'Nunca feito';let days=this.backupDays();let date=new Date(this.

===== paidValue @ 6457028 =====
lter(s=>(this.serviceFilter==='todos'||s.status===this.serviceFilter)&&(this.serviceCategoryFilter==='todas'||s.category===this.serviceCategoryFilter)&&(this.serviceProviderFilter==='todos'||Number(s.providerId)===Number(this.serviceProviderFilter))&&(!this.serviceMonthFilter||s.reference===this.serviceMonthFilter)).sort((a,b)=>String(b.serviceDate).localeCompare(String(a.serviceDate)))},
  get filteredPayments(){return this.services.filter(s=>(!this.paymentMonthFilter||s.reference===this.paymentMonthFilter)&&(this.paymentStatusFilter==='todos'||s.status===this.paymentStatusFilter)&&(this.paymentProviderFilter==='todos'||Number(s.providerId)===Number(this.paymentProviderFilter))).sort((a,b)=>String(a.dueDate||'9999-12-31').localeCompare(String(b.dueDate||'9999-12-31')))},
  get paymentSummary(){let rows=this.filteredPayments, paid=rows.reduce((a,s)=>a+Number(s.paidValue||0),0), expected=rows.reduce((a,s)=>a+Number(s.amount||0),0), open=rows.reduce((a,s)=>a+this.balance(s),0), paidCount=rows.filter(s=>Number(s.paidValue||0)>0).length, missingReceipt=rows.filter(s=>Number(s.paidValue||0)>0&&this.reportReceiptNumber(s)==='-').length;return{rows,expected,paid,open,paidCount,missingReceipt,overdue:rows.filter(s=>s.status!=='Pago'&&s.dueDate<this.today()).length,today:rows.filter(s=>s.status!=='Pago'&&s.dueDate===this.today()).length}},
  get paymentRecent(){return this.services.filter(s=>Number(s.paidValue||0)>0).sort((a,b)=>String(b.paymentDate||'').localeCompare(String(a.paymentDate||''))).slice(0,8)},
  get servicePanelRows(){return this.services.filter(s=>s.reference===this.servicePanelMonth).sort((a,b)=>String(b.paymentDate||b.serviceDate).localeCompare(String(a.paymentDate||a.serviceDate)))},
  get servicePanelPaidRows(){return this.servicePanelRows.filter(s=>Number(s.paidValue||0)>0).sort((a,b)=>String(b.paymentDate||b.serviceDate).localeCompare(String(a.paymentDate||a.serviceDate)))},
  get servicePanelOpenRows(){return this.servicePanelRows.filter(s=>this.balance(s)>0).sort((a,b)=>String(a.dueDate||'2999-12-31').localeCompare(String(b.dueDate||'2999-12-31')))},
  get servicePanelTotals(){let expected=this.servicePanelRows.reduce((a,s)=>a+Number(s.amount||0),0),paid=this.servicePanelRows.reduce((a,s)=>a+Number(s.paidValue||0),0);return{expected,paid,open:Math.max(0,expected-paid)}},
  get servicePanelRecurringGenerated(){return this.recurrences.filter(r=>r.active&&this.recurrenceGenerated(r,this.servicePanelMonth))},
  get servicePanelPendingRecurrences(){return this.recurrences.filter(r=>r.active&&!this.recurrenceGenerated(r,this.servicePanelMonth))},
  get servicePanelReceiptCount(){return this.receipts.filter(r=>String(r.paymentDate||r.date||'').slice(0,7)===this.servicePanelMonth).length},
  get filteredReceipts(){let q=String(this.receiptFilter.q||'').toLowerCase().trim();return this.receipts.filter(r=>{let txt=[r.number,r.providerName,r.category,r.description,r.paymentMethod,r.total].join(' ').toLowerCase();return(!q||txt.includes(q))&&(!this.receiptFilter.month||r.reference===this.receiptFilter.month)&&(this.receiptFilter.category==='todas'||r.category===this.receiptFilter.category)}).sort((a,b)=>String(b.date||'').localeCompare(String(a.date||''))||String(b.number||'').localeCompare(String(a.number||'')))},
  get reportRows(){return this.services.filter(s=>(!this.report.from||s.serviceDate>=this.report.from)&&(!this.report.to||s.serviceDate<=this.report.to)&&(this.report.category==='todas'||s.category===this.report.category)&&(this.report.status==='todos'||s.status===this.report.status)).sort((a,b)=>{let nums=x=>this.receipts.filter(r=>Number(r.serviceId)===Number(x.id)).map(r=>Number(String(r.number||'').replace(/\D/g,''))||0).filter(Boolean),aa=nums(a),bb=nums(b),na=aa.length?Math.min(...aa):0,nb=bb.length?Math.min(...bb):0;if(na&&nb&&na!==nb)return na-nb;if(na&&!nb)return-1;if(!na&&nb)return 1;return String(a.serviceDate||'').localeCompare(String(b.serviceDate||''))||Number(a.id||0)-Number(b.id||0)})},
  get reportDetailRows(){let rows=[];for(const s of this.reportRows){let rs=this.receipts.filter(r=>Number(r.serviceId)===Number(s.id)).sort((a,b)=>(Number(String(a.number||'').replace(/\D/g,''))||0)-(Number(String(b.number||'').replace(/\D/g,''))||0)),sum=0;for(const r of rs){let v=Number(r.total||0);sum+=v;rows.push({...s,reportKey:'r-'+r.id,receiptNumber:r.number||'-',paymentDate:r.paymentDate||r.date||s.paymentDate,description:r.description||s.description,reportValue:v,paymentMethod:r.paymentMethod||s.paymentMethod||''})}let u=Math.max(0,Number(s.paidValue||0)-sum);if(u>0)rows.push({...s,reportKey:'u-'+s.id,receiptNumber:'PENDENTE',paymentDate:s.paymentDate,description:s.description,reportValue:u,paymentMethod:s.paymentMethod||''});if(!rs.length&&u<=0)rows.push({...s,reportKey:'s-'+s.id,receiptNumber:'-',description:s.description,reportValue:Number(s.paidValue||s.amount||0),paymentMethod:s.paymentMethod||''})}return rows},get reportTotals(){let e=this.reportRows.reduce((a,s)=>a+Number(s.amount||0),0),p=this.reportRows.reduce((a,s)=>a+Number(s.paidValue||0),0);return{expected:e,paid:p,open:e-p}},backupStats(){let records=this.providers.length+this.services.length+this.receipts.length+this.recurrences.length;return{records,providers:this.providers.length,services:this.services.length,receipts:this.receipts.length,recurrences:this.recurrences.length}},backupDays(){if(!this.backupLast)return null;let d=new Date(this.backupLast), now=new Date();return Math.max(0,Math.floor((now-d)/86400000))},backupLastLabel(){if(!this.backupLast)return'Nunca feito';let days=this.backupDays();let date=new Date(this.backupLast).toLocaleString('pt-BR');return days===0?'Hoje • '+date:days+' dia(s) • '+date},backupHealthLabel(){let d=this.backupDays();if(d===null)return'Backup pendente';if(d<=7)return'Protegido';if(d<=15)return'Atenção';return'Backup atrasado'},backupHealthClass(){let d=this.backupDays();if(d===null||d>15)return'bg-red-500/10 text-red-700';if(d>7)return'bg-amber-500/10 text-amber-

===== paidValue @ 6457483 =====
paymentMonthFilter||s.reference===this.paymentMonthFilter)&&(this.paymentStatusFilter==='todos'||s.status===this.paymentStatusFilter)&&(this.paymentProviderFilter==='todos'||Number(s.providerId)===Number(this.paymentProviderFilter))).sort((a,b)=>String(a.dueDate||'9999-12-31').localeCompare(String(b.dueDate||'9999-12-31')))},
  get paymentSummary(){let rows=this.filteredPayments, paid=rows.reduce((a,s)=>a+Number(s.paidValue||0),0), expected=rows.reduce((a,s)=>a+Number(s.amount||0),0), open=rows.reduce((a,s)=>a+this.balance(s),0), paidCount=rows.filter(s=>Number(s.paidValue||0)>0).length, missingReceipt=rows.filter(s=>Number(s.paidValue||0)>0&&this.reportReceiptNumber(s)==='-').length;return{rows,expected,paid,open,paidCount,missingReceipt,overdue:rows.filter(s=>s.status!=='Pago'&&s.dueDate<this.today()).length,today:rows.filter(s=>s.status!=='Pago'&&s.dueDate===this.today()).length}},
  get paymentRecent(){return this.services.filter(s=>Number(s.paidValue||0)>0).sort((a,b)=>String(b.paymentDate||'').localeCompare(String(a.paymentDate||''))).slice(0,8)},
  get servicePanelRows(){return this.services.filter(s=>s.reference===this.servicePanelMonth).sort((a,b)=>String(b.paymentDate||b.serviceDate).localeCompare(String(a.paymentDate||a.serviceDate)))},
  get servicePanelPaidRows(){return this.servicePanelRows.filter(s=>Number(s.paidValue||0)>0).sort((a,b)=>String(b.paymentDate||b.serviceDate).localeCompare(String(a.paymentDate||a.serviceDate)))},
  get servicePanelOpenRows(){return this.servicePanelRows.filter(s=>this.balance(s)>0).sort((a,b)=>String(a.dueDate||'2999-12-31').localeCompare(String(b.dueDate||'2999-12-31')))},
  get servicePanelTotals(){let expected=this.servicePanelRows.reduce((a,s)=>a+Number(s.amount||0),0),paid=this.servicePanelRows.reduce((a,s)=>a+Number(s.paidValue||0),0);return{expected,paid,open:Math.max(0,expected-paid)}},
  get servicePanelRecurringGenerated(){return this.recurrences.filter(r=>r.active&&this.recurrenceGenerated(r,this.servicePanelMonth))},
  get servicePanelPendingRecurrences(){return this.recurrences.filter(r=>r.active&&!this.recurrenceGenerated(r,this.servicePanelMonth))},
  get servicePanelReceiptCount(){return this.receipts.filter(r=>String(r.paymentDate||r.date||'').slice(0,7)===this.servicePanelMonth).length},
  get filteredReceipts(){let q=String(this.receiptFilter.q||'').toLowerCase().trim();return this.receipts.filter(r=>{let txt=[r.number,r.providerName,r.category,r.description,r.paymentMethod,r.total].join(' ').toLowerCase();return(!q||txt.includes(q))&&(!this.receiptFilter.month||r.reference===this.receiptFilter.month)&&(this.receiptFilter.category==='todas'||r.category===this.receiptFilter.category)}).sort((a,b)=>String(b.date||'').localeCompare(String(a.date||''))||String(b.number||'').localeCompare(String(a.number||'')))},
  get reportRows(){return this.services.filter(s=>(!this.report.from||s.serviceDate>=this.report.from)&&(!this.report.to||s.serviceDate<=this.report.to)&&(this.report.category==='todas'||s.category===this.report.category)&&(this.report.status==='todos'||s.status===this.report.status)).sort((a,b)=>{let nums=x=>this.receipts.filter(r=>Number(r.serviceId)===Number(x.id)).map(r=>Number(String(r.number||'').replace(/\D/g,''))||0).filter(Boolean),aa=nums(a),bb=nums(b),na=aa.length?Math.min(...aa):0,nb=bb.length?Math.min(...bb):0;if(na&&nb&&na!==nb)return na-nb;if(na&&!nb)return-1;if(!na&&nb)return 1;return String(a.serviceDate||'').localeCompare(String(b.serviceDate||''))||Number(a.id||0)-Number(b.id||0)})},
  get reportDetailRows(){let rows=[];for(const s of this.reportRows){let rs=this.receipts.filter(r=>Number(r.serviceId)===Number(s.id)).sort((a,b)=>(Number(String(a.number||'').replace(/\D/g,''))||0)-(Number(String(b.number||'').replace(/\D/g,''))||0)),sum=0;for(const r of rs){let v=Number(r.total||0);sum+=v;rows.push({...s,reportKey:'r-'+r.id,receiptNumber:r.number||'-',paymentDate:r.paymentDate||r.date||s.paymentDate,description:r.description||s.description,reportValue:v,paymentMethod:r.paymentMethod||s.paymentMethod||''})}let u=Math.max(0,Number(s.paidValue||0)-sum);if(u>0)rows.push({...s,reportKey:'u-'+s.id,receiptNumber:'PENDENTE',paymentDate:s.paymentDate,description:s.description,reportValue:u,paymentMethod:s.paymentMethod||''});if(!rs.length&&u<=0)rows.push({...s,reportKey:'s-'+s.id,receiptNumber:'-',description:s.description,reportValue:Number(s.paidValue||s.amount||0),paymentMethod:s.paymentMethod||''})}return rows},get reportTotals(){let e=this.reportRows.reduce((a,s)=>a+Number(s.amount||0),0),p=this.reportRows.reduce((a,s)=>a+Number(s.paidValue||0),0);return{expected:e,paid:p,open:e-p}},backupStats(){let records=this.providers.length+this.services.length+this.receipts.length+this.recurrences.length;return{records,providers:this.providers.length,services:this.services.length,receipts:this.receipts.length,recurrences:this.recurrences.length}},backupDays(){if(!this.backupLast)return null;let d=new Date(this.backupLast), now=new Date();return Math.max(0,Math.floor((now-d)/86400000))},backupLastLabel(){if(!this.backupLast)return'Nunca feito';let days=this.backupDays();let date=new Date(this.backupLast).toLocaleString('pt-BR');return days===0?'Hoje • '+date:days+' dia(s) • '+date},backupHealthLabel(){let d=this.backupDays();if(d===null)return'Backup pendente';if(d<=7)return'Protegido';if(d<=15)return'Atenção';return'Backup atrasado'},backupHealthClass(){let d=this.backupDays();if(d===null||d>15)return'bg-red-500/10 text-red-700';if(d>7)return'bg-amber-500/10 text-amber-700';return'bg-emerald-500/10 text-emerald-700'},backupHint(){let s=this.backupStats();let d=this.backupDays();if(d===null)return'Você ainda não exportou um backup. Recomendo gerar um JSON antes de continuar cadastrando dados importantes.';if(d>15)return'Seu último backup já passou de 15 dias. Exporte um novo JSON para proteger '+s.records+' registros locais.';if(d>7)return'Backup existente, mas já vale gerar uma cópia atualizada com os últimos lançam

===== paidValue @ 6459775 =====
 get filteredReceipts(){let q=String(this.receiptFilter.q||'').toLowerCase().trim();return this.receipts.filter(r=>{let txt=[r.number,r.providerName,r.category,r.description,r.paymentMethod,r.total].join(' ').toLowerCase();return(!q||txt.includes(q))&&(!this.receiptFilter.month||r.reference===this.receiptFilter.month)&&(this.receiptFilter.category==='todas'||r.category===this.receiptFilter.category)}).sort((a,b)=>String(b.date||'').localeCompare(String(a.date||''))||String(b.number||'').localeCompare(String(a.number||'')))},
  get reportRows(){return this.services.filter(s=>(!this.report.from||s.serviceDate>=this.report.from)&&(!this.report.to||s.serviceDate<=this.report.to)&&(this.report.category==='todas'||s.category===this.report.category)&&(this.report.status==='todos'||s.status===this.report.status)).sort((a,b)=>{let nums=x=>this.receipts.filter(r=>Number(r.serviceId)===Number(x.id)).map(r=>Number(String(r.number||'').replace(/\D/g,''))||0).filter(Boolean),aa=nums(a),bb=nums(b),na=aa.length?Math.min(...aa):0,nb=bb.length?Math.min(...bb):0;if(na&&nb&&na!==nb)return na-nb;if(na&&!nb)return-1;if(!na&&nb)return 1;return String(a.serviceDate||'').localeCompare(String(b.serviceDate||''))||Number(a.id||0)-Number(b.id||0)})},
  get reportDetailRows(){let rows=[];for(const s of this.reportRows){let rs=this.receipts.filter(r=>Number(r.serviceId)===Number(s.id)).sort((a,b)=>(Number(String(a.number||'').replace(/\D/g,''))||0)-(Number(String(b.number||'').replace(/\D/g,''))||0)),sum=0;for(const r of rs){let v=Number(r.total||0);sum+=v;rows.push({...s,reportKey:'r-'+r.id,receiptNumber:r.number||'-',paymentDate:r.paymentDate||r.date||s.paymentDate,description:r.description||s.description,reportValue:v,paymentMethod:r.paymentMethod||s.paymentMethod||''})}let u=Math.max(0,Number(s.paidValue||0)-sum);if(u>0)rows.push({...s,reportKey:'u-'+s.id,receiptNumber:'PENDENTE',paymentDate:s.paymentDate,description:s.description,reportValue:u,paymentMethod:s.paymentMethod||''});if(!rs.length&&u<=0)rows.push({...s,reportKey:'s-'+s.id,receiptNumber:'-',description:s.description,reportValue:Number(s.paidValue||s.amount||0),paymentMethod:s.paymentMethod||''})}return rows},get reportTotals(){let e=this.reportRows.reduce((a,s)=>a+Number(s.amount||0),0),p=this.reportRows.reduce((a,s)=>a+Number(s.paidValue||0),0);return{expected:e,paid:p,open:e-p}},backupStats(){let records=this.providers.length+this.services.length+this.receipts.length+this.recurrences.length;return{records,providers:this.providers.length,services:this.services.length,receipts:this.receipts.length,recurrences:this.recurrences.length}},backupDays(){if(!this.backupLast)return null;let d=new Date(this.backupLast), now=new Date();return Math.max(0,Math.floor((now-d)/86400000))},backupLastLabel(){if(!this.backupLast)return'Nunca feito';let days=this.backupDays();let date=new Date(this.backupLast).toLocaleString('pt-BR');return days===0?'Hoje • '+date:days+' dia(s) • '+date},backupHealthLabel(){let d=this.backupDays();if(d===null)return'Backup pendente';if(d<=7)return'Protegido';if(d<=15)return'Atenção';return'Backup atrasado'},backupHealthClass(){let d=this.backupDays();if(d===null||d>15)return'bg-red-500/10 text-red-700';if(d>7)return'bg-amber-500/10 text-amber-700';return'bg-emerald-500/10 text-emerald-700'},backupHint(){let s=this.backupStats();let d=this.backupDays();if(d===null)return'Você ainda não exportou um backup. Recomendo gerar um JSON antes de continuar cadastrando dados importantes.';if(d>15)return'Seu último backup já passou de 15 dias. Exporte um novo JSON para proteger '+s.records+' registros locais.';if(d>7)return'Backup existente, mas já vale gerar uma cópia atualizada com os últimos lançamentos e recibos.';return'Backup em dia. Mesmo assim, exporte uma nova cópia sempre antes de grandes alterações.'},
  get topProviders(){let y=new Date().getFullYear(), map={};this.services.filter(s=>String(s.serviceDate||'').startsWith(String(y))).forEach(s=>{let id=s.providerId,p=this.providers.find(x=>Number(x.id)===Number(id))||{};if(!map[id])map[id]={id,name:p.name||'-',category:p.category||s.category,count:0,total:0,open:0};map[id].count++;map[id].total+=Number(s.paidValue||s.amount||0);map[id].open+=this.balance(s)});return Object.values(map).sort((a,b)=>b.total-a.total).slice(0,8)},
  get categorySummary(){let total=this.services.reduce((a,s)=>a+Number(s.paidValue||s.amount||0),0)||1;return this.categories.map(category=>{let val=this.services.filter(s=>s.category===category).reduce((a,s)=>a+Number(s.paidValue||s.amount||0),0);return{category,total:val,percent:Math.min(100,Math.round(val/total*100))}}).filter(x=>x.total>0).sort((a,b)=>b.total-a.total)},
  get criticalOpen(){return this.openServices.slice().sort((a,b)=>new Date(a.dueDate||'2999-12-31')-new Date(b.dueDate||'2999-12-31')).slice(0,8)},
  get notifications(){const now=this.today();let pend=this.services.filter(s=>s.status!=='Pago'&&s.dueDate<=now).slice(0,3).map(s=>({id:s.id,title:'Pagamento pendente',body:this.providerName(s.providerId)+' • '+s.category+' • '+this.money(this.balance(s))}));let rec=this.recurrences.filter(r=>r.active&&!this.recurrenceGenerated(r)).slice(0,2).map(r=>({id:'r'+r.id,title:'Recorrência do mês pendente',body:this.providerName(r.providerId)+' • '+r.category+' • '+this.money(r.amount)}));return[...rec,...pend].slice(0,4)},
  get dayOverdue(){let t=this.today();return this.services.filter(s=>s.status!=='Pago'&&s.dueDate&&s.dueDate<t).sort((a,b)=>String(a.dueDate).localeCompare(String(b.dueDate)))},
  get dayDueToday(){let t=this.today();return this.services.filter(s=>s.status!=='Pago'&&s.dueDate===t).sort((a,b)=>this.balance(b)-this.balance(a))},
  get dayNextDue(){let t=new Date(this.today()+'T00:00:00'),limit=new Date(t);limit.setDate(t.getDate()+7);let a=this.today(),b=limit.toISOString().slice(0,10);return this.services.filter(s=>s.status!=='Pago'&&s.dueDate&&s.dueDate>a&&s.dueDate<=b).sort((x,y)=>String(x.dueDate).localeCompare(String(y.dueDate)))},
  get

===== paidValue @ 6460085 =====
er.month)&&(this.receiptFilter.category==='todas'||r.category===this.receiptFilter.category)}).sort((a,b)=>String(b.date||'').localeCompare(String(a.date||''))||String(b.number||'').localeCompare(String(a.number||'')))},
  get reportRows(){return this.services.filter(s=>(!this.report.from||s.serviceDate>=this.report.from)&&(!this.report.to||s.serviceDate<=this.report.to)&&(this.report.category==='todas'||s.category===this.report.category)&&(this.report.status==='todos'||s.status===this.report.status)).sort((a,b)=>{let nums=x=>this.receipts.filter(r=>Number(r.serviceId)===Number(x.id)).map(r=>Number(String(r.number||'').replace(/\D/g,''))||0).filter(Boolean),aa=nums(a),bb=nums(b),na=aa.length?Math.min(...aa):0,nb=bb.length?Math.min(...bb):0;if(na&&nb&&na!==nb)return na-nb;if(na&&!nb)return-1;if(!na&&nb)return 1;return String(a.serviceDate||'').localeCompare(String(b.serviceDate||''))||Number(a.id||0)-Number(b.id||0)})},
  get reportDetailRows(){let rows=[];for(const s of this.reportRows){let rs=this.receipts.filter(r=>Number(r.serviceId)===Number(s.id)).sort((a,b)=>(Number(String(a.number||'').replace(/\D/g,''))||0)-(Number(String(b.number||'').replace(/\D/g,''))||0)),sum=0;for(const r of rs){let v=Number(r.total||0);sum+=v;rows.push({...s,reportKey:'r-'+r.id,receiptNumber:r.number||'-',paymentDate:r.paymentDate||r.date||s.paymentDate,description:r.description||s.description,reportValue:v,paymentMethod:r.paymentMethod||s.paymentMethod||''})}let u=Math.max(0,Number(s.paidValue||0)-sum);if(u>0)rows.push({...s,reportKey:'u-'+s.id,receiptNumber:'PENDENTE',paymentDate:s.paymentDate,description:s.description,reportValue:u,paymentMethod:s.paymentMethod||''});if(!rs.length&&u<=0)rows.push({...s,reportKey:'s-'+s.id,receiptNumber:'-',description:s.description,reportValue:Number(s.paidValue||s.amount||0),paymentMethod:s.paymentMethod||''})}return rows},get reportTotals(){let e=this.reportRows.reduce((a,s)=>a+Number(s.amount||0),0),p=this.reportRows.reduce((a,s)=>a+Number(s.paidValue||0),0);return{expected:e,paid:p,open:e-p}},backupStats(){let records=this.providers.length+this.services.length+this.receipts.length+this.recurrences.length;return{records,providers:this.providers.length,services:this.services.length,receipts:this.receipts.length,recurrences:this.recurrences.length}},backupDays(){if(!this.backupLast)return null;let d=new Date(this.backupLast), now=new Date();return Math.max(0,Math.floor((now-d)/86400000))},backupLastLabel(){if(!this.backupLast)return'Nunca feito';let days=this.backupDays();let date=new Date(this.backupLast).toLocaleString('pt-BR');return days===0?'Hoje • '+date:days+' dia(s) • '+date},backupHealthLabel(){let d=this.backupDays();if(d===null)return'Backup pendente';if(d<=7)return'Protegido';if(d<=15)return'Atenção';return'Backup atrasado'},backupHealthClass(){let d=this.backupDays();if(d===null||d>15)return'bg-red-500/10 text-red-700';if(d>7)return'bg-amber-500/10 text-amber-700';return'bg-emerald-500/10 text-emerald-700'},backupHint(){let s=this.backupStats();let d=this.backupDays();if(d===null)return'Você ainda não exportou um backup. Recomendo gerar um JSON antes de continuar cadastrando dados importantes.';if(d>15)return'Seu último backup já passou de 15 dias. Exporte um novo JSON para proteger '+s.records+' registros locais.';if(d>7)return'Backup existente, mas já vale gerar uma cópia atualizada com os últimos lançamentos e recibos.';return'Backup em dia. Mesmo assim, exporte uma nova cópia sempre antes de grandes alterações.'},
  get topProviders(){let y=new Date().getFullYear(), map={};this.services.filter(s=>String(s.serviceDate||'').startsWith(String(y))).forEach(s=>{let id=s.providerId,p=this.providers.find(x=>Number(x.id)===Number(id))||{};if(!map[id])map[id]={id,name:p.name||'-',category:p.category||s.category,count:0,total:0,open:0};map[id].count++;map[id].total+=Number(s.paidValue||s.amount||0);map[id].open+=this.balance(s)});return Object.values(map).sort((a,b)=>b.total-a.total).slice(0,8)},
  get categorySummary(){let total=this.services.reduce((a,s)=>a+Number(s.paidValue||s.amount||0),0)||1;return this.categories.map(category=>{let val=this.services.filter(s=>s.category===category).reduce((a,s)=>a+Number(s.paidValue||s.amount||0),0);return{category,total:val,percent:Math.min(100,Math.round(val/total*100))}}).filter(x=>x.total>0).sort((a,b)=>b.total-a.total)},
  get criticalOpen(){return this.openServices.slice().sort((a,b)=>new Date(a.dueDate||'2999-12-31')-new Date(b.dueDate||'2999-12-31')).slice(0,8)},
  get notifications(){const now=this.today();let pend=this.services.filter(s=>s.status!=='Pago'&&s.dueDate<=now).slice(0,3).map(s=>({id:s.id,title:'Pagamento pendente',body:this.providerName(s.providerId)+' • '+s.category+' • '+this.money(this.balance(s))}));let rec=this.recurrences.filter(r=>r.active&&!this.recurrenceGenerated(r)).slice(0,2).map(r=>({id:'r'+r.id,title:'Recorrência do mês pendente',body:this.providerName(r.providerId)+' • '+r.category+' • '+this.money(r.amount)}));return[...rec,...pend].slice(0,4)},
  get dayOverdue(){let t=this.today();return this.services.filter(s=>s.status!=='Pago'&&s.dueDate&&s.dueDate<t).sort((a,b)=>String(a.dueDate).localeCompare(String(b.dueDate)))},
  get dayDueToday(){let t=this.today();return this.services.filter(s=>s.status!=='Pago'&&s.dueDate===t).sort((a,b)=>this.balance(b)-this.balance(a))},
  get dayNextDue(){let t=new Date(this.today()+'T00:00:00'),limit=new Date(t);limit.setDate(t.getDate()+7);let a=this.today(),b=limit.toISOString().slice(0,10);return this.services.filter(s=>s.status!=='Pago'&&s.dueDate&&s.dueDate>a&&s.dueDate<=b).sort((x,y)=>String(x.dueDate).localeCompare(String(y.dueDate)))},
  get dayPendingRecurrences(){return this.recurrences.filter(r=>r.active&&!this.recurrenceGenerated(r))},
  get dayCenterRows(){let rows=[];this.dayPendingRecurrences.forEach(r=>rows.push({key:'r'+r.id,kind:'recurrence',recurrence:r,title:this.providerName(r.providerId),subtitle:'Recorrência pendente • '+r.categor

===== paidValue @ 6460281 =====
String(a.number||'')))},
  get reportRows(){return this.services.filter(s=>(!this.report.from||s.serviceDate>=this.report.from)&&(!this.report.to||s.serviceDate<=this.report.to)&&(this.report.category==='todas'||s.category===this.report.category)&&(this.report.status==='todos'||s.status===this.report.status)).sort((a,b)=>{let nums=x=>this.receipts.filter(r=>Number(r.serviceId)===Number(x.id)).map(r=>Number(String(r.number||'').replace(/\D/g,''))||0).filter(Boolean),aa=nums(a),bb=nums(b),na=aa.length?Math.min(...aa):0,nb=bb.length?Math.min(...bb):0;if(na&&nb&&na!==nb)return na-nb;if(na&&!nb)return-1;if(!na&&nb)return 1;return String(a.serviceDate||'').localeCompare(String(b.serviceDate||''))||Number(a.id||0)-Number(b.id||0)})},
  get reportDetailRows(){let rows=[];for(const s of this.reportRows){let rs=this.receipts.filter(r=>Number(r.serviceId)===Number(s.id)).sort((a,b)=>(Number(String(a.number||'').replace(/\D/g,''))||0)-(Number(String(b.number||'').replace(/\D/g,''))||0)),sum=0;for(const r of rs){let v=Number(r.total||0);sum+=v;rows.push({...s,reportKey:'r-'+r.id,receiptNumber:r.number||'-',paymentDate:r.paymentDate||r.date||s.paymentDate,description:r.description||s.description,reportValue:v,paymentMethod:r.paymentMethod||s.paymentMethod||''})}let u=Math.max(0,Number(s.paidValue||0)-sum);if(u>0)rows.push({...s,reportKey:'u-'+s.id,receiptNumber:'PENDENTE',paymentDate:s.paymentDate,description:s.description,reportValue:u,paymentMethod:s.paymentMethod||''});if(!rs.length&&u<=0)rows.push({...s,reportKey:'s-'+s.id,receiptNumber:'-',description:s.description,reportValue:Number(s.paidValue||s.amount||0),paymentMethod:s.paymentMethod||''})}return rows},get reportTotals(){let e=this.reportRows.reduce((a,s)=>a+Number(s.amount||0),0),p=this.reportRows.reduce((a,s)=>a+Number(s.paidValue||0),0);return{expected:e,paid:p,open:e-p}},backupStats(){let records=this.providers.length+this.services.length+this.receipts.length+this.recurrences.length;return{records,providers:this.providers.length,services:this.services.length,receipts:this.receipts.length,recurrences:this.recurrences.length}},backupDays(){if(!this.backupLast)return null;let d=new Date(this.backupLast), now=new Date();return Math.max(0,Math.floor((now-d)/86400000))},backupLastLabel(){if(!this.backupLast)return'Nunca feito';let days=this.backupDays();let date=new Date(this.backupLast).toLocaleString('pt-BR');return days===0?'Hoje • '+date:days+' dia(s) • '+date},backupHealthLabel(){let d=this.backupDays();if(d===null)return'Backup pendente';if(d<=7)return'Protegido';if(d<=15)return'Atenção';return'Backup atrasado'},backupHealthClass(){let d=this.backupDays();if(d===null||d>15)return'bg-red-500/10 text-red-700';if(d>7)return'bg-amber-500/10 text-amber-700';return'bg-emerald-500/10 text-emerald-700'},backupHint(){let s=this.backupStats();let d=this.backupDays();if(d===null)return'Você ainda não exportou um backup. Recomendo gerar um JSON antes de continuar cadastrando dados importantes.';if(d>15)return'Seu último backup já passou de 15 dias. Exporte um novo JSON para proteger '+s.records+' registros locais.';if(d>7)return'Backup existente, mas já vale gerar uma cópia atualizada com os últimos lançamentos e recibos.';return'Backup em dia. Mesmo assim, exporte uma nova cópia sempre antes de grandes alterações.'},
  get topProviders(){let y=new Date().getFullYear(), map={};this.services.filter(s=>String(s.serviceDate||'').startsWith(String(y))).forEach(s=>{let id=s.providerId,p=this.providers.find(x=>Number(x.id)===Number(id))||{};if(!map[id])map[id]={id,name:p.name||'-',category:p.category||s.category,count:0,total:0,open:0};map[id].count++;map[id].total+=Number(s.paidValue||s.amount||0);map[id].open+=this.balance(s)});return Object.values(map).sort((a,b)=>b.total-a.total).slice(0,8)},
  get categorySummary(){let total=this.services.reduce((a,s)=>a+Number(s.paidValue||s.amount||0),0)||1;return this.categories.map(category=>{let val=this.services.filter(s=>s.category===category).reduce((a,s)=>a+Number(s.paidValue||s.amount||0),0);return{category,total:val,percent:Math.min(100,Math.round(val/total*100))}}).filter(x=>x.total>0).sort((a,b)=>b.total-a.total)},
  get criticalOpen(){return this.openServices.slice().sort((a,b)=>new Date(a.dueDate||'2999-12-31')-new Date(b.dueDate||'2999-12-31')).slice(0,8)},
  get notifications(){const now=this.today();let pend=this.services.filter(s=>s.status!=='Pago'&&s.dueDate<=now).slice(0,3).map(s=>({id:s.id,title:'Pagamento pendente',body:this.providerName(s.providerId)+' • '+s.category+' • '+this.money(this.balance(s))}));let rec=this.recurrences.filter(r=>r.active&&!this.recurrenceGenerated(r)).slice(0,2).map(r=>({id:'r'+r.id,title:'Recorrência do mês pendente',body:this.providerName(r.providerId)+' • '+r.category+' • '+this.money(r.amount)}));return[...rec,...pend].slice(0,4)},
  get dayOverdue(){let t=this.today();return this.services.filter(s=>s.status!=='Pago'&&s.dueDate&&s.dueDate<t).sort((a,b)=>String(a.dueDate).localeCompare(String(b.dueDate)))},
  get dayDueToday(){let t=this.today();return this.services.filter(s=>s.status!=='Pago'&&s.dueDate===t).sort((a,b)=>this.balance(b)-this.balance(a))},
  get dayNextDue(){let t=new Date(this.today()+'T00:00:00'),limit=new Date(t);limit.setDate(t.getDate()+7);let a=this.today(),b=limit.toISOString().slice(0,10);return this.services.filter(s=>s.status!=='Pago'&&s.dueDate&&s.dueDate>a&&s.dueDate<=b).sort((x,y)=>String(x.dueDate).localeCompare(String(y.dueDate)))},
  get dayPendingRecurrences(){return this.recurrences.filter(r=>r.active&&!this.recurrenceGenerated(r))},
  get dayCenterRows(){let rows=[];this.dayPendingRecurrences.forEach(r=>rows.push({key:'r'+r.id,kind:'recurrence',recurrence:r,title:this.providerName(r.providerId),subtitle:'Recorrência pendente • '+r.category+' • '+this.month(this.today().slice(0,7)),badge:'Gerar mês',value:this.money(r.amount||0),tone:'success'}));this.dayOverdue.forEach(s=>rows.push({key:'v'+s.id,kind:'service',service:s,title:this

===== paidValue @ 6462156 =====
rds=this.providers.length+this.services.length+this.receipts.length+this.recurrences.length;return{records,providers:this.providers.length,services:this.services.length,receipts:this.receipts.length,recurrences:this.recurrences.length}},backupDays(){if(!this.backupLast)return null;let d=new Date(this.backupLast), now=new Date();return Math.max(0,Math.floor((now-d)/86400000))},backupLastLabel(){if(!this.backupLast)return'Nunca feito';let days=this.backupDays();let date=new Date(this.backupLast).toLocaleString('pt-BR');return days===0?'Hoje • '+date:days+' dia(s) • '+date},backupHealthLabel(){let d=this.backupDays();if(d===null)return'Backup pendente';if(d<=7)return'Protegido';if(d<=15)return'Atenção';return'Backup atrasado'},backupHealthClass(){let d=this.backupDays();if(d===null||d>15)return'bg-red-500/10 text-red-700';if(d>7)return'bg-amber-500/10 text-amber-700';return'bg-emerald-500/10 text-emerald-700'},backupHint(){let s=this.backupStats();let d=this.backupDays();if(d===null)return'Você ainda não exportou um backup. Recomendo gerar um JSON antes de continuar cadastrando dados importantes.';if(d>15)return'Seu último backup já passou de 15 dias. Exporte um novo JSON para proteger '+s.records+' registros locais.';if(d>7)return'Backup existente, mas já vale gerar uma cópia atualizada com os últimos lançamentos e recibos.';return'Backup em dia. Mesmo assim, exporte uma nova cópia sempre antes de grandes alterações.'},
  get topProviders(){let y=new Date().getFullYear(), map={};this.services.filter(s=>String(s.serviceDate||'').startsWith(String(y))).forEach(s=>{let id=s.providerId,p=this.providers.find(x=>Number(x.id)===Number(id))||{};if(!map[id])map[id]={id,name:p.name||'-',category:p.category||s.category,count:0,total:0,open:0};map[id].count++;map[id].total+=Number(s.paidValue||s.amount||0);map[id].open+=this.balance(s)});return Object.values(map).sort((a,b)=>b.total-a.total).slice(0,8)},
  get categorySummary(){let total=this.services.reduce((a,s)=>a+Number(s.paidValue||s.amount||0),0)||1;return this.categories.map(category=>{let val=this.services.filter(s=>s.category===category).reduce((a,s)=>a+Number(s.paidValue||s.amount||0),0);return{category,total:val,percent:Math.min(100,Math.round(val/total*100))}}).filter(x=>x.total>0).sort((a,b)=>b.total-a.total)},
  get criticalOpen(){return this.openServices.slice().sort((a,b)=>new Date(a.dueDate||'2999-12-31')-new Date(b.dueDate||'2999-12-31')).slice(0,8)},
  get notifications(){const now=this.today();let pend=this.services.filter(s=>s.status!=='Pago'&&s.dueDate<=now).slice(0,3).map(s=>({id:s.id,title:'Pagamento pendente',body:this.providerName(s.providerId)+' • '+s.category+' • '+this.money(this.balance(s))}));let rec=this.recurrences.filter(r=>r.active&&!this.recurrenceGenerated(r)).slice(0,2).map(r=>({id:'r'+r.id,title:'Recorrência do mês pendente',body:this.providerName(r.providerId)+' • '+r.category+' • '+this.money(r.amount)}));return[...rec,...pend].slice(0,4)},
  get dayOverdue(){let t=this.today();return this.services.filter(s=>s.status!=='Pago'&&s.dueDate&&s.dueDate<t).sort((a,b)=>String(a.dueDate).localeCompare(String(b.dueDate)))},
  get dayDueToday(){let t=this.today();return this.services.filter(s=>s.status!=='Pago'&&s.dueDate===t).sort((a,b)=>this.balance(b)-this.balance(a))},
  get dayNextDue(){let t=new Date(this.today()+'T00:00:00'),limit=new Date(t);limit.setDate(t.getDate()+7);let a=this.today(),b=limit.toISOString().slice(0,10);return this.services.filter(s=>s.status!=='Pago'&&s.dueDate&&s.dueDate>a&&s.dueDate<=b).sort((x,y)=>String(x.dueDate).localeCompare(String(y.dueDate)))},
  get dayPendingRecurrences(){return this.recurrences.filter(r=>r.active&&!this.recurrenceGenerated(r))},
  get dayCenterRows(){let rows=[];this.dayPendingRecurrences.forEach(r=>rows.push({key:'r'+r.id,kind:'recurrence',recurrence:r,title:this.providerName(r.providerId),subtitle:'Recorrência pendente • '+r.category+' • '+this.month(this.today().slice(0,7)),badge:'Gerar mês',value:this.money(r.amount||0),tone:'success'}));this.dayOverdue.forEach(s=>rows.push({key:'v'+s.id,kind:'service',service:s,title:this.providerName(s.providerId),subtitle:s.category+' • venceu '+this.fmt(s.dueDate)+' • '+this.overdueLabel(s),badge:'Vencido',value:this.money(this.balance(s)),tone:'danger'}));this.dayDueToday.forEach(s=>rows.push({key:'h'+s.id,kind:'service',service:s,title:this.providerName(s.providerId),subtitle:s.category+' • vence hoje',badge:'Hoje',value:this.money(this.balance(s)),tone:'warning'}));this.dayNextDue.forEach(s=>rows.push({key:'p'+s.id,kind:'service',service:s,title:this.providerName(s.providerId),subtitle:s.category+' • vence '+this.fmt(s.dueDate)+' • '+this.overdueLabel(s),badge:'Próximo',value:this.money(this.balance(s)),tone:'success'}));return rows},
  get dayCenterSummary(){let v=this.dayOverdue.length,h=this.dayDueToday.length,p=this.dayNextDue.length,r=this.dayPendingRecurrences.length,total=this.dayOverdue.concat(this.dayDueToday,this.dayNextDue).reduce((a,s)=>a+this.balance(s),0);if(!v&&!h&&!p&&!r)return 'Nenhuma ação urgente no momento. Sem vencimentos críticos ou recorrências pendentes para o mês.';return `${v} vencido(s), ${h} vencendo hoje, ${p} próximo(s) e ${r} recorrência(s) a gerar. Valor monitorado: ${this.money(total)}.`},
  get searchResults(){let q=this.q.toLowerCase().trim();if(!q)return[];let match=(arr)=>arr.join(' ').toLowerCase().includes(q);let a=this.providers.filter(p=>match([p.name,p.document,p.category,p.phone,p.whatsapp,p.pix,p.type,p.notes])).map(p=>({kind:'provider',type:'Prestador',id:p.id,data:p,title:p.name,subtitle:(p.category||'-')+' • '+(p.document||'sem documento'),amount:this.money(this.providerSpend(p.id)),date:'em aberto '+this.money(this.providerOpen(p.id)),description:'Contato: '+(p.phone||p.whatsapp||'não informado')+' • PIX/Banco: '+(p.pix||'não informado')}));let b=this.services.filter(s=>match([this.providerName(s.providerId),s.category,s.description,s.location,s.status,s.paymentMethod,s.ref

===== paidValue @ 6462353 =====
h,recurrences:this.recurrences.length}},backupDays(){if(!this.backupLast)return null;let d=new Date(this.backupLast), now=new Date();return Math.max(0,Math.floor((now-d)/86400000))},backupLastLabel(){if(!this.backupLast)return'Nunca feito';let days=this.backupDays();let date=new Date(this.backupLast).toLocaleString('pt-BR');return days===0?'Hoje • '+date:days+' dia(s) • '+date},backupHealthLabel(){let d=this.backupDays();if(d===null)return'Backup pendente';if(d<=7)return'Protegido';if(d<=15)return'Atenção';return'Backup atrasado'},backupHealthClass(){let d=this.backupDays();if(d===null||d>15)return'bg-red-500/10 text-red-700';if(d>7)return'bg-amber-500/10 text-amber-700';return'bg-emerald-500/10 text-emerald-700'},backupHint(){let s=this.backupStats();let d=this.backupDays();if(d===null)return'Você ainda não exportou um backup. Recomendo gerar um JSON antes de continuar cadastrando dados importantes.';if(d>15)return'Seu último backup já passou de 15 dias. Exporte um novo JSON para proteger '+s.records+' registros locais.';if(d>7)return'Backup existente, mas já vale gerar uma cópia atualizada com os últimos lançamentos e recibos.';return'Backup em dia. Mesmo assim, exporte uma nova cópia sempre antes de grandes alterações.'},
  get topProviders(){let y=new Date().getFullYear(), map={};this.services.filter(s=>String(s.serviceDate||'').startsWith(String(y))).forEach(s=>{let id=s.providerId,p=this.providers.find(x=>Number(x.id)===Number(id))||{};if(!map[id])map[id]={id,name:p.name||'-',category:p.category||s.category,count:0,total:0,open:0};map[id].count++;map[id].total+=Number(s.paidValue||s.amount||0);map[id].open+=this.balance(s)});return Object.values(map).sort((a,b)=>b.total-a.total).slice(0,8)},
  get categorySummary(){let total=this.services.reduce((a,s)=>a+Number(s.paidValue||s.amount||0),0)||1;return this.categories.map(category=>{let val=this.services.filter(s=>s.category===category).reduce((a,s)=>a+Number(s.paidValue||s.amount||0),0);return{category,total:val,percent:Math.min(100,Math.round(val/total*100))}}).filter(x=>x.total>0).sort((a,b)=>b.total-a.total)},
  get criticalOpen(){return this.openServices.slice().sort((a,b)=>new Date(a.dueDate||'2999-12-31')-new Date(b.dueDate||'2999-12-31')).slice(0,8)},
  get notifications(){const now=this.today();let pend=this.services.filter(s=>s.status!=='Pago'&&s.dueDate<=now).slice(0,3).map(s=>({id:s.id,title:'Pagamento pendente',body:this.providerName(s.providerId)+' • '+s.category+' • '+this.money(this.balance(s))}));let rec=this.recurrences.filter(r=>r.active&&!this.recurrenceGenerated(r)).slice(0,2).map(r=>({id:'r'+r.id,title:'Recorrência do mês pendente',body:this.providerName(r.providerId)+' • '+r.category+' • '+this.money(r.amount)}));return[...rec,...pend].slice(0,4)},
  get dayOverdue(){let t=this.today();return this.services.filter(s=>s.status!=='Pago'&&s.dueDate&&s.dueDate<t).sort((a,b)=>String(a.dueDate).localeCompare(String(b.dueDate)))},
  get dayDueToday(){let t=this.today();return this.services.filter(s=>s.status!=='Pago'&&s.dueDate===t).sort((a,b)=>this.balance(b)-this.balance(a))},
  get dayNextDue(){let t=new Date(this.today()+'T00:00:00'),limit=new Date(t);limit.setDate(t.getDate()+7);let a=this.today(),b=limit.toISOString().slice(0,10);return this.services.filter(s=>s.status!=='Pago'&&s.dueDate&&s.dueDate>a&&s.dueDate<=b).sort((x,y)=>String(x.dueDate).localeCompare(String(y.dueDate)))},
  get dayPendingRecurrences(){return this.recurrences.filter(r=>r.active&&!this.recurrenceGenerated(r))},
  get dayCenterRows(){let rows=[];this.dayPendingRecurrences.forEach(r=>rows.push({key:'r'+r.id,kind:'recurrence',recurrence:r,title:this.providerName(r.providerId),subtitle:'Recorrência pendente • '+r.category+' • '+this.month(this.today().slice(0,7)),badge:'Gerar mês',value:this.money(r.amount||0),tone:'success'}));this.dayOverdue.forEach(s=>rows.push({key:'v'+s.id,kind:'service',service:s,title:this.providerName(s.providerId),subtitle:s.category+' • venceu '+this.fmt(s.dueDate)+' • '+this.overdueLabel(s),badge:'Vencido',value:this.money(this.balance(s)),tone:'danger'}));this.dayDueToday.forEach(s=>rows.push({key:'h'+s.id,kind:'service',service:s,title:this.providerName(s.providerId),subtitle:s.category+' • vence hoje',badge:'Hoje',value:this.money(this.balance(s)),tone:'warning'}));this.dayNextDue.forEach(s=>rows.push({key:'p'+s.id,kind:'service',service:s,title:this.providerName(s.providerId),subtitle:s.category+' • vence '+this.fmt(s.dueDate)+' • '+this.overdueLabel(s),badge:'Próximo',value:this.money(this.balance(s)),tone:'success'}));return rows},
  get dayCenterSummary(){let v=this.dayOverdue.length,h=this.dayDueToday.length,p=this.dayNextDue.length,r=this.dayPendingRecurrences.length,total=this.dayOverdue.concat(this.dayDueToday,this.dayNextDue).reduce((a,s)=>a+this.balance(s),0);if(!v&&!h&&!p&&!r)return 'Nenhuma ação urgente no momento. Sem vencimentos críticos ou recorrências pendentes para o mês.';return `${v} vencido(s), ${h} vencendo hoje, ${p} próximo(s) e ${r} recorrência(s) a gerar. Valor monitorado: ${this.money(total)}.`},
  get searchResults(){let q=this.q.toLowerCase().trim();if(!q)return[];let match=(arr)=>arr.join(' ').toLowerCase().includes(q);let a=this.providers.filter(p=>match([p.name,p.document,p.category,p.phone,p.whatsapp,p.pix,p.type,p.notes])).map(p=>({kind:'provider',type:'Prestador',id:p.id,data:p,title:p.name,subtitle:(p.category||'-')+' • '+(p.document||'sem documento'),amount:this.money(this.providerSpend(p.id)),date:'em aberto '+this.money(this.providerOpen(p.id)),description:'Contato: '+(p.phone||p.whatsapp||'não informado')+' • PIX/Banco: '+(p.pix||'não informado')}));let b=this.services.filter(s=>match([this.providerName(s.providerId),s.category,s.description,s.location,s.status,s.paymentMethod,s.reference,this.month(s.reference),this.fmt(s.serviceDate),this.fmt(s.paymentDate)])).map(s=>({kind:'service',type:'Serviço',id:s.id,data:s,title:this.providerName(s.providerId),subtitle:(s.category||'

===== paidValue @ 6462501 =====
(0,Math.floor((now-d)/86400000))},backupLastLabel(){if(!this.backupLast)return'Nunca feito';let days=this.backupDays();let date=new Date(this.backupLast).toLocaleString('pt-BR');return days===0?'Hoje • '+date:days+' dia(s) • '+date},backupHealthLabel(){let d=this.backupDays();if(d===null)return'Backup pendente';if(d<=7)return'Protegido';if(d<=15)return'Atenção';return'Backup atrasado'},backupHealthClass(){let d=this.backupDays();if(d===null||d>15)return'bg-red-500/10 text-red-700';if(d>7)return'bg-amber-500/10 text-amber-700';return'bg-emerald-500/10 text-emerald-700'},backupHint(){let s=this.backupStats();let d=this.backupDays();if(d===null)return'Você ainda não exportou um backup. Recomendo gerar um JSON antes de continuar cadastrando dados importantes.';if(d>15)return'Seu último backup já passou de 15 dias. Exporte um novo JSON para proteger '+s.records+' registros locais.';if(d>7)return'Backup existente, mas já vale gerar uma cópia atualizada com os últimos lançamentos e recibos.';return'Backup em dia. Mesmo assim, exporte uma nova cópia sempre antes de grandes alterações.'},
  get topProviders(){let y=new Date().getFullYear(), map={};this.services.filter(s=>String(s.serviceDate||'').startsWith(String(y))).forEach(s=>{let id=s.providerId,p=this.providers.find(x=>Number(x.id)===Number(id))||{};if(!map[id])map[id]={id,name:p.name||'-',category:p.category||s.category,count:0,total:0,open:0};map[id].count++;map[id].total+=Number(s.paidValue||s.amount||0);map[id].open+=this.balance(s)});return Object.values(map).sort((a,b)=>b.total-a.total).slice(0,8)},
  get categorySummary(){let total=this.services.reduce((a,s)=>a+Number(s.paidValue||s.amount||0),0)||1;return this.categories.map(category=>{let val=this.services.filter(s=>s.category===category).reduce((a,s)=>a+Number(s.paidValue||s.amount||0),0);return{category,total:val,percent:Math.min(100,Math.round(val/total*100))}}).filter(x=>x.total>0).sort((a,b)=>b.total-a.total)},
  get criticalOpen(){return this.openServices.slice().sort((a,b)=>new Date(a.dueDate||'2999-12-31')-new Date(b.dueDate||'2999-12-31')).slice(0,8)},
  get notifications(){const now=this.today();let pend=this.services.filter(s=>s.status!=='Pago'&&s.dueDate<=now).slice(0,3).map(s=>({id:s.id,title:'Pagamento pendente',body:this.providerName(s.providerId)+' • '+s.category+' • '+this.money(this.balance(s))}));let rec=this.recurrences.filter(r=>r.active&&!this.recurrenceGenerated(r)).slice(0,2).map(r=>({id:'r'+r.id,title:'Recorrência do mês pendente',body:this.providerName(r.providerId)+' • '+r.category+' • '+this.money(r.amount)}));return[...rec,...pend].slice(0,4)},
  get dayOverdue(){let t=this.today();return this.services.filter(s=>s.status!=='Pago'&&s.dueDate&&s.dueDate<t).sort((a,b)=>String(a.dueDate).localeCompare(String(b.dueDate)))},
  get dayDueToday(){let t=this.today();return this.services.filter(s=>s.status!=='Pago'&&s.dueDate===t).sort((a,b)=>this.balance(b)-this.balance(a))},
  get dayNextDue(){let t=new Date(this.today()+'T00:00:00'),limit=new Date(t);limit.setDate(t.getDate()+7);let a=this.today(),b=limit.toISOString().slice(0,10);return this.services.filter(s=>s.status!=='Pago'&&s.dueDate&&s.dueDate>a&&s.dueDate<=b).sort((x,y)=>String(x.dueDate).localeCompare(String(y.dueDate)))},
  get dayPendingRecurrences(){return this.recurrences.filter(r=>r.active&&!this.recurrenceGenerated(r))},
  get dayCenterRows(){let rows=[];this.dayPendingRecurrences.forEach(r=>rows.push({key:'r'+r.id,kind:'recurrence',recurrence:r,title:this.providerName(r.providerId),subtitle:'Recorrência pendente • '+r.category+' • '+this.month(this.today().slice(0,7)),badge:'Gerar mês',value:this.money(r.amount||0),tone:'success'}));this.dayOverdue.forEach(s=>rows.push({key:'v'+s.id,kind:'service',service:s,title:this.providerName(s.providerId),subtitle:s.category+' • venceu '+this.fmt(s.dueDate)+' • '+this.overdueLabel(s),badge:'Vencido',value:this.money(this.balance(s)),tone:'danger'}));this.dayDueToday.forEach(s=>rows.push({key:'h'+s.id,kind:'service',service:s,title:this.providerName(s.providerId),subtitle:s.category+' • vence hoje',badge:'Hoje',value:this.money(this.balance(s)),tone:'warning'}));this.dayNextDue.forEach(s=>rows.push({key:'p'+s.id,kind:'service',service:s,title:this.providerName(s.providerId),subtitle:s.category+' • vence '+this.fmt(s.dueDate)+' • '+this.overdueLabel(s),badge:'Próximo',value:this.money(this.balance(s)),tone:'success'}));return rows},
  get dayCenterSummary(){let v=this.dayOverdue.length,h=this.dayDueToday.length,p=this.dayNextDue.length,r=this.dayPendingRecurrences.length,total=this.dayOverdue.concat(this.dayDueToday,this.dayNextDue).reduce((a,s)=>a+this.balance(s),0);if(!v&&!h&&!p&&!r)return 'Nenhuma ação urgente no momento. Sem vencimentos críticos ou recorrências pendentes para o mês.';return `${v} vencido(s), ${h} vencendo hoje, ${p} próximo(s) e ${r} recorrência(s) a gerar. Valor monitorado: ${this.money(total)}.`},
  get searchResults(){let q=this.q.toLowerCase().trim();if(!q)return[];let match=(arr)=>arr.join(' ').toLowerCase().includes(q);let a=this.providers.filter(p=>match([p.name,p.document,p.category,p.phone,p.whatsapp,p.pix,p.type,p.notes])).map(p=>({kind:'provider',type:'Prestador',id:p.id,data:p,title:p.name,subtitle:(p.category||'-')+' • '+(p.document||'sem documento'),amount:this.money(this.providerSpend(p.id)),date:'em aberto '+this.money(this.providerOpen(p.id)),description:'Contato: '+(p.phone||p.whatsapp||'não informado')+' • PIX/Banco: '+(p.pix||'não informado')}));let b=this.services.filter(s=>match([this.providerName(s.providerId),s.category,s.description,s.location,s.status,s.paymentMethod,s.reference,this.month(s.reference),this.fmt(s.serviceDate),this.fmt(s.paymentDate)])).map(s=>({kind:'service',type:'Serviço',id:s.id,data:s,title:this.providerName(s.providerId),subtitle:(s.category||'-')+' • '+this.month(s.reference)+' • '+(s.status||'-'),amount:this.money(s.paidValue||s.amount),date:s.paymentDate?this.fmt(s.paymentDate):this.fmt

===== paidValue @ 6466629 =====
s.money(this.balance(s)),tone:'warning'}));this.dayNextDue.forEach(s=>rows.push({key:'p'+s.id,kind:'service',service:s,title:this.providerName(s.providerId),subtitle:s.category+' • vence '+this.fmt(s.dueDate)+' • '+this.overdueLabel(s),badge:'Próximo',value:this.money(this.balance(s)),tone:'success'}));return rows},
  get dayCenterSummary(){let v=this.dayOverdue.length,h=this.dayDueToday.length,p=this.dayNextDue.length,r=this.dayPendingRecurrences.length,total=this.dayOverdue.concat(this.dayDueToday,this.dayNextDue).reduce((a,s)=>a+this.balance(s),0);if(!v&&!h&&!p&&!r)return 'Nenhuma ação urgente no momento. Sem vencimentos críticos ou recorrências pendentes para o mês.';return `${v} vencido(s), ${h} vencendo hoje, ${p} próximo(s) e ${r} recorrência(s) a gerar. Valor monitorado: ${this.money(total)}.`},
  get searchResults(){let q=this.q.toLowerCase().trim();if(!q)return[];let match=(arr)=>arr.join(' ').toLowerCase().includes(q);let a=this.providers.filter(p=>match([p.name,p.document,p.category,p.phone,p.whatsapp,p.pix,p.type,p.notes])).map(p=>({kind:'provider',type:'Prestador',id:p.id,data:p,title:p.name,subtitle:(p.category||'-')+' • '+(p.document||'sem documento'),amount:this.money(this.providerSpend(p.id)),date:'em aberto '+this.money(this.providerOpen(p.id)),description:'Contato: '+(p.phone||p.whatsapp||'não informado')+' • PIX/Banco: '+(p.pix||'não informado')}));let b=this.services.filter(s=>match([this.providerName(s.providerId),s.category,s.description,s.location,s.status,s.paymentMethod,s.reference,this.month(s.reference),this.fmt(s.serviceDate),this.fmt(s.paymentDate)])).map(s=>({kind:'service',type:'Serviço',id:s.id,data:s,title:this.providerName(s.providerId),subtitle:(s.category||'-')+' • '+this.month(s.reference)+' • '+(s.status||'-'),amount:this.money(s.paidValue||s.amount),date:s.paymentDate?this.fmt(s.paymentDate):this.fmt(s.serviceDate),description:(s.description||'Sem descrição')+' • '+(s.location||'sem local informado')}));let c=this.receipts.filter(r=>match([r.number,r.providerName,r.category,r.description,r.paymentMethod,r.reference,this.month(r.reference),this.fmt(r.date),this.fmt(r.paymentDate),r.total])).map(r=>({kind:'receipt',type:'Recibo',id:r.id,data:r,title:'Recibo nº '+(r.number||'-'),subtitle:(r.providerName||'-')+' • '+(r.category||'-'),amount:this.money(r.total||0),date:this.fmt(r.paymentDate||r.date),description:(r.description||'Sem descrição')+' • Forma: '+(r.paymentMethod||'não informada')}));let d=(this.recurrences||[]).filter(r=>match([this.providerName(r.providerId),r.category,r.description,r.location,r.paymentMethod,r.amount,r.active?'ativa':'inativa'])).map(r=>({kind:'recurrence',type:'Recorrência',id:r.id,data:r,title:this.providerName(r.providerId),subtitle:(r.category||'-')+' • '+(r.active?'ativa':'inativa'),amount:this.money(r.amount||0),date:'todo mês dia '+(r.dueDay||'-'),description:(r.description||'Serviço recorrente mensal')+' • Forma padrão: '+(r.paymentMethod||'não informada')}));return[...c,...a,...b,...d].slice(0,40)},
  toastType(msg){let s=String(msg||'').toLowerCase();if(s.includes('inválida')||s.includes('não consegui')||s.includes('erro')||s.includes('falha'))return'error';if(s.includes('já ')||s.includes('inativa')||s.includes('atenção'))return'warning';return'success'},toast(message,type='success'){let cfg={success:{title:'Tudo certo',icon:'check-circle-2',bar:'bg-emerald-600',border:'border-emerald-500/25',iconBg:'bg-emerald-600'},error:{title:'Atenção',icon:'triangle-alert',bar:'bg-red-600',border:'border-red-500/25',iconBg:'bg-red-600'},warning:{title:'Aviso',icon:'alert-circle',bar:'bg-amber-500',border:'border-amber-500/30',iconBg:'bg-amber-500'},info:{title:'Informação',icon:'info',bar:'bg-rc-800',border:'border-rc-800/25',iconBg:'bg-gradient-to-br from-rc-800 to-rc-500'}}[type]||{};let id=Date.now()+Math.random();this.toasts.push({id,message,type,...cfg});this.refreshIcons();setTimeout(()=>this.dismissToast(id),4200)},dismissToast(id){this.toasts=this.toasts.filter(t=>t.id!==id)},doLogin(){let p=JSON.parse(localStorage.getItem('rc_passwords')||'{"admin":"123456","viewer":"123456"}');if(this.login.password===p[this.login.role]){this.auth={logged:true,role:this.login.role};this.login.password='';this.load();this.after()}else this.toast('Senha inválida','error')},logout(){this.auth={logged:false,role:'viewer'}},go(v){this.view=v;this.after()},toggleDark(){this.dark=!this.dark;localStorage.setItem('rc_dark',this.dark?'1':'0');document.documentElement.classList.toggle('dark',this.dark)},
  providerAction(action,p){if(!action||!p)return;if(action==='painel')this.selectProvider(p);else if(action==='editar')this.editProvider(p);else if(action==='lancar'){if(!this.isViewer)this.fillServiceFromProvider(p)}else if(action==='whatsapp')this.openWhatsapp(p);else if(action==='excluir'){if(!this.isViewer)this.openDeleteProviderModal(p)}},serviceAction(action,s){if(!action||!s)return;if(action==='pagar'){if(!this.isViewer)this.pay(s)}else if(action==='recibo'){if(!this.isViewer&&Number(s.paidValue||0)>0)this.quickReceipt(s)}else if(action==='duplicar'){if(!this.isViewer)this.openDuplicateModal(s)}else if(action==='excluir'){if(!this.isViewer)this.openDeleteServiceModal(s)}},paymentAction(action,s){if(!action||!s)return;if(action==='pagar'){if(!this.isViewer&&s.status!=='Pago')this.pay(s)}else if(action==='recibo'){if(!this.isViewer&&Number(s.paidValue||0)>0)this.quickReceipt(s)}else if(action==='servico')this.openSearchService(s);else if(action==='excluirPagamento'){if(!this.isViewer&&Number(s.paidValue||0)>0)this.openDeletePaymentModal(s)}},openSearchProvider(p){this.selectedProviderId=p?.id||null;this.view='prestadores';this.after()},openSearchService(s){this.serviceFilter='todos';this.serviceCategoryFilter='todas';this.serviceProviderFilter=String(s.providerId||'todos');this.serviceMonthFilter=s.reference||'';this.view='servicos';this.after()},openSearchReceipt(r){this.currentReceipt=r;this.receiptFilter={q:r.number||'',month:r.refe

===== paidValue @ 6469863 =====
||s.includes('atenção'))return'warning';return'success'},toast(message,type='success'){let cfg={success:{title:'Tudo certo',icon:'check-circle-2',bar:'bg-emerald-600',border:'border-emerald-500/25',iconBg:'bg-emerald-600'},error:{title:'Atenção',icon:'triangle-alert',bar:'bg-red-600',border:'border-red-500/25',iconBg:'bg-red-600'},warning:{title:'Aviso',icon:'alert-circle',bar:'bg-amber-500',border:'border-amber-500/30',iconBg:'bg-amber-500'},info:{title:'Informação',icon:'info',bar:'bg-rc-800',border:'border-rc-800/25',iconBg:'bg-gradient-to-br from-rc-800 to-rc-500'}}[type]||{};let id=Date.now()+Math.random();this.toasts.push({id,message,type,...cfg});this.refreshIcons();setTimeout(()=>this.dismissToast(id),4200)},dismissToast(id){this.toasts=this.toasts.filter(t=>t.id!==id)},doLogin(){let p=JSON.parse(localStorage.getItem('rc_passwords')||'{"admin":"123456","viewer":"123456"}');if(this.login.password===p[this.login.role]){this.auth={logged:true,role:this.login.role};this.login.password='';this.load();this.after()}else this.toast('Senha inválida','error')},logout(){this.auth={logged:false,role:'viewer'}},go(v){this.view=v;this.after()},toggleDark(){this.dark=!this.dark;localStorage.setItem('rc_dark',this.dark?'1':'0');document.documentElement.classList.toggle('dark',this.dark)},
  providerAction(action,p){if(!action||!p)return;if(action==='painel')this.selectProvider(p);else if(action==='editar')this.editProvider(p);else if(action==='lancar'){if(!this.isViewer)this.fillServiceFromProvider(p)}else if(action==='whatsapp')this.openWhatsapp(p);else if(action==='excluir'){if(!this.isViewer)this.openDeleteProviderModal(p)}},serviceAction(action,s){if(!action||!s)return;if(action==='pagar'){if(!this.isViewer)this.pay(s)}else if(action==='recibo'){if(!this.isViewer&&Number(s.paidValue||0)>0)this.quickReceipt(s)}else if(action==='duplicar'){if(!this.isViewer)this.openDuplicateModal(s)}else if(action==='excluir'){if(!this.isViewer)this.openDeleteServiceModal(s)}},paymentAction(action,s){if(!action||!s)return;if(action==='pagar'){if(!this.isViewer&&s.status!=='Pago')this.pay(s)}else if(action==='recibo'){if(!this.isViewer&&Number(s.paidValue||0)>0)this.quickReceipt(s)}else if(action==='servico')this.openSearchService(s);else if(action==='excluirPagamento'){if(!this.isViewer&&Number(s.paidValue||0)>0)this.openDeletePaymentModal(s)}},openSearchProvider(p){this.selectedProviderId=p?.id||null;this.view='prestadores';this.after()},openSearchService(s){this.serviceFilter='todos';this.serviceCategoryFilter='todas';this.serviceProviderFilter=String(s.providerId||'todos');this.serviceMonthFilter=s.reference||'';this.view='servicos';this.after()},openSearchReceipt(r){this.currentReceipt=r;this.receiptFilter={q:r.number||'',month:r.reference||'',category:'todas'};this.view='recibos';this.after()},openSearchRecurrence(r){this.recurrenceForm={...r};this.view='servicos';this.after()},selectProvider(p){this.selectedProviderId=p?.id||null;this.after()},providerSortBy(field){if(this.providerSortField===field){this.providerSortDir=this.providerSortDir==='asc'?'desc':'asc';}else{this.providerSortField=field;this.providerSortDir='asc';}},resetProvider(){this.providerForm={id:null,name:'',type:'Autônomo',document:'',category:'Jardineiro',phone:'',whatsapp:'',pix:'',notes:''}},editProvider(p){this.providerForm={...p};this.view='prestadores';this.after()},async saveProvider(){if(this.isViewer)return;let d={...this.providerForm};if(d.id){await db.providers.update(d.id,d);await this.logAction('provider','Prestador atualizado',d.name||'Cadastro atualizado')}else{delete d.id;await db.providers.add(d);await this.logAction('provider','Prestador cadastrado',d.name||'Novo prestador')}this.resetProvider();await this.load();this.toast('Prestador salvo')},openDeleteProviderModal(p){if(this.isViewer)return;this.actionModal={open:true,type:'deleteProvider',tone:'danger',icon:'user-x',target:p,service:null,title:'Excluir prestador',subtitle:'Essa ação remove o cadastro do prestador.',note:'Prestadores com lançamentos, recibos ou recorrências vinculadas não podem ser excluídos. Isso preserva a rastreabilidade do histórico.',confirmLabel:'Excluir prestador',details:[{label:'Prestador',value:p.name||'-'},{label:'Categoria',value:p.category||'-'},{label:'Documento',value:p.document||'não informado'}]};this.after()},async removeProviderInternal(id){let p=this.providers.find(x=>Number(x.id)===Number(id));if(!p)return;let linkedServices=this.services.some(s=>Number(s.providerId)===Number(id)),linkedReceipts=this.receipts.some(r=>Number(r.providerId)===Number(id)),linkedRecurrences=(this.recurrences||[]).some(r=>Number(r.providerId)===Number(id));if(linkedServices||linkedReceipts||linkedRecurrences){return this.toast('Este prestador possui histórico ou recorrência vinculada e não pode ser excluído. Mantenha o cadastro para preservar a rastreabilidade.','warning')}await db.providers.delete(id);await this.logAction('delete','Prestador excluído',p.name||('ID '+id));await this.load()},
  resetRecurrence(){this.recurrenceForm={id:null,providerId:'',category:'Jardineiro',amount:0,dueDay:5,paymentDay:5,paymentMethod:'PIX',location:'',description:'',active:true}},editRecurrence(r){this.recurrenceForm={...r};this.view='servicos';this.after()},async saveRecurrence(){if(this.isViewer)return;let d={...this.recurrenceForm,providerId:Number(this.recurrenceForm.providerId),amount:Number(this.recurrenceForm.amount||0),dueDay:Number(this.recurrenceForm.dueDay||1),paymentDay:Number(this.recurrenceForm.paymentDay||this.recurrenceForm.dueDay||1),active:!!this.recurrenceForm.active};if(d.id){await db.recurrences.update(d.id,d);await this.logAction('recurrence','Recorrência atualizada',this.providerName(d.providerId)+' • '+this.money(d.amount||0))}else{delete d.id;await db.recurrences.add(d);await this.logAction('recurrence','Recorrência criada',this.providerName(d.providerId)+' • '+this.money(d.amount||0))}this.resetRecurrence();await this.load();this.toast('Re

===== paidValue @ 6470224 =====
'alert-circle',bar:'bg-amber-500',border:'border-amber-500/30',iconBg:'bg-amber-500'},info:{title:'Informação',icon:'info',bar:'bg-rc-800',border:'border-rc-800/25',iconBg:'bg-gradient-to-br from-rc-800 to-rc-500'}}[type]||{};let id=Date.now()+Math.random();this.toasts.push({id,message,type,...cfg});this.refreshIcons();setTimeout(()=>this.dismissToast(id),4200)},dismissToast(id){this.toasts=this.toasts.filter(t=>t.id!==id)},doLogin(){let p=JSON.parse(localStorage.getItem('rc_passwords')||'{"admin":"123456","viewer":"123456"}');if(this.login.password===p[this.login.role]){this.auth={logged:true,role:this.login.role};this.login.password='';this.load();this.after()}else this.toast('Senha inválida','error')},logout(){this.auth={logged:false,role:'viewer'}},go(v){this.view=v;this.after()},toggleDark(){this.dark=!this.dark;localStorage.setItem('rc_dark',this.dark?'1':'0');document.documentElement.classList.toggle('dark',this.dark)},
  providerAction(action,p){if(!action||!p)return;if(action==='painel')this.selectProvider(p);else if(action==='editar')this.editProvider(p);else if(action==='lancar'){if(!this.isViewer)this.fillServiceFromProvider(p)}else if(action==='whatsapp')this.openWhatsapp(p);else if(action==='excluir'){if(!this.isViewer)this.openDeleteProviderModal(p)}},serviceAction(action,s){if(!action||!s)return;if(action==='pagar'){if(!this.isViewer)this.pay(s)}else if(action==='recibo'){if(!this.isViewer&&Number(s.paidValue||0)>0)this.quickReceipt(s)}else if(action==='duplicar'){if(!this.isViewer)this.openDuplicateModal(s)}else if(action==='excluir'){if(!this.isViewer)this.openDeleteServiceModal(s)}},paymentAction(action,s){if(!action||!s)return;if(action==='pagar'){if(!this.isViewer&&s.status!=='Pago')this.pay(s)}else if(action==='recibo'){if(!this.isViewer&&Number(s.paidValue||0)>0)this.quickReceipt(s)}else if(action==='servico')this.openSearchService(s);else if(action==='excluirPagamento'){if(!this.isViewer&&Number(s.paidValue||0)>0)this.openDeletePaymentModal(s)}},openSearchProvider(p){this.selectedProviderId=p?.id||null;this.view='prestadores';this.after()},openSearchService(s){this.serviceFilter='todos';this.serviceCategoryFilter='todas';this.serviceProviderFilter=String(s.providerId||'todos');this.serviceMonthFilter=s.reference||'';this.view='servicos';this.after()},openSearchReceipt(r){this.currentReceipt=r;this.receiptFilter={q:r.number||'',month:r.reference||'',category:'todas'};this.view='recibos';this.after()},openSearchRecurrence(r){this.recurrenceForm={...r};this.view='servicos';this.after()},selectProvider(p){this.selectedProviderId=p?.id||null;this.after()},providerSortBy(field){if(this.providerSortField===field){this.providerSortDir=this.providerSortDir==='asc'?'desc':'asc';}else{this.providerSortField=field;this.providerSortDir='asc';}},resetProvider(){this.providerForm={id:null,name:'',type:'Autônomo',document:'',category:'Jardineiro',phone:'',whatsapp:'',pix:'',notes:''}},editProvider(p){this.providerForm={...p};this.view='prestadores';this.after()},async saveProvider(){if(this.isViewer)return;let d={...this.providerForm};if(d.id){await db.providers.update(d.id,d);await this.logAction('provider','Prestador atualizado',d.name||'Cadastro atualizado')}else{delete d.id;await db.providers.add(d);await this.logAction('provider','Prestador cadastrado',d.name||'Novo prestador')}this.resetProvider();await this.load();this.toast('Prestador salvo')},openDeleteProviderModal(p){if(this.isViewer)return;this.actionModal={open:true,type:'deleteProvider',tone:'danger',icon:'user-x',target:p,service:null,title:'Excluir prestador',subtitle:'Essa ação remove o cadastro do prestador.',note:'Prestadores com lançamentos, recibos ou recorrências vinculadas não podem ser excluídos. Isso preserva a rastreabilidade do histórico.',confirmLabel:'Excluir prestador',details:[{label:'Prestador',value:p.name||'-'},{label:'Categoria',value:p.category||'-'},{label:'Documento',value:p.document||'não informado'}]};this.after()},async removeProviderInternal(id){let p=this.providers.find(x=>Number(x.id)===Number(id));if(!p)return;let linkedServices=this.services.some(s=>Number(s.providerId)===Number(id)),linkedReceipts=this.receipts.some(r=>Number(r.providerId)===Number(id)),linkedRecurrences=(this.recurrences||[]).some(r=>Number(r.providerId)===Number(id));if(linkedServices||linkedReceipts||linkedRecurrences){return this.toast('Este prestador possui histórico ou recorrência vinculada e não pode ser excluído. Mantenha o cadastro para preservar a rastreabilidade.','warning')}await db.providers.delete(id);await this.logAction('delete','Prestador excluído',p.name||('ID '+id));await this.load()},
  resetRecurrence(){this.recurrenceForm={id:null,providerId:'',category:'Jardineiro',amount:0,dueDay:5,paymentDay:5,paymentMethod:'PIX',location:'',description:'',active:true}},editRecurrence(r){this.recurrenceForm={...r};this.view='servicos';this.after()},async saveRecurrence(){if(this.isViewer)return;let d={...this.recurrenceForm,providerId:Number(this.recurrenceForm.providerId),amount:Number(this.recurrenceForm.amount||0),dueDay:Number(this.recurrenceForm.dueDay||1),paymentDay:Number(this.recurrenceForm.paymentDay||this.recurrenceForm.dueDay||1),active:!!this.recurrenceForm.active};if(d.id){await db.recurrences.update(d.id,d);await this.logAction('recurrence','Recorrência atualizada',this.providerName(d.providerId)+' • '+this.money(d.amount||0))}else{delete d.id;await db.recurrences.add(d);await this.logAction('recurrence','Recorrência criada',this.providerName(d.providerId)+' • '+this.money(d.amount||0))}this.resetRecurrence();await this.load();this.toast('Recorrência salva. Lançamentos gerados mensalmente.')},recurrenceGenerated(r,ref=this.today().slice(0,7)){return this.services.some(s=>Number(s.recurrenceId)===Number(r.id)&&s.reference===ref)},dateForDay(ref,day){let [y,m]=String(ref).split('-').map(Number);let last=new Date(y,m,0).getDate();let d=String(Math.min(Math.max(Number(day||1),1),last)).padStart(2,'0

===== paidValue @ 6470379 =====
c-800/25',iconBg:'bg-gradient-to-br from-rc-800 to-rc-500'}}[type]||{};let id=Date.now()+Math.random();this.toasts.push({id,message,type,...cfg});this.refreshIcons();setTimeout(()=>this.dismissToast(id),4200)},dismissToast(id){this.toasts=this.toasts.filter(t=>t.id!==id)},doLogin(){let p=JSON.parse(localStorage.getItem('rc_passwords')||'{"admin":"123456","viewer":"123456"}');if(this.login.password===p[this.login.role]){this.auth={logged:true,role:this.login.role};this.login.password='';this.load();this.after()}else this.toast('Senha inválida','error')},logout(){this.auth={logged:false,role:'viewer'}},go(v){this.view=v;this.after()},toggleDark(){this.dark=!this.dark;localStorage.setItem('rc_dark',this.dark?'1':'0');document.documentElement.classList.toggle('dark',this.dark)},
  providerAction(action,p){if(!action||!p)return;if(action==='painel')this.selectProvider(p);else if(action==='editar')this.editProvider(p);else if(action==='lancar'){if(!this.isViewer)this.fillServiceFromProvider(p)}else if(action==='whatsapp')this.openWhatsapp(p);else if(action==='excluir'){if(!this.isViewer)this.openDeleteProviderModal(p)}},serviceAction(action,s){if(!action||!s)return;if(action==='pagar'){if(!this.isViewer)this.pay(s)}else if(action==='recibo'){if(!this.isViewer&&Number(s.paidValue||0)>0)this.quickReceipt(s)}else if(action==='duplicar'){if(!this.isViewer)this.openDuplicateModal(s)}else if(action==='excluir'){if(!this.isViewer)this.openDeleteServiceModal(s)}},paymentAction(action,s){if(!action||!s)return;if(action==='pagar'){if(!this.isViewer&&s.status!=='Pago')this.pay(s)}else if(action==='recibo'){if(!this.isViewer&&Number(s.paidValue||0)>0)this.quickReceipt(s)}else if(action==='servico')this.openSearchService(s);else if(action==='excluirPagamento'){if(!this.isViewer&&Number(s.paidValue||0)>0)this.openDeletePaymentModal(s)}},openSearchProvider(p){this.selectedProviderId=p?.id||null;this.view='prestadores';this.after()},openSearchService(s){this.serviceFilter='todos';this.serviceCategoryFilter='todas';this.serviceProviderFilter=String(s.providerId||'todos');this.serviceMonthFilter=s.reference||'';this.view='servicos';this.after()},openSearchReceipt(r){this.currentReceipt=r;this.receiptFilter={q:r.number||'',month:r.reference||'',category:'todas'};this.view='recibos';this.after()},openSearchRecurrence(r){this.recurrenceForm={...r};this.view='servicos';this.after()},selectProvider(p){this.selectedProviderId=p?.id||null;this.after()},providerSortBy(field){if(this.providerSortField===field){this.providerSortDir=this.providerSortDir==='asc'?'desc':'asc';}else{this.providerSortField=field;this.providerSortDir='asc';}},resetProvider(){this.providerForm={id:null,name:'',type:'Autônomo',document:'',category:'Jardineiro',phone:'',whatsapp:'',pix:'',notes:''}},editProvider(p){this.providerForm={...p};this.view='prestadores';this.after()},async saveProvider(){if(this.isViewer)return;let d={...this.providerForm};if(d.id){await db.providers.update(d.id,d);await this.logAction('provider','Prestador atualizado',d.name||'Cadastro atualizado')}else{delete d.id;await db.providers.add(d);await this.logAction('provider','Prestador cadastrado',d.name||'Novo prestador')}this.resetProvider();await this.load();this.toast('Prestador salvo')},openDeleteProviderModal(p){if(this.isViewer)return;this.actionModal={open:true,type:'deleteProvider',tone:'danger',icon:'user-x',target:p,service:null,title:'Excluir prestador',subtitle:'Essa ação remove o cadastro do prestador.',note:'Prestadores com lançamentos, recibos ou recorrências vinculadas não podem ser excluídos. Isso preserva a rastreabilidade do histórico.',confirmLabel:'Excluir prestador',details:[{label:'Prestador',value:p.name||'-'},{label:'Categoria',value:p.category||'-'},{label:'Documento',value:p.document||'não informado'}]};this.after()},async removeProviderInternal(id){let p=this.providers.find(x=>Number(x.id)===Number(id));if(!p)return;let linkedServices=this.services.some(s=>Number(s.providerId)===Number(id)),linkedReceipts=this.receipts.some(r=>Number(r.providerId)===Number(id)),linkedRecurrences=(this.recurrences||[]).some(r=>Number(r.providerId)===Number(id));if(linkedServices||linkedReceipts||linkedRecurrences){return this.toast('Este prestador possui histórico ou recorrência vinculada e não pode ser excluído. Mantenha o cadastro para preservar a rastreabilidade.','warning')}await db.providers.delete(id);await this.logAction('delete','Prestador excluído',p.name||('ID '+id));await this.load()},
  resetRecurrence(){this.recurrenceForm={id:null,providerId:'',category:'Jardineiro',amount:0,dueDay:5,paymentDay:5,paymentMethod:'PIX',location:'',description:'',active:true}},editRecurrence(r){this.recurrenceForm={...r};this.view='servicos';this.after()},async saveRecurrence(){if(this.isViewer)return;let d={...this.recurrenceForm,providerId:Number(this.recurrenceForm.providerId),amount:Number(this.recurrenceForm.amount||0),dueDay:Number(this.recurrenceForm.dueDay||1),paymentDay:Number(this.recurrenceForm.paymentDay||this.recurrenceForm.dueDay||1),active:!!this.recurrenceForm.active};if(d.id){await db.recurrences.update(d.id,d);await this.logAction('recurrence','Recorrência atualizada',this.providerName(d.providerId)+' • '+this.money(d.amount||0))}else{delete d.id;await db.recurrences.add(d);await this.logAction('recurrence','Recorrência criada',this.providerName(d.providerId)+' • '+this.money(d.amount||0))}this.resetRecurrence();await this.load();this.toast('Recorrência salva. Lançamentos gerados mensalmente.')},recurrenceGenerated(r,ref=this.today().slice(0,7)){return this.services.some(s=>Number(s.recurrenceId)===Number(r.id)&&s.reference===ref)},dateForDay(ref,day){let [y,m]=String(ref).split('-').map(Number);let last=new Date(y,m,0).getDate();let d=String(Math.min(Math.max(Number(day||1),1),last)).padStart(2,'0');return ref+'-'+d},async generateRecurrence(r,ref=this.today().slice(0,7)){if(this.isViewer)return;if(!r.active)return this.toast('Esta recorrência está 

===== paidValue @ 6475004 =====
ymentMethod:'PIX',location:'',description:'',active:true}},editRecurrence(r){this.recurrenceForm={...r};this.view='servicos';this.after()},async saveRecurrence(){if(this.isViewer)return;let d={...this.recurrenceForm,providerId:Number(this.recurrenceForm.providerId),amount:Number(this.recurrenceForm.amount||0),dueDay:Number(this.recurrenceForm.dueDay||1),paymentDay:Number(this.recurrenceForm.paymentDay||this.recurrenceForm.dueDay||1),active:!!this.recurrenceForm.active};if(d.id){await db.recurrences.update(d.id,d);await this.logAction('recurrence','Recorrência atualizada',this.providerName(d.providerId)+' • '+this.money(d.amount||0))}else{delete d.id;await db.recurrences.add(d);await this.logAction('recurrence','Recorrência criada',this.providerName(d.providerId)+' • '+this.money(d.amount||0))}this.resetRecurrence();await this.load();this.toast('Recorrência salva. Lançamentos gerados mensalmente.')},recurrenceGenerated(r,ref=this.today().slice(0,7)){return this.services.some(s=>Number(s.recurrenceId)===Number(r.id)&&s.reference===ref)},dateForDay(ref,day){let [y,m]=String(ref).split('-').map(Number);let last=new Date(y,m,0).getDate();let d=String(Math.min(Math.max(Number(day||1),1),last)).padStart(2,'0');return ref+'-'+d},async generateRecurrence(r,ref=this.today().slice(0,7)){if(this.isViewer)return;if(!r.active)return this.toast('Esta recorrência está inativa.','warning');if(this.recurrenceGenerated(r,ref))return this.toast('Lançamento já gerado para '+this.month(ref)+'.','warning');let d={providerId:Number(r.providerId),category:r.category,reference:ref,serviceDate:this.dateForDay(ref,r.paymentDay||r.dueDay),dueDate:this.dateForDay(ref,r.dueDay),amount:Number(r.amount||0),location:r.location||'',description:r.description||'Serviço recorrente mensal',status:'Pendente',paidValue:0,paymentDate:'',paymentMethod:r.paymentMethod||'',recurrenceId:r.id};await db.services.add(d);await this.logAction('recurrence','Lançamento recorrente gerado',this.providerName(r.providerId)+' • '+this.month(ref)+' • '+this.money(r.amount||0));await this.load();this.toast('Lançamento de '+this.month(ref)+' gerado.')},async generateAllRecurrencesForMonth(){if(this.isViewer)return;let active=this.recurrences.filter(r=>r.active);if(!active.length)return alert('Nenhuma recorrência ativa.');let count=0;for(const r of active){if(!this.recurrenceGenerated(r)){let d={providerId:Number(r.providerId),category:r.category,reference:this.today().slice(0,7),serviceDate:this.dateForDay(this.today().slice(0,7),r.paymentDay||r.dueDay),dueDate:this.dateForDay(this.today().slice(0,7),r.dueDay),amount:Number(r.amount||0),location:r.location||'',description:r.description||'Serviço recorrente mensal',status:'Pendente',paidValue:0,paymentDate:'',paymentMethod:r.paymentMethod||'',recurrenceId:r.id};await db.services.add(d);await this.logAction('recurrence','Lançamento recorrente gerado',this.providerName(r.providerId)+' • '+this.month(this.today().slice(0,7))+' • '+this.money(r.amount||0));count++}}await this.load();this.toast(count+' lançamento(s) gerado(s) para '+this.month(this.today().slice(0,7))+'.')},async toggleRecurrence(r){if(this.isViewer)return;await db.recurrences.update(r.id,{active:!r.active});await this.load()},openDeleteRecurrenceModal(r){if(this.isViewer)return;this.actionModal={open:true,type:'deleteRecurrence',tone:'danger',icon:'repeat-2',target:r,service:null,title:'Excluir recorrência',subtitle:'Remove apenas a regra recorrente quando ainda não há histórico gerado.',note:'Se esta recorrência já gerou lançamentos, ela deverá ser inativada em vez de excluída, preservando a origem do histórico.',confirmLabel:'Excluir recorrência',details:[{label:'Prestador',value:this.providerName(r.providerId)},{label:'Categoria',value:r.category||'-'},{label:'Valor padrão',value:this.money(r.amount||0)}]};this.after()},async removeRecurrenceInternal(id){let r=this.recurrences.find(x=>Number(x.id)===Number(id));if(!r)return;let linked=this.services.some(s=>Number(s.recurrenceId)===Number(id));if(linked){if(r.active)await db.recurrences.update(id,{active:false});await this.logAction('recurrence','Recorrência preservada como inativa',this.providerName(r.providerId)+' • '+this.money(r.amount||0));await this.load();return this.toast('Esta recorrência já possui lançamentos gerados e foi mantida como inativa para preservar o histórico.','warning')}await db.recurrences.delete(id);await this.logAction('delete','Recorrência excluída',this.providerName(r.providerId)+' • '+this.money(r.amount||0));await this.load()},async saveService(){if(this.isViewer)return;let paid=Number(this.serviceForm.paidValue||0), amount=Number(this.serviceForm.amount||0);let status=paid<=0?'Pendente':(paid>=amount?'Pago':'Parcial');let d={...this.serviceForm,providerId:Number(this.serviceForm.providerId),amount,paidValue:paid,status,paymentDate:paid>0?(this.serviceForm.paymentDate||this.today()):'',paymentMethod:paid>0?(this.serviceForm.paymentMethod||'Não informado'):''};await db.services.add(d);await this.logAction('service','Lançamento salvo',this.providerName(d.providerId)+' • '+d.category+' • '+this.money(d.amount||0));this.serviceForm={...this.serviceForm,amount:0,location:'',description:'',paymentDate:'',paymentMethod:'',paidValue:0};await this.load();this.toast('Lançamento salvo')},clearServiceFilters(){this.serviceFilter='todos';this.serviceCategoryFilter='todas';this.serviceProviderFilter='todos';this.serviceMonthFilter=''},clearReceiptFilters(){this.receiptFilter={q:'',month:'',category:'todas'}},fillServiceFromProvider(p){this.serviceForm.providerId=p.id;this.serviceForm.category=p.category||this.serviceForm.category;this.view='servicos';this.after()},openDuplicateModal(s){if(this.isViewer)return;this.actionModal={open:true,type:'duplicate',tone:'default',icon:'copy-plus',target:s,service:s,title:'Duplicar lançamento',subtitle:'Criar uma nova cópia para a referência atual.',note:'O novo lançamento será criado como pendente, sem pagamento e sem recibo, pre

===== paidValue @ 6475925 =====
eGenerated(r,ref=this.today().slice(0,7)){return this.services.some(s=>Number(s.recurrenceId)===Number(r.id)&&s.reference===ref)},dateForDay(ref,day){let [y,m]=String(ref).split('-').map(Number);let last=new Date(y,m,0).getDate();let d=String(Math.min(Math.max(Number(day||1),1),last)).padStart(2,'0');return ref+'-'+d},async generateRecurrence(r,ref=this.today().slice(0,7)){if(this.isViewer)return;if(!r.active)return this.toast('Esta recorrência está inativa.','warning');if(this.recurrenceGenerated(r,ref))return this.toast('Lançamento já gerado para '+this.month(ref)+'.','warning');let d={providerId:Number(r.providerId),category:r.category,reference:ref,serviceDate:this.dateForDay(ref,r.paymentDay||r.dueDay),dueDate:this.dateForDay(ref,r.dueDay),amount:Number(r.amount||0),location:r.location||'',description:r.description||'Serviço recorrente mensal',status:'Pendente',paidValue:0,paymentDate:'',paymentMethod:r.paymentMethod||'',recurrenceId:r.id};await db.services.add(d);await this.logAction('recurrence','Lançamento recorrente gerado',this.providerName(r.providerId)+' • '+this.month(ref)+' • '+this.money(r.amount||0));await this.load();this.toast('Lançamento de '+this.month(ref)+' gerado.')},async generateAllRecurrencesForMonth(){if(this.isViewer)return;let active=this.recurrences.filter(r=>r.active);if(!active.length)return alert('Nenhuma recorrência ativa.');let count=0;for(const r of active){if(!this.recurrenceGenerated(r)){let d={providerId:Number(r.providerId),category:r.category,reference:this.today().slice(0,7),serviceDate:this.dateForDay(this.today().slice(0,7),r.paymentDay||r.dueDay),dueDate:this.dateForDay(this.today().slice(0,7),r.dueDay),amount:Number(r.amount||0),location:r.location||'',description:r.description||'Serviço recorrente mensal',status:'Pendente',paidValue:0,paymentDate:'',paymentMethod:r.paymentMethod||'',recurrenceId:r.id};await db.services.add(d);await this.logAction('recurrence','Lançamento recorrente gerado',this.providerName(r.providerId)+' • '+this.month(this.today().slice(0,7))+' • '+this.money(r.amount||0));count++}}await this.load();this.toast(count+' lançamento(s) gerado(s) para '+this.month(this.today().slice(0,7))+'.')},async toggleRecurrence(r){if(this.isViewer)return;await db.recurrences.update(r.id,{active:!r.active});await this.load()},openDeleteRecurrenceModal(r){if(this.isViewer)return;this.actionModal={open:true,type:'deleteRecurrence',tone:'danger',icon:'repeat-2',target:r,service:null,title:'Excluir recorrência',subtitle:'Remove apenas a regra recorrente quando ainda não há histórico gerado.',note:'Se esta recorrência já gerou lançamentos, ela deverá ser inativada em vez de excluída, preservando a origem do histórico.',confirmLabel:'Excluir recorrência',details:[{label:'Prestador',value:this.providerName(r.providerId)},{label:'Categoria',value:r.category||'-'},{label:'Valor padrão',value:this.money(r.amount||0)}]};this.after()},async removeRecurrenceInternal(id){let r=this.recurrences.find(x=>Number(x.id)===Number(id));if(!r)return;let linked=this.services.some(s=>Number(s.recurrenceId)===Number(id));if(linked){if(r.active)await db.recurrences.update(id,{active:false});await this.logAction('recurrence','Recorrência preservada como inativa',this.providerName(r.providerId)+' • '+this.money(r.amount||0));await this.load();return this.toast('Esta recorrência já possui lançamentos gerados e foi mantida como inativa para preservar o histórico.','warning')}await db.recurrences.delete(id);await this.logAction('delete','Recorrência excluída',this.providerName(r.providerId)+' • '+this.money(r.amount||0));await this.load()},async saveService(){if(this.isViewer)return;let paid=Number(this.serviceForm.paidValue||0), amount=Number(this.serviceForm.amount||0);let status=paid<=0?'Pendente':(paid>=amount?'Pago':'Parcial');let d={...this.serviceForm,providerId:Number(this.serviceForm.providerId),amount,paidValue:paid,status,paymentDate:paid>0?(this.serviceForm.paymentDate||this.today()):'',paymentMethod:paid>0?(this.serviceForm.paymentMethod||'Não informado'):''};await db.services.add(d);await this.logAction('service','Lançamento salvo',this.providerName(d.providerId)+' • '+d.category+' • '+this.money(d.amount||0));this.serviceForm={...this.serviceForm,amount:0,location:'',description:'',paymentDate:'',paymentMethod:'',paidValue:0};await this.load();this.toast('Lançamento salvo')},clearServiceFilters(){this.serviceFilter='todos';this.serviceCategoryFilter='todas';this.serviceProviderFilter='todos';this.serviceMonthFilter=''},clearReceiptFilters(){this.receiptFilter={q:'',month:'',category:'todas'}},fillServiceFromProvider(p){this.serviceForm.providerId=p.id;this.serviceForm.category=p.category||this.serviceForm.category;this.view='servicos';this.after()},openDuplicateModal(s){if(this.isViewer)return;this.actionModal={open:true,type:'duplicate',tone:'default',icon:'copy-plus',target:s,service:s,title:'Duplicar lançamento',subtitle:'Criar uma nova cópia para a referência atual.',note:'O novo lançamento será criado como pendente, sem pagamento e sem recibo, preservando a numeração oficial.',confirmLabel:'Duplicar',details:[{label:'Prestador',value:this.providerName(s.providerId)},{label:'Categoria',value:s.category||'-'},{label:'Valor',value:this.money(s.amount||0)}]};this.after()},openDeleteServiceModal(s){if(this.isViewer)return;this.actionModal={open:true,type:'deleteService',tone:'danger',icon:'trash-2',target:s,service:s,title:'Excluir lançamento',subtitle:'Essa ação remove apenas este lançamento da lista de serviços.',note:'Lançamentos com pagamento ou recibo vinculado não podem ser excluídos por aqui. Use o fluxo de pagamentos/recibos para preservar o histórico.',confirmLabel:'Excluir',details:[{label:'Prestador',value:this.providerName(s.providerId)},{label:'Categoria',value:s.category||'-'},{label:'Valor',value:this.money(s.amount||0)}]};this.after()},openDeletePaymentModal(s){if(this.isViewer)return;let receipt=this.receipts.find(r=>Number(r.serviceId)==

===== paidValue @ 6477826 =====
(d);await this.logAction('recurrence','Lançamento recorrente gerado',this.providerName(r.providerId)+' • '+this.month(this.today().slice(0,7))+' • '+this.money(r.amount||0));count++}}await this.load();this.toast(count+' lançamento(s) gerado(s) para '+this.month(this.today().slice(0,7))+'.')},async toggleRecurrence(r){if(this.isViewer)return;await db.recurrences.update(r.id,{active:!r.active});await this.load()},openDeleteRecurrenceModal(r){if(this.isViewer)return;this.actionModal={open:true,type:'deleteRecurrence',tone:'danger',icon:'repeat-2',target:r,service:null,title:'Excluir recorrência',subtitle:'Remove apenas a regra recorrente quando ainda não há histórico gerado.',note:'Se esta recorrência já gerou lançamentos, ela deverá ser inativada em vez de excluída, preservando a origem do histórico.',confirmLabel:'Excluir recorrência',details:[{label:'Prestador',value:this.providerName(r.providerId)},{label:'Categoria',value:r.category||'-'},{label:'Valor padrão',value:this.money(r.amount||0)}]};this.after()},async removeRecurrenceInternal(id){let r=this.recurrences.find(x=>Number(x.id)===Number(id));if(!r)return;let linked=this.services.some(s=>Number(s.recurrenceId)===Number(id));if(linked){if(r.active)await db.recurrences.update(id,{active:false});await this.logAction('recurrence','Recorrência preservada como inativa',this.providerName(r.providerId)+' • '+this.money(r.amount||0));await this.load();return this.toast('Esta recorrência já possui lançamentos gerados e foi mantida como inativa para preservar o histórico.','warning')}await db.recurrences.delete(id);await this.logAction('delete','Recorrência excluída',this.providerName(r.providerId)+' • '+this.money(r.amount||0));await this.load()},async saveService(){if(this.isViewer)return;let paid=Number(this.serviceForm.paidValue||0), amount=Number(this.serviceForm.amount||0);let status=paid<=0?'Pendente':(paid>=amount?'Pago':'Parcial');let d={...this.serviceForm,providerId:Number(this.serviceForm.providerId),amount,paidValue:paid,status,paymentDate:paid>0?(this.serviceForm.paymentDate||this.today()):'',paymentMethod:paid>0?(this.serviceForm.paymentMethod||'Não informado'):''};await db.services.add(d);await this.logAction('service','Lançamento salvo',this.providerName(d.providerId)+' • '+d.category+' • '+this.money(d.amount||0));this.serviceForm={...this.serviceForm,amount:0,location:'',description:'',paymentDate:'',paymentMethod:'',paidValue:0};await this.load();this.toast('Lançamento salvo')},clearServiceFilters(){this.serviceFilter='todos';this.serviceCategoryFilter='todas';this.serviceProviderFilter='todos';this.serviceMonthFilter=''},clearReceiptFilters(){this.receiptFilter={q:'',month:'',category:'todas'}},fillServiceFromProvider(p){this.serviceForm.providerId=p.id;this.serviceForm.category=p.category||this.serviceForm.category;this.view='servicos';this.after()},openDuplicateModal(s){if(this.isViewer)return;this.actionModal={open:true,type:'duplicate',tone:'default',icon:'copy-plus',target:s,service:s,title:'Duplicar lançamento',subtitle:'Criar uma nova cópia para a referência atual.',note:'O novo lançamento será criado como pendente, sem pagamento e sem recibo, preservando a numeração oficial.',confirmLabel:'Duplicar',details:[{label:'Prestador',value:this.providerName(s.providerId)},{label:'Categoria',value:s.category||'-'},{label:'Valor',value:this.money(s.amount||0)}]};this.after()},openDeleteServiceModal(s){if(this.isViewer)return;this.actionModal={open:true,type:'deleteService',tone:'danger',icon:'trash-2',target:s,service:s,title:'Excluir lançamento',subtitle:'Essa ação remove apenas este lançamento da lista de serviços.',note:'Lançamentos com pagamento ou recibo vinculado não podem ser excluídos por aqui. Use o fluxo de pagamentos/recibos para preservar o histórico.',confirmLabel:'Excluir',details:[{label:'Prestador',value:this.providerName(s.providerId)},{label:'Categoria',value:s.category||'-'},{label:'Valor',value:this.money(s.amount||0)}]};this.after()},openDeletePaymentModal(s){if(this.isViewer)return;let receipt=this.receipts.find(r=>Number(r.serviceId)===Number(s.id));this.actionModal={open:true,type:'deletePayment',tone:'danger',icon:'circle-x',target:s,service:s,title:'Excluir todos os pagamentos',subtitle:'Essa ação zera todo o valor pago deste lançamento.',note:receipt?'O recibo vinculado nº '+receipt.number+' também será removido. O lançamento será mantido e voltará como Pendente; a numeração do recibo não será reaproveitada.':'O pagamento será removido, mas o lançamento será mantido e voltará como Pendente.',confirmLabel:'Excluir pagamentos',details:[{label:'Prestador',value:this.providerName(s.providerId)},{label:'Valor pago',value:this.money(s.paidValue||0)},{label:'Data/forma',value:(s.paymentDate?this.fmt(s.paymentDate):'-')+' • '+(s.paymentMethod||'não informada')},{label:'Recibo vinculado',value:receipt?receipt.number:'não emitido'}]};this.after()},openDeleteReceiptModal(r){if(this.isViewer)return;this.actionModal={open:true,type:'deleteReceipt',tone:'danger',icon:'receipt-text',target:r,service:null,title:'Excluir recibo',subtitle:'Essa ação remove o recibo do histórico.',note:'A numeração já utilizada não será reaproveitada, para manter a rastreabilidade da prestação de contas.',confirmLabel:'Excluir recibo',details:[{label:'Recibo nº',value:r.number||'-'},{label:'Prestador',value:r.providerName||'-'},{label:'Valor',value:this.money(r.total||0)}]};this.after()},openClearAllModal(){if(this.isViewer)return;this.actionModal={open:true,type:'clearAll',tone:'danger',icon:'database-zap',target:null,service:null,title:'Apagar dados operacionais',subtitle:'Essa ação remove os registros operacionais salvos neste navegador.',note:'A numeração oficial dos recibos e os dados do condomínio serão preservados. Faça backup JSON antes de apagar, caso precise recuperar os registros depois.',confirmLabel:'Apagar registros',details:[{label:'Remove',value:'Prestadores, serviços, recibos, recorrências e logs'},{label:'Preserv

===== paidValue @ 6478026 =====
;this.toast(count+' lançamento(s) gerado(s) para '+this.month(this.today().slice(0,7))+'.')},async toggleRecurrence(r){if(this.isViewer)return;await db.recurrences.update(r.id,{active:!r.active});await this.load()},openDeleteRecurrenceModal(r){if(this.isViewer)return;this.actionModal={open:true,type:'deleteRecurrence',tone:'danger',icon:'repeat-2',target:r,service:null,title:'Excluir recorrência',subtitle:'Remove apenas a regra recorrente quando ainda não há histórico gerado.',note:'Se esta recorrência já gerou lançamentos, ela deverá ser inativada em vez de excluída, preservando a origem do histórico.',confirmLabel:'Excluir recorrência',details:[{label:'Prestador',value:this.providerName(r.providerId)},{label:'Categoria',value:r.category||'-'},{label:'Valor padrão',value:this.money(r.amount||0)}]};this.after()},async removeRecurrenceInternal(id){let r=this.recurrences.find(x=>Number(x.id)===Number(id));if(!r)return;let linked=this.services.some(s=>Number(s.recurrenceId)===Number(id));if(linked){if(r.active)await db.recurrences.update(id,{active:false});await this.logAction('recurrence','Recorrência preservada como inativa',this.providerName(r.providerId)+' • '+this.money(r.amount||0));await this.load();return this.toast('Esta recorrência já possui lançamentos gerados e foi mantida como inativa para preservar o histórico.','warning')}await db.recurrences.delete(id);await this.logAction('delete','Recorrência excluída',this.providerName(r.providerId)+' • '+this.money(r.amount||0));await this.load()},async saveService(){if(this.isViewer)return;let paid=Number(this.serviceForm.paidValue||0), amount=Number(this.serviceForm.amount||0);let status=paid<=0?'Pendente':(paid>=amount?'Pago':'Parcial');let d={...this.serviceForm,providerId:Number(this.serviceForm.providerId),amount,paidValue:paid,status,paymentDate:paid>0?(this.serviceForm.paymentDate||this.today()):'',paymentMethod:paid>0?(this.serviceForm.paymentMethod||'Não informado'):''};await db.services.add(d);await this.logAction('service','Lançamento salvo',this.providerName(d.providerId)+' • '+d.category+' • '+this.money(d.amount||0));this.serviceForm={...this.serviceForm,amount:0,location:'',description:'',paymentDate:'',paymentMethod:'',paidValue:0};await this.load();this.toast('Lançamento salvo')},clearServiceFilters(){this.serviceFilter='todos';this.serviceCategoryFilter='todas';this.serviceProviderFilter='todos';this.serviceMonthFilter=''},clearReceiptFilters(){this.receiptFilter={q:'',month:'',category:'todas'}},fillServiceFromProvider(p){this.serviceForm.providerId=p.id;this.serviceForm.category=p.category||this.serviceForm.category;this.view='servicos';this.after()},openDuplicateModal(s){if(this.isViewer)return;this.actionModal={open:true,type:'duplicate',tone:'default',icon:'copy-plus',target:s,service:s,title:'Duplicar lançamento',subtitle:'Criar uma nova cópia para a referência atual.',note:'O novo lançamento será criado como pendente, sem pagamento e sem recibo, preservando a numeração oficial.',confirmLabel:'Duplicar',details:[{label:'Prestador',value:this.providerName(s.providerId)},{label:'Categoria',value:s.category||'-'},{label:'Valor',value:this.money(s.amount||0)}]};this.after()},openDeleteServiceModal(s){if(this.isViewer)return;this.actionModal={open:true,type:'deleteService',tone:'danger',icon:'trash-2',target:s,service:s,title:'Excluir lançamento',subtitle:'Essa ação remove apenas este lançamento da lista de serviços.',note:'Lançamentos com pagamento ou recibo vinculado não podem ser excluídos por aqui. Use o fluxo de pagamentos/recibos para preservar o histórico.',confirmLabel:'Excluir',details:[{label:'Prestador',value:this.providerName(s.providerId)},{label:'Categoria',value:s.category||'-'},{label:'Valor',value:this.money(s.amount||0)}]};this.after()},openDeletePaymentModal(s){if(this.isViewer)return;let receipt=this.receipts.find(r=>Number(r.serviceId)===Number(s.id));this.actionModal={open:true,type:'deletePayment',tone:'danger',icon:'circle-x',target:s,service:s,title:'Excluir todos os pagamentos',subtitle:'Essa ação zera todo o valor pago deste lançamento.',note:receipt?'O recibo vinculado nº '+receipt.number+' também será removido. O lançamento será mantido e voltará como Pendente; a numeração do recibo não será reaproveitada.':'O pagamento será removido, mas o lançamento será mantido e voltará como Pendente.',confirmLabel:'Excluir pagamentos',details:[{label:'Prestador',value:this.providerName(s.providerId)},{label:'Valor pago',value:this.money(s.paidValue||0)},{label:'Data/forma',value:(s.paymentDate?this.fmt(s.paymentDate):'-')+' • '+(s.paymentMethod||'não informada')},{label:'Recibo vinculado',value:receipt?receipt.number:'não emitido'}]};this.after()},openDeleteReceiptModal(r){if(this.isViewer)return;this.actionModal={open:true,type:'deleteReceipt',tone:'danger',icon:'receipt-text',target:r,service:null,title:'Excluir recibo',subtitle:'Essa ação remove o recibo do histórico.',note:'A numeração já utilizada não será reaproveitada, para manter a rastreabilidade da prestação de contas.',confirmLabel:'Excluir recibo',details:[{label:'Recibo nº',value:r.number||'-'},{label:'Prestador',value:r.providerName||'-'},{label:'Valor',value:this.money(r.total||0)}]};this.after()},openClearAllModal(){if(this.isViewer)return;this.actionModal={open:true,type:'clearAll',tone:'danger',icon:'database-zap',target:null,service:null,title:'Apagar dados operacionais',subtitle:'Essa ação remove os registros operacionais salvos neste navegador.',note:'A numeração oficial dos recibos e os dados do condomínio serão preservados. Faça backup JSON antes de apagar, caso precise recuperar os registros depois.',confirmLabel:'Apagar registros',details:[{label:'Remove',value:'Prestadores, serviços, recibos, recorrências e logs'},{label:'Preserva',value:'Dados do condomínio e sequência dos recibos'}]};this.after()},closeActionModal(){this.actionModal={open:false,type:'',tone:'default',icon:'',target:null,service:null,title:'',subtitle:'',not

===== paidValue @ 6478451 =====
 regra recorrente quando ainda não há histórico gerado.',note:'Se esta recorrência já gerou lançamentos, ela deverá ser inativada em vez de excluída, preservando a origem do histórico.',confirmLabel:'Excluir recorrência',details:[{label:'Prestador',value:this.providerName(r.providerId)},{label:'Categoria',value:r.category||'-'},{label:'Valor padrão',value:this.money(r.amount||0)}]};this.after()},async removeRecurrenceInternal(id){let r=this.recurrences.find(x=>Number(x.id)===Number(id));if(!r)return;let linked=this.services.some(s=>Number(s.recurrenceId)===Number(id));if(linked){if(r.active)await db.recurrences.update(id,{active:false});await this.logAction('recurrence','Recorrência preservada como inativa',this.providerName(r.providerId)+' • '+this.money(r.amount||0));await this.load();return this.toast('Esta recorrência já possui lançamentos gerados e foi mantida como inativa para preservar o histórico.','warning')}await db.recurrences.delete(id);await this.logAction('delete','Recorrência excluída',this.providerName(r.providerId)+' • '+this.money(r.amount||0));await this.load()},async saveService(){if(this.isViewer)return;let paid=Number(this.serviceForm.paidValue||0), amount=Number(this.serviceForm.amount||0);let status=paid<=0?'Pendente':(paid>=amount?'Pago':'Parcial');let d={...this.serviceForm,providerId:Number(this.serviceForm.providerId),amount,paidValue:paid,status,paymentDate:paid>0?(this.serviceForm.paymentDate||this.today()):'',paymentMethod:paid>0?(this.serviceForm.paymentMethod||'Não informado'):''};await db.services.add(d);await this.logAction('service','Lançamento salvo',this.providerName(d.providerId)+' • '+d.category+' • '+this.money(d.amount||0));this.serviceForm={...this.serviceForm,amount:0,location:'',description:'',paymentDate:'',paymentMethod:'',paidValue:0};await this.load();this.toast('Lançamento salvo')},clearServiceFilters(){this.serviceFilter='todos';this.serviceCategoryFilter='todas';this.serviceProviderFilter='todos';this.serviceMonthFilter=''},clearReceiptFilters(){this.receiptFilter={q:'',month:'',category:'todas'}},fillServiceFromProvider(p){this.serviceForm.providerId=p.id;this.serviceForm.category=p.category||this.serviceForm.category;this.view='servicos';this.after()},openDuplicateModal(s){if(this.isViewer)return;this.actionModal={open:true,type:'duplicate',tone:'default',icon:'copy-plus',target:s,service:s,title:'Duplicar lançamento',subtitle:'Criar uma nova cópia para a referência atual.',note:'O novo lançamento será criado como pendente, sem pagamento e sem recibo, preservando a numeração oficial.',confirmLabel:'Duplicar',details:[{label:'Prestador',value:this.providerName(s.providerId)},{label:'Categoria',value:s.category||'-'},{label:'Valor',value:this.money(s.amount||0)}]};this.after()},openDeleteServiceModal(s){if(this.isViewer)return;this.actionModal={open:true,type:'deleteService',tone:'danger',icon:'trash-2',target:s,service:s,title:'Excluir lançamento',subtitle:'Essa ação remove apenas este lançamento da lista de serviços.',note:'Lançamentos com pagamento ou recibo vinculado não podem ser excluídos por aqui. Use o fluxo de pagamentos/recibos para preservar o histórico.',confirmLabel:'Excluir',details:[{label:'Prestador',value:this.providerName(s.providerId)},{label:'Categoria',value:s.category||'-'},{label:'Valor',value:this.money(s.amount||0)}]};this.after()},openDeletePaymentModal(s){if(this.isViewer)return;let receipt=this.receipts.find(r=>Number(r.serviceId)===Number(s.id));this.actionModal={open:true,type:'deletePayment',tone:'danger',icon:'circle-x',target:s,service:s,title:'Excluir todos os pagamentos',subtitle:'Essa ação zera todo o valor pago deste lançamento.',note:receipt?'O recibo vinculado nº '+receipt.number+' também será removido. O lançamento será mantido e voltará como Pendente; a numeração do recibo não será reaproveitada.':'O pagamento será removido, mas o lançamento será mantido e voltará como Pendente.',confirmLabel:'Excluir pagamentos',details:[{label:'Prestador',value:this.providerName(s.providerId)},{label:'Valor pago',value:this.money(s.paidValue||0)},{label:'Data/forma',value:(s.paymentDate?this.fmt(s.paymentDate):'-')+' • '+(s.paymentMethod||'não informada')},{label:'Recibo vinculado',value:receipt?receipt.number:'não emitido'}]};this.after()},openDeleteReceiptModal(r){if(this.isViewer)return;this.actionModal={open:true,type:'deleteReceipt',tone:'danger',icon:'receipt-text',target:r,service:null,title:'Excluir recibo',subtitle:'Essa ação remove o recibo do histórico.',note:'A numeração já utilizada não será reaproveitada, para manter a rastreabilidade da prestação de contas.',confirmLabel:'Excluir recibo',details:[{label:'Recibo nº',value:r.number||'-'},{label:'Prestador',value:r.providerName||'-'},{label:'Valor',value:this.money(r.total||0)}]};this.after()},openClearAllModal(){if(this.isViewer)return;this.actionModal={open:true,type:'clearAll',tone:'danger',icon:'database-zap',target:null,service:null,title:'Apagar dados operacionais',subtitle:'Essa ação remove os registros operacionais salvos neste navegador.',note:'A numeração oficial dos recibos e os dados do condomínio serão preservados. Faça backup JSON antes de apagar, caso precise recuperar os registros depois.',confirmLabel:'Apagar registros',details:[{label:'Remove',value:'Prestadores, serviços, recibos, recorrências e logs'},{label:'Preserva',value:'Dados do condomínio e sequência dos recibos'}]};this.after()},closeActionModal(){this.actionModal={open:false,type:'',tone:'default',icon:'',target:null,service:null,title:'',subtitle:'',note:'',confirmLabel:'',details:[]}},async confirmActionModal(){if(this.isViewer)return;let m=this.actionModal,t=m.target||m.service;if(m.type==='duplicate'){await this.duplicateServiceInternal(m.service)}else if(m.type==='deleteService'){await this.removeServiceInternal(t.id)}else if(m.type==='deletePayment'){await this.removePaymentInternal(t.id)}else if(m.type==='deleteProvider'){await this.removeProviderInternal(t.id)}el

===== paidValue @ 6480735 =====
eturn;this.actionModal={open:true,type:'duplicate',tone:'default',icon:'copy-plus',target:s,service:s,title:'Duplicar lançamento',subtitle:'Criar uma nova cópia para a referência atual.',note:'O novo lançamento será criado como pendente, sem pagamento e sem recibo, preservando a numeração oficial.',confirmLabel:'Duplicar',details:[{label:'Prestador',value:this.providerName(s.providerId)},{label:'Categoria',value:s.category||'-'},{label:'Valor',value:this.money(s.amount||0)}]};this.after()},openDeleteServiceModal(s){if(this.isViewer)return;this.actionModal={open:true,type:'deleteService',tone:'danger',icon:'trash-2',target:s,service:s,title:'Excluir lançamento',subtitle:'Essa ação remove apenas este lançamento da lista de serviços.',note:'Lançamentos com pagamento ou recibo vinculado não podem ser excluídos por aqui. Use o fluxo de pagamentos/recibos para preservar o histórico.',confirmLabel:'Excluir',details:[{label:'Prestador',value:this.providerName(s.providerId)},{label:'Categoria',value:s.category||'-'},{label:'Valor',value:this.money(s.amount||0)}]};this.after()},openDeletePaymentModal(s){if(this.isViewer)return;let receipt=this.receipts.find(r=>Number(r.serviceId)===Number(s.id));this.actionModal={open:true,type:'deletePayment',tone:'danger',icon:'circle-x',target:s,service:s,title:'Excluir todos os pagamentos',subtitle:'Essa ação zera todo o valor pago deste lançamento.',note:receipt?'O recibo vinculado nº '+receipt.number+' também será removido. O lançamento será mantido e voltará como Pendente; a numeração do recibo não será reaproveitada.':'O pagamento será removido, mas o lançamento será mantido e voltará como Pendente.',confirmLabel:'Excluir pagamentos',details:[{label:'Prestador',value:this.providerName(s.providerId)},{label:'Valor pago',value:this.money(s.paidValue||0)},{label:'Data/forma',value:(s.paymentDate?this.fmt(s.paymentDate):'-')+' • '+(s.paymentMethod||'não informada')},{label:'Recibo vinculado',value:receipt?receipt.number:'não emitido'}]};this.after()},openDeleteReceiptModal(r){if(this.isViewer)return;this.actionModal={open:true,type:'deleteReceipt',tone:'danger',icon:'receipt-text',target:r,service:null,title:'Excluir recibo',subtitle:'Essa ação remove o recibo do histórico.',note:'A numeração já utilizada não será reaproveitada, para manter a rastreabilidade da prestação de contas.',confirmLabel:'Excluir recibo',details:[{label:'Recibo nº',value:r.number||'-'},{label:'Prestador',value:r.providerName||'-'},{label:'Valor',value:this.money(r.total||0)}]};this.after()},openClearAllModal(){if(this.isViewer)return;this.actionModal={open:true,type:'clearAll',tone:'danger',icon:'database-zap',target:null,service:null,title:'Apagar dados operacionais',subtitle:'Essa ação remove os registros operacionais salvos neste navegador.',note:'A numeração oficial dos recibos e os dados do condomínio serão preservados. Faça backup JSON antes de apagar, caso precise recuperar os registros depois.',confirmLabel:'Apagar registros',details:[{label:'Remove',value:'Prestadores, serviços, recibos, recorrências e logs'},{label:'Preserva',value:'Dados do condomínio e sequência dos recibos'}]};this.after()},closeActionModal(){this.actionModal={open:false,type:'',tone:'default',icon:'',target:null,service:null,title:'',subtitle:'',note:'',confirmLabel:'',details:[]}},async confirmActionModal(){if(this.isViewer)return;let m=this.actionModal,t=m.target||m.service;if(m.type==='duplicate'){await this.duplicateServiceInternal(m.service)}else if(m.type==='deleteService'){await this.removeServiceInternal(t.id)}else if(m.type==='deletePayment'){await this.removePaymentInternal(t.id)}else if(m.type==='deleteProvider'){await this.removeProviderInternal(t.id)}else if(m.type==='deleteReceipt'){await this.removeReceiptInternal(t.id)}else if(m.type==='deleteRecurrence'){await this.removeRecurrenceInternal(t.id)}else if(m.type==='clearAll'){await this.clearAllInternal()}this.closeActionModal()},async duplicateServiceInternal(s){let d={...s};delete d.id;d.status='Pendente';d.paidValue=0;d.paymentDate='';d.paymentMethod='';d.serviceDate=this.today();d.dueDate=this.today();d.reference=this.today().slice(0,7);await db.services.add(d);await this.logAction('service','Lançamento duplicado',this.providerName(d.providerId)+' • '+this.money(d.amount||0));await this.load()},async removeServiceInternal(id){let s=this.services.find(x=>Number(x.id)===Number(id));if(!s)return;let linked=this.receipts.find(r=>Number(r.serviceId)===Number(id));if(linked||Number(s.paidValue||0)>0){let linkedCount=this.receipts.filter(r=>Number(r.serviceId)===Number(id)).length;return this.toast(linkedCount>1?'Este lançamento possui '+linkedCount+' recibos vinculados. Exclua pelo fluxo de pagamentos/recibos para preservar o histórico.':linked?'Este lançamento possui o recibo nº '+linked.number+'. Exclua pelo fluxo de pagamento/recibo para preservar o histórico.':'Este lançamento possui pagamento registrado. Exclua pelo fluxo de pagamentos para preservar o histórico.','warning')}await db.services.delete(id);await this.logAction('delete','Lançamento excluído',this.providerName(s.providerId)+' • '+this.money(s.amount||0));await this.load()},async removePaymentInternal(id){let s=this.services.find(x=>Number(x.id)===Number(id));if(!s)return;let linked=this.receipts.filter(r=>Number(r.serviceId)===Number(id));await db.transaction('rw',db.receipts,db.services,async()=>{for(const r of linked)await db.receipts.delete(r.id);await db.services.update(id,{paidValue:0,paymentDate:'',paymentMethod:'',status:'Pendente'})});for(const r of linked)await this.logAction('delete','Recibo removido por exclusão de pagamento','Nº '+r.number+' • '+(r.providerName||this.providerName(s.providerId)));await this.logAction('delete','Pagamento excluído; lançamento mantido',this.providerName(s.providerId)+' • '+this.money(s.paidValue||0));await this.load()},
  pay(s){this.paymentTarget=s;this.payment={date:this.today(),value:this.balance(s),method:'PIX'};this.payModal=true

===== paidValue @ 6482966 =====
istórico.',note:'A numeração já utilizada não será reaproveitada, para manter a rastreabilidade da prestação de contas.',confirmLabel:'Excluir recibo',details:[{label:'Recibo nº',value:r.number||'-'},{label:'Prestador',value:r.providerName||'-'},{label:'Valor',value:this.money(r.total||0)}]};this.after()},openClearAllModal(){if(this.isViewer)return;this.actionModal={open:true,type:'clearAll',tone:'danger',icon:'database-zap',target:null,service:null,title:'Apagar dados operacionais',subtitle:'Essa ação remove os registros operacionais salvos neste navegador.',note:'A numeração oficial dos recibos e os dados do condomínio serão preservados. Faça backup JSON antes de apagar, caso precise recuperar os registros depois.',confirmLabel:'Apagar registros',details:[{label:'Remove',value:'Prestadores, serviços, recibos, recorrências e logs'},{label:'Preserva',value:'Dados do condomínio e sequência dos recibos'}]};this.after()},closeActionModal(){this.actionModal={open:false,type:'',tone:'default',icon:'',target:null,service:null,title:'',subtitle:'',note:'',confirmLabel:'',details:[]}},async confirmActionModal(){if(this.isViewer)return;let m=this.actionModal,t=m.target||m.service;if(m.type==='duplicate'){await this.duplicateServiceInternal(m.service)}else if(m.type==='deleteService'){await this.removeServiceInternal(t.id)}else if(m.type==='deletePayment'){await this.removePaymentInternal(t.id)}else if(m.type==='deleteProvider'){await this.removeProviderInternal(t.id)}else if(m.type==='deleteReceipt'){await this.removeReceiptInternal(t.id)}else if(m.type==='deleteRecurrence'){await this.removeRecurrenceInternal(t.id)}else if(m.type==='clearAll'){await this.clearAllInternal()}this.closeActionModal()},async duplicateServiceInternal(s){let d={...s};delete d.id;d.status='Pendente';d.paidValue=0;d.paymentDate='';d.paymentMethod='';d.serviceDate=this.today();d.dueDate=this.today();d.reference=this.today().slice(0,7);await db.services.add(d);await this.logAction('service','Lançamento duplicado',this.providerName(d.providerId)+' • '+this.money(d.amount||0));await this.load()},async removeServiceInternal(id){let s=this.services.find(x=>Number(x.id)===Number(id));if(!s)return;let linked=this.receipts.find(r=>Number(r.serviceId)===Number(id));if(linked||Number(s.paidValue||0)>0){let linkedCount=this.receipts.filter(r=>Number(r.serviceId)===Number(id)).length;return this.toast(linkedCount>1?'Este lançamento possui '+linkedCount+' recibos vinculados. Exclua pelo fluxo de pagamentos/recibos para preservar o histórico.':linked?'Este lançamento possui o recibo nº '+linked.number+'. Exclua pelo fluxo de pagamento/recibo para preservar o histórico.':'Este lançamento possui pagamento registrado. Exclua pelo fluxo de pagamentos para preservar o histórico.','warning')}await db.services.delete(id);await this.logAction('delete','Lançamento excluído',this.providerName(s.providerId)+' • '+this.money(s.amount||0));await this.load()},async removePaymentInternal(id){let s=this.services.find(x=>Number(x.id)===Number(id));if(!s)return;let linked=this.receipts.filter(r=>Number(r.serviceId)===Number(id));await db.transaction('rw',db.receipts,db.services,async()=>{for(const r of linked)await db.receipts.delete(r.id);await db.services.update(id,{paidValue:0,paymentDate:'',paymentMethod:'',status:'Pendente'})});for(const r of linked)await this.logAction('delete','Recibo removido por exclusão de pagamento','Nº '+r.number+' • '+(r.providerName||this.providerName(s.providerId)));await this.logAction('delete','Pagamento excluído; lançamento mantido',this.providerName(s.providerId)+' • '+this.money(s.paidValue||0));await this.load()},
  pay(s){this.paymentTarget=s;this.payment={date:this.today(),value:this.balance(s),method:'PIX'};this.payModal=true},async savePayment(){if(this.isViewer)return;let s=this.paymentTarget,value=Number(this.payment.value||0),remaining=Math.max(0,Number(s.amount||0)-Number(s.paidValue||0));if(value<=0)return this.toast('Informe um valor de pagamento maior que zero.','warning');if(value>remaining+0.000001)return this.toast('O pagamento não pode ser maior que o saldo pendente de '+this.money(remaining)+'.','warning');let paid=Number(s.paidValue||0)+value,status=paid>=Number(s.amount)?'Pago':'Parcial';await db.services.update(s.id,{paidValue:paid,paymentDate:this.payment.date,paymentMethod:this.payment.method,status});await this.logAction('payment','Pagamento registrado',this.providerName(s.providerId)+' • '+this.money(value)+' • '+this.payment.method);this.payModal=false;await this.load();this.toast(status==='Pago'?'Pagamento registrado. Lançamento quitado.':'Pagamento parcial registrado. Saldo restante: '+this.money(Math.max(0,Number(s.amount||0)-paid)))},prepareReceipt(s){this.receiptServiceId=s.id;this.view='recibos';this.after()},async quickReceipt(s){if(this.isViewer)return;if(Number(s.paidValue||0)<=0)return this.toast('Registre o pagamento antes de emitir o recibo.','warning');let linked=this.receipts.filter(r=>Number(r.serviceId)===Number(s.id)),issued=linked.reduce((a,r)=>a+Number(r.total||0),0),pending=Math.max(0,Number(s.paidValue||0)-issued);if(pending<=0){let latest=linked.slice().sort((a,b)=>Number(b.id||0)-Number(a.id||0))[0];if(latest){this.currentReceipt=latest;this.view='recibos';this.after();this.toast('Todos os pagamentos registrados já possuem recibo.','warning');setTimeout(()=>this.printReceipt(),220)}else this.toast('Não há valor pago pendente de recibo.','warning');return}this.receiptServiceId=s.id;await this.issueReceipt();this.view='recibos';this.after();setTimeout(()=>this.printReceipt(),260)},async issueReceipt(){if(this.isViewer)return;let s=this.services.find(x=>Number(x.id)===Number(this.receiptServiceId));if(!s)return this.toast('Selecione um lançamento.','warning');let linked=this.receipts.filter(r=>Number(r.serviceId)===Number(s.id)),issued=linked.reduce((a,r)=>a+Number(r.total||0),0),pending=Math.max(0,Number(s.paidValue||0)-issued);if(pending<=0)return this.toast('Todos os pa

===== paidValue @ 6483448 =====
nais',subtitle:'Essa ação remove os registros operacionais salvos neste navegador.',note:'A numeração oficial dos recibos e os dados do condomínio serão preservados. Faça backup JSON antes de apagar, caso precise recuperar os registros depois.',confirmLabel:'Apagar registros',details:[{label:'Remove',value:'Prestadores, serviços, recibos, recorrências e logs'},{label:'Preserva',value:'Dados do condomínio e sequência dos recibos'}]};this.after()},closeActionModal(){this.actionModal={open:false,type:'',tone:'default',icon:'',target:null,service:null,title:'',subtitle:'',note:'',confirmLabel:'',details:[]}},async confirmActionModal(){if(this.isViewer)return;let m=this.actionModal,t=m.target||m.service;if(m.type==='duplicate'){await this.duplicateServiceInternal(m.service)}else if(m.type==='deleteService'){await this.removeServiceInternal(t.id)}else if(m.type==='deletePayment'){await this.removePaymentInternal(t.id)}else if(m.type==='deleteProvider'){await this.removeProviderInternal(t.id)}else if(m.type==='deleteReceipt'){await this.removeReceiptInternal(t.id)}else if(m.type==='deleteRecurrence'){await this.removeRecurrenceInternal(t.id)}else if(m.type==='clearAll'){await this.clearAllInternal()}this.closeActionModal()},async duplicateServiceInternal(s){let d={...s};delete d.id;d.status='Pendente';d.paidValue=0;d.paymentDate='';d.paymentMethod='';d.serviceDate=this.today();d.dueDate=this.today();d.reference=this.today().slice(0,7);await db.services.add(d);await this.logAction('service','Lançamento duplicado',this.providerName(d.providerId)+' • '+this.money(d.amount||0));await this.load()},async removeServiceInternal(id){let s=this.services.find(x=>Number(x.id)===Number(id));if(!s)return;let linked=this.receipts.find(r=>Number(r.serviceId)===Number(id));if(linked||Number(s.paidValue||0)>0){let linkedCount=this.receipts.filter(r=>Number(r.serviceId)===Number(id)).length;return this.toast(linkedCount>1?'Este lançamento possui '+linkedCount+' recibos vinculados. Exclua pelo fluxo de pagamentos/recibos para preservar o histórico.':linked?'Este lançamento possui o recibo nº '+linked.number+'. Exclua pelo fluxo de pagamento/recibo para preservar o histórico.':'Este lançamento possui pagamento registrado. Exclua pelo fluxo de pagamentos para preservar o histórico.','warning')}await db.services.delete(id);await this.logAction('delete','Lançamento excluído',this.providerName(s.providerId)+' • '+this.money(s.amount||0));await this.load()},async removePaymentInternal(id){let s=this.services.find(x=>Number(x.id)===Number(id));if(!s)return;let linked=this.receipts.filter(r=>Number(r.serviceId)===Number(id));await db.transaction('rw',db.receipts,db.services,async()=>{for(const r of linked)await db.receipts.delete(r.id);await db.services.update(id,{paidValue:0,paymentDate:'',paymentMethod:'',status:'Pendente'})});for(const r of linked)await this.logAction('delete','Recibo removido por exclusão de pagamento','Nº '+r.number+' • '+(r.providerName||this.providerName(s.providerId)));await this.logAction('delete','Pagamento excluído; lançamento mantido',this.providerName(s.providerId)+' • '+this.money(s.paidValue||0));await this.load()},
  pay(s){this.paymentTarget=s;this.payment={date:this.today(),value:this.balance(s),method:'PIX'};this.payModal=true},async savePayment(){if(this.isViewer)return;let s=this.paymentTarget,value=Number(this.payment.value||0),remaining=Math.max(0,Number(s.amount||0)-Number(s.paidValue||0));if(value<=0)return this.toast('Informe um valor de pagamento maior que zero.','warning');if(value>remaining+0.000001)return this.toast('O pagamento não pode ser maior que o saldo pendente de '+this.money(remaining)+'.','warning');let paid=Number(s.paidValue||0)+value,status=paid>=Number(s.amount)?'Pago':'Parcial';await db.services.update(s.id,{paidValue:paid,paymentDate:this.payment.date,paymentMethod:this.payment.method,status});await this.logAction('payment','Pagamento registrado',this.providerName(s.providerId)+' • '+this.money(value)+' • '+this.payment.method);this.payModal=false;await this.load();this.toast(status==='Pago'?'Pagamento registrado. Lançamento quitado.':'Pagamento parcial registrado. Saldo restante: '+this.money(Math.max(0,Number(s.amount||0)-paid)))},prepareReceipt(s){this.receiptServiceId=s.id;this.view='recibos';this.after()},async quickReceipt(s){if(this.isViewer)return;if(Number(s.paidValue||0)<=0)return this.toast('Registre o pagamento antes de emitir o recibo.','warning');let linked=this.receipts.filter(r=>Number(r.serviceId)===Number(s.id)),issued=linked.reduce((a,r)=>a+Number(r.total||0),0),pending=Math.max(0,Number(s.paidValue||0)-issued);if(pending<=0){let latest=linked.slice().sort((a,b)=>Number(b.id||0)-Number(a.id||0))[0];if(latest){this.currentReceipt=latest;this.view='recibos';this.after();this.toast('Todos os pagamentos registrados já possuem recibo.','warning');setTimeout(()=>this.printReceipt(),220)}else this.toast('Não há valor pago pendente de recibo.','warning');return}this.receiptServiceId=s.id;await this.issueReceipt();this.view='recibos';this.after();setTimeout(()=>this.printReceipt(),260)},async issueReceipt(){if(this.isViewer)return;let s=this.services.find(x=>Number(x.id)===Number(this.receiptServiceId));if(!s)return this.toast('Selecione um lançamento.','warning');let linked=this.receipts.filter(r=>Number(r.serviceId)===Number(s.id)),issued=linked.reduce((a,r)=>a+Number(r.total||0),0),pending=Math.max(0,Number(s.paidValue||0)-issued);if(pending<=0)return this.toast('Todos os pagamentos registrados deste lançamento já possuem recibo.','warning');let desc=String(s.description||'').replace(/(\bparcela\s+)(\d+)\s*\/\s*(\d+)/i,(m,prefix,current,total)=>prefix+String(linked.length+1).padStart(current.length,'0')+'/'+total);let p=this.providers.find(x=>Number(x.id)===Number(s.providerId))||{};let meta=await db.meta.get('receiptSeq'),next=Math.max(Number(meta?.value||0),this.receiptMaxNumber())+1;await db.meta.put({key:'receiptSeq',value:next});let r={number

===== paidValue @ 6484428 =====
oviderInternal(t.id)}else if(m.type==='deleteReceipt'){await this.removeReceiptInternal(t.id)}else if(m.type==='deleteRecurrence'){await this.removeRecurrenceInternal(t.id)}else if(m.type==='clearAll'){await this.clearAllInternal()}this.closeActionModal()},async duplicateServiceInternal(s){let d={...s};delete d.id;d.status='Pendente';d.paidValue=0;d.paymentDate='';d.paymentMethod='';d.serviceDate=this.today();d.dueDate=this.today();d.reference=this.today().slice(0,7);await db.services.add(d);await this.logAction('service','Lançamento duplicado',this.providerName(d.providerId)+' • '+this.money(d.amount||0));await this.load()},async removeServiceInternal(id){let s=this.services.find(x=>Number(x.id)===Number(id));if(!s)return;let linked=this.receipts.find(r=>Number(r.serviceId)===Number(id));if(linked||Number(s.paidValue||0)>0){let linkedCount=this.receipts.filter(r=>Number(r.serviceId)===Number(id)).length;return this.toast(linkedCount>1?'Este lançamento possui '+linkedCount+' recibos vinculados. Exclua pelo fluxo de pagamentos/recibos para preservar o histórico.':linked?'Este lançamento possui o recibo nº '+linked.number+'. Exclua pelo fluxo de pagamento/recibo para preservar o histórico.':'Este lançamento possui pagamento registrado. Exclua pelo fluxo de pagamentos para preservar o histórico.','warning')}await db.services.delete(id);await this.logAction('delete','Lançamento excluído',this.providerName(s.providerId)+' • '+this.money(s.amount||0));await this.load()},async removePaymentInternal(id){let s=this.services.find(x=>Number(x.id)===Number(id));if(!s)return;let linked=this.receipts.filter(r=>Number(r.serviceId)===Number(id));await db.transaction('rw',db.receipts,db.services,async()=>{for(const r of linked)await db.receipts.delete(r.id);await db.services.update(id,{paidValue:0,paymentDate:'',paymentMethod:'',status:'Pendente'})});for(const r of linked)await this.logAction('delete','Recibo removido por exclusão de pagamento','Nº '+r.number+' • '+(r.providerName||this.providerName(s.providerId)));await this.logAction('delete','Pagamento excluído; lançamento mantido',this.providerName(s.providerId)+' • '+this.money(s.paidValue||0));await this.load()},
  pay(s){this.paymentTarget=s;this.payment={date:this.today(),value:this.balance(s),method:'PIX'};this.payModal=true},async savePayment(){if(this.isViewer)return;let s=this.paymentTarget,value=Number(this.payment.value||0),remaining=Math.max(0,Number(s.amount||0)-Number(s.paidValue||0));if(value<=0)return this.toast('Informe um valor de pagamento maior que zero.','warning');if(value>remaining+0.000001)return this.toast('O pagamento não pode ser maior que o saldo pendente de '+this.money(remaining)+'.','warning');let paid=Number(s.paidValue||0)+value,status=paid>=Number(s.amount)?'Pago':'Parcial';await db.services.update(s.id,{paidValue:paid,paymentDate:this.payment.date,paymentMethod:this.payment.method,status});await this.logAction('payment','Pagamento registrado',this.providerName(s.providerId)+' • '+this.money(value)+' • '+this.payment.method);this.payModal=false;await this.load();this.toast(status==='Pago'?'Pagamento registrado. Lançamento quitado.':'Pagamento parcial registrado. Saldo restante: '+this.money(Math.max(0,Number(s.amount||0)-paid)))},prepareReceipt(s){this.receiptServiceId=s.id;this.view='recibos';this.after()},async quickReceipt(s){if(this.isViewer)return;if(Number(s.paidValue||0)<=0)return this.toast('Registre o pagamento antes de emitir o recibo.','warning');let linked=this.receipts.filter(r=>Number(r.serviceId)===Number(s.id)),issued=linked.reduce((a,r)=>a+Number(r.total||0),0),pending=Math.max(0,Number(s.paidValue||0)-issued);if(pending<=0){let latest=linked.slice().sort((a,b)=>Number(b.id||0)-Number(a.id||0))[0];if(latest){this.currentReceipt=latest;this.view='recibos';this.after();this.toast('Todos os pagamentos registrados já possuem recibo.','warning');setTimeout(()=>this.printReceipt(),220)}else this.toast('Não há valor pago pendente de recibo.','warning');return}this.receiptServiceId=s.id;await this.issueReceipt();this.view='recibos';this.after();setTimeout(()=>this.printReceipt(),260)},async issueReceipt(){if(this.isViewer)return;let s=this.services.find(x=>Number(x.id)===Number(this.receiptServiceId));if(!s)return this.toast('Selecione um lançamento.','warning');let linked=this.receipts.filter(r=>Number(r.serviceId)===Number(s.id)),issued=linked.reduce((a,r)=>a+Number(r.total||0),0),pending=Math.max(0,Number(s.paidValue||0)-issued);if(pending<=0)return this.toast('Todos os pagamentos registrados deste lançamento já possuem recibo.','warning');let desc=String(s.description||'').replace(/(\bparcela\s+)(\d+)\s*\/\s*(\d+)/i,(m,prefix,current,total)=>prefix+String(linked.length+1).padStart(current.length,'0')+'/'+total);let p=this.providers.find(x=>Number(x.id)===Number(s.providerId))||{};let meta=await db.meta.get('receiptSeq'),next=Math.max(Number(meta?.value||0),this.receiptMaxNumber())+1;await db.meta.put({key:'receiptSeq',value:next});let r={number:String(next).padStart(6,'0'),date:this.today(),paymentDate:s.paymentDate||this.today(),providerId:p.id,providerName:p.name||'Prestador não informado',document:p.document||'',category:s.category,reference:s.reference,description:desc,total:pending,paymentMethod:s.paymentMethod||'Não informado',location:s.location,serviceId:s.id,manager:this.settings.manager,condoName:this.settings.condoName};let id=await db.receipts.add(r);r.id=id;this.currentReceipt=r;await this.logAction('receipt','Recibo emitido','Nº '+r.number+' • '+r.providerName+' • '+this.money(r.total||0));await this.load();this.toast('Recibo nº '+r.number+' emitido no valor de '+this.money(r.total||0)+'.')},printReceipt(){if(!this.currentReceipt&&this.receipts.length)this.currentReceipt=this.receipts[this.receipts.length-1];document.getElementById('receiptPrint').classList.remove('hidden');document.getElementById('reportPrint').classList.add('hidden');document.getElementById('annualReportPrint').classList.a

===== paidValue @ 6484784 =====
entDate='';d.paymentMethod='';d.serviceDate=this.today();d.dueDate=this.today();d.reference=this.today().slice(0,7);await db.services.add(d);await this.logAction('service','Lançamento duplicado',this.providerName(d.providerId)+' • '+this.money(d.amount||0));await this.load()},async removeServiceInternal(id){let s=this.services.find(x=>Number(x.id)===Number(id));if(!s)return;let linked=this.receipts.find(r=>Number(r.serviceId)===Number(id));if(linked||Number(s.paidValue||0)>0){let linkedCount=this.receipts.filter(r=>Number(r.serviceId)===Number(id)).length;return this.toast(linkedCount>1?'Este lançamento possui '+linkedCount+' recibos vinculados. Exclua pelo fluxo de pagamentos/recibos para preservar o histórico.':linked?'Este lançamento possui o recibo nº '+linked.number+'. Exclua pelo fluxo de pagamento/recibo para preservar o histórico.':'Este lançamento possui pagamento registrado. Exclua pelo fluxo de pagamentos para preservar o histórico.','warning')}await db.services.delete(id);await this.logAction('delete','Lançamento excluído',this.providerName(s.providerId)+' • '+this.money(s.amount||0));await this.load()},async removePaymentInternal(id){let s=this.services.find(x=>Number(x.id)===Number(id));if(!s)return;let linked=this.receipts.filter(r=>Number(r.serviceId)===Number(id));await db.transaction('rw',db.receipts,db.services,async()=>{for(const r of linked)await db.receipts.delete(r.id);await db.services.update(id,{paidValue:0,paymentDate:'',paymentMethod:'',status:'Pendente'})});for(const r of linked)await this.logAction('delete','Recibo removido por exclusão de pagamento','Nº '+r.number+' • '+(r.providerName||this.providerName(s.providerId)));await this.logAction('delete','Pagamento excluído; lançamento mantido',this.providerName(s.providerId)+' • '+this.money(s.paidValue||0));await this.load()},
  pay(s){this.paymentTarget=s;this.payment={date:this.today(),value:this.balance(s),method:'PIX'};this.payModal=true},async savePayment(){if(this.isViewer)return;let s=this.paymentTarget,value=Number(this.payment.value||0),remaining=Math.max(0,Number(s.amount||0)-Number(s.paidValue||0));if(value<=0)return this.toast('Informe um valor de pagamento maior que zero.','warning');if(value>remaining+0.000001)return this.toast('O pagamento não pode ser maior que o saldo pendente de '+this.money(remaining)+'.','warning');let paid=Number(s.paidValue||0)+value,status=paid>=Number(s.amount)?'Pago':'Parcial';await db.services.update(s.id,{paidValue:paid,paymentDate:this.payment.date,paymentMethod:this.payment.method,status});await this.logAction('payment','Pagamento registrado',this.providerName(s.providerId)+' • '+this.money(value)+' • '+this.payment.method);this.payModal=false;await this.load();this.toast(status==='Pago'?'Pagamento registrado. Lançamento quitado.':'Pagamento parcial registrado. Saldo restante: '+this.money(Math.max(0,Number(s.amount||0)-paid)))},prepareReceipt(s){this.receiptServiceId=s.id;this.view='recibos';this.after()},async quickReceipt(s){if(this.isViewer)return;if(Number(s.paidValue||0)<=0)return this.toast('Registre o pagamento antes de emitir o recibo.','warning');let linked=this.receipts.filter(r=>Number(r.serviceId)===Number(s.id)),issued=linked.reduce((a,r)=>a+Number(r.total||0),0),pending=Math.max(0,Number(s.paidValue||0)-issued);if(pending<=0){let latest=linked.slice().sort((a,b)=>Number(b.id||0)-Number(a.id||0))[0];if(latest){this.currentReceipt=latest;this.view='recibos';this.after();this.toast('Todos os pagamentos registrados já possuem recibo.','warning');setTimeout(()=>this.printReceipt(),220)}else this.toast('Não há valor pago pendente de recibo.','warning');return}this.receiptServiceId=s.id;await this.issueReceipt();this.view='recibos';this.after();setTimeout(()=>this.printReceipt(),260)},async issueReceipt(){if(this.isViewer)return;let s=this.services.find(x=>Number(x.id)===Number(this.receiptServiceId));if(!s)return this.toast('Selecione um lançamento.','warning');let linked=this.receipts.filter(r=>Number(r.serviceId)===Number(s.id)),issued=linked.reduce((a,r)=>a+Number(r.total||0),0),pending=Math.max(0,Number(s.paidValue||0)-issued);if(pending<=0)return this.toast('Todos os pagamentos registrados deste lançamento já possuem recibo.','warning');let desc=String(s.description||'').replace(/(\bparcela\s+)(\d+)\s*\/\s*(\d+)/i,(m,prefix,current,total)=>prefix+String(linked.length+1).padStart(current.length,'0')+'/'+total);let p=this.providers.find(x=>Number(x.id)===Number(s.providerId))||{};let meta=await db.meta.get('receiptSeq'),next=Math.max(Number(meta?.value||0),this.receiptMaxNumber())+1;await db.meta.put({key:'receiptSeq',value:next});let r={number:String(next).padStart(6,'0'),date:this.today(),paymentDate:s.paymentDate||this.today(),providerId:p.id,providerName:p.name||'Prestador não informado',document:p.document||'',category:s.category,reference:s.reference,description:desc,total:pending,paymentMethod:s.paymentMethod||'Não informado',location:s.location,serviceId:s.id,manager:this.settings.manager,condoName:this.settings.condoName};let id=await db.receipts.add(r);r.id=id;this.currentReceipt=r;await this.logAction('receipt','Recibo emitido','Nº '+r.number+' • '+r.providerName+' • '+this.money(r.total||0));await this.load();this.toast('Recibo nº '+r.number+' emitido no valor de '+this.money(r.total||0)+'.')},printReceipt(){if(!this.currentReceipt&&this.receipts.length)this.currentReceipt=this.receipts[this.receipts.length-1];document.getElementById('receiptPrint').classList.remove('hidden');document.getElementById('reportPrint').classList.add('hidden');document.getElementById('annualReportPrint').classList.add('hidden');setTimeout(()=>window.print(),100)},async removeReceiptInternal(id){let r=this.receipts.find(x=>Number(x.id)===Number(id));if(!r)return;let linked=this.receipts.filter(x=>Number(x.serviceId)===Number(r.serviceId)).sort((a,b)=>Number(b.id||0)-Number(a.id||0)),latest=linked[0];if(latest&&Number(latest.id)!==Number(r.id))return this.toast('Para

===== paidValue @ 6485092 =====
{let s=this.services.find(x=>Number(x.id)===Number(id));if(!s)return;let linked=this.receipts.find(r=>Number(r.serviceId)===Number(id));if(linked||Number(s.paidValue||0)>0){let linkedCount=this.receipts.filter(r=>Number(r.serviceId)===Number(id)).length;return this.toast(linkedCount>1?'Este lançamento possui '+linkedCount+' recibos vinculados. Exclua pelo fluxo de pagamentos/recibos para preservar o histórico.':linked?'Este lançamento possui o recibo nº '+linked.number+'. Exclua pelo fluxo de pagamento/recibo para preservar o histórico.':'Este lançamento possui pagamento registrado. Exclua pelo fluxo de pagamentos para preservar o histórico.','warning')}await db.services.delete(id);await this.logAction('delete','Lançamento excluído',this.providerName(s.providerId)+' • '+this.money(s.amount||0));await this.load()},async removePaymentInternal(id){let s=this.services.find(x=>Number(x.id)===Number(id));if(!s)return;let linked=this.receipts.filter(r=>Number(r.serviceId)===Number(id));await db.transaction('rw',db.receipts,db.services,async()=>{for(const r of linked)await db.receipts.delete(r.id);await db.services.update(id,{paidValue:0,paymentDate:'',paymentMethod:'',status:'Pendente'})});for(const r of linked)await this.logAction('delete','Recibo removido por exclusão de pagamento','Nº '+r.number+' • '+(r.providerName||this.providerName(s.providerId)));await this.logAction('delete','Pagamento excluído; lançamento mantido',this.providerName(s.providerId)+' • '+this.money(s.paidValue||0));await this.load()},
  pay(s){this.paymentTarget=s;this.payment={date:this.today(),value:this.balance(s),method:'PIX'};this.payModal=true},async savePayment(){if(this.isViewer)return;let s=this.paymentTarget,value=Number(this.payment.value||0),remaining=Math.max(0,Number(s.amount||0)-Number(s.paidValue||0));if(value<=0)return this.toast('Informe um valor de pagamento maior que zero.','warning');if(value>remaining+0.000001)return this.toast('O pagamento não pode ser maior que o saldo pendente de '+this.money(remaining)+'.','warning');let paid=Number(s.paidValue||0)+value,status=paid>=Number(s.amount)?'Pago':'Parcial';await db.services.update(s.id,{paidValue:paid,paymentDate:this.payment.date,paymentMethod:this.payment.method,status});await this.logAction('payment','Pagamento registrado',this.providerName(s.providerId)+' • '+this.money(value)+' • '+this.payment.method);this.payModal=false;await this.load();this.toast(status==='Pago'?'Pagamento registrado. Lançamento quitado.':'Pagamento parcial registrado. Saldo restante: '+this.money(Math.max(0,Number(s.amount||0)-paid)))},prepareReceipt(s){this.receiptServiceId=s.id;this.view='recibos';this.after()},async quickReceipt(s){if(this.isViewer)return;if(Number(s.paidValue||0)<=0)return this.toast('Registre o pagamento antes de emitir o recibo.','warning');let linked=this.receipts.filter(r=>Number(r.serviceId)===Number(s.id)),issued=linked.reduce((a,r)=>a+Number(r.total||0),0),pending=Math.max(0,Number(s.paidValue||0)-issued);if(pending<=0){let latest=linked.slice().sort((a,b)=>Number(b.id||0)-Number(a.id||0))[0];if(latest){this.currentReceipt=latest;this.view='recibos';this.after();this.toast('Todos os pagamentos registrados já possuem recibo.','warning');setTimeout(()=>this.printReceipt(),220)}else this.toast('Não há valor pago pendente de recibo.','warning');return}this.receiptServiceId=s.id;await this.issueReceipt();this.view='recibos';this.after();setTimeout(()=>this.printReceipt(),260)},async issueReceipt(){if(this.isViewer)return;let s=this.services.find(x=>Number(x.id)===Number(this.receiptServiceId));if(!s)return this.toast('Selecione um lançamento.','warning');let linked=this.receipts.filter(r=>Number(r.serviceId)===Number(s.id)),issued=linked.reduce((a,r)=>a+Number(r.total||0),0),pending=Math.max(0,Number(s.paidValue||0)-issued);if(pending<=0)return this.toast('Todos os pagamentos registrados deste lançamento já possuem recibo.','warning');let desc=String(s.description||'').replace(/(\bparcela\s+)(\d+)\s*\/\s*(\d+)/i,(m,prefix,current,total)=>prefix+String(linked.length+1).padStart(current.length,'0')+'/'+total);let p=this.providers.find(x=>Number(x.id)===Number(s.providerId))||{};let meta=await db.meta.get('receiptSeq'),next=Math.max(Number(meta?.value||0),this.receiptMaxNumber())+1;await db.meta.put({key:'receiptSeq',value:next});let r={number:String(next).padStart(6,'0'),date:this.today(),paymentDate:s.paymentDate||this.today(),providerId:p.id,providerName:p.name||'Prestador não informado',document:p.document||'',category:s.category,reference:s.reference,description:desc,total:pending,paymentMethod:s.paymentMethod||'Não informado',location:s.location,serviceId:s.id,manager:this.settings.manager,condoName:this.settings.condoName};let id=await db.receipts.add(r);r.id=id;this.currentReceipt=r;await this.logAction('receipt','Recibo emitido','Nº '+r.number+' • '+r.providerName+' • '+this.money(r.total||0));await this.load();this.toast('Recibo nº '+r.number+' emitido no valor de '+this.money(r.total||0)+'.')},printReceipt(){if(!this.currentReceipt&&this.receipts.length)this.currentReceipt=this.receipts[this.receipts.length-1];document.getElementById('receiptPrint').classList.remove('hidden');document.getElementById('reportPrint').classList.add('hidden');document.getElementById('annualReportPrint').classList.add('hidden');setTimeout(()=>window.print(),100)},async removeReceiptInternal(id){let r=this.receipts.find(x=>Number(x.id)===Number(id));if(!r)return;let linked=this.receipts.filter(x=>Number(x.serviceId)===Number(r.serviceId)).sort((a,b)=>Number(b.id||0)-Number(a.id||0)),latest=linked[0];if(latest&&Number(latest.id)!==Number(r.id))return this.toast('Para preservar a data e a forma de pagamento das parcelas, somente o recibo mais recente deste lançamento pode ser excluído e reemitido.','warning');await db.receipts.delete(id);await this.logAction('delete','Recibo excluído',r?('Nº '+r.number+' • '+r.providerName):('ID '+id));await this.load()},
  printReport(

===== paidValue @ 6485355 =====
is.toast(linkedCount>1?'Este lançamento possui '+linkedCount+' recibos vinculados. Exclua pelo fluxo de pagamentos/recibos para preservar o histórico.':linked?'Este lançamento possui o recibo nº '+linked.number+'. Exclua pelo fluxo de pagamento/recibo para preservar o histórico.':'Este lançamento possui pagamento registrado. Exclua pelo fluxo de pagamentos para preservar o histórico.','warning')}await db.services.delete(id);await this.logAction('delete','Lançamento excluído',this.providerName(s.providerId)+' • '+this.money(s.amount||0));await this.load()},async removePaymentInternal(id){let s=this.services.find(x=>Number(x.id)===Number(id));if(!s)return;let linked=this.receipts.filter(r=>Number(r.serviceId)===Number(id));await db.transaction('rw',db.receipts,db.services,async()=>{for(const r of linked)await db.receipts.delete(r.id);await db.services.update(id,{paidValue:0,paymentDate:'',paymentMethod:'',status:'Pendente'})});for(const r of linked)await this.logAction('delete','Recibo removido por exclusão de pagamento','Nº '+r.number+' • '+(r.providerName||this.providerName(s.providerId)));await this.logAction('delete','Pagamento excluído; lançamento mantido',this.providerName(s.providerId)+' • '+this.money(s.paidValue||0));await this.load()},
  pay(s){this.paymentTarget=s;this.payment={date:this.today(),value:this.balance(s),method:'PIX'};this.payModal=true},async savePayment(){if(this.isViewer)return;let s=this.paymentTarget,value=Number(this.payment.value||0),remaining=Math.max(0,Number(s.amount||0)-Number(s.paidValue||0));if(value<=0)return this.toast('Informe um valor de pagamento maior que zero.','warning');if(value>remaining+0.000001)return this.toast('O pagamento não pode ser maior que o saldo pendente de '+this.money(remaining)+'.','warning');let paid=Number(s.paidValue||0)+value,status=paid>=Number(s.amount)?'Pago':'Parcial';await db.services.update(s.id,{paidValue:paid,paymentDate:this.payment.date,paymentMethod:this.payment.method,status});await this.logAction('payment','Pagamento registrado',this.providerName(s.providerId)+' • '+this.money(value)+' • '+this.payment.method);this.payModal=false;await this.load();this.toast(status==='Pago'?'Pagamento registrado. Lançamento quitado.':'Pagamento parcial registrado. Saldo restante: '+this.money(Math.max(0,Number(s.amount||0)-paid)))},prepareReceipt(s){this.receiptServiceId=s.id;this.view='recibos';this.after()},async quickReceipt(s){if(this.isViewer)return;if(Number(s.paidValue||0)<=0)return this.toast('Registre o pagamento antes de emitir o recibo.','warning');let linked=this.receipts.filter(r=>Number(r.serviceId)===Number(s.id)),issued=linked.reduce((a,r)=>a+Number(r.total||0),0),pending=Math.max(0,Number(s.paidValue||0)-issued);if(pending<=0){let latest=linked.slice().sort((a,b)=>Number(b.id||0)-Number(a.id||0))[0];if(latest){this.currentReceipt=latest;this.view='recibos';this.after();this.toast('Todos os pagamentos registrados já possuem recibo.','warning');setTimeout(()=>this.printReceipt(),220)}else this.toast('Não há valor pago pendente de recibo.','warning');return}this.receiptServiceId=s.id;await this.issueReceipt();this.view='recibos';this.after();setTimeout(()=>this.printReceipt(),260)},async issueReceipt(){if(this.isViewer)return;let s=this.services.find(x=>Number(x.id)===Number(this.receiptServiceId));if(!s)return this.toast('Selecione um lançamento.','warning');let linked=this.receipts.filter(r=>Number(r.serviceId)===Number(s.id)),issued=linked.reduce((a,r)=>a+Number(r.total||0),0),pending=Math.max(0,Number(s.paidValue||0)-issued);if(pending<=0)return this.toast('Todos os pagamentos registrados deste lançamento já possuem recibo.','warning');let desc=String(s.description||'').replace(/(\bparcela\s+)(\d+)\s*\/\s*(\d+)/i,(m,prefix,current,total)=>prefix+String(linked.length+1).padStart(current.length,'0')+'/'+total);let p=this.providers.find(x=>Number(x.id)===Number(s.providerId))||{};let meta=await db.meta.get('receiptSeq'),next=Math.max(Number(meta?.value||0),this.receiptMaxNumber())+1;await db.meta.put({key:'receiptSeq',value:next});let r={number:String(next).padStart(6,'0'),date:this.today(),paymentDate:s.paymentDate||this.today(),providerId:p.id,providerName:p.name||'Prestador não informado',document:p.document||'',category:s.category,reference:s.reference,description:desc,total:pending,paymentMethod:s.paymentMethod||'Não informado',location:s.location,serviceId:s.id,manager:this.settings.manager,condoName:this.settings.condoName};let id=await db.receipts.add(r);r.id=id;this.currentReceipt=r;await this.logAction('receipt','Recibo emitido','Nº '+r.number+' • '+r.providerName+' • '+this.money(r.total||0));await this.load();this.toast('Recibo nº '+r.number+' emitido no valor de '+this.money(r.total||0)+'.')},printReceipt(){if(!this.currentReceipt&&this.receipts.length)this.currentReceipt=this.receipts[this.receipts.length-1];document.getElementById('receiptPrint').classList.remove('hidden');document.getElementById('reportPrint').classList.add('hidden');document.getElementById('annualReportPrint').classList.add('hidden');setTimeout(()=>window.print(),100)},async removeReceiptInternal(id){let r=this.receipts.find(x=>Number(x.id)===Number(id));if(!r)return;let linked=this.receipts.filter(x=>Number(x.serviceId)===Number(r.serviceId)).sort((a,b)=>Number(b.id||0)-Number(a.id||0)),latest=linked[0];if(latest&&Number(latest.id)!==Number(r.id))return this.toast('Para preservar a data e a forma de pagamento das parcelas, somente o recibo mais recente deste lançamento pode ser excluído e reemitido.','warning');await db.receipts.delete(id);await this.logAction('delete','Recibo excluído',r?('Nº '+r.number+' • '+r.providerName):('ID '+id));await this.load()},
  printReport(){document.body.classList.add('printing-report');document.getElementById('receiptPrint').classList.add('hidden');document.getElementById('annualReportPrint').classList.add('hidden');document.getElementById('reportPrint').classList.remove('hidden');setTimeout(()=>

===== paidValue @ 6485453 =====
xo de pagamentos/recibos para preservar o histórico.':linked?'Este lançamento possui o recibo nº '+linked.number+'. Exclua pelo fluxo de pagamento/recibo para preservar o histórico.':'Este lançamento possui pagamento registrado. Exclua pelo fluxo de pagamentos para preservar o histórico.','warning')}await db.services.delete(id);await this.logAction('delete','Lançamento excluído',this.providerName(s.providerId)+' • '+this.money(s.amount||0));await this.load()},async removePaymentInternal(id){let s=this.services.find(x=>Number(x.id)===Number(id));if(!s)return;let linked=this.receipts.filter(r=>Number(r.serviceId)===Number(id));await db.transaction('rw',db.receipts,db.services,async()=>{for(const r of linked)await db.receipts.delete(r.id);await db.services.update(id,{paidValue:0,paymentDate:'',paymentMethod:'',status:'Pendente'})});for(const r of linked)await this.logAction('delete','Recibo removido por exclusão de pagamento','Nº '+r.number+' • '+(r.providerName||this.providerName(s.providerId)));await this.logAction('delete','Pagamento excluído; lançamento mantido',this.providerName(s.providerId)+' • '+this.money(s.paidValue||0));await this.load()},
  pay(s){this.paymentTarget=s;this.payment={date:this.today(),value:this.balance(s),method:'PIX'};this.payModal=true},async savePayment(){if(this.isViewer)return;let s=this.paymentTarget,value=Number(this.payment.value||0),remaining=Math.max(0,Number(s.amount||0)-Number(s.paidValue||0));if(value<=0)return this.toast('Informe um valor de pagamento maior que zero.','warning');if(value>remaining+0.000001)return this.toast('O pagamento não pode ser maior que o saldo pendente de '+this.money(remaining)+'.','warning');let paid=Number(s.paidValue||0)+value,status=paid>=Number(s.amount)?'Pago':'Parcial';await db.services.update(s.id,{paidValue:paid,paymentDate:this.payment.date,paymentMethod:this.payment.method,status});await this.logAction('payment','Pagamento registrado',this.providerName(s.providerId)+' • '+this.money(value)+' • '+this.payment.method);this.payModal=false;await this.load();this.toast(status==='Pago'?'Pagamento registrado. Lançamento quitado.':'Pagamento parcial registrado. Saldo restante: '+this.money(Math.max(0,Number(s.amount||0)-paid)))},prepareReceipt(s){this.receiptServiceId=s.id;this.view='recibos';this.after()},async quickReceipt(s){if(this.isViewer)return;if(Number(s.paidValue||0)<=0)return this.toast('Registre o pagamento antes de emitir o recibo.','warning');let linked=this.receipts.filter(r=>Number(r.serviceId)===Number(s.id)),issued=linked.reduce((a,r)=>a+Number(r.total||0),0),pending=Math.max(0,Number(s.paidValue||0)-issued);if(pending<=0){let latest=linked.slice().sort((a,b)=>Number(b.id||0)-Number(a.id||0))[0];if(latest){this.currentReceipt=latest;this.view='recibos';this.after();this.toast('Todos os pagamentos registrados já possuem recibo.','warning');setTimeout(()=>this.printReceipt(),220)}else this.toast('Não há valor pago pendente de recibo.','warning');return}this.receiptServiceId=s.id;await this.issueReceipt();this.view='recibos';this.after();setTimeout(()=>this.printReceipt(),260)},async issueReceipt(){if(this.isViewer)return;let s=this.services.find(x=>Number(x.id)===Number(this.receiptServiceId));if(!s)return this.toast('Selecione um lançamento.','warning');let linked=this.receipts.filter(r=>Number(r.serviceId)===Number(s.id)),issued=linked.reduce((a,r)=>a+Number(r.total||0),0),pending=Math.max(0,Number(s.paidValue||0)-issued);if(pending<=0)return this.toast('Todos os pagamentos registrados deste lançamento já possuem recibo.','warning');let desc=String(s.description||'').replace(/(\bparcela\s+)(\d+)\s*\/\s*(\d+)/i,(m,prefix,current,total)=>prefix+String(linked.length+1).padStart(current.length,'0')+'/'+total);let p=this.providers.find(x=>Number(x.id)===Number(s.providerId))||{};let meta=await db.meta.get('receiptSeq'),next=Math.max(Number(meta?.value||0),this.receiptMaxNumber())+1;await db.meta.put({key:'receiptSeq',value:next});let r={number:String(next).padStart(6,'0'),date:this.today(),paymentDate:s.paymentDate||this.today(),providerId:p.id,providerName:p.name||'Prestador não informado',document:p.document||'',category:s.category,reference:s.reference,description:desc,total:pending,paymentMethod:s.paymentMethod||'Não informado',location:s.location,serviceId:s.id,manager:this.settings.manager,condoName:this.settings.condoName};let id=await db.receipts.add(r);r.id=id;this.currentReceipt=r;await this.logAction('receipt','Recibo emitido','Nº '+r.number+' • '+r.providerName+' • '+this.money(r.total||0));await this.load();this.toast('Recibo nº '+r.number+' emitido no valor de '+this.money(r.total||0)+'.')},printReceipt(){if(!this.currentReceipt&&this.receipts.length)this.currentReceipt=this.receipts[this.receipts.length-1];document.getElementById('receiptPrint').classList.remove('hidden');document.getElementById('reportPrint').classList.add('hidden');document.getElementById('annualReportPrint').classList.add('hidden');setTimeout(()=>window.print(),100)},async removeReceiptInternal(id){let r=this.receipts.find(x=>Number(x.id)===Number(id));if(!r)return;let linked=this.receipts.filter(x=>Number(x.serviceId)===Number(r.serviceId)).sort((a,b)=>Number(b.id||0)-Number(a.id||0)),latest=linked[0];if(latest&&Number(latest.id)!==Number(r.id))return this.toast('Para preservar a data e a forma de pagamento das parcelas, somente o recibo mais recente deste lançamento pode ser excluído e reemitido.','warning');await db.receipts.delete(id);await this.logAction('delete','Recibo excluído',r?('Nº '+r.number+' • '+r.providerName):('ID '+id));await this.load()},
  printReport(){document.body.classList.add('printing-report');document.getElementById('receiptPrint').classList.add('hidden');document.getElementById('annualReportPrint').classList.add('hidden');document.getElementById('reportPrint').classList.remove('hidden');setTimeout(()=>{window.print();document.body.classList.remove('printing-report');document.getElementById('receipt

===== paidValue @ 6486024 =====
ked=this.receipts.filter(r=>Number(r.serviceId)===Number(id));await db.transaction('rw',db.receipts,db.services,async()=>{for(const r of linked)await db.receipts.delete(r.id);await db.services.update(id,{paidValue:0,paymentDate:'',paymentMethod:'',status:'Pendente'})});for(const r of linked)await this.logAction('delete','Recibo removido por exclusão de pagamento','Nº '+r.number+' • '+(r.providerName||this.providerName(s.providerId)));await this.logAction('delete','Pagamento excluído; lançamento mantido',this.providerName(s.providerId)+' • '+this.money(s.paidValue||0));await this.load()},
  pay(s){this.paymentTarget=s;this.payment={date:this.today(),value:this.balance(s),method:'PIX'};this.payModal=true},async savePayment(){if(this.isViewer)return;let s=this.paymentTarget,value=Number(this.payment.value||0),remaining=Math.max(0,Number(s.amount||0)-Number(s.paidValue||0));if(value<=0)return this.toast('Informe um valor de pagamento maior que zero.','warning');if(value>remaining+0.000001)return this.toast('O pagamento não pode ser maior que o saldo pendente de '+this.money(remaining)+'.','warning');let paid=Number(s.paidValue||0)+value,status=paid>=Number(s.amount)?'Pago':'Parcial';await db.services.update(s.id,{paidValue:paid,paymentDate:this.payment.date,paymentMethod:this.payment.method,status});await this.logAction('payment','Pagamento registrado',this.providerName(s.providerId)+' • '+this.money(value)+' • '+this.payment.method);this.payModal=false;await this.load();this.toast(status==='Pago'?'Pagamento registrado. Lançamento quitado.':'Pagamento parcial registrado. Saldo restante: '+this.money(Math.max(0,Number(s.amount||0)-paid)))},prepareReceipt(s){this.receiptServiceId=s.id;this.view='recibos';this.after()},async quickReceipt(s){if(this.isViewer)return;if(Number(s.paidValue||0)<=0)return this.toast('Registre o pagamento antes de emitir o recibo.','warning');let linked=this.receipts.filter(r=>Number(r.serviceId)===Number(s.id)),issued=linked.reduce((a,r)=>a+Number(r.total||0),0),pending=Math.max(0,Number(s.paidValue||0)-issued);if(pending<=0){let latest=linked.slice().sort((a,b)=>Number(b.id||0)-Number(a.id||0))[0];if(latest){this.currentReceipt=latest;this.view='recibos';this.after();this.toast('Todos os pagamentos registrados já possuem recibo.','warning');setTimeout(()=>this.printReceipt(),220)}else this.toast('Não há valor pago pendente de recibo.','warning');return}this.receiptServiceId=s.id;await this.issueReceipt();this.view='recibos';this.after();setTimeout(()=>this.printReceipt(),260)},async issueReceipt(){if(this.isViewer)return;let s=this.services.find(x=>Number(x.id)===Number(this.receiptServiceId));if(!s)return this.toast('Selecione um lançamento.','warning');let linked=this.receipts.filter(r=>Number(r.serviceId)===Number(s.id)),issued=linked.reduce((a,r)=>a+Number(r.total||0),0),pending=Math.max(0,Number(s.paidValue||0)-issued);if(pending<=0)return this.toast('Todos os pagamentos registrados deste lançamento já possuem recibo.','warning');let desc=String(s.description||'').replace(/(\bparcela\s+)(\d+)\s*\/\s*(\d+)/i,(m,prefix,current,total)=>prefix+String(linked.length+1).padStart(current.length,'0')+'/'+total);let p=this.providers.find(x=>Number(x.id)===Number(s.providerId))||{};let meta=await db.meta.get('receiptSeq'),next=Math.max(Number(meta?.value||0),this.receiptMaxNumber())+1;await db.meta.put({key:'receiptSeq',value:next});let r={number:String(next).padStart(6,'0'),date:this.today(),paymentDate:s.paymentDate||this.today(),providerId:p.id,providerName:p.name||'Prestador não informado',document:p.document||'',category:s.category,reference:s.reference,description:desc,total:pending,paymentMethod:s.paymentMethod||'Não informado',location:s.location,serviceId:s.id,manager:this.settings.manager,condoName:this.settings.condoName};let id=await db.receipts.add(r);r.id=id;this.currentReceipt=r;await this.logAction('receipt','Recibo emitido','Nº '+r.number+' • '+r.providerName+' • '+this.money(r.total||0));await this.load();this.toast('Recibo nº '+r.number+' emitido no valor de '+this.money(r.total||0)+'.')},printReceipt(){if(!this.currentReceipt&&this.receipts.length)this.currentReceipt=this.receipts[this.receipts.length-1];document.getElementById('receiptPrint').classList.remove('hidden');document.getElementById('reportPrint').classList.add('hidden');document.getElementById('annualReportPrint').classList.add('hidden');setTimeout(()=>window.print(),100)},async removeReceiptInternal(id){let r=this.receipts.find(x=>Number(x.id)===Number(id));if(!r)return;let linked=this.receipts.filter(x=>Number(x.serviceId)===Number(r.serviceId)).sort((a,b)=>Number(b.id||0)-Number(a.id||0)),latest=linked[0];if(latest&&Number(latest.id)!==Number(r.id))return this.toast('Para preservar a data e a forma de pagamento das parcelas, somente o recibo mais recente deste lançamento pode ser excluído e reemitido.','warning');await db.receipts.delete(id);await this.logAction('delete','Recibo excluído',r?('Nº '+r.number+' • '+r.providerName):('ID '+id));await this.load()},
  printReport(){document.body.classList.add('printing-report');document.getElementById('receiptPrint').classList.add('hidden');document.getElementById('annualReportPrint').classList.add('hidden');document.getElementById('reportPrint').classList.remove('hidden');setTimeout(()=>{window.print();document.body.classList.remove('printing-report');document.getElementById('receiptPrint').classList.remove('hidden');document.getElementById('annualReportPrint').classList.add('hidden');document.getElementById('reportPrint').classList.add('hidden')},100)},printAnnualReport(){document.body.classList.add('printing-annual');let rp=document.getElementById('receiptPrint'),mp=document.getElementById('reportPrint'),ap=document.getElementById('annualReportPrint');rp.classList.add('hidden');mp.classList.add('hidden');ap.classList.remove('hidden');ap.style.display='block';ap.style.visibility='visible';setTimeout(()=>{window.print();document.body.classList

===== paidValue @ 6486270 =====
',status:'Pendente'})});for(const r of linked)await this.logAction('delete','Recibo removido por exclusão de pagamento','Nº '+r.number+' • '+(r.providerName||this.providerName(s.providerId)));await this.logAction('delete','Pagamento excluído; lançamento mantido',this.providerName(s.providerId)+' • '+this.money(s.paidValue||0));await this.load()},
  pay(s){this.paymentTarget=s;this.payment={date:this.today(),value:this.balance(s),method:'PIX'};this.payModal=true},async savePayment(){if(this.isViewer)return;let s=this.paymentTarget,value=Number(this.payment.value||0),remaining=Math.max(0,Number(s.amount||0)-Number(s.paidValue||0));if(value<=0)return this.toast('Informe um valor de pagamento maior que zero.','warning');if(value>remaining+0.000001)return this.toast('O pagamento não pode ser maior que o saldo pendente de '+this.money(remaining)+'.','warning');let paid=Number(s.paidValue||0)+value,status=paid>=Number(s.amount)?'Pago':'Parcial';await db.services.update(s.id,{paidValue:paid,paymentDate:this.payment.date,paymentMethod:this.payment.method,status});await this.logAction('payment','Pagamento registrado',this.providerName(s.providerId)+' • '+this.money(value)+' • '+this.payment.method);this.payModal=false;await this.load();this.toast(status==='Pago'?'Pagamento registrado. Lançamento quitado.':'Pagamento parcial registrado. Saldo restante: '+this.money(Math.max(0,Number(s.amount||0)-paid)))},prepareReceipt(s){this.receiptServiceId=s.id;this.view='recibos';this.after()},async quickReceipt(s){if(this.isViewer)return;if(Number(s.paidValue||0)<=0)return this.toast('Registre o pagamento antes de emitir o recibo.','warning');let linked=this.receipts.filter(r=>Number(r.serviceId)===Number(s.id)),issued=linked.reduce((a,r)=>a+Number(r.total||0),0),pending=Math.max(0,Number(s.paidValue||0)-issued);if(pending<=0){let latest=linked.slice().sort((a,b)=>Number(b.id||0)-Number(a.id||0))[0];if(latest){this.currentReceipt=latest;this.view='recibos';this.after();this.toast('Todos os pagamentos registrados já possuem recibo.','warning');setTimeout(()=>this.printReceipt(),220)}else this.toast('Não há valor pago pendente de recibo.','warning');return}this.receiptServiceId=s.id;await this.issueReceipt();this.view='recibos';this.after();setTimeout(()=>this.printReceipt(),260)},async issueReceipt(){if(this.isViewer)return;let s=this.services.find(x=>Number(x.id)===Number(this.receiptServiceId));if(!s)return this.toast('Selecione um lançamento.','warning');let linked=this.receipts.filter(r=>Number(r.serviceId)===Number(s.id)),issued=linked.reduce((a,r)=>a+Number(r.total||0),0),pending=Math.max(0,Number(s.paidValue||0)-issued);if(pending<=0)return this.toast('Todos os pagamentos registrados deste lançamento já possuem recibo.','warning');let desc=String(s.description||'').replace(/(\bparcela\s+)(\d+)\s*\/\s*(\d+)/i,(m,prefix,current,total)=>prefix+String(linked.length+1).padStart(current.length,'0')+'/'+total);let p=this.providers.find(x=>Number(x.id)===Number(s.providerId))||{};let meta=await db.meta.get('receiptSeq'),next=Math.max(Number(meta?.value||0),this.receiptMaxNumber())+1;await db.meta.put({key:'receiptSeq',value:next});let r={number:String(next).padStart(6,'0'),date:this.today(),paymentDate:s.paymentDate||this.today(),providerId:p.id,providerName:p.name||'Prestador não informado',document:p.document||'',category:s.category,reference:s.reference,description:desc,total:pending,paymentMethod:s.paymentMethod||'Não informado',location:s.location,serviceId:s.id,manager:this.settings.manager,condoName:this.settings.condoName};let id=await db.receipts.add(r);r.id=id;this.currentReceipt=r;await this.logAction('receipt','Recibo emitido','Nº '+r.number+' • '+r.providerName+' • '+this.money(r.total||0));await this.load();this.toast('Recibo nº '+r.number+' emitido no valor de '+this.money(r.total||0)+'.')},printReceipt(){if(!this.currentReceipt&&this.receipts.length)this.currentReceipt=this.receipts[this.receipts.length-1];document.getElementById('receiptPrint').classList.remove('hidden');document.getElementById('reportPrint').classList.add('hidden');document.getElementById('annualReportPrint').classList.add('hidden');setTimeout(()=>window.print(),100)},async removeReceiptInternal(id){let r=this.receipts.find(x=>Number(x.id)===Number(id));if(!r)return;let linked=this.receipts.filter(x=>Number(x.serviceId)===Number(r.serviceId)).sort((a,b)=>Number(b.id||0)-Number(a.id||0)),latest=linked[0];if(latest&&Number(latest.id)!==Number(r.id))return this.toast('Para preservar a data e a forma de pagamento das parcelas, somente o recibo mais recente deste lançamento pode ser excluído e reemitido.','warning');await db.receipts.delete(id);await this.logAction('delete','Recibo excluído',r?('Nº '+r.number+' • '+r.providerName):('ID '+id));await this.load()},
  printReport(){document.body.classList.add('printing-report');document.getElementById('receiptPrint').classList.add('hidden');document.getElementById('annualReportPrint').classList.add('hidden');document.getElementById('reportPrint').classList.remove('hidden');setTimeout(()=>{window.print();document.body.classList.remove('printing-report');document.getElementById('receiptPrint').classList.remove('hidden');document.getElementById('annualReportPrint').classList.add('hidden');document.getElementById('reportPrint').classList.add('hidden')},100)},printAnnualReport(){document.body.classList.add('printing-annual');let rp=document.getElementById('receiptPrint'),mp=document.getElementById('reportPrint'),ap=document.getElementById('annualReportPrint');rp.classList.add('hidden');mp.classList.add('hidden');ap.classList.remove('hidden');ap.style.display='block';ap.style.visibility='visible';setTimeout(()=>{window.print();document.body.classList.remove('printing-annual');rp.classList.remove('hidden');ap.classList.add('hidden');ap.style.display='';ap.style.visibility='';mp.classList.add('hidden')},180)},csv(){let rows=[['Recibo nº','Data do serviço','Data do pagamento','Prestador','Categ

===== paidValue @ 6487100 =====
this.money(remaining)+'.','warning');let paid=Number(s.paidValue||0)+value,status=paid>=Number(s.amount)?'Pago':'Parcial';await db.services.update(s.id,{paidValue:paid,paymentDate:this.payment.date,paymentMethod:this.payment.method,status});await this.logAction('payment','Pagamento registrado',this.providerName(s.providerId)+' • '+this.money(value)+' • '+this.payment.method);this.payModal=false;await this.load();this.toast(status==='Pago'?'Pagamento registrado. Lançamento quitado.':'Pagamento parcial registrado. Saldo restante: '+this.money(Math.max(0,Number(s.amount||0)-paid)))},prepareReceipt(s){this.receiptServiceId=s.id;this.view='recibos';this.after()},async quickReceipt(s){if(this.isViewer)return;if(Number(s.paidValue||0)<=0)return this.toast('Registre o pagamento antes de emitir o recibo.','warning');let linked=this.receipts.filter(r=>Number(r.serviceId)===Number(s.id)),issued=linked.reduce((a,r)=>a+Number(r.total||0),0),pending=Math.max(0,Number(s.paidValue||0)-issued);if(pending<=0){let latest=linked.slice().sort((a,b)=>Number(b.id||0)-Number(a.id||0))[0];if(latest){this.currentReceipt=latest;this.view='recibos';this.after();this.toast('Todos os pagamentos registrados já possuem recibo.','warning');setTimeout(()=>this.printReceipt(),220)}else this.toast('Não há valor pago pendente de recibo.','warning');return}this.receiptServiceId=s.id;await this.issueReceipt();this.view='recibos';this.after();setTimeout(()=>this.printReceipt(),260)},async issueReceipt(){if(this.isViewer)return;let s=this.services.find(x=>Number(x.id)===Number(this.receiptServiceId));if(!s)return this.toast('Selecione um lançamento.','warning');let linked=this.receipts.filter(r=>Number(r.serviceId)===Number(s.id)),issued=linked.reduce((a,r)=>a+Number(r.total||0),0),pending=Math.max(0,Number(s.paidValue||0)-issued);if(pending<=0)return this.toast('Todos os pagamentos registrados deste lançamento já possuem recibo.','warning');let desc=String(s.description||'').replace(/(\bparcela\s+)(\d+)\s*\/\s*(\d+)/i,(m,prefix,current,total)=>prefix+String(linked.length+1).padStart(current.length,'0')+'/'+total);let p=this.providers.find(x=>Number(x.id)===Number(s.providerId))||{};let meta=await db.meta.get('receiptSeq'),next=Math.max(Number(meta?.value||0),this.receiptMaxNumber())+1;await db.meta.put({key:'receiptSeq',value:next});let r={number:String(next).padStart(6,'0'),date:this.today(),paymentDate:s.paymentDate||this.today(),providerId:p.id,providerName:p.name||'Prestador não informado',document:p.document||'',category:s.category,reference:s.reference,description:desc,total:pending,paymentMethod:s.paymentMethod||'Não informado',location:s.location,serviceId:s.id,manager:this.settings.manager,condoName:this.settings.condoName};let id=await db.receipts.add(r);r.id=id;this.currentReceipt=r;await this.logAction('receipt','Recibo emitido','Nº '+r.number+' • '+r.providerName+' • '+this.money(r.total||0));await this.load();this.toast('Recibo nº '+r.number+' emitido no valor de '+this.money(r.total||0)+'.')},printReceipt(){if(!this.currentReceipt&&this.receipts.length)this.currentReceipt=this.receipts[this.receipts.length-1];document.getElementById('receiptPrint').classList.remove('hidden');document.getElementById('reportPrint').classList.add('hidden');document.getElementById('annualReportPrint').classList.add('hidden');setTimeout(()=>window.print(),100)},async removeReceiptInternal(id){let r=this.receipts.find(x=>Number(x.id)===Number(id));if(!r)return;let linked=this.receipts.filter(x=>Number(x.serviceId)===Number(r.serviceId)).sort((a,b)=>Number(b.id||0)-Number(a.id||0)),latest=linked[0];if(latest&&Number(latest.id)!==Number(r.id))return this.toast('Para preservar a data e a forma de pagamento das parcelas, somente o recibo mais recente deste lançamento pode ser excluído e reemitido.','warning');await db.receipts.delete(id);await this.logAction('delete','Recibo excluído',r?('Nº '+r.number+' • '+r.providerName):('ID '+id));await this.load()},
  printReport(){document.body.classList.add('printing-report');document.getElementById('receiptPrint').classList.add('hidden');document.getElementById('annualReportPrint').classList.add('hidden');document.getElementById('reportPrint').classList.remove('hidden');setTimeout(()=>{window.print();document.body.classList.remove('printing-report');document.getElementById('receiptPrint').classList.remove('hidden');document.getElementById('annualReportPrint').classList.add('hidden');document.getElementById('reportPrint').classList.add('hidden')},100)},printAnnualReport(){document.body.classList.add('printing-annual');let rp=document.getElementById('receiptPrint'),mp=document.getElementById('reportPrint'),ap=document.getElementById('annualReportPrint');rp.classList.add('hidden');mp.classList.add('hidden');ap.classList.remove('hidden');ap.style.display='block';ap.style.visibility='visible';setTimeout(()=>{window.print();document.body.classList.remove('printing-annual');rp.classList.remove('hidden');ap.classList.add('hidden');ap.style.display='';ap.style.visibility='';mp.classList.add('hidden')},180)},csv(){let rows=[['Recibo nº','Data do serviço','Data do pagamento','Prestador','Categoria','Descrição','Valor do serviço','Valor deste pagamento','Status','Forma']].concat(this.reportDetailRows.map(s=>[s.receiptNumber,s.serviceDate,s.paymentDate||'',this.providerName(s.providerId),s.category,s.description,s.amount,s.reportValue||0,s.status,s.paymentMethod||'']));let csv=rows.map(r=>r.map(v=>'"'+String(v??'').replaceAll('"','""')+'"').join(';')).join('\n');this.download('relatorio-prestadores-recibocondo.csv',csv,'text/csv;charset=utf-8')},
  async saveSettings(){let plainSettings=JSON.parse(JSON.stringify(this.settings));await db.settings.put({key:'main',value:plainSettings});await this.logAction('settings','Configurações salvas','Dados do condomínio atualizados.');this.toast('Configurações salvas')},async savePasswords(){if(this.isViewer)return;let cur=JSON.parse(localStorage.getItem('rc_passwords')||

===== paidValue @ 6495528 =====
afeSeq})});await this.logAction('restore','Backup restaurado','Dados restaurados por arquivo JSON.');await this.load();this.toast('Backup restaurado com sucesso!')}catch(e){console.error(e);this.toast('Não foi possível restaurar o backup: '+e.message,'error')}},async clearAllInternal(){await db.transaction('rw',db.providers,db.services,db.receipts,db.recurrences,db.logs,async()=>{await db.providers.clear();await db.services.clear();await db.receipts.clear();await db.recurrences.clear();await db.logs.clear()});await this.load()},async clearAll(){this.openClearAllModal()},readLogo(e){let f=e.target.files[0];if(!f)return;let r=new FileReader();r.onload=()=>this.settings.logo=r.result;r.readAsDataURL(f)},
  receiptMaxNumber(){return this.receipts.reduce((max,r)=>r.source==='Excel legado'?max:Math.max(max,Number(String(r.number||'').replace(/\D/g,'')||0)),0)},nextReceiptNumber(){return Math.max(Number(this.receiptSeqValue||0),this.receiptMaxNumber())+1},receiptSequenceStatus(){return Number(this.receiptSeqValue||0)<this.receiptMaxNumber()?'Sincronizar':'Sequência OK'},async saveReceiptSequence(){if(this.isViewer)return;let target=Number(this.receiptSeqInput||0), max=this.receiptMaxNumber();if(!target||target<1)return this.toast('Informe um próximo número válido.','warning');if(target<=max)return this.toast('O número precisa ser maior que o último emitido: '+String(max).padStart(6,'0')+'.','warning');await db.meta.put({key:'receiptSeq',value:target-1});this.receiptSeqValue=target-1;await this.logAction('settings','Sequência de recibos ajustada','Próximo recibo definido para nº '+String(target).padStart(6,'0'));this.toast('Sequência atualizada. Próximo recibo: '+String(target).padStart(6,'0'));await this.load()},
  topPaidNames(rows){let map={};rows.forEach(s=>{let v=Number(s.paidValue||0);if(v<=0)return;let name=this.providerName(s.providerId);map[name]=(map[name]||0)+v});return Object.entries(map).sort((a,b)=>b[1]-a[1]).slice(0,3).map(([name,total])=>name+' • '+this.money(total))},topOpenNames(rows){let map={};rows.forEach(s=>{let v=this.balance(s);if(v<=0)return;let name=this.providerName(s.providerId);map[name]=(map[name]||0)+v});return Object.entries(map).sort((a,b)=>b[1]-a[1]).slice(0,3).map(([name,total])=>name+' • '+this.money(total))},
  providerServices(id){return this.services.filter(s=>Number(s.providerId)===Number(id)).sort((a,b)=>String(b.serviceDate||'').localeCompare(String(a.serviceDate||'')))},providerReceipts(id){return this.receipts.filter(r=>Number(r.providerId)===Number(id)).sort((a,b)=>String(b.date||'').localeCompare(String(a.date||'')))},providerLastServiceLabel(id){let s=this.providerServices(id)[0];return s?this.fmt(s.serviceDate)+' • '+this.money(s.paidValue||s.amount):'-'},async logAction(type,title,detail){try{await db.logs.add({at:new Date().toISOString(),type,title,detail,role:this.auth?.role||'sistema'});this.logs=(await db.logs.orderBy('at').reverse().limit(30).toArray())}catch(e){console.warn('logAction',e)}},logIcon(type){return {payment:'circle-dollar-sign',receipt:'receipt-text',service:'clipboard-list',provider:'users',recurrence:'repeat-2',backup:'shield-check',restore:'rotate-ccw',danger:'triangle-alert',settings:'settings',delete:'trash-2'}[type]||'history'},fmtDateTime(d){if(!d)return'-';return new Date(d).toLocaleString('pt-BR')},async clearLogs(){if(this.isViewer)return;await db.logs.clear();this.logs=[];this.after()},providerSpend(id){return this.services.filter(s=>Number(s.providerId)===Number(id)).reduce((a,s)=>a+Number(s.paidValue||s.amount||0),0)},providerOpen(id){return this.services.filter(s=>Number(s.providerId)===Number(id)).reduce((a,s)=>a+this.balance(s),0)},openWhatsapp(p){let raw=String(p.whatsapp||p.phone||'').replace(/\D/g,'');if(!raw)return this.toast('Este prestador não tem WhatsApp/telefone cadastrado.','warning');let phone=raw.startsWith('55')?raw:'55'+raw;let msg=encodeURIComponent('Olá, '+p.name+'. Aqui é do '+this.settings.condoName+'.');window.open('https://wa.me/'+phone+'?text='+msg,'_blank')},overdueLabel(s){if(!s.dueDate||this.balance(s)<=0)return'';let today=new Date(this.today()+'T00:00:00'), due=new Date(s.dueDate+'T00:00:00'), days=Math.ceil((due-today)/86400000);if(days<0)return Math.abs(days)+' dia(s) em atraso';if(days===0)return'vence hoje';return'faltam '+days+' dia(s)'},providerName(id){return (this.providers.find(p=>Number(p.id)===Number(id))||{}).name||'-'},reportReceiptNumber(service){let r=this.receipts.find(x=>Number(x.serviceId)===Number(service.id));return r?.number||'-';},balance(s){return Math.max(0,Number(s.amount||0)-Number(s.paidValue||0))},statusClass(st){return st==='Pago'?'bg-emerald-500/10 text-emerald-700':st==='Parcial'?'bg-amber-500/10 text-amber-700':'bg-red-500/10 text-red-700'},valorPorExtenso(v){v=Number(v||0);let reais=Math.floor(v),cent=Math.round((v-reais)*100);let base={0:'zero',1:'um',2:'dois',3:'três',4:'quatro',5:'cinco',6:'seis',7:'sete',8:'oito',9:'nove',10:'dez',11:'onze',12:'doze',13:'treze',14:'quatorze',15:'quinze',16:'dezesseis',17:'dezessete',18:'dezoito',19:'dezenove',20:'vinte',30:'trinta',40:'quarenta',50:'cinquenta',60:'sessenta',70:'setenta',80:'oitenta',90:'noventa',100:'cem'};let ext=(n)=>{if(n<=20||base[n])return base[n];if(n<100){let d=Math.floor(n/10)*10,u=n%10;return base[d]+(u?' e '+base[u]:'')}if(n<1000){let c=Math.floor(n/100),r=n%100,cn=c===1?'cento':base[c]+' centos';cn=cn.replace('dois centos','duzentos').replace('três centos','trezentos').replace('quatro centos','quatrocentos').replace('cinco centos','quinhentos').replace('seis centos','seiscentos').replace('sete centos','setecentos').replace('oito centos','oitocentos').replace('nove centos','novecentos');return r?cn+' e '+ext(r):cn}if(n<1000000){let m=Math.floor(n/1000),r=n%1000;return (m===1?'mil':ext(m)+' mil')+(r?' e '+ext(r):'')}return n.toLocaleString('pt-BR')};let txt=ext(reais)+' '+(reais===1?'real':'reais');if(cent)txt+=' e '+ext(cent)+' '+(cent===1?'centavo':'centavos');return txt;},money(v){return N

===== paidValue @ 6496446 =====
eiptSeqValue||0),this.receiptMaxNumber())+1},receiptSequenceStatus(){return Number(this.receiptSeqValue||0)<this.receiptMaxNumber()?'Sincronizar':'Sequência OK'},async saveReceiptSequence(){if(this.isViewer)return;let target=Number(this.receiptSeqInput||0), max=this.receiptMaxNumber();if(!target||target<1)return this.toast('Informe um próximo número válido.','warning');if(target<=max)return this.toast('O número precisa ser maior que o último emitido: '+String(max).padStart(6,'0')+'.','warning');await db.meta.put({key:'receiptSeq',value:target-1});this.receiptSeqValue=target-1;await this.logAction('settings','Sequência de recibos ajustada','Próximo recibo definido para nº '+String(target).padStart(6,'0'));this.toast('Sequência atualizada. Próximo recibo: '+String(target).padStart(6,'0'));await this.load()},
  topPaidNames(rows){let map={};rows.forEach(s=>{let v=Number(s.paidValue||0);if(v<=0)return;let name=this.providerName(s.providerId);map[name]=(map[name]||0)+v});return Object.entries(map).sort((a,b)=>b[1]-a[1]).slice(0,3).map(([name,total])=>name+' • '+this.money(total))},topOpenNames(rows){let map={};rows.forEach(s=>{let v=this.balance(s);if(v<=0)return;let name=this.providerName(s.providerId);map[name]=(map[name]||0)+v});return Object.entries(map).sort((a,b)=>b[1]-a[1]).slice(0,3).map(([name,total])=>name+' • '+this.money(total))},
  providerServices(id){return this.services.filter(s=>Number(s.providerId)===Number(id)).sort((a,b)=>String(b.serviceDate||'').localeCompare(String(a.serviceDate||'')))},providerReceipts(id){return this.receipts.filter(r=>Number(r.providerId)===Number(id)).sort((a,b)=>String(b.date||'').localeCompare(String(a.date||'')))},providerLastServiceLabel(id){let s=this.providerServices(id)[0];return s?this.fmt(s.serviceDate)+' • '+this.money(s.paidValue||s.amount):'-'},async logAction(type,title,detail){try{await db.logs.add({at:new Date().toISOString(),type,title,detail,role:this.auth?.role||'sistema'});this.logs=(await db.logs.orderBy('at').reverse().limit(30).toArray())}catch(e){console.warn('logAction',e)}},logIcon(type){return {payment:'circle-dollar-sign',receipt:'receipt-text',service:'clipboard-list',provider:'users',recurrence:'repeat-2',backup:'shield-check',restore:'rotate-ccw',danger:'triangle-alert',settings:'settings',delete:'trash-2'}[type]||'history'},fmtDateTime(d){if(!d)return'-';return new Date(d).toLocaleString('pt-BR')},async clearLogs(){if(this.isViewer)return;await db.logs.clear();this.logs=[];this.after()},providerSpend(id){return this.services.filter(s=>Number(s.providerId)===Number(id)).reduce((a,s)=>a+Number(s.paidValue||s.amount||0),0)},providerOpen(id){return this.services.filter(s=>Number(s.providerId)===Number(id)).reduce((a,s)=>a+this.balance(s),0)},openWhatsapp(p){let raw=String(p.whatsapp||p.phone||'').replace(/\D/g,'');if(!raw)return this.toast('Este prestador não tem WhatsApp/telefone cadastrado.','warning');let phone=raw.startsWith('55')?raw:'55'+raw;let msg=encodeURIComponent('Olá, '+p.name+'. Aqui é do '+this.settings.condoName+'.');window.open('https://wa.me/'+phone+'?text='+msg,'_blank')},overdueLabel(s){if(!s.dueDate||this.balance(s)<=0)return'';let today=new Date(this.today()+'T00:00:00'), due=new Date(s.dueDate+'T00:00:00'), days=Math.ceil((due-today)/86400000);if(days<0)return Math.abs(days)+' dia(s) em atraso';if(days===0)return'vence hoje';return'faltam '+days+' dia(s)'},providerName(id){return (this.providers.find(p=>Number(p.id)===Number(id))||{}).name||'-'},reportReceiptNumber(service){let r=this.receipts.find(x=>Number(x.serviceId)===Number(service.id));return r?.number||'-';},balance(s){return Math.max(0,Number(s.amount||0)-Number(s.paidValue||0))},statusClass(st){return st==='Pago'?'bg-emerald-500/10 text-emerald-700':st==='Parcial'?'bg-amber-500/10 text-amber-700':'bg-red-500/10 text-red-700'},valorPorExtenso(v){v=Number(v||0);let reais=Math.floor(v),cent=Math.round((v-reais)*100);let base={0:'zero',1:'um',2:'dois',3:'três',4:'quatro',5:'cinco',6:'seis',7:'sete',8:'oito',9:'nove',10:'dez',11:'onze',12:'doze',13:'treze',14:'quatorze',15:'quinze',16:'dezesseis',17:'dezessete',18:'dezoito',19:'dezenove',20:'vinte',30:'trinta',40:'quarenta',50:'cinquenta',60:'sessenta',70:'setenta',80:'oitenta',90:'noventa',100:'cem'};let ext=(n)=>{if(n<=20||base[n])return base[n];if(n<100){let d=Math.floor(n/10)*10,u=n%10;return base[d]+(u?' e '+base[u]:'')}if(n<1000){let c=Math.floor(n/100),r=n%100,cn=c===1?'cento':base[c]+' centos';cn=cn.replace('dois centos','duzentos').replace('três centos','trezentos').replace('quatro centos','quatrocentos').replace('cinco centos','quinhentos').replace('seis centos','seiscentos').replace('sete centos','setecentos').replace('oito centos','oitocentos').replace('nove centos','novecentos');return r?cn+' e '+ext(r):cn}if(n<1000000){let m=Math.floor(n/1000),r=n%1000;return (m===1?'mil':ext(m)+' mil')+(r?' e '+ext(r):'')}return n.toLocaleString('pt-BR')};let txt=ext(reais)+' '+(reais===1?'real':'reais');if(cent)txt+=' e '+ext(cent)+' '+(cent===1?'centavo':'centavos');return txt;},money(v){return Number(v||0).toLocaleString('pt-BR',{style:'currency',currency:'BRL'})},today(){return new Date().toISOString().slice(0,10)},fmt(d){if(!d)return'-';return new Date(d+'T00:00:00').toLocaleDateString('pt-BR')},month(m){if(!m)return'-';let [y,mo]=String(m).split('-');return new Date(Number(y),Number(mo)-1,1).toLocaleDateString('pt-BR',{month:'long',year:'numeric'})},reportMonthLabel(){let src=(this.reportRows[0]?.reference)||String(this.report.from||this.today()).slice(0,7);let label=this.month(src);return String(label).replace(/ de \d{4}/,'').toUpperCase();},download(n,c,t){let a=document.createElement('a');a.href=URL.createObjectURL(new Blob([c],{type:t}));a.download=n;a.click();URL.revokeObjectURL(a.href)},refreshIcons(){setTimeout(()=>window.lucide&&lucide.createIcons(),60)},after(){this.$nextTick(()=>{this.enhanceAccordions();this.refreshIcons();this.renderCharts();setTimeout(()=>this.resizeCharts(),120)

===== paidValue @ 6497255 =====
load()},
  topPaidNames(rows){let map={};rows.forEach(s=>{let v=Number(s.paidValue||0);if(v<=0)return;let name=this.providerName(s.providerId);map[name]=(map[name]||0)+v});return Object.entries(map).sort((a,b)=>b[1]-a[1]).slice(0,3).map(([name,total])=>name+' • '+this.money(total))},topOpenNames(rows){let map={};rows.forEach(s=>{let v=this.balance(s);if(v<=0)return;let name=this.providerName(s.providerId);map[name]=(map[name]||0)+v});return Object.entries(map).sort((a,b)=>b[1]-a[1]).slice(0,3).map(([name,total])=>name+' • '+this.money(total))},
  providerServices(id){return this.services.filter(s=>Number(s.providerId)===Number(id)).sort((a,b)=>String(b.serviceDate||'').localeCompare(String(a.serviceDate||'')))},providerReceipts(id){return this.receipts.filter(r=>Number(r.providerId)===Number(id)).sort((a,b)=>String(b.date||'').localeCompare(String(a.date||'')))},providerLastServiceLabel(id){let s=this.providerServices(id)[0];return s?this.fmt(s.serviceDate)+' • '+this.money(s.paidValue||s.amount):'-'},async logAction(type,title,detail){try{await db.logs.add({at:new Date().toISOString(),type,title,detail,role:this.auth?.role||'sistema'});this.logs=(await db.logs.orderBy('at').reverse().limit(30).toArray())}catch(e){console.warn('logAction',e)}},logIcon(type){return {payment:'circle-dollar-sign',receipt:'receipt-text',service:'clipboard-list',provider:'users',recurrence:'repeat-2',backup:'shield-check',restore:'rotate-ccw',danger:'triangle-alert',settings:'settings',delete:'trash-2'}[type]||'history'},fmtDateTime(d){if(!d)return'-';return new Date(d).toLocaleString('pt-BR')},async clearLogs(){if(this.isViewer)return;await db.logs.clear();this.logs=[];this.after()},providerSpend(id){return this.services.filter(s=>Number(s.providerId)===Number(id)).reduce((a,s)=>a+Number(s.paidValue||s.amount||0),0)},providerOpen(id){return this.services.filter(s=>Number(s.providerId)===Number(id)).reduce((a,s)=>a+this.balance(s),0)},openWhatsapp(p){let raw=String(p.whatsapp||p.phone||'').replace(/\D/g,'');if(!raw)return this.toast('Este prestador não tem WhatsApp/telefone cadastrado.','warning');let phone=raw.startsWith('55')?raw:'55'+raw;let msg=encodeURIComponent('Olá, '+p.name+'. Aqui é do '+this.settings.condoName+'.');window.open('https://wa.me/'+phone+'?text='+msg,'_blank')},overdueLabel(s){if(!s.dueDate||this.balance(s)<=0)return'';let today=new Date(this.today()+'T00:00:00'), due=new Date(s.dueDate+'T00:00:00'), days=Math.ceil((due-today)/86400000);if(days<0)return Math.abs(days)+' dia(s) em atraso';if(days===0)return'vence hoje';return'faltam '+days+' dia(s)'},providerName(id){return (this.providers.find(p=>Number(p.id)===Number(id))||{}).name||'-'},reportReceiptNumber(service){let r=this.receipts.find(x=>Number(x.serviceId)===Number(service.id));return r?.number||'-';},balance(s){return Math.max(0,Number(s.amount||0)-Number(s.paidValue||0))},statusClass(st){return st==='Pago'?'bg-emerald-500/10 text-emerald-700':st==='Parcial'?'bg-amber-500/10 text-amber-700':'bg-red-500/10 text-red-700'},valorPorExtenso(v){v=Number(v||0);let reais=Math.floor(v),cent=Math.round((v-reais)*100);let base={0:'zero',1:'um',2:'dois',3:'três',4:'quatro',5:'cinco',6:'seis',7:'sete',8:'oito',9:'nove',10:'dez',11:'onze',12:'doze',13:'treze',14:'quatorze',15:'quinze',16:'dezesseis',17:'dezessete',18:'dezoito',19:'dezenove',20:'vinte',30:'trinta',40:'quarenta',50:'cinquenta',60:'sessenta',70:'setenta',80:'oitenta',90:'noventa',100:'cem'};let ext=(n)=>{if(n<=20||base[n])return base[n];if(n<100){let d=Math.floor(n/10)*10,u=n%10;return base[d]+(u?' e '+base[u]:'')}if(n<1000){let c=Math.floor(n/100),r=n%100,cn=c===1?'cento':base[c]+' centos';cn=cn.replace('dois centos','duzentos').replace('três centos','trezentos').replace('quatro centos','quatrocentos').replace('cinco centos','quinhentos').replace('seis centos','seiscentos').replace('sete centos','setecentos').replace('oito centos','oitocentos').replace('nove centos','novecentos');return r?cn+' e '+ext(r):cn}if(n<1000000){let m=Math.floor(n/1000),r=n%1000;return (m===1?'mil':ext(m)+' mil')+(r?' e '+ext(r):'')}return n.toLocaleString('pt-BR')};let txt=ext(reais)+' '+(reais===1?'real':'reais');if(cent)txt+=' e '+ext(cent)+' '+(cent===1?'centavo':'centavos');return txt;},money(v){return Number(v||0).toLocaleString('pt-BR',{style:'currency',currency:'BRL'})},today(){return new Date().toISOString().slice(0,10)},fmt(d){if(!d)return'-';return new Date(d+'T00:00:00').toLocaleDateString('pt-BR')},month(m){if(!m)return'-';let [y,mo]=String(m).split('-');return new Date(Number(y),Number(mo)-1,1).toLocaleDateString('pt-BR',{month:'long',year:'numeric'})},reportMonthLabel(){let src=(this.reportRows[0]?.reference)||String(this.report.from||this.today()).slice(0,7);let label=this.month(src);return String(label).replace(/ de \d{4}/,'').toUpperCase();},download(n,c,t){let a=document.createElement('a');a.href=URL.createObjectURL(new Blob([c],{type:t}));a.download=n;a.click();URL.revokeObjectURL(a.href)},refreshIcons(){setTimeout(()=>window.lucide&&lucide.createIcons(),60)},after(){this.$nextTick(()=>{this.enhanceAccordions();this.refreshIcons();this.renderCharts();setTimeout(()=>this.resizeCharts(),120)})},enhanceAccordions(){try{const scopes=['prestadores','servicos','pagamentos','recibos','busca','config'];scopes.forEach(scope=>{const section=[...document.querySelectorAll('main section')].find(s=>(s.getAttribute('x-show')||'').includes(scope));if(!section)return;const panels=[...section.children].filter(el=>el.classList&&el.classList.contains('glass'));panels.forEach((panel,idx)=>{if(panel.dataset.rcAccordion==='1')return;panel.dataset.rcAccordion='1';panel.classList.add('rc-accordion-panel');const titleEl=panel.querySelector('h3,h2,h4');const title=(titleEl?.textContent||({prestadores:'Prestadores',servicos:'Serviços',pagamentos:'Pagamentos',recibos:'Recibos',busca:'Busca global',config:'Configurações'}[scope]||'Painel')).trim();const subEl=panel.querySelector('p.text-sm,p.text-xs');const sub=

===== paidValue @ 6498323 =====
s.add({at:new Date().toISOString(),type,title,detail,role:this.auth?.role||'sistema'});this.logs=(await db.logs.orderBy('at').reverse().limit(30).toArray())}catch(e){console.warn('logAction',e)}},logIcon(type){return {payment:'circle-dollar-sign',receipt:'receipt-text',service:'clipboard-list',provider:'users',recurrence:'repeat-2',backup:'shield-check',restore:'rotate-ccw',danger:'triangle-alert',settings:'settings',delete:'trash-2'}[type]||'history'},fmtDateTime(d){if(!d)return'-';return new Date(d).toLocaleString('pt-BR')},async clearLogs(){if(this.isViewer)return;await db.logs.clear();this.logs=[];this.after()},providerSpend(id){return this.services.filter(s=>Number(s.providerId)===Number(id)).reduce((a,s)=>a+Number(s.paidValue||s.amount||0),0)},providerOpen(id){return this.services.filter(s=>Number(s.providerId)===Number(id)).reduce((a,s)=>a+this.balance(s),0)},openWhatsapp(p){let raw=String(p.whatsapp||p.phone||'').replace(/\D/g,'');if(!raw)return this.toast('Este prestador não tem WhatsApp/telefone cadastrado.','warning');let phone=raw.startsWith('55')?raw:'55'+raw;let msg=encodeURIComponent('Olá, '+p.name+'. Aqui é do '+this.settings.condoName+'.');window.open('https://wa.me/'+phone+'?text='+msg,'_blank')},overdueLabel(s){if(!s.dueDate||this.balance(s)<=0)return'';let today=new Date(this.today()+'T00:00:00'), due=new Date(s.dueDate+'T00:00:00'), days=Math.ceil((due-today)/86400000);if(days<0)return Math.abs(days)+' dia(s) em atraso';if(days===0)return'vence hoje';return'faltam '+days+' dia(s)'},providerName(id){return (this.providers.find(p=>Number(p.id)===Number(id))||{}).name||'-'},reportReceiptNumber(service){let r=this.receipts.find(x=>Number(x.serviceId)===Number(service.id));return r?.number||'-';},balance(s){return Math.max(0,Number(s.amount||0)-Number(s.paidValue||0))},statusClass(st){return st==='Pago'?'bg-emerald-500/10 text-emerald-700':st==='Parcial'?'bg-amber-500/10 text-amber-700':'bg-red-500/10 text-red-700'},valorPorExtenso(v){v=Number(v||0);let reais=Math.floor(v),cent=Math.round((v-reais)*100);let base={0:'zero',1:'um',2:'dois',3:'três',4:'quatro',5:'cinco',6:'seis',7:'sete',8:'oito',9:'nove',10:'dez',11:'onze',12:'doze',13:'treze',14:'quatorze',15:'quinze',16:'dezesseis',17:'dezessete',18:'dezoito',19:'dezenove',20:'vinte',30:'trinta',40:'quarenta',50:'cinquenta',60:'sessenta',70:'setenta',80:'oitenta',90:'noventa',100:'cem'};let ext=(n)=>{if(n<=20||base[n])return base[n];if(n<100){let d=Math.floor(n/10)*10,u=n%10;return base[d]+(u?' e '+base[u]:'')}if(n<1000){let c=Math.floor(n/100),r=n%100,cn=c===1?'cento':base[c]+' centos';cn=cn.replace('dois centos','duzentos').replace('três centos','trezentos').replace('quatro centos','quatrocentos').replace('cinco centos','quinhentos').replace('seis centos','seiscentos').replace('sete centos','setecentos').replace('oito centos','oitocentos').replace('nove centos','novecentos');return r?cn+' e '+ext(r):cn}if(n<1000000){let m=Math.floor(n/1000),r=n%1000;return (m===1?'mil':ext(m)+' mil')+(r?' e '+ext(r):'')}return n.toLocaleString('pt-BR')};let txt=ext(reais)+' '+(reais===1?'real':'reais');if(cent)txt+=' e '+ext(cent)+' '+(cent===1?'centavo':'centavos');return txt;},money(v){return Number(v||0).toLocaleString('pt-BR',{style:'currency',currency:'BRL'})},today(){return new Date().toISOString().slice(0,10)},fmt(d){if(!d)return'-';return new Date(d+'T00:00:00').toLocaleDateString('pt-BR')},month(m){if(!m)return'-';let [y,mo]=String(m).split('-');return new Date(Number(y),Number(mo)-1,1).toLocaleDateString('pt-BR',{month:'long',year:'numeric'})},reportMonthLabel(){let src=(this.reportRows[0]?.reference)||String(this.report.from||this.today()).slice(0,7);let label=this.month(src);return String(label).replace(/ de \d{4}/,'').toUpperCase();},download(n,c,t){let a=document.createElement('a');a.href=URL.createObjectURL(new Blob([c],{type:t}));a.download=n;a.click();URL.revokeObjectURL(a.href)},refreshIcons(){setTimeout(()=>window.lucide&&lucide.createIcons(),60)},after(){this.$nextTick(()=>{this.enhanceAccordions();this.refreshIcons();this.renderCharts();setTimeout(()=>this.resizeCharts(),120)})},enhanceAccordions(){try{const scopes=['prestadores','servicos','pagamentos','recibos','busca','config'];scopes.forEach(scope=>{const section=[...document.querySelectorAll('main section')].find(s=>(s.getAttribute('x-show')||'').includes(scope));if(!section)return;const panels=[...section.children].filter(el=>el.classList&&el.classList.contains('glass'));panels.forEach((panel,idx)=>{if(panel.dataset.rcAccordion==='1')return;panel.dataset.rcAccordion='1';panel.classList.add('rc-accordion-panel');const titleEl=panel.querySelector('h3,h2,h4');const title=(titleEl?.textContent||({prestadores:'Prestadores',servicos:'Serviços',pagamentos:'Pagamentos',recibos:'Recibos',busca:'Busca global',config:'Configurações'}[scope]||'Painel')).trim();const subEl=panel.querySelector('p.text-sm,p.text-xs');const sub=(subEl?.textContent||'Clique para abrir ou recolher este painel.').trim();const body=document.createElement('div');body.className='rc-accordion-body';while(panel.firstChild)body.appendChild(panel.firstChild);const head=document.createElement('button');head.type='button';head.className='rc-accordion-head no-print';head.innerHTML=`<span><span class="rc-accordion-kicker">${scope==='config'?'organização':'painel premium'}</span><span class="rc-accordion-title">${title}</span><span class="rc-accordion-sub">${sub}</span></span><span class="rc-accordion-icon"><i data-lucide="chevron-down" class="w-5 h-5"></i></span>`;head.addEventListener('click',()=>{panel.classList.toggle('rc-open');localStorage.setItem('rc_acc_'+scope+'_'+idx,panel.classList.contains('rc-open')?'1':'0');setTimeout(()=>this.resizeCharts(),90);this.refreshIcons()});panel.appendChild(head);panel.appendChild(body);let saved=localStorage.getItem('rc_acc_'+scope+'_'+idx);if(saved!==null?saved==='1':(idx===0||scope==='busca'||scope==='prestadores'))panel.classList.add('rc-open');});});}catch(e){c

===== paidValue @ 6504181 =====
+scope+'_'+idx);if(saved!==null?saved==='1':(idx===0||scope==='busca'||scope==='prestadores'))panel.classList.add('rc-open');});});}catch(e){console.warn('enhanceAccordions',e)}},resizeCharts(){Object.values(this.charts||{}).forEach(ch=>{try{ch.resize()}catch(e){}})},renderCharts(){if(!this.auth.logged||!window.Chart)return;let baseOpts={responsive:true,maintainAspectRatio:false,animation:{duration:250},plugins:{legend:{position:'bottom'}}};let monthly=this.monthlyHistory, mEl=document.getElementById('monthlyChart');if(mEl){if(this.charts.monthly)this.charts.monthly.destroy();this.charts.monthly=new Chart(mEl,{type:'bar',data:{labels:monthly.map(x=>x.label.slice(0,3)),datasets:[{label:'Total pago',data:monthly.map(x=>x.paid)},{label:'Em aberto',data:monthly.map(x=>x.open)}]},options:{...baseOpts,scales:{y:{beginAtZero:true}}}})}let cats=this.annualCategorySummary.slice(0,8), cEl=document.getElementById('annualCatChart');if(cEl){if(this.charts.annualCat)this.charts.annualCat.destroy();this.charts.annualCat=new Chart(cEl,{type:'doughnut',data:{labels:cats.map(x=>x.category),datasets:[{label:'Pago',data:cats.map(x=>x.total)}]},options:baseOpts})}let months=[...new Set(this.reportRows.map(s=>s.reference))].sort(), er=document.getElementById('repChart');if(er){if(this.charts.rep){this.charts.rep.destroy();this.charts.rep=null}er.removeAttribute('height');er.removeAttribute('width');er.style.height='240px';er.style.maxHeight='240px';er.style.width='100%';this.charts.rep=new Chart(er,{type:'line',data:{labels:months.map(m=>this.month(m)),datasets:[{label:'Lançado',data:months.map(m=>this.reportRows.filter(s=>s.reference===m).reduce((a,s)=>a+Number(s.amount||0),0)),tension:.35},{label:'Pago',data:months.map(m=>this.reportRows.filter(s=>s.reference===m).reduce((a,s)=>a+Number(s.paidValue||0),0)),tension:.35}]},options:{responsive:true,maintainAspectRatio:false,resizeDelay:150,animation:false,plugins:{legend:{position:'bottom'}},scales:{y:{beginAtZero:true}}}})}}
}}
</script>

<script id="rc-qrcode-script">
(function(){
  function createReceiptAuth(){
    const codeEl = document.getElementById('receiptAuthCode');
    const qrEl = document.getElementById('receiptQrCode');
    if(!codeEl || !qrEl || typeof QRCode === 'undefined') return;

    const stamp = new Date();
    const code = 'RC-' +
      stamp.getFullYear() + '-' +
      String(stamp.getMonth()+1).padStart(2,'0') +
      String(stamp.getDate()).padStart(2,'0') + '-' +
      Math.floor(Math.random()*9000+1000);

    codeEl.textContent = code;

    qrEl.innerHTML = '';

    new QRCode(qrEl,{
      text:'ReciboCondo|' + code + '|Emitido em ' + stamp.toLocaleString('pt-BR'),
      width:76,
      height:76
    });
  }

  if(document.readyState === 'loading'){
    document.addEventListener('DOMContentLoaded', createReceiptAuth);
  }else{
    createReceiptAuth();
  }
})();
</script>


<script id="rc-qrcode-force-visible">
(function(){
function buildQr(){
  const codeEl=document.getElementById('receiptAuthCodeVisible');
  const qrEl=document.getElementById('receiptQrCodeVisible');

  if(!codeEl || !qrEl) return;

  const now=new Date();
  const code='RC-'+now.getFullYear()+'-'+String(now.getMonth()+1).padStart(2,'0')+
  String(now.getDate()).padStart(2,'0')+'-'+Math.floor(Math.random()*90000+10000);

  codeEl.textContent=code;

  qrEl.innerHTML='';

  if(typeof QRCode!=='undefined'){
      new QRCode(qrEl,{
      text:'ReciboCondo|' + code,
      width:94,
      height:94
    });
  }else{
    qrEl.innerHTML='<div style="font-size:11px;font-weight:700;text-align:center;color:#0F2A3A">QR indisponível</div>';
  }
}
window.addEventListener('load',buildQr);
setTimeout(buildQr,1200);
})();
</script>


<script id="rc-report-logo-sync">
window.addEventListener('load',function(){
  try{
    const existingLogo=document.querySelector('.rc-icon,.receipt-logo img,.app-logo img,img');
    const reportLogo=document.getElementById('rcReportLogo');

    if(existingLogo && reportLogo && existingLogo.src){
      reportLogo.src=existingLogo.src;
    }
  }catch(e){}
});
</script>

</body>
</html>


===== receipts.reduce @ 6493512 =====
ISOString();let seqMeta=await db.meta.get('receiptSeq');let data={settings:this.settings,providers:this.providers,services:this.services,receipts:this.receipts,recurrences:this.recurrences,logs:this.logs,receiptSeq:Number(seqMeta?.value||this.receiptMaxNumber()||0),backupStats:this.backupStats(),exportedAt:now};this.download('backup-recibocondo-prestadores.json',JSON.stringify(data,null,2),'application/json');this.backupLast=now;localStorage.setItem('rc_last_backup',now);await this.logAction('backup','Backup exportado','Backup JSON gerado com '+data.backupStats.records+' registros.')},async restore(ev){let f=ev.target.files[0];if(!f)return;try{let data=JSON.parse(await f.text());if(!data||typeof data!=='object')throw new Error('Backup inválido');let providers=Array.isArray(data.providers)?data.providers:[],services=Array.isArray(data.services)?data.services:[],receipts=Array.isArray(data.receipts)?data.receipts:[],recurrences=Array.isArray(data.recurrences)?data.recurrences:[],logs=Array.isArray(data.logs)?data.logs:[];await db.transaction('rw',db.providers,db.services,db.receipts,db.recurrences,db.logs,db.settings,db.meta,async()=>{await db.providers.clear();await db.services.clear();await db.receipts.clear();await db.recurrences.clear();await db.logs.clear();if(data.settings){let restoredSettings=JSON.parse(JSON.stringify(data.settings));await db.settings.put({key:'main',value:restoredSettings});this.settings=restoredSettings}if(providers.length)await db.providers.bulkAdd(providers);if(services.length)await db.services.bulkAdd(services);if(receipts.length)await db.receipts.bulkAdd(receipts);if(recurrences.length)await db.recurrences.bulkAdd(recurrences);if(logs.length)await db.logs.bulkAdd(logs.map(l=>{let x={...l};delete x.id;return x}).slice(0,200));let restoredMax=receipts.reduce((m,r)=>r.source==='Excel legado'?m:Math.max(m,Number(String(r.number||'').replace(/\D/g,'')||0)),0),safeSeq=Math.max(Number(data.receiptSeq||0),restoredMax);await db.meta.put({key:'receiptSeq',value:safeSeq})});await this.logAction('restore','Backup restaurado','Dados restaurados por arquivo JSON.');await this.load();this.toast('Backup restaurado com sucesso!')}catch(e){console.error(e);this.toast('Não foi possível restaurar o backup: '+e.message,'error')}},async clearAllInternal(){await db.transaction('rw',db.providers,db.services,db.receipts,db.recurrences,db.logs,async()=>{await db.providers.clear();await db.services.clear();await db.receipts.clear();await db.recurrences.clear();await db.logs.clear()});await this.load()},async clearAll(){this.openClearAllModal()},readLogo(e){let f=e.target.files[0];if(!f)return;let r=new FileReader();r.onload=()=>this.settings.logo=r.result;r.readAsDataURL(f)},
  receiptMaxNumber(){return this.receipts.reduce((max,r)=>r.source==='Excel legado'?max:Math.max(max,Number(String(r.number||'').replace(/\D/g,'')||0)),0)},nextReceiptNumber(){return Math.max(Number(this.receiptSeqValue||0),this.receiptMaxNumber())+1},receiptSequenceStatus(){return Number(this.receiptSeqValue||0)<this.receiptMaxNumber()?'Sincronizar':'Sequência OK'},async saveReceiptSequence(){if(this.isViewer)return;let target=Number(this.receiptSeqInput||0), max=this.receiptMaxNumber();if(!target||target<1)return this.toast('Informe um próximo número válido.','warning');if(target<=max)return this.toast('O número precisa ser maior que o último emitido: '+String(max).padStart(6,'0')+'.','warning');await db.meta.put({key:'receiptSeq',value:target-1});this.receiptSeqValue=target-1;await this.logAction('settings','Sequência de recibos ajustada','Próximo recibo definido para nº '+String(target).padStart(6,'0'));this.toast('Sequência atualizada. Próximo recibo: '+String(target).padStart(6,'0'));await this.load()},
  topPaidNames(rows){let map={};rows.forEach(s=>{let v=Number(s.paidValue||0);if(v<=0)return;let name=this.providerName(s.providerId);map[name]=(map[name]||0)+v});return Object.entries(map).sort((a,b)=>b[1]-a[1]).slice(0,3).map(([name,total])=>name+' • '+this.money(total))},topOpenNames(rows){let map={};rows.forEach(s=>{let v=this.balance(s);if(v<=0)return;let name=this.providerName(s.providerId);map[name]=(map[name]||0)+v});return Object.entries(map).sort((a,b)=>b[1]-a[1]).slice(0,3).map(([name,total])=>name+' • '+this.money(total))},
  providerServices(id){return this.services.filter(s=>Number(s.providerId)===Number(id)).sort((a,b)=>String(b.serviceDate||'').localeCompare(String(a.serviceDate||'')))},providerReceipts(id){return this.receipts.filter(r=>Number(r.providerId)===Number(id)).sort((a,b)=>String(b.date||'').localeCompare(String(a.date||'')))},providerLastServiceLabel(id){let s=this.providerServices(id)[0];return s?this.fmt(s.serviceDate)+' • '+this.money(s.paidValue||s.amount):'-'},async logAction(type,title,detail){try{await db.logs.add({at:new Date().toISOString(),type,title,detail,role:this.auth?.role||'sistema'});this.logs=(await db.logs.orderBy('at').reverse().limit(30).toArray())}catch(e){console.warn('logAction',e)}},logIcon(type){return {payment:'circle-dollar-sign',receipt:'receipt-text',service:'clipboard-list',provider:'users',recurrence:'repeat-2',backup:'shield-check',restore:'rotate-ccw',danger:'triangle-alert',settings:'settings',delete:'trash-2'}[type]||'history'},fmtDateTime(d){if(!d)return'-';return new Date(d).toLocaleString('pt-BR')},async clearLogs(){if(this.isViewer)return;await db.logs.clear();this.logs=[];this.after()},providerSpend(id){return this.services.filter(s=>Number(s.providerId)===Number(id)).reduce((a,s)=>a+Number(s.paidValue||s.amount||0),0)},providerOpen(id){return this.services.filter(s=>Number(s.providerId)===Number(id)).reduce((a,s)=>a+this.balance(s),0)},openWhatsapp(p){let raw=String(p.whatsapp||p.phone||'').replace(/\D/g,'');if(!raw)return this.toast('Este prestador não tem WhatsApp/telefone cadastrado.','warning');let phone=raw.startsWith('55')?raw:'55'+raw;let msg=encodeURIComponent('Olá, '+p.name+'. Aqui é do '+this.settings.condoName+'.');window.open('h

===== receipts.reduce @ 6494472 =====
ecurrences)?data.recurrences:[],logs=Array.isArray(data.logs)?data.logs:[];await db.transaction('rw',db.providers,db.services,db.receipts,db.recurrences,db.logs,db.settings,db.meta,async()=>{await db.providers.clear();await db.services.clear();await db.receipts.clear();await db.recurrences.clear();await db.logs.clear();if(data.settings){let restoredSettings=JSON.parse(JSON.stringify(data.settings));await db.settings.put({key:'main',value:restoredSettings});this.settings=restoredSettings}if(providers.length)await db.providers.bulkAdd(providers);if(services.length)await db.services.bulkAdd(services);if(receipts.length)await db.receipts.bulkAdd(receipts);if(recurrences.length)await db.recurrences.bulkAdd(recurrences);if(logs.length)await db.logs.bulkAdd(logs.map(l=>{let x={...l};delete x.id;return x}).slice(0,200));let restoredMax=receipts.reduce((m,r)=>r.source==='Excel legado'?m:Math.max(m,Number(String(r.number||'').replace(/\D/g,'')||0)),0),safeSeq=Math.max(Number(data.receiptSeq||0),restoredMax);await db.meta.put({key:'receiptSeq',value:safeSeq})});await this.logAction('restore','Backup restaurado','Dados restaurados por arquivo JSON.');await this.load();this.toast('Backup restaurado com sucesso!')}catch(e){console.error(e);this.toast('Não foi possível restaurar o backup: '+e.message,'error')}},async clearAllInternal(){await db.transaction('rw',db.providers,db.services,db.receipts,db.recurrences,db.logs,async()=>{await db.providers.clear();await db.services.clear();await db.receipts.clear();await db.recurrences.clear();await db.logs.clear()});await this.load()},async clearAll(){this.openClearAllModal()},readLogo(e){let f=e.target.files[0];if(!f)return;let r=new FileReader();r.onload=()=>this.settings.logo=r.result;r.readAsDataURL(f)},
  receiptMaxNumber(){return this.receipts.reduce((max,r)=>r.source==='Excel legado'?max:Math.max(max,Number(String(r.number||'').replace(/\D/g,'')||0)),0)},nextReceiptNumber(){return Math.max(Number(this.receiptSeqValue||0),this.receiptMaxNumber())+1},receiptSequenceStatus(){return Number(this.receiptSeqValue||0)<this.receiptMaxNumber()?'Sincronizar':'Sequência OK'},async saveReceiptSequence(){if(this.isViewer)return;let target=Number(this.receiptSeqInput||0), max=this.receiptMaxNumber();if(!target||target<1)return this.toast('Informe um próximo número válido.','warning');if(target<=max)return this.toast('O número precisa ser maior que o último emitido: '+String(max).padStart(6,'0')+'.','warning');await db.meta.put({key:'receiptSeq',value:target-1});this.receiptSeqValue=target-1;await this.logAction('settings','Sequência de recibos ajustada','Próximo recibo definido para nº '+String(target).padStart(6,'0'));this.toast('Sequência atualizada. Próximo recibo: '+String(target).padStart(6,'0'));await this.load()},
  topPaidNames(rows){let map={};rows.forEach(s=>{let v=Number(s.paidValue||0);if(v<=0)return;let name=this.providerName(s.providerId);map[name]=(map[name]||0)+v});return Object.entries(map).sort((a,b)=>b[1]-a[1]).slice(0,3).map(([name,total])=>name+' • '+this.money(total))},topOpenNames(rows){let map={};rows.forEach(s=>{let v=this.balance(s);if(v<=0)return;let name=this.providerName(s.providerId);map[name]=(map[name]||0)+v});return Object.entries(map).sort((a,b)=>b[1]-a[1]).slice(0,3).map(([name,total])=>name+' • '+this.money(total))},
  providerServices(id){return this.services.filter(s=>Number(s.providerId)===Number(id)).sort((a,b)=>String(b.serviceDate||'').localeCompare(String(a.serviceDate||'')))},providerReceipts(id){return this.receipts.filter(r=>Number(r.providerId)===Number(id)).sort((a,b)=>String(b.date||'').localeCompare(String(a.date||'')))},providerLastServiceLabel(id){let s=this.providerServices(id)[0];return s?this.fmt(s.serviceDate)+' • '+this.money(s.paidValue||s.amount):'-'},async logAction(type,title,detail){try{await db.logs.add({at:new Date().toISOString(),type,title,detail,role:this.auth?.role||'sistema'});this.logs=(await db.logs.orderBy('at').reverse().limit(30).toArray())}catch(e){console.warn('logAction',e)}},logIcon(type){return {payment:'circle-dollar-sign',receipt:'receipt-text',service:'clipboard-list',provider:'users',recurrence:'repeat-2',backup:'shield-check',restore:'rotate-ccw',danger:'triangle-alert',settings:'settings',delete:'trash-2'}[type]||'history'},fmtDateTime(d){if(!d)return'-';return new Date(d).toLocaleString('pt-BR')},async clearLogs(){if(this.isViewer)return;await db.logs.clear();this.logs=[];this.after()},providerSpend(id){return this.services.filter(s=>Number(s.providerId)===Number(id)).reduce((a,s)=>a+Number(s.paidValue||s.amount||0),0)},providerOpen(id){return this.services.filter(s=>Number(s.providerId)===Number(id)).reduce((a,s)=>a+this.balance(s),0)},openWhatsapp(p){let raw=String(p.whatsapp||p.phone||'').replace(/\D/g,'');if(!raw)return this.toast('Este prestador não tem WhatsApp/telefone cadastrado.','warning');let phone=raw.startsWith('55')?raw:'55'+raw;let msg=encodeURIComponent('Olá, '+p.name+'. Aqui é do '+this.settings.condoName+'.');window.open('https://wa.me/'+phone+'?text='+msg,'_blank')},overdueLabel(s){if(!s.dueDate||this.balance(s)<=0)return'';let today=new Date(this.today()+'T00:00:00'), due=new Date(s.dueDate+'T00:00:00'), days=Math.ceil((due-today)/86400000);if(days<0)return Math.abs(days)+' dia(s) em atraso';if(days===0)return'vence hoje';return'faltam '+days+' dia(s)'},providerName(id){return (this.providers.find(p=>Number(p.id)===Number(id))||{}).name||'-'},reportReceiptNumber(service){let r=this.receipts.find(x=>Number(x.serviceId)===Number(service.id));return r?.number||'-';},balance(s){return Math.max(0,Number(s.amount||0)-Number(s.paidValue||0))},statusClass(st){return st==='Pago'?'bg-emerald-500/10 text-emerald-700':st==='Parcial'?'bg-amber-500/10 text-amber-700':'bg-red-500/10 text-red-700'},valorPorExtenso(v){v=Number(v||0);let reais=Math.floor(v),cent=Math.round((v-reais)*100);let base={0:'zero',1:'um',2:'dois',3:'três',4:'quatro',5:'cinco',6:'seis',7:'sete',8:'oito',9:

===== init @ 123 =====
<!doctype html>
<html lang="pt-BR">
<head>
  <meta charset="utf-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1, viewport-fit=cover" />
  <meta name="theme-color" content="#0F2A3A" />
  <meta name="mobile-web-app-capable" content="yes" />
  <meta name="apple-mobile-web-app-capable" content="yes" />
  <meta name="apple-mobile-web-app-status-bar-style" content="black-translucent" />
  <meta name="apple-mobile-web-app-title" content="ReciboCondo" />
  <title>ReciboCondo – Controle de Recibos Condominiais</title>
  <link rel="manifest" href="manifest.json" />
  <link rel="icon" href="favicon.ico" />
  <link rel="icon" type="image/png" sizes="32x32" href="assets/icons/icon-32.png" />
  <link rel="icon" type="image/png" sizes="192x192" href="assets/icons/icon-192.png" />
  <link rel="apple-touch-icon" href="assets/icons/icon-180.png" />
  <link rel="preconnect" href="https://fonts.googleapis.com" />
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
  <link href="https://fonts.googleapis.com/css2?family=Merriweather:wght@300;400;700;900&display=swap" rel="stylesheet" />
  <script src="https://cdn.tailwindcss.com"></script>
  <script>
    tailwind.config={darkMode:'class',theme:{extend:{fontFamily:{sans:['Merriweather','Georgia','ui-serif','serif'],serif:['Merriweather','Georgia','serif']},colors:{rc:{950:'#07111f',900:'#0b1726',800:'#0F2A3A',700:'#173b5f',600:'#1f5f8f',500:'#15803D',400:'#26a881',100:'#e8f4f1',gold:'#C89A3D',goldsoft:'#D8B66A',soft:'#F7F5EF'}},boxShadow:{premium:'0 24px 70px rgba(15,42,68,.22)',soft:'0 14px 35px rgba(15,42,68,.12)'}}}}
  </script>
  <script defer src="https://unpkg.com/alpinejs@3.x.x/dist/cdn.min.js"></script>
  <script src="https://cdn.jsdelivr.net/npm/dexie@4/dist/dexie.min.js"></script>
  <script src="https://cdn.jsdelivr.net/npm/chart.js@4.4.1/dist/chart.umd.min.js"></script>
  <script src="https://unpkg.com/lucide@latest"></script>
  <style>
    :root{color-scheme:light;--rc-primary:#0F2A3A;--rc-gold:#C89A3D;--rc-gold-soft:#D8B66A;--rc-success:#15803D;--rc-danger:#B91C1C;--rc-text:#1F2933;--rc-muted:#6B7280;--rc-bg-soft:#F7F5EF;--rc-panel:#FFFFFF;--bg:#F7F5EF;--card:rgba(255,255,255,.90);--text:var(--rc-text);--muted:var(--rc-muted);--border:rgba(15,42,58,.13)}
    html.dark{color-scheme:dark;--bg:#07111f;--card:rgba(15,23,42,.86);--text:#e5edf7;--muted:#94a3b8;--border:rgba(216,182,106,.22)}
    body{font-family:'Merriweather',Georgia,serif;background:radial-gradient(circle at 0 0,rgba(21,128,61,.14),transparent 32%),radial-gradient(circle at 90% 5%,rgba(200,154,61,.15),transparent 28%),linear-gradient(180deg,#fbfaf6 0%,var(--bg) 50%,#eef3f4 100%);color:var(--text);font-variant-numeric:tabular-nums}.dark body{background:radial-gradient(circle at 0 0,rgba(21,128,61,.18),transparent 32%),radial-gradient(circle at 90% 5%,rgba(200,154,61,.16),transparent 28%),var(--bg)}
    /* Merriweather global + paleta azul petróleo, verde e dourado premium */
    *,*::before,*::after{box-sizing:border-box}
    *:not(i):not(svg):not(path){font-family:'Merriweather',Georgia,serif}
    [x-cloak]{display:none!important}
    html,body{max-width:100%;overflow-x:hidden} main{min-width:0;max-width:100vw}.glass{background:var(--card);backdrop-filter:blur(18px);border:1px solid var(--border);border-radius:1.75rem;box-shadow:0 16px 42px rgba(15,42,58,.10)}
    .glass:not(header):not(aside .glass){overflow:hidden}
    main section.glass,main div.glass,main form.glass,.fixed .glass{border-top:4px solid var(--rc-gold);box-shadow:0 16px 42px rgba(15,42,58,.10),inset 0 3px 0 rgba(216,182,106,.18)}
    main header.glass,aside .glass{border-top:1px solid var(--border);box-shadow:0 14px 34px rgba(15,42,58,.10)}
    .premium{border-radius:1.75rem;box-shadow:0 18px 45px rgba(15,42,58,.12);transition:.18s ease}.premium:hover{transform:translateY(-2px);box-shadow:0 24px 70px rgba(15,42,58,.18)}
    main section:not(.print-area) .rounded-none, main section:not(.print-area) .rounded-sm{border-radius:1.25rem!important}
    form, fieldset, .card-panel, table, .overflow-auto, .receipt-box{border-radius:1.75rem}
    .input{width:100%;border-radius:1rem;border:1px solid rgba(148,163,184,.35);background:rgba(255,255,255,.76);padding:.78rem 1rem;outline:none}.dark .input{background:rgba(15,23,42,.72);border-co

===== init @ 24924 =====
rflow:hidden!important;margin:10mm auto 0!important;padding:7mm 8.5mm!important;font-size:10.1px!important;line-height:1.28!important}
      body.printing-annual .report-premium{box-sizing:border-box!important;border:0.35mm solid rgba(15,42,58,.22)!important;border-radius:5mm!important;background:#fff!important;box-shadow:none!important;page-break-after:avoid!important;break-after:avoid!important;page-break-inside:avoid!important;break-inside:avoid!important}
      body.printing-annual #annualReportPrint h1{font-size:17px!important;line-height:1.14!important} body.printing-annual #annualReportPrint h2{font-size:19px!important;line-height:1.12!important}
      body.printing-annual #annualReportPrint .report-kpi{padding:2.6mm!important;border-radius:3mm!important}.annual-mini-table{font-size:8.5px!important}.annual-mini-table th{font-size:6.7px!important}.annual-mini-table th,.annual-mini-table td{padding:4px 5px!important;line-height:1.15!important}.annual-report-list{font-size:8.6px!important}

    }
  </style>

<style id="rc-dynamic-splash-css">
#rcDynamicSplash{position:fixed;inset:0;z-index:2147483647;display:grid;place-items:center;overflow:hidden;background:radial-gradient(circle at 22% 10%,rgba(216,182,106,.16),transparent 34%),radial-gradient(circle at 80% 92%,rgba(200,154,61,.22),transparent 36%),linear-gradient(135deg,#0F2A3A 0%,#07111F 58%,#020814 100%);color:#fff;font-family:Merriweather,Georgia,serif;transition:opacity .65s ease,visibility .65s ease}
#rcDynamicSplash.rc-hide{opacity:0;visibility:hidden;pointer-events:none}
#rcDynamicSplash .rc-glow{position:absolute;width:72vmax;height:72vmax;border-radius:50%;background:radial-gradient(circle,rgba(216,182,106,.15),transparent 62%);left:-26vmax;top:-36vmax;filter:blur(4px);animation:rcGlow 4s ease-in-out infinite alternate}
#rcDynamicSplash .rc-skyline{position:absolute;inset:auto 0 18% 0;height:44%;opacity:.25;background:linear-gradient(90deg,transparent 0 5%,rgba(0,0,0,.55) 5% 13%,transparent 13% 18%,rgba(0,0,0,.45) 18% 27%,transparent 27% 70%,rgba(0,0,0,.42) 70% 79%,transparent 79% 84%,rgba(0,0,0,.55) 84% 94%,transparent 94%),repeating-linear-gradient(0deg,rgba(255,255,255,.05) 0 2px,transparent 2px 18px);animation:rcSky 10s ease-in-out infinite alternate}
#rcDynamicSplash .rc-wave{position:absolute;left:-10%;right:-10%;bottom:14%;height:24%;pointer-events:none}
#rcDynamicSplash .rc-wave:before,#rcDynamicSplash .rc-wave:after{content:"";position:absolute;left:0;right:0;height:2px;background:linear-gradient(90deg,transparent,rgba(216,182,106,.95),transparent);transform:rotate(-5deg);box-shadow:0 0 18px rgba(216,182,106,.75),0 0 60px rgba(216,182,106,.25);animation:rcSweep 2.2s ease-in-out infinite}
#rcDynamicSplash .rc-wave:after{top:28px;opacity:.35;animation-delay:.55s}
#rcDynamicSplash .rc-card{position:relative;z-index:2;width:min(88vw,440px);text-align:center;padding:22px 20px;transform:translateY(-1vh)}
#rcDynamicSplash .rc-icon-wrap{position:relative;width:clamp(92px,18vw,150px);height:clamp(92px,18vw,150px);margin:0 auto 18px;border-radius:28%;filter:drop-shadow(0 20px 40px rgba(0,0,0,.42));animation:rcFloat 3.2s ease-in-out infinite}
#rcDynamicSplash .rc-icon-wrap:before{content:"";position:absolute;inset:-10px;border-radius:30%;background:conic-gradient(from 110deg,transparent,rgba(216,182,106,.82),transparent 35%,rgba(21,128,61,.45),transparent 70%);filter:blur(10px);opacity:.75;animation:rcSpin 4.8s linear infinite}
#rcDynamicSplash .rc-icon{position:relative;width:100%;height:100%;object-fit:cover;border-radius:26%;box-shadow:inset 0 1px 0 rgba(255,255,255,.25),0 0 0 1px rgba(216,182,106,.28)}
#rcDynamicSplash h1{margin:0;font-size:clamp(34px,5vw,58px);line-height:.96;letter-spacing:-.05em;text-shadow:0 10px 26px rgba(0,0,0,.46)}
#rcDynamicSplash h1 span{color:#f8fafc}#rcDynamicSplash h1 b{color:#D8B66A;font-weight:900}
#rcDynamicSplash .rc-line{width:min(58vw,320px);height:1px;margin:16px auto 14px;background:linear-gradient(90deg,transparent,rgba(216,182,106,.92),transparent);position:relative}
#rcDynamicSplash .rc-line:after{content:"";position:absolute;left:50%;top:50%;width:9px;height:9px;border-radius:50%;background:#D8B66A;transform:translate(-50%,-50%);box-shadow:0 0 22px #D8B66A;animation:rcPulse 1.4s ease-in-out infinite}
#rcDynamicSplash .rc-sub{margin:0 auto;color:#D8B66A;text-transform:uppercase;letter-spacing:.22em;font-size:clamp(9px,1.5vw,12px);line-height:1.6;max-width:92%}
#rcDynamicSplash .rc-load-label{margin-top:34px;font-family:Inter,system-ui,sans-serif;color:rgba(255,255,255,.9);font-size:14px;letter-spacing:.04em}
#rcDynamicSplash .rc-progress{width:min(58vw,320px);height:7px;margin:14px auto 8px;border-radius:999px;padding:1px;background:rgba(255,255,255,.13);border:1px solid rgba(216,182,106,.38);overflow:hidden}
#rcDynamicSplash .rc-progress>i{display:block;height:100%;width:64%;border-radius:999px;background:linear-gradient(90deg,#8A641E,#fff1a8,#C89A3D);box-shadow:0 0 28px rgba(216,182,106,.9);animation:rcLoad 1.75s ease-in-out infinite}
#rcDynamicSplash .rc-percent{color:#D8B66A;font-family:Inter,system-ui,sans-serif;font-size:12px}
#rcDynamicSplash .rc-credit{position:absolute;left:0;right:0;bottom:max(18px,2vh);text-align:center;color:rgba(216,182,106,.9);font-family:Merriweather,Georgia,serif;font-size:clamp(13px,2vw,18px);letter-spacing:.04em}
#rcDynamicSplash .rc-credit:before{content:"";display:block;width:110px;height:1px;margin:0 auto 10px;background:linear-gradient(90deg,transparent,rgba(216,182,106,.78),transparent)}
@keyframes rcFloat{0%,100%{transform:translateY(0) scale(1)}50%{transform:translateY(-10px) scale(1.018)}}@keyframes rcSpin{to{transform:rotate(360deg)}}@keyframes rcPulse{0%,100%{opacity:.55;transform:translate(-50%,-50%) scale(.9)}50%{opacity:1;transform:translate(-50%,-50%) scale(1.18)}}@keyframes rcLoad{0%{transform:translateX(-105%)}55%{transform:translateX(18%)}100%{transform:translateX(170%)}}@keyframes rcSweep{0%,100%{opacity:.45;transform:translateX

===== init @ 25368 =====
de:avoid!important}
      body.printing-annual #annualReportPrint h1{font-size:17px!important;line-height:1.14!important} body.printing-annual #annualReportPrint h2{font-size:19px!important;line-height:1.12!important}
      body.printing-annual #annualReportPrint .report-kpi{padding:2.6mm!important;border-radius:3mm!important}.annual-mini-table{font-size:8.5px!important}.annual-mini-table th{font-size:6.7px!important}.annual-mini-table th,.annual-mini-table td{padding:4px 5px!important;line-height:1.15!important}.annual-report-list{font-size:8.6px!important}

    }
  </style>

<style id="rc-dynamic-splash-css">
#rcDynamicSplash{position:fixed;inset:0;z-index:2147483647;display:grid;place-items:center;overflow:hidden;background:radial-gradient(circle at 22% 10%,rgba(216,182,106,.16),transparent 34%),radial-gradient(circle at 80% 92%,rgba(200,154,61,.22),transparent 36%),linear-gradient(135deg,#0F2A3A 0%,#07111F 58%,#020814 100%);color:#fff;font-family:Merriweather,Georgia,serif;transition:opacity .65s ease,visibility .65s ease}
#rcDynamicSplash.rc-hide{opacity:0;visibility:hidden;pointer-events:none}
#rcDynamicSplash .rc-glow{position:absolute;width:72vmax;height:72vmax;border-radius:50%;background:radial-gradient(circle,rgba(216,182,106,.15),transparent 62%);left:-26vmax;top:-36vmax;filter:blur(4px);animation:rcGlow 4s ease-in-out infinite alternate}
#rcDynamicSplash .rc-skyline{position:absolute;inset:auto 0 18% 0;height:44%;opacity:.25;background:linear-gradient(90deg,transparent 0 5%,rgba(0,0,0,.55) 5% 13%,transparent 13% 18%,rgba(0,0,0,.45) 18% 27%,transparent 27% 70%,rgba(0,0,0,.42) 70% 79%,transparent 79% 84%,rgba(0,0,0,.55) 84% 94%,transparent 94%),repeating-linear-gradient(0deg,rgba(255,255,255,.05) 0 2px,transparent 2px 18px);animation:rcSky 10s ease-in-out infinite alternate}
#rcDynamicSplash .rc-wave{position:absolute;left:-10%;right:-10%;bottom:14%;height:24%;pointer-events:none}
#rcDynamicSplash .rc-wave:before,#rcDynamicSplash .rc-wave:after{content:"";position:absolute;left:0;right:0;height:2px;background:linear-gradient(90deg,transparent,rgba(216,182,106,.95),transparent);transform:rotate(-5deg);box-shadow:0 0 18px rgba(216,182,106,.75),0 0 60px rgba(216,182,106,.25);animation:rcSweep 2.2s ease-in-out infinite}
#rcDynamicSplash .rc-wave:after{top:28px;opacity:.35;animation-delay:.55s}
#rcDynamicSplash .rc-card{position:relative;z-index:2;width:min(88vw,440px);text-align:center;padding:22px 20px;transform:translateY(-1vh)}
#rcDynamicSplash .rc-icon-wrap{position:relative;width:clamp(92px,18vw,150px);height:clamp(92px,18vw,150px);margin:0 auto 18px;border-radius:28%;filter:drop-shadow(0 20px 40px rgba(0,0,0,.42));animation:rcFloat 3.2s ease-in-out infinite}
#rcDynamicSplash .rc-icon-wrap:before{content:"";position:absolute;inset:-10px;border-radius:30%;background:conic-gradient(from 110deg,transparent,rgba(216,182,106,.82),transparent 35%,rgba(21,128,61,.45),transparent 70%);filter:blur(10px);opacity:.75;animation:rcSpin 4.8s linear infinite}
#rcDynamicSplash .rc-icon{position:relative;width:100%;height:100%;object-fit:cover;border-radius:26%;box-shadow:inset 0 1px 0 rgba(255,255,255,.25),0 0 0 1px rgba(216,182,106,.28)}
#rcDynamicSplash h1{margin:0;font-size:clamp(34px,5vw,58px);line-height:.96;letter-spacing:-.05em;text-shadow:0 10px 26px rgba(0,0,0,.46)}
#rcDynamicSplash h1 span{color:#f8fafc}#rcDynamicSplash h1 b{color:#D8B66A;font-weight:900}
#rcDynamicSplash .rc-line{width:min(58vw,320px);height:1px;margin:16px auto 14px;background:linear-gradient(90deg,transparent,rgba(216,182,106,.92),transparent);position:relative}
#rcDynamicSplash .rc-line:after{content:"";position:absolute;left:50%;top:50%;width:9px;height:9px;border-radius:50%;background:#D8B66A;transform:translate(-50%,-50%);box-shadow:0 0 22px #D8B66A;animation:rcPulse 1.4s ease-in-out infinite}
#rcDynamicSplash .rc-sub{margin:0 auto;color:#D8B66A;text-transform:uppercase;letter-spacing:.22em;font-size:clamp(9px,1.5vw,12px);line-height:1.6;max-width:92%}
#rcDynamicSplash .rc-load-label{margin-top:34px;font-family:Inter,system-ui,sans-serif;color:rgba(255,255,255,.9);font-size:14px;letter-spacing:.04em}
#rcDynamicSplash .rc-progress{width:min(58vw,320px);height:7px;margin:14px auto 8px;border-radius:999px;padding:1px;background:rgba(255,255,255,.13);border:1px solid rgba(216,182,106,.38);overflow:hidden}
#rcDynamicSplash .rc-progress>i{display:block;height:100%;width:64%;border-radius:999px;background:linear-gradient(90deg,#8A641E,#fff1a8,#C89A3D);box-shadow:0 0 28px rgba(216,182,106,.9);animation:rcLoad 1.75s ease-in-out infinite}
#rcDynamicSplash .rc-percent{color:#D8B66A;font-family:Inter,system-ui,sans-serif;font-size:12px}
#rcDynamicSplash .rc-credit{position:absolute;left:0;right:0;bottom:max(18px,2vh);text-align:center;color:rgba(216,182,106,.9);font-family:Merriweather,Georgia,serif;font-size:clamp(13px,2vw,18px);letter-spacing:.04em}
#rcDynamicSplash .rc-credit:before{content:"";display:block;width:110px;height:1px;margin:0 auto 10px;background:linear-gradient(90deg,transparent,rgba(216,182,106,.78),transparent)}
@keyframes rcFloat{0%,100%{transform:translateY(0) scale(1)}50%{transform:translateY(-10px) scale(1.018)}}@keyframes rcSpin{to{transform:rotate(360deg)}}@keyframes rcPulse{0%,100%{opacity:.55;transform:translate(-50%,-50%) scale(.9)}50%{opacity:1;transform:translate(-50%,-50%) scale(1.18)}}@keyframes rcLoad{0%{transform:translateX(-105%)}55%{transform:translateX(18%)}100%{transform:translateX(170%)}}@keyframes rcSweep{0%,100%{opacity:.45;transform:translateX(-6%) rotate(-5deg)}50%{opacity:1;transform:translateX(6%) rotate(-5deg)}}@keyframes rcGlow{from{opacity:.65;transform:scale(.95)}to{opacity:1;transform:scale(1.08)}}@keyframes rcSky{from{transform:translateX(-1.2%)}to{transform:translateX(1.2%)}}
@media(max-width:640px){
  #rcDynamicSplash .rc-card{
    width:min(90vw,340px);
    padding:14px 14px 18px;
    transform:translateY(-2vh);
  }
  #rcDynamicSplash .rc-icon-wrap{
    width:84px;
 

===== init @ 25828 =====
e td{padding:4px 5px!important;line-height:1.15!important}.annual-report-list{font-size:8.6px!important}

    }
  </style>

<style id="rc-dynamic-splash-css">
#rcDynamicSplash{position:fixed;inset:0;z-index:2147483647;display:grid;place-items:center;overflow:hidden;background:radial-gradient(circle at 22% 10%,rgba(216,182,106,.16),transparent 34%),radial-gradient(circle at 80% 92%,rgba(200,154,61,.22),transparent 36%),linear-gradient(135deg,#0F2A3A 0%,#07111F 58%,#020814 100%);color:#fff;font-family:Merriweather,Georgia,serif;transition:opacity .65s ease,visibility .65s ease}
#rcDynamicSplash.rc-hide{opacity:0;visibility:hidden;pointer-events:none}
#rcDynamicSplash .rc-glow{position:absolute;width:72vmax;height:72vmax;border-radius:50%;background:radial-gradient(circle,rgba(216,182,106,.15),transparent 62%);left:-26vmax;top:-36vmax;filter:blur(4px);animation:rcGlow 4s ease-in-out infinite alternate}
#rcDynamicSplash .rc-skyline{position:absolute;inset:auto 0 18% 0;height:44%;opacity:.25;background:linear-gradient(90deg,transparent 0 5%,rgba(0,0,0,.55) 5% 13%,transparent 13% 18%,rgba(0,0,0,.45) 18% 27%,transparent 27% 70%,rgba(0,0,0,.42) 70% 79%,transparent 79% 84%,rgba(0,0,0,.55) 84% 94%,transparent 94%),repeating-linear-gradient(0deg,rgba(255,255,255,.05) 0 2px,transparent 2px 18px);animation:rcSky 10s ease-in-out infinite alternate}
#rcDynamicSplash .rc-wave{position:absolute;left:-10%;right:-10%;bottom:14%;height:24%;pointer-events:none}
#rcDynamicSplash .rc-wave:before,#rcDynamicSplash .rc-wave:after{content:"";position:absolute;left:0;right:0;height:2px;background:linear-gradient(90deg,transparent,rgba(216,182,106,.95),transparent);transform:rotate(-5deg);box-shadow:0 0 18px rgba(216,182,106,.75),0 0 60px rgba(216,182,106,.25);animation:rcSweep 2.2s ease-in-out infinite}
#rcDynamicSplash .rc-wave:after{top:28px;opacity:.35;animation-delay:.55s}
#rcDynamicSplash .rc-card{position:relative;z-index:2;width:min(88vw,440px);text-align:center;padding:22px 20px;transform:translateY(-1vh)}
#rcDynamicSplash .rc-icon-wrap{position:relative;width:clamp(92px,18vw,150px);height:clamp(92px,18vw,150px);margin:0 auto 18px;border-radius:28%;filter:drop-shadow(0 20px 40px rgba(0,0,0,.42));animation:rcFloat 3.2s ease-in-out infinite}
#rcDynamicSplash .rc-icon-wrap:before{content:"";position:absolute;inset:-10px;border-radius:30%;background:conic-gradient(from 110deg,transparent,rgba(216,182,106,.82),transparent 35%,rgba(21,128,61,.45),transparent 70%);filter:blur(10px);opacity:.75;animation:rcSpin 4.8s linear infinite}
#rcDynamicSplash .rc-icon{position:relative;width:100%;height:100%;object-fit:cover;border-radius:26%;box-shadow:inset 0 1px 0 rgba(255,255,255,.25),0 0 0 1px rgba(216,182,106,.28)}
#rcDynamicSplash h1{margin:0;font-size:clamp(34px,5vw,58px);line-height:.96;letter-spacing:-.05em;text-shadow:0 10px 26px rgba(0,0,0,.46)}
#rcDynamicSplash h1 span{color:#f8fafc}#rcDynamicSplash h1 b{color:#D8B66A;font-weight:900}
#rcDynamicSplash .rc-line{width:min(58vw,320px);height:1px;margin:16px auto 14px;background:linear-gradient(90deg,transparent,rgba(216,182,106,.92),transparent);position:relative}
#rcDynamicSplash .rc-line:after{content:"";position:absolute;left:50%;top:50%;width:9px;height:9px;border-radius:50%;background:#D8B66A;transform:translate(-50%,-50%);box-shadow:0 0 22px #D8B66A;animation:rcPulse 1.4s ease-in-out infinite}
#rcDynamicSplash .rc-sub{margin:0 auto;color:#D8B66A;text-transform:uppercase;letter-spacing:.22em;font-size:clamp(9px,1.5vw,12px);line-height:1.6;max-width:92%}
#rcDynamicSplash .rc-load-label{margin-top:34px;font-family:Inter,system-ui,sans-serif;color:rgba(255,255,255,.9);font-size:14px;letter-spacing:.04em}
#rcDynamicSplash .rc-progress{width:min(58vw,320px);height:7px;margin:14px auto 8px;border-radius:999px;padding:1px;background:rgba(255,255,255,.13);border:1px solid rgba(216,182,106,.38);overflow:hidden}
#rcDynamicSplash .rc-progress>i{display:block;height:100%;width:64%;border-radius:999px;background:linear-gradient(90deg,#8A641E,#fff1a8,#C89A3D);box-shadow:0 0 28px rgba(216,182,106,.9);animation:rcLoad 1.75s ease-in-out infinite}
#rcDynamicSplash .rc-percent{color:#D8B66A;font-family:Inter,system-ui,sans-serif;font-size:12px}
#rcDynamicSplash .rc-credit{position:absolute;left:0;right:0;bottom:max(18px,2vh);text-align:center;color:rgba(216,182,106,.9);font-family:Merriweather,Georgia,serif;font-size:clamp(13px,2vw,18px);letter-spacing:.04em}
#rcDynamicSplash .rc-credit:before{content:"";display:block;width:110px;height:1px;margin:0 auto 10px;background:linear-gradient(90deg,transparent,rgba(216,182,106,.78),transparent)}
@keyframes rcFloat{0%,100%{transform:translateY(0) scale(1)}50%{transform:translateY(-10px) scale(1.018)}}@keyframes rcSpin{to{transform:rotate(360deg)}}@keyframes rcPulse{0%,100%{opacity:.55;transform:translate(-50%,-50%) scale(.9)}50%{opacity:1;transform:translate(-50%,-50%) scale(1.18)}}@keyframes rcLoad{0%{transform:translateX(-105%)}55%{transform:translateX(18%)}100%{transform:translateX(170%)}}@keyframes rcSweep{0%,100%{opacity:.45;transform:translateX(-6%) rotate(-5deg)}50%{opacity:1;transform:translateX(6%) rotate(-5deg)}}@keyframes rcGlow{from{opacity:.65;transform:scale(.95)}to{opacity:1;transform:scale(1.08)}}@keyframes rcSky{from{transform:translateX(-1.2%)}to{transform:translateX(1.2%)}}
@media(max-width:640px){
  #rcDynamicSplash .rc-card{
    width:min(90vw,340px);
    padding:14px 14px 18px;
    transform:translateY(-2vh);
  }
  #rcDynamicSplash .rc-icon-wrap{
    width:84px;
    height:84px;
    margin-bottom:14px;
  }
  #rcDynamicSplash h1{
    font-size:34px;
    line-height:.98;
  }
  #rcDynamicSplash .rc-line{
    width:58vw;
    margin:14px auto 12px;
  }
  #rcDynamicSplash .rc-sub{
    font-size:9px;
    letter-spacing:.16em;
    line-height:1.55;
  }
  #rcDynamicSplash .rc-load-label{
    margin-top:24px;
    font-size:11px;
  }
  #rcDynamicSplash .rc-progress{
    width:58vw;
    height:6px;
    margin-top:12px;
  }
  #

===== init @ 26281 =====
0%,#07111F 58%,#020814 100%);color:#fff;font-family:Merriweather,Georgia,serif;transition:opacity .65s ease,visibility .65s ease}
#rcDynamicSplash.rc-hide{opacity:0;visibility:hidden;pointer-events:none}
#rcDynamicSplash .rc-glow{position:absolute;width:72vmax;height:72vmax;border-radius:50%;background:radial-gradient(circle,rgba(216,182,106,.15),transparent 62%);left:-26vmax;top:-36vmax;filter:blur(4px);animation:rcGlow 4s ease-in-out infinite alternate}
#rcDynamicSplash .rc-skyline{position:absolute;inset:auto 0 18% 0;height:44%;opacity:.25;background:linear-gradient(90deg,transparent 0 5%,rgba(0,0,0,.55) 5% 13%,transparent 13% 18%,rgba(0,0,0,.45) 18% 27%,transparent 27% 70%,rgba(0,0,0,.42) 70% 79%,transparent 79% 84%,rgba(0,0,0,.55) 84% 94%,transparent 94%),repeating-linear-gradient(0deg,rgba(255,255,255,.05) 0 2px,transparent 2px 18px);animation:rcSky 10s ease-in-out infinite alternate}
#rcDynamicSplash .rc-wave{position:absolute;left:-10%;right:-10%;bottom:14%;height:24%;pointer-events:none}
#rcDynamicSplash .rc-wave:before,#rcDynamicSplash .rc-wave:after{content:"";position:absolute;left:0;right:0;height:2px;background:linear-gradient(90deg,transparent,rgba(216,182,106,.95),transparent);transform:rotate(-5deg);box-shadow:0 0 18px rgba(216,182,106,.75),0 0 60px rgba(216,182,106,.25);animation:rcSweep 2.2s ease-in-out infinite}
#rcDynamicSplash .rc-wave:after{top:28px;opacity:.35;animation-delay:.55s}
#rcDynamicSplash .rc-card{position:relative;z-index:2;width:min(88vw,440px);text-align:center;padding:22px 20px;transform:translateY(-1vh)}
#rcDynamicSplash .rc-icon-wrap{position:relative;width:clamp(92px,18vw,150px);height:clamp(92px,18vw,150px);margin:0 auto 18px;border-radius:28%;filter:drop-shadow(0 20px 40px rgba(0,0,0,.42));animation:rcFloat 3.2s ease-in-out infinite}
#rcDynamicSplash .rc-icon-wrap:before{content:"";position:absolute;inset:-10px;border-radius:30%;background:conic-gradient(from 110deg,transparent,rgba(216,182,106,.82),transparent 35%,rgba(21,128,61,.45),transparent 70%);filter:blur(10px);opacity:.75;animation:rcSpin 4.8s linear infinite}
#rcDynamicSplash .rc-icon{position:relative;width:100%;height:100%;object-fit:cover;border-radius:26%;box-shadow:inset 0 1px 0 rgba(255,255,255,.25),0 0 0 1px rgba(216,182,106,.28)}
#rcDynamicSplash h1{margin:0;font-size:clamp(34px,5vw,58px);line-height:.96;letter-spacing:-.05em;text-shadow:0 10px 26px rgba(0,0,0,.46)}
#rcDynamicSplash h1 span{color:#f8fafc}#rcDynamicSplash h1 b{color:#D8B66A;font-weight:900}
#rcDynamicSplash .rc-line{width:min(58vw,320px);height:1px;margin:16px auto 14px;background:linear-gradient(90deg,transparent,rgba(216,182,106,.92),transparent);position:relative}
#rcDynamicSplash .rc-line:after{content:"";position:absolute;left:50%;top:50%;width:9px;height:9px;border-radius:50%;background:#D8B66A;transform:translate(-50%,-50%);box-shadow:0 0 22px #D8B66A;animation:rcPulse 1.4s ease-in-out infinite}
#rcDynamicSplash .rc-sub{margin:0 auto;color:#D8B66A;text-transform:uppercase;letter-spacing:.22em;font-size:clamp(9px,1.5vw,12px);line-height:1.6;max-width:92%}
#rcDynamicSplash .rc-load-label{margin-top:34px;font-family:Inter,system-ui,sans-serif;color:rgba(255,255,255,.9);font-size:14px;letter-spacing:.04em}
#rcDynamicSplash .rc-progress{width:min(58vw,320px);height:7px;margin:14px auto 8px;border-radius:999px;padding:1px;background:rgba(255,255,255,.13);border:1px solid rgba(216,182,106,.38);overflow:hidden}
#rcDynamicSplash .rc-progress>i{display:block;height:100%;width:64%;border-radius:999px;background:linear-gradient(90deg,#8A641E,#fff1a8,#C89A3D);box-shadow:0 0 28px rgba(216,182,106,.9);animation:rcLoad 1.75s ease-in-out infinite}
#rcDynamicSplash .rc-percent{color:#D8B66A;font-family:Inter,system-ui,sans-serif;font-size:12px}
#rcDynamicSplash .rc-credit{position:absolute;left:0;right:0;bottom:max(18px,2vh);text-align:center;color:rgba(216,182,106,.9);font-family:Merriweather,Georgia,serif;font-size:clamp(13px,2vw,18px);letter-spacing:.04em}
#rcDynamicSplash .rc-credit:before{content:"";display:block;width:110px;height:1px;margin:0 auto 10px;background:linear-gradient(90deg,transparent,rgba(216,182,106,.78),transparent)}
@keyframes rcFloat{0%,100%{transform:translateY(0) scale(1)}50%{transform:translateY(-10px) scale(1.018)}}@keyframes rcSpin{to{transform:rotate(360deg)}}@keyframes rcPulse{0%,100%{opacity:.55;transform:translate(-50%,-50%) scale(.9)}50%{opacity:1;transform:translate(-50%,-50%) scale(1.18)}}@keyframes rcLoad{0%{transform:translateX(-105%)}55%{transform:translateX(18%)}100%{transform:translateX(170%)}}@keyframes rcSweep{0%,100%{opacity:.45;transform:translateX(-6%) rotate(-5deg)}50%{opacity:1;transform:translateX(6%) rotate(-5deg)}}@keyframes rcGlow{from{opacity:.65;transform:scale(.95)}to{opacity:1;transform:scale(1.08)}}@keyframes rcSky{from{transform:translateX(-1.2%)}to{transform:translateX(1.2%)}}
@media(max-width:640px){
  #rcDynamicSplash .rc-card{
    width:min(90vw,340px);
    padding:14px 14px 18px;
    transform:translateY(-2vh);
  }
  #rcDynamicSplash .rc-icon-wrap{
    width:84px;
    height:84px;
    margin-bottom:14px;
  }
  #rcDynamicSplash h1{
    font-size:34px;
    line-height:.98;
  }
  #rcDynamicSplash .rc-line{
    width:58vw;
    margin:14px auto 12px;
  }
  #rcDynamicSplash .rc-sub{
    font-size:9px;
    letter-spacing:.16em;
    line-height:1.55;
  }
  #rcDynamicSplash .rc-load-label{
    margin-top:24px;
    font-size:11px;
  }
  #rcDynamicSplash .rc-progress{
    width:58vw;
    height:6px;
    margin-top:12px;
  }
  #rcDynamicSplash .rc-percent{
    font-size:12px;
  }
  #rcDynamicSplash .rc-credit{
    font-size:12px;
    bottom:14px;
  }
  #rcDynamicSplash .rc-credit:before{
    width:82px;
    margin-bottom:8px;
  }
  #rcDynamicSplash .rc-wave{
    bottom:11%;
  }
}
</style>


<script src="https://cdnjs.cloudflare.com/ajax/libs/qrcodejs/1.0.0/qrcode.min.js"></script>

<style id="rc-qrcode-auth-style">
.receipt-auth-box{
  margin-top:18px;
  border:1px solid r

===== init @ 26572 =====
%;background:radial-gradient(circle,rgba(216,182,106,.15),transparent 62%);left:-26vmax;top:-36vmax;filter:blur(4px);animation:rcGlow 4s ease-in-out infinite alternate}
#rcDynamicSplash .rc-skyline{position:absolute;inset:auto 0 18% 0;height:44%;opacity:.25;background:linear-gradient(90deg,transparent 0 5%,rgba(0,0,0,.55) 5% 13%,transparent 13% 18%,rgba(0,0,0,.45) 18% 27%,transparent 27% 70%,rgba(0,0,0,.42) 70% 79%,transparent 79% 84%,rgba(0,0,0,.55) 84% 94%,transparent 94%),repeating-linear-gradient(0deg,rgba(255,255,255,.05) 0 2px,transparent 2px 18px);animation:rcSky 10s ease-in-out infinite alternate}
#rcDynamicSplash .rc-wave{position:absolute;left:-10%;right:-10%;bottom:14%;height:24%;pointer-events:none}
#rcDynamicSplash .rc-wave:before,#rcDynamicSplash .rc-wave:after{content:"";position:absolute;left:0;right:0;height:2px;background:linear-gradient(90deg,transparent,rgba(216,182,106,.95),transparent);transform:rotate(-5deg);box-shadow:0 0 18px rgba(216,182,106,.75),0 0 60px rgba(216,182,106,.25);animation:rcSweep 2.2s ease-in-out infinite}
#rcDynamicSplash .rc-wave:after{top:28px;opacity:.35;animation-delay:.55s}
#rcDynamicSplash .rc-card{position:relative;z-index:2;width:min(88vw,440px);text-align:center;padding:22px 20px;transform:translateY(-1vh)}
#rcDynamicSplash .rc-icon-wrap{position:relative;width:clamp(92px,18vw,150px);height:clamp(92px,18vw,150px);margin:0 auto 18px;border-radius:28%;filter:drop-shadow(0 20px 40px rgba(0,0,0,.42));animation:rcFloat 3.2s ease-in-out infinite}
#rcDynamicSplash .rc-icon-wrap:before{content:"";position:absolute;inset:-10px;border-radius:30%;background:conic-gradient(from 110deg,transparent,rgba(216,182,106,.82),transparent 35%,rgba(21,128,61,.45),transparent 70%);filter:blur(10px);opacity:.75;animation:rcSpin 4.8s linear infinite}
#rcDynamicSplash .rc-icon{position:relative;width:100%;height:100%;object-fit:cover;border-radius:26%;box-shadow:inset 0 1px 0 rgba(255,255,255,.25),0 0 0 1px rgba(216,182,106,.28)}
#rcDynamicSplash h1{margin:0;font-size:clamp(34px,5vw,58px);line-height:.96;letter-spacing:-.05em;text-shadow:0 10px 26px rgba(0,0,0,.46)}
#rcDynamicSplash h1 span{color:#f8fafc}#rcDynamicSplash h1 b{color:#D8B66A;font-weight:900}
#rcDynamicSplash .rc-line{width:min(58vw,320px);height:1px;margin:16px auto 14px;background:linear-gradient(90deg,transparent,rgba(216,182,106,.92),transparent);position:relative}
#rcDynamicSplash .rc-line:after{content:"";position:absolute;left:50%;top:50%;width:9px;height:9px;border-radius:50%;background:#D8B66A;transform:translate(-50%,-50%);box-shadow:0 0 22px #D8B66A;animation:rcPulse 1.4s ease-in-out infinite}
#rcDynamicSplash .rc-sub{margin:0 auto;color:#D8B66A;text-transform:uppercase;letter-spacing:.22em;font-size:clamp(9px,1.5vw,12px);line-height:1.6;max-width:92%}
#rcDynamicSplash .rc-load-label{margin-top:34px;font-family:Inter,system-ui,sans-serif;color:rgba(255,255,255,.9);font-size:14px;letter-spacing:.04em}
#rcDynamicSplash .rc-progress{width:min(58vw,320px);height:7px;margin:14px auto 8px;border-radius:999px;padding:1px;background:rgba(255,255,255,.13);border:1px solid rgba(216,182,106,.38);overflow:hidden}
#rcDynamicSplash .rc-progress>i{display:block;height:100%;width:64%;border-radius:999px;background:linear-gradient(90deg,#8A641E,#fff1a8,#C89A3D);box-shadow:0 0 28px rgba(216,182,106,.9);animation:rcLoad 1.75s ease-in-out infinite}
#rcDynamicSplash .rc-percent{color:#D8B66A;font-family:Inter,system-ui,sans-serif;font-size:12px}
#rcDynamicSplash .rc-credit{position:absolute;left:0;right:0;bottom:max(18px,2vh);text-align:center;color:rgba(216,182,106,.9);font-family:Merriweather,Georgia,serif;font-size:clamp(13px,2vw,18px);letter-spacing:.04em}
#rcDynamicSplash .rc-credit:before{content:"";display:block;width:110px;height:1px;margin:0 auto 10px;background:linear-gradient(90deg,transparent,rgba(216,182,106,.78),transparent)}
@keyframes rcFloat{0%,100%{transform:translateY(0) scale(1)}50%{transform:translateY(-10px) scale(1.018)}}@keyframes rcSpin{to{transform:rotate(360deg)}}@keyframes rcPulse{0%,100%{opacity:.55;transform:translate(-50%,-50%) scale(.9)}50%{opacity:1;transform:translate(-50%,-50%) scale(1.18)}}@keyframes rcLoad{0%{transform:translateX(-105%)}55%{transform:translateX(18%)}100%{transform:translateX(170%)}}@keyframes rcSweep{0%,100%{opacity:.45;transform:translateX(-6%) rotate(-5deg)}50%{opacity:1;transform:translateX(6%) rotate(-5deg)}}@keyframes rcGlow{from{opacity:.65;transform:scale(.95)}to{opacity:1;transform:scale(1.08)}}@keyframes rcSky{from{transform:translateX(-1.2%)}to{transform:translateX(1.2%)}}
@media(max-width:640px){
  #rcDynamicSplash .rc-card{
    width:min(90vw,340px);
    padding:14px 14px 18px;
    transform:translateY(-2vh);
  }
  #rcDynamicSplash .rc-icon-wrap{
    width:84px;
    height:84px;
    margin-bottom:14px;
  }
  #rcDynamicSplash h1{
    font-size:34px;
    line-height:.98;
  }
  #rcDynamicSplash .rc-line{
    width:58vw;
    margin:14px auto 12px;
  }
  #rcDynamicSplash .rc-sub{
    font-size:9px;
    letter-spacing:.16em;
    line-height:1.55;
  }
  #rcDynamicSplash .rc-load-label{
    margin-top:24px;
    font-size:11px;
  }
  #rcDynamicSplash .rc-progress{
    width:58vw;
    height:6px;
    margin-top:12px;
  }
  #rcDynamicSplash .rc-percent{
    font-size:12px;
  }
  #rcDynamicSplash .rc-credit{
    font-size:12px;
    bottom:14px;
  }
  #rcDynamicSplash .rc-credit:before{
    width:82px;
    margin-bottom:8px;
  }
  #rcDynamicSplash .rc-wave{
    bottom:11%;
  }
}
</style>


<script src="https://cdnjs.cloudflare.com/ajax/libs/qrcodejs/1.0.0/qrcode.min.js"></script>

<style id="rc-qrcode-auth-style">
.receipt-auth-box{
  margin-top:18px;
  border:1px solid rgba(200,154,61,.28);
  border-top:3px solid rgba(200,154,61,.72);
  border-radius:18px;
  background:linear-gradient(135deg,#fffdf8,#f8f5eb);
  padding:14px;
  display:flex;
  align-items:center;
  justify-content:space-between;
  gap:16px;
}
.dark .receipt-auth-box{
  background:linear-gra

===== init @ 27405 =====
ght:2px;background:linear-gradient(90deg,transparent,rgba(216,182,106,.95),transparent);transform:rotate(-5deg);box-shadow:0 0 18px rgba(216,182,106,.75),0 0 60px rgba(216,182,106,.25);animation:rcSweep 2.2s ease-in-out infinite}
#rcDynamicSplash .rc-wave:after{top:28px;opacity:.35;animation-delay:.55s}
#rcDynamicSplash .rc-card{position:relative;z-index:2;width:min(88vw,440px);text-align:center;padding:22px 20px;transform:translateY(-1vh)}
#rcDynamicSplash .rc-icon-wrap{position:relative;width:clamp(92px,18vw,150px);height:clamp(92px,18vw,150px);margin:0 auto 18px;border-radius:28%;filter:drop-shadow(0 20px 40px rgba(0,0,0,.42));animation:rcFloat 3.2s ease-in-out infinite}
#rcDynamicSplash .rc-icon-wrap:before{content:"";position:absolute;inset:-10px;border-radius:30%;background:conic-gradient(from 110deg,transparent,rgba(216,182,106,.82),transparent 35%,rgba(21,128,61,.45),transparent 70%);filter:blur(10px);opacity:.75;animation:rcSpin 4.8s linear infinite}
#rcDynamicSplash .rc-icon{position:relative;width:100%;height:100%;object-fit:cover;border-radius:26%;box-shadow:inset 0 1px 0 rgba(255,255,255,.25),0 0 0 1px rgba(216,182,106,.28)}
#rcDynamicSplash h1{margin:0;font-size:clamp(34px,5vw,58px);line-height:.96;letter-spacing:-.05em;text-shadow:0 10px 26px rgba(0,0,0,.46)}
#rcDynamicSplash h1 span{color:#f8fafc}#rcDynamicSplash h1 b{color:#D8B66A;font-weight:900}
#rcDynamicSplash .rc-line{width:min(58vw,320px);height:1px;margin:16px auto 14px;background:linear-gradient(90deg,transparent,rgba(216,182,106,.92),transparent);position:relative}
#rcDynamicSplash .rc-line:after{content:"";position:absolute;left:50%;top:50%;width:9px;height:9px;border-radius:50%;background:#D8B66A;transform:translate(-50%,-50%);box-shadow:0 0 22px #D8B66A;animation:rcPulse 1.4s ease-in-out infinite}
#rcDynamicSplash .rc-sub{margin:0 auto;color:#D8B66A;text-transform:uppercase;letter-spacing:.22em;font-size:clamp(9px,1.5vw,12px);line-height:1.6;max-width:92%}
#rcDynamicSplash .rc-load-label{margin-top:34px;font-family:Inter,system-ui,sans-serif;color:rgba(255,255,255,.9);font-size:14px;letter-spacing:.04em}
#rcDynamicSplash .rc-progress{width:min(58vw,320px);height:7px;margin:14px auto 8px;border-radius:999px;padding:1px;background:rgba(255,255,255,.13);border:1px solid rgba(216,182,106,.38);overflow:hidden}
#rcDynamicSplash .rc-progress>i{display:block;height:100%;width:64%;border-radius:999px;background:linear-gradient(90deg,#8A641E,#fff1a8,#C89A3D);box-shadow:0 0 28px rgba(216,182,106,.9);animation:rcLoad 1.75s ease-in-out infinite}
#rcDynamicSplash .rc-percent{color:#D8B66A;font-family:Inter,system-ui,sans-serif;font-size:12px}
#rcDynamicSplash .rc-credit{position:absolute;left:0;right:0;bottom:max(18px,2vh);text-align:center;color:rgba(216,182,106,.9);font-family:Merriweather,Georgia,serif;font-size:clamp(13px,2vw,18px);letter-spacing:.04em}
#rcDynamicSplash .rc-credit:before{content:"";display:block;width:110px;height:1px;margin:0 auto 10px;background:linear-gradient(90deg,transparent,rgba(216,182,106,.78),transparent)}
@keyframes rcFloat{0%,100%{transform:translateY(0) scale(1)}50%{transform:translateY(-10px) scale(1.018)}}@keyframes rcSpin{to{transform:rotate(360deg)}}@keyframes rcPulse{0%,100%{opacity:.55;transform:translate(-50%,-50%) scale(.9)}50%{opacity:1;transform:translate(-50%,-50%) scale(1.18)}}@keyframes rcLoad{0%{transform:translateX(-105%)}55%{transform:translateX(18%)}100%{transform:translateX(170%)}}@keyframes rcSweep{0%,100%{opacity:.45;transform:translateX(-6%) rotate(-5deg)}50%{opacity:1;transform:translateX(6%) rotate(-5deg)}}@keyframes rcGlow{from{opacity:.65;transform:scale(.95)}to{opacity:1;transform:scale(1.08)}}@keyframes rcSky{from{transform:translateX(-1.2%)}to{transform:translateX(1.2%)}}
@media(max-width:640px){
  #rcDynamicSplash .rc-card{
    width:min(90vw,340px);
    padding:14px 14px 18px;
    transform:translateY(-2vh);
  }
  #rcDynamicSplash .rc-icon-wrap{
    width:84px;
    height:84px;
    margin-bottom:14px;
  }
  #rcDynamicSplash h1{
    font-size:34px;
    line-height:.98;
  }
  #rcDynamicSplash .rc-line{
    width:58vw;
    margin:14px auto 12px;
  }
  #rcDynamicSplash .rc-sub{
    font-size:9px;
    letter-spacing:.16em;
    line-height:1.55;
  }
  #rcDynamicSplash .rc-load-label{
    margin-top:24px;
    font-size:11px;
  }
  #rcDynamicSplash .rc-progress{
    width:58vw;
    height:6px;
    margin-top:12px;
  }
  #rcDynamicSplash .rc-percent{
    font-size:12px;
  }
  #rcDynamicSplash .rc-credit{
    font-size:12px;
    bottom:14px;
  }
  #rcDynamicSplash .rc-credit:before{
    width:82px;
    margin-bottom:8px;
  }
  #rcDynamicSplash .rc-wave{
    bottom:11%;
  }
}
</style>


<script src="https://cdnjs.cloudflare.com/ajax/libs/qrcodejs/1.0.0/qrcode.min.js"></script>

<style id="rc-qrcode-auth-style">
.receipt-auth-box{
  margin-top:18px;
  border:1px solid rgba(200,154,61,.28);
  border-top:3px solid rgba(200,154,61,.72);
  border-radius:18px;
  background:linear-gradient(135deg,#fffdf8,#f8f5eb);
  padding:14px;
  display:flex;
  align-items:center;
  justify-content:space-between;
  gap:16px;
}
.dark .receipt-auth-box{
  background:linear-gradient(135deg,rgba(15,23,42,.96),rgba(15,42,58,.92));
}
.receipt-auth-meta{
  flex:1;
}
.receipt-auth-title{
  font-size:11px;
  letter-spacing:.18em;
  text-transform:uppercase;
  color:#8a6a24;
  font-weight:900;
}
.receipt-auth-code{
  margin-top:6px;
  font-size:18px;
  font-weight:900;
  color:#0F2A3A;
  letter-spacing:.04em;
}
.receipt-auth-desc{
  margin-top:6px;
  font-size:11px;
  line-height:1.5;
  color:#64748b;
}
.receipt-qr{
  width:88px;
  height:88px;
  min-width:88px;
  background:#fff;
  border-radius:14px;
  padding:6px;
  border:1px solid rgba(15,42,58,.12);
  box-shadow:0 10px 24px rgba(15,42,58,.08);
}
</style>


<style id="rc-qrcode-visible-fix">
.receipt-auth-box{
display:flex!important;
align-items:center!important;
justify-content:space-between!important;
gap:18px!important;
margin-top:20px!importa

===== init @ 28155 =====
inset:-10px;border-radius:30%;background:conic-gradient(from 110deg,transparent,rgba(216,182,106,.82),transparent 35%,rgba(21,128,61,.45),transparent 70%);filter:blur(10px);opacity:.75;animation:rcSpin 4.8s linear infinite}
#rcDynamicSplash .rc-icon{position:relative;width:100%;height:100%;object-fit:cover;border-radius:26%;box-shadow:inset 0 1px 0 rgba(255,255,255,.25),0 0 0 1px rgba(216,182,106,.28)}
#rcDynamicSplash h1{margin:0;font-size:clamp(34px,5vw,58px);line-height:.96;letter-spacing:-.05em;text-shadow:0 10px 26px rgba(0,0,0,.46)}
#rcDynamicSplash h1 span{color:#f8fafc}#rcDynamicSplash h1 b{color:#D8B66A;font-weight:900}
#rcDynamicSplash .rc-line{width:min(58vw,320px);height:1px;margin:16px auto 14px;background:linear-gradient(90deg,transparent,rgba(216,182,106,.92),transparent);position:relative}
#rcDynamicSplash .rc-line:after{content:"";position:absolute;left:50%;top:50%;width:9px;height:9px;border-radius:50%;background:#D8B66A;transform:translate(-50%,-50%);box-shadow:0 0 22px #D8B66A;animation:rcPulse 1.4s ease-in-out infinite}
#rcDynamicSplash .rc-sub{margin:0 auto;color:#D8B66A;text-transform:uppercase;letter-spacing:.22em;font-size:clamp(9px,1.5vw,12px);line-height:1.6;max-width:92%}
#rcDynamicSplash .rc-load-label{margin-top:34px;font-family:Inter,system-ui,sans-serif;color:rgba(255,255,255,.9);font-size:14px;letter-spacing:.04em}
#rcDynamicSplash .rc-progress{width:min(58vw,320px);height:7px;margin:14px auto 8px;border-radius:999px;padding:1px;background:rgba(255,255,255,.13);border:1px solid rgba(216,182,106,.38);overflow:hidden}
#rcDynamicSplash .rc-progress>i{display:block;height:100%;width:64%;border-radius:999px;background:linear-gradient(90deg,#8A641E,#fff1a8,#C89A3D);box-shadow:0 0 28px rgba(216,182,106,.9);animation:rcLoad 1.75s ease-in-out infinite}
#rcDynamicSplash .rc-percent{color:#D8B66A;font-family:Inter,system-ui,sans-serif;font-size:12px}
#rcDynamicSplash .rc-credit{position:absolute;left:0;right:0;bottom:max(18px,2vh);text-align:center;color:rgba(216,182,106,.9);font-family:Merriweather,Georgia,serif;font-size:clamp(13px,2vw,18px);letter-spacing:.04em}
#rcDynamicSplash .rc-credit:before{content:"";display:block;width:110px;height:1px;margin:0 auto 10px;background:linear-gradient(90deg,transparent,rgba(216,182,106,.78),transparent)}
@keyframes rcFloat{0%,100%{transform:translateY(0) scale(1)}50%{transform:translateY(-10px) scale(1.018)}}@keyframes rcSpin{to{transform:rotate(360deg)}}@keyframes rcPulse{0%,100%{opacity:.55;transform:translate(-50%,-50%) scale(.9)}50%{opacity:1;transform:translate(-50%,-50%) scale(1.18)}}@keyframes rcLoad{0%{transform:translateX(-105%)}55%{transform:translateX(18%)}100%{transform:translateX(170%)}}@keyframes rcSweep{0%,100%{opacity:.45;transform:translateX(-6%) rotate(-5deg)}50%{opacity:1;transform:translateX(6%) rotate(-5deg)}}@keyframes rcGlow{from{opacity:.65;transform:scale(.95)}to{opacity:1;transform:scale(1.08)}}@keyframes rcSky{from{transform:translateX(-1.2%)}to{transform:translateX(1.2%)}}
@media(max-width:640px){
  #rcDynamicSplash .rc-card{
    width:min(90vw,340px);
    padding:14px 14px 18px;
    transform:translateY(-2vh);
  }
  #rcDynamicSplash .rc-icon-wrap{
    width:84px;
    height:84px;
    margin-bottom:14px;
  }
  #rcDynamicSplash h1{
    font-size:34px;
    line-height:.98;
  }
  #rcDynamicSplash .rc-line{
    width:58vw;
    margin:14px auto 12px;
  }
  #rcDynamicSplash .rc-sub{
    font-size:9px;
    letter-spacing:.16em;
    line-height:1.55;
  }
  #rcDynamicSplash .rc-load-label{
    margin-top:24px;
    font-size:11px;
  }
  #rcDynamicSplash .rc-progress{
    width:58vw;
    height:6px;
    margin-top:12px;
  }
  #rcDynamicSplash .rc-percent{
    font-size:12px;
  }
  #rcDynamicSplash .rc-credit{
    font-size:12px;
    bottom:14px;
  }
  #rcDynamicSplash .rc-credit:before{
    width:82px;
    margin-bottom:8px;
  }
  #rcDynamicSplash .rc-wave{
    bottom:11%;
  }
}
</style>


<script src="https://cdnjs.cloudflare.com/ajax/libs/qrcodejs/1.0.0/qrcode.min.js"></script>

<style id="rc-qrcode-auth-style">
.receipt-auth-box{
  margin-top:18px;
  border:1px solid rgba(200,154,61,.28);
  border-top:3px solid rgba(200,154,61,.72);
  border-radius:18px;
  background:linear-gradient(135deg,#fffdf8,#f8f5eb);
  padding:14px;
  display:flex;
  align-items:center;
  justify-content:space-between;
  gap:16px;
}
.dark .receipt-auth-box{
  background:linear-gradient(135deg,rgba(15,23,42,.96),rgba(15,42,58,.92));
}
.receipt-auth-meta{
  flex:1;
}
.receipt-auth-title{
  font-size:11px;
  letter-spacing:.18em;
  text-transform:uppercase;
  color:#8a6a24;
  font-weight:900;
}
.receipt-auth-code{
  margin-top:6px;
  font-size:18px;
  font-weight:900;
  color:#0F2A3A;
  letter-spacing:.04em;
}
.receipt-auth-desc{
  margin-top:6px;
  font-size:11px;
  line-height:1.5;
  color:#64748b;
}
.receipt-qr{
  width:88px;
  height:88px;
  min-width:88px;
  background:#fff;
  border-radius:14px;
  padding:6px;
  border:1px solid rgba(15,42,58,.12);
  box-shadow:0 10px 24px rgba(15,42,58,.08);
}
</style>


<style id="rc-qrcode-visible-fix">
.receipt-auth-box{
display:flex!important;
align-items:center!important;
justify-content:space-between!important;
gap:18px!important;
margin-top:20px!important;
padding:16px!important;
border-radius:20px!important;
border:1px solid rgba(200,154,61,.35)!important;
border-top:4px solid #C89A3D!important;
background:linear-gradient(135deg,#fffdf8,#f6f1e4)!important;
box-shadow:0 14px 32px rgba(15,42,58,.08)!important;
}
.receipt-auth-meta{flex:1}
.receipt-auth-title{
font-size:11px!important;
font-weight:900!important;
letter-spacing:.18em!important;
text-transform:uppercase!important;
color:#8a6a24!important;
}
.receipt-auth-code{
font-size:20px!important;
font-weight:900!important;
margin-top:6px!important;
color:#0F2A3A!important;
}
.receipt-auth-desc{
margin-top:8px!important;
font-size:11px!important;
line-height:1.5!important;
color:#64748b!important;
}
.receipt-qr{
width:110px!important;
he

===== init @ 35310 =====
ght:74px;
border-radius:22px;
overflow:hidden;
border:1px solid rgba(216,182,106,.28);
box-shadow:0 14px 34px rgba(0,0,0,.22);
background:#fff;
display:flex;
align-items:center;
justify-content:center;
}
.report-brand-logo img{
width:100%;
height:100%;
object-fit:cover;
display:block;
}
.report-brand-title{
color:#fff;
}
.report-brand-kicker{
font-size:10px;
letter-spacing:.24em;
text-transform:uppercase;
color:#D8B66A;
font-weight:900;
}
.report-brand-name{
font-size:30px;
font-weight:900;
line-height:1;
letter-spacing:-.05em;
margin-top:4px;
}
.report-brand-sub{
margin-top:6px;
font-size:12px;
color:rgba(255,255,255,.72);
}
.report-brand-badge{
padding:10px 14px;
border-radius:999px;
background:rgba(216,182,106,.12);
border:1px solid rgba(216,182,106,.26);
color:#F8E4AD;
font-size:11px;
font-weight:900;
letter-spacing:.14em;
text-transform:uppercase;
position:relative;
z-index:2;
}
@media(max-width:640px){
.report-brand-header{
flex-direction:column;
align-items:flex-start;
padding:14px;
}
.report-brand-logo{
width:60px;
height:60px;
border-radius:18px;
}
.report-brand-name{
font-size:24px;
}
}
</style>


<style id="rc-report-logo-fix">
.report-brand-logo{
background:#ffffff!important;
padding:6px!important;
}
.report-brand-logo img{
object-fit:contain!important;
padding:2px!important;
}
</style>


<style id="rc-report-real-logo-clean-fix">
#reportPrint .relative.z-10 > .flex:first-child{
  align-items:center!important;
}
#reportPrint .relative.z-10 > .flex:first-child img{
  background:#fff!important;
  object-fit:contain!important;
}
@media print{
  #reportPrint .h-16.w-16{
    width:14mm!important;
    height:14mm!important;
    border-radius:3mm!important;
    padding:1.2mm!important;
  }
}
</style>

</head>
<body class="min-h-screen antialiased" x-data="app()" x-init="init()" :class="dark?'dark':''">

<div id="rcDynamicSplash" aria-label="Carregando ReciboCondo">
  <div class="rc-glow"></div>
  <div class="rc-skyline"></div>
  <div class="rc-wave"></div>
  <div class="rc-card">
    <div class="rc-icon-wrap"><img class="rc-icon" src="data:image/png;base64,

===== init @ 35316 =====
px;
border-radius:22px;
overflow:hidden;
border:1px solid rgba(216,182,106,.28);
box-shadow:0 14px 34px rgba(0,0,0,.22);
background:#fff;
display:flex;
align-items:center;
justify-content:center;
}
.report-brand-logo img{
width:100%;
height:100%;
object-fit:cover;
display:block;
}
.report-brand-title{
color:#fff;
}
.report-brand-kicker{
font-size:10px;
letter-spacing:.24em;
text-transform:uppercase;
color:#D8B66A;
font-weight:900;
}
.report-brand-name{
font-size:30px;
font-weight:900;
line-height:1;
letter-spacing:-.05em;
margin-top:4px;
}
.report-brand-sub{
margin-top:6px;
font-size:12px;
color:rgba(255,255,255,.72);
}
.report-brand-badge{
padding:10px 14px;
border-radius:999px;
background:rgba(216,182,106,.12);
border:1px solid rgba(216,182,106,.26);
color:#F8E4AD;
font-size:11px;
font-weight:900;
letter-spacing:.14em;
text-transform:uppercase;
position:relative;
z-index:2;
}
@media(max-width:640px){
.report-brand-header{
flex-direction:column;
align-items:flex-start;
padding:14px;
}
.report-brand-logo{
width:60px;
height:60px;
border-radius:18px;
}
.report-brand-name{
font-size:24px;
}
}
</style>


<style id="rc-report-logo-fix">
.report-brand-logo{
background:#ffffff!important;
padding:6px!important;
}
.report-brand-logo img{
object-fit:contain!important;
padding:2px!important;
}
</style>


<style id="rc-report-real-logo-clean-fix">
#reportPrint .relative.z-10 > .flex:first-child{
  align-items:center!important;
}
#reportPrint .relative.z-10 > .flex:first-child img{
  background:#fff!important;
  object-fit:contain!important;
}
@media print{
  #reportPrint .h-16.w-16{
    width:14mm!important;
    height:14mm!important;
    border-radius:3mm!important;
    padding:1.2mm!important;
  }
}
</style>

</head>
<body class="min-h-screen antialiased" x-data="app()" x-init="init()" :class="dark?'dark':''">

<div id="rcDynamicSplash" aria-label="Carregando ReciboCondo">
  <div class="rc-glow"></div>
  <div class="rc-skyline"></div>
  <div class="rc-wave"></div>
  <div class="rc-card">
    <div class="rc-icon-wrap"><img class="rc-icon" src="data:image/png;base64,

===== init @ 6447371 =====
dor','Limpeza','Dedetização','Manutenção de bomba','Portão/Interfone','Gratificação natalina','Outros'],
  providers:[],services:[],receipts:[],recurrences:[],logs:[],receiptSeqValue:0,receiptSeqInput:"",settings:{condoName:'Condomínio Exemplo',cnpj:'CNPJ 00.000.000/0001-00',address:'Endereço do condomínio',manager:'Síndico/Tesoureiro',logo:''},
  providerForm:{id:null,name:'',type:'Autônomo',document:'',category:'Jardineiro',phone:'',whatsapp:'',pix:'',notes:''},providerSearch:'',providerCategoryFilter:'',providerSortField:'name',providerSortDir:'asc',
  serviceForm:{providerId:'',category:'Jardineiro',reference:new Date().toISOString().slice(0,7),serviceDate:new Date().toISOString().slice(0,10),dueDate:new Date().toISOString().slice(0,10),amount:0,location:'',description:'',paymentDate:'',paymentMethod:'',paidValue:0},
  recurrenceForm:{id:null,providerId:'',category:'Jardineiro',amount:0,dueDay:5,paymentDay:5,paymentMethod:'PIX',location:'',description:'',active:true},
  serviceFilter:'todos',serviceCategoryFilter:'todas',serviceProviderFilter:'todos',serviceMonthFilter:'',paymentMonthFilter:new Date().toISOString().slice(0,7),paymentStatusFilter:'todos',paymentProviderFilter:'todos',receiptFilter:{q:'',month:'',category:'todas'},receiptServiceId:'',currentReceipt:null,dashboardYear:new Date().getFullYear(),payModal:false,paymentTarget:null,payment:{date:new Date().toISOString().slice(0,10),value:0,method:'PIX'},actionModal:{open:false,type:'',tone:'default',icon:'',target:null,service:null,title:'',subtitle:'',note:'',confirmLabel:'',details:[]},toasts:[],
  report:{from:new Date(new Date().getFullYear(),new Date().getMonth(),1).toISOString().slice(0,10),to:new Date().toISOString().slice(0,10),category:'todas',status:'todos'},passwords:{admin:'',viewer:''},
  async init(){document.documentElement.classList.toggle('dark',this.dark);window.alert=(msg)=>this.toast(String(msg||''),this.toastType(String(msg||'')));if('serviceWorker'in navigator){navigator.serviceWorker.register('./sw.js').catch(console.warn)}await this.seed();this.refreshIcons()},
  async seed(){let s=await db.settings.get('main');if(s)this.settings=s.value;await this.load();},
  async load(){this.providers=await db.providers.toArray();this.services=await db.services.toArray();this.receipts=await db.receipts.toArray();this.recurrences=await db.recurrences.toArray();this.logs=(await db.logs.orderBy('at').reverse().limit(30).toArray());let meta=await db.meta.get('receiptSeq');this.receiptSeqValue=Number(meta?.value||0);if(!this.receiptSeqInput)this.receiptSeqInput=this.nextReceiptNumber();this.after()},
  get isViewer(){return this.auth.role==='viewer'},get title(){return {'dashboard':'Dashboard inicial','prestadores':'Cadastro de prestadores','servicos':'Serviços','pagamentos':'Central de pagamentos','recibos':'Emissão de recibos','relatorios':'Relatórios','busca':'Busca global','config':'Configurações'}[this.view]||'ReciboCondo'},get selectedProvider(){return this.providers.find(p=>Number(p.id)===Number(this.selectedProviderId))||null},
  get cards(){return this.dashboardCards},
  get yearlyRows(){let y=String(this.dashboardYear||new Date().getFullYear());return this.services.filter(s=>String(s.paymentDate||s.serviceDate||'').startsWith(y))},
  get dashboardCards(){let nowRef=new Date().toISOString().slice(0,7), year=this.yearlyRows, month=year.filter(s=>String(s.paymentDate||s.serviceDate||'').slice(0,7)===nowRef), paidYear=year.reduce((a,s)=>a+Number(s.paidValue||0),0), paidMonth=month.reduce((a,s)=>a+Number(s.paidValue||0),0), countMonth=month.filter(s=>Number(s.paidValue||0)>0).length, open=year.reduce((a,s)=>a+this.balance(s),0), top=this.annualCategorySummary[0], topYear=this.topPaidNames(year), topMonth=this.topPaidNames(month), openTop=this.topOpenNames(year), catTop=this.annualCategorySummary.slice(0,3).map(c=>c.category+' • '+this.money(c.total)), activeCats=[...new Set(this.providers.map(p=>p.category).filter(Boolean))].slice(0,4), receiptsMonth=this.receipts.filter(r=>String(r.paymentDate||r.date||'').slice(0,7)===nowRef).length;return[
    {label:'Pago no ano',value:this.money(paidYear),hint:String(this.dashboardYear),icon:'wallet-cards',color:'text-rc-600',tooltipTitle:'Resumo anual',tooltipMain:this.money(paidYear),tooltipTag:'Ano '+this.dashboardYear,tooltip:['Total efetivamente pago no ano selecionado.', 'Top recebimentos: '+(topYear.length?topYear.join(' | '):'sem pagamentos lançados.'), 'Use para comparar o volume anual com o relatório anual premium.']},
    {label:'Pago no mês',value:this.money(paidMonth),hint:this.month(nowRef),icon:'circle-dollar-sign',color:'text-emerald-600',tooltipTitle:'Resumo do mês',tooltipMain:this.money(paidMonth),tooltipTag:'Mês atual',tooltip:['Mês de referência: '+this.month(nowRef)+'.', 'Quem recebeu: '+(topMonth.length?topMonth.join(' | '):'sem pagamentos neste mês.'), 'Ajuda a conferir rapidamente o fechamento mensal.']},
    {label:'Pagamentos no mês',value:countMonth,hint:'registros pagos',icon:'receipt-text',color:'text-blue-600',tooltipTitle:'Movimento mensal',tooltipMain:countMonth+' pagamento(s)',tooltipTag:'Recibos '+receiptsMonth,tooltip:['Valor médio por pagamento: '+this.money(countMonth?paidMonth/countMonth:0)+'.', 'Recibos emitidos no mês: '+receiptsMonth+'.', 'Bom para conferir se todos os pagamentos pagos já têm recibo.']},
    {label:'Prestadores ativos',value:this.providers.length,hint:'cadastrados',icon:'users',color:'text-rc-600',tooltipTitle:'Base de prestadores',tooltipMain:this.providers.length+' cadastro(s)',tooltipTag:'Cadastros',tooltip:['Categorias em uso: '+(activeCats.length?activeCats.join(', '):'nenhuma categoria cadastrada.'), 'Use para localizar quem presta serviço recorrente ou avulso.', 'Dica: mantenha PIX, WhatsApp e categoria sempre atualizados.']},
    {label:'Em aberto no ano',value:this.money(open),hint:'pendente/parcial',icon:'triangle-alert',color:'text-red-600',tooltipTitle:'Pendências do ano',tooltipMain:this.money(open),tooltip

===== x-init @ 35308 =====
eight:74px;
border-radius:22px;
overflow:hidden;
border:1px solid rgba(216,182,106,.28);
box-shadow:0 14px 34px rgba(0,0,0,.22);
background:#fff;
display:flex;
align-items:center;
justify-content:center;
}
.report-brand-logo img{
width:100%;
height:100%;
object-fit:cover;
display:block;
}
.report-brand-title{
color:#fff;
}
.report-brand-kicker{
font-size:10px;
letter-spacing:.24em;
text-transform:uppercase;
color:#D8B66A;
font-weight:900;
}
.report-brand-name{
font-size:30px;
font-weight:900;
line-height:1;
letter-spacing:-.05em;
margin-top:4px;
}
.report-brand-sub{
margin-top:6px;
font-size:12px;
color:rgba(255,255,255,.72);
}
.report-brand-badge{
padding:10px 14px;
border-radius:999px;
background:rgba(216,182,106,.12);
border:1px solid rgba(216,182,106,.26);
color:#F8E4AD;
font-size:11px;
font-weight:900;
letter-spacing:.14em;
text-transform:uppercase;
position:relative;
z-index:2;
}
@media(max-width:640px){
.report-brand-header{
flex-direction:column;
align-items:flex-start;
padding:14px;
}
.report-brand-logo{
width:60px;
height:60px;
border-radius:18px;
}
.report-brand-name{
font-size:24px;
}
}
</style>


<style id="rc-report-logo-fix">
.report-brand-logo{
background:#ffffff!important;
padding:6px!important;
}
.report-brand-logo img{
object-fit:contain!important;
padding:2px!important;
}
</style>


<style id="rc-report-real-logo-clean-fix">
#reportPrint .relative.z-10 > .flex:first-child{
  align-items:center!important;
}
#reportPrint .relative.z-10 > .flex:first-child img{
  background:#fff!important;
  object-fit:contain!important;
}
@media print{
  #reportPrint .h-16.w-16{
    width:14mm!important;
    height:14mm!important;
    border-radius:3mm!important;
    padding:1.2mm!important;
  }
}
</style>

</head>
<body class="min-h-screen antialiased" x-data="app()" x-init="init()" :class="dark?'dark':''">

<div id="rcDynamicSplash" aria-label="Carregando ReciboCondo">
  <div class="rc-glow"></div>
  <div class="rc-skyline"></div>
  <div class="rc-wave"></div>
  <div class="rc-card">
    <div class="rc-icon-wrap"><img class="rc-icon" src="data:image/png;base64,