
===== repChart =====
COUNT=2

--- MATCH 1 @ 2202138 ---
t-sm font-bold">Fim</span><input x-model="report.to" type="date" class="input mt-1"></label><label><span class="text-sm font-bold">Categoria</span><select x-model="report.category" class="input mt-1"><option value="todas">Todas</option><template x-for="c in categories"><option x-text="c"></option></template></select></label><label><span class="text-sm font-bold">Status</span><select x-model="report.status" class="input mt-1"><option value="todos">Todos</option><option>Pendente</option><option>Parcial</option><option>Pago</option></select></label><div class="flex gap-2"><button @click="csv" class="primary flex-1">CSV</button><button @click="printReport" class="secondary">PDF</button></div></div>
          <div class="grid xl:grid-cols-3 gap-4"><div class="glass rounded-3xl p-4 xl:col-span-2"><h3 class="font-black mb-3">Relatório de pagamentos</h3><div class="report-chart-box"><canvas id="repChart"></canvas></div></div><div class="glass rounded-3xl p-4"><h3 class="font-black mb-3">Resumo</h3><p class="flex justify-between"><span>Lançado no período</span><b x-text="money(reportTotals.expected)"></b></p><p class="flex justify-between"><span>Pago no período</span><b class="text-emerald-600" x-text="money(reportTotals.paid)"></b></p><p class="flex justify-between"><span>Em aberto dos lançamentos</span><b class="text-red-600" x-text="money(reportTotals.open)"></b></p></div></div>
          <div class="glass rounded-3xl p-4 overflow-auto scrollbar"><table class="w-full text-sm"><thead class="text-left text-slate-500"><tr><th class="p-2">Recibo nº</th><th class="p-2">Serviço</th><th class="p-2">Pagamento</th><th class="p-2">Prestador</th><th class="p-2">Categoria</th><th class="p-2">Valor</th><th class="p-2">Pago</th><th class="p-2">Status</th></tr></thead><tbody><template x-for="s in reportDetailRows" :key="s.reportKey"><tr class="border-t border-slate-200/60 dark:border-slate-700/60"><td class="p-2 font-bold text-amber-700" x-text="s.receiptNumber"></td><td class="p-2" x-text="fmt(s.serviceDate)"></td><td class="p-2" x-text="fmt(s.paymentDate)"></td><td class="p-2" x-text="providerName(s.providerId)"></td><td class="p-2" x-text="s.category"></td><td class="p-2" x-text="money(s.amount)"></td><td class="p-2" x-text="money(s.reportValue||0)"></td><td class="p-2" x-text="s.status"></td></tr></template></tbody></table></div>
        </section>

        <section x-show="view==='busca'" class="space-y-4">
          <div class="glass rounded-3xl p-4 md:p-5">
            <div class="flex flex-col lg:flex-row lg:items-end justify-between gap-4">
              <div>
                <p class="text-xs uppercase tracking-[.18em] text-rc-gold font-black">Central de localização</p>


--- MATCH 2 @ 6522696 ---
troy();this.charts.monthly=new Chart(mEl,{type:'bar',data:{labels:monthly.map(x=>x.label.slice(0,3)),datasets:[{label:'Total pago',data:monthly.map(x=>x.paid)},{label:'Em aberto',data:monthly.map(x=>x.open)}]},options:{...baseOpts,scales:{y:{beginAtZero:true}}}})}let cats=this.annualCategorySummary.slice(0,8), cEl=document.getElementById('annualCatChart');if(cEl){if(this.charts.annualCat)this.charts.annualCat.destroy();this.charts.annualCat=new Chart(cEl,{type:'doughnut',data:{labels:cats.map(x=>x.category),datasets:[{label:'Pago',data:cats.map(x=>x.total)}]},options:baseOpts})}let launchMonths=this.reportRows.map(s=>String(s.serviceDate||'').slice(0,7)).filter(Boolean),paidMonths=this.reportDetailRows.filter(s=>Number(s.reportValue||0)>0).map(s=>String(s.paymentDate||'').slice(0,7)).filter(Boolean),months=[...new Set([...launchMonths,...paidMonths])].sort(), er=document.getElementById('repChart');if(er){if(this.charts.rep){this.charts.rep.destroy();this.charts.rep=null}er.removeAttribute('height');er.removeAttribute('width');er.style.height='240px';er.style.maxHeight='240px';er.style.width='100%';this.charts.rep=new Chart(er,{type:'line',data:{labels:months.map(m=>this.month(m)),datasets:[{label:'Lançado',data:months.map(m=>this.reportRows.filter(s=>String(s.serviceDate||'').slice(0,7)===m).reduce((a,s)=>a+Number(s.amount||0),0)),tension:.35},{label:'Pago',data:months.map(m=>this.reportDetailRows.filter(s=>String(s.paymentDate||'').slice(0,7)===m).reduce((a,s)=>a+Number(s.reportValue||0),0)),tension:.35}]},options:{responsive:true,maintainAspectRatio:false,resizeDelay:150,animation:false,plugins:{legend:{position:'bottom'}},scales:{y:{beginAtZero:true}}}})}}
}}
</script>

<script id="rc-qrcode-script">
(function(){
  function createReceiptAuth(){
    const codeEl = document.getElementById('receiptAuthCode');
    const qrEl = document.getElementById('receiptQrCode');
    if(!codeEl || !qrEl || typeof QRCode === 'undefined') return;

    const stamp = new Date();
    const code = 'RC-' +
      stamp.getFullYear() + '-' +
      String(stamp.getMonth()+1).padStart(2,'0') +
      String(stamp.getDate()).padStart(2,'0') + '-' +
      Math.floor(Math.random()*9000+1000);

    codeEl.textContent = code;

    qrEl.innerHTML = '';

    new QRCode(qrEl,{
      text:'ReciboCondo|' + code + '|Emitido em ' + stamp.toLocaleString('pt-BR'),
      width:76,
      height:76
    });
  }

  if(document.readyState === 'loading'){
    document.addEventListener('DOMContentLoaded', createReceiptAuth);
  }else{
    createReceiptAuth();
  }
})();
</script>


<script id="rc-qrcode-force-visible">
(function(){
function buildQr(){
  const codeEl=document.getElementById('receiptAuthCodeVi

===== new Chart =====
COUNT=3

--- MATCH 1 @ 6521823 ---
ntListener('click',()=>{panel.classList.toggle('rc-open');localStorage.setItem('rc_acc_'+scope+'_'+idx,panel.classList.contains('rc-open')?'1':'0');setTimeout(()=>this.resizeCharts(),90);this.refreshIcons()});panel.appendChild(head);panel.appendChild(body);let saved=localStorage.getItem('rc_acc_'+scope+'_'+idx);if(saved!==null?saved==='1':(idx===0||scope==='busca'||scope==='prestadores'))panel.classList.add('rc-open');});});}catch(e){console.warn('enhanceAccordions',e)}},resizeCharts(){Object.values(this.charts||{}).forEach(ch=>{try{ch.resize()}catch(e){}})},renderCharts(){if(!this.auth.logged||!window.Chart)return;let baseOpts={responsive:true,maintainAspectRatio:false,animation:{duration:250},plugins:{legend:{position:'bottom'}}};let monthly=this.monthlyHistory, mEl=document.getElementById('monthlyChart');if(mEl){if(this.charts.monthly)this.charts.monthly.destroy();this.charts.monthly=new Chart(mEl,{type:'bar',data:{labels:monthly.map(x=>x.label.slice(0,3)),datasets:[{label:'Total pago',data:monthly.map(x=>x.paid)},{label:'Em aberto',data:monthly.map(x=>x.open)}]},options:{...baseOpts,scales:{y:{beginAtZero:true}}}})}let cats=this.annualCategorySummary.slice(0,8), cEl=document.getElementById('annualCatChart');if(cEl){if(this.charts.annualCat)this.charts.annualCat.destroy();this.charts.annualCat=new Chart(cEl,{type:'doughnut',data:{labels:cats.map(x=>x.category),datasets:[{label:'Pago',data:cats.map(x=>x.total)}]},options:baseOpts})}let launchMonths=this.reportRows.map(s=>String(s.serviceDate||'').slice(0,7)).filter(Boolean),paidMonths=this.reportDetailRows.filter(s=>Number(s.reportValue||0)>0).map(s=>String(s.paymentDate||'').slice(0,7)).filter(Boolean),months=[...new Set([...launchMonths,...paidMonths])].sort(), er=document.getElementById('repChart');if(er){if(this.charts.rep){this.charts.rep.destroy();this.charts.rep=null}er.removeAttribute('height');er.removeAttribute('width');er.style.height='240px';er.style.maxHeight='240px';er.style.width='100%';this.charts.rep=new Chart(er,{type:'line',data:{labels:months.map(m=>this.month(m)),datasets:[{label:'Lançado',data:months.map(m=>this.reportRows.filter(s=>String(s.serviceDate||'').slice(0,7)===m).reduce((a,s)=>a+Number(s.amount||0),0)),tension:.35},{label:'Pago',data:months.map(m=>this.reportDetailRows.filter(s=>String(s.paymentDate||'').slice(0,7)===m).reduce((a,s)=>a+Number(s.reportValue||0),0)),tension:.35}]},options:{responsive:true,maintainAspectRatio:false,resizeDelay:150,animation:false,plugins:{legend:{position:'bottom'}},scales:{y:{beginAtZero:true}}}})}}
}}
</script>

<script id="rc-qrcode-script">
(function(){
  function createReceiptAuth(){
    const codeEl = document.getElementById('receiptAuthCod

--- MATCH 2 @ 6522241 ---
n');});});}catch(e){console.warn('enhanceAccordions',e)}},resizeCharts(){Object.values(this.charts||{}).forEach(ch=>{try{ch.resize()}catch(e){}})},renderCharts(){if(!this.auth.logged||!window.Chart)return;let baseOpts={responsive:true,maintainAspectRatio:false,animation:{duration:250},plugins:{legend:{position:'bottom'}}};let monthly=this.monthlyHistory, mEl=document.getElementById('monthlyChart');if(mEl){if(this.charts.monthly)this.charts.monthly.destroy();this.charts.monthly=new Chart(mEl,{type:'bar',data:{labels:monthly.map(x=>x.label.slice(0,3)),datasets:[{label:'Total pago',data:monthly.map(x=>x.paid)},{label:'Em aberto',data:monthly.map(x=>x.open)}]},options:{...baseOpts,scales:{y:{beginAtZero:true}}}})}let cats=this.annualCategorySummary.slice(0,8), cEl=document.getElementById('annualCatChart');if(cEl){if(this.charts.annualCat)this.charts.annualCat.destroy();this.charts.annualCat=new Chart(cEl,{type:'doughnut',data:{labels:cats.map(x=>x.category),datasets:[{label:'Pago',data:cats.map(x=>x.total)}]},options:baseOpts})}let launchMonths=this.reportRows.map(s=>String(s.serviceDate||'').slice(0,7)).filter(Boolean),paidMonths=this.reportDetailRows.filter(s=>Number(s.reportValue||0)>0).map(s=>String(s.paymentDate||'').slice(0,7)).filter(Boolean),months=[...new Set([...launchMonths,...paidMonths])].sort(), er=document.getElementById('repChart');if(er){if(this.charts.rep){this.charts.rep.destroy();this.charts.rep=null}er.removeAttribute('height');er.removeAttribute('width');er.style.height='240px';er.style.maxHeight='240px';er.style.width='100%';this.charts.rep=new Chart(er,{type:'line',data:{labels:months.map(m=>this.month(m)),datasets:[{label:'Lançado',data:months.map(m=>this.reportRows.filter(s=>String(s.serviceDate||'').slice(0,7)===m).reduce((a,s)=>a+Number(s.amount||0),0)),tension:.35},{label:'Pago',data:months.map(m=>this.reportDetailRows.filter(s=>String(s.paymentDate||'').slice(0,7)===m).reduce((a,s)=>a+Number(s.reportValue||0),0)),tension:.35}]},options:{responsive:true,maintainAspectRatio:false,resizeDelay:150,animation:false,plugins:{legend:{position:'bottom'}},scales:{y:{beginAtZero:true}}}})}}
}}
</script>

<script id="rc-qrcode-script">
(function(){
  function createReceiptAuth(){
    const codeEl = document.getElementById('receiptAuthCode');
    const qrEl = document.getElementById('receiptQrCode');
    if(!codeEl || !qrEl || typeof QRCode === 'undefined') return;

    const stamp = new Date();
    const code = 'RC-' +
      stamp.getFullYear() + '-' +
      String(stamp.getMonth()+1).padStart(2,'0') +
      String(stamp.getDate()).padStart(2,'0') + '-' +
      Math.floor(Math.random()*9000+1000);

    codeEl.textContent = code;

    qrEl.innerHTM

--- MATCH 3 @ 6522927 ---
scales:{y:{beginAtZero:true}}}})}let cats=this.annualCategorySummary.slice(0,8), cEl=document.getElementById('annualCatChart');if(cEl){if(this.charts.annualCat)this.charts.annualCat.destroy();this.charts.annualCat=new Chart(cEl,{type:'doughnut',data:{labels:cats.map(x=>x.category),datasets:[{label:'Pago',data:cats.map(x=>x.total)}]},options:baseOpts})}let launchMonths=this.reportRows.map(s=>String(s.serviceDate||'').slice(0,7)).filter(Boolean),paidMonths=this.reportDetailRows.filter(s=>Number(s.reportValue||0)>0).map(s=>String(s.paymentDate||'').slice(0,7)).filter(Boolean),months=[...new Set([...launchMonths,...paidMonths])].sort(), er=document.getElementById('repChart');if(er){if(this.charts.rep){this.charts.rep.destroy();this.charts.rep=null}er.removeAttribute('height');er.removeAttribute('width');er.style.height='240px';er.style.maxHeight='240px';er.style.width='100%';this.charts.rep=new Chart(er,{type:'line',data:{labels:months.map(m=>this.month(m)),datasets:[{label:'Lançado',data:months.map(m=>this.reportRows.filter(s=>String(s.serviceDate||'').slice(0,7)===m).reduce((a,s)=>a+Number(s.amount||0),0)),tension:.35},{label:'Pago',data:months.map(m=>this.reportDetailRows.filter(s=>String(s.paymentDate||'').slice(0,7)===m).reduce((a,s)=>a+Number(s.reportValue||0),0)),tension:.35}]},options:{responsive:true,maintainAspectRatio:false,resizeDelay:150,animation:false,plugins:{legend:{position:'bottom'}},scales:{y:{beginAtZero:true}}}})}}
}}
</script>

<script id="rc-qrcode-script">
(function(){
  function createReceiptAuth(){
    const codeEl = document.getElementById('receiptAuthCode');
    const qrEl = document.getElementById('receiptQrCode');
    if(!codeEl || !qrEl || typeof QRCode === 'undefined') return;

    const stamp = new Date();
    const code = 'RC-' +
      stamp.getFullYear() + '-' +
      String(stamp.getMonth()+1).padStart(2,'0') +
      String(stamp.getDate()).padStart(2,'0') + '-' +
      Math.floor(Math.random()*9000+1000);

    codeEl.textContent = code;

    qrEl.innerHTML = '';

    new QRCode(qrEl,{
      text:'ReciboCondo|' + code + '|Emitido em ' + stamp.toLocaleString('pt-BR'),
      width:76,
      height:76
    });
  }

  if(document.readyState === 'loading'){
    document.addEventListener('DOMContentLoaded', createReceiptAuth);
  }else{
    createReceiptAuth();
  }
})();
</script>


<script id="rc-qrcode-force-visible">
(function(){
function buildQr(){
  const codeEl=document.getElementById('receiptAuthCodeVisible');
  const qrEl=document.getElementById('receiptQrCodeVisible');

  if(!codeEl || !qrEl) return;

  const now=new Date();
  const code='RC-'+now.getFullYear()+'-'+String(now.getMonth()+1).padStart(2,'0')+
  String(now.getDate(

===== Chart( =====
COUNT=3

--- MATCH 1 @ 6521827 ---
stener('click',()=>{panel.classList.toggle('rc-open');localStorage.setItem('rc_acc_'+scope+'_'+idx,panel.classList.contains('rc-open')?'1':'0');setTimeout(()=>this.resizeCharts(),90);this.refreshIcons()});panel.appendChild(head);panel.appendChild(body);let saved=localStorage.getItem('rc_acc_'+scope+'_'+idx);if(saved!==null?saved==='1':(idx===0||scope==='busca'||scope==='prestadores'))panel.classList.add('rc-open');});});}catch(e){console.warn('enhanceAccordions',e)}},resizeCharts(){Object.values(this.charts||{}).forEach(ch=>{try{ch.resize()}catch(e){}})},renderCharts(){if(!this.auth.logged||!window.Chart)return;let baseOpts={responsive:true,maintainAspectRatio:false,animation:{duration:250},plugins:{legend:{position:'bottom'}}};let monthly=this.monthlyHistory, mEl=document.getElementById('monthlyChart');if(mEl){if(this.charts.monthly)this.charts.monthly.destroy();this.charts.monthly=new Chart(mEl,{type:'bar',data:{labels:monthly.map(x=>x.label.slice(0,3)),datasets:[{label:'Total pago',data:monthly.map(x=>x.paid)},{label:'Em aberto',data:monthly.map(x=>x.open)}]},options:{...baseOpts,scales:{y:{beginAtZero:true}}}})}let cats=this.annualCategorySummary.slice(0,8), cEl=document.getElementById('annualCatChart');if(cEl){if(this.charts.annualCat)this.charts.annualCat.destroy();this.charts.annualCat=new Chart(cEl,{type:'doughnut',data:{labels:cats.map(x=>x.category),datasets:[{label:'Pago',data:cats.map(x=>x.total)}]},options:baseOpts})}let launchMonths=this.reportRows.map(s=>String(s.serviceDate||'').slice(0,7)).filter(Boolean),paidMonths=this.reportDetailRows.filter(s=>Number(s.reportValue||0)>0).map(s=>String(s.paymentDate||'').slice(0,7)).filter(Boolean),months=[...new Set([...launchMonths,...paidMonths])].sort(), er=document.getElementById('repChart');if(er){if(this.charts.rep){this.charts.rep.destroy();this.charts.rep=null}er.removeAttribute('height');er.removeAttribute('width');er.style.height='240px';er.style.maxHeight='240px';er.style.width='100%';this.charts.rep=new Chart(er,{type:'line',data:{labels:months.map(m=>this.month(m)),datasets:[{label:'Lançado',data:months.map(m=>this.reportRows.filter(s=>String(s.serviceDate||'').slice(0,7)===m).reduce((a,s)=>a+Number(s.amount||0),0)),tension:.35},{label:'Pago',data:months.map(m=>this.reportDetailRows.filter(s=>String(s.paymentDate||'').slice(0,7)===m).reduce((a,s)=>a+Number(s.reportValue||0),0)),tension:.35}]},options:{responsive:true,maintainAspectRatio:false,resizeDelay:150,animation:false,plugins:{legend:{position:'bottom'}},scales:{y:{beginAtZero:true}}}})}}
}}
</script>

<script id="rc-qrcode-script">
(function(){
  function createReceiptAuth(){
    const codeEl = document.getElementById('receiptAuthCode

--- MATCH 2 @ 6522245 ---
});});}catch(e){console.warn('enhanceAccordions',e)}},resizeCharts(){Object.values(this.charts||{}).forEach(ch=>{try{ch.resize()}catch(e){}})},renderCharts(){if(!this.auth.logged||!window.Chart)return;let baseOpts={responsive:true,maintainAspectRatio:false,animation:{duration:250},plugins:{legend:{position:'bottom'}}};let monthly=this.monthlyHistory, mEl=document.getElementById('monthlyChart');if(mEl){if(this.charts.monthly)this.charts.monthly.destroy();this.charts.monthly=new Chart(mEl,{type:'bar',data:{labels:monthly.map(x=>x.label.slice(0,3)),datasets:[{label:'Total pago',data:monthly.map(x=>x.paid)},{label:'Em aberto',data:monthly.map(x=>x.open)}]},options:{...baseOpts,scales:{y:{beginAtZero:true}}}})}let cats=this.annualCategorySummary.slice(0,8), cEl=document.getElementById('annualCatChart');if(cEl){if(this.charts.annualCat)this.charts.annualCat.destroy();this.charts.annualCat=new Chart(cEl,{type:'doughnut',data:{labels:cats.map(x=>x.category),datasets:[{label:'Pago',data:cats.map(x=>x.total)}]},options:baseOpts})}let launchMonths=this.reportRows.map(s=>String(s.serviceDate||'').slice(0,7)).filter(Boolean),paidMonths=this.reportDetailRows.filter(s=>Number(s.reportValue||0)>0).map(s=>String(s.paymentDate||'').slice(0,7)).filter(Boolean),months=[...new Set([...launchMonths,...paidMonths])].sort(), er=document.getElementById('repChart');if(er){if(this.charts.rep){this.charts.rep.destroy();this.charts.rep=null}er.removeAttribute('height');er.removeAttribute('width');er.style.height='240px';er.style.maxHeight='240px';er.style.width='100%';this.charts.rep=new Chart(er,{type:'line',data:{labels:months.map(m=>this.month(m)),datasets:[{label:'Lançado',data:months.map(m=>this.reportRows.filter(s=>String(s.serviceDate||'').slice(0,7)===m).reduce((a,s)=>a+Number(s.amount||0),0)),tension:.35},{label:'Pago',data:months.map(m=>this.reportDetailRows.filter(s=>String(s.paymentDate||'').slice(0,7)===m).reduce((a,s)=>a+Number(s.reportValue||0),0)),tension:.35}]},options:{responsive:true,maintainAspectRatio:false,resizeDelay:150,animation:false,plugins:{legend:{position:'bottom'}},scales:{y:{beginAtZero:true}}}})}}
}}
</script>

<script id="rc-qrcode-script">
(function(){
  function createReceiptAuth(){
    const codeEl = document.getElementById('receiptAuthCode');
    const qrEl = document.getElementById('receiptQrCode');
    if(!codeEl || !qrEl || typeof QRCode === 'undefined') return;

    const stamp = new Date();
    const code = 'RC-' +
      stamp.getFullYear() + '-' +
      String(stamp.getMonth()+1).padStart(2,'0') +
      String(stamp.getDate()).padStart(2,'0') + '-' +
      Math.floor(Math.random()*9000+1000);

    codeEl.textContent = code;

    qrEl.innerHTML

--- MATCH 3 @ 6522931 ---
es:{y:{beginAtZero:true}}}})}let cats=this.annualCategorySummary.slice(0,8), cEl=document.getElementById('annualCatChart');if(cEl){if(this.charts.annualCat)this.charts.annualCat.destroy();this.charts.annualCat=new Chart(cEl,{type:'doughnut',data:{labels:cats.map(x=>x.category),datasets:[{label:'Pago',data:cats.map(x=>x.total)}]},options:baseOpts})}let launchMonths=this.reportRows.map(s=>String(s.serviceDate||'').slice(0,7)).filter(Boolean),paidMonths=this.reportDetailRows.filter(s=>Number(s.reportValue||0)>0).map(s=>String(s.paymentDate||'').slice(0,7)).filter(Boolean),months=[...new Set([...launchMonths,...paidMonths])].sort(), er=document.getElementById('repChart');if(er){if(this.charts.rep){this.charts.rep.destroy();this.charts.rep=null}er.removeAttribute('height');er.removeAttribute('width');er.style.height='240px';er.style.maxHeight='240px';er.style.width='100%';this.charts.rep=new Chart(er,{type:'line',data:{labels:months.map(m=>this.month(m)),datasets:[{label:'Lançado',data:months.map(m=>this.reportRows.filter(s=>String(s.serviceDate||'').slice(0,7)===m).reduce((a,s)=>a+Number(s.amount||0),0)),tension:.35},{label:'Pago',data:months.map(m=>this.reportDetailRows.filter(s=>String(s.paymentDate||'').slice(0,7)===m).reduce((a,s)=>a+Number(s.reportValue||0),0)),tension:.35}]},options:{responsive:true,maintainAspectRatio:false,resizeDelay:150,animation:false,plugins:{legend:{position:'bottom'}},scales:{y:{beginAtZero:true}}}})}}
}}
</script>

<script id="rc-qrcode-script">
(function(){
  function createReceiptAuth(){
    const codeEl = document.getElementById('receiptAuthCode');
    const qrEl = document.getElementById('receiptQrCode');
    if(!codeEl || !qrEl || typeof QRCode === 'undefined') return;

    const stamp = new Date();
    const code = 'RC-' +
      stamp.getFullYear() + '-' +
      String(stamp.getMonth()+1).padStart(2,'0') +
      String(stamp.getDate()).padStart(2,'0') + '-' +
      Math.floor(Math.random()*9000+1000);

    codeEl.textContent = code;

    qrEl.innerHTML = '';

    new QRCode(qrEl,{
      text:'ReciboCondo|' + code + '|Emitido em ' + stamp.toLocaleString('pt-BR'),
      width:76,
      height:76
    });
  }

  if(document.readyState === 'loading'){
    document.addEventListener('DOMContentLoaded', createReceiptAuth);
  }else{
    createReceiptAuth();
  }
})();
</script>


<script id="rc-qrcode-force-visible">
(function(){
function buildQr(){
  const codeEl=document.getElementById('receiptAuthCodeVisible');
  const qrEl=document.getElementById('receiptQrCodeVisible');

  if(!codeEl || !qrEl) return;

  const now=new Date();
  const code='RC-'+now.getFullYear()+'-'+String(now.getMonth()+1).padStart(2,'0')+
  String(now.getDate()

===== $watch =====
COUNT=0

===== view==='relatorios' =====
COUNT=1

--- MATCH 1 @ 2200974 ---
 x-text="'Recibo Nº '+r.number"></b><span class="text-xs text-slate-500" x-text="fmt(r.date)"></span></div>
                  <p class="text-sm mt-1" x-text="r.providerName+' • '+r.category"></p>
                  <p class="text-xs text-slate-500" x-text="'Ref.: '+month(r.reference)"></p>
                  <p class="font-black text-emerald-600 mt-2" x-text="money(r.total)"></p>
                  <div class="mt-3 flex gap-2 flex-wrap"><button @click="currentReceipt=r;printReceipt()" class="mini">Reimprimir</button><button @click="openDeleteReceiptModal(r)" class="danger" :disabled="isViewer">Excluir</button></div>
                </div>
              </template>
            </div>
            <p x-show="!filteredReceipts.length" class="text-sm text-slate-500 text-center py-6">Nenhum recibo encontrado com os filtros atuais.</p>
          </div>
        </section>

        <section x-show="view==='relatorios'" class="space-y-4">
          <div class="glass rounded-3xl p-4 grid md:grid-cols-5 gap-3 items-end"><label><span class="text-sm font-bold">Início</span><input x-model="report.from" type="date" class="input mt-1"></label><label><span class="text-sm font-bold">Fim</span><input x-model="report.to" type="date" class="input mt-1"></label><label><span class="text-sm font-bold">Categoria</span><select x-model="report.category" class="input mt-1"><option value="todas">Todas</option><template x-for="c in categories"><option x-text="c"></option></template></select></label><label><span class="text-sm font-bold">Status</span><select x-model="report.status" class="input mt-1"><option value="todos">Todos</option><option>Pendente</option><option>Parcial</option><option>Pago</option></select></label><div class="flex gap-2"><button @click="csv" class="primary flex-1">CSV</button><button @click="printReport" class="secondary">PDF</button></div></div>
          <div class="grid xl:grid-cols-3 gap-4"><div class="glass rounded-3xl p-4 xl:col-span-2"><h3 class="font-black mb-3">Relatório de pagamentos</h3><div class="report-chart-box"><canvas id="repChart"></canvas></div></div><div class="glass rounded-3xl p-4"><h3 class="font-black mb-3">Resumo</h3><p class="flex justify-between"><span>Lançado no período</span><b x-text="money(reportTotals.expected)"></b></p><p class="flex justify-between"><span>Pago no período</span><b class="text-emerald-600" x-text="money(reportTotals.paid)"></b></p><p class="flex justify-between"><span>Em aberto dos lançamentos</span><b class="text-red-600" x-text="money(reportTotals.open)"></b></p></div></div>
          <div class="glass rounded-3xl p-4 overflow-auto scrollbar"><table class="w-full text-sm"><thead class="text-left text-slate-500"><tr><th class="p-2">

===== report.from =====
COUNT=6

--- MATCH 1 @ 2201163 ---
"></p>
                  <p class="text-xs text-slate-500" x-text="'Ref.: '+month(r.reference)"></p>
                  <p class="font-black text-emerald-600 mt-2" x-text="money(r.total)"></p>
                  <div class="mt-3 flex gap-2 flex-wrap"><button @click="currentReceipt=r;printReceipt()" class="mini">Reimprimir</button><button @click="openDeleteReceiptModal(r)" class="danger" :disabled="isViewer">Excluir</button></div>
                </div>
              </template>
            </div>
            <p x-show="!filteredReceipts.length" class="text-sm text-slate-500 text-center py-6">Nenhum recibo encontrado com os filtros atuais.</p>
          </div>
        </section>

        <section x-show="view==='relatorios'" class="space-y-4">
          <div class="glass rounded-3xl p-4 grid md:grid-cols-5 gap-3 items-end"><label><span class="text-sm font-bold">Início</span><input x-model="report.from" type="date" class="input mt-1"></label><label><span class="text-sm font-bold">Fim</span><input x-model="report.to" type="date" class="input mt-1"></label><label><span class="text-sm font-bold">Categoria</span><select x-model="report.category" class="input mt-1"><option value="todas">Todas</option><template x-for="c in categories"><option x-text="c"></option></template></select></label><label><span class="text-sm font-bold">Status</span><select x-model="report.status" class="input mt-1"><option value="todos">Todos</option><option>Pendente</option><option>Parcial</option><option>Pago</option></select></label><div class="flex gap-2"><button @click="csv" class="primary flex-1">CSV</button><button @click="printReport" class="secondary">PDF</button></div></div>
          <div class="grid xl:grid-cols-3 gap-4"><div class="glass rounded-3xl p-4 xl:col-span-2"><h3 class="font-black mb-3">Relatório de pagamentos</h3><div class="report-chart-box"><canvas id="repChart"></canvas></div></div><div class="glass rounded-3xl p-4"><h3 class="font-black mb-3">Resumo</h3><p class="flex justify-between"><span>Lançado no período</span><b x-text="money(reportTotals.expected)"></b></p><p class="flex justify-between"><span>Pago no período</span><b class="text-emerald-600" x-text="money(reportTotals.paid)"></b></p><p class="flex justify-between"><span>Em aberto dos lançamentos</span><b class="text-red-600" x-text="money(reportTotals.open)"></b></p></div></div>
          <div class="glass rounded-3xl p-4 overflow-auto scrollbar"><table class="w-full text-sm"><thead class="text-left text-slate-500"><tr><th class="p-2">Recibo nº</th><th class="p-2">Serviço</th><th class="p-2">Pagamento</th><th class="p-2">Prestador</th><th class="p-2">Categoria</th><th class="p-2">Valor</th><th class="p-2">Pago</t

--- MATCH 2 @ 6468472 ---
r,this.servicePanelMonth))},
  get servicePanelPendingRecurrences(){return this.recurrences.filter(r=>r.active&&!this.recurrenceGenerated(r,this.servicePanelMonth))},
  get servicePanelReceiptCount(){return this.receipts.filter(r=>String(r.paymentDate||r.date||'').slice(0,7)===this.servicePanelMonth).length},
  get filteredReceipts(){let q=String(this.receiptFilter.q||'').toLowerCase().trim();return this.receipts.filter(r=>{let txt=[r.number,r.providerName,r.category,r.description,r.paymentMethod,r.total].join(' ').toLowerCase();return(!q||txt.includes(q))&&(!this.receiptFilter.month||r.reference===this.receiptFilter.month)&&(this.receiptFilter.category==='todas'||r.category===this.receiptFilter.category)}).sort((a,b)=>String(b.date||'').localeCompare(String(a.date||''))||String(b.number||'').localeCompare(String(a.number||'')))},
  get reportRows(){return this.services.filter(s=>(!this.report.from||s.serviceDate>=this.report.from)&&(!this.report.to||s.serviceDate<=this.report.to)&&(this.report.category==='todas'||s.category===this.report.category)&&(this.report.status==='todos'||s.status===this.report.status)).sort((a,b)=>{let nums=x=>this.receipts.filter(r=>Number(r.serviceId)===Number(x.id)).map(r=>Number(String(r.number||'').replace(/\D/g,''))||0).filter(Boolean),aa=nums(a),bb=nums(b),na=aa.length?Math.min(...aa):0,nb=bb.length?Math.min(...bb):0;if(na&&nb&&na!==nb)return na-nb;if(na&&!nb)return-1;if(!na&&nb)return 1;return String(a.serviceDate||'').localeCompare(String(b.serviceDate||''))||Number(a.id||0)-Number(b.id||0)})},
  get reportDetailRows(){let rows=[],inPeriod=d=>(!this.report.from||String(d||'')>=this.report.from)&&(!this.report.to||String(d||'')<=this.report.to),services=this.services.filter(s=>(this.report.category==='todas'||s.category===this.report.category)&&(this.report.status==='todos'||s.status===this.report.status));for(const s of services){let ps=this.payments.filter(p=>Number(p.serviceId)===Number(s.id)).sort((a,b)=>String(a.date||'').localeCompare(String(b.date||''))||Number(a.id||0)-Number(b.id||0));if(ps.length){for(const p of ps){if(!inPeriod(p.date))continue;let r=p.receiptId?this.receipts.find(x=>Number(x.id)===Number(p.receiptId)):null;rows.push({...s,reportKey:'p-'+p.id,receiptNumber:r?.number||'PENDENTE',paymentDate:p.date||'',description:r?.description||s.description,reportValue:Number(p.amount||0),paymentMethod:p.method||r?.paymentMethod||s.paymentMethod||''})}}else if(inPeriod(s.serviceDate))rows.push({...s,reportKey:'s-'+s.id,receiptNumber:'-',paymentDate:'',description:s.description,reportValue:0,paymentMethod:''})}return rows.sort((a,b)=>String(a.paymentDate||a.serviceDate||'').localeCompare(String(b.paymentDate||b.service

--- MATCH 3 @ 6468505 ---
t servicePanelPendingRecurrences(){return this.recurrences.filter(r=>r.active&&!this.recurrenceGenerated(r,this.servicePanelMonth))},
  get servicePanelReceiptCount(){return this.receipts.filter(r=>String(r.paymentDate||r.date||'').slice(0,7)===this.servicePanelMonth).length},
  get filteredReceipts(){let q=String(this.receiptFilter.q||'').toLowerCase().trim();return this.receipts.filter(r=>{let txt=[r.number,r.providerName,r.category,r.description,r.paymentMethod,r.total].join(' ').toLowerCase();return(!q||txt.includes(q))&&(!this.receiptFilter.month||r.reference===this.receiptFilter.month)&&(this.receiptFilter.category==='todas'||r.category===this.receiptFilter.category)}).sort((a,b)=>String(b.date||'').localeCompare(String(a.date||''))||String(b.number||'').localeCompare(String(a.number||'')))},
  get reportRows(){return this.services.filter(s=>(!this.report.from||s.serviceDate>=this.report.from)&&(!this.report.to||s.serviceDate<=this.report.to)&&(this.report.category==='todas'||s.category===this.report.category)&&(this.report.status==='todos'||s.status===this.report.status)).sort((a,b)=>{let nums=x=>this.receipts.filter(r=>Number(r.serviceId)===Number(x.id)).map(r=>Number(String(r.number||'').replace(/\D/g,''))||0).filter(Boolean),aa=nums(a),bb=nums(b),na=aa.length?Math.min(...aa):0,nb=bb.length?Math.min(...bb):0;if(na&&nb&&na!==nb)return na-nb;if(na&&!nb)return-1;if(!na&&nb)return 1;return String(a.serviceDate||'').localeCompare(String(b.serviceDate||''))||Number(a.id||0)-Number(b.id||0)})},
  get reportDetailRows(){let rows=[],inPeriod=d=>(!this.report.from||String(d||'')>=this.report.from)&&(!this.report.to||String(d||'')<=this.report.to),services=this.services.filter(s=>(this.report.category==='todas'||s.category===this.report.category)&&(this.report.status==='todos'||s.status===this.report.status));for(const s of services){let ps=this.payments.filter(p=>Number(p.serviceId)===Number(s.id)).sort((a,b)=>String(a.date||'').localeCompare(String(b.date||''))||Number(a.id||0)-Number(b.id||0));if(ps.length){for(const p of ps){if(!inPeriod(p.date))continue;let r=p.receiptId?this.receipts.find(x=>Number(x.id)===Number(p.receiptId)):null;rows.push({...s,reportKey:'p-'+p.id,receiptNumber:r?.number||'PENDENTE',paymentDate:p.date||'',description:r?.description||s.description,reportValue:Number(p.amount||0),paymentMethod:p.method||r?.paymentMethod||s.paymentMethod||''})}}else if(inPeriod(s.serviceDate))rows.push({...s,reportKey:'s-'+s.id,receiptNumber:'-',paymentDate:'',description:s.description,reportValue:0,paymentMethod:''})}return rows.sort((a,b)=>String(a.paymentDate||a.serviceDate||'').localeCompare(String(b.paymentDate||b.serviceDate||''))||String(a.receiptNumbe

--- MATCH 4 @ 6469183 ---
ry)}).sort((a,b)=>String(b.date||'').localeCompare(String(a.date||''))||String(b.number||'').localeCompare(String(a.number||'')))},
  get reportRows(){return this.services.filter(s=>(!this.report.from||s.serviceDate>=this.report.from)&&(!this.report.to||s.serviceDate<=this.report.to)&&(this.report.category==='todas'||s.category===this.report.category)&&(this.report.status==='todos'||s.status===this.report.status)).sort((a,b)=>{let nums=x=>this.receipts.filter(r=>Number(r.serviceId)===Number(x.id)).map(r=>Number(String(r.number||'').replace(/\D/g,''))||0).filter(Boolean),aa=nums(a),bb=nums(b),na=aa.length?Math.min(...aa):0,nb=bb.length?Math.min(...bb):0;if(na&&nb&&na!==nb)return na-nb;if(na&&!nb)return-1;if(!na&&nb)return 1;return String(a.serviceDate||'').localeCompare(String(b.serviceDate||''))||Number(a.id||0)-Number(b.id||0)})},
  get reportDetailRows(){let rows=[],inPeriod=d=>(!this.report.from||String(d||'')>=this.report.from)&&(!this.report.to||String(d||'')<=this.report.to),services=this.services.filter(s=>(this.report.category==='todas'||s.category===this.report.category)&&(this.report.status==='todos'||s.status===this.report.status));for(const s of services){let ps=this.payments.filter(p=>Number(p.serviceId)===Number(s.id)).sort((a,b)=>String(a.date||'').localeCompare(String(b.date||''))||Number(a.id||0)-Number(b.id||0));if(ps.length){for(const p of ps){if(!inPeriod(p.date))continue;let r=p.receiptId?this.receipts.find(x=>Number(x.id)===Number(p.receiptId)):null;rows.push({...s,reportKey:'p-'+p.id,receiptNumber:r?.number||'PENDENTE',paymentDate:p.date||'',description:r?.description||s.description,reportValue:Number(p.amount||0),paymentMethod:p.method||r?.paymentMethod||s.paymentMethod||''})}}else if(inPeriod(s.serviceDate))rows.push({...s,reportKey:'s-'+s.id,receiptNumber:'-',paymentDate:'',description:s.description,reportValue:0,paymentMethod:''})}return rows.sort((a,b)=>String(a.paymentDate||a.serviceDate||'').localeCompare(String(b.paymentDate||b.serviceDate||''))||String(a.receiptNumber||'').localeCompare(String(b.receiptNumber||'')))},get reportTotals(){let e=this.reportRows.reduce((a,s)=>a+Number(s.amount||0),0),p=this.reportDetailRows.reduce((a,s)=>a+Number(s.reportValue||0),0),o=this.reportRows.reduce((a,s)=>a+this.balance(s),0);return{expected:e,paid:p,open:o}},backupStats(){let records=this.providers.length+this.services.length+this.receipts.length+this.recurrences.length+this.payments.length;return{records,providers:this.providers.length,services:this.services.length,receipts:this.receipts.length,recurrences:this.recurrences.length,payments:this.payments.length}},backupDays(){if(!this.backupLast)return null;let d=new Date(this.backupLast), now

--- MATCH 5 @ 6469216 ---
'').localeCompare(String(a.date||''))||String(b.number||'').localeCompare(String(a.number||'')))},
  get reportRows(){return this.services.filter(s=>(!this.report.from||s.serviceDate>=this.report.from)&&(!this.report.to||s.serviceDate<=this.report.to)&&(this.report.category==='todas'||s.category===this.report.category)&&(this.report.status==='todos'||s.status===this.report.status)).sort((a,b)=>{let nums=x=>this.receipts.filter(r=>Number(r.serviceId)===Number(x.id)).map(r=>Number(String(r.number||'').replace(/\D/g,''))||0).filter(Boolean),aa=nums(a),bb=nums(b),na=aa.length?Math.min(...aa):0,nb=bb.length?Math.min(...bb):0;if(na&&nb&&na!==nb)return na-nb;if(na&&!nb)return-1;if(!na&&nb)return 1;return String(a.serviceDate||'').localeCompare(String(b.serviceDate||''))||Number(a.id||0)-Number(b.id||0)})},
  get reportDetailRows(){let rows=[],inPeriod=d=>(!this.report.from||String(d||'')>=this.report.from)&&(!this.report.to||String(d||'')<=this.report.to),services=this.services.filter(s=>(this.report.category==='todas'||s.category===this.report.category)&&(this.report.status==='todos'||s.status===this.report.status));for(const s of services){let ps=this.payments.filter(p=>Number(p.serviceId)===Number(s.id)).sort((a,b)=>String(a.date||'').localeCompare(String(b.date||''))||Number(a.id||0)-Number(b.id||0));if(ps.length){for(const p of ps){if(!inPeriod(p.date))continue;let r=p.receiptId?this.receipts.find(x=>Number(x.id)===Number(p.receiptId)):null;rows.push({...s,reportKey:'p-'+p.id,receiptNumber:r?.number||'PENDENTE',paymentDate:p.date||'',description:r?.description||s.description,reportValue:Number(p.amount||0),paymentMethod:p.method||r?.paymentMethod||s.paymentMethod||''})}}else if(inPeriod(s.serviceDate))rows.push({...s,reportKey:'s-'+s.id,receiptNumber:'-',paymentDate:'',description:s.description,reportValue:0,paymentMethod:''})}return rows.sort((a,b)=>String(a.paymentDate||a.serviceDate||'').localeCompare(String(b.paymentDate||b.serviceDate||''))||String(a.receiptNumber||'').localeCompare(String(b.receiptNumber||'')))},get reportTotals(){let e=this.reportRows.reduce((a,s)=>a+Number(s.amount||0),0),p=this.reportDetailRows.reduce((a,s)=>a+Number(s.reportValue||0),0),o=this.reportRows.reduce((a,s)=>a+this.balance(s),0);return{expected:e,paid:p,open:o}},backupStats(){let records=this.providers.length+this.services.length+this.receipts.length+this.recurrences.length+this.payments.length;return{records,providers:this.providers.length,services:this.services.length,receipts:this.receipts.length,recurrences:this.recurrences.length,payments:this.payments.length}},backupDays(){if(!this.backupLast)return null;let d=new Date(this.backupLast), now=new Date();return Math.max(0,Mat

--- MATCH 6 @ 6519004 ---
hentos').replace('seis centos','seiscentos').replace('sete centos','setecentos').replace('oito centos','oitocentos').replace('nove centos','novecentos');return r?cn+' e '+ext(r):cn}if(n<1000000){let m=Math.floor(n/1000),r=n%1000;return (m===1?'mil':ext(m)+' mil')+(r?' e '+ext(r):'')}return n.toLocaleString('pt-BR')};let txt=ext(reais)+' '+(reais===1?'real':'reais');if(cent)txt+=' e '+ext(cent)+' '+(cent===1?'centavo':'centavos');return txt;},money(v){return Number(v||0).toLocaleString('pt-BR',{style:'currency',currency:'BRL'})},today(){return new Date().toISOString().slice(0,10)},fmt(d){if(!d)return'-';return new Date(d+'T00:00:00').toLocaleDateString('pt-BR')},month(m){if(!m)return'-';let [y,mo]=String(m).split('-');return new Date(Number(y),Number(mo)-1,1).toLocaleDateString('pt-BR',{month:'long',year:'numeric'})},reportMonthLabel(){let src=(this.reportRows[0]?.reference)||String(this.report.from||this.today()).slice(0,7);let label=this.month(src);return String(label).replace(/ de \d{4}/,'').toUpperCase();},download(n,c,t){let a=document.createElement('a');a.href=URL.createObjectURL(new Blob([c],{type:t}));a.download=n;a.click();URL.revokeObjectURL(a.href)},refreshIcons(){setTimeout(()=>window.lucide&&lucide.createIcons(),60)},after(){this.$nextTick(()=>{this.enhanceAccordions();this.refreshIcons();this.renderCharts();setTimeout(()=>this.resizeCharts(),120)})},enhanceAccordions(){try{const scopes=['prestadores','servicos','pagamentos','recibos','busca','config'];scopes.forEach(scope=>{const section=[...document.querySelectorAll('main section')].find(s=>(s.getAttribute('x-show')||'').includes(scope));if(!section)return;const panels=[...section.children].filter(el=>el.classList&&el.classList.contains('glass'));panels.forEach((panel,idx)=>{if(panel.dataset.rcAccordion==='1')return;panel.dataset.rcAccordion='1';panel.classList.add('rc-accordion-panel');const titleEl=panel.querySelector('h3,h2,h4');const title=(titleEl?.textContent||({prestadores:'Prestadores',servicos:'Serviços',pagamentos:'Pagamentos',recibos:'Recibos',busca:'Busca global',config:'Configurações'}[scope]||'Painel')).trim();const subEl=panel.querySelector('p.text-sm,p.text-xs');const sub=(subEl?.textContent||'Clique para abrir ou recolher este painel.').trim();const body=document.createElement('div');body.className='rc-accordion-body';while(panel.firstChild)body.appendChild(panel.firstChild);const head=document.createElement('button');head.type='button';head.className='rc-accordion-head no-print';head.innerHTML=`<span><span class="rc-accordion-kicker">${scope==='config'?'organização':'painel premium'}</span><span class="rc-accordion-title">${title}</span><span class="rc-accordion-sub">${sub}</span><

===== report.to =====
COUNT=5

--- MATCH 1 @ 2201280 ---
  <p class="font-black text-emerald-600 mt-2" x-text="money(r.total)"></p>
                  <div class="mt-3 flex gap-2 flex-wrap"><button @click="currentReceipt=r;printReceipt()" class="mini">Reimprimir</button><button @click="openDeleteReceiptModal(r)" class="danger" :disabled="isViewer">Excluir</button></div>
                </div>
              </template>
            </div>
            <p x-show="!filteredReceipts.length" class="text-sm text-slate-500 text-center py-6">Nenhum recibo encontrado com os filtros atuais.</p>
          </div>
        </section>

        <section x-show="view==='relatorios'" class="space-y-4">
          <div class="glass rounded-3xl p-4 grid md:grid-cols-5 gap-3 items-end"><label><span class="text-sm font-bold">Início</span><input x-model="report.from" type="date" class="input mt-1"></label><label><span class="text-sm font-bold">Fim</span><input x-model="report.to" type="date" class="input mt-1"></label><label><span class="text-sm font-bold">Categoria</span><select x-model="report.category" class="input mt-1"><option value="todas">Todas</option><template x-for="c in categories"><option x-text="c"></option></template></select></label><label><span class="text-sm font-bold">Status</span><select x-model="report.status" class="input mt-1"><option value="todos">Todos</option><option>Pendente</option><option>Parcial</option><option>Pago</option></select></label><div class="flex gap-2"><button @click="csv" class="primary flex-1">CSV</button><button @click="printReport" class="secondary">PDF</button></div></div>
          <div class="grid xl:grid-cols-3 gap-4"><div class="glass rounded-3xl p-4 xl:col-span-2"><h3 class="font-black mb-3">Relatório de pagamentos</h3><div class="report-chart-box"><canvas id="repChart"></canvas></div></div><div class="glass rounded-3xl p-4"><h3 class="font-black mb-3">Resumo</h3><p class="flex justify-between"><span>Lançado no período</span><b x-text="money(reportTotals.expected)"></b></p><p class="flex justify-between"><span>Pago no período</span><b class="text-emerald-600" x-text="money(reportTotals.paid)"></b></p><p class="flex justify-between"><span>Em aberto dos lançamentos</span><b class="text-red-600" x-text="money(reportTotals.open)"></b></p></div></div>
          <div class="glass rounded-3xl p-4 overflow-auto scrollbar"><table class="w-full text-sm"><thead class="text-left text-slate-500"><tr><th class="p-2">Recibo nº</th><th class="p-2">Serviço</th><th class="p-2">Pagamento</th><th class="p-2">Prestador</th><th class="p-2">Categoria</th><th class="p-2">Valor</th><th class="p-2">Pago</th><th class="p-2">Status</th></tr></thead><tbody><template x-for="s in reportDetailRows" :key="s.reportKey"><tr cla

--- MATCH 2 @ 6468526 ---
Recurrences(){return this.recurrences.filter(r=>r.active&&!this.recurrenceGenerated(r,this.servicePanelMonth))},
  get servicePanelReceiptCount(){return this.receipts.filter(r=>String(r.paymentDate||r.date||'').slice(0,7)===this.servicePanelMonth).length},
  get filteredReceipts(){let q=String(this.receiptFilter.q||'').toLowerCase().trim();return this.receipts.filter(r=>{let txt=[r.number,r.providerName,r.category,r.description,r.paymentMethod,r.total].join(' ').toLowerCase();return(!q||txt.includes(q))&&(!this.receiptFilter.month||r.reference===this.receiptFilter.month)&&(this.receiptFilter.category==='todas'||r.category===this.receiptFilter.category)}).sort((a,b)=>String(b.date||'').localeCompare(String(a.date||''))||String(b.number||'').localeCompare(String(a.number||'')))},
  get reportRows(){return this.services.filter(s=>(!this.report.from||s.serviceDate>=this.report.from)&&(!this.report.to||s.serviceDate<=this.report.to)&&(this.report.category==='todas'||s.category===this.report.category)&&(this.report.status==='todos'||s.status===this.report.status)).sort((a,b)=>{let nums=x=>this.receipts.filter(r=>Number(r.serviceId)===Number(x.id)).map(r=>Number(String(r.number||'').replace(/\D/g,''))||0).filter(Boolean),aa=nums(a),bb=nums(b),na=aa.length?Math.min(...aa):0,nb=bb.length?Math.min(...bb):0;if(na&&nb&&na!==nb)return na-nb;if(na&&!nb)return-1;if(!na&&nb)return 1;return String(a.serviceDate||'').localeCompare(String(b.serviceDate||''))||Number(a.id||0)-Number(b.id||0)})},
  get reportDetailRows(){let rows=[],inPeriod=d=>(!this.report.from||String(d||'')>=this.report.from)&&(!this.report.to||String(d||'')<=this.report.to),services=this.services.filter(s=>(this.report.category==='todas'||s.category===this.report.category)&&(this.report.status==='todos'||s.status===this.report.status));for(const s of services){let ps=this.payments.filter(p=>Number(p.serviceId)===Number(s.id)).sort((a,b)=>String(a.date||'').localeCompare(String(b.date||''))||Number(a.id||0)-Number(b.id||0));if(ps.length){for(const p of ps){if(!inPeriod(p.date))continue;let r=p.receiptId?this.receipts.find(x=>Number(x.id)===Number(p.receiptId)):null;rows.push({...s,reportKey:'p-'+p.id,receiptNumber:r?.number||'PENDENTE',paymentDate:p.date||'',description:r?.description||s.description,reportValue:Number(p.amount||0),paymentMethod:p.method||r?.paymentMethod||s.paymentMethod||''})}}else if(inPeriod(s.serviceDate))rows.push({...s,reportKey:'s-'+s.id,receiptNumber:'-',paymentDate:'',description:s.description,reportValue:0,paymentMethod:''})}return rows.sort((a,b)=>String(a.paymentDate||a.serviceDate||'').localeCompare(String(b.paymentDate||b.serviceDate||''))||String(a.receiptNumber||'').localeCompar

--- MATCH 3 @ 6468557 ---
rences.filter(r=>r.active&&!this.recurrenceGenerated(r,this.servicePanelMonth))},
  get servicePanelReceiptCount(){return this.receipts.filter(r=>String(r.paymentDate||r.date||'').slice(0,7)===this.servicePanelMonth).length},
  get filteredReceipts(){let q=String(this.receiptFilter.q||'').toLowerCase().trim();return this.receipts.filter(r=>{let txt=[r.number,r.providerName,r.category,r.description,r.paymentMethod,r.total].join(' ').toLowerCase();return(!q||txt.includes(q))&&(!this.receiptFilter.month||r.reference===this.receiptFilter.month)&&(this.receiptFilter.category==='todas'||r.category===this.receiptFilter.category)}).sort((a,b)=>String(b.date||'').localeCompare(String(a.date||''))||String(b.number||'').localeCompare(String(a.number||'')))},
  get reportRows(){return this.services.filter(s=>(!this.report.from||s.serviceDate>=this.report.from)&&(!this.report.to||s.serviceDate<=this.report.to)&&(this.report.category==='todas'||s.category===this.report.category)&&(this.report.status==='todos'||s.status===this.report.status)).sort((a,b)=>{let nums=x=>this.receipts.filter(r=>Number(r.serviceId)===Number(x.id)).map(r=>Number(String(r.number||'').replace(/\D/g,''))||0).filter(Boolean),aa=nums(a),bb=nums(b),na=aa.length?Math.min(...aa):0,nb=bb.length?Math.min(...bb):0;if(na&&nb&&na!==nb)return na-nb;if(na&&!nb)return-1;if(!na&&nb)return 1;return String(a.serviceDate||'').localeCompare(String(b.serviceDate||''))||Number(a.id||0)-Number(b.id||0)})},
  get reportDetailRows(){let rows=[],inPeriod=d=>(!this.report.from||String(d||'')>=this.report.from)&&(!this.report.to||String(d||'')<=this.report.to),services=this.services.filter(s=>(this.report.category==='todas'||s.category===this.report.category)&&(this.report.status==='todos'||s.status===this.report.status));for(const s of services){let ps=this.payments.filter(p=>Number(p.serviceId)===Number(s.id)).sort((a,b)=>String(a.date||'').localeCompare(String(b.date||''))||Number(a.id||0)-Number(b.id||0));if(ps.length){for(const p of ps){if(!inPeriod(p.date))continue;let r=p.receiptId?this.receipts.find(x=>Number(x.id)===Number(p.receiptId)):null;rows.push({...s,reportKey:'p-'+p.id,receiptNumber:r?.number||'PENDENTE',paymentDate:p.date||'',description:r?.description||s.description,reportValue:Number(p.amount||0),paymentMethod:p.method||r?.paymentMethod||s.paymentMethod||''})}}else if(inPeriod(s.serviceDate))rows.push({...s,reportKey:'s-'+s.id,receiptNumber:'-',paymentDate:'',description:s.description,reportValue:0,paymentMethod:''})}return rows.sort((a,b)=>String(a.paymentDate||a.serviceDate||'').localeCompare(String(b.paymentDate||b.serviceDate||''))||String(a.receiptNumber||'').localeCompare(String(b.receiptNumber||'')))

--- MATCH 4 @ 6469237 ---
ing(a.date||''))||String(b.number||'').localeCompare(String(a.number||'')))},
  get reportRows(){return this.services.filter(s=>(!this.report.from||s.serviceDate>=this.report.from)&&(!this.report.to||s.serviceDate<=this.report.to)&&(this.report.category==='todas'||s.category===this.report.category)&&(this.report.status==='todos'||s.status===this.report.status)).sort((a,b)=>{let nums=x=>this.receipts.filter(r=>Number(r.serviceId)===Number(x.id)).map(r=>Number(String(r.number||'').replace(/\D/g,''))||0).filter(Boolean),aa=nums(a),bb=nums(b),na=aa.length?Math.min(...aa):0,nb=bb.length?Math.min(...bb):0;if(na&&nb&&na!==nb)return na-nb;if(na&&!nb)return-1;if(!na&&nb)return 1;return String(a.serviceDate||'').localeCompare(String(b.serviceDate||''))||Number(a.id||0)-Number(b.id||0)})},
  get reportDetailRows(){let rows=[],inPeriod=d=>(!this.report.from||String(d||'')>=this.report.from)&&(!this.report.to||String(d||'')<=this.report.to),services=this.services.filter(s=>(this.report.category==='todas'||s.category===this.report.category)&&(this.report.status==='todos'||s.status===this.report.status));for(const s of services){let ps=this.payments.filter(p=>Number(p.serviceId)===Number(s.id)).sort((a,b)=>String(a.date||'').localeCompare(String(b.date||''))||Number(a.id||0)-Number(b.id||0));if(ps.length){for(const p of ps){if(!inPeriod(p.date))continue;let r=p.receiptId?this.receipts.find(x=>Number(x.id)===Number(p.receiptId)):null;rows.push({...s,reportKey:'p-'+p.id,receiptNumber:r?.number||'PENDENTE',paymentDate:p.date||'',description:r?.description||s.description,reportValue:Number(p.amount||0),paymentMethod:p.method||r?.paymentMethod||s.paymentMethod||''})}}else if(inPeriod(s.serviceDate))rows.push({...s,reportKey:'s-'+s.id,receiptNumber:'-',paymentDate:'',description:s.description,reportValue:0,paymentMethod:''})}return rows.sort((a,b)=>String(a.paymentDate||a.serviceDate||'').localeCompare(String(b.paymentDate||b.serviceDate||''))||String(a.receiptNumber||'').localeCompare(String(b.receiptNumber||'')))},get reportTotals(){let e=this.reportRows.reduce((a,s)=>a+Number(s.amount||0),0),p=this.reportDetailRows.reduce((a,s)=>a+Number(s.reportValue||0),0),o=this.reportRows.reduce((a,s)=>a+this.balance(s),0);return{expected:e,paid:p,open:o}},backupStats(){let records=this.providers.length+this.services.length+this.receipts.length+this.recurrences.length+this.payments.length;return{records,providers:this.providers.length,services:this.services.length,receipts:this.receipts.length,recurrences:this.recurrences.length,payments:this.payments.length}},backupDays(){if(!this.backupLast)return null;let d=new Date(this.backupLast), now=new Date();return Math.max(0,Math.floor((now-d)/864

--- MATCH 5 @ 6469268 ---
er||'').localeCompare(String(a.number||'')))},
  get reportRows(){return this.services.filter(s=>(!this.report.from||s.serviceDate>=this.report.from)&&(!this.report.to||s.serviceDate<=this.report.to)&&(this.report.category==='todas'||s.category===this.report.category)&&(this.report.status==='todos'||s.status===this.report.status)).sort((a,b)=>{let nums=x=>this.receipts.filter(r=>Number(r.serviceId)===Number(x.id)).map(r=>Number(String(r.number||'').replace(/\D/g,''))||0).filter(Boolean),aa=nums(a),bb=nums(b),na=aa.length?Math.min(...aa):0,nb=bb.length?Math.min(...bb):0;if(na&&nb&&na!==nb)return na-nb;if(na&&!nb)return-1;if(!na&&nb)return 1;return String(a.serviceDate||'').localeCompare(String(b.serviceDate||''))||Number(a.id||0)-Number(b.id||0)})},
  get reportDetailRows(){let rows=[],inPeriod=d=>(!this.report.from||String(d||'')>=this.report.from)&&(!this.report.to||String(d||'')<=this.report.to),services=this.services.filter(s=>(this.report.category==='todas'||s.category===this.report.category)&&(this.report.status==='todos'||s.status===this.report.status));for(const s of services){let ps=this.payments.filter(p=>Number(p.serviceId)===Number(s.id)).sort((a,b)=>String(a.date||'').localeCompare(String(b.date||''))||Number(a.id||0)-Number(b.id||0));if(ps.length){for(const p of ps){if(!inPeriod(p.date))continue;let r=p.receiptId?this.receipts.find(x=>Number(x.id)===Number(p.receiptId)):null;rows.push({...s,reportKey:'p-'+p.id,receiptNumber:r?.number||'PENDENTE',paymentDate:p.date||'',description:r?.description||s.description,reportValue:Number(p.amount||0),paymentMethod:p.method||r?.paymentMethod||s.paymentMethod||''})}}else if(inPeriod(s.serviceDate))rows.push({...s,reportKey:'s-'+s.id,receiptNumber:'-',paymentDate:'',description:s.description,reportValue:0,paymentMethod:''})}return rows.sort((a,b)=>String(a.paymentDate||a.serviceDate||'').localeCompare(String(b.paymentDate||b.serviceDate||''))||String(a.receiptNumber||'').localeCompare(String(b.receiptNumber||'')))},get reportTotals(){let e=this.reportRows.reduce((a,s)=>a+Number(s.amount||0),0),p=this.reportDetailRows.reduce((a,s)=>a+Number(s.reportValue||0),0),o=this.reportRows.reduce((a,s)=>a+this.balance(s),0);return{expected:e,paid:p,open:o}},backupStats(){let records=this.providers.length+this.services.length+this.receipts.length+this.recurrences.length+this.payments.length;return{records,providers:this.providers.length,services:this.services.length,receipts:this.receipts.length,recurrences:this.recurrences.length,payments:this.payments.length}},backupDays(){if(!this.backupLast)return null;let d=new Date(this.backupLast), now=new Date();return Math.max(0,Math.floor((now-d)/86400000))},backupLastLabel(){if(!
