
===== removeReceiptInternal( =====

--- occurrence 1 at 6498726 ---
is.isViewer)return;this.actionModal={open:true,type:'deleteReceipt',tone:'danger',icon:'receipt-text',target:r,service:null,title:'Excluir recibo',subtitle:'Essa ação remove o recibo do histórico.',note:'A numeração já utilizada não será reaproveitada, para manter a rastreabilidade da prestação de contas.',confirmLabel:'Excluir recibo',details:[{label:'Recibo nº',value:r.number||'-'},{label:'Prestador',value:r.providerName||'-'},{label:'Valor',value:this.money(r.total||0)}]};this.after()},openClearAllModal(){if(this.isViewer)return;this.actionModal={open:true,type:'clearAll',tone:'danger',icon:'database-zap',target:null,service:null,title:'Apagar dados operacionais',subtitle:'Essa ação remove os registros operacionais salvos neste navegador.',note:'A numeração oficial dos recibos e os dados do condomínio serão preservados. Faça backup JSON antes de apagar, caso precise recuperar os registros depois.',confirmLabel:'Apagar registros',details:[{label:'Remove',value:'Prestadores, serviços, recibos, recorrências e logs'},{label:'Preserva',value:'Dados do condomínio e sequência dos recibos'}]};this.after()},closeActionModal(){this.actionModal={open:false,type:'',tone:'default',icon:'',target:null,service:null,title:'',subtitle:'',note:'',confirmLabel:'',details:[]}},async confirmActionModal(){if(this.isViewer)return;let m=this.actionModal,t=m.target||m.service;if(m.type==='duplicate'){await this.duplicateServiceInternal(m.service)}else if(m.type==='deleteService'){await this.removeServiceInternal(t.id)}else if(m.type==='deleteSinglePayment'){await this.removeSinglePaymentInternal(t.id)}else if(m.type==='deletePayment'){await this.removePaymentInternal(t.id)}else if(m.type==='deleteProvider'){await this.removeProviderInternal(t.id)}else if(m.type==='deleteReceipt'){await this.removeReceiptInternal(t.id)}else if(m.type==='deleteRecurrence'){await this.removeRecurrenceInternal(t.id)}else if(m.type==='clearAll'){await this.clearAllInternal()}this.closeActionModal()},async duplicateServiceInternal(s){let d={...s};delete d.id;delete d.recurrenceId;d.status='Pendente';d.paidValue=0;d.paymentDate='';d.paymentMethod='';d.serviceDate=this.today();d.dueDate=this.today();d.reference=this.today().slice(0,7);await db.services.add(d);await this.logAction('service','Lançamento duplicado',this.providerName(d.providerId)+' • '+this.money(d.amount||0));await this.load()},async removeServiceInternal(id){let s=this.services.find(x=>Number(x.id)===Number(id));if(!s)return;let linked=this.receipts.find(r=>Number(r.serviceId)===Number(id));if(linked||Number(s.paidValue||0)>0){let linkedCount=this.receipts.filter(r=>Number(r.serviceId)===Number(id)).length;return this.toast(linkedCount>1?'Este lançamento possui '+linkedCount+' recibos vinculados. Exclua pelo fluxo de pagamentos/recibos para preservar o histórico.':linked?'Este lançamento possui o recibo nº '+linked.number+'. Exclua pelo fluxo de pagamento/recibo para preservar o histórico.':'Este lançamento possui pagamento registrado. Exclua pelo fluxo de pagamentos para preservar o histórico.','warning')}await db.services.delete(id);await this.logAction('delete','Lançamento excluído',this.providerName(s.providerId)+' • '+this.money(s.amount||0));await this.load()},async removeSinglePaymentInternal(id){let p=this.payments.find(x=>Number(x.id)===Number(id));if(!p)return;let s=this.services.find(x=>Number(x.id)===Number(p.serviceId));if(!s)return this.toast('Lançamento deste pagamento não foi encontrado.','warning');if(p.receiptId){let r=this.receipts.find(x=>Number(x.id)===Number(p.receiptId));return this.toast('Este pagamento possui o recibo nº '+(r?.number||p.receiptId)+'. Exclua o recibo primeiro para preservar a rastreabilidade.','warning')}let removed=Number(p.amount||0);await db.transaction('rw',db.payments,db.services,async()=>{await db.payments.delete(p.id);let remaining=await db.payments.where('serviceId').equals(s.id).toArray(),paid=remaining.reduce((a,x)=>a+Number(x.amount||0),0),latest=remaining.slice().sort((a,b)=>String(b.date||'').localeCompare(String(a.date||''))||Number(b.id||0)-Number(a.id||0))[0]||null,status=paid<=0?'Pendente':(paid>=Number(s.amount||0)-0.000001?'Pago':'Parcial');await db.services.update(s.id,{paidValue:paid,paymentDate:latest?.date||'',paymentMethod:latest?.method||'',status})});await this.logAction('delete','Pagamento individual excluído',this.providerName(p.providerId??s.providerId)+' • '+this.money(removed)+' • '+this.fmt(p.date));await this.load();this.toast('Pagamento de '+this.money(removed)+' excluído. Os demais registros foram preservados.')},async removePaymentInternal(id){let s=this.services.find(x=>Number(x.id)===Number(id));if(!s)return;let linked=this.receipts.filter(r=>Number(r.serviceId)===Number(id));await db.transaction('rw',db.receipts,db.services,db.payments,async()=>{for(const r of linked)await db.receipts.delete(r.id);await db.payments.where('serviceId').equals(id).delete();await db.services.update(id,{paidValue:0,paymentDate:'',paymentMethod:'',status:'Pendente'})});for(const r of linked)await this.logAction('delete','Recibo removido por exclusão de pagamento','Nº '+r.number+' • '+(r.providerName||this.providerName(s.providerId)));await this.logAction('delete','Pagamentos excluídos; lançamento mantido',this.providerName(s.providerId)+' • '+this.money(s.paidValue||0));await this.load()},
  pay(s){this.paymentTarget=s;this.payment={date:this.today(),value:this.balance(s),method:'PIX'};this.payModal=true},async savePayment(){if(this.isViewer)return;let s=this.paymentTarget,value=Number(this.payment.value||0),paymentDate=this.payment.date||this.today(),method=this.payment.method||'Não informado',remaining=Math.max(0,Number(s.amount||0)-Number(s.paidValue||0));if(value<=0)return this.toast('Informe um valor de pagamento maior que zero.','warning');if(value>remaining+0.000001)return this.toast('O pagamento não pode ser maior que o saldo pendente de '+this.money(remaining)+'.','warning');let paid=Number(s.paidValue||0)+value,status=paid>=Number(s.amount)?'Pago':'Parcial';await db.transaction('rw',db.services,db.payments,async()=>{await db.payments.add({serviceId:s.id,providerId:s.providerId,date:paymentDate,amount:value,method,receiptId:null,source:'manual',createdAt:new Date().toISOString()});await db.service
--- occurrence 2 at 6507905 ---
receiptTotal=payment?Number(payment.amount||0):legacyPending,paymentDate=payment?.date||s.paymentDate||this.today(),paymentMethod=payment?.method||s.paymentMethod||'Não informado',desc=String(s.description||'').replace(/(\bparcela\s+)(\d+)\s*\/\s*(\d+)/i,(m,prefix,current,total)=>prefix+String(linked.length+1).padStart(current.length,'0')+'/'+total),p=this.providers.find(x=>Number(x.id)===Number(s.providerId))||{},r=null;await db.transaction('rw',db.meta,db.receipts,db.payments,async()=>{let meta=await db.meta.get('receiptSeq'),next=Math.max(Number(meta?.value||0),this.receiptMaxNumber())+1;r={number:String(next).padStart(6,'0'),date:this.today(),paymentDate,providerId:p.id,providerName:p.name||'Prestador não informado',document:p.document||'',category:s.category,reference:s.reference,description:desc,total:receiptTotal,paymentMethod,location:s.location,serviceId:s.id,paymentId:payment?.id||null,manager:this.settings.manager,condoName:this.settings.condoName,condoCnpj:this.settings.cnpj||'',condoAddress:this.settings.address||''};await db.meta.put({key:'receiptSeq',value:next});let id=await db.receipts.add(r);r.id=id;if(payment)await db.payments.update(payment.id,{receiptId:id})});this.currentReceipt=r;await this.logAction('receipt','Recibo emitido','Nº '+r.number+' • '+r.providerName+' • '+this.money(r.total||0));await this.load();this.toast('Recibo nº '+r.number+' emitido no valor de '+this.money(r.total||0)+'.')},printReceipt(){if(!this.currentReceipt&&this.receipts.length)this.currentReceipt=this.receipts[this.receipts.length-1];document.getElementById('receiptPrint').classList.remove('hidden');document.getElementById('reportPrint').classList.add('hidden');document.getElementById('annualReportPrint').classList.add('hidden');setTimeout(()=>window.print(),100)},async removeReceiptInternal(id){let r=this.receipts.find(x=>Number(x.id)===Number(id));if(!r)return;let linked=this.receipts.filter(x=>Number(x.serviceId)===Number(r.serviceId)).sort((a,b)=>Number(b.id||0)-Number(a.id||0)),latest=linked[0];if(latest&&Number(latest.id)!==Number(r.id))return this.toast('Para preservar a data e a forma de pagamento das parcelas, somente o recibo mais recente deste lançamento pode ser excluído e reemitido.','warning');await db.transaction('rw',db.receipts,db.payments,async()=>{await db.receipts.delete(id);let ps=this.payments.filter(p=>Number(p.receiptId)===Number(id));for(const p of ps)await db.payments.update(p.id,{receiptId:null})});if(this.currentReceipt&&Number(this.currentReceipt.id)===Number(id))this.currentReceipt=null;await this.logAction('delete','Recibo excluído',r?('Nº '+r.number+' • '+r.providerName):('ID '+id));await this.load()},
  printReport(){document.body.classList.add('printing-report');document.getElementById('receiptPrint').classList.add('hidden');document.getElementById('annualReportPrint').classList.add('hidden');document.getElementById('reportPrint').classList.remove('hidden');setTimeout(()=>{window.print();document.body.classList.remove('printing-report');document.getElementById('receiptPrint').classList.remove('hidden');document.getElementById('annualReportPrint').classList.add('hidden');document.getElementById('reportPrint').classList.add('hidden')},100)},printAnnualReport(){document.body.classList.add('printing-annual');let rp=document.getElementById('receiptPrint'),mp=document.getElementById('reportPrint'),ap=document.getElementById('annualReportPrint');rp.classList.add('hidden');mp.classList.add('hidden');ap.classList.remove('hidden');ap.style.display='block';ap.style.visibility='visible';setTimeout(()=>{window.print();document.body.classList.remove('printing-annual');rp.classList.remove('hidden');ap.classList.add('hidden');ap.style.display='';ap.style.visibility='';mp.classList.add('hidden')},180)},csv(){let rows=[['Recibo nº','Data do serviço','Data do pagamento','Prestador','Categoria','Descrição','Valor do serviço','Valor deste pagamento','Status','Forma']].concat(this.reportDetailRows.map(s=>[s.receiptNumber,s.serviceDate,s.paymentDate||'',this.providerName(s.providerId),s.category,s.description,s.amount,s.reportValue||0,s.status,s.paymentMethod||'']));let csv=rows.map(r=>r.map(v=>'"'+String(v??'').replaceAll('"','""')+'"').join(';')).join('\n');this.download('relatorio-prestadores-recibocondo.csv',csv,'text/csv;charset=utf-8')},
  async saveSettings(){let plainSettings=JSON.parse(JSON.stringify(this.settings));await db.settings.put({key:'main',value:plainSettings});await this.logAction('settings','Configurações salvas','Dados do condomínio atualizados.');this.toast('Configurações salvas')},async savePasswords(){if(this.isViewer)return;let cur=JSON.parse(localStorage.getItem('rc_passwords')||'{"admin":"123456","viewer":"123456"}');if(this.passwords.admin)cur.admin=this.passwords.admin;if(this.passwords.viewer)cur.viewer=this.passwords.viewer;localStorage.setItem('rc_passwords',JSON.stringify(cur));await this.logAction('settings','Senhas atualizadas','Senhas locais do app foram atualizadas.');this.passwords={admin:'',viewer:''};this.toast('Senhas atualizadas')},async backup(){let now=new Date().toISOString();let seqMeta=await db.meta.get('receiptSeq');let data={app:'ReciboCondo',backupFormat:3,settings:this.settings,providers:this.providers,services:this.services,receipts:this.receipts,recurrences:this.recurrences,payments:this.payments,logs:this.logs,receiptSeq:Number(seqMeta?.value||this.receiptMaxNumber()||0),backupStats:this.backupStats(),exportedAt:now};this.download('backup-recibocondo-prestadores.json',JSON.stringify(data,null,2),'application/json');this.backupLast=now;localStorage.setItem('rc_last_backup',now);await this.logAction('backup','Backup exportado','Backup JSON gerado com '+data.backupStats.records+' registros.')},async restore(ev){let f=ev.target.files[0];if(!f)return;try{let data=JSON.parse(await f.text());if(!data||typeof data!=='object'||Array.isArray(data))throw new Error('Backup inválido');let known=['settings','providers','services','receipts','recurrences','payments','logs','receiptSeq','backupStats','exportedAt','backupFormat','app'].some(k=>Object.prototype.hasOwnProperty.call(data,k));if(!known)throw new Error('O arquivo não parece ser um backup do ReciboCondo.');let providers=Array.isArray(data.providers)?data.providers:[],servic
COUNT=2

===== openDeleteReceipt =====

--- occurrence 1 at 2200664 ---
eceiptFilter.month" type="month" class="input mt-1"></label>
              <label><span class="text-xs font-black uppercase tracking-[.12em] text-slate-500">Categoria</span><select x-model="receiptFilter.category" class="input mt-1"><option value="todas">Todas</option><template x-for="c in categories"><option x-text="c"></option></template></select></label>
              <div class="flex items-end"><button @click="clearReceiptFilters" class="secondary w-full">Limpar filtros</button></div>
            </div>
            <div class="rounded-2xl border border-slate-200/60 dark:border-slate-700/60 bg-white/45 dark:bg-slate-950/30 px-4 py-3 mb-4 text-sm flex flex-wrap gap-2 justify-between">
              <span><b x-text="filteredReceipts.length"></b> recibo(s) filtrado(s)</span>
              <span class="font-black text-emerald-700" x-text="money(filteredReceipts.reduce((a,r)=>a+Number(r.total||0),0))"></span>
            </div>
            <div class="grid md:grid-cols-2 xl:grid-cols-3 gap-3">
              <template x-for="r in filteredReceipts" :key="r.id">
                <div class="rounded-2xl border border-slate-200/60 dark:border-slate-700/60 p-3 bg-white/55 dark:bg-slate-950/30">
                  <div class="flex justify-between gap-3"><b x-text="'Recibo Nº '+r.number"></b><span class="text-xs text-slate-500" x-text="fmt(r.date)"></span></div>
                  <p class="text-sm mt-1" x-text="r.providerName+' • '+r.category"></p>
                  <p class="text-xs text-slate-500" x-text="'Ref.: '+month(r.reference)"></p>
                  <p class="font-black text-emerald-600 mt-2" x-text="money(r.total)"></p>
                  <div class="mt-3 flex gap-2 flex-wrap"><button @click="currentReceipt=r;printReceipt()" class="mini">Reimprimir</button><button @click="openDeleteReceiptModal(r)" class="danger" :disabled="isViewer">Excluir</button></div>
                </div>
              </template>
            </div>
            <p x-show="!filteredReceipts.length" class="text-sm text-slate-500 text-center py-6">Nenhum recibo encontrado com os filtros atuais.</p>
          </div>
        </section>

        <section x-show="view==='relatorios'" x-effect="view; report.from; report.to; report.category; report.status; if(view==='relatorios') $nextTick(()=>renderCharts())" class="space-y-4">
          <div class="glass rounded-3xl p-4 grid md:grid-cols-5 gap-3 items-end"><label><span class="text-sm font-bold">Início</span><input x-model="report.from" @change="$nextTick(()=>renderCharts())" type="date" class="input mt-1"></label><label><span class="text-sm font-bold">Fim</span><input x-model="report.to" @change="$nextTick(()=>renderCharts())" type="date" class="input mt-1"></label><label><span class="text-sm font-bold">Categoria</span><select x-model="report.category" @change="$nextTick(()=>renderCharts())" class="input mt-1"><option value="todas">Todas</option><template x-for="c in categories"><option x-text="c"></option></template></select></label><label><span class="text-sm font-bold">Status</span><select x-model="report.status" @change="$nextTick(()=>renderCharts())" class="input mt-1"><option value="todos">Todos</option><option>Pendente</option><option>Parcial</option><option>Pago</option></select></label><div class="flex gap-2"><button @click="csv" class="primary flex-1">CSV</button><button @click="printReport" class="secondary">PDF</button></div></div>
          <div class="grid xl:grid-cols-3 gap-4"><div class="glass rounded-3xl p-4 xl:col-span-2"><div class="flex items-center justify-between gap-3 mb-3"><div><h3 class="font-black">Pagamentos no ano</h3><p class="text-xs text-slate-500">Visão anual independente dos filtros do relatório</p></div><label class="flex items-center gap-2 text-sm font-bold">Ano <select x-model.number="reportChartYear" @change="$nextTick(()=>renderCharts())" class="input py-2 w-28"><template x-for="y in reportChartYears" :key="y"><option :value="y" x-text="y"></option></template></select></label></div><div class="report-chart-box"><canvas id="repChart"></canvas></div></div><div class="glass rounded-3xl p-4"><h3 class="font-black mb-3">Resumo</h3><p class="flex justify-between"><span>Lançado no período</span><b x-text="money(reportTotals.expected)"></b></p><p class="flex justify-between"><span>Pago no período</span><b class="text-emerald-600" x-text="money(reportTotals.paid)"></b></p><p class="flex justify-between"><span>Em aberto dos lançamentos</span><b class="text-red-600" x-text="money(reportTotals.open)"></b></p></div></div>
          <div class="glass rounded-3xl p-4 overflow-auto scrollbar"><table class="w-full text-sm"><thead class="text-left text-slate-500"><tr><th class="p-2">Recibo nº</th><th class="p-2">Serviço</th><th class="p-2">Pagamento</th><th class="p-2">Prestador</th><th class="p-2">Categoria</th><th class="p-2">Valor</th><th class="p-2">Pago</th><th class="p-2">Status</th></tr></thead><tbody><template x-for="s in reportDetailRows" :key="s.reportKey"><tr class="border-t border-slate-200/60 dark:border-slate-700/60"><td class="p-2 font-bold text-amber-700" x-text="s.receiptNumber"></td><td class="p-2" x-text="fmt(s.serviceDate)"></td><td class="p-2" x-text="fmt(s.paymentDate)"></td><td class="p-2" x-text="providerName(s.providerId)"></td><td class="p-2" x-text="s.category"></td><td class="p-2" x-text="money(s.amount)"></td><td class="p-2" x-text="money(s.reportValue||0)"></td><td class="p-2" x-text="s.status"></td></tr></template></tbody></table></div>
        </section>

        <section x-show="view==='busca'" class="space-y-4">
          <div class="glass rounded-3xl p-4 md:p-5">
            <div class="flex flex-col lg:flex-row lg:items-end justify-between gap-4">
              <div>
                <p class="text-xs uppercase tracking-[.18em] text-rc-gold font-black">Central de localização</p>
                <h3 class="font-black text-2xl">Busca global inteligente</h3>
                <p class="text-sm text-slate-500 dark:text-slate-400 mt-1">Localize prestadores, serviços, recibos e recorrências em um só lugar.</p>
              </div>
              <div class="w-full lg:w-[420px]">
                <label class="text-sm font-bold">Pesquisar</label>
                <input x-model="q" class="input mt-1" placeholder="Ex.: jardine
--- occurrence 2 at 6496895 ---
e pagamentos.',note:receipt?'Este pagamento possui o recibo nº '+(receipt.number||'-')+'. Exclua primeiro esse recibo para liberar a correção do pagamento.':'Os demais pagamentos e recibos deste lançamento serão preservados. O total pago e o status serão recalculados automaticamente.',confirmLabel:'Excluir este pagamento',details:[{label:'Prestador',value:this.providerName(p.providerId??s.providerId)},{label:'Valor',value:this.money(p.amount||0)},{label:'Data/forma',value:this.fmt(p.date)+' • '+(p.method||'não informada')},{label:'Recibo',value:receipt?('nº '+(receipt.number||'-')):'não emitido'}]};this.after()},openDeletePaymentModal(s){if(this.isViewer)return;let linked=this.receipts.filter(r=>Number(r.serviceId)===Number(s.id)),numbers=linked.map(r=>r.number).filter(Boolean),receiptText=linked.length===1?'O recibo vinculado nº '+numbers[0]+' também será removido.':linked.length>1?linked.length+' recibos vinculados também serão removidos: '+numbers.join(', ')+'.':'O pagamento será removido, mas o lançamento será mantido e voltará como Pendente.';this.actionModal={open:true,type:'deletePayment',tone:'danger',icon:'circle-x',target:s,service:s,title:'Excluir todos os pagamentos',subtitle:'Essa ação zera todo o valor pago deste lançamento.',note:linked.length?receiptText+' O lançamento será mantido e voltará como Pendente; a numeração dos recibos não será reaproveitada.':receiptText,confirmLabel:'Excluir pagamentos',details:[{label:'Prestador',value:this.providerName(s.providerId)},{label:'Valor pago',value:this.money(s.paidValue||0)},{label:'Data/forma',value:(s.paymentDate?this.fmt(s.paymentDate):'-')+' • '+(s.paymentMethod||'não informada')},{label:'Recibos vinculados',value:linked.length?(numbers.join(', ')||linked.length+' recibo(s)'):'não emitido'}]};this.after()},openDeleteReceiptModal(r){if(this.isViewer)return;this.actionModal={open:true,type:'deleteReceipt',tone:'danger',icon:'receipt-text',target:r,service:null,title:'Excluir recibo',subtitle:'Essa ação remove o recibo do histórico.',note:'A numeração já utilizada não será reaproveitada, para manter a rastreabilidade da prestação de contas.',confirmLabel:'Excluir recibo',details:[{label:'Recibo nº',value:r.number||'-'},{label:'Prestador',value:r.providerName||'-'},{label:'Valor',value:this.money(r.total||0)}]};this.after()},openClearAllModal(){if(this.isViewer)return;this.actionModal={open:true,type:'clearAll',tone:'danger',icon:'database-zap',target:null,service:null,title:'Apagar dados operacionais',subtitle:'Essa ação remove os registros operacionais salvos neste navegador.',note:'A numeração oficial dos recibos e os dados do condomínio serão preservados. Faça backup JSON antes de apagar, caso precise recuperar os registros depois.',confirmLabel:'Apagar registros',details:[{label:'Remove',value:'Prestadores, serviços, recibos, recorrências e logs'},{label:'Preserva',value:'Dados do condomínio e sequência dos recibos'}]};this.after()},closeActionModal(){this.actionModal={open:false,type:'',tone:'default',icon:'',target:null,service:null,title:'',subtitle:'',note:'',confirmLabel:'',details:[]}},async confirmActionModal(){if(this.isViewer)return;let m=this.actionModal,t=m.target||m.service;if(m.type==='duplicate'){await this.duplicateServiceInternal(m.service)}else if(m.type==='deleteService'){await this.removeServiceInternal(t.id)}else if(m.type==='deleteSinglePayment'){await this.removeSinglePaymentInternal(t.id)}else if(m.type==='deletePayment'){await this.removePaymentInternal(t.id)}else if(m.type==='deleteProvider'){await this.removeProviderInternal(t.id)}else if(m.type==='deleteReceipt'){await this.removeReceiptInternal(t.id)}else if(m.type==='deleteRecurrence'){await this.removeRecurrenceInternal(t.id)}else if(m.type==='clearAll'){await this.clearAllInternal()}this.closeActionModal()},async duplicateServiceInternal(s){let d={...s};delete d.id;delete d.recurrenceId;d.status='Pendente';d.paidValue=0;d.paymentDate='';d.paymentMethod='';d.serviceDate=this.today();d.dueDate=this.today();d.reference=this.today().slice(0,7);await db.services.add(d);await this.logAction('service','Lançamento duplicado',this.providerName(d.providerId)+' • '+this.money(d.amount||0));await this.load()},async removeServiceInternal(id){let s=this.services.find(x=>Number(x.id)===Number(id));if(!s)return;let linked=this.receipts.find(r=>Number(r.serviceId)===Number(id));if(linked||Number(s.paidValue||0)>0){let linkedCount=this.receipts.filter(r=>Number(r.serviceId)===Number(id)).length;return this.toast(linkedCount>1?'Este lançamento possui '+linkedCount+' recibos vinculados. Exclua pelo fluxo de pagamentos/recibos para preservar o histórico.':linked?'Este lançamento possui o recibo nº '+linked.number+'. Exclua pelo fluxo de pagamento/recibo para preservar o histórico.':'Este lançamento possui pagamento registrado. Exclua pelo fluxo de pagamentos para preservar o histórico.','warning')}await db.services.delete(id);await this.logAction('delete','Lançamento excluído',this.providerName(s.providerId)+' • '+this.money(s.amount||0));await this.load()},async removeSinglePaymentInternal(id){let p=this.payments.find(x=>Number(x.id)===Number(id));if(!p)return;let s=this.services.find(x=>Number(x.id)===Number(p.serviceId));if(!s)return this.toast('Lançamento deste pagamento não foi encontrado.','warning');if(p.receiptId){let r=this.receipts.find(x=>Number(x.id)===Number(p.receiptId));return this.toast('Este pagamento possui o recibo nº '+(r?.number||p.receiptId)+'. Exclua o recibo primeiro para preservar a rastreabilidade.','warning')}let removed=Number(p.amount||0);await db.transaction('rw',db.payments,db.services,async()=>{await db.payments.delete(p.id);let remaining=await db.payments.where('serviceId').equals(s.id).toArray(),paid=remaining.reduce((a,x)=>a+Number(x.amount||0),0),latest=remaining.slice().sort((a,b)=>String(b.date||'').localeCompare(String(a.date||''))||Number(b.id||0)-Number(a.id||0))[0]||null,status=paid<=0?'Pendente':(paid>=Number(s.amount||0)-0.000001?'Pago':'Parcial');await db.services.update(s.id,{paidValue:paid,paymentDate:latest?.date||'',paymentMethod:latest?.method||'',status})});await this.logAction('delete','Pagamento individual excluído',this.providerName(p.providerId??s.providerId)+' • '+this.money(removed)+' • '+this.fmt(p.da
COUNT=2

===== deleteReceipt =====

--- occurrence 1 at 6496979 ---
')+'. Exclua primeiro esse recibo para liberar a correção do pagamento.':'Os demais pagamentos e recibos deste lançamento serão preservados. O total pago e o status serão recalculados automaticamente.',confirmLabel:'Excluir este pagamento',details:[{label:'Prestador',value:this.providerName(p.providerId??s.providerId)},{label:'Valor',value:this.money(p.amount||0)},{label:'Data/forma',value:this.fmt(p.date)+' • '+(p.method||'não informada')},{label:'Recibo',value:receipt?('nº '+(receipt.number||'-')):'não emitido'}]};this.after()},openDeletePaymentModal(s){if(this.isViewer)return;let linked=this.receipts.filter(r=>Number(r.serviceId)===Number(s.id)),numbers=linked.map(r=>r.number).filter(Boolean),receiptText=linked.length===1?'O recibo vinculado nº '+numbers[0]+' também será removido.':linked.length>1?linked.length+' recibos vinculados também serão removidos: '+numbers.join(', ')+'.':'O pagamento será removido, mas o lançamento será mantido e voltará como Pendente.';this.actionModal={open:true,type:'deletePayment',tone:'danger',icon:'circle-x',target:s,service:s,title:'Excluir todos os pagamentos',subtitle:'Essa ação zera todo o valor pago deste lançamento.',note:linked.length?receiptText+' O lançamento será mantido e voltará como Pendente; a numeração dos recibos não será reaproveitada.':receiptText,confirmLabel:'Excluir pagamentos',details:[{label:'Prestador',value:this.providerName(s.providerId)},{label:'Valor pago',value:this.money(s.paidValue||0)},{label:'Data/forma',value:(s.paymentDate?this.fmt(s.paymentDate):'-')+' • '+(s.paymentMethod||'não informada')},{label:'Recibos vinculados',value:linked.length?(numbers.join(', ')||linked.length+' recibo(s)'):'não emitido'}]};this.after()},openDeleteReceiptModal(r){if(this.isViewer)return;this.actionModal={open:true,type:'deleteReceipt',tone:'danger',icon:'receipt-text',target:r,service:null,title:'Excluir recibo',subtitle:'Essa ação remove o recibo do histórico.',note:'A numeração já utilizada não será reaproveitada, para manter a rastreabilidade da prestação de contas.',confirmLabel:'Excluir recibo',details:[{label:'Recibo nº',value:r.number||'-'},{label:'Prestador',value:r.providerName||'-'},{label:'Valor',value:this.money(r.total||0)}]};this.after()},openClearAllModal(){if(this.isViewer)return;this.actionModal={open:true,type:'clearAll',tone:'danger',icon:'database-zap',target:null,service:null,title:'Apagar dados operacionais',subtitle:'Essa ação remove os registros operacionais salvos neste navegador.',note:'A numeração oficial dos recibos e os dados do condomínio serão preservados. Faça backup JSON antes de apagar, caso precise recuperar os registros depois.',confirmLabel:'Apagar registros',details:[{label:'Remove',value:'Prestadores, serviços, recibos, recorrências e logs'},{label:'Preserva',value:'Dados do condomínio e sequência dos recibos'}]};this.after()},closeActionModal(){this.actionModal={open:false,type:'',tone:'default',icon:'',target:null,service:null,title:'',subtitle:'',note:'',confirmLabel:'',details:[]}},async confirmActionModal(){if(this.isViewer)return;let m=this.actionModal,t=m.target||m.service;if(m.type==='duplicate'){await this.duplicateServiceInternal(m.service)}else if(m.type==='deleteService'){await this.removeServiceInternal(t.id)}else if(m.type==='deleteSinglePayment'){await this.removeSinglePaymentInternal(t.id)}else if(m.type==='deletePayment'){await this.removePaymentInternal(t.id)}else if(m.type==='deleteProvider'){await this.removeProviderInternal(t.id)}else if(m.type==='deleteReceipt'){await this.removeReceiptInternal(t.id)}else if(m.type==='deleteRecurrence'){await this.removeRecurrenceInternal(t.id)}else if(m.type==='clearAll'){await this.clearAllInternal()}this.closeActionModal()},async duplicateServiceInternal(s){let d={...s};delete d.id;delete d.recurrenceId;d.status='Pendente';d.paidValue=0;d.paymentDate='';d.paymentMethod='';d.serviceDate=this.today();d.dueDate=this.today();d.reference=this.today().slice(0,7);await db.services.add(d);await this.logAction('service','Lançamento duplicado',this.providerName(d.providerId)+' • '+this.money(d.amount||0));await this.load()},async removeServiceInternal(id){let s=this.services.find(x=>Number(x.id)===Number(id));if(!s)return;let linked=this.receipts.find(r=>Number(r.serviceId)===Number(id));if(linked||Number(s.paidValue||0)>0){let linkedCount=this.receipts.filter(r=>Number(r.serviceId)===Number(id)).length;return this.toast(linkedCount>1?'Este lançamento possui '+linkedCount+' recibos vinculados. Exclua pelo fluxo de pagamentos/recibos para preservar o histórico.':linked?'Este lançamento possui o recibo nº '+linked.number+'. Exclua pelo fluxo de pagamento/recibo para preservar o histórico.':'Este lançamento possui pagamento registrado. Exclua pelo fluxo de pagamentos para preservar o histórico.','warning')}await db.services.delete(id);await this.logAction('delete','Lançamento excluído',this.providerName(s.providerId)+' • '+this.money(s.amount||0));await this.load()},async removeSinglePaymentInternal(id){let p=this.payments.find(x=>Number(x.id)===Number(id));if(!p)return;let s=this.services.find(x=>Number(x.id)===Number(p.serviceId));if(!s)return this.toast('Lançamento deste pagamento não foi encontrado.','warning');if(p.receiptId){let r=this.receipts.find(x=>Number(x.id)===Number(p.receiptId));return this.toast('Este pagamento possui o recibo nº '+(r?.number||p.receiptId)+'. Exclua o recibo primeiro para preservar a rastreabilidade.','warning')}let removed=Number(p.amount||0);await db.transaction('rw',db.payments,db.services,async()=>{await db.payments.delete(p.id);let remaining=await db.payments.where('serviceId').equals(s.id).toArray(),paid=remaining.reduce((a,x)=>a+Number(x.amount||0),0),latest=remaining.slice().sort((a,b)=>String(b.date||'').localeCompare(String(a.date||''))||Number(b.id||0)-Number(a.id||0))[0]||null,status=paid<=0?'Pendente':(paid>=Number(s.amount||0)-0.000001?'Pago':'Parcial');await db.services.update(s.id,{paidValue:paid,paymentDate:latest?.date||'',paymentMethod:latest?.method||'',status})});await this.logAction('delete','Pagamento individual excluído',this.providerName(p.providerId??s.providerId)+' • '+this.money(removed)+' • '+this.fmt(p.date));await this.load();this.toast('Pagamento de '+this.money(removed)+' excluído. Os
--- occurrence 2 at 6498699 ---
DeleteReceiptModal(r){if(this.isViewer)return;this.actionModal={open:true,type:'deleteReceipt',tone:'danger',icon:'receipt-text',target:r,service:null,title:'Excluir recibo',subtitle:'Essa ação remove o recibo do histórico.',note:'A numeração já utilizada não será reaproveitada, para manter a rastreabilidade da prestação de contas.',confirmLabel:'Excluir recibo',details:[{label:'Recibo nº',value:r.number||'-'},{label:'Prestador',value:r.providerName||'-'},{label:'Valor',value:this.money(r.total||0)}]};this.after()},openClearAllModal(){if(this.isViewer)return;this.actionModal={open:true,type:'clearAll',tone:'danger',icon:'database-zap',target:null,service:null,title:'Apagar dados operacionais',subtitle:'Essa ação remove os registros operacionais salvos neste navegador.',note:'A numeração oficial dos recibos e os dados do condomínio serão preservados. Faça backup JSON antes de apagar, caso precise recuperar os registros depois.',confirmLabel:'Apagar registros',details:[{label:'Remove',value:'Prestadores, serviços, recibos, recorrências e logs'},{label:'Preserva',value:'Dados do condomínio e sequência dos recibos'}]};this.after()},closeActionModal(){this.actionModal={open:false,type:'',tone:'default',icon:'',target:null,service:null,title:'',subtitle:'',note:'',confirmLabel:'',details:[]}},async confirmActionModal(){if(this.isViewer)return;let m=this.actionModal,t=m.target||m.service;if(m.type==='duplicate'){await this.duplicateServiceInternal(m.service)}else if(m.type==='deleteService'){await this.removeServiceInternal(t.id)}else if(m.type==='deleteSinglePayment'){await this.removeSinglePaymentInternal(t.id)}else if(m.type==='deletePayment'){await this.removePaymentInternal(t.id)}else if(m.type==='deleteProvider'){await this.removeProviderInternal(t.id)}else if(m.type==='deleteReceipt'){await this.removeReceiptInternal(t.id)}else if(m.type==='deleteRecurrence'){await this.removeRecurrenceInternal(t.id)}else if(m.type==='clearAll'){await this.clearAllInternal()}this.closeActionModal()},async duplicateServiceInternal(s){let d={...s};delete d.id;delete d.recurrenceId;d.status='Pendente';d.paidValue=0;d.paymentDate='';d.paymentMethod='';d.serviceDate=this.today();d.dueDate=this.today();d.reference=this.today().slice(0,7);await db.services.add(d);await this.logAction('service','Lançamento duplicado',this.providerName(d.providerId)+' • '+this.money(d.amount||0));await this.load()},async removeServiceInternal(id){let s=this.services.find(x=>Number(x.id)===Number(id));if(!s)return;let linked=this.receipts.find(r=>Number(r.serviceId)===Number(id));if(linked||Number(s.paidValue||0)>0){let linkedCount=this.receipts.filter(r=>Number(r.serviceId)===Number(id)).length;return this.toast(linkedCount>1?'Este lançamento possui '+linkedCount+' recibos vinculados. Exclua pelo fluxo de pagamentos/recibos para preservar o histórico.':linked?'Este lançamento possui o recibo nº '+linked.number+'. Exclua pelo fluxo de pagamento/recibo para preservar o histórico.':'Este lançamento possui pagamento registrado. Exclua pelo fluxo de pagamentos para preservar o histórico.','warning')}await db.services.delete(id);await this.logAction('delete','Lançamento excluído',this.providerName(s.providerId)+' • '+this.money(s.amount||0));await this.load()},async removeSinglePaymentInternal(id){let p=this.payments.find(x=>Number(x.id)===Number(id));if(!p)return;let s=this.services.find(x=>Number(x.id)===Number(p.serviceId));if(!s)return this.toast('Lançamento deste pagamento não foi encontrado.','warning');if(p.receiptId){let r=this.receipts.find(x=>Number(x.id)===Number(p.receiptId));return this.toast('Este pagamento possui o recibo nº '+(r?.number||p.receiptId)+'. Exclua o recibo primeiro para preservar a rastreabilidade.','warning')}let removed=Number(p.amount||0);await db.transaction('rw',db.payments,db.services,async()=>{await db.payments.delete(p.id);let remaining=await db.payments.where('serviceId').equals(s.id).toArray(),paid=remaining.reduce((a,x)=>a+Number(x.amount||0),0),latest=remaining.slice().sort((a,b)=>String(b.date||'').localeCompare(String(a.date||''))||Number(b.id||0)-Number(a.id||0))[0]||null,status=paid<=0?'Pendente':(paid>=Number(s.amount||0)-0.000001?'Pago':'Parcial');await db.services.update(s.id,{paidValue:paid,paymentDate:latest?.date||'',paymentMethod:latest?.method||'',status})});await this.logAction('delete','Pagamento individual excluído',this.providerName(p.providerId??s.providerId)+' • '+this.money(removed)+' • '+this.fmt(p.date));await this.load();this.toast('Pagamento de '+this.money(removed)+' excluído. Os demais registros foram preservados.')},async removePaymentInternal(id){let s=this.services.find(x=>Number(x.id)===Number(id));if(!s)return;let linked=this.receipts.filter(r=>Number(r.serviceId)===Number(id));await db.transaction('rw',db.receipts,db.services,db.payments,async()=>{for(const r of linked)await db.receipts.delete(r.id);await db.payments.where('serviceId').equals(id).delete();await db.services.update(id,{paidValue:0,paymentDate:'',paymentMethod:'',status:'Pendente'})});for(const r of linked)await this.logAction('delete','Recibo removido por exclusão de pagamento','Nº '+r.number+' • '+(r.providerName||this.providerName(s.providerId)));await this.logAction('delete','Pagamentos excluídos; lançamento mantido',this.providerName(s.providerId)+' • '+this.money(s.paidValue||0));await this.load()},
  pay(s){this.paymentTarget=s;this.payment={date:this.today(),value:this.balance(s),method:'PIX'};this.payModal=true},async savePayment(){if(this.isViewer)return;let s=this.paymentTarget,value=Number(this.payment.value||0),paymentDate=this.payment.date||this.today(),method=this.payment.method||'Não informado',remaining=Math.max(0,Number(s.amount||0)-Number(s.paidValue||0));if(value<=0)return this.toast('Informe um valor de pagamento maior que zero.','warning');if(value>remaining+0.000001)return this.toast('O pagamento não pode ser maior que o saldo pendente de '+this.money(remaining)+'.','warning');let paid=Number(s.paidValue||0)+value,status=paid>=Number(s.amount)?'Pago':'Parcial';await db.transaction('rw',db.services,db.payments,async()=>{await db.payments.add({serviceId:s.id,providerId:s.providerId,date:paymentDate,amount:value,method,receiptId:null,source:'manual',createdAt:new Date().toISO
COUNT=2

===== receiptHistory =====

COUNT=0

===== receiptModal =====

COUNT=0

===== recentPaymentReceipt( =====

--- occurrence 1 at 2174672 ---
        <div class="rounded-3xl border border-amber-500/20 bg-amber-500/10 p-3"><p class="text-xs uppercase tracking-[.16em] text-amber-700 font-black">Recorrências</p><p class="text-xl font-black text-amber-700" x-text="servicePanelRecurringGenerated.length+'/'+recurrences.filter(r=>r.active).length"></p><p class="text-xs text-amber-700/80">geradas no mês</p></div>
              <div class="rounded-3xl border border-slate-200/70 dark:border-slate-700/70 bg-white/60 dark:bg-slate-950/30 p-3"><p class="text-xs uppercase tracking-[.16em] text-slate-500 font-black">Recibos</p><p class="text-xl font-black text-rc-gold" x-text="servicePanelReceiptCount"></p><p class="text-xs text-slate-500">emitidos no mês</p></div>
            </div>
            <div class="grid xl:grid-cols-3 gap-3">
              <div class="rounded-3xl border border-slate-200/70 dark:border-slate-700/70 bg-white/50 dark:bg-slate-950/30 p-3 xl:col-span-2">
                <div class="flex items-center justify-between gap-2 mb-2"><h4 class="font-black">Últimos pagamentos do mês</h4><span class="badge" x-text="month(servicePanelMonth)"></span></div>
                <div class="space-y-2">
                  <template x-for="p in servicePanelPaymentEvents.slice(0,5)" :key="'panel-pay-'+p.id">
          <div class="flex items-center justify-between gap-3 rounded-2xl bg-white/70 dark:bg-slate-900/40 border border-slate-200/60 dark:border-slate-700/60 p-2">
            <div><p class="font-black text-sm" x-text="providerName(p.providerId)"></p><p class="text-xs text-slate-500" x-text="fmt(p.date)+' • '+(p.service?.category||'Sem categoria')+' • '+(p.method||'forma não informada')"></p></div>
            <div class="text-right"><p class="font-black text-emerald-700" x-text="money(p.amount||0)"></p><button @click="recentPaymentReceipt(p)" class="mini" :disabled="isViewer || !p.service" x-text="p.receipt?'Ver recibo':'Emitir recibo'"></button></div>
          </div>
        </template>
                  <p x-show="!servicePanelPaymentEvents.length" class="text-sm text-slate-500 text-center py-3">Nenhum pagamento registrado neste mês.</p>
                </div>
              </div>
              <div class="rounded-3xl border border-slate-200/70 dark:border-slate-700/70 bg-white/50 dark:bg-slate-950/30 p-3">
                <h4 class="font-black mb-2">Pendências e recorrências</h4>
                <div class="space-y-2">
                  <template x-for="s in servicePanelOpenRows.slice(0,4)" :key="'open-'+s.id">
                    <div class="rounded-2xl border border-red-500/15 bg-red-500/5 p-2"><p class="font-black text-sm" x-text="providerName(s.providerId)"></p><p class="text-xs text-red-700" x-text="money(balance(s))+' • '+overdueLabel(s)"></p></div>
                  </template>
                  <template x-for="r in servicePanelPendingRecurrences.slice(0,3)" :key="'rec-'+r.id">
                    <div class="rounded-2xl border border-amber-500/20 bg-amber-500/8 p-2"><p class="font-black text-sm" x-text="providerName(r.providerId)"></p><p class="text-xs text-amber-700" x-text="'Recorrência pendente • '+money(r.amount)"></p></div>
                  </template>
                  <p x-show="!servicePanelOpenRows.length && !servicePanelPendingRecurrences.length" class="text-sm text-slate-500 text-center py-3">Mês sem pendências operacionais.</p>
                </div>
              </div>
            </div>
          </div>
          <form @submit.prevent="saveService" class="glass rounded-3xl p-4 space-y-3">
            <div class="flex flex-col sm:flex-row sm:items-center sm:justify-between gap-2"><div><h3 class="font-black text-lg">Lançar serviço/pagamento</h3><p class="text-xs text-slate-500 dark:text-slate-400">Cadastre primeiro o serviço, gratificação ou pagamento avulso. A lista de lançamentos fica organizada logo abaixo.</p></div><span class="badge">novo lançamento</span></div>
            <div class="grid md:grid-cols-3 gap-3"><label class="md:col-span-1"><span class="text-sm font-bold">Prestador</span><select x-model="serviceForm.providerId" required class="input mt-1"><option value="">Selecione</option><template x-for="p in providers"><option :value="p.id" x-text="p.name"></option></template></select></label><label><span class="text-sm font-bold">Categoria</span><select x-model="serviceForm.category" class="input mt-1"><template x-for="c in categories"><option x-text="c"></option></template></select></label><label><span class="text-sm font-bold">Referência</span><input x-model="serviceForm.reference" type="month" required class="input mt-1"></label></div>
            <div class="grid sm:grid-cols-2 gap-3"><label><span class="text-sm font-bold">Data do serviço</span><input x-model="serviceForm.serviceDate" type="date" required class="input mt-1"></label><label><span class="text-sm font-bold">Vencimento</span><input x-model="serviceForm.dueDate" type="date" required class="input mt-1"></label></div>
            <div class="grid sm:grid-cols-2 gap-3"><label><span class="text-sm font-bold">Valor do serviço</span><input x-model.number="serviceForm.amount" type="number" step="0.01" required class="input mt-1"></label><label><span class="text-sm font-bold">Local/Área</span><input x-model="serviceForm.location" class="input mt-1" placeholder="Jardim, portão, bloco..."></label></div>
            <div class="rounded-3xl border border-emerald-500/20 bg-emerald-500/5 p-3 space-y-3">
              <div class="flex items-start gap-2"><i data-lucide="circle-dollar-sign" class="w-4 h-4 mt-0.5 text-emerald-700"></i><div><p class="text-sm font-black">Dados do pagamento</p><p class="text-xs text-slate-500 dark:text-slate-400">Preencha se o serviço já foi pago. Esses campos sairão no recibo oficial.</p></div></div>
              <div class="grid sm:grid-cols-3 gap-3">
                <label><span class="text-sm font-bold">Data do pagamento</span><input x-model="serviceForm.paymentDate" type="date" class="input mt-1"></label>
                <label><span class="text-sm font-bold">Forma de pagamento</span><select x-model="serviceForm.paymentMethod" class="input mt-1"><option value="">Não informado</option><option>PIX</option><option>Dinheiro</option><option>Transferência</option><option>Boleto</option><option>Cheque</option><option>C
--- occurrence 2 at 2195110 ---
statusClass(s.status)" x-text="s.status"></span><br><span class="text-xs text-slate-500" x-show="s.paymentDate" x-text="fmt(s.paymentDate)+' • '+(s.paymentMethod||'forma não informada')"></span><span class="text-xs text-red-600" x-show="balance(s)>0" x-text="overdueLabel(s)"></span></td>
                        <td class="p-3 min-w-[150px]"><select class="input py-2 text-xs font-black" @change="paymentAction($event.target.value,s); $event.target.value=''"><option value="">Ações</option><option value="pagar" :disabled="isViewer || s.status==='Pago'">Pagar</option><option value="recibo" :disabled="isViewer || Number(s.paidValue||0)<=0" x-text="receiptActionLabel(s)"></option><option value="servico">Abrir serviço</option></select></td>
                      </tr>
                    </template>
                    <tr x-show="!filteredPayments.length"><td colspan="5" class="p-6 text-center text-slate-500">Nenhum pagamento encontrado para os filtros selecionados.</td></tr>
                  </tbody>
                </table>
              </div>
            </div>
            <div class="glass rounded-3xl p-4 space-y-3">
              <h3 class="font-black text-lg">Últimos pagamentos</h3>
              <template x-for="p in paymentRecent" :key="'recent-pay-'+p.id">
      <div class="rounded-2xl border border-slate-200/70 dark:border-slate-700/70 bg-white/55 dark:bg-slate-950/30 p-3">
        <div class="flex items-start justify-between gap-3"><div><p class="font-black text-sm" x-text="providerName(p.providerId)"></p><p class="text-xs text-slate-500" x-text="fmt(p.paymentDate)+' • '+(p.paymentMethod||'forma não informada')"></p></div><p class="font-black text-emerald-700" x-text="money(p.paidValue||0)"></p></div>
        <div class="flex flex-wrap gap-2 mt-2"><button @click="recentPaymentReceipt(p)" class="mini" :disabled="isViewer || !p.service" x-text="p.receipt?'Ver recibo':'Emitir recibo'"></button><button @click="p.service && openSearchService(p.service)" class="mini" :disabled="!p.service">Serviço</button><button @click="openDeleteSinglePaymentModal(p)" class="danger" :disabled="isViewer || !p.service" title="Exclui somente este pagamento">Excluir pagamento</button></div>
      </div>
    </template>
              <p x-show="!paymentRecent.length" class="text-sm text-slate-500 text-center py-3">Nenhum pagamento registrado ainda.</p>
            </div>
          </div>
        </section>

        <section x-show="view==='recibos'" class="space-y-4">
          <div class="glass rounded-3xl p-4 space-y-4">
            <div class="flex flex-col md:flex-row md:items-center md:justify-between gap-3">
              <div>
                <p class="text-xs uppercase tracking-[.18em] text-emerald-600 dark:text-emerald-400 font-black">Emissão</p>
                <h3 class="font-black text-lg">Emitir recibo profissional</h3>
                <p class="text-xs text-slate-500 dark:text-slate-400">Selecione um serviço/pagamento já lançado para gerar o recibo formal.</p>
              </div>
              <div class="rounded-2xl px-4 py-3 bg-emerald-500/10 border border-emerald-500/20 text-sm">
                <b x-text="receipts.length"></b> recibos no histórico
              </div>
            </div>
            <div class="grid lg:grid-cols-[1fr_auto] gap-3 items-end">
              <label>
                <span class="text-sm font-bold">Lançamento para emissão</span>
                <select x-model="receiptServiceId" class="input mt-1">
                  <option value="">Selecione um serviço/pagamento</option>
                  <template x-for="s in services">
                    <option :value="s.id" x-text="'#'+s.id+' • '+providerName(s.providerId)+' • '+s.category+' • '+money(s.amount)"></option>
                  </template>
                </select>
              </label>
              <div class="grid sm:grid-cols-2 gap-2 lg:min-w-[360px]">
                <button @click="issueReceipt" class="primary w-full" :disabled="isViewer">Emitir recibo</button>
                <button @click="printReceipt" class="secondary w-full">Imprimir / Salvar PDF</button>
              </div>
            </div>
            <div class="rounded-2xl border border-slate-200/60 dark:border-slate-700/60 bg-white/50 dark:bg-slate-950/30 p-3 text-xs text-slate-500 dark:text-slate-400">
              Use <b>Imprimir / Salvar PDF</b> e escolha “Salvar como PDF”. O recibo agora sai em layout A4 premium/oficial compacto, ajustado para caber em uma única página, com quadro de conferência, valor por extenso, assinatura e rodapé para prestação de contas.
            </div>
          </div>

          <div class="glass rounded-3xl p-4">
            <div class="flex flex-col md:flex-row md:items-center md:justify-between gap-2 mb-3">
              <div>
                <p class="text-xs uppercase tracking-[.18em] text-rc-600 dark:text-rc-300 font-black">Histórico</p>
                <h3 class="font-black text-lg">Recibos emitidos</h3>
              </div>
              <span class="text-xs text-slate-500 dark:text-slate-400">Reimpressão rápida e controle dos comprovantes já emitidos.</span>
            </div>
            <div class="grid md:grid-cols-4 gap-3 mb-4">
              <label><span class="text-xs font-black uppercase tracking-[.12em] text-slate-500">Buscar</span><input x-model="receiptFilter.q" class="input mt-1" placeholder="Nº, prestador, categoria..."></label>
              <label><span class="text-xs font-black uppercase tracking-[.12em] text-slate-500">Mês</span><input x-model="receiptFilter.month" type="month" class="input mt-1"></label>
              <label><span class="text-xs font-black uppercase tracking-[.12em] text-slate-500">Categoria</span><select x-model="receiptFilter.category" class="input mt-1"><option value="todas">Todas</option><template x-for="c in categories"><option x-text="c"></option></template></select></label>
              <div class="flex items-end"><button @click="clearReceiptFilters" class="secondary w-full">Limpar filtros</button></div>
            </div>
            <div class="rounded-2xl border border-slate-200/60 dark:border-slate-700/60 bg-white/45 dark:bg-slate-950/30 px-4 py-3 mb-4 text-sm flex flex-wrap gap-2 justify-between">
              <span><b x-text="filteredReceipts.le
--- occurrence 3 at 6503718 ---
Date:'',paymentMethod:'',status:'Pendente'})});for(const r of linked)await this.logAction('delete','Recibo removido por exclusão de pagamento','Nº '+r.number+' • '+(r.providerName||this.providerName(s.providerId)));await this.logAction('delete','Pagamentos excluídos; lançamento mantido',this.providerName(s.providerId)+' • '+this.money(s.paidValue||0));await this.load()},
  pay(s){this.paymentTarget=s;this.payment={date:this.today(),value:this.balance(s),method:'PIX'};this.payModal=true},async savePayment(){if(this.isViewer)return;let s=this.paymentTarget,value=Number(this.payment.value||0),paymentDate=this.payment.date||this.today(),method=this.payment.method||'Não informado',remaining=Math.max(0,Number(s.amount||0)-Number(s.paidValue||0));if(value<=0)return this.toast('Informe um valor de pagamento maior que zero.','warning');if(value>remaining+0.000001)return this.toast('O pagamento não pode ser maior que o saldo pendente de '+this.money(remaining)+'.','warning');let paid=Number(s.paidValue||0)+value,status=paid>=Number(s.amount)?'Pago':'Parcial';await db.transaction('rw',db.services,db.payments,async()=>{await db.payments.add({serviceId:s.id,providerId:s.providerId,date:paymentDate,amount:value,method,receiptId:null,source:'manual',createdAt:new Date().toISOString()});await db.services.update(s.id,{paidValue:paid,paymentDate,paymentMethod:method,status})});await this.logAction('payment','Pagamento registrado',this.providerName(s.providerId)+' • '+this.money(value)+' • '+method);this.payModal=false;await this.load();this.toast(status==='Pago'?'Pagamento registrado. Lançamento quitado.':'Pagamento parcial registrado. Saldo restante: '+this.money(Math.max(0,Number(s.amount||0)-paid)))},prepareReceipt(s){this.receiptServiceId=s.id;this.view='recibos';this.after()},async recentPaymentReceipt(p){if(this.isViewer||!p||!p.service)return;if(p.receipt){this.currentReceipt=p.receipt;this.view='recibos';this.after();setTimeout(()=>this.printReceipt(),220);return}this.receiptServiceId=p.service.id;await this.issueReceipt(p.id);this.view='recibos';this.after();setTimeout(()=>this.printReceipt(),260)},async quickReceipt(s){if(this.isViewer)return;if(Number(s.paidValue||0)<=0)return this.toast('Registre o pagamento antes de emitir o recibo.','warning');let receiptPaymentIds=new Set(this.receipts.map(r=>Number(r.paymentId)).filter(Boolean)),candidates=this.payments.filter(p=>Number(p.serviceId)===Number(s.id)&&!p.receiptId&&!receiptPaymentIds.has(Number(p.id))).sort((a,b)=>String(a.date||'').localeCompare(String(b.date||''))||Number(a.id||0)-Number(b.id||0));if(candidates.length){this.receiptServiceId=s.id;await this.issueReceipt(candidates[0].id);this.view='recibos';this.after();setTimeout(()=>this.printReceipt(),260);return}let linked=this.receipts.filter(r=>Number(r.serviceId)===Number(s.id)),latest=linked.slice().sort((a,b)=>Number(b.id||0)-Number(a.id||0))[0];if(latest){this.currentReceipt=latest;this.view='recibos';this.after();this.toast('Todos os pagamentos registrados já possuem recibo.','warning');setTimeout(()=>this.printReceipt(),220)}else this.toast('Não há pagamento pendente de recibo.','warning')},async issueReceipt(paymentId=null){if(this.isViewer)return;let s=this.services.find(x=>Number(x.id)===Number(this.receiptServiceId));if(!s)return this.toast('Selecione um lançamento.','warning');let linked=this.receipts.filter(r=>Number(r.serviceId)===Number(s.id)),issued=linked.reduce((a,r)=>a+Number(r.total||0),0),legacyPending=Math.max(0,Number(s.paidValue||0)-issued),receiptPaymentIds=new Set(linked.map(r=>Number(r.paymentId)).filter(Boolean)),candidates=this.payments.filter(p=>Number(p.serviceId)===Number(s.id)&&!p.receiptId&&!receiptPaymentIds.has(Number(p.id))).sort((a,b)=>String(a.date||'').localeCompare(String(b.date||''))||Number(a.id||0)-Number(b.id||0)),payment=paymentId?(candidates.find(p=>Number(p.id)===Number(paymentId))||null):(candidates[0]||null);if(paymentId&&!payment)return this.toast('Este pagamento já possui recibo ou não está disponível para emissão.','warning');if(!payment&&legacyPending<=0)return this.toast('Todos os pagamentos registrados deste lançamento já possuem recibo.','warning');let receiptTotal=payment?Number(payment.amount||0):legacyPending,paymentDate=payment?.date||s.paymentDate||this.today(),paymentMethod=payment?.method||s.paymentMethod||'Não informado',desc=String(s.description||'').replace(/(\bparcela\s+)(\d+)\s*\/\s*(\d+)/i,(m,prefix,current,total)=>prefix+String(linked.length+1).padStart(current.length,'0')+'/'+total),p=this.providers.find(x=>Number(x.id)===Number(s.providerId))||{},r=null;await db.transaction('rw',db.meta,db.receipts,db.payments,async()=>{let meta=await db.meta.get('receiptSeq'),next=Math.max(Number(meta?.value||0),this.receiptMaxNumber())+1;r={number:String(next).padStart(6,'0'),date:this.today(),paymentDate,providerId:p.id,providerName:p.name||'Prestador não informado',document:p.document||'',category:s.category,reference:s.reference,description:desc,total:receiptTotal,paymentMethod,location:s.location,serviceId:s.id,paymentId:payment?.id||null,manager:this.settings.manager,condoName:this.settings.condoName,condoCnpj:this.settings.cnpj||'',condoAddress:this.settings.address||''};await db.meta.put({key:'receiptSeq',value:next});let id=await db.receipts.add(r);r.id=id;if(payment)await db.payments.update(payment.id,{receiptId:id})});this.currentReceipt=r;await this.logAction('receipt','Recibo emitido','Nº '+r.number+' • '+r.providerName+' • '+this.money(r.total||0));await this.load();this.toast('Recibo nº '+r.number+' emitido no valor de '+this.money(r.total||0)+'.')},printReceipt(){if(!this.currentReceipt&&this.receipts.length)this.currentReceipt=this.receipts[this.receipts.length-1];document.getElementById('receiptPrint').classList.remove('hidden');document.getElementById('reportPrint').classList.add('hidden');document.getElementById('annualReportPrint').classList.add('hidden');setTimeout(()=>window.print(),100)},async removeReceiptInternal(id){let r=this.receipts.find(x=>Number(x.id)===Number(id));if(!r)return;let linked=this.receipts.filter(x=>Number(x.serviceId)===Number(r.serviceId)).sort((a,b)=>Number(b.id||0)-Number(a.id||0)),latest=linked[0];if(latest&&Number(latest.id)!==Number(r.id))return this.toast('Para preservar a
COUNT=3

===== quickReceipt( =====

--- occurrence 1 at 2150561 ---
">Recorrências</p><p class="text-2xl font-black mt-1" x-text="dayPendingRecurrences.length"></p><p class="text-xs text-slate-500">pendente(s) no mês</p></div>
            </div>
            <div class="grid xl:grid-cols-2 gap-3">
              <div class="space-y-2">
                <div class="flex items-center justify-between"><h4 class="font-black">Ações recomendadas</h4><span class="badge" x-text="dayCenterRows.length+' item(ns)'"></span></div>
                <div class="space-y-2 max-h-80 overflow-auto scrollbar">
                  <template x-for="item in dayCenterRows.slice(0,8)" :key="item.key">
                    <div class="day-center-row">
                      <div class="flex items-start justify-between gap-3">
                        <div class="min-w-0">
                          <p class="font-black truncate" x-text="item.title"></p>
                          <p class="text-xs text-slate-500 dark:text-slate-400" x-text="item.subtitle"></p>
                        </div>
                        <span class="text-xs font-black px-2 py-1 rounded-full shrink-0" :class="item.tone==='danger'?'bg-red-500/10 text-red-700':item.tone==='warning'?'bg-amber-500/10 text-amber-700':'bg-emerald-500/10 text-emerald-700'" x-text="item.badge"></span>
                      </div>
                      <div class="mt-2 flex items-center justify-between gap-2">
                        <b :class="item.tone==='danger'?'text-red-600':'text-emerald-700'" x-text="item.value"></b>
                        <div class="flex gap-2">
                          <button x-show="item.kind==='service'" @click="pay(item.service)" class="mini" :disabled="isViewer">Pagar</button>
                          <button x-show="item.kind==='service' && Number(item.service?.paidValue||0)>0" @click="quickReceipt(item.service)" class="mini">Recibo</button>
                          <button x-show="item.kind==='recurrence'" @click="generateRecurrence(item.recurrence)" class="mini" :disabled="isViewer">Gerar</button>
                        </div>
                      </div>
                    </div>
                  </template>
                  <p x-show="!dayCenterRows.length" class="text-sm text-slate-500 text-center py-4">Tudo em ordem para hoje.</p>
                </div>
              </div>
              <div class="rounded-3xl border border-emerald-500/20 bg-emerald-500/10 p-4">
                <div class="flex items-start gap-3">
                  <div class="h-10 w-10 rounded-2xl bg-emerald-600 text-white grid place-items-center shrink-0"><i data-lucide="shield-check" class="w-5 h-5"></i></div>
                  <div>
                    <h4 class="font-black">Leitura rápida</h4>
                    <p class="text-sm text-slate-600 dark:text-slate-300 mt-1" x-text="dayCenterSummary"></p>
                    <p class="text-xs text-slate-500 dark:text-slate-400 mt-3">Recorrência gera lançamento. Recibo continua sendo numerado somente quando emitido após pagamento.</p>
                  </div>
                </div>
              </div>
            </div>
          </div>

          <div class="grid xl:grid-cols-3 gap-4">
            <div class="glass rounded-3xl p-4 xl:col-span-2">
              <div class="flex flex-col sm:flex-row sm:items-center sm:justify-between gap-2 mb-3">
                <div><h3 class="font-black">Gasto mensal no ano</h3><p class="text-xs text-slate-500 dark:text-slate-400">Total pago por mês em <span x-text="dashboardYear"></span></p></div>
                <span class="badge" x-text="yearlyRows.length+' lançamento(s) no ano'"></span>
              </div>
              <div class="chart-box"><canvas id="monthlyChart"></canvas></div>
            </div>
            <div class="glass rounded-3xl p-4">
              <div class="flex items-center justify-between gap-2 mb-3"><h3 class="font-black">Pagamentos por categoria</h3><span class="badge" x-text="annualCategorySummary.length+' categorias'"></span></div>
              <div class="chart-box chart-box-sm"><canvas id="annualCatChart"></canvas></div>
            </div>
          </div>

          <div class="grid xl:grid-cols-3 gap-4">
            <div class="glass rounded-3xl p-4 xl:col-span-2 overflow-auto scrollbar">
              <div class="flex items-center justify-between mb-3"><h3 class="font-black">Histórico mensal consolidado</h3><span class="badge">Janeiro a dezembro</span></div>
              <table class="w-full text-sm min-w-[760px]">
                <thead class="text-left text-slate-500 dark:text-slate-400"><tr><th class="p-2">Mês</th><th class="p-2 text-right">Pagamentos</th><th class="p-2 text-right">Total pago</th><th class="p-2 text-right">Em aberto</th><th class="p-2">Principal categoria</th></tr></thead>
                <tbody>
                  <template x-for="m in monthlyHistory" :key="m.key">
                    <tr class="border-t border-slate-200/60 dark:border-slate-700/60">
                      <td class="p-2 font-black" x-text="m.label"></td>
                      <td class="p-2 text-right" x-text="m.count"></td>
                      <td class="p-2 text-right font-black text-emerald-700" x-text="money(m.paid)"></td>
                      <td class="p-2 text-right font-bold" :class="m.open>0?'text-red-600':'text-slate-500'" x-text="money(m.open)"></td>
                      <td class="p-2" x-text="m.topCategory"></td>
                    </tr>
                  </template>
                </tbody>
              </table>
            </div>
            <div class="glass rounded-3xl p-4">
              <h3 class="font-black mb-3">Top prestadores do ano</h3>
              <div class="space-y-2 max-h-[410px] overflow-auto scrollbar">
                <template x-for="(p,idx) in annualTopProviders" :key="p.id">
                  <div class="rounded-2xl border border-slate-200/60 dark:border-slate-700/60 p-3 flex items-center justify-between gap-3">
                    <div class="flex items-center gap-3 min-w-0">
                      <div class="h-9 w-9 rounded-2xl grid place-items-center font-black" :class="idx===0?'bg-yellow-500/15 text-yellow-700':idx===1?'bg-slate-500/15 text-slate-700':idx===2?'bg-orange-500/15 text-orange-700':'bg-rc-800/10 text-rc-800'" x-text="idx+1"></div>
                      <d
--- occurrence 2 at 2208419 ---
or="item in searchResults" :key="item.kind+'-'+item.id">
              <div class="glass rounded-3xl p-4 border-t-4" :class="item.kind==='receipt'?'border-t-amber-500':item.kind==='provider'?'border-t-emerald-600':item.kind==='recurrence'?'border-t-blue-500':'border-t-rc-gold'">
                <div class="flex items-start justify-between gap-3">
                  <div>
                    <span class="badge" x-text="item.type"></span>
                    <h4 class="font-black text-lg mt-2" x-text="item.title"></h4>
                    <p class="text-sm text-slate-500 dark:text-slate-400" x-text="item.subtitle"></p>
                  </div>
                  <div class="text-right shrink-0">
                    <p class="font-black" x-text="item.amount"></p>
                    <p class="text-xs text-slate-500" x-text="item.date"></p>
                  </div>
                </div>
                <p class="text-sm mt-3 leading-relaxed" x-text="item.description"></p>
                <div class="flex flex-wrap gap-2 mt-4">
                  <button x-show="item.kind==='provider'" @click="openSearchProvider(item.data)" class="mini">Painel</button>
                  <button x-show="item.kind==='provider'" @click="fillServiceFromProvider(item.data)" class="mini" :disabled="isViewer">Lançar</button>
                  <button x-show="item.kind==='provider'" @click="openWhatsapp(item.data)" class="mini">WhatsApp</button>

                  <button x-show="item.kind==='service'" @click="openSearchService(item.data)" class="mini">Ver serviço</button>
                  <button x-show="item.kind==='service'" @click="pay(item.data)" class="mini" :disabled="isViewer">Pagar</button>
                  <button x-show="item.kind==='service' && Number(item.data?.paidValue||0)>0" @click="quickReceipt(item.data)" class="mini" :disabled="isViewer" x-text="receiptActionLabel(item.data)"></button>

                  <button x-show="item.kind==='receipt'" @click="openSearchReceipt(item.data)" class="mini">Abrir recibo</button>
                  <button x-show="item.kind==='receipt'" @click="currentReceipt=item.data; printReceipt()" class="mini">Imprimir</button>

                  <button x-show="item.kind==='recurrence'" @click="openSearchRecurrence(item.data)" class="mini">Ver recorrência</button>
                  <button x-show="item.kind==='recurrence'" @click="generateRecurrence(item.data)" class="mini" :disabled="isViewer">Gerar mês</button>
                </div>
              </div>
            </template>
          </div>
          <div x-show="q && !searchResults.length" class="glass rounded-3xl p-5 text-center text-slate-500">
            Nenhum resultado encontrado para a busca atual.
          </div>
          <div x-show="!q" class="glass rounded-3xl p-5 text-center text-slate-500">
            Digite no campo de busca para localizar informações do app.
          </div>
        </section>

        <section x-show="view==='config'" class="space-y-4">
          <form @submit.prevent="saveSettings" class="glass rounded-3xl p-4 space-y-4">
            <div class="flex items-start gap-3">
              <div class="h-11 w-11 rounded-2xl bg-gradient-to-br from-rc-800 to-rc-500 text-white grid place-items-center shadow-soft"><i data-lucide="building-2" class="w-5 h-5"></i></div>
              <div>
                <h3 class="font-black text-lg">Dados do condomínio</h3>
                <p class="text-xs text-slate-500 dark:text-slate-400">Identidade usada nos recibos, relatórios e PDFs de prestação de contas.</p>
              </div>
            </div>
            <div class="grid lg:grid-cols-2 gap-3">
              <label><span class="text-sm font-bold">Nome</span><input x-model="settings.condoName" class="input mt-1"></label>
              <label><span class="text-sm font-bold">Responsável</span><input x-model="settings.manager" class="input mt-1"></label>
            </div>
            <div class="grid lg:grid-cols-2 gap-3">
              <label><span class="text-sm font-bold">CNPJ</span><input x-model="settings.cnpj" class="input mt-1"></label>
              <label><span class="text-sm font-bold">Logo</span><input @change="readLogo" type="file" accept="image/*" class="input mt-1"></label>
            </div>
            <label><span class="text-sm font-bold">Endereço</span><input x-model="settings.address" class="input mt-1"></label>
            <button class="primary">Salvar configurações</button>
          </form>


          <div class="glass rounded-3xl p-4 space-y-4">
            <div class="flex items-start justify-between gap-3">
              <div class="flex items-start gap-3">
                <div class="h-11 w-11 rounded-2xl bg-gradient-to-br from-rc-800 to-rc-gold text-white grid place-items-center shadow-soft"><i data-lucide="shield-check" class="w-5 h-5"></i></div>
                <div>
                  <h3 class="font-black text-lg">Backup</h3>
                  <p class="text-xs text-slate-500 dark:text-slate-400">Proteção e recuperação dos dados locais do ReciboCondo.</p>
                </div>
              </div>
              <span class="badge" :class="backupHealthClass()" x-text="backupHealthLabel()"></span>
            </div>

            <div class="rounded-3xl border border-slate-200/70 dark:border-slate-700/70 bg-white/70 dark:bg-slate-950/35 p-4 space-y-3">
              <div class="grid sm:grid-cols-4 gap-3 text-sm">
                <div><p class="text-xs uppercase tracking-[.14em] text-slate-500 font-black">Último backup</p><p class="font-black" x-text="backupLastLabel()"></p></div>
                <div><p class="text-xs uppercase tracking-[.14em] text-slate-500 font-black">Registros</p><p class="font-black" x-text="backupStats().records"></p></div>
                <div><p class="text-xs uppercase tracking-[.14em] text-slate-500 font-black">Recibos</p><p class="font-black" x-text="receipts.length"></p></div>
                <div><p class="text-xs uppercase tracking-[.14em] text-slate-500 font-black">Recorrências</p><p class="font-black" x-text="recurrences.length"></p></div>
              </div>
              <p class="text-xs leading-relaxed text-slate-500 dark:text-slate-400" x-text="backupHint()"></p>
            </div>

            <div class="grid sm:grid-cols-
--- occurrence 3 at 6483488 ---
'))return'warning';return'success'},toast(message,type='success'){let cfg={success:{title:'Tudo certo',icon:'check-circle-2',bar:'bg-emerald-600',border:'border-emerald-500/25',iconBg:'bg-emerald-600'},error:{title:'Atenção',icon:'triangle-alert',bar:'bg-red-600',border:'border-red-500/25',iconBg:'bg-red-600'},warning:{title:'Aviso',icon:'alert-circle',bar:'bg-amber-500',border:'border-amber-500/30',iconBg:'bg-amber-500'},info:{title:'Informação',icon:'info',bar:'bg-rc-800',border:'border-rc-800/25',iconBg:'bg-gradient-to-br from-rc-800 to-rc-500'}}[type]||{};let id=Date.now()+Math.random();this.toasts.push({id,message,type,...cfg});this.refreshIcons();setTimeout(()=>this.dismissToast(id),4200)},dismissToast(id){this.toasts=this.toasts.filter(t=>t.id!==id)},doLogin(){let p=JSON.parse(localStorage.getItem('rc_passwords')||'{"admin":"123456","viewer":"123456"}');if(this.login.password===p[this.login.role]){this.auth={logged:true,role:this.login.role};this.login.password='';this.load();this.after()}else this.toast('Senha inválida','error')},logout(){this.auth={logged:false,role:'viewer'}},go(v){this.view=v;this.after()},toggleDark(){this.dark=!this.dark;localStorage.setItem('rc_dark',this.dark?'1':'0');document.documentElement.classList.toggle('dark',this.dark)},
  providerAction(action,p){if(!action||!p)return;if(action==='painel')this.selectProvider(p);else if(action==='editar')this.editProvider(p);else if(action==='lancar'){if(!this.isViewer)this.fillServiceFromProvider(p)}else if(action==='whatsapp')this.openWhatsapp(p);else if(action==='excluir'){if(!this.isViewer)this.openDeleteProviderModal(p)}},serviceAction(action,s){if(!action||!s)return;if(action==='pagar'){if(!this.isViewer)this.pay(s)}else if(action==='recibo'){if(!this.isViewer&&Number(s.paidValue||0)>0)this.quickReceipt(s)}else if(action==='duplicar'){if(!this.isViewer)this.openDuplicateModal(s)}else if(action==='excluir'){if(!this.isViewer)this.openDeleteServiceModal(s)}},paymentAction(action,s){if(!action||!s)return;if(action==='pagar'){if(!this.isViewer&&s.status!=='Pago')this.pay(s)}else if(action==='recibo'){if(!this.isViewer&&Number(s.paidValue||0)>0)this.quickReceipt(s)}else if(action==='servico')this.openSearchService(s)},openSearchProvider(p){this.selectedProviderId=p?.id||null;this.view='prestadores';this.after()},openSearchService(s){this.serviceFilter='todos';this.serviceCategoryFilter='todas';this.serviceProviderFilter=String(s.providerId||'todos');this.serviceMonthFilter=s.reference||'';this.view='servicos';this.after()},openSearchReceipt(r){this.currentReceipt=r;this.receiptFilter={q:r.number||'',month:r.reference||'',category:'todas'};this.view='recibos';this.after()},openSearchRecurrence(r){this.recurrenceForm={...r};this.view='servicos';this.after()},selectProvider(p){this.selectedProviderId=p?.id||null;this.after()},providerSortBy(field){if(this.providerSortField===field){this.providerSortDir=this.providerSortDir==='asc'?'desc':'asc';}else{this.providerSortField=field;this.providerSortDir='asc';}},resetProvider(){this.providerForm={id:null,name:'',type:'Autônomo',document:'',category:'Jardineiro',phone:'',whatsapp:'',pix:'',notes:''}},editProvider(p){this.providerForm={...p};this.view='prestadores';this.after()},async saveProvider(){if(this.isViewer)return;let d={...this.providerForm};let doc=String(d.document||'').replace(/\D/g,'');if(doc){let duplicate=this.providers.find(p=>Number(p.id)!==Number(d.id)&&String(p.document||'').replace(/\D/g,'')===doc);if(duplicate)return this.toast('Já existe um prestador cadastrado com este CPF/CNPJ: '+(duplicate.name||'cadastro existente')+'.','warning')}if(d.id){await db.providers.update(d.id,d);await this.logAction('provider','Prestador atualizado',d.name||'Cadastro atualizado')}else{delete d.id;await db.providers.add(d);await this.logAction('provider','Prestador cadastrado',d.name||'Novo prestador')}this.resetProvider();await this.load();this.toast('Prestador salvo')},openDeleteProviderModal(p){if(this.isViewer)return;this.actionModal={open:true,type:'deleteProvider',tone:'danger',icon:'user-x',target:p,service:null,title:'Excluir prestador',subtitle:'Essa ação remove o cadastro do prestador.',note:'Prestadores com lançamentos, recibos ou recorrências vinculadas não podem ser excluídos. Isso preserva a rastreabilidade do histórico.',confirmLabel:'Excluir prestador',details:[{label:'Prestador',value:p.name||'-'},{label:'Categoria',value:p.category||'-'},{label:'Documento',value:p.document||'não informado'}]};this.after()},async removeProviderInternal(id){let p=this.providers.find(x=>Number(x.id)===Number(id));if(!p)return;let linkedServices=this.services.some(s=>Number(s.providerId)===Number(id)),linkedReceipts=this.receipts.some(r=>Number(r.providerId)===Number(id)),linkedRecurrences=(this.recurrences||[]).some(r=>Number(r.providerId)===Number(id));if(linkedServices||linkedReceipts||linkedRecurrences){return this.toast('Este prestador possui histórico ou recorrência vinculada e não pode ser excluído. Mantenha o cadastro para preservar a rastreabilidade.','warning')}await db.providers.delete(id);await this.logAction('delete','Prestador excluído',p.name||('ID '+id));await this.load()},
  resetRecurrence(){this.recurrenceForm={id:null,providerId:'',category:'Jardineiro',amount:0,serviceDay:5,dueDay:5,paymentDay:5,paymentMethod:'PIX',location:'',description:'',active:true}},editRecurrence(r){this.recurrenceForm={...r,serviceDay:Number(r.serviceDay||r.paymentDay||r.dueDay||1)};this.view='servicos';this.after()},async saveRecurrence(){if(this.isViewer)return;let d={...this.recurrenceForm,providerId:Number(this.recurrenceForm.providerId),amount:Number(this.recurrenceForm.amount||0),serviceDay:Number(this.recurrenceForm.serviceDay||this.recurrenceForm.paymentDay||this.recurrenceForm.dueDay||1),dueDay:Number(this.recurrenceForm.dueDay||1),paymentDay:Number(this.recurrenceForm.paymentDay||this.recurrenceForm.dueDay||1),active:!!this.recurrenceForm.active};if(d.id){await db.recurrences.update(d.id,d);await this.logAction('recurrence','Recorrência atualizada',this.providerName(d.providerId)+' • '+this.money(d.amount||0))}else{delete d.id;await db.recurrences.add(d);await this.logAction('recurrence','Recorrência criada',this.providerName(d.providerId)+' • '+this.money(d.amou
--- occurrence 4 at 6483849 ---
g-amber-500',border:'border-amber-500/30',iconBg:'bg-amber-500'},info:{title:'Informação',icon:'info',bar:'bg-rc-800',border:'border-rc-800/25',iconBg:'bg-gradient-to-br from-rc-800 to-rc-500'}}[type]||{};let id=Date.now()+Math.random();this.toasts.push({id,message,type,...cfg});this.refreshIcons();setTimeout(()=>this.dismissToast(id),4200)},dismissToast(id){this.toasts=this.toasts.filter(t=>t.id!==id)},doLogin(){let p=JSON.parse(localStorage.getItem('rc_passwords')||'{"admin":"123456","viewer":"123456"}');if(this.login.password===p[this.login.role]){this.auth={logged:true,role:this.login.role};this.login.password='';this.load();this.after()}else this.toast('Senha inválida','error')},logout(){this.auth={logged:false,role:'viewer'}},go(v){this.view=v;this.after()},toggleDark(){this.dark=!this.dark;localStorage.setItem('rc_dark',this.dark?'1':'0');document.documentElement.classList.toggle('dark',this.dark)},
  providerAction(action,p){if(!action||!p)return;if(action==='painel')this.selectProvider(p);else if(action==='editar')this.editProvider(p);else if(action==='lancar'){if(!this.isViewer)this.fillServiceFromProvider(p)}else if(action==='whatsapp')this.openWhatsapp(p);else if(action==='excluir'){if(!this.isViewer)this.openDeleteProviderModal(p)}},serviceAction(action,s){if(!action||!s)return;if(action==='pagar'){if(!this.isViewer)this.pay(s)}else if(action==='recibo'){if(!this.isViewer&&Number(s.paidValue||0)>0)this.quickReceipt(s)}else if(action==='duplicar'){if(!this.isViewer)this.openDuplicateModal(s)}else if(action==='excluir'){if(!this.isViewer)this.openDeleteServiceModal(s)}},paymentAction(action,s){if(!action||!s)return;if(action==='pagar'){if(!this.isViewer&&s.status!=='Pago')this.pay(s)}else if(action==='recibo'){if(!this.isViewer&&Number(s.paidValue||0)>0)this.quickReceipt(s)}else if(action==='servico')this.openSearchService(s)},openSearchProvider(p){this.selectedProviderId=p?.id||null;this.view='prestadores';this.after()},openSearchService(s){this.serviceFilter='todos';this.serviceCategoryFilter='todas';this.serviceProviderFilter=String(s.providerId||'todos');this.serviceMonthFilter=s.reference||'';this.view='servicos';this.after()},openSearchReceipt(r){this.currentReceipt=r;this.receiptFilter={q:r.number||'',month:r.reference||'',category:'todas'};this.view='recibos';this.after()},openSearchRecurrence(r){this.recurrenceForm={...r};this.view='servicos';this.after()},selectProvider(p){this.selectedProviderId=p?.id||null;this.after()},providerSortBy(field){if(this.providerSortField===field){this.providerSortDir=this.providerSortDir==='asc'?'desc':'asc';}else{this.providerSortField=field;this.providerSortDir='asc';}},resetProvider(){this.providerForm={id:null,name:'',type:'Autônomo',document:'',category:'Jardineiro',phone:'',whatsapp:'',pix:'',notes:''}},editProvider(p){this.providerForm={...p};this.view='prestadores';this.after()},async saveProvider(){if(this.isViewer)return;let d={...this.providerForm};let doc=String(d.document||'').replace(/\D/g,'');if(doc){let duplicate=this.providers.find(p=>Number(p.id)!==Number(d.id)&&String(p.document||'').replace(/\D/g,'')===doc);if(duplicate)return this.toast('Já existe um prestador cadastrado com este CPF/CNPJ: '+(duplicate.name||'cadastro existente')+'.','warning')}if(d.id){await db.providers.update(d.id,d);await this.logAction('provider','Prestador atualizado',d.name||'Cadastro atualizado')}else{delete d.id;await db.providers.add(d);await this.logAction('provider','Prestador cadastrado',d.name||'Novo prestador')}this.resetProvider();await this.load();this.toast('Prestador salvo')},openDeleteProviderModal(p){if(this.isViewer)return;this.actionModal={open:true,type:'deleteProvider',tone:'danger',icon:'user-x',target:p,service:null,title:'Excluir prestador',subtitle:'Essa ação remove o cadastro do prestador.',note:'Prestadores com lançamentos, recibos ou recorrências vinculadas não podem ser excluídos. Isso preserva a rastreabilidade do histórico.',confirmLabel:'Excluir prestador',details:[{label:'Prestador',value:p.name||'-'},{label:'Categoria',value:p.category||'-'},{label:'Documento',value:p.document||'não informado'}]};this.after()},async removeProviderInternal(id){let p=this.providers.find(x=>Number(x.id)===Number(id));if(!p)return;let linkedServices=this.services.some(s=>Number(s.providerId)===Number(id)),linkedReceipts=this.receipts.some(r=>Number(r.providerId)===Number(id)),linkedRecurrences=(this.recurrences||[]).some(r=>Number(r.providerId)===Number(id));if(linkedServices||linkedReceipts||linkedRecurrences){return this.toast('Este prestador possui histórico ou recorrência vinculada e não pode ser excluído. Mantenha o cadastro para preservar a rastreabilidade.','warning')}await db.providers.delete(id);await this.logAction('delete','Prestador excluído',p.name||('ID '+id));await this.load()},
  resetRecurrence(){this.recurrenceForm={id:null,providerId:'',category:'Jardineiro',amount:0,serviceDay:5,dueDay:5,paymentDay:5,paymentMethod:'PIX',location:'',description:'',active:true}},editRecurrence(r){this.recurrenceForm={...r,serviceDay:Number(r.serviceDay||r.paymentDay||r.dueDay||1)};this.view='servicos';this.after()},async saveRecurrence(){if(this.isViewer)return;let d={...this.recurrenceForm,providerId:Number(this.recurrenceForm.providerId),amount:Number(this.recurrenceForm.amount||0),serviceDay:Number(this.recurrenceForm.serviceDay||this.recurrenceForm.paymentDay||this.recurrenceForm.dueDay||1),dueDay:Number(this.recurrenceForm.dueDay||1),paymentDay:Number(this.recurrenceForm.paymentDay||this.recurrenceForm.dueDay||1),active:!!this.recurrenceForm.active};if(d.id){await db.recurrences.update(d.id,d);await this.logAction('recurrence','Recorrência atualizada',this.providerName(d.providerId)+' • '+this.money(d.amount||0))}else{delete d.id;await db.recurrences.add(d);await this.logAction('recurrence','Recorrência criada',this.providerName(d.providerId)+' • '+this.money(d.amount||0))}this.resetRecurrence();await this.load();this.toast('Recorrência salva. Lançamentos gerados mensalmente.')},recurrenceGenerated(r,ref=this.today().slice(0,7)){return this.services.some(s=>Number(s.recurrenceId)===Number(r.id)&&s.reference===ref)},dateForDay(ref,day){let [y,m]=String(ref).split('-').map(Number);let last=new Date(y,m,0).getDate();let d=
--- occurrence 5 at 6504051 ---
ney(s.paidValue||0));await this.load()},
  pay(s){this.paymentTarget=s;this.payment={date:this.today(),value:this.balance(s),method:'PIX'};this.payModal=true},async savePayment(){if(this.isViewer)return;let s=this.paymentTarget,value=Number(this.payment.value||0),paymentDate=this.payment.date||this.today(),method=this.payment.method||'Não informado',remaining=Math.max(0,Number(s.amount||0)-Number(s.paidValue||0));if(value<=0)return this.toast('Informe um valor de pagamento maior que zero.','warning');if(value>remaining+0.000001)return this.toast('O pagamento não pode ser maior que o saldo pendente de '+this.money(remaining)+'.','warning');let paid=Number(s.paidValue||0)+value,status=paid>=Number(s.amount)?'Pago':'Parcial';await db.transaction('rw',db.services,db.payments,async()=>{await db.payments.add({serviceId:s.id,providerId:s.providerId,date:paymentDate,amount:value,method,receiptId:null,source:'manual',createdAt:new Date().toISOString()});await db.services.update(s.id,{paidValue:paid,paymentDate,paymentMethod:method,status})});await this.logAction('payment','Pagamento registrado',this.providerName(s.providerId)+' • '+this.money(value)+' • '+method);this.payModal=false;await this.load();this.toast(status==='Pago'?'Pagamento registrado. Lançamento quitado.':'Pagamento parcial registrado. Saldo restante: '+this.money(Math.max(0,Number(s.amount||0)-paid)))},prepareReceipt(s){this.receiptServiceId=s.id;this.view='recibos';this.after()},async recentPaymentReceipt(p){if(this.isViewer||!p||!p.service)return;if(p.receipt){this.currentReceipt=p.receipt;this.view='recibos';this.after();setTimeout(()=>this.printReceipt(),220);return}this.receiptServiceId=p.service.id;await this.issueReceipt(p.id);this.view='recibos';this.after();setTimeout(()=>this.printReceipt(),260)},async quickReceipt(s){if(this.isViewer)return;if(Number(s.paidValue||0)<=0)return this.toast('Registre o pagamento antes de emitir o recibo.','warning');let receiptPaymentIds=new Set(this.receipts.map(r=>Number(r.paymentId)).filter(Boolean)),candidates=this.payments.filter(p=>Number(p.serviceId)===Number(s.id)&&!p.receiptId&&!receiptPaymentIds.has(Number(p.id))).sort((a,b)=>String(a.date||'').localeCompare(String(b.date||''))||Number(a.id||0)-Number(b.id||0));if(candidates.length){this.receiptServiceId=s.id;await this.issueReceipt(candidates[0].id);this.view='recibos';this.after();setTimeout(()=>this.printReceipt(),260);return}let linked=this.receipts.filter(r=>Number(r.serviceId)===Number(s.id)),latest=linked.slice().sort((a,b)=>Number(b.id||0)-Number(a.id||0))[0];if(latest){this.currentReceipt=latest;this.view='recibos';this.after();this.toast('Todos os pagamentos registrados já possuem recibo.','warning');setTimeout(()=>this.printReceipt(),220)}else this.toast('Não há pagamento pendente de recibo.','warning')},async issueReceipt(paymentId=null){if(this.isViewer)return;let s=this.services.find(x=>Number(x.id)===Number(this.receiptServiceId));if(!s)return this.toast('Selecione um lançamento.','warning');let linked=this.receipts.filter(r=>Number(r.serviceId)===Number(s.id)),issued=linked.reduce((a,r)=>a+Number(r.total||0),0),legacyPending=Math.max(0,Number(s.paidValue||0)-issued),receiptPaymentIds=new Set(linked.map(r=>Number(r.paymentId)).filter(Boolean)),candidates=this.payments.filter(p=>Number(p.serviceId)===Number(s.id)&&!p.receiptId&&!receiptPaymentIds.has(Number(p.id))).sort((a,b)=>String(a.date||'').localeCompare(String(b.date||''))||Number(a.id||0)-Number(b.id||0)),payment=paymentId?(candidates.find(p=>Number(p.id)===Number(paymentId))||null):(candidates[0]||null);if(paymentId&&!payment)return this.toast('Este pagamento já possui recibo ou não está disponível para emissão.','warning');if(!payment&&legacyPending<=0)return this.toast('Todos os pagamentos registrados deste lançamento já possuem recibo.','warning');let receiptTotal=payment?Number(payment.amount||0):legacyPending,paymentDate=payment?.date||s.paymentDate||this.today(),paymentMethod=payment?.method||s.paymentMethod||'Não informado',desc=String(s.description||'').replace(/(\bparcela\s+)(\d+)\s*\/\s*(\d+)/i,(m,prefix,current,total)=>prefix+String(linked.length+1).padStart(current.length,'0')+'/'+total),p=this.providers.find(x=>Number(x.id)===Number(s.providerId))||{},r=null;await db.transaction('rw',db.meta,db.receipts,db.payments,async()=>{let meta=await db.meta.get('receiptSeq'),next=Math.max(Number(meta?.value||0),this.receiptMaxNumber())+1;r={number:String(next).padStart(6,'0'),date:this.today(),paymentDate,providerId:p.id,providerName:p.name||'Prestador não informado',document:p.document||'',category:s.category,reference:s.reference,description:desc,total:receiptTotal,paymentMethod,location:s.location,serviceId:s.id,paymentId:payment?.id||null,manager:this.settings.manager,condoName:this.settings.condoName,condoCnpj:this.settings.cnpj||'',condoAddress:this.settings.address||''};await db.meta.put({key:'receiptSeq',value:next});let id=await db.receipts.add(r);r.id=id;if(payment)await db.payments.update(payment.id,{receiptId:id})});this.currentReceipt=r;await this.logAction('receipt','Recibo emitido','Nº '+r.number+' • '+r.providerName+' • '+this.money(r.total||0));await this.load();this.toast('Recibo nº '+r.number+' emitido no valor de '+this.money(r.total||0)+'.')},printReceipt(){if(!this.currentReceipt&&this.receipts.length)this.currentReceipt=this.receipts[this.receipts.length-1];document.getElementById('receiptPrint').classList.remove('hidden');document.getElementById('reportPrint').classList.add('hidden');document.getElementById('annualReportPrint').classList.add('hidden');setTimeout(()=>window.print(),100)},async removeReceiptInternal(id){let r=this.receipts.find(x=>Number(x.id)===Number(id));if(!r)return;let linked=this.receipts.filter(x=>Number(x.serviceId)===Number(r.serviceId)).sort((a,b)=>Number(b.id||0)-Number(a.id||0)),latest=linked[0];if(latest&&Number(latest.id)!==Number(r.id))return this.toast('Para preservar a data e a forma de pagamento das parcelas, somente o recibo mais recente deste lançamento pode ser excluído e reemitido.','warning');await db.transaction('rw',db.receipts,db.payments,async()=>{await db.receipts.delete(id);let ps=this.payments.filter(p=>Number(p.receiptId)===Number(id));for(const p of ps)await db.payments.update(p.i
COUNT=5

===== receipts.filter =====

--- occurrence 1 at 6453351 ---
).getFullYear(),payModal:false,paymentTarget:null,payment:{date:new Date().toISOString().slice(0,10),value:0,method:'PIX'},actionModal:{open:false,type:'',tone:'default',icon:'',target:null,service:null,title:'',subtitle:'',note:'',confirmLabel:'',details:[]},toasts:[],
  reportChartYear:new Date().getFullYear(),report:{from:new Date(new Date().getFullYear(),new Date().getMonth(),1).toISOString().slice(0,10),to:new Date().toISOString().slice(0,10),category:'todas',status:'todos'},passwords:{admin:'',viewer:''},
  async init(){document.documentElement.classList.toggle('dark',this.dark);window.alert=(msg)=>this.toast(String(msg||''),this.toastType(String(msg||'')));if('serviceWorker'in navigator){navigator.serviceWorker.register('./sw.js').catch(console.warn)}await this.seed();this.refreshIcons()},
  async seed(){let s=await db.settings.get('main');if(s)this.settings=s.value;await this.load();},
  async load(){this.providers=await db.providers.toArray();this.services=await db.services.toArray();this.receipts=await db.receipts.toArray();this.recurrences=await db.recurrences.toArray();this.payments=await db.payments.toArray();this.logs=(await db.logs.orderBy('at').reverse().limit(30).toArray());let migrated=await this.ensurePaymentLedger();if(migrated)this.payments=await db.payments.toArray();let meta=await db.meta.get('receiptSeq');this.receiptSeqValue=Number(meta?.value||0);if(!this.receiptSeqInput)this.receiptSeqInput=this.nextReceiptNumber();this.after();this.auditDataIntegrity()},
  async ensurePaymentLedger(){let created=0;for(const s of this.services){let paid=Number(s.paidValue||0);if(paid<=0)continue;await db.transaction('rw',db.payments,async()=>{let existing=await db.payments.where('serviceId').equals(s.id).toArray();if(existing.length)return;let rows=[],rs=this.receipts.filter(r=>Number(r.serviceId)===Number(s.id)).sort((a,b)=>Number(a.id||0)-Number(b.id||0)),known=0;for(const r of rs){let amount=Number(r.total||0);if(amount<=0)continue;rows.push({serviceId:s.id,providerId:s.providerId,date:r.paymentDate||r.date||s.paymentDate||s.serviceDate||this.today(),amount,method:r.paymentMethod||s.paymentMethod||'Não informado',receiptId:r.id,source:'receipt-migration',createdAt:new Date().toISOString()});known+=amount}let residual=Math.max(0,paid-known);if(residual>0.009)rows.push({serviceId:s.id,providerId:s.providerId,date:s.paymentDate||s.serviceDate||this.today(),amount:residual,method:s.paymentMethod||'Não informado',receiptId:null,source:'service-migration',createdAt:new Date().toISOString()});if(rows.length){await db.payments.bulkAdd(rows);created+=rows.length}})}if(created){await db.meta.put({key:'paymentsLedgerV1',value:{created,at:new Date().toISOString()}});console.info('[ReciboCondo] Histórico de pagamentos reconstruído:',created)}return created},
  auditDataIntegrity(){let issues=[],providerIds=new Set(this.providers.map(p=>Number(p.id))),serviceIds=new Set(this.services.map(s=>Number(s.id))),modernReceipts=this.receipts.filter(r=>r.source!=='Excel legado'),records=this.providers.length+this.services.length+this.receipts.length+this.recurrences.length+this.payments.length;for(const s of this.services){if(s.providerId!=null&&s.providerId!==''&&!providerIds.has(Number(s.providerId)))issues.push('Lançamento '+(s.id||'?')+' aponta para prestador inexistente.')}for(const r of this.receipts){if(r.serviceId!=null&&r.serviceId!==''&&!serviceIds.has(Number(r.serviceId)))issues.push('Recibo '+(r.number||r.id||'?')+' aponta para lançamento inexistente.');if(r.providerId!=null&&r.providerId!==''&&!providerIds.has(Number(r.providerId)))issues.push('Recibo '+(r.number||r.id||'?')+' aponta para prestador inexistente.')}for(const p of this.payments){if(p.serviceId!=null&&!serviceIds.has(Number(p.serviceId)))issues.push('Pagamento '+(p.id||'?')+' aponta para lançamento inexistente.');if(p.providerId!=null&&!providerIds.has(Number(p.providerId)))issues.push('Pagamento '+(p.id||'?')+' aponta para prestador inexistente.');if(Number(p.amount||0)<=0)issues.push('Pagamento '+(p.id||'?')+' possui valor inválido: '+this.money(Number(p.amount||0))+'.');let svc=this.services.find(s=>Number(s.id)===Number(p.serviceId));if(svc&&p.providerId!=null&&Number(p.providerId)!==Number(svc.providerId))issues.push('Pagamento '+(p.id||'?')+' aponta para prestador diferente do lançamento '+(svc.id||'?')+'.')}let receiptIds=new Set(this.receipts.map(r=>Number(r.id))),paymentIds=new Set(this.payments.map(p=>Number(p.id))),usedReceiptIds=new Set(),usedPaymentIds=new Set();for(const p of this.payments){if(p.receiptId!=null&&!receiptIds.has(Number(p.receiptId)))issues.push('Pagamento '+(p.id||'?')+' aponta para recibo inexistente.');if(p.receiptId!=null){let rid=Number(p.receiptId);if(usedReceiptIds.has(rid))issues.push('Mais de um pagamento aponta para o mesmo recibo ID '+rid+'.');usedReceiptIds.add(rid);let r=this.receipts.find(x=>Number(x.id)===rid);if(r&&Number(r.serviceId)!==Number(p.serviceId))issues.push('Pagamento '+(p.id||'?')+' e recibo '+(r.number||r.id||'?')+' apontam para lançamentos diferentes.');if(r&&r.paymentId!=null&&Number(r.paymentId)!==Number(p.id))issues.push('Pagamento '+(p.id||'?')+' e recibo '+(r.number||r.id||'?')+' discordam no vínculo da parcela.')}}for(const r of this.receipts){if(r.paymentId!=null){let pid=Number(r.paymentId);if(!paymentIds.has(pid))issues.push('Recibo '+(r.number||r.id||'?')+' aponta para pagamento inexistente.');if(usedPaymentIds.has(pid))issues.push('Mais de um recibo aponta para o mesmo pagamento ID '+pid+'.');usedPaymentIds.add(pid);let pay=this.payments.find(p=>Number(p.id)===pid);if(pay&&Number(pay.serviceId)!==Number(r.serviceId))issues.push('Recibo '+(r.number||r.id||'?')+' e pagamento '+(pay.id||'?')+' apontam para lançamentos diferentes.');if(pay&&pay.receiptId!=null&&Number(pay.receiptId)!==Number(r.id))issues.push('Recibo '+(r.number||r.id||'?')+' e pagamento '+(pay.id||'?')+' discordam no vínculo do recibo.')}let svc=this.services.find(s=>Number(s.id)===Number(r.serviceId));if(svc&&r.providerId!=null&&Number(r.providerId)!==Number(svc.providerId))issues.push('Recibo '+(r.number||r.id||'?')+' aponta para prestador diferente do lançamento '+(svc.id||'?')+'.')}let seen=new Set();for(const r of modernReceipts){let n=String(r.number||'').replace(/\D/g,'');if(!
--- occurrence 2 at 6454529 ---
.reverse().limit(30).toArray());let migrated=await this.ensurePaymentLedger();if(migrated)this.payments=await db.payments.toArray();let meta=await db.meta.get('receiptSeq');this.receiptSeqValue=Number(meta?.value||0);if(!this.receiptSeqInput)this.receiptSeqInput=this.nextReceiptNumber();this.after();this.auditDataIntegrity()},
  async ensurePaymentLedger(){let created=0;for(const s of this.services){let paid=Number(s.paidValue||0);if(paid<=0)continue;await db.transaction('rw',db.payments,async()=>{let existing=await db.payments.where('serviceId').equals(s.id).toArray();if(existing.length)return;let rows=[],rs=this.receipts.filter(r=>Number(r.serviceId)===Number(s.id)).sort((a,b)=>Number(a.id||0)-Number(b.id||0)),known=0;for(const r of rs){let amount=Number(r.total||0);if(amount<=0)continue;rows.push({serviceId:s.id,providerId:s.providerId,date:r.paymentDate||r.date||s.paymentDate||s.serviceDate||this.today(),amount,method:r.paymentMethod||s.paymentMethod||'Não informado',receiptId:r.id,source:'receipt-migration',createdAt:new Date().toISOString()});known+=amount}let residual=Math.max(0,paid-known);if(residual>0.009)rows.push({serviceId:s.id,providerId:s.providerId,date:s.paymentDate||s.serviceDate||this.today(),amount:residual,method:s.paymentMethod||'Não informado',receiptId:null,source:'service-migration',createdAt:new Date().toISOString()});if(rows.length){await db.payments.bulkAdd(rows);created+=rows.length}})}if(created){await db.meta.put({key:'paymentsLedgerV1',value:{created,at:new Date().toISOString()}});console.info('[ReciboCondo] Histórico de pagamentos reconstruído:',created)}return created},
  auditDataIntegrity(){let issues=[],providerIds=new Set(this.providers.map(p=>Number(p.id))),serviceIds=new Set(this.services.map(s=>Number(s.id))),modernReceipts=this.receipts.filter(r=>r.source!=='Excel legado'),records=this.providers.length+this.services.length+this.receipts.length+this.recurrences.length+this.payments.length;for(const s of this.services){if(s.providerId!=null&&s.providerId!==''&&!providerIds.has(Number(s.providerId)))issues.push('Lançamento '+(s.id||'?')+' aponta para prestador inexistente.')}for(const r of this.receipts){if(r.serviceId!=null&&r.serviceId!==''&&!serviceIds.has(Number(r.serviceId)))issues.push('Recibo '+(r.number||r.id||'?')+' aponta para lançamento inexistente.');if(r.providerId!=null&&r.providerId!==''&&!providerIds.has(Number(r.providerId)))issues.push('Recibo '+(r.number||r.id||'?')+' aponta para prestador inexistente.')}for(const p of this.payments){if(p.serviceId!=null&&!serviceIds.has(Number(p.serviceId)))issues.push('Pagamento '+(p.id||'?')+' aponta para lançamento inexistente.');if(p.providerId!=null&&!providerIds.has(Number(p.providerId)))issues.push('Pagamento '+(p.id||'?')+' aponta para prestador inexistente.');if(Number(p.amount||0)<=0)issues.push('Pagamento '+(p.id||'?')+' possui valor inválido: '+this.money(Number(p.amount||0))+'.');let svc=this.services.find(s=>Number(s.id)===Number(p.serviceId));if(svc&&p.providerId!=null&&Number(p.providerId)!==Number(svc.providerId))issues.push('Pagamento '+(p.id||'?')+' aponta para prestador diferente do lançamento '+(svc.id||'?')+'.')}let receiptIds=new Set(this.receipts.map(r=>Number(r.id))),paymentIds=new Set(this.payments.map(p=>Number(p.id))),usedReceiptIds=new Set(),usedPaymentIds=new Set();for(const p of this.payments){if(p.receiptId!=null&&!receiptIds.has(Number(p.receiptId)))issues.push('Pagamento '+(p.id||'?')+' aponta para recibo inexistente.');if(p.receiptId!=null){let rid=Number(p.receiptId);if(usedReceiptIds.has(rid))issues.push('Mais de um pagamento aponta para o mesmo recibo ID '+rid+'.');usedReceiptIds.add(rid);let r=this.receipts.find(x=>Number(x.id)===rid);if(r&&Number(r.serviceId)!==Number(p.serviceId))issues.push('Pagamento '+(p.id||'?')+' e recibo '+(r.number||r.id||'?')+' apontam para lançamentos diferentes.');if(r&&r.paymentId!=null&&Number(r.paymentId)!==Number(p.id))issues.push('Pagamento '+(p.id||'?')+' e recibo '+(r.number||r.id||'?')+' discordam no vínculo da parcela.')}}for(const r of this.receipts){if(r.paymentId!=null){let pid=Number(r.paymentId);if(!paymentIds.has(pid))issues.push('Recibo '+(r.number||r.id||'?')+' aponta para pagamento inexistente.');if(usedPaymentIds.has(pid))issues.push('Mais de um recibo aponta para o mesmo pagamento ID '+pid+'.');usedPaymentIds.add(pid);let pay=this.payments.find(p=>Number(p.id)===pid);if(pay&&Number(pay.serviceId)!==Number(r.serviceId))issues.push('Recibo '+(r.number||r.id||'?')+' e pagamento '+(pay.id||'?')+' apontam para lançamentos diferentes.');if(pay&&pay.receiptId!=null&&Number(pay.receiptId)!==Number(r.id))issues.push('Recibo '+(r.number||r.id||'?')+' e pagamento '+(pay.id||'?')+' discordam no vínculo do recibo.')}let svc=this.services.find(s=>Number(s.id)===Number(r.serviceId));if(svc&&r.providerId!=null&&Number(r.providerId)!==Number(svc.providerId))issues.push('Recibo '+(r.number||r.id||'?')+' aponta para prestador diferente do lançamento '+(svc.id||'?')+'.')}let seen=new Set();for(const r of modernReceipts){let n=String(r.number||'').replace(/\D/g,'');if(!n)continue;if(seen.has(n))issues.push('Número de recibo duplicado: '+String(r.number||n)+'.');seen.add(n)}for(const s of this.services){let linked=modernReceipts.filter(r=>Number(r.serviceId)===Number(s.id)),issued=linked.reduce((a,r)=>a+Number(r.total||0),0),paid=Number(s.paidValue||0),ledger=this.payments.filter(p=>Number(p.serviceId)===Number(s.id)).reduce((a,p)=>a+Number(p.amount||0),0);if(linked.length&&issued>paid+0.009)issues.push('Recibos do lançamento '+(s.id||'?')+' somam '+this.money(issued)+' acima do pago '+this.money(paid)+'.');if(Math.abs(ledger-paid)>0.009)issues.push('Histórico de pagamentos do lançamento '+(s.id||'?')+' soma '+this.money(ledger)+' mas o acumulado é '+this.money(paid)+'.')}let condo=String(this.settings?.condoName||'').trim(),manager=String(this.settings?.manager||'').trim();if(records>0&&(!condo||!manager))issues.push('Há dados operacionais, mas os dados principais do condomínio estão incompletos.');let max=this.receiptMaxNumber();if(Number(this.receiptSeqValue||0)<max)issues.push('Sequência gravada está atrás do maior recibo; o próximo número continua protegido pelo maior recibo existente.');this.integrityIssues=issues;if(!i
--- occurrence 3 at 6461482 ---
ões'}[this.view]||'ReciboCondo'},get selectedProvider(){return this.providers.find(p=>Number(p.id)===Number(this.selectedProviderId))||null},
  get cards(){return this.dashboardCards},
  get yearlyRows(){let y=String(this.dashboardYear||new Date().getFullYear());return this.services.filter(s=>String(s.serviceDate||'').startsWith(y))},
  get yearlyPaymentRows(){let y=String(this.dashboardYear||new Date().getFullYear());return this.payments.filter(p=>String(p.date||'').startsWith(y)).map(p=>{let svc=this.services.find(s=>Number(s.id)===Number(p.serviceId));return{providerId:p.providerId??svc?.providerId,category:svc?.category||'Outros',paymentDate:p.date,total:Number(p.amount||0),method:p.method||''}})},
  get dashboardCards(){let now=new Date(),nowRef=String(this.dashboardYear||now.getFullYear())+'-'+String(now.getMonth()+1).padStart(2,'0'),year=this.yearlyRows,payments=this.yearlyPaymentRows,monthPayments=payments.filter(r=>String(r.paymentDate||'').slice(0,7)===nowRef),paidYear=payments.reduce((a,r)=>a+Number(r.total||0),0),paidMonth=monthPayments.reduce((a,r)=>a+Number(r.total||0),0),countMonth=monthPayments.length,open=year.reduce((a,s)=>a+this.balance(s),0),top=this.annualCategorySummary[0],topYear=this.annualTopProviders.slice(0,3).map(p=>p.name+' • '+this.money(p.total)),topMonth=Object.values(monthPayments.reduce((m,r)=>{let id=String(r.providerId??'');if(!m[id])m[id]={name:this.providerName(r.providerId),total:0};m[id].total+=Number(r.total||0);return m},{})).sort((a,b)=>b.total-a.total).slice(0,3).map(p=>p.name+' • '+this.money(p.total)),openTop=this.topOpenNames(year),catTop=this.annualCategorySummary.slice(0,3).map(c=>c.category+' • '+this.money(c.total)),activeCats=[...new Set(this.providers.map(p=>p.category).filter(Boolean))].slice(0,4),receiptsMonth=this.receipts.filter(r=>String(r.paymentDate||r.date||'').slice(0,7)===nowRef).length;return[
    {label:'Pago no ano',value:this.money(paidYear),hint:String(this.dashboardYear),icon:'wallet-cards',color:'text-rc-600',tooltipTitle:'Resumo anual',tooltipMain:this.money(paidYear),tooltipTag:'Ano '+this.dashboardYear,tooltip:['Total efetivamente pago no ano selecionado.','Top recebimentos: '+(topYear.length?topYear.join(' | '):'sem pagamentos lançados.'),'Use para comparar o volume anual com o relatório anual premium.']},
    {label:'Pago no mês',value:this.money(paidMonth),hint:this.month(nowRef),icon:'circle-dollar-sign',color:'text-emerald-600',tooltipTitle:'Resumo do mês',tooltipMain:this.money(paidMonth),tooltipTag:'Mês atual',tooltip:['Mês de referência: '+this.month(nowRef)+'.','Quem recebeu: '+(topMonth.length?topMonth.join(' | '):'sem pagamentos neste mês.'),'Ajuda a conferir rapidamente o fechamento mensal.']},
    {label:'Pagamentos no mês',value:countMonth,hint:'registros pagos',icon:'receipt-text',color:'text-blue-600',tooltipTitle:'Movimento mensal',tooltipMain:countMonth+' pagamento(s)',tooltipTag:'Recibos '+receiptsMonth,tooltip:['Valor médio por pagamento: '+this.money(countMonth?paidMonth/countMonth:0)+'.','Recibos emitidos no mês: '+receiptsMonth+'.','Bom para conferir se todos os pagamentos pagos já têm recibo.']},
    {label:'Prestadores ativos',value:this.providers.length,hint:'cadastrados',icon:'users',color:'text-rc-600',tooltipTitle:'Base de prestadores',tooltipMain:this.providers.length+' cadastro(s)',tooltipTag:'Cadastros',tooltip:['Categorias em uso: '+(activeCats.length?activeCats.join(', '):'nenhuma categoria cadastrada.'),'Use para localizar quem presta serviço recorrente ou avulso.','Dica: mantenha PIX, WhatsApp e categoria sempre atualizados.']},
    {label:'Em aberto no ano',value:this.money(open),hint:'lançamentos do ano',icon:'triangle-alert',color:'text-red-600',tooltipTitle:'Pendências do ano',tooltipMain:this.money(open),tooltipTag:'Atenção',tooltip:['Saldo atual dos lançamentos feitos no ano e ainda não quitados.','Maiores pendências: '+(openTop.length?openTop.join(' | '):'nenhuma pendência no ano.'),'Depois de pagar, registre o pagamento antes de emitir o recibo.']},
    {label:'Maior categoria',value:top?top.category:'-',hint:top?this.money(top.total):'sem dados',icon:'trophy',color:'text-yellow-600',tooltipTitle:'Categoria líder',tooltipMain:top?top.category:'Sem dados',tooltipTag:top?this.money(top.total):'Sem dados',tooltip:['Categoria com maior volume pago no ano.','Ranking: '+(catTop.length?catTop.join(' | '):'sem categorias pagas no ano.'),'Ajuda a identificar onde o condomínio mais gastou.']}
  ]},
  get annualTotals(){let paid=this.yearlyPaymentRows.reduce((a,r)=>a+Number(r.total||0),0),open=this.yearlyRows.reduce((a,s)=>a+this.balance(s),0),count=this.yearlyPaymentRows.length;return{paid,open,count}},
  get monthlyHistory(){let labels=['Janeiro','Fevereiro','Março','Abril','Maio','Junho','Julho','Agosto','Setembro','Outubro','Novembro','Dezembro'];return labels.map((label,i)=>{let key=String(this.dashboardYear)+'-'+String(i+1).padStart(2,'0'),payments=this.yearlyPaymentRows.filter(r=>String(r.paymentDate||'').slice(0,7)===key),services=this.yearlyRows.filter(s=>String(s.serviceDate||'').slice(0,7)===key),paid=payments.reduce((a,r)=>a+Number(r.total||0),0),open=services.reduce((a,s)=>a+this.balance(s),0),map={};payments.forEach(r=>{map[r.category]=(map[r.category]||0)+Number(r.total||0)});let top=Object.entries(map).sort((a,b)=>b[1]-a[1])[0];return{key,label,count:payments.length,paid,open,topCategory:top?top[0]:'-'}})},
  get annualTopProviders(){let map={};this.yearlyPaymentRows.forEach(r=>{let id=r.providerId,p=this.providers.find(x=>Number(x.id)===Number(id))||{};if(!map[id])map[id]={id,name:p.name||'-',category:p.category||r.category,count:0,total:0,open:0};map[id].count++;map[id].total+=Number(r.total||0)});this.yearlyRows.forEach(s=>{let id=s.providerId,p=this.providers.find(x=>Number(x.id)===Number(id))||{};if(!map[id])map[id]={id,name:p.name||'-',category:p.category||s.category,count:0,total:0,open:0};map[id].open+=this.balance(s)});return Object.values(map).filter(x=>x.total>0||x.open>0).sort((a,b)=>b.total-a.total).slice(0,10)},
  get annualCategorySummary(){let rows=this.yearlyPaymentRows,total=rows.reduce((a,r)=>a+Number(r.total||0),0)||1;return this.categories.map(category=>{let val=rows.filter(r=>r.category===category).reduce((a,r)=>a+Number(r.total||0),0);retu
--- occurrence 4 at 6470820 ---
Id))||null:null;return{...p,service,receipt,providerId:p.providerId??service?.providerId,paymentDate:p.date,paymentMethod:p.method,paidValue:Number(p.amount||0)}})},
  get servicePanelRows(){return this.services.filter(s=>s.reference===this.servicePanelMonth).sort((a,b)=>String(b.paymentDate||b.serviceDate).localeCompare(String(a.paymentDate||a.serviceDate)))},
  get servicePanelPaidRows(){return this.servicePanelRows.filter(s=>Number(s.paidValue||0)>0).sort((a,b)=>String(b.paymentDate||b.serviceDate).localeCompare(String(a.paymentDate||a.serviceDate)))},
  get servicePanelPaymentEvents(){let ref=this.servicePanelMonth;return this.payments.map(p=>{let service=this.services.find(s=>Number(s.id)===Number(p.serviceId))||null,receipt=p.receiptId?this.receipts.find(r=>Number(r.id)===Number(p.receiptId)):null;return{...p,paymentDate:p.date||'',paymentMethod:p.method||'',paidValue:Number(p.amount||0),service,receipt}}).filter(p=>String(p.date||'').slice(0,7)===ref).sort((a,b)=>String(b.date||'').localeCompare(String(a.date||''))||Number(b.id||0)-Number(a.id||0))},
  get servicePanelOpenRows(){return this.servicePanelRows.filter(s=>this.balance(s)>0).sort((a,b)=>String(a.dueDate||'2999-12-31').localeCompare(String(b.dueDate||'2999-12-31')))},
  get servicePanelTotals(){let expected=this.servicePanelRows.reduce((a,s)=>a+Number(s.amount||0),0),paid=this.servicePanelRows.reduce((a,s)=>a+Number(s.paidValue||0),0);return{expected,paid,open:Math.max(0,expected-paid)}},
  get servicePanelRecurringGenerated(){return this.recurrences.filter(r=>r.active&&this.recurrenceGenerated(r,this.servicePanelMonth))},
  get servicePanelPendingRecurrences(){return this.recurrences.filter(r=>r.active&&!this.recurrenceGenerated(r,this.servicePanelMonth))},
  get servicePanelReceiptCount(){return this.receipts.filter(r=>String(r.paymentDate||r.date||'').slice(0,7)===this.servicePanelMonth).length},
  get filteredReceipts(){let q=String(this.receiptFilter.q||'').toLowerCase().trim();return this.receipts.filter(r=>{let txt=[r.number,r.providerName,r.category,r.description,r.paymentMethod,r.total].join(' ').toLowerCase();return(!q||txt.includes(q))&&(!this.receiptFilter.month||r.reference===this.receiptFilter.month)&&(this.receiptFilter.category==='todas'||r.category===this.receiptFilter.category)}).sort((a,b)=>String(b.date||'').localeCompare(String(a.date||''))||String(b.number||'').localeCompare(String(a.number||'')))},
  get reportChartYears(){let ys=this.payments.map(p=>Number(String(p.date||'').slice(0,4))).filter(Boolean);ys.push(Number(this.reportChartYear||new Date().getFullYear()));return[...new Set(ys)].sort((a,b)=>b-a)},get reportRows(){return this.services.filter(s=>(!this.report.from||s.serviceDate>=this.report.from)&&(!this.report.to||s.serviceDate<=this.report.to)&&(this.report.category==='todas'||s.category===this.report.category)&&(this.report.status==='todos'||s.status===this.report.status)).sort((a,b)=>{let nums=x=>this.receipts.filter(r=>Number(r.serviceId)===Number(x.id)).map(r=>Number(String(r.number||'').replace(/\D/g,''))||0).filter(Boolean),aa=nums(a),bb=nums(b),na=aa.length?Math.min(...aa):0,nb=bb.length?Math.min(...bb):0;if(na&&nb&&na!==nb)return na-nb;if(na&&!nb)return-1;if(!na&&nb)return 1;return String(a.serviceDate||'').localeCompare(String(b.serviceDate||''))||Number(a.id||0)-Number(b.id||0)})},
  get reportDetailRows(){let rows=[],inPeriod=d=>(!this.report.from||String(d||'')>=this.report.from)&&(!this.report.to||String(d||'')<=this.report.to),services=this.services.filter(s=>(this.report.category==='todas'||s.category===this.report.category)&&(this.report.status==='todos'||s.status===this.report.status));for(const s of services){let ps=this.payments.filter(p=>Number(p.serviceId)===Number(s.id)).sort((a,b)=>String(a.date||'').localeCompare(String(b.date||''))||Number(a.id||0)-Number(b.id||0));if(ps.length){for(const p of ps){if(!inPeriod(p.date))continue;let r=p.receiptId?this.receipts.find(x=>Number(x.id)===Number(p.receiptId)):null;rows.push({...s,reportKey:'p-'+p.id,receiptNumber:r?.number||'PENDENTE',paymentDate:p.date||'',description:r?.description||s.description,reportValue:Number(p.amount||0),paymentMethod:p.method||r?.paymentMethod||s.paymentMethod||''})}}else if(inPeriod(s.serviceDate))rows.push({...s,reportKey:'s-'+s.id,receiptNumber:'-',paymentDate:'',description:s.description,reportValue:0,paymentMethod:''})}return rows.sort((a,b)=>String(a.paymentDate||a.serviceDate||'').localeCompare(String(b.paymentDate||b.serviceDate||''))||String(a.receiptNumber||'').localeCompare(String(b.receiptNumber||'')))},get reportTotals(){let e=this.reportRows.reduce((a,s)=>a+Number(s.amount||0),0),p=this.reportDetailRows.reduce((a,s)=>a+Number(s.reportValue||0),0),o=this.reportRows.reduce((a,s)=>a+this.balance(s),0);return{expected:e,paid:p,open:o}},backupStats(){let records=this.providers.length+this.services.length+this.receipts.length+this.recurrences.length+this.payments.length;return{records,providers:this.providers.length,services:this.services.length,receipts:this.receipts.length,recurrences:this.recurrences.length,payments:this.payments.length}},backupDays(){if(!this.backupLast)return null;let d=new Date(this.backupLast), now=new Date();return Math.max(0,Math.floor((now-d)/86400000))},backupLastLabel(){if(!this.backupLast)return'Nunca feito';let days=this.backupDays();let date=new Date(this.backupLast).toLocaleString('pt-BR');return days===0?'Hoje • '+date:days+' dia(s) • '+date},backupHealthLabel(){let d=this.backupDays();if(d===null)return'Backup pendente';if(d<=7)return'Protegido';if(d<=15)return'Atenção';return'Backup atrasado'},backupHealthClass(){let d=this.backupDays();if(d===null||d>15)return'bg-red-500/10 text-red-700';if(d>7)return'bg-amber-500/10 text-amber-700';return'bg-emerald-500/10 text-emerald-700'},backupHint(){let s=this.backupStats();let d=this.backupDays();if(d===null)return'Você ainda não exportou um backup. Recomendo gerar um JSON antes de continuar cadastrando dados importantes.';if(d>15)return'Seu último backup já passou de 15 dias. Exporte um novo JSON para proteger '+s.records+' registros locais.';if(d>7)return'Backup existente, mas já vale gerar uma cópia atualizada com os últimos lançamentos e recibos.';return'Backup em dia. Mesmo assim, exporte uma nova 
--- occurrence 5 at 6471016 ---
n this.services.filter(s=>s.reference===this.servicePanelMonth).sort((a,b)=>String(b.paymentDate||b.serviceDate).localeCompare(String(a.paymentDate||a.serviceDate)))},
  get servicePanelPaidRows(){return this.servicePanelRows.filter(s=>Number(s.paidValue||0)>0).sort((a,b)=>String(b.paymentDate||b.serviceDate).localeCompare(String(a.paymentDate||a.serviceDate)))},
  get servicePanelPaymentEvents(){let ref=this.servicePanelMonth;return this.payments.map(p=>{let service=this.services.find(s=>Number(s.id)===Number(p.serviceId))||null,receipt=p.receiptId?this.receipts.find(r=>Number(r.id)===Number(p.receiptId)):null;return{...p,paymentDate:p.date||'',paymentMethod:p.method||'',paidValue:Number(p.amount||0),service,receipt}}).filter(p=>String(p.date||'').slice(0,7)===ref).sort((a,b)=>String(b.date||'').localeCompare(String(a.date||''))||Number(b.id||0)-Number(a.id||0))},
  get servicePanelOpenRows(){return this.servicePanelRows.filter(s=>this.balance(s)>0).sort((a,b)=>String(a.dueDate||'2999-12-31').localeCompare(String(b.dueDate||'2999-12-31')))},
  get servicePanelTotals(){let expected=this.servicePanelRows.reduce((a,s)=>a+Number(s.amount||0),0),paid=this.servicePanelRows.reduce((a,s)=>a+Number(s.paidValue||0),0);return{expected,paid,open:Math.max(0,expected-paid)}},
  get servicePanelRecurringGenerated(){return this.recurrences.filter(r=>r.active&&this.recurrenceGenerated(r,this.servicePanelMonth))},
  get servicePanelPendingRecurrences(){return this.recurrences.filter(r=>r.active&&!this.recurrenceGenerated(r,this.servicePanelMonth))},
  get servicePanelReceiptCount(){return this.receipts.filter(r=>String(r.paymentDate||r.date||'').slice(0,7)===this.servicePanelMonth).length},
  get filteredReceipts(){let q=String(this.receiptFilter.q||'').toLowerCase().trim();return this.receipts.filter(r=>{let txt=[r.number,r.providerName,r.category,r.description,r.paymentMethod,r.total].join(' ').toLowerCase();return(!q||txt.includes(q))&&(!this.receiptFilter.month||r.reference===this.receiptFilter.month)&&(this.receiptFilter.category==='todas'||r.category===this.receiptFilter.category)}).sort((a,b)=>String(b.date||'').localeCompare(String(a.date||''))||String(b.number||'').localeCompare(String(a.number||'')))},
  get reportChartYears(){let ys=this.payments.map(p=>Number(String(p.date||'').slice(0,4))).filter(Boolean);ys.push(Number(this.reportChartYear||new Date().getFullYear()));return[...new Set(ys)].sort((a,b)=>b-a)},get reportRows(){return this.services.filter(s=>(!this.report.from||s.serviceDate>=this.report.from)&&(!this.report.to||s.serviceDate<=this.report.to)&&(this.report.category==='todas'||s.category===this.report.category)&&(this.report.status==='todos'||s.status===this.report.status)).sort((a,b)=>{let nums=x=>this.receipts.filter(r=>Number(r.serviceId)===Number(x.id)).map(r=>Number(String(r.number||'').replace(/\D/g,''))||0).filter(Boolean),aa=nums(a),bb=nums(b),na=aa.length?Math.min(...aa):0,nb=bb.length?Math.min(...bb):0;if(na&&nb&&na!==nb)return na-nb;if(na&&!nb)return-1;if(!na&&nb)return 1;return String(a.serviceDate||'').localeCompare(String(b.serviceDate||''))||Number(a.id||0)-Number(b.id||0)})},
  get reportDetailRows(){let rows=[],inPeriod=d=>(!this.report.from||String(d||'')>=this.report.from)&&(!this.report.to||String(d||'')<=this.report.to),services=this.services.filter(s=>(this.report.category==='todas'||s.category===this.report.category)&&(this.report.status==='todos'||s.status===this.report.status));for(const s of services){let ps=this.payments.filter(p=>Number(p.serviceId)===Number(s.id)).sort((a,b)=>String(a.date||'').localeCompare(String(b.date||''))||Number(a.id||0)-Number(b.id||0));if(ps.length){for(const p of ps){if(!inPeriod(p.date))continue;let r=p.receiptId?this.receipts.find(x=>Number(x.id)===Number(p.receiptId)):null;rows.push({...s,reportKey:'p-'+p.id,receiptNumber:r?.number||'PENDENTE',paymentDate:p.date||'',description:r?.description||s.description,reportValue:Number(p.amount||0),paymentMethod:p.method||r?.paymentMethod||s.paymentMethod||''})}}else if(inPeriod(s.serviceDate))rows.push({...s,reportKey:'s-'+s.id,receiptNumber:'-',paymentDate:'',description:s.description,reportValue:0,paymentMethod:''})}return rows.sort((a,b)=>String(a.paymentDate||a.serviceDate||'').localeCompare(String(b.paymentDate||b.serviceDate||''))||String(a.receiptNumber||'').localeCompare(String(b.receiptNumber||'')))},get reportTotals(){let e=this.reportRows.reduce((a,s)=>a+Number(s.amount||0),0),p=this.reportDetailRows.reduce((a,s)=>a+Number(s.reportValue||0),0),o=this.reportRows.reduce((a,s)=>a+this.balance(s),0);return{expected:e,paid:p,open:o}},backupStats(){let records=this.providers.length+this.services.length+this.receipts.length+this.recurrences.length+this.payments.length;return{records,providers:this.providers.length,services:this.services.length,receipts:this.receipts.length,recurrences:this.recurrences.length,payments:this.payments.length}},backupDays(){if(!this.backupLast)return null;let d=new Date(this.backupLast), now=new Date();return Math.max(0,Math.floor((now-d)/86400000))},backupLastLabel(){if(!this.backupLast)return'Nunca feito';let days=this.backupDays();let date=new Date(this.backupLast).toLocaleString('pt-BR');return days===0?'Hoje • '+date:days+' dia(s) • '+date},backupHealthLabel(){let d=this.backupDays();if(d===null)return'Backup pendente';if(d<=7)return'Protegido';if(d<=15)return'Atenção';return'Backup atrasado'},backupHealthClass(){let d=this.backupDays();if(d===null||d>15)return'bg-red-500/10 text-red-700';if(d>7)return'bg-amber-500/10 text-amber-700';return'bg-emerald-500/10 text-emerald-700'},backupHint(){let s=this.backupStats();let d=this.backupDays();if(d===null)return'Você ainda não exportou um backup. Recomendo gerar um JSON antes de continuar cadastrando dados importantes.';if(d>15)return'Seu último backup já passou de 15 dias. Exporte um novo JSON para proteger '+s.records+' registros locais.';if(d>7)return'Backup existente, mas já vale gerar uma cópia atualizada com os últimos lançamentos e recibos.';return'Backup em dia. Mesmo assim, exporte uma nova cópia sempre antes de grandes alterações.'},
  get topProviders(){let y=new Date().getFullYear(), map={};this.services.filter(s=>String(s.serviceDate||'').startsWith(String(y))).forEach(s=>{let id
--- occurrence 6 at 6471978 ---
0).sort((a,b)=>String(a.dueDate||'2999-12-31').localeCompare(String(b.dueDate||'2999-12-31')))},
  get servicePanelTotals(){let expected=this.servicePanelRows.reduce((a,s)=>a+Number(s.amount||0),0),paid=this.servicePanelRows.reduce((a,s)=>a+Number(s.paidValue||0),0);return{expected,paid,open:Math.max(0,expected-paid)}},
  get servicePanelRecurringGenerated(){return this.recurrences.filter(r=>r.active&&this.recurrenceGenerated(r,this.servicePanelMonth))},
  get servicePanelPendingRecurrences(){return this.recurrences.filter(r=>r.active&&!this.recurrenceGenerated(r,this.servicePanelMonth))},
  get servicePanelReceiptCount(){return this.receipts.filter(r=>String(r.paymentDate||r.date||'').slice(0,7)===this.servicePanelMonth).length},
  get filteredReceipts(){let q=String(this.receiptFilter.q||'').toLowerCase().trim();return this.receipts.filter(r=>{let txt=[r.number,r.providerName,r.category,r.description,r.paymentMethod,r.total].join(' ').toLowerCase();return(!q||txt.includes(q))&&(!this.receiptFilter.month||r.reference===this.receiptFilter.month)&&(this.receiptFilter.category==='todas'||r.category===this.receiptFilter.category)}).sort((a,b)=>String(b.date||'').localeCompare(String(a.date||''))||String(b.number||'').localeCompare(String(a.number||'')))},
  get reportChartYears(){let ys=this.payments.map(p=>Number(String(p.date||'').slice(0,4))).filter(Boolean);ys.push(Number(this.reportChartYear||new Date().getFullYear()));return[...new Set(ys)].sort((a,b)=>b-a)},get reportRows(){return this.services.filter(s=>(!this.report.from||s.serviceDate>=this.report.from)&&(!this.report.to||s.serviceDate<=this.report.to)&&(this.report.category==='todas'||s.category===this.report.category)&&(this.report.status==='todos'||s.status===this.report.status)).sort((a,b)=>{let nums=x=>this.receipts.filter(r=>Number(r.serviceId)===Number(x.id)).map(r=>Number(String(r.number||'').replace(/\D/g,''))||0).filter(Boolean),aa=nums(a),bb=nums(b),na=aa.length?Math.min(...aa):0,nb=bb.length?Math.min(...bb):0;if(na&&nb&&na!==nb)return na-nb;if(na&&!nb)return-1;if(!na&&nb)return 1;return String(a.serviceDate||'').localeCompare(String(b.serviceDate||''))||Number(a.id||0)-Number(b.id||0)})},
  get reportDetailRows(){let rows=[],inPeriod=d=>(!this.report.from||String(d||'')>=this.report.from)&&(!this.report.to||String(d||'')<=this.report.to),services=this.services.filter(s=>(this.report.category==='todas'||s.category===this.report.category)&&(this.report.status==='todos'||s.status===this.report.status));for(const s of services){let ps=this.payments.filter(p=>Number(p.serviceId)===Number(s.id)).sort((a,b)=>String(a.date||'').localeCompare(String(b.date||''))||Number(a.id||0)-Number(b.id||0));if(ps.length){for(const p of ps){if(!inPeriod(p.date))continue;let r=p.receiptId?this.receipts.find(x=>Number(x.id)===Number(p.receiptId)):null;rows.push({...s,reportKey:'p-'+p.id,receiptNumber:r?.number||'PENDENTE',paymentDate:p.date||'',description:r?.description||s.description,reportValue:Number(p.amount||0),paymentMethod:p.method||r?.paymentMethod||s.paymentMethod||''})}}else if(inPeriod(s.serviceDate))rows.push({...s,reportKey:'s-'+s.id,receiptNumber:'-',paymentDate:'',description:s.description,reportValue:0,paymentMethod:''})}return rows.sort((a,b)=>String(a.paymentDate||a.serviceDate||'').localeCompare(String(b.paymentDate||b.serviceDate||''))||String(a.receiptNumber||'').localeCompare(String(b.receiptNumber||'')))},get reportTotals(){let e=this.reportRows.reduce((a,s)=>a+Number(s.amount||0),0),p=this.reportDetailRows.reduce((a,s)=>a+Number(s.reportValue||0),0),o=this.reportRows.reduce((a,s)=>a+this.balance(s),0);return{expected:e,paid:p,open:o}},backupStats(){let records=this.providers.length+this.services.length+this.receipts.length+this.recurrences.length+this.payments.length;return{records,providers:this.providers.length,services:this.services.length,receipts:this.receipts.length,recurrences:this.recurrences.length,payments:this.payments.length}},backupDays(){if(!this.backupLast)return null;let d=new Date(this.backupLast), now=new Date();return Math.max(0,Math.floor((now-d)/86400000))},backupLastLabel(){if(!this.backupLast)return'Nunca feito';let days=this.backupDays();let date=new Date(this.backupLast).toLocaleString('pt-BR');return days===0?'Hoje • '+date:days+' dia(s) • '+date},backupHealthLabel(){let d=this.backupDays();if(d===null)return'Backup pendente';if(d<=7)return'Protegido';if(d<=15)return'Atenção';return'Backup atrasado'},backupHealthClass(){let d=this.backupDays();if(d===null||d>15)return'bg-red-500/10 text-red-700';if(d>7)return'bg-amber-500/10 text-amber-700';return'bg-emerald-500/10 text-emerald-700'},backupHint(){let s=this.backupStats();let d=this.backupDays();if(d===null)return'Você ainda não exportou um backup. Recomendo gerar um JSON antes de continuar cadastrando dados importantes.';if(d>15)return'Seu último backup já passou de 15 dias. Exporte um novo JSON para proteger '+s.records+' registros locais.';if(d>7)return'Backup existente, mas já vale gerar uma cópia atualizada com os últimos lançamentos e recibos.';return'Backup em dia. Mesmo assim, exporte uma nova cópia sempre antes de grandes alterações.'},
  get topProviders(){let y=new Date().getFullYear(), map={};this.services.filter(s=>String(s.serviceDate||'').startsWith(String(y))).forEach(s=>{let id=s.providerId,p=this.providers.find(x=>Number(x.id)===Number(id))||{};if(!map[id])map[id]={id,name:p.name||'-',category:p.category||s.category,count:0,total:0,open:0};map[id].count++;map[id].total+=Number(s.paidValue||s.amount||0);map[id].open+=this.balance(s)});return Object.values(map).sort((a,b)=>b.total-a.total).slice(0,8)},
  get categorySummary(){let total=this.services.reduce((a,s)=>a+Number(s.paidValue||s.amount||0),0)||1;return this.categories.map(category=>{let val=this.services.filter(s=>s.category===category).reduce((a,s)=>a+Number(s.paidValue||s.amount||0),0);return{category,total:val,percent:Math.min(100,Math.round(val/total*100))}}).filter(x=>x.total>0).sort((a,b)=>b.total-a.total)},
  get criticalOpen(){return this.openServices.slice().sort((a,b)=>new Date(a.dueDate||'2999-12-31')-new Date(b.dueDate||'2999-12-31')).slice(0,8)},
  get notifications(){const now=this.today();let pend=this.services.filter(s=>s.status!=='Pago'&&s.dueDate
--- occurrence 7 at 6480422 ---
eLabel(s),badge:'Próximo',value:this.money(this.balance(s)),tone:'success'}));return rows},
  get dayCenterSummary(){let v=this.dayOverdue.length,h=this.dayDueToday.length,p=this.dayNextDue.length,r=this.dayPendingRecurrences.length,total=this.dayOverdue.concat(this.dayDueToday,this.dayNextDue).reduce((a,s)=>a+this.balance(s),0);if(!v&&!h&&!p&&!r)return 'Nenhuma ação urgente no momento. Sem vencimentos críticos ou recorrências pendentes para o mês.';return `${v} vencido(s), ${h} vencendo hoje, ${p} próximo(s) e ${r} recorrência(s) a gerar. Valor monitorado: ${this.money(total)}.`},
  get searchResults(){let q=this.q.toLowerCase().trim();if(!q)return[];let match=(arr)=>arr.join(' ').toLowerCase().includes(q);let a=this.providers.filter(p=>match([p.name,p.document,p.category,p.phone,p.whatsapp,p.pix,p.type,p.notes])).map(p=>({kind:'provider',type:'Prestador',id:p.id,data:p,title:p.name,subtitle:(p.category||'-')+' • '+(p.document||'sem documento'),amount:this.money(this.providerSpend(p.id)),date:'pago '+this.money(this.providerPaid(p.id))+' • em aberto '+this.money(this.providerOpen(p.id)),description:'Contato: '+(p.phone||p.whatsapp||'não informado')+' • PIX/Banco: '+(p.pix||'não informado')}));let b=this.services.filter(s=>match([this.providerName(s.providerId),s.category,s.description,s.location,s.status,s.paymentMethod,s.reference,this.month(s.reference),this.fmt(s.serviceDate),this.fmt(s.paymentDate)])).map(s=>({kind:'service',type:'Serviço',id:s.id,data:s,title:this.providerName(s.providerId),subtitle:(s.category||'-')+' • '+this.month(s.reference)+' • '+(s.status||'-'),amount:this.money(s.amount||0),date:s.paymentDate?this.fmt(s.paymentDate):this.fmt(s.serviceDate),description:(s.description||'Sem descrição')+' • '+(s.location||'sem local informado')}));let c=this.receipts.filter(r=>match([r.number,r.providerName,r.category,r.description,r.paymentMethod,r.reference,this.month(r.reference),this.fmt(r.date),this.fmt(r.paymentDate),r.total])).map(r=>({kind:'receipt',type:'Recibo',id:r.id,data:r,title:'Recibo nº '+(r.number||'-'),subtitle:(r.providerName||'-')+' • '+(r.category||'-'),amount:this.money(r.total||0),date:this.fmt(r.paymentDate||r.date),description:(r.description||'Sem descrição')+' • Forma: '+(r.paymentMethod||'não informada')}));let d=(this.recurrences||[]).filter(r=>match([this.providerName(r.providerId),r.category,r.description,r.location,r.paymentMethod,r.amount,r.active?'ativa':'inativa'])).map(r=>({kind:'recurrence',type:'Recorrência',id:r.id,data:r,title:this.providerName(r.providerId),subtitle:(r.category||'-')+' • '+(r.active?'ativa':'inativa'),amount:this.money(r.amount||0),date:'todo mês dia '+(r.dueDay||'-'),description:(r.description||'Serviço recorrente mensal')+' • Forma padrão: '+(r.paymentMethod||'não informada')}));return[...c,...a,...b,...d].slice(0,40)},
  toastType(msg){let s=String(msg||'').toLowerCase();if(s.includes('inválida')||s.includes('não consegui')||s.includes('erro')||s.includes('falha'))return'error';if(s.includes('já ')||s.includes('inativa')||s.includes('atenção'))return'warning';return'success'},toast(message,type='success'){let cfg={success:{title:'Tudo certo',icon:'check-circle-2',bar:'bg-emerald-600',border:'border-emerald-500/25',iconBg:'bg-emerald-600'},error:{title:'Atenção',icon:'triangle-alert',bar:'bg-red-600',border:'border-red-500/25',iconBg:'bg-red-600'},warning:{title:'Aviso',icon:'alert-circle',bar:'bg-amber-500',border:'border-amber-500/30',iconBg:'bg-amber-500'},info:{title:'Informação',icon:'info',bar:'bg-rc-800',border:'border-rc-800/25',iconBg:'bg-gradient-to-br from-rc-800 to-rc-500'}}[type]||{};let id=Date.now()+Math.random();this.toasts.push({id,message,type,...cfg});this.refreshIcons();setTimeout(()=>this.dismissToast(id),4200)},dismissToast(id){this.toasts=this.toasts.filter(t=>t.id!==id)},doLogin(){let p=JSON.parse(localStorage.getItem('rc_passwords')||'{"admin":"123456","viewer":"123456"}');if(this.login.password===p[this.login.role]){this.auth={logged:true,role:this.login.role};this.login.password='';this.load();this.after()}else this.toast('Senha inválida','error')},logout(){this.auth={logged:false,role:'viewer'}},go(v){this.view=v;this.after()},toggleDark(){this.dark=!this.dark;localStorage.setItem('rc_dark',this.dark?'1':'0');document.documentElement.classList.toggle('dark',this.dark)},
  providerAction(action,p){if(!action||!p)return;if(action==='painel')this.selectProvider(p);else if(action==='editar')this.editProvider(p);else if(action==='lancar'){if(!this.isViewer)this.fillServiceFromProvider(p)}else if(action==='whatsapp')this.openWhatsapp(p);else if(action==='excluir'){if(!this.isViewer)this.openDeleteProviderModal(p)}},serviceAction(action,s){if(!action||!s)return;if(action==='pagar'){if(!this.isViewer)this.pay(s)}else if(action==='recibo'){if(!this.isViewer&&Number(s.paidValue||0)>0)this.quickReceipt(s)}else if(action==='duplicar'){if(!this.isViewer)this.openDuplicateModal(s)}else if(action==='excluir'){if(!this.isViewer)this.openDeleteServiceModal(s)}},paymentAction(action,s){if(!action||!s)return;if(action==='pagar'){if(!this.isViewer&&s.status!=='Pago')this.pay(s)}else if(action==='recibo'){if(!this.isViewer&&Number(s.paidValue||0)>0)this.quickReceipt(s)}else if(action==='servico')this.openSearchService(s)},openSearchProvider(p){this.selectedProviderId=p?.id||null;this.view='prestadores';this.after()},openSearchService(s){this.serviceFilter='todos';this.serviceCategoryFilter='todas';this.serviceProviderFilter=String(s.providerId||'todos');this.serviceMonthFilter=s.reference||'';this.view='servicos';this.after()},openSearchReceipt(r){this.currentReceipt=r;this.receiptFilter={q:r.number||'',month:r.reference||'',category:'todas'};this.view='recibos';this.after()},openSearchRecurrence(r){this.recurrenceForm={...r};this.view='servicos';this.after()},selectProvider(p){this.selectedProviderId=p?.id||null;this.after()},providerSortBy(field){if(this.providerSortField===field){this.providerSortDir=this.providerSortDir==='asc'?'desc':'asc';}else{this.providerSortField=field;this.providerSortDir='asc';}},resetProvider(){this.providerForm={id:null,name:'',type:'Autônomo',document:'',category:'Jardineiro',phone:'',whatsapp:'',pix:'',notes:''}},editProvider(p){this.providerForm={...p};this.view='prestador
--- occurrence 8 at 6495781 ---
:this.money(s.amount||0)}]};this.after()},openDeleteServiceModal(s){if(this.isViewer)return;this.actionModal={open:true,type:'deleteService',tone:'danger',icon:'trash-2',target:s,service:s,title:'Excluir lançamento',subtitle:'Essa ação remove apenas este lançamento da lista de serviços.',note:'Lançamentos com pagamento ou recibo vinculado não podem ser excluídos por aqui. Use o fluxo de pagamentos/recibos para preservar o histórico.',confirmLabel:'Excluir',details:[{label:'Prestador',value:this.providerName(s.providerId)},{label:'Categoria',value:s.category||'-'},{label:'Valor',value:this.money(s.amount||0)}]};this.after()},openDeleteSinglePaymentModal(p){if(this.isViewer||!p)return;let s=this.services.find(x=>Number(x.id)===Number(p.serviceId));if(!s)return this.toast('Lançamento deste pagamento não foi encontrado.','warning');let receipt=p.receiptId?this.receipts.find(r=>Number(r.id)===Number(p.receiptId)):null;this.actionModal={open:true,type:'deleteSinglePayment',tone:'danger',icon:'circle-x',target:p,service:s,title:'Excluir este pagamento',subtitle:'Remove somente esta parcela do histórico de pagamentos.',note:receipt?'Este pagamento possui o recibo nº '+(receipt.number||'-')+'. Exclua primeiro esse recibo para liberar a correção do pagamento.':'Os demais pagamentos e recibos deste lançamento serão preservados. O total pago e o status serão recalculados automaticamente.',confirmLabel:'Excluir este pagamento',details:[{label:'Prestador',value:this.providerName(p.providerId??s.providerId)},{label:'Valor',value:this.money(p.amount||0)},{label:'Data/forma',value:this.fmt(p.date)+' • '+(p.method||'não informada')},{label:'Recibo',value:receipt?('nº '+(receipt.number||'-')):'não emitido'}]};this.after()},openDeletePaymentModal(s){if(this.isViewer)return;let linked=this.receipts.filter(r=>Number(r.serviceId)===Number(s.id)),numbers=linked.map(r=>r.number).filter(Boolean),receiptText=linked.length===1?'O recibo vinculado nº '+numbers[0]+' também será removido.':linked.length>1?linked.length+' recibos vinculados também serão removidos: '+numbers.join(', ')+'.':'O pagamento será removido, mas o lançamento será mantido e voltará como Pendente.';this.actionModal={open:true,type:'deletePayment',tone:'danger',icon:'circle-x',target:s,service:s,title:'Excluir todos os pagamentos',subtitle:'Essa ação zera todo o valor pago deste lançamento.',note:linked.length?receiptText+' O lançamento será mantido e voltará como Pendente; a numeração dos recibos não será reaproveitada.':receiptText,confirmLabel:'Excluir pagamentos',details:[{label:'Prestador',value:this.providerName(s.providerId)},{label:'Valor pago',value:this.money(s.paidValue||0)},{label:'Data/forma',value:(s.paymentDate?this.fmt(s.paymentDate):'-')+' • '+(s.paymentMethod||'não informada')},{label:'Recibos vinculados',value:linked.length?(numbers.join(', ')||linked.length+' recibo(s)'):'não emitido'}]};this.after()},openDeleteReceiptModal(r){if(this.isViewer)return;this.actionModal={open:true,type:'deleteReceipt',tone:'danger',icon:'receipt-text',target:r,service:null,title:'Excluir recibo',subtitle:'Essa ação remove o recibo do histórico.',note:'A numeração já utilizada não será reaproveitada, para manter a rastreabilidade da prestação de contas.',confirmLabel:'Excluir recibo',details:[{label:'Recibo nº',value:r.number||'-'},{label:'Prestador',value:r.providerName||'-'},{label:'Valor',value:this.money(r.total||0)}]};this.after()},openClearAllModal(){if(this.isViewer)return;this.actionModal={open:true,type:'clearAll',tone:'danger',icon:'database-zap',target:null,service:null,title:'Apagar dados operacionais',subtitle:'Essa ação remove os registros operacionais salvos neste navegador.',note:'A numeração oficial dos recibos e os dados do condomínio serão preservados. Faça backup JSON antes de apagar, caso precise recuperar os registros depois.',confirmLabel:'Apagar registros',details:[{label:'Remove',value:'Prestadores, serviços, recibos, recorrências e logs'},{label:'Preserva',value:'Dados do condomínio e sequência dos recibos'}]};this.after()},closeActionModal(){this.actionModal={open:false,type:'',tone:'default',icon:'',target:null,service:null,title:'',subtitle:'',note:'',confirmLabel:'',details:[]}},async confirmActionModal(){if(this.isViewer)return;let m=this.actionModal,t=m.target||m.service;if(m.type==='duplicate'){await this.duplicateServiceInternal(m.service)}else if(m.type==='deleteService'){await this.removeServiceInternal(t.id)}else if(m.type==='deleteSinglePayment'){await this.removeSinglePaymentInternal(t.id)}else if(m.type==='deletePayment'){await this.removePaymentInternal(t.id)}else if(m.type==='deleteProvider'){await this.removeProviderInternal(t.id)}else if(m.type==='deleteReceipt'){await this.removeReceiptInternal(t.id)}else if(m.type==='deleteRecurrence'){await this.removeRecurrenceInternal(t.id)}else if(m.type==='clearAll'){await this.clearAllInternal()}this.closeActionModal()},async duplicateServiceInternal(s){let d={...s};delete d.id;delete d.recurrenceId;d.status='Pendente';d.paidValue=0;d.paymentDate='';d.paymentMethod='';d.serviceDate=this.today();d.dueDate=this.today();d.reference=this.today().slice(0,7);await db.services.add(d);await this.logAction('service','Lançamento duplicado',this.providerName(d.providerId)+' • '+this.money(d.amount||0));await this.load()},async removeServiceInternal(id){let s=this.services.find(x=>Number(x.id)===Number(id));if(!s)return;let linked=this.receipts.find(r=>Number(r.serviceId)===Number(id));if(linked||Number(s.paidValue||0)>0){let linkedCount=this.receipts.filter(r=>Number(r.serviceId)===Number(id)).length;return this.toast(linkedCount>1?'Este lançamento possui '+linkedCount+' recibos vinculados. Exclua pelo fluxo de pagamentos/recibos para preservar o histórico.':linked?'Este lançamento possui o recibo nº '+linked.number+'. Exclua pelo fluxo de pagamento/recibo para preservar o histórico.':'Este lançamento possui pagamento registrado. Exclua pelo fluxo de pagamentos para preservar o histórico.','warning')}await db.services.delete(id);await this.logAction('delete','Lançamento excluído',this.providerName(s.providerId)+' • '+this.money(s.amount||0));await this.load()},async removeSinglePaymentInternal(id){let p=this.payments.find(x=>Number(x.id)===Number(id));if(!p)return;let 
--- occurrence 9 at 6499540 ---
o serão preservados. Faça backup JSON antes de apagar, caso precise recuperar os registros depois.',confirmLabel:'Apagar registros',details:[{label:'Remove',value:'Prestadores, serviços, recibos, recorrências e logs'},{label:'Preserva',value:'Dados do condomínio e sequência dos recibos'}]};this.after()},closeActionModal(){this.actionModal={open:false,type:'',tone:'default',icon:'',target:null,service:null,title:'',subtitle:'',note:'',confirmLabel:'',details:[]}},async confirmActionModal(){if(this.isViewer)return;let m=this.actionModal,t=m.target||m.service;if(m.type==='duplicate'){await this.duplicateServiceInternal(m.service)}else if(m.type==='deleteService'){await this.removeServiceInternal(t.id)}else if(m.type==='deleteSinglePayment'){await this.removeSinglePaymentInternal(t.id)}else if(m.type==='deletePayment'){await this.removePaymentInternal(t.id)}else if(m.type==='deleteProvider'){await this.removeProviderInternal(t.id)}else if(m.type==='deleteReceipt'){await this.removeReceiptInternal(t.id)}else if(m.type==='deleteRecurrence'){await this.removeRecurrenceInternal(t.id)}else if(m.type==='clearAll'){await this.clearAllInternal()}this.closeActionModal()},async duplicateServiceInternal(s){let d={...s};delete d.id;delete d.recurrenceId;d.status='Pendente';d.paidValue=0;d.paymentDate='';d.paymentMethod='';d.serviceDate=this.today();d.dueDate=this.today();d.reference=this.today().slice(0,7);await db.services.add(d);await this.logAction('service','Lançamento duplicado',this.providerName(d.providerId)+' • '+this.money(d.amount||0));await this.load()},async removeServiceInternal(id){let s=this.services.find(x=>Number(x.id)===Number(id));if(!s)return;let linked=this.receipts.find(r=>Number(r.serviceId)===Number(id));if(linked||Number(s.paidValue||0)>0){let linkedCount=this.receipts.filter(r=>Number(r.serviceId)===Number(id)).length;return this.toast(linkedCount>1?'Este lançamento possui '+linkedCount+' recibos vinculados. Exclua pelo fluxo de pagamentos/recibos para preservar o histórico.':linked?'Este lançamento possui o recibo nº '+linked.number+'. Exclua pelo fluxo de pagamento/recibo para preservar o histórico.':'Este lançamento possui pagamento registrado. Exclua pelo fluxo de pagamentos para preservar o histórico.','warning')}await db.services.delete(id);await this.logAction('delete','Lançamento excluído',this.providerName(s.providerId)+' • '+this.money(s.amount||0));await this.load()},async removeSinglePaymentInternal(id){let p=this.payments.find(x=>Number(x.id)===Number(id));if(!p)return;let s=this.services.find(x=>Number(x.id)===Number(p.serviceId));if(!s)return this.toast('Lançamento deste pagamento não foi encontrado.','warning');if(p.receiptId){let r=this.receipts.find(x=>Number(x.id)===Number(p.receiptId));return this.toast('Este pagamento possui o recibo nº '+(r?.number||p.receiptId)+'. Exclua o recibo primeiro para preservar a rastreabilidade.','warning')}let removed=Number(p.amount||0);await db.transaction('rw',db.payments,db.services,async()=>{await db.payments.delete(p.id);let remaining=await db.payments.where('serviceId').equals(s.id).toArray(),paid=remaining.reduce((a,x)=>a+Number(x.amount||0),0),latest=remaining.slice().sort((a,b)=>String(b.date||'').localeCompare(String(a.date||''))||Number(b.id||0)-Number(a.id||0))[0]||null,status=paid<=0?'Pendente':(paid>=Number(s.amount||0)-0.000001?'Pago':'Parcial');await db.services.update(s.id,{paidValue:paid,paymentDate:latest?.date||'',paymentMethod:latest?.method||'',status})});await this.logAction('delete','Pagamento individual excluído',this.providerName(p.providerId??s.providerId)+' • '+this.money(removed)+' • '+this.fmt(p.date));await this.load();this.toast('Pagamento de '+this.money(removed)+' excluído. Os demais registros foram preservados.')},async removePaymentInternal(id){let s=this.services.find(x=>Number(x.id)===Number(id));if(!s)return;let linked=this.receipts.filter(r=>Number(r.serviceId)===Number(id));await db.transaction('rw',db.receipts,db.services,db.payments,async()=>{for(const r of linked)await db.receipts.delete(r.id);await db.payments.where('serviceId').equals(id).delete();await db.services.update(id,{paidValue:0,paymentDate:'',paymentMethod:'',status:'Pendente'})});for(const r of linked)await this.logAction('delete','Recibo removido por exclusão de pagamento','Nº '+r.number+' • '+(r.providerName||this.providerName(s.providerId)));await this.logAction('delete','Pagamentos excluídos; lançamento mantido',this.providerName(s.providerId)+' • '+this.money(s.paidValue||0));await this.load()},
  pay(s){this.paymentTarget=s;this.payment={date:this.today(),value:this.balance(s),method:'PIX'};this.payModal=true},async savePayment(){if(this.isViewer)return;let s=this.paymentTarget,value=Number(this.payment.value||0),paymentDate=this.payment.date||this.today(),method=this.payment.method||'Não informado',remaining=Math.max(0,Number(s.amount||0)-Number(s.paidValue||0));if(value<=0)return this.toast('Informe um valor de pagamento maior que zero.','warning');if(value>remaining+0.000001)return this.toast('O pagamento não pode ser maior que o saldo pendente de '+this.money(remaining)+'.','warning');let paid=Number(s.paidValue||0)+value,status=paid>=Number(s.amount)?'Pago':'Parcial';await db.transaction('rw',db.services,db.payments,async()=>{await db.payments.add({serviceId:s.id,providerId:s.providerId,date:paymentDate,amount:value,method,receiptId:null,source:'manual',createdAt:new Date().toISOString()});await db.services.update(s.id,{paidValue:paid,paymentDate,paymentMethod:method,status})});await this.logAction('payment','Pagamento registrado',this.providerName(s.providerId)+' • '+this.money(value)+' • '+method);this.payModal=false;await this.load();this.toast(status==='Pago'?'Pagamento registrado. Lançamento quitado.':'Pagamento parcial registrado. Saldo restante: '+this.money(Math.max(0,Number(s.amount||0)-paid)))},prepareReceipt(s){this.receiptServiceId=s.id;this.view='recibos';this.after()},async recentPaymentReceipt(p){if(this.isViewer||!p||!p.service)return;if(p.receipt){this.currentReceipt=p.receipt;this.view='recibos';this.after();setTimeout(()=>this.printReceipt(),220);return}this.receiptServiceId=p.service.id;await this.issueReceipt(p.id);this.view='recibos';this.after();setTimeout(()=>this.printReceipt(),2
--- occurrence 10 at 6501635 ---
fluxo de pagamento/recibo para preservar o histórico.':'Este lançamento possui pagamento registrado. Exclua pelo fluxo de pagamentos para preservar o histórico.','warning')}await db.services.delete(id);await this.logAction('delete','Lançamento excluído',this.providerName(s.providerId)+' • '+this.money(s.amount||0));await this.load()},async removeSinglePaymentInternal(id){let p=this.payments.find(x=>Number(x.id)===Number(id));if(!p)return;let s=this.services.find(x=>Number(x.id)===Number(p.serviceId));if(!s)return this.toast('Lançamento deste pagamento não foi encontrado.','warning');if(p.receiptId){let r=this.receipts.find(x=>Number(x.id)===Number(p.receiptId));return this.toast('Este pagamento possui o recibo nº '+(r?.number||p.receiptId)+'. Exclua o recibo primeiro para preservar a rastreabilidade.','warning')}let removed=Number(p.amount||0);await db.transaction('rw',db.payments,db.services,async()=>{await db.payments.delete(p.id);let remaining=await db.payments.where('serviceId').equals(s.id).toArray(),paid=remaining.reduce((a,x)=>a+Number(x.amount||0),0),latest=remaining.slice().sort((a,b)=>String(b.date||'').localeCompare(String(a.date||''))||Number(b.id||0)-Number(a.id||0))[0]||null,status=paid<=0?'Pendente':(paid>=Number(s.amount||0)-0.000001?'Pago':'Parcial');await db.services.update(s.id,{paidValue:paid,paymentDate:latest?.date||'',paymentMethod:latest?.method||'',status})});await this.logAction('delete','Pagamento individual excluído',this.providerName(p.providerId??s.providerId)+' • '+this.money(removed)+' • '+this.fmt(p.date));await this.load();this.toast('Pagamento de '+this.money(removed)+' excluído. Os demais registros foram preservados.')},async removePaymentInternal(id){let s=this.services.find(x=>Number(x.id)===Number(id));if(!s)return;let linked=this.receipts.filter(r=>Number(r.serviceId)===Number(id));await db.transaction('rw',db.receipts,db.services,db.payments,async()=>{for(const r of linked)await db.receipts.delete(r.id);await db.payments.where('serviceId').equals(id).delete();await db.services.update(id,{paidValue:0,paymentDate:'',paymentMethod:'',status:'Pendente'})});for(const r of linked)await this.logAction('delete','Recibo removido por exclusão de pagamento','Nº '+r.number+' • '+(r.providerName||this.providerName(s.providerId)));await this.logAction('delete','Pagamentos excluídos; lançamento mantido',this.providerName(s.providerId)+' • '+this.money(s.paidValue||0));await this.load()},
  pay(s){this.paymentTarget=s;this.payment={date:this.today(),value:this.balance(s),method:'PIX'};this.payModal=true},async savePayment(){if(this.isViewer)return;let s=this.paymentTarget,value=Number(this.payment.value||0),paymentDate=this.payment.date||this.today(),method=this.payment.method||'Não informado',remaining=Math.max(0,Number(s.amount||0)-Number(s.paidValue||0));if(value<=0)return this.toast('Informe um valor de pagamento maior que zero.','warning');if(value>remaining+0.000001)return this.toast('O pagamento não pode ser maior que o saldo pendente de '+this.money(remaining)+'.','warning');let paid=Number(s.paidValue||0)+value,status=paid>=Number(s.amount)?'Pago':'Parcial';await db.transaction('rw',db.services,db.payments,async()=>{await db.payments.add({serviceId:s.id,providerId:s.providerId,date:paymentDate,amount:value,method,receiptId:null,source:'manual',createdAt:new Date().toISOString()});await db.services.update(s.id,{paidValue:paid,paymentDate,paymentMethod:method,status})});await this.logAction('payment','Pagamento registrado',this.providerName(s.providerId)+' • '+this.money(value)+' • '+method);this.payModal=false;await this.load();this.toast(status==='Pago'?'Pagamento registrado. Lançamento quitado.':'Pagamento parcial registrado. Saldo restante: '+this.money(Math.max(0,Number(s.amount||0)-paid)))},prepareReceipt(s){this.receiptServiceId=s.id;this.view='recibos';this.after()},async recentPaymentReceipt(p){if(this.isViewer||!p||!p.service)return;if(p.receipt){this.currentReceipt=p.receipt;this.view='recibos';this.after();setTimeout(()=>this.printReceipt(),220);return}this.receiptServiceId=p.service.id;await this.issueReceipt(p.id);this.view='recibos';this.after();setTimeout(()=>this.printReceipt(),260)},async quickReceipt(s){if(this.isViewer)return;if(Number(s.paidValue||0)<=0)return this.toast('Registre o pagamento antes de emitir o recibo.','warning');let receiptPaymentIds=new Set(this.receipts.map(r=>Number(r.paymentId)).filter(Boolean)),candidates=this.payments.filter(p=>Number(p.serviceId)===Number(s.id)&&!p.receiptId&&!receiptPaymentIds.has(Number(p.id))).sort((a,b)=>String(a.date||'').localeCompare(String(b.date||''))||Number(a.id||0)-Number(b.id||0));if(candidates.length){this.receiptServiceId=s.id;await this.issueReceipt(candidates[0].id);this.view='recibos';this.after();setTimeout(()=>this.printReceipt(),260);return}let linked=this.receipts.filter(r=>Number(r.serviceId)===Number(s.id)),latest=linked.slice().sort((a,b)=>Number(b.id||0)-Number(a.id||0))[0];if(latest){this.currentReceipt=latest;this.view='recibos';this.after();this.toast('Todos os pagamentos registrados já possuem recibo.','warning');setTimeout(()=>this.printReceipt(),220)}else this.toast('Não há pagamento pendente de recibo.','warning')},async issueReceipt(paymentId=null){if(this.isViewer)return;let s=this.services.find(x=>Number(x.id)===Number(this.receiptServiceId));if(!s)return this.toast('Selecione um lançamento.','warning');let linked=this.receipts.filter(r=>Number(r.serviceId)===Number(s.id)),issued=linked.reduce((a,r)=>a+Number(r.total||0),0),legacyPending=Math.max(0,Number(s.paidValue||0)-issued),receiptPaymentIds=new Set(linked.map(r=>Number(r.paymentId)).filter(Boolean)),candidates=this.payments.filter(p=>Number(p.serviceId)===Number(s.id)&&!p.receiptId&&!receiptPaymentIds.has(Number(p.id))).sort((a,b)=>String(a.date||'').localeCompare(String(b.date||''))||Number(a.id||0)-Number(b.id||0)),payment=paymentId?(candidates.find(p=>Number(p.id)===Number(paymentId))||null):(candidates[0]||null);if(paymentId&&!payment)return this.toast('Este pagamento já possui recibo ou não está disponível para emissão.','warning');if(!payment&&legacyPending<=0)return this.toast('Todos os pagamentos registrados deste lançamento já possuem recibo.','warning');let receiptTotal=payment?Number(pa
--- occurrence 11 at 6504696 ---
);let paid=Number(s.paidValue||0)+value,status=paid>=Number(s.amount)?'Pago':'Parcial';await db.transaction('rw',db.services,db.payments,async()=>{await db.payments.add({serviceId:s.id,providerId:s.providerId,date:paymentDate,amount:value,method,receiptId:null,source:'manual',createdAt:new Date().toISOString()});await db.services.update(s.id,{paidValue:paid,paymentDate,paymentMethod:method,status})});await this.logAction('payment','Pagamento registrado',this.providerName(s.providerId)+' • '+this.money(value)+' • '+method);this.payModal=false;await this.load();this.toast(status==='Pago'?'Pagamento registrado. Lançamento quitado.':'Pagamento parcial registrado. Saldo restante: '+this.money(Math.max(0,Number(s.amount||0)-paid)))},prepareReceipt(s){this.receiptServiceId=s.id;this.view='recibos';this.after()},async recentPaymentReceipt(p){if(this.isViewer||!p||!p.service)return;if(p.receipt){this.currentReceipt=p.receipt;this.view='recibos';this.after();setTimeout(()=>this.printReceipt(),220);return}this.receiptServiceId=p.service.id;await this.issueReceipt(p.id);this.view='recibos';this.after();setTimeout(()=>this.printReceipt(),260)},async quickReceipt(s){if(this.isViewer)return;if(Number(s.paidValue||0)<=0)return this.toast('Registre o pagamento antes de emitir o recibo.','warning');let receiptPaymentIds=new Set(this.receipts.map(r=>Number(r.paymentId)).filter(Boolean)),candidates=this.payments.filter(p=>Number(p.serviceId)===Number(s.id)&&!p.receiptId&&!receiptPaymentIds.has(Number(p.id))).sort((a,b)=>String(a.date||'').localeCompare(String(b.date||''))||Number(a.id||0)-Number(b.id||0));if(candidates.length){this.receiptServiceId=s.id;await this.issueReceipt(candidates[0].id);this.view='recibos';this.after();setTimeout(()=>this.printReceipt(),260);return}let linked=this.receipts.filter(r=>Number(r.serviceId)===Number(s.id)),latest=linked.slice().sort((a,b)=>Number(b.id||0)-Number(a.id||0))[0];if(latest){this.currentReceipt=latest;this.view='recibos';this.after();this.toast('Todos os pagamentos registrados já possuem recibo.','warning');setTimeout(()=>this.printReceipt(),220)}else this.toast('Não há pagamento pendente de recibo.','warning')},async issueReceipt(paymentId=null){if(this.isViewer)return;let s=this.services.find(x=>Number(x.id)===Number(this.receiptServiceId));if(!s)return this.toast('Selecione um lançamento.','warning');let linked=this.receipts.filter(r=>Number(r.serviceId)===Number(s.id)),issued=linked.reduce((a,r)=>a+Number(r.total||0),0),legacyPending=Math.max(0,Number(s.paidValue||0)-issued),receiptPaymentIds=new Set(linked.map(r=>Number(r.paymentId)).filter(Boolean)),candidates=this.payments.filter(p=>Number(p.serviceId)===Number(s.id)&&!p.receiptId&&!receiptPaymentIds.has(Number(p.id))).sort((a,b)=>String(a.date||'').localeCompare(String(b.date||''))||Number(a.id||0)-Number(b.id||0)),payment=paymentId?(candidates.find(p=>Number(p.id)===Number(paymentId))||null):(candidates[0]||null);if(paymentId&&!payment)return this.toast('Este pagamento já possui recibo ou não está disponível para emissão.','warning');if(!payment&&legacyPending<=0)return this.toast('Todos os pagamentos registrados deste lançamento já possuem recibo.','warning');let receiptTotal=payment?Number(payment.amount||0):legacyPending,paymentDate=payment?.date||s.paymentDate||this.today(),paymentMethod=payment?.method||s.paymentMethod||'Não informado',desc=String(s.description||'').replace(/(\bparcela\s+)(\d+)\s*\/\s*(\d+)/i,(m,prefix,current,total)=>prefix+String(linked.length+1).padStart(current.length,'0')+'/'+total),p=this.providers.find(x=>Number(x.id)===Number(s.providerId))||{},r=null;await db.transaction('rw',db.meta,db.receipts,db.payments,async()=>{let meta=await db.meta.get('receiptSeq'),next=Math.max(Number(meta?.value||0),this.receiptMaxNumber())+1;r={number:String(next).padStart(6,'0'),date:this.today(),paymentDate,providerId:p.id,providerName:p.name||'Prestador não informado',document:p.document||'',category:s.category,reference:s.reference,description:desc,total:receiptTotal,paymentMethod,location:s.location,serviceId:s.id,paymentId:payment?.id||null,manager:this.settings.manager,condoName:this.settings.condoName,condoCnpj:this.settings.cnpj||'',condoAddress:this.settings.address||''};await db.meta.put({key:'receiptSeq',value:next});let id=await db.receipts.add(r);r.id=id;if(payment)await db.payments.update(payment.id,{receiptId:id})});this.currentReceipt=r;await this.logAction('receipt','Recibo emitido','Nº '+r.number+' • '+r.providerName+' • '+this.money(r.total||0));await this.load();this.toast('Recibo nº '+r.number+' emitido no valor de '+this.money(r.total||0)+'.')},printReceipt(){if(!this.currentReceipt&&this.receipts.length)this.currentReceipt=this.receipts[this.receipts.length-1];document.getElementById('receiptPrint').classList.remove('hidden');document.getElementById('reportPrint').classList.add('hidden');document.getElementById('annualReportPrint').classList.add('hidden');setTimeout(()=>window.print(),100)},async removeReceiptInternal(id){let r=this.receipts.find(x=>Number(x.id)===Number(id));if(!r)return;let linked=this.receipts.filter(x=>Number(x.serviceId)===Number(r.serviceId)).sort((a,b)=>Number(b.id||0)-Number(a.id||0)),latest=linked[0];if(latest&&Number(latest.id)!==Number(r.id))return this.toast('Para preservar a data e a forma de pagamento das parcelas, somente o recibo mais recente deste lançamento pode ser excluído e reemitido.','warning');await db.transaction('rw',db.receipts,db.payments,async()=>{await db.receipts.delete(id);let ps=this.payments.filter(p=>Number(p.receiptId)===Number(id));for(const p of ps)await db.payments.update(p.id,{receiptId:null})});if(this.currentReceipt&&Number(this.currentReceipt.id)===Number(id))this.currentReceipt=null;await this.logAction('delete','Recibo excluído',r?('Nº '+r.number+' • '+r.providerName):('ID '+id));await this.load()},
  printReport(){document.body.classList.add('printing-report');document.getElementById('receiptPrint').classList.add('hidden');document.getElementById('annualReportPrint').classList.add('hidden');document.getElementById('reportPrint').classList.remove('hidden');setTimeout(()=>{window.print();document.body.classList.remove('printing-report');document.getElementById('receiptPrint').classList.remove('hidden');
--- occurrence 12 at 6505285 ---
go'?'Pagamento registrado. Lançamento quitado.':'Pagamento parcial registrado. Saldo restante: '+this.money(Math.max(0,Number(s.amount||0)-paid)))},prepareReceipt(s){this.receiptServiceId=s.id;this.view='recibos';this.after()},async recentPaymentReceipt(p){if(this.isViewer||!p||!p.service)return;if(p.receipt){this.currentReceipt=p.receipt;this.view='recibos';this.after();setTimeout(()=>this.printReceipt(),220);return}this.receiptServiceId=p.service.id;await this.issueReceipt(p.id);this.view='recibos';this.after();setTimeout(()=>this.printReceipt(),260)},async quickReceipt(s){if(this.isViewer)return;if(Number(s.paidValue||0)<=0)return this.toast('Registre o pagamento antes de emitir o recibo.','warning');let receiptPaymentIds=new Set(this.receipts.map(r=>Number(r.paymentId)).filter(Boolean)),candidates=this.payments.filter(p=>Number(p.serviceId)===Number(s.id)&&!p.receiptId&&!receiptPaymentIds.has(Number(p.id))).sort((a,b)=>String(a.date||'').localeCompare(String(b.date||''))||Number(a.id||0)-Number(b.id||0));if(candidates.length){this.receiptServiceId=s.id;await this.issueReceipt(candidates[0].id);this.view='recibos';this.after();setTimeout(()=>this.printReceipt(),260);return}let linked=this.receipts.filter(r=>Number(r.serviceId)===Number(s.id)),latest=linked.slice().sort((a,b)=>Number(b.id||0)-Number(a.id||0))[0];if(latest){this.currentReceipt=latest;this.view='recibos';this.after();this.toast('Todos os pagamentos registrados já possuem recibo.','warning');setTimeout(()=>this.printReceipt(),220)}else this.toast('Não há pagamento pendente de recibo.','warning')},async issueReceipt(paymentId=null){if(this.isViewer)return;let s=this.services.find(x=>Number(x.id)===Number(this.receiptServiceId));if(!s)return this.toast('Selecione um lançamento.','warning');let linked=this.receipts.filter(r=>Number(r.serviceId)===Number(s.id)),issued=linked.reduce((a,r)=>a+Number(r.total||0),0),legacyPending=Math.max(0,Number(s.paidValue||0)-issued),receiptPaymentIds=new Set(linked.map(r=>Number(r.paymentId)).filter(Boolean)),candidates=this.payments.filter(p=>Number(p.serviceId)===Number(s.id)&&!p.receiptId&&!receiptPaymentIds.has(Number(p.id))).sort((a,b)=>String(a.date||'').localeCompare(String(b.date||''))||Number(a.id||0)-Number(b.id||0)),payment=paymentId?(candidates.find(p=>Number(p.id)===Number(paymentId))||null):(candidates[0]||null);if(paymentId&&!payment)return this.toast('Este pagamento já possui recibo ou não está disponível para emissão.','warning');if(!payment&&legacyPending<=0)return this.toast('Todos os pagamentos registrados deste lançamento já possuem recibo.','warning');let receiptTotal=payment?Number(payment.amount||0):legacyPending,paymentDate=payment?.date||s.paymentDate||this.today(),paymentMethod=payment?.method||s.paymentMethod||'Não informado',desc=String(s.description||'').replace(/(\bparcela\s+)(\d+)\s*\/\s*(\d+)/i,(m,prefix,current,total)=>prefix+String(linked.length+1).padStart(current.length,'0')+'/'+total),p=this.providers.find(x=>Number(x.id)===Number(s.providerId))||{},r=null;await db.transaction('rw',db.meta,db.receipts,db.payments,async()=>{let meta=await db.meta.get('receiptSeq'),next=Math.max(Number(meta?.value||0),this.receiptMaxNumber())+1;r={number:String(next).padStart(6,'0'),date:this.today(),paymentDate,providerId:p.id,providerName:p.name||'Prestador não informado',document:p.document||'',category:s.category,reference:s.reference,description:desc,total:receiptTotal,paymentMethod,location:s.location,serviceId:s.id,paymentId:payment?.id||null,manager:this.settings.manager,condoName:this.settings.condoName,condoCnpj:this.settings.cnpj||'',condoAddress:this.settings.address||''};await db.meta.put({key:'receiptSeq',value:next});let id=await db.receipts.add(r);r.id=id;if(payment)await db.payments.update(payment.id,{receiptId:id})});this.currentReceipt=r;await this.logAction('receipt','Recibo emitido','Nº '+r.number+' • '+r.providerName+' • '+this.money(r.total||0));await this.load();this.toast('Recibo nº '+r.number+' emitido no valor de '+this.money(r.total||0)+'.')},printReceipt(){if(!this.currentReceipt&&this.receipts.length)this.currentReceipt=this.receipts[this.receipts.length-1];document.getElementById('receiptPrint').classList.remove('hidden');document.getElementById('reportPrint').classList.add('hidden');document.getElementById('annualReportPrint').classList.add('hidden');setTimeout(()=>window.print(),100)},async removeReceiptInternal(id){let r=this.receipts.find(x=>Number(x.id)===Number(id));if(!r)return;let linked=this.receipts.filter(x=>Number(x.serviceId)===Number(r.serviceId)).sort((a,b)=>Number(b.id||0)-Number(a.id||0)),latest=linked[0];if(latest&&Number(latest.id)!==Number(r.id))return this.toast('Para preservar a data e a forma de pagamento das parcelas, somente o recibo mais recente deste lançamento pode ser excluído e reemitido.','warning');await db.transaction('rw',db.receipts,db.payments,async()=>{await db.receipts.delete(id);let ps=this.payments.filter(p=>Number(p.receiptId)===Number(id));for(const p of ps)await db.payments.update(p.id,{receiptId:null})});if(this.currentReceipt&&Number(this.currentReceipt.id)===Number(id))this.currentReceipt=null;await this.logAction('delete','Recibo excluído',r?('Nº '+r.number+' • '+r.providerName):('ID '+id));await this.load()},
  printReport(){document.body.classList.add('printing-report');document.getElementById('receiptPrint').classList.add('hidden');document.getElementById('annualReportPrint').classList.add('hidden');document.getElementById('reportPrint').classList.remove('hidden');setTimeout(()=>{window.print();document.body.classList.remove('printing-report');document.getElementById('receiptPrint').classList.remove('hidden');document.getElementById('annualReportPrint').classList.add('hidden');document.getElementById('reportPrint').classList.add('hidden')},100)},printAnnualReport(){document.body.classList.add('printing-annual');let rp=document.getElementById('receiptPrint'),mp=document.getElementById('reportPrint'),ap=document.getElementById('annualReportPrint');rp.classList.add('hidden');mp.classList.add('hidden');ap.classList.remove('hidden');ap.style.display='block';ap.style.visibility='visible';setTimeout(()=>{window.print();document.body.classList.remove('printing-annual');rp.classList.remove('hidde
--- occurrence 13 at 6508015 ---
day(),paymentMethod=payment?.method||s.paymentMethod||'Não informado',desc=String(s.description||'').replace(/(\bparcela\s+)(\d+)\s*\/\s*(\d+)/i,(m,prefix,current,total)=>prefix+String(linked.length+1).padStart(current.length,'0')+'/'+total),p=this.providers.find(x=>Number(x.id)===Number(s.providerId))||{},r=null;await db.transaction('rw',db.meta,db.receipts,db.payments,async()=>{let meta=await db.meta.get('receiptSeq'),next=Math.max(Number(meta?.value||0),this.receiptMaxNumber())+1;r={number:String(next).padStart(6,'0'),date:this.today(),paymentDate,providerId:p.id,providerName:p.name||'Prestador não informado',document:p.document||'',category:s.category,reference:s.reference,description:desc,total:receiptTotal,paymentMethod,location:s.location,serviceId:s.id,paymentId:payment?.id||null,manager:this.settings.manager,condoName:this.settings.condoName,condoCnpj:this.settings.cnpj||'',condoAddress:this.settings.address||''};await db.meta.put({key:'receiptSeq',value:next});let id=await db.receipts.add(r);r.id=id;if(payment)await db.payments.update(payment.id,{receiptId:id})});this.currentReceipt=r;await this.logAction('receipt','Recibo emitido','Nº '+r.number+' • '+r.providerName+' • '+this.money(r.total||0));await this.load();this.toast('Recibo nº '+r.number+' emitido no valor de '+this.money(r.total||0)+'.')},printReceipt(){if(!this.currentReceipt&&this.receipts.length)this.currentReceipt=this.receipts[this.receipts.length-1];document.getElementById('receiptPrint').classList.remove('hidden');document.getElementById('reportPrint').classList.add('hidden');document.getElementById('annualReportPrint').classList.add('hidden');setTimeout(()=>window.print(),100)},async removeReceiptInternal(id){let r=this.receipts.find(x=>Number(x.id)===Number(id));if(!r)return;let linked=this.receipts.filter(x=>Number(x.serviceId)===Number(r.serviceId)).sort((a,b)=>Number(b.id||0)-Number(a.id||0)),latest=linked[0];if(latest&&Number(latest.id)!==Number(r.id))return this.toast('Para preservar a data e a forma de pagamento das parcelas, somente o recibo mais recente deste lançamento pode ser excluído e reemitido.','warning');await db.transaction('rw',db.receipts,db.payments,async()=>{await db.receipts.delete(id);let ps=this.payments.filter(p=>Number(p.receiptId)===Number(id));for(const p of ps)await db.payments.update(p.id,{receiptId:null})});if(this.currentReceipt&&Number(this.currentReceipt.id)===Number(id))this.currentReceipt=null;await this.logAction('delete','Recibo excluído',r?('Nº '+r.number+' • '+r.providerName):('ID '+id));await this.load()},
  printReport(){document.body.classList.add('printing-report');document.getElementById('receiptPrint').classList.add('hidden');document.getElementById('annualReportPrint').classList.add('hidden');document.getElementById('reportPrint').classList.remove('hidden');setTimeout(()=>{window.print();document.body.classList.remove('printing-report');document.getElementById('receiptPrint').classList.remove('hidden');document.getElementById('annualReportPrint').classList.add('hidden');document.getElementById('reportPrint').classList.add('hidden')},100)},printAnnualReport(){document.body.classList.add('printing-annual');let rp=document.getElementById('receiptPrint'),mp=document.getElementById('reportPrint'),ap=document.getElementById('annualReportPrint');rp.classList.add('hidden');mp.classList.add('hidden');ap.classList.remove('hidden');ap.style.display='block';ap.style.visibility='visible';setTimeout(()=>{window.print();document.body.classList.remove('printing-annual');rp.classList.remove('hidden');ap.classList.add('hidden');ap.style.display='';ap.style.visibility='';mp.classList.add('hidden')},180)},csv(){let rows=[['Recibo nº','Data do serviço','Data do pagamento','Prestador','Categoria','Descrição','Valor do serviço','Valor deste pagamento','Status','Forma']].concat(this.reportDetailRows.map(s=>[s.receiptNumber,s.serviceDate,s.paymentDate||'',this.providerName(s.providerId),s.category,s.description,s.amount,s.reportValue||0,s.status,s.paymentMethod||'']));let csv=rows.map(r=>r.map(v=>'"'+String(v??'').replaceAll('"','""')+'"').join(';')).join('\n');this.download('relatorio-prestadores-recibocondo.csv',csv,'text/csv;charset=utf-8')},
  async saveSettings(){let plainSettings=JSON.parse(JSON.stringify(this.settings));await db.settings.put({key:'main',value:plainSettings});await this.logAction('settings','Configurações salvas','Dados do condomínio atualizados.');this.toast('Configurações salvas')},async savePasswords(){if(this.isViewer)return;let cur=JSON.parse(localStorage.getItem('rc_passwords')||'{"admin":"123456","viewer":"123456"}');if(this.passwords.admin)cur.admin=this.passwords.admin;if(this.passwords.viewer)cur.viewer=this.passwords.viewer;localStorage.setItem('rc_passwords',JSON.stringify(cur));await this.logAction('settings','Senhas atualizadas','Senhas locais do app foram atualizadas.');this.passwords={admin:'',viewer:''};this.toast('Senhas atualizadas')},async backup(){let now=new Date().toISOString();let seqMeta=await db.meta.get('receiptSeq');let data={app:'ReciboCondo',backupFormat:3,settings:this.settings,providers:this.providers,services:this.services,receipts:this.receipts,recurrences:this.recurrences,payments:this.payments,logs:this.logs,receiptSeq:Number(seqMeta?.value||this.receiptMaxNumber()||0),backupStats:this.backupStats(),exportedAt:now};this.download('backup-recibocondo-prestadores.json',JSON.stringify(data,null,2),'application/json');this.backupLast=now;localStorage.setItem('rc_last_backup',now);await this.logAction('backup','Backup exportado','Backup JSON gerado com '+data.backupStats.records+' registros.')},async restore(ev){let f=ev.target.files[0];if(!f)return;try{let data=JSON.parse(await f.text());if(!data||typeof data!=='object'||Array.isArray(data))throw new Error('Backup inválido');let known=['settings','providers','services','receipts','recurrences','payments','logs','receiptSeq','backupStats','exportedAt','backupFormat','app'].some(k=>Object.prototype.hasOwnProperty.call(data,k));if(!known)throw new Error('O arquivo não parece ser um backup do ReciboCondo.');let providers=Array.isArray(data.providers)?data.providers:[],services=Array.isArray(data.services)?data.services:[],receipts=Array.isArray(data.receipts)?data.receipts:[],recurr
--- occurrence 14 at 6518466 ---
receiptMaxNumber(){return this.receipts.reduce((max,r)=>r.source==='Excel legado'?max:Math.max(max,Number(String(r.number||'').replace(/\D/g,'')||0)),0)},nextReceiptNumber(){return Math.max(Number(this.receiptSeqValue||0),this.receiptMaxNumber())+1},receiptSequenceStatus(){return Number(this.receiptSeqValue||0)<this.receiptMaxNumber()?'Sincronizar':'Sequência OK'},async saveReceiptSequence(){if(this.isViewer)return;let target=Number(this.receiptSeqInput||0), max=this.receiptMaxNumber();if(!Number.isSafeInteger(target)||target<1)return this.toast('Informe um próximo número inteiro e válido.','warning');if(target<=max)return this.toast('O número precisa ser maior que o último emitido: '+String(max).padStart(6,'0')+'.','warning');await db.meta.put({key:'receiptSeq',value:target-1});this.receiptSeqValue=target-1;await this.logAction('settings','Sequência de recibos ajustada','Próximo recibo definido para nº '+String(target).padStart(6,'0'));this.toast('Sequência atualizada. Próximo recibo: '+String(target).padStart(6,'0'));await this.load()},
  topPaidNames(rows){let map={};rows.forEach(s=>{let v=Number(s.paidValue||0);if(v<=0)return;let name=this.providerName(s.providerId);map[name]=(map[name]||0)+v});return Object.entries(map).sort((a,b)=>b[1]-a[1]).slice(0,3).map(([name,total])=>name+' • '+this.money(total))},topOpenNames(rows){let map={};rows.forEach(s=>{let v=this.balance(s);if(v<=0)return;let name=this.providerName(s.providerId);map[name]=(map[name]||0)+v});return Object.entries(map).sort((a,b)=>b[1]-a[1]).slice(0,3).map(([name,total])=>name+' • '+this.money(total))},
  providerServices(id){return this.services.filter(s=>Number(s.providerId)===Number(id)).sort((a,b)=>String(b.serviceDate||'').localeCompare(String(a.serviceDate||'')))},providerReceipts(id){return this.receipts.filter(r=>Number(r.providerId)===Number(id)).sort((a,b)=>String(b.date||'').localeCompare(String(a.date||'')))},providerLastServiceLabel(id){let s=this.providerServices(id)[0];return s?this.fmt(s.serviceDate)+' • '+this.money(s.amount||0):'-'},async logAction(type,title,detail){try{await db.logs.add({at:new Date().toISOString(),type,title,detail,role:this.auth?.role||'sistema'});this.logs=(await db.logs.orderBy('at').reverse().limit(30).toArray())}catch(e){console.warn('logAction',e)}},logIcon(type){return {payment:'circle-dollar-sign',receipt:'receipt-text',service:'clipboard-list',provider:'users',recurrence:'repeat-2',backup:'shield-check',restore:'rotate-ccw',danger:'triangle-alert',settings:'settings',delete:'trash-2'}[type]||'history'},fmtDateTime(d){if(!d)return'-';return new Date(d).toLocaleString('pt-BR')},async clearLogs(){if(this.isViewer)return;await db.logs.clear();this.logs=[];this.after()},providerSpend(id){return this.services.filter(s=>Number(s.providerId)===Number(id)).reduce((a,s)=>a+Number(s.amount||0),0)},providerPaid(id){return this.payments.filter(p=>Number(p.providerId)===Number(id)).reduce((a,p)=>a+Number(p.amount||0),0)},providerOpen(id){return this.services.filter(s=>Number(s.providerId)===Number(id)).reduce((a,s)=>a+this.balance(s),0)},openWhatsapp(p){let raw=String(p.whatsapp||p.phone||'').replace(/\D/g,'');if(!raw)return this.toast('Este prestador não tem WhatsApp/telefone cadastrado.','warning');let phone=raw.startsWith('55')?raw:'55'+raw;let msg=encodeURIComponent('Olá, '+p.name+'. Aqui é do '+this.settings.condoName+'.');window.open('https://wa.me/'+phone+'?text='+msg,'_blank')},overdueLabel(s){if(!s.dueDate||this.balance(s)<=0)return'';let today=new Date(this.today()+'T00:00:00'), due=new Date(s.dueDate+'T00:00:00'), days=Math.ceil((due-today)/86400000);if(days<0)return Math.abs(days)+' dia(s) em atraso';if(days===0)return'vence hoje';return'faltam '+days+' dia(s)'},providerName(id){return (this.providers.find(p=>Number(p.id)===Number(id))||{}).name||'-'},receiptPending(service){if(!service)return false;let linked=this.receipts.filter(r=>Number(r.serviceId)===Number(service.id)),receiptPaymentIds=new Set(linked.map(r=>Number(r.paymentId)).filter(Boolean)),candidates=this.payments.filter(p=>Number(p.serviceId)===Number(service.id)&&!p.receiptId&&!receiptPaymentIds.has(Number(p.id)));if(candidates.length)return true;let issued=linked.reduce((a,r)=>a+Number(r.total||0),0),servicePayments=this.payments.filter(p=>Number(p.serviceId)===Number(service.id));return !servicePayments.length&&Number(service.paidValue||0)-issued>0.000001},receiptActionLabel(service){if(this.receiptPending(service))return'Emitir recibo';let n=this.receipts.filter(r=>Number(r.serviceId)===Number(service?.id)).length;return n>1?'Ver recibos':n===1?'Ver recibo':'Emitir recibo'},reportReceiptNumber(service){let rs=this.receipts.filter(x=>Number(x.serviceId)===Number(service.id)).sort((a,b)=>Number(a.id||0)-Number(b.id||0));return rs.length?rs.map(r=>r.number).filter(Boolean).join(', '):'-';},balance(s){return Math.max(0,Number(s.amount||0)-Number(s.paidValue||0))},statusClass(st){return st==='Pago'?'bg-emerald-500/10 text-emerald-700':st==='Parcial'?'bg-amber-500/10 text-amber-700':'bg-red-500/10 text-red-700'},valorPorExtenso(v){v=Number(v||0);let reais=Math.floor(v),cent=Math.round((v-reais)*100);let base={0:'zero',1:'um',2:'dois',3:'três',4:'quatro',5:'cinco',6:'seis',7:'sete',8:'oito',9:'nove',10:'dez',11:'onze',12:'doze',13:'treze',14:'quatorze',15:'quinze',16:'dezesseis',17:'dezessete',18:'dezoito',19:'dezenove',20:'vinte',30:'trinta',40:'quarenta',50:'cinquenta',60:'sessenta',70:'setenta',80:'oitenta',90:'noventa',100:'cem'};let ext=(n)=>{if(n<=20||base[n])return base[n];if(n<100){let d=Math.floor(n/10)*10,u=n%10;return base[d]+(u?' e '+base[u]:'')}if(n<1000){let c=Math.floor(n/100),r=n%100,cn=c===1?'cento':base[c]+' centos';cn=cn.replace('dois centos','duzentos').replace('três centos','trezentos').replace('quatro centos','quatrocentos').replace('cinco centos','quinhentos').replace('seis centos','seiscentos').replace('sete centos','setecentos').replace('oito centos','oitocentos').replace('nove centos','novecentos');return r?cn+' e '+ext(r):cn}if(n<1000000){let m=Math.floor(n/1000),r=n%1000;return (m===1?'mil':ext(m)+' mil')+(r?' e '+ext(r):'')}return n.toLocaleString('pt-BR')};let txt=ext(reais)+' '+(reais===1?'real':'reais');if(cent)txt+=' e '+ext(cent)+' '+(cent===1?'centavo':'centavos');return txt;}
--- occurrence 15 at 6520564 ---
db.logs.add({at:new Date().toISOString(),type,title,detail,role:this.auth?.role||'sistema'});this.logs=(await db.logs.orderBy('at').reverse().limit(30).toArray())}catch(e){console.warn('logAction',e)}},logIcon(type){return {payment:'circle-dollar-sign',receipt:'receipt-text',service:'clipboard-list',provider:'users',recurrence:'repeat-2',backup:'shield-check',restore:'rotate-ccw',danger:'triangle-alert',settings:'settings',delete:'trash-2'}[type]||'history'},fmtDateTime(d){if(!d)return'-';return new Date(d).toLocaleString('pt-BR')},async clearLogs(){if(this.isViewer)return;await db.logs.clear();this.logs=[];this.after()},providerSpend(id){return this.services.filter(s=>Number(s.providerId)===Number(id)).reduce((a,s)=>a+Number(s.amount||0),0)},providerPaid(id){return this.payments.filter(p=>Number(p.providerId)===Number(id)).reduce((a,p)=>a+Number(p.amount||0),0)},providerOpen(id){return this.services.filter(s=>Number(s.providerId)===Number(id)).reduce((a,s)=>a+this.balance(s),0)},openWhatsapp(p){let raw=String(p.whatsapp||p.phone||'').replace(/\D/g,'');if(!raw)return this.toast('Este prestador não tem WhatsApp/telefone cadastrado.','warning');let phone=raw.startsWith('55')?raw:'55'+raw;let msg=encodeURIComponent('Olá, '+p.name+'. Aqui é do '+this.settings.condoName+'.');window.open('https://wa.me/'+phone+'?text='+msg,'_blank')},overdueLabel(s){if(!s.dueDate||this.balance(s)<=0)return'';let today=new Date(this.today()+'T00:00:00'), due=new Date(s.dueDate+'T00:00:00'), days=Math.ceil((due-today)/86400000);if(days<0)return Math.abs(days)+' dia(s) em atraso';if(days===0)return'vence hoje';return'faltam '+days+' dia(s)'},providerName(id){return (this.providers.find(p=>Number(p.id)===Number(id))||{}).name||'-'},receiptPending(service){if(!service)return false;let linked=this.receipts.filter(r=>Number(r.serviceId)===Number(service.id)),receiptPaymentIds=new Set(linked.map(r=>Number(r.paymentId)).filter(Boolean)),candidates=this.payments.filter(p=>Number(p.serviceId)===Number(service.id)&&!p.receiptId&&!receiptPaymentIds.has(Number(p.id)));if(candidates.length)return true;let issued=linked.reduce((a,r)=>a+Number(r.total||0),0),servicePayments=this.payments.filter(p=>Number(p.serviceId)===Number(service.id));return !servicePayments.length&&Number(service.paidValue||0)-issued>0.000001},receiptActionLabel(service){if(this.receiptPending(service))return'Emitir recibo';let n=this.receipts.filter(r=>Number(r.serviceId)===Number(service?.id)).length;return n>1?'Ver recibos':n===1?'Ver recibo':'Emitir recibo'},reportReceiptNumber(service){let rs=this.receipts.filter(x=>Number(x.serviceId)===Number(service.id)).sort((a,b)=>Number(a.id||0)-Number(b.id||0));return rs.length?rs.map(r=>r.number).filter(Boolean).join(', '):'-';},balance(s){return Math.max(0,Number(s.amount||0)-Number(s.paidValue||0))},statusClass(st){return st==='Pago'?'bg-emerald-500/10 text-emerald-700':st==='Parcial'?'bg-amber-500/10 text-amber-700':'bg-red-500/10 text-red-700'},valorPorExtenso(v){v=Number(v||0);let reais=Math.floor(v),cent=Math.round((v-reais)*100);let base={0:'zero',1:'um',2:'dois',3:'três',4:'quatro',5:'cinco',6:'seis',7:'sete',8:'oito',9:'nove',10:'dez',11:'onze',12:'doze',13:'treze',14:'quatorze',15:'quinze',16:'dezesseis',17:'dezessete',18:'dezoito',19:'dezenove',20:'vinte',30:'trinta',40:'quarenta',50:'cinquenta',60:'sessenta',70:'setenta',80:'oitenta',90:'noventa',100:'cem'};let ext=(n)=>{if(n<=20||base[n])return base[n];if(n<100){let d=Math.floor(n/10)*10,u=n%10;return base[d]+(u?' e '+base[u]:'')}if(n<1000){let c=Math.floor(n/100),r=n%100,cn=c===1?'cento':base[c]+' centos';cn=cn.replace('dois centos','duzentos').replace('três centos','trezentos').replace('quatro centos','quatrocentos').replace('cinco centos','quinhentos').replace('seis centos','seiscentos').replace('sete centos','setecentos').replace('oito centos','oitocentos').replace('nove centos','novecentos');return r?cn+' e '+ext(r):cn}if(n<1000000){let m=Math.floor(n/1000),r=n%1000;return (m===1?'mil':ext(m)+' mil')+(r?' e '+ext(r):'')}return n.toLocaleString('pt-BR')};let txt=ext(reais)+' '+(reais===1?'real':'reais');if(cent)txt+=' e '+ext(cent)+' '+(cent===1?'centavo':'centavos');return txt;},money(v){return Number(v||0).toLocaleString('pt-BR',{style:'currency',currency:'BRL'})},today(){let d=new Date(),y=d.getFullYear(),m=String(d.getMonth()+1).padStart(2,'0'),day=String(d.getDate()).padStart(2,'0');return y+'-'+m+'-'+day},fmt(d){if(!d)return'-';return new Date(d+'T00:00:00').toLocaleDateString('pt-BR')},month(m){if(!m)return'-';let [y,mo]=String(m).split('-');return new Date(Number(y),Number(mo)-1,1).toLocaleDateString('pt-BR',{month:'long',year:'numeric'})},reportMonthLabel(){let src=(this.reportRows[0]?.reference)||String(this.report.from||this.today()).slice(0,7);let label=this.month(src);return String(label).replace(/ de \d{4}/,'').toUpperCase();},download(n,c,t){let a=document.createElement('a');a.href=URL.createObjectURL(new Blob([c],{type:t}));a.download=n;a.click();URL.revokeObjectURL(a.href)},refreshIcons(){setTimeout(()=>window.lucide&&lucide.createIcons(),60)},after(){this.$nextTick(()=>{this.enhanceAccordions();this.refreshIcons();this.renderCharts();setTimeout(()=>this.resizeCharts(),120)})},enhanceAccordions(){try{const scopes=['prestadores','servicos','pagamentos','recibos','busca','config'];scopes.forEach(scope=>{const section=[...document.querySelectorAll('main section')].find(s=>(s.getAttribute('x-show')||'').includes(scope));if(!section)return;const panels=[...section.children].filter(el=>el.classList&&el.classList.contains('glass'));panels.forEach((panel,idx)=>{if(panel.dataset.rcAccordion==='1')return;panel.dataset.rcAccordion='1';panel.classList.add('rc-accordion-panel');const titleEl=panel.querySelector('h3,h2,h4');const title=(titleEl?.textContent||({prestadores:'Prestadores',servicos:'Serviços',pagamentos:'Pagamentos',recibos:'Recibos',busca:'Busca global',config:'Configurações'}[scope]||'Painel')).trim();const subEl=panel.querySelector('p.text-sm,p.text-xs');const sub=(subEl?.textContent||'Clique para abrir ou recolher este painel.').trim();const body=document.createElement('div');body.className='rc-accordion-body';while(panel.firstChild)body.appendChild(panel.firstChild);const head=document.createElement('button');head.t
--- occurrence 16 at 6521174 ---
s=[];this.after()},providerSpend(id){return this.services.filter(s=>Number(s.providerId)===Number(id)).reduce((a,s)=>a+Number(s.amount||0),0)},providerPaid(id){return this.payments.filter(p=>Number(p.providerId)===Number(id)).reduce((a,p)=>a+Number(p.amount||0),0)},providerOpen(id){return this.services.filter(s=>Number(s.providerId)===Number(id)).reduce((a,s)=>a+this.balance(s),0)},openWhatsapp(p){let raw=String(p.whatsapp||p.phone||'').replace(/\D/g,'');if(!raw)return this.toast('Este prestador não tem WhatsApp/telefone cadastrado.','warning');let phone=raw.startsWith('55')?raw:'55'+raw;let msg=encodeURIComponent('Olá, '+p.name+'. Aqui é do '+this.settings.condoName+'.');window.open('https://wa.me/'+phone+'?text='+msg,'_blank')},overdueLabel(s){if(!s.dueDate||this.balance(s)<=0)return'';let today=new Date(this.today()+'T00:00:00'), due=new Date(s.dueDate+'T00:00:00'), days=Math.ceil((due-today)/86400000);if(days<0)return Math.abs(days)+' dia(s) em atraso';if(days===0)return'vence hoje';return'faltam '+days+' dia(s)'},providerName(id){return (this.providers.find(p=>Number(p.id)===Number(id))||{}).name||'-'},receiptPending(service){if(!service)return false;let linked=this.receipts.filter(r=>Number(r.serviceId)===Number(service.id)),receiptPaymentIds=new Set(linked.map(r=>Number(r.paymentId)).filter(Boolean)),candidates=this.payments.filter(p=>Number(p.serviceId)===Number(service.id)&&!p.receiptId&&!receiptPaymentIds.has(Number(p.id)));if(candidates.length)return true;let issued=linked.reduce((a,r)=>a+Number(r.total||0),0),servicePayments=this.payments.filter(p=>Number(p.serviceId)===Number(service.id));return !servicePayments.length&&Number(service.paidValue||0)-issued>0.000001},receiptActionLabel(service){if(this.receiptPending(service))return'Emitir recibo';let n=this.receipts.filter(r=>Number(r.serviceId)===Number(service?.id)).length;return n>1?'Ver recibos':n===1?'Ver recibo':'Emitir recibo'},reportReceiptNumber(service){let rs=this.receipts.filter(x=>Number(x.serviceId)===Number(service.id)).sort((a,b)=>Number(a.id||0)-Number(b.id||0));return rs.length?rs.map(r=>r.number).filter(Boolean).join(', '):'-';},balance(s){return Math.max(0,Number(s.amount||0)-Number(s.paidValue||0))},statusClass(st){return st==='Pago'?'bg-emerald-500/10 text-emerald-700':st==='Parcial'?'bg-amber-500/10 text-amber-700':'bg-red-500/10 text-red-700'},valorPorExtenso(v){v=Number(v||0);let reais=Math.floor(v),cent=Math.round((v-reais)*100);let base={0:'zero',1:'um',2:'dois',3:'três',4:'quatro',5:'cinco',6:'seis',7:'sete',8:'oito',9:'nove',10:'dez',11:'onze',12:'doze',13:'treze',14:'quatorze',15:'quinze',16:'dezesseis',17:'dezessete',18:'dezoito',19:'dezenove',20:'vinte',30:'trinta',40:'quarenta',50:'cinquenta',60:'sessenta',70:'setenta',80:'oitenta',90:'noventa',100:'cem'};let ext=(n)=>{if(n<=20||base[n])return base[n];if(n<100){let d=Math.floor(n/10)*10,u=n%10;return base[d]+(u?' e '+base[u]:'')}if(n<1000){let c=Math.floor(n/100),r=n%100,cn=c===1?'cento':base[c]+' centos';cn=cn.replace('dois centos','duzentos').replace('três centos','trezentos').replace('quatro centos','quatrocentos').replace('cinco centos','quinhentos').replace('seis centos','seiscentos').replace('sete centos','setecentos').replace('oito centos','oitocentos').replace('nove centos','novecentos');return r?cn+' e '+ext(r):cn}if(n<1000000){let m=Math.floor(n/1000),r=n%1000;return (m===1?'mil':ext(m)+' mil')+(r?' e '+ext(r):'')}return n.toLocaleString('pt-BR')};let txt=ext(reais)+' '+(reais===1?'real':'reais');if(cent)txt+=' e '+ext(cent)+' '+(cent===1?'centavo':'centavos');return txt;},money(v){return Number(v||0).toLocaleString('pt-BR',{style:'currency',currency:'BRL'})},today(){let d=new Date(),y=d.getFullYear(),m=String(d.getMonth()+1).padStart(2,'0'),day=String(d.getDate()).padStart(2,'0');return y+'-'+m+'-'+day},fmt(d){if(!d)return'-';return new Date(d+'T00:00:00').toLocaleDateString('pt-BR')},month(m){if(!m)return'-';let [y,mo]=String(m).split('-');return new Date(Number(y),Number(mo)-1,1).toLocaleDateString('pt-BR',{month:'long',year:'numeric'})},reportMonthLabel(){let src=(this.reportRows[0]?.reference)||String(this.report.from||this.today()).slice(0,7);let label=this.month(src);return String(label).replace(/ de \d{4}/,'').toUpperCase();},download(n,c,t){let a=document.createElement('a');a.href=URL.createObjectURL(new Blob([c],{type:t}));a.download=n;a.click();URL.revokeObjectURL(a.href)},refreshIcons(){setTimeout(()=>window.lucide&&lucide.createIcons(),60)},after(){this.$nextTick(()=>{this.enhanceAccordions();this.refreshIcons();this.renderCharts();setTimeout(()=>this.resizeCharts(),120)})},enhanceAccordions(){try{const scopes=['prestadores','servicos','pagamentos','recibos','busca','config'];scopes.forEach(scope=>{const section=[...document.querySelectorAll('main section')].find(s=>(s.getAttribute('x-show')||'').includes(scope));if(!section)return;const panels=[...section.children].filter(el=>el.classList&&el.classList.contains('glass'));panels.forEach((panel,idx)=>{if(panel.dataset.rcAccordion==='1')return;panel.dataset.rcAccordion='1';panel.classList.add('rc-accordion-panel');const titleEl=panel.querySelector('h3,h2,h4');const title=(titleEl?.textContent||({prestadores:'Prestadores',servicos:'Serviços',pagamentos:'Pagamentos',recibos:'Recibos',busca:'Busca global',config:'Configurações'}[scope]||'Painel')).trim();const subEl=panel.querySelector('p.text-sm,p.text-xs');const sub=(subEl?.textContent||'Clique para abrir ou recolher este painel.').trim();const body=document.createElement('div');body.className='rc-accordion-body';while(panel.firstChild)body.appendChild(panel.firstChild);const head=document.createElement('button');head.type='button';head.className='rc-accordion-head no-print';head.innerHTML=`<span><span class="rc-accordion-kicker">${scope==='config'?'organização':'painel premium'}</span><span class="rc-accordion-title">${title}</span><span class="rc-accordion-sub">${sub}</span></span><span class="rc-accordion-icon"><i data-lucide="chevron-down" class="w-5 h-5"></i></span>`;head.addEventListener('click',()=>{panel.classList.toggle('rc-open');localStorage.setItem('rc_acc_'+scope+'_'+idx,panel.classList.contains('rc-open')?'1':'0');setTimeout(()=>this.resizeCharts(),90);this.refreshIcons()});panel.appendChild(head);panel.
--- occurrence 17 at 6521345 ---
.payments.filter(p=>Number(p.providerId)===Number(id)).reduce((a,p)=>a+Number(p.amount||0),0)},providerOpen(id){return this.services.filter(s=>Number(s.providerId)===Number(id)).reduce((a,s)=>a+this.balance(s),0)},openWhatsapp(p){let raw=String(p.whatsapp||p.phone||'').replace(/\D/g,'');if(!raw)return this.toast('Este prestador não tem WhatsApp/telefone cadastrado.','warning');let phone=raw.startsWith('55')?raw:'55'+raw;let msg=encodeURIComponent('Olá, '+p.name+'. Aqui é do '+this.settings.condoName+'.');window.open('https://wa.me/'+phone+'?text='+msg,'_blank')},overdueLabel(s){if(!s.dueDate||this.balance(s)<=0)return'';let today=new Date(this.today()+'T00:00:00'), due=new Date(s.dueDate+'T00:00:00'), days=Math.ceil((due-today)/86400000);if(days<0)return Math.abs(days)+' dia(s) em atraso';if(days===0)return'vence hoje';return'faltam '+days+' dia(s)'},providerName(id){return (this.providers.find(p=>Number(p.id)===Number(id))||{}).name||'-'},receiptPending(service){if(!service)return false;let linked=this.receipts.filter(r=>Number(r.serviceId)===Number(service.id)),receiptPaymentIds=new Set(linked.map(r=>Number(r.paymentId)).filter(Boolean)),candidates=this.payments.filter(p=>Number(p.serviceId)===Number(service.id)&&!p.receiptId&&!receiptPaymentIds.has(Number(p.id)));if(candidates.length)return true;let issued=linked.reduce((a,r)=>a+Number(r.total||0),0),servicePayments=this.payments.filter(p=>Number(p.serviceId)===Number(service.id));return !servicePayments.length&&Number(service.paidValue||0)-issued>0.000001},receiptActionLabel(service){if(this.receiptPending(service))return'Emitir recibo';let n=this.receipts.filter(r=>Number(r.serviceId)===Number(service?.id)).length;return n>1?'Ver recibos':n===1?'Ver recibo':'Emitir recibo'},reportReceiptNumber(service){let rs=this.receipts.filter(x=>Number(x.serviceId)===Number(service.id)).sort((a,b)=>Number(a.id||0)-Number(b.id||0));return rs.length?rs.map(r=>r.number).filter(Boolean).join(', '):'-';},balance(s){return Math.max(0,Number(s.amount||0)-Number(s.paidValue||0))},statusClass(st){return st==='Pago'?'bg-emerald-500/10 text-emerald-700':st==='Parcial'?'bg-amber-500/10 text-amber-700':'bg-red-500/10 text-red-700'},valorPorExtenso(v){v=Number(v||0);let reais=Math.floor(v),cent=Math.round((v-reais)*100);let base={0:'zero',1:'um',2:'dois',3:'três',4:'quatro',5:'cinco',6:'seis',7:'sete',8:'oito',9:'nove',10:'dez',11:'onze',12:'doze',13:'treze',14:'quatorze',15:'quinze',16:'dezesseis',17:'dezessete',18:'dezoito',19:'dezenove',20:'vinte',30:'trinta',40:'quarenta',50:'cinquenta',60:'sessenta',70:'setenta',80:'oitenta',90:'noventa',100:'cem'};let ext=(n)=>{if(n<=20||base[n])return base[n];if(n<100){let d=Math.floor(n/10)*10,u=n%10;return base[d]+(u?' e '+base[u]:'')}if(n<1000){let c=Math.floor(n/100),r=n%100,cn=c===1?'cento':base[c]+' centos';cn=cn.replace('dois centos','duzentos').replace('três centos','trezentos').replace('quatro centos','quatrocentos').replace('cinco centos','quinhentos').replace('seis centos','seiscentos').replace('sete centos','setecentos').replace('oito centos','oitocentos').replace('nove centos','novecentos');return r?cn+' e '+ext(r):cn}if(n<1000000){let m=Math.floor(n/1000),r=n%1000;return (m===1?'mil':ext(m)+' mil')+(r?' e '+ext(r):'')}return n.toLocaleString('pt-BR')};let txt=ext(reais)+' '+(reais===1?'real':'reais');if(cent)txt+=' e '+ext(cent)+' '+(cent===1?'centavo':'centavos');return txt;},money(v){return Number(v||0).toLocaleString('pt-BR',{style:'currency',currency:'BRL'})},today(){let d=new Date(),y=d.getFullYear(),m=String(d.getMonth()+1).padStart(2,'0'),day=String(d.getDate()).padStart(2,'0');return y+'-'+m+'-'+day},fmt(d){if(!d)return'-';return new Date(d+'T00:00:00').toLocaleDateString('pt-BR')},month(m){if(!m)return'-';let [y,mo]=String(m).split('-');return new Date(Number(y),Number(mo)-1,1).toLocaleDateString('pt-BR',{month:'long',year:'numeric'})},reportMonthLabel(){let src=(this.reportRows[0]?.reference)||String(this.report.from||this.today()).slice(0,7);let label=this.month(src);return String(label).replace(/ de \d{4}/,'').toUpperCase();},download(n,c,t){let a=document.createElement('a');a.href=URL.createObjectURL(new Blob([c],{type:t}));a.download=n;a.click();URL.revokeObjectURL(a.href)},refreshIcons(){setTimeout(()=>window.lucide&&lucide.createIcons(),60)},after(){this.$nextTick(()=>{this.enhanceAccordions();this.refreshIcons();this.renderCharts();setTimeout(()=>this.resizeCharts(),120)})},enhanceAccordions(){try{const scopes=['prestadores','servicos','pagamentos','recibos','busca','config'];scopes.forEach(scope=>{const section=[...document.querySelectorAll('main section')].find(s=>(s.getAttribute('x-show')||'').includes(scope));if(!section)return;const panels=[...section.children].filter(el=>el.classList&&el.classList.contains('glass'));panels.forEach((panel,idx)=>{if(panel.dataset.rcAccordion==='1')return;panel.dataset.rcAccordion='1';panel.classList.add('rc-accordion-panel');const titleEl=panel.querySelector('h3,h2,h4');const title=(titleEl?.textContent||({prestadores:'Prestadores',servicos:'Serviços',pagamentos:'Pagamentos',recibos:'Recibos',busca:'Busca global',config:'Configurações'}[scope]||'Painel')).trim();const subEl=panel.querySelector('p.text-sm,p.text-xs');const sub=(subEl?.textContent||'Clique para abrir ou recolher este painel.').trim();const body=document.createElement('div');body.className='rc-accordion-body';while(panel.firstChild)body.appendChild(panel.firstChild);const head=document.createElement('button');head.type='button';head.className='rc-accordion-head no-print';head.innerHTML=`<span><span class="rc-accordion-kicker">${scope==='config'?'organização':'painel premium'}</span><span class="rc-accordion-title">${title}</span><span class="rc-accordion-sub">${sub}</span></span><span class="rc-accordion-icon"><i data-lucide="chevron-down" class="w-5 h-5"></i></span>`;head.addEventListener('click',()=>{panel.classList.toggle('rc-open');localStorage.setItem('rc_acc_'+scope+'_'+idx,panel.classList.contains('rc-open')?'1':'0');setTimeout(()=>this.resizeCharts(),90);this.refreshIcons()});panel.appendChild(head);panel.appendChild(body);let saved=localStorage.getItem('rc_acc_'+scope+'_'+idx);if(saved!==null?saved==='1':(idx===0||scope==='busca'||scope==='prestadores'))panel.classList.add
COUNT=17
