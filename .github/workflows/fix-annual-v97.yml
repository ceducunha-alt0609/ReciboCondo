name: Corrigir relatorio anual v97
on:
  push:
    branches:
      - audit/provider-duplicates
permissions:
  contents: write
jobs:
  fix:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: python3 scripts/fix_annual_v97.py
      - run: |
          git config user.name 'ReciboCondo Guard'
          git config user.email 'actions@users.noreply.github.com'
          git add index.html sw.js
          git commit -m 'Align annual report with payment dates' || exit 0
          git push