name: Auditar exclusao individual de recibo
on:
  push:
    branches: [audit/provider-duplicates]
permissions:
  contents: write
jobs:
  inspect:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: |
          python3 - <<'PY'
          from pathlib import Path
          s=Path('index.html').read_text()
          keys=['removeReceiptInternal','deleteReceipt','db.receipts.delete','receiptSeq','issueReceipt','quickReceipt']
          out=[]
          for k in keys:
            pos=0
            while True:
              i=s.find(k,pos)
              if i<0: break
              out.append('===== '+k+' @ '+str(i)+' =====\n'+s[max(0,i-1800):i+5000])
              pos=i+len(k)
          Path('receipt-delete-inspection.txt').write_text('\n\n'.join(out))
          PY
          git config user.name 'ReciboCondo Audit'
          git config user.email 'actions@users.noreply.github.com'
          git add receipt-delete-inspection.txt
          git commit -m 'Inspect individual receipt deletion' || exit 0
          git push